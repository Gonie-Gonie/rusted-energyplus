[CmdletBinding()]
param()

$ErrorActionPreference = "Stop"
Set-StrictMode -Version Latest

Write-Host "Performance regression is not implemented before the first runnable subset."
Write-Host "This placeholder exists so release checklists can call a stable script path."
