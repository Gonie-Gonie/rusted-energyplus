[CmdletBinding()]
param()

$ErrorActionPreference = "Stop"
Set-StrictMode -Version Latest

Write-Host "Regression comparison is not implemented before the trace/compare milestone."
Write-Host "This placeholder exists so release checklists can call a stable script path."
