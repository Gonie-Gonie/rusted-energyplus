schema = "rusted-energyplus.output-requests.v1"
case_id = "official_1zone_uncontrolled_varied_timestep_family_001"
inherits = "../../output-requests.toml"
frequency = "hourly"
notes = ["Uses the shared output family to expose timestep-sensitive averaging and history regressions."]
