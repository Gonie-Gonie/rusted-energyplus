<!-- DO NOT EDIT.
     Generated from specs/ and data/conformance_cases by tools/docs/generate_docs.py. -->

# Capability Index

Capability metadata is maintained in `specs/capabilities.toml` and consumed by `ep_run` support assessment.

## Capabilities

| ID | Domain | Support level | Run state | Required objects | Forbidden active features | Algorithms | Evidence cases | Claim boundary |
|---|---|---|---|---|---|---|---|---|
| official_1zone_uncontrolled_declared_heat_balance | heat_balance | compatibility | supported_compatibility_run | `Zone`<br>`BuildingSurface:Detailed`<br>`Construction`<br>`Material`<br>`Material:NoMass`<br>`RunPeriod`<br>`Timestep` | `AirLoopHVAC`<br>`PlantLoop`<br>`EMS`<br>`PythonPlugin`<br>`AirflowNetwork`<br>`Daylighting`<br>`FenestrationSurface`<br>`Autosizing` | `heat_balance_manager_source_order`<br>`heat_balance_surface_manager_source_order`<br>`heat_balance_air_manager_source_order`<br>`zone_temp_predictor_corrector_source_order`<br>`official_dynamic_heat_balance_compat_candidate` | `official_1zone_uncontrolled_dynamic_conformance_candidate_001` | Official 1ZoneUncontrolled compatibility only for declared variables and tolerances. |
| ideal_loads_no_oa_sensible | ideal_loads | compatibility | supported_compatibility_run | `ZoneHVAC:IdealLoadsAirSystem`<br>`ZoneHVAC:EquipmentConnections`<br>`ZoneHVAC:EquipmentList`<br>`ZoneControl:Thermostat`<br>`ThermostatSetpoint:DualSetpoint` | `OutdoorAir`<br>`Economizer`<br>`HeatRecovery`<br>`Autosizing`<br>`EMS`<br>`PythonPlugin` | `ideal_loads_zone_equipment_purchased_air_source_order`<br>`ideal_loads_air_system` | `ideal_loads_blank_fuel_efficiency_conformance_candidate_001`<br>`ideal_loads_constant_fuel_efficiency_conformance_candidate_001`<br>`ideal_loads_no_oa_facility_meter_conformance_candidate_001`<br>`ideal_loads_no_oa_facility_meter_monthly_run_period_conformance_candidate_001`<br>`ideal_loads_no_oa_report_energy_conformance_candidate_001`<br>`ideal_loads_no_oa_sensible_conformance_001`<br>`ideal_loads_non_constant_fuel_efficiency_conformance_candidate_001` | Arbitrary release compatibility is limited to the CP303 exact direct-Zone binding: one fully mixed Zone, zero-hysteresis DualSetpoint, one sequence-one SequentialLoad IdealLoads system, one distinct supply/Zone-air/return topology, no OA or humidity control, and state-backed SourceSetpointThresholds demand. No-OA/no-limit conformance evidence remains limited to declared case variables; every otherwise-valid out-of-binding fixed-demand adapter is diagnostic-only, while blocking models remain unsupported. CP305 additionally requires the exact direct release lane to execute persistent single-unit InitPurchasedAir state, prove one hard-size and BeginEnvrn initialization, and consume its cached finite-flow mass limits; diagnostic fallback still carries no release lifecycle evidence. CP306 additionally builds an immutable TypedModel declaration-order manager plan, rejects active plenum topology before runtime-state mutation, and eagerly snapshots membership outcomes in retained Zone declaration order through each Zone EquipmentConnection and its referenced list entry order, ignoring unreferenced list objects. Until Zone equipment inputs are filled, only the runtime latch and outcome recording are deferred; the first ready call latches before one infallible declaration-order replay. That replay retains membership results, ordered missing-membership diagnostics without fail-fast, immutable plan validation, and selected-unit work afterward. The matched-list ID is Rust diagnostic evidence; this InitPurchasedAir call observes only CheckZoneEquipmentList's Boolean return and does not request optional CtrlZoneNum. Release support remains the exact one-unit direct-Zone binding. Active plenum arrays/barriers, exhaust/multiple-return behavior, Autosize/SizePurchasedAir, multi-unit release dispatch/residual/results, exact message registry, reset/concurrency, broader environment/warmup/adaptive lifecycle, and full Init parity remain outside this claim. CP307 additionally requires an immutable typed selected-unit topology plan and source-ordered one-time transition after the manager sweep. The transition commits its latch before supply validation, preserves invalid-exhaust Severe before return fallback, one-return assignment, the multiple-return warning-without-assignment quirk, zero-return fatal, and the nonfatal-outcome-only OA/economizer advisory as structured lifecycle evidence. Release validation still requires blank IdealLoads exhaust, no Zone exhaust topology, exactly one Zone return, no OA, `SingleZoneReturn`, exact bound identities, one completion, and no topology diagnostic, failure, or advisory. Explicit/invalid exhaust, multiple/zero returns, and economizer branches are direct lifecycle tests only and do not remove `OutdoorAir` or `Economizer` from the forbidden features. The C++ zero-supply bypass is outside this typed required-supply subset, and retained fatal replay is a Rust fail-closed divergence rather than retry parity. Plenum ownership, full sizing/autosizing, multi-unit execution, broader OA behavior, exact messages, wrong-ControlledZone replay, and reset/concurrency remain excluded; capability level and evidence cases are unchanged. CP308 additionally requires the exact release lane to execute the bounded direct-hard-sized `SizePurchasedAir` legacy route after selected-unit topology and before `BeginEnvrn`. The route requires a positive current Zone-equipment slot, no `DesignSpecification:ZoneHVAC:Sizing` binding, no completed Zone sizing run, no `AutoSize` value, and finite nonnegative direct fields. It retains a source-ordered four-field overlay in persistent per-unit state, clears the Init size latch only after normal child return, derives environment flow caches from that overlay, and threads the same capacities into Calc. The source `CurZoneEqNum <= 0` suppression is characterized by direct lifecycle tests but is not release-admitted. This changes lifecycle evidence, not the support envelope or numerical evidence: Autosizing remains forbidden, evidence cases and support level are unchanged, and custom/scalable sizing, design days, Zone sizing arrays and `FinalZoneSizing`, complete sizer calculations, emitted sizing reports and warnings, EMS and shared scratch, child-error and partial-side-effect parity, retry/reset/concurrency, and full `SizePurchasedAir` or `InitPurchasedAir` parity remain excluded. CP309 additionally executes the bounded post-BeginEnvrn supply-temperature diagnostic suffix in cooling-before-heating order, allocates or reuses relative recurring identities, aggregates call count and reported extrema, and exposes the registry through lifecycle JSON. Exact release validation requires every diagnostic identity, event, index, and count to remain zero, so active diagnostics remain direct lifecycle evidence only. Actual message/report sinks, schedule-service effects, exact EnergyPlus process-global registry parity, warmup/sizing counters, broader multi-unit release execution, and reset/concurrency remain excluded; support level, evidence cases, and forbidden features are unchanged. CP310 additionally requires the exact release lane to execute the bounded `CalcPurchAirLoads` entry prefix after each successful Init and before the existing calculation. It records caller-supplied resolved node identities, an exact 12-target zero-result snapshot while actually clearing the three retained fields, heating-then-cooling Remaining demand, availability-manager ordering, unconditional read-site evidence from pre-sampled overall/heating/cooling values, independent mode gates, and the line-2022 `UnitOn` body decision. Release validation requires one prefix call/reset/demand/read-site group plus one normally preallocated ZoneComp `NoAction` visit/Zone write/status copy per coupling, zero `ForceOff`, heating and cooling values/on-counts equal to one/call count, exact bound latest identities, and JSON lifecycle evidence; non-direct runtimes reject this evidence. Line 2023 onward, live schedule/ZoneComp service ownership, OA/EMS/mode selection and all downstream Calc behavior remain outside the claim. Support level, run state, forbidden features, evidence cases, numerical conformance, and full lifecycle status are unchanged. CP311 supersedes CP310's line-2023 exclusion only for the `CalcPurchAirLoads` parent prefix at lines 2023-2040 and the bounded no-OA child route at lines 2781, 2783, 2785, and 2806-2809. The exact release lane consumes the retained CP310 gate in one-for-one order, records a Zone heat-balance reference binding without reading its fields, calls the child and rewrites retained pre-EMS minimum OA flow to zero only for an entered body, reads false EMS and `OutdoorAir` predicates, and records zero sensible and moisture effects; UnitOff skips every body site. Release validation requires reconciled active/UnitOff, child, retained-write, predicate-read, zero-effect, and latest-state evidence plus zero EMS applications, OA effects, and psychrometric calls in direct-only JSON. `OutdoorAir` and `EMS` remain forbidden. Full minimum-OA child services, OA-true/EMS numerics, operating-mode selection at line 2046 and later, support level, run state, evidence cases, and numerical conformance remain unchanged. CP312 supersedes CP311's line-2046 exclusion only for the lines 2046-2047 cooling-entry gate. The exact release lane consumes linked CP310/CP311 snapshots, compares zero minimum-OA sensible effect against finite cooling demand with inclusive `>=`, conditionally records the prevalidated DualHeatCool value after short-circuiting, records no SingleHeat block, and makes a local Cooling assignment exactly when the existing numerical DTO returns Cooling; UnitOff skips the gate. Direct lifecycle JSON reconciles source/UnitOff and cooling/fallthrough partitions, conditional reads, assignments, numerical Cooling counts, and latest state, while non-direct evidence is rejected. This is lifecycle evidence rather than a new numerical or thermostat-family claim. At the CP312 boundary, the cooling body at line 2056, Heat/DeadBand selection at line 2348, live temperature-control service, non-Dual release routes, support level, run state, required or forbidden features, evidence cases, and numerical conformance remain unchanged. CP313 supersedes only that line-2056 exclusion for the lines 2056-2057 cooling OA maximum-flow guard. Exact release preserves nested selector and flow-read short-circuiting across NoLimit, Capacity, FlowRate, and FlowRateAndCapacity; the selected-flow routes compare no-OA `+0.0` with a finite nonnegative initialized maximum, so the excluded warning/reduction body is never entered. Direct lifecycle JSON reconciles CP312 Cooling/UnitOff/non-cooling, selector/read, strict-comparison, and zero-body-entry evidence while non-direct evidence is rejected. This is guard-only lifecycle evidence, not OA, economizer, warning, clamp, numerical, or new finite-limit conformance support. At the CP313 boundary, line 2058 and later behavior, support level, run state, required and forbidden features, evidence cases, and numerical conformance remain unchanged. CP314 supersedes only that line-2058 exclusion for the true body at lines 2058-2078. Direct characterization converts parent outdoor-air mass flow with `StdRhoAir`, retains the zero-initialized first/recurring warning counter and relative index, records first Warning/Continue/timestamp versus later recurring calls with max-only `OAVolFlowRate` and empty maximum units, and clamps local outdoor-air mass flow after either branch. Exact no-OA release never enters the CP313 body, so direct-only lifecycle JSON reconciles zero CP314 density reads, conversions, warnings, indices, and clamps while non-direct evidence is rejected. Line 2082 is the lexical first excluded executable and the dynamic continuation only when CP312 entered cooling and CP313 was false; a CP314-true path instead skips the sibling `else` and next reaches line 2109. This remains structured lifecycle evidence, not actual warning output, OA/economizer behavior, clamp-enabled release numerics, or new finite-limit conformance. Support level, run state, required and forbidden features, evidence cases, and numerical conformance remain unchanged. CP315 supersedes only CP314's line-2082 exclusion for the outer `EconomizerType != NoEconomizer` guard. Public direct release admits exact UnitOff, non-cooling, or active CP313-false fallthrough partitions; the CP313-true/CP314-body skip is pure/internal characterization only. UnitOff and non-cooling skip the guard, while the active fallthrough reads the typed enum once. Exact no-OA release requires `NoEconomizer`, so each active comparison is false and the line-2083 inner condition has zero entries. Internal DifferentialDryBulb/DifferentialEnthalpy characterization stops at that boundary and remains non-release. Direct-only lifecycle JSON reconciles the predecessor partitions, enum reads, comparisons, false results, and zero inner entries; non-direct evidence is rejected. The active true route begins at excluded line 2083, while false and CP314-true routes continue at excluded line 2109. Inner node state, psychrometrics, supply/OA flow effects, `EconoOn`, `TimeEconoActive`, and later Calc numerics remain outside this slice. This does not connect or promote the separate outdoor-air economizer helper: `OutdoorAir` and `Economizer` remain forbidden for the exact direct binding, and support level, run state, required features, evidence cases, and numerical conformance remain unchanged. CP316 supersedes only CP315's line-2083 exclusion for the executable lines 2083-2086 compound cooling economizer condition. Internal characterization preserves the source's repeated typed enum read, left-to-right `&&`/`\|\|` short-circuiting, conditional pre-sampled temperature and stored-enthalpy reads, and strict `<` comparisons. Exact no-OA release always consumes CP315's false `NoEconomizer` result and proves one complete CP316 source-site skip per CP315 transition, with zero selector enum reads, node reads, comparisons, satisfied predicates, or line-2089 body entries. Lines 2087-2088 are non-executable; true continues at the first excluded executable line 2089 and false at line 2109. CP316 does not sample live Node state, recompute enthalpy, call the separate outdoor-air economizer helper, or implement psychrometric and flow effects. This is condition-only lifecycle evidence, not live OA/economizer ownership or numerical support; `OutdoorAir` and `Economizer` remain forbidden, and support level, run state, required features, evidence cases, and numerical conformance remain unchanged. CP317 supersedes only CP316's line-2089 exclusion for the cooling economizer true body at executable lines 2089-2101. Internal characterization preserves all 37 source sites: the Zone humidity-ratio read, canonical `energyplus_psy_cp_air_fn_w` scalar result and explicit `CpAir` assignment; temperature reads, subtraction, `DeltaT` assignment and gate re-read; left-associated divisions with local `CpAir`/`DeltaT` re-reads and initial supply assignment; repeated `CoolingLimit` reads and selector short-circuit; the maximum-flow guard read followed by the clamp-time maximum upper-bound re-read; the inner maximum, outer minimum, and clamped assignment; the resulting supply-flow read followed by outdoor-air flow; and the activation-time supply-flow re-read before ordered `EconoOn`, outdoor-air-flow, system-timestep, and `TimeEconoActive` effects over pre-sampled scalars. This pure psychrometric characterization covers the scalar result only; source static `dwSave`/`cpaSave` state, the initial `dwSave == -100.0` sentinel, cache-hit/miss identity, and concurrent cache lifecycle remain excluded. Exact no-OA release consumes a CP316 predecessor with zero calculation-body entries, so every CP317 transition is a complete all-sites skip with zero reads, calculations, comparisons, clamps, assignments, or timestep effects. Line 2109 is the first excluded executable after the mapped body and intervening closing delimiters/comments. The separate outdoor-air economizer helper remains disconnected. This is lifecycle evidence, not live Node/OA/economizer ownership or numerical support; `OutdoorAir` and `Economizer` remain forbidden, and support level, run state, required features, evidence cases, numerical conformance, and Roadmap state remain unchanged. CP318 additionally requires the exact direct lane to execute the 19-site common Cooling continuation at `PurchasedAirManager.cc` lines 2109-2116 after CP317 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets the candidate to positive zero and exact release proves retained CP310 `CoolOn=true`, consumes retained `QZnCoolSP` plus identity-checked live Zone humidity ratio and temperature, evaluates canonical `PsyCpAirFnW`, applies strict `DeltaT < -1.0e-5`, and preserves left-associated `(QZnCoolSP / CpAir) / DeltaT`, raw IEEE values, and signed zero. Direct-only lifecycle evidence reconciles all sites. The source psychrometric static cache, line 2119 and later humidity/EMS/limit/mixed-air behavior, live schedule or general Node ownership, and numerical-result reconciliation remain excluded. This changes no support level, run state, required or forbidden features, evidence case, or numerical conformance; `OutdoorAir`, `Economizer`, and `EMS` remain forbidden and broad humidity control remains outside the claim. CP319 additionally requires the exact direct lane to execute the 21-site Cooling dehumidification-flow candidate at `PurchasedAirManager.cc` lines 2119-2128 after CP318 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets positive zero, reads retained `CoolOn`, and exact direct `DehumidificationControlType::None` proves selector fallthrough without requesting live moisture-demand or Node-humidity services. Private Humidistat characterization alone preserves local assignments and rereads, strict left-to-right `DeltaHumRat < -0.00025 && MdotZnDehumidSP < 0.0`, short-circuiting, raw division, IEEE values, and signed zero. CP319 does not reuse the broader humidity helper or reconcile its candidate with the numerical DTO. Line 2133 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; broad humidity control remains outside the claim. CP320 additionally requires the exact direct lane to execute the 26-site Cooling humidification-flow candidate at `PurchasedAirManager.cc` lines 2133-2144 after CP319 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets positive zero, reads retained same-call `HeatOn`, and exact direct `HumidificationControlType::None` proves the outer selector fallthrough without reading the nested dehumidification selector or requesting live moisture-demand or Node-humidity services. Private Humidistat characterization alone preserves the lazy repeated `DehumidCtrlType == Humidistat \|\| DehumidCtrlType == None` reads, local assignments and rereads, strict left-to-right `DeltaHumRat > 0.00025 && MdotZnHumidSP > 0.0`, short-circuiting, raw division, IEEE values, and signed zero. CP320 does not reuse the broader humidification helper or reconcile its candidate with the numerical DTO. Line 2147 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; broad humidity control remains outside the claim. CP321 additionally requires the exact direct lane to execute the ten-site Cooling capacity-zero flow reset at `PurchasedAirManager.cc` lines 2147-2152 after CP320 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling preserves lazy repeated `CoolingLimit == Capacity \|\| CoolingLimit == FlowRateAndCapacity` reads, reads retained hard-sized `MaxCoolTotCap` only after a selector match, and compares exact `== 0.0`. A true result assigns positive zero in source order to all three flow candidates; false paths preserve retained same-call CP318 cooling, CP319 dehumidification, and CP320 humidification candidate bits. Exact direct release consumes those three candidate snapshots plus retained model/Init state without live services or numerical-DTO reconciliation. Its pure source transition does not reuse the broader `cooling_capacity_limit_is_zero` helper, perform a finite check, normalize, or clamp; existing public direct admission still rejects unresolved or nonfinite selected sized values before the transition. Line 2155 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP322 additionally requires the exact direct lane to execute the six-site Cooling supply mass-flow maximum at `PurchasedAirManager.cc` executable line 2155 after CP321 and before the unchanged numerical DTO. The lexical sites read retained outdoor-air, cooling, dehumidification, and humidification mass flows, apply the source-shaped five-argument maximum with a positive-zero floor, and assign `SupplyMassFlowRate`; they do not claim a C++ function-argument evaluation order. UnitOff and non-cooling skip the slice. Active Cooling preserves ObjexxFCL's strict-`<` pair tree, left-first tie or unordered selection, and exact signed-zero, NaN, and infinity behavior without Rust floating-point maximum helpers, total ordering, finite checks, normalization, or clamps. Exact direct release consumes post-CP321 candidate results plus retained same-call CP311 pre-EMS outdoor-air state, requiring bitwise positive zero on the no-OA route without duplicate caller scalars, live services, or numerical-DTO reconciliation. Line 2157 and later EMS override, flow-clamp, mixed-air, capacity, and supply-state behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP323 additionally requires the exact direct lane to execute the three-site Cooling supply mass-flow EMS override guard at `PurchasedAirManager.cc` executable line 2157 after CP322 and before the unchanged numerical DTO. The sites read `EMSOverrideMdotOn`, evaluate the Boolean guard, and enter the override body when enabled; UnitOff and non-cooling skip the slice. Private characterization preserves both outcomes, while exact direct release validates the completed same-call CP322 snapshot and private witness and requires the false EMS-disabled route, recording one read and evaluation, zero body entries, and no EMS actuator-service request per active transition. Line 2158 is the first excluded executable; the true body at lines 2158-2159, `EMSValueMassFlowRate`, supply and outdoor-air assignments, `min`, the line-2161 flow-limit condition, and later flow behavior remain excluded. `EMS` remains forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP324 additionally requires the exact direct lane to execute the complete six-site Cooling supply mass-flow EMS override body at `PurchasedAirManager.cc` executable lines 2158-2159 after CP323 and before the unchanged numerical DTO. The sites read `EMSValueMassFlowRate`, assign supply flow, read outdoor-air and overridden supply flow as lexical minimum operands, apply the source-shaped two-argument minimum, and assign outdoor-air flow. Private true-body characterization preserves strict-`<` comparison and right-operand selection on a tie or unordered result without claiming C++ function-argument evaluation order. UnitOff and non-cooling skip the slice; exact direct release validates the completed same-call CP323 latest snapshot and private witness and requires its false EMS-disabled route, so active direct transitions also complete-skip all six sites with zero body execution, value or flow reads, assignments, minimum evaluations, or EMS service requests. Line 2161 is the first excluded executable; its cooling flow-limit condition and assignment, the line-2166 very-small-flow clamp, and later mixed-air, capacity, and supply-state behavior remain excluded. No EMS field or service is added and `EMS` remains forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP325 additionally requires the exact direct lane to execute the complete seven-site Cooling supply mass-flow limit guard at `PurchasedAirManager.cc` executable lines 2161-2162 after CP324 and before the unchanged numerical DTO. UnitOff and non-cooling skip every site. Active Cooling preserves lazy repeated `CoolingLimit == FlowRate \|\| CoolingLimit == FlowRateAndCapacity` reads, reads retained `MaxCoolMassFlowRate` only after a selector match, compares strict `> 0.0`, and records whether the deliberately excluded clamp body is entered. Exact direct release validates the completed same-call CP324 latest snapshot and private witness and requires its EMS-disabled complete skip, then obtains the selector from the selected typed system and the maximum from the bit-validated retained Init cache without duplicate caller scalars or live sizing, schedule, Node, psychrometric, EMS, or diagnostic services. A selected positive maximum may record body entry, but CP325 performs no supply-flow read, minimum, assignment, or numerical-DTO reconciliation. Line 2163 is the first excluded executable; the actual supply-flow clamp, line-2166 very-small-flow guard/reset, and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP326 additionally requires the exact direct lane to execute the complete four-site Cooling supply mass-flow limit body at `PurchasedAirManager.cc` executable line 2163 after CP325 and before the unchanged numerical DTO. The sites read retained supply and maximum cooling mass flows as textual minimum operands without claiming C++ function-argument evaluation order, apply ObjexxFCL's source-shaped `a < b ? a : b` two-argument minimum, and assign the result to retained source-local supply flow. UnitOff, non-cooling, and active CP325 guard-false transitions skip every site; only a true body entry executes the body. Private characterization preserves strict-`<` comparison, right-operand selection on tie or unordered values, and exact signed-zero, NaN, and infinity behavior. Exact direct release validates the completed same-call CP325 latest snapshot and private witness, obtains the pre-clamp supply flow from the bit-validated retained CP322 result through unchanged CP323-CP325 lineage, and re-reads the maximum from the retained Init cache without duplicate caller scalars or live sizing, schedule, Node, psychrometric, EMS, or diagnostic services. Coupled and pipeline validation require bit-exact retained CP322-result and Init-cache provenance plus internal CP326 minimum/assignment consistency; they do not reconcile this line-2163 checkpoint with the final numerical DTO because that DTO represents line-2166-and-later downstream work. CP326 does not consume that DTO as input and does not feed or replace it. Line 2166, not line 2167, is the first excluded executable; its very-small-flow guard, line-2167 positive-zero reset, and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP327 additionally requires the exact direct lane to execute the complete four-site Cooling supply mass-flow very-small-flow guard at `PurchasedAirManager.cc` executable line 2166 after CP326 and before the unchanged numerical DTO. The sites read retained supply flow and the `HVAC::VerySmallMassFlow` constexpr as textual relational operands without claiming a C++ built-in relational-operand evaluation order, apply the source `<=`, and record reset-body entry. The operands are side-effect-free, so the observable semantics are only the binary64 `<=` result. UnitOff and non-cooling skip every site; every active Cooling CP326 predecessor evaluates the guard. Equality, both signed zeros, and negative infinity enter the body, while positive infinity and NaN fall through. The threshold is pinned to EnergyPlus 26.1 `DataHVACGlobals.hh:89`, exact `Real64 constexpr VerySmallMassFlow(1.0E-30)` with binary64 bits `0x39b4484bfeebc2a0`. Exact direct release validates the completed same-call CP326 latest snapshot and private witness and obtains the operand from CP326's bit-exact resulting supply flow without a duplicate caller scalar or live sizing, schedule, Node, psychrometric, EMS, or diagnostic service. Coupled and pipeline validation require retained post-CP326 supply provenance plus internal CP327 threshold, comparison, and body-entry consistency; they do not consume, reconcile, feed, or replace the final numerical DTO. Line 2167 is the first excluded executable; its positive-zero assignment and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP328 additionally requires the exact direct lane to execute the single-site Cooling supply mass-flow positive-zero reset body at `PurchasedAirManager.cc` executable line 2167 after CP327 and before the unchanged numerical DTO. The one lexical site assigns source `0.0` to retained `SupplyMassFlowRate`, producing exact binary64 positive zero with IEEE bits `0x0000000000000000`; it does not re-read the line-2166 operands, repeat `<=`, normalize, invoke a minimum or maximum helper, or apply another clamp. UnitOff and non-cooling skip the assignment and retain no local supply-flow result. An active CP327 true route performs the assignment, including for `-0.0`, equality, finite below-threshold values, and negative infinity, while an active guard-false route skips it and preserves predecessor bits exactly, including positive infinity or a NaN payload. Exact direct release validates the completed same-call CP327 latest snapshot and private witness and obtains body entry and pre-assignment flow only from that bit-exact predecessor without a duplicate caller scalar or live sizing, schedule, Node, psychrometric, EMS, or diagnostic service. Coupled and pipeline validation require exact predecessor, assignment, and result-bit consistency; they do not consume, reconcile, feed, or replace the final numerical DTO. Line 2168 is an excluded non-executable closing delimiter, and the `CalcPurchAirMixedAir` call beginning at line 2171 is the first excluded executable; mixed-air, capacity, supply-state, and later behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP329 additionally requires the exact direct lane to execute the complete Cooling `CalcPurchAirMixedAir` call at `PurchasedAirManager.cc` lines 2171-2178 plus only the bounded direct no-OA child fallback. Nine textual caller sites bind/read the state, selected unit, retained no-OA flow, CP328 supply result, three distinct mixed-air output references, and Cooling mode, then invoke and normally return from the child without claiming C++ argument evaluation order. UnitOff and non-cooling skip this Cooling call; every active Cooling predecessor calls once even for zero supply flow. Exact release validates same-call CP328, `OAMassFlowRate=+0.0`, selected identity, Cooling mode, and coherent finite recirculation state. The child performs no OA-node or psychrometric work, copies recirculation T/W and a coherent enthalpy projection to mixed outputs, zeros both heat-recovery outputs, and leaves heat-recovery active time unchanged. The coherent projection does not claim parity for arbitrary independently stored Node enthalpy, torn/late reads, aliases, active OA/recovery and saturation/cache/diagnostics, failure/replay, or concurrency; the broader outdoor-air mixed-state helper is not the exact child. Direct-only lifecycle evidence preserves CP328-to-CP329-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2183 is the first excluded executable after comments at lines 2180-2182; the Heat/DeadBand call at 2454-2461 and later work remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, or Roadmap state; both Calc routines remain `source_mapped`. CP330 additionally requires the exact direct lane to execute the complete three-site Cooling positive-supply guard at `PurchasedAirManager.cc` executable line 2183 after CP329 and before the unchanged numerical DTO. UnitOff and non-cooling skip it. Every active Cooling predecessor reads retained CP329 `SupplyMassFlowRate` and evaluates strict `> +0.0` once, then executes the conditional body-entry site only when true, yielding `2 * active + positive_body_entries` dynamic site executions; the literal is not another site and no C++ relational-operand order is claimed. Raw IEEE semantics make signed zeros, negative finite values, negative infinity, and NaN false and positive finite values and positive infinity true without finite checks, normalization, total ordering, or clamps. Exact release consumes only the same-call CP329 direct no-OA snapshot and witness; CP327/CP328 direct lineage has already canonicalized ordered values at or below `HVAC::VerySmallMassFlow` to positive zero while an unordered NaN can remain bit-exact. Direct-only lifecycle evidence preserves CP329-to-CP330-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2184 is commentary; line 2185 is the first excluded lexical executable and true-body executable. Lines 2185-2337, the 2339-2345 false body whose dynamic next executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, or Roadmap state; both Calc routines remain `source_mapped`. CP329/CP330 fail-closed retained-chain hardening additionally requires lifecycle admission to prove the full retained predecessor chain. CP329's runtime-aware completed proof recursively validates completed CP328 through its latest/private witness and upstream prefix, while completed and pending CP329 UnitOff/non-cooling/active histories must match CP328 exactly; a route-aware checked-arithmetic preflight covers every next counter and caller/child source-site increment before mutation. CP330 completed and pending history must match CP329 exactly and recursively retain that CP328-chain proof, so coordinated CP329/CP330 counter or witness corruption cannot bypass CP328. Chain, history, witness, and overflow failures leave runtime state and private witnesses unchanged. This adds no numerical DTO consumption or reconciliation and changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state. CP331 additionally requires the exact direct lane to execute the single three-site Cooling positive-supply `CpAir` assignment at `PurchasedAirManager.cc` executable line 2185 after CP330 and before the unchanged numerical DTO. The sites read controlled Zone heat-balance `airHumRat`, evaluate canonical `PsyCpAirFnW`, and assign local `CpAir`. UnitOff, non-cooling, and CP330 active guard-false routes skip every site; a CP330 positive-body entry executes all three, so dynamic source-site executions equal `3 * positive_supply_mass_flow_body_entries`. Exact release conditionally reads the identity-checked live Zone humidity and requires its bits to match CP329's retained no-OA recirculation- and mixed-air humidity copies, which are same-call lineage evidence rather than substitute source operands. Positive-route admission requires the live humidity to be finite and `>= 0.0` (both signed zeros are admitted) and the canonical `CpAir` result to be finite. It recursively validates CP330 latest/private-witness and route history through the retained predecessor chain and preflights every conditional source and private-witness increment transactionally. Rust claims only the existing stateless canonical scalar on the admitted physical-domain route. The C++ program-wide `dwSave`/`cpaSave` cache, `-100.0` sentinel and raw-`-100.0` first-call anomaly, hit/miss/reset/interleaving/concurrency behavior, all-`f64` cache equivalence, and full `PsyCpAirFnW` parity remain excluded. Direct-only lifecycle evidence preserves CP330-to-CP331-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2186 is the first excluded lexical executable on the true route; lines 2186-2337, the 2339-2345 false body whose dynamic next executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and supply-temperature/capacity behavior remain unpromoted. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP332 additionally requires the exact direct lane to execute the single eight-site Cooling positive-supply temperature arithmetic and assignment at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2186 after CP331 and before the unchanged numerical DTO. The sites read retained `QZnCoolSP`, read local CP331 `CpAir`, re-read retained CP330 `SupplyMassFlowRate`, form the parenthesized denominator product, divide the load by that product, read controlled Zone Node temperature, add that temperature, and assign `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ built-in operand-evaluation-order claim. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while each CP331 assignment executes all eight, so dynamic source-site executions equal both `8 * cp_air_assignment_count` and `8 * positive_supply_mass_flow_body_entries`. Raw binary64 `QZnCoolSP / (CpAir * SupplyMassFlowRate) + ZoneTemp` grouping and product/division/addition/assignment bits are preserved without reassociation, reciprocal replacement, `mul_add`, normalization, clamping, minimum, or maximum. Exact release consumes retained CP310 demand, the bit-exact CP331 `CpAir`, retained CP330 flow, and only then conditionally reads identity-checked live Zone temperature; CP318 Zone-temperature and CP329 recirculation-/mixed-air-temperature bits remain same-call lineage evidence, not substitute operands. No duplicate caller scalar, psychrometric recomputation, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP331 latest/private witness and predecessor chain, preserves all four predecessor routes, preflights source and witness increments transactionally, and maintains CP331-to-CP332-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2187 is the first excluded lexical executable and CP333 boundary; its minimum-cooling-temperature maximum, the line-2189 mixed-air minimum, humidity/enthalpy assignments, capacity controls, lines 2187-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP333 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply temperature minimum-limit statement at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2187 after CP332 and before the unchanged numerical DTO. The sites read retained CP332 `PurchAir.SupplyTemp`, re-read selected-system `PurchAir.MinCoolSuppAirTemp`, apply ObjexxFCL's source-shaped two-argument maximum, and assign the selected value back to `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ function-argument-evaluation-order claim because both operands are side-effect-free. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while every CP332 assignment executes all four, so dynamic source-site executions equal `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`. The imported two-`double` maximum is exactly `a < b ? b : a`: true selects the right minimum-temperature operand and tie or unordered comparison retains the first CP332 operand, preserving operand-order-sensitive signed zero, NaN, and infinity behavior without Rust `f64::max`, total or partial ordering, a finite check, normalization, another clamp, or a minimum. Exact release takes the first operand only from the bit-exact CP332 result, re-reads the second from the identity-checked selected typed system's `minimum_cooling_supply_air_temperature_c`, and treats CP318's retained minimum only as same-call lineage evidence, not a substitute operand. No duplicate caller scalar, live service, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP332 latest/private witness and full predecessor chain, preserves all four routes, preflights source and witness increments transactionally, and maintains CP332-to-CP333-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2188 is commentary; physical line 2189 is the first excluded lexical executable and CP334 boundary. Its mixed-air minimum, line-2190 humidity assignment, line-2191 enthalpy calculation, capacity controls, lines 2189-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP334 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply temperature mixed-air-limit statement at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2189 after CP333 and before the unchanged numerical DTO. The sites read retained CP333 `PurchAir.SupplyTemp`, read retained CP329 `PurchAir.MixedAirTemp`, apply ObjexxFCL's source-shaped two-argument minimum, and assign the selected value back to `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ function-argument-evaluation-order claim because both operands are side-effect-free. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while every CP333 minimum-limit assignment executes all four, so dynamic source-site executions equal `4 * supply_temperature_mixed_air_limit_count`, `4 * supply_temperature_minimum_limit_count`, `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`. The imported two-`double` minimum is exactly `a < b ? a : b`: true selects the first CP333 operand and tie or unordered comparison selects the second CP329 operand, preserving operand-order-sensitive signed zero, NaN, and infinity behavior without Rust `f64::min`, total or partial ordering, a clamp, normalization, or finite coercion. Exact release takes the first operand only from the bit-exact CP333 result and takes the second only from the same-call completed CP329 latest/private witness's finite `mixed_air_temperature_c`. CP329 owns the retained mixed-air source field, and the equal no-OA Zone temperature is same-call lineage evidence, not a substitute operand. No duplicate caller scalar, Zone-state re-read, typed-model value, live service, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP333 and CP329 latest/private witnesses and full predecessor chains, preserves all four routes, preflights source and witness increments transactionally, and maintains CP333-to-CP334-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO; non-direct paths carry no CP334 evidence. Physical line 2190 is the first excluded lexical executable and CP335 boundary. Its mixed-air humidity assignment, line-2191 enthalpy calculation, capacity controls, lines 2190-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, enthalpy, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP335 additionally requires the exact direct lane to execute the single two-site Cooling positive-supply humidity-ratio mixed-air assignment at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2190 after CP334 and before the unchanged numerical DTO. The exact textual sites are `read-purchased-air-mixed-air-humidity-ratio` and `assign-purchased-air-supply-humidity-ratio`, in that order: they read the same-call retained CP329 `PurchAir.MixedAirHumRat` value and assign the identical binary64 payload to `PurchAir.SupplyHumRat`. The statement contains no arithmetic, psychrometric evaluation, minimum, maximum, clamp, normalization, or finite coercion. UnitOff, non-cooling, and CP330 active guard-false routes skip both sites, while every CP334 mixed-air temperature-limit execution performs both, so dynamic source-site executions equal `2 * supply_humidity_ratio_mixed_air_assignment_count`, `2 * supply_temperature_mixed_air_limit_count`, `2 * supply_temperature_minimum_limit_count`, `2 * supply_temperature_assignment_count`, `2 * cp_air_assignment_count`, and `2 * positive_supply_mass_flow_body_entries`; each per-site counter equals the active assignment count. The pure transition copies signed-zero, NaN, and infinity payloads bit-for-bit. Exact release accepts only the CP334 snapshot predecessor and obtains the active RHS only from the same-call completed CP329 latest/private witness's `mixed_air_humidity_ratio`; CP335's local exact snapshot predicate requires that value finite and `>= 0.0` and preserves negative-zero bits; the complete public release chain cannot currently reach that characterization because retained CP332 operand-lineage proof rejects it first. No duplicate caller scalar, Zone-state re-read, typed-model value, live service, psychrometric recomputation, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains CP334 and CP329 latest/private witnesses and full predecessor chains, preserves all four routes including `SupplyHumidityRatioMixedAirAssigned`, preflights source and witness increments transactionally, and maintains CP334-to-CP335-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO; non-direct paths carry no CP335 evidence. Physical line 2191 is the first excluded lexical executable and CP336 boundary. Its `PsyHFnTdbW` enthalpy calculation, capacity controls, lines 2191-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature, capacity, enthalpy, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP336 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply enthalpy assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2191 after CP335 and before the unchanged numerical DTO; the locked source-file SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact textual sites are `read-purchased-air-supply-temperature-for-enthalpy`, `read-purchased-air-supply-humidity-ratio-for-enthalpy`, `evaluate-psy-h-fn-tdb-w`, and `assign-local-supply-enthalpy`; the side-effect-free argument reads make no C++ function-argument evaluation-order claim. Only CP335's active `SupplyHumidityRatioMixedAirAssigned` route executes CP336. Exact release accepts only the CP335 predecessor, recursively validates completed CP335 and CP334 latest/private witnesses and retained chains, obtains temperature bit-exactly from same-call CP334 `assigned_supply_temperature_c`, and obtains humidity from CP335 `assigned_supply_humidity_ratio`. It evaluates canonical `energyplus_psy_h_fn_tdb_w` with source `max(W, 1.0e-5)` floor behavior; the legacy IdealLoads `moist_air_enthalpy_j_per_kg` helper, duplicate caller scalars, Zone or typed-model re-reads, live services, and numerical-DTO inputs are forbidden. The pure transition characterizes source-shaped IEEE signed-zero, NaN, infinity, floor, grouping, and overflow behavior. Exact direct admission requires finite temperature, finite `>= 0.0` humidity including negative zero, and finite enthalpy; both signed zeros use the positive floor, while CP335's complete public chain cannot currently deliver negative zero because retained CP332 operand-lineage proof rejects it first. UnitOff, non-cooling, and CP330 active guard-false routes retain null operands and result. Each active assignment executes all four sites, so dynamic source-site executions equal `4 * supply_enthalpy_assignment_count`, `4 * supply_humidity_ratio_mixed_air_assignment_count`, `4 * supply_temperature_mixed_air_limit_count`, `4 * supply_temperature_minimum_limit_count`, `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`; every per-site counter equals the active count. Four-route parity and checked-arithmetic preflight make identity, ordinal, operand, provenance, history, witness, replay, corruption, nonfinite-result, and overflow rejection transactional. Direct-only lifecycle evidence preserves CP335-to-CP336-to-numerical order without consuming, reconciling with, feeding, or replacing the unchanged numerical DTO; non-direct paths publish `None` and reject CP336 evidence. Physical line 2192 is blank and lines 2193-2194 are comments; physical line 2195 is the first excluded lexical executable and CP337 boundary. Its capacity-limit guard, lines 2195-2337, the zero-flow `else` at 2339-2345 whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, broader capacity and enthalpy behavior, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, status, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP337 additionally requires the exact direct lane to execute the complete five-site Cooling positive-supply capacity-limit guard at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2195 after CP336 and before the unchanged numerical DTO; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The textual Rust witness sites are `read-cooling-limit-for-capacity-comparison`, `compare-cooling-limit-equal-to-capacity`, `read-cooling-limit-for-flow-rate-and-capacity-comparison-after-first-false`, `compare-cooling-limit-equal-to-flow-rate-and-capacity`, and `enter-capacity-limit-body-if-compound-condition-satisfied`. Enum literals are not additional sites, and the inventory makes no C++ built-in `==` operand-evaluation-order claim. Source `\|\|` short-circuiting reaches the second textual `CoolingLimit` occurrence only after the Capacity comparison is false. CP337 is active exactly when CP336 assigned supply enthalpy; UnitOff, non-cooling, and CP330 active guard-false routes skip every CP337 site and retain null selector/result evidence. Exact release takes only the same-call CP336 predecessor, recursively validates its latest/private witness and retained chain, and re-reads `cooling_limit` only from the identity-checked selected typed system, with retained initialization and earlier same-call selector records used only as lineage evidence. `LimitCapacity` executes three sites, `LimitFlowRateAndCapacity` five, and `NoLimit` or `LimitFlowRate` four. For active count `A`, Capacity matches `C`, second comparisons `S=A-C`, combined matches `F`, and body entries `B=C+F`, exact counters require active false fallthroughs `A-B` and dynamic source sites `2*A+2*S+B`; checked preflight makes every failure transactional. Direct-only lifecycle evidence preserves CP336-to-CP337-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2196 is the first excluded lexical executable, true-body statement, and CP338 boundary; a false guard next dynamically executes line 2208. The line-2196 capacity body, its operands and lines 2196-2203, false-path line 2208 and later work through 2337, zero-flow `else` at 2339-2345, Heat/DeadBand sibling at 2347-2348, mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. No maximum-capacity, sizing, psychrometric, supply-state, live-service, or numerical-result ownership is added. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, broader capacity/enthalpy/output behavior, support level, run state, feature boundary, evidence cases, numerical conformance, capability, counts, readiness, status, output claims, and Roadmap state remain unchanged; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP338 additionally requires the exact direct lane to execute the single three-site Cooling positive-supply capacity-limit `CpAir` assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2196 after CP337 and before the unchanged numerical DTO; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact sites are `read-purchased-air-mixed-air-humidity-ratio`, `evaluate-psy-cp-air-fn-w`, and `assign-local-cp-air`. Only CP337 `CapacityLimitBodyEntered` executes CP338, so `LimitCapacity` and `LimitFlowRateAndCapacity` execute all three sites. The exact five retained routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, and `CapacityLimitCpAirAssigned`; the first four skip all sites with null operand/result evidence. For active assignments `A`, equal to CP337 body entries `B`, exact state requires `source_site_execution_count=3*A=3*B` and each per-site counter equals `A`. Exact release accepts only the same-call CP337 predecessor, recursively validates its latest/private witness and chain, and reads `PurchAir.MixedAirHumRat` only from the same-call completed CP329 latest/private witness's `mixed_air_humidity_ratio`; equal no-OA Zone humidity is lineage evidence, not a substitute operand. Duplicate caller scalars, Zone/model re-reads, maximum-capacity or sizing values, live services, later body operands, and numerical-DTO inputs are forbidden. Canonical `energyplus_psy_cp_air_fn_w` applies the source humidity floor and its result is assigned to local `CpAir`; mutable C++ cache behavior and broader psychrometric parity remain deferred. Direct-only lifecycle evidence preserves CP337-to-CP338-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2197 is the first excluded lexical executable and CP339 boundary: `CoolSensOutput = SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy);`; its arithmetic and all later capacity work remain excluded. No support level, readiness, run state, feature boundary, evidence case, numerical conformance, capability, count, output claim, numerical-result ownership, or Roadmap state changes; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP339 additionally requires the exact direct lane to execute the single six-site Cooling positive-supply capacity-limit sensible-output assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2197 after CP338 and before the unchanged numerical DTO: `CoolSensOutput = SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy);`; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites are `read-retained-supply-mass-flow-rate-for-sensible-output-product`, `read-retained-mixed-air-enthalpy-for-sensible-output-difference`, `read-retained-supply-enthalpy-for-sensible-output-difference`, `calculate-mixed-air-enthalpy-minus-supply-enthalpy`, `calculate-supply-mass-flow-rate-times-enthalpy-difference`, and `assign-local-cooling-sensible-output`; their deterministic Rust ordering makes no C++ built-in operand-evaluation-order claim because the reads are side-effect-free. Only CP338 `CapacityLimitCpAirAssigned` executes CP339. The exact five routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, and `CapacityLimitSensibleOutputAssigned`; skipped routes retain null operands, intermediate, and result. For active assignments `A`, equal to CP338 `capacity_limit_cp_air_assignment_count` and CP337 body entries `B`, exact state requires `source_site_execution_count=6*A=6*B` and every per-site counter equals `A`. Exact release accepts only the same-call CP338 predecessor, recursively proves its latest/private witness and complete retained chain, and reads active operands only from retained CP330 `supply_mass_flow_rate_kg_per_s`, CP329 `mixed_air_enthalpy_projection_j_per_kg`, and CP336 `supply_enthalpy_j_per_kg` latest/private witnesses. It preserves exact `SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy)` grouping by subtracting before multiplying, with raw IEEE binary64 behavior and no derived finite-result rejection. CP329 and CP336 enthalpy operands remain finite, while CP330 may retain a permitted `+infinity` flow. Active Rust snapshots keep that possible nonfinite operand and any nonfinite derived difference or product as `Some(value)`. During serde JSON serialization, each such nonfinite numeric field is projected as `null`, while a separate IEEE bit string derived from the retained value remains non-null; skipped Rust snapshots retain corresponding values as `None`, and serialized JSON keeps both numeric and bit-string fields null. CP338 `CpAir`, `MaxCoolTotCap`, model/sizing values, live services, caller scalars, and numerical-DTO consumption, reconciliation, feed, or replacement are excluded. Direct-only lifecycle evidence preserves CP338-to-CP339-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_assignment_lifecycle`; non-direct paths carry no CP339 evidence. Physical line 2198 is the first excluded lexical executable and CP340 boundary, `if (CoolSensOutput >= PurchAir.MaxCoolTotCap) {`; lines 2198-2203, false continuation 2208, and all broader capacity/supply-state/output behavior remain excluded. CP339 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP340 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output guard at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2198 after CP339 and before the unchanged numerical DTO: `if (CoolSensOutput >= PurchAir.MaxCoolTotCap) {`; the locked raw source SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites, in order, are `read-retained-cooling-sensible-output-for-maximum-capacity-comparison`, `read-retained-maximum-total-cooling-capacity-for-sensible-output-comparison`, `compare-cooling-sensible-output-greater-than-or-equal-to-maximum-total-cooling-capacity`, and `enter-cooling-capacity-adjustment-body-if-comparison-satisfied`. This deterministic Rust/source-text witness order makes no C++ built-in `>=` operand-evaluation-order claim because both source reads are side-effect-free. Only CP339 `CapacityLimitSensibleOutputAssigned` executes CP340. The exact six routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputAdjustmentBodyEntered`. For transitions `T`, inherited counts `U/N/P/G`, active assignments `A`, false fallthroughs `F`, and body entries `E`, exact state requires `T=U+N+P+G+F+E=U+N+P+G+A`, `A=F+E=CP338 assignments=CP337 body entries`, both operand reads and comparison equal `A`, body entries equal `E`, `source_site_execution_count=3*A+E`, and witnessed `F/E` parity. Exact release takes only `runtime`, selected `system`, and the same-call CP339 predecessor. It extracts final `cooling_sensible_output_w` only from the retained CP339 latest snapshot after bit-exact parity with the supplied predecessor and private witness plus recursive lineage validation, and extracts finite nonnegative `maximum_total_cooling_capacity_w` only from the retained same-call CP321 latest snapshot after bit-exact private-witness parity and recursive validation; CP321 aggregate reads may exceed `A`. Caller/model/sized-limit re-reads, live services, and numerical-DTO inputs are excluded. Raw IEEE binary64 `>=` makes NaN false, `+infinity` true, and `-infinity` false against the finite nonnegative maximum. Signed-zero capacity bits are characterized, but CP321's zero reset prevents the complete public active chain from reaching either signed zero; active zero forgery is rejected and the real skip retains null capacity. In the direct no-OA lineage, `+infinity` flow has an exact-zero enthalpy difference, so CP339 produces NaN and CP340 takes the false route. The publicly reachable true nonfinite case instead uses finite positive flow and finite enthalpy operands whose subtraction overflows to `+infinity`; CP339 produces `+infinity` and CP340 enters the body. `-infinity` remains a pure-transition characterization only. Active Rust snapshots preserve the possibly nonfinite operand as `Some(value)`; serde JSON projects its nonfinite numeric field to `null` while a separate IEEE bit string remains non-null. Skips retain Rust `None` and JSON null numeric and bit fields. Direct-only lifecycle evidence preserves CP339-to-CP340-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_guard_lifecycle`; non-direct paths carry no CP340 evidence, and CP340 does not consume, reconcile with, feed, or replace the numerical DTO. Physical line 2199 is the first excluded lexical executable, `CoolSensOutput = PurchAir.MaxCoolTotCap;`; that assignment, later adjustment through line 2203, false continuation at line 2208, and broader capacity/supply/output behavior remain excluded. CP340 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP341 additionally requires the exact direct lane to execute the single two-site Cooling positive-supply maximum-capacity assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2199 after CP340 and before the unchanged numerical DTO: `CoolSensOutput = PurchAir.MaxCoolTotCap;`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites, in order, are `read-retained-maximum-total-cooling-capacity-for-sensible-output-assignment` and `assign-local-cooling-sensible-output-from-maximum-total-cooling-capacity`. This deterministic Rust right-hand-side-read-to-left-hand-side-write witness makes no general claim about C++ built-in `=` evaluation order. Only CP340 `CapacityLimitSensibleOutputAdjustmentBodyEntered` executes both sites. The exact six routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputMaximumCapacityAssigned`. For transitions `T`, inherited counts `U/N/P/G`, CP340 false fallthroughs `F`, CP341 maximum-capacity assignments `M`, and CP340 active guard evaluations `A`, exact state requires `T=U+N+P+G+F+M`, `A=F+M`, `M=CP340 adjustment-body entries`, both source-site counters equal `M`, `source_site_execution_count=2*M`, and witnessed route-counter parity. No invariant equates `M` with `A` or with CP321 aggregate maximum-capacity reads. Exact release takes only `runtime`, selected `system`, and the same-call CP340 predecessor. It obtains the preexisting cooling sensible output and assignment RHS solely from the retained same-call CP340 latest snapshot after bit-exact parity with the supplied predecessor and private witness plus recursive lineage validation. It never reaches through to CP321 or re-reads caller, typed-model, sizing, live-service, or numerical-DTO values. The false CP340 route preserves predecessor output bits without an RHS read or LHS write; the true route reads the retained maximum and assigns that exact binary64 payload. Complete public active lineage requires the maximum finite and strictly greater than zero. Arbitrary IEEE payload copying by the pure transition is characterization only, with no arithmetic, normalization, clamp, finite coercion, or broader public reachability claim. Active false-route NaN remains `Some(value)` for both preexisting and resulting Rust fields; serde JSON projects both numeric fields to `null` while preserving identical non-null IEEE bit strings and keeps the unread maximum numeric and bit fields null. On a publicly reachable true route with a `+infinity` predecessor, JSON keeps the preexisting numeric field null and its IEEE bits non-null, while the finite positive retained maximum becomes the non-null resulting numeric value with matching result bits. Direct-only lifecycle evidence preserves CP340-to-CP341-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_maximum_capacity_assignment_lifecycle`; CP341 does not consume, reconcile with, feed, or replace the unchanged numerical DTO. Non-direct paths carry no CP341 evidence and reject attached evidence. Physical line 2200 is the first excluded lexical executable: `SupplyEnthalpy = MixedAirEnthalpy - CoolSensOutput / SupplyMassFlowRate;`; that calculation, later adjustment through line 2203, false continuation at line 2208, and broader capacity, supply-state, and output behavior remain excluded. CP341 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP342 additionally requires the exact direct lane to execute the single six-site Cooling positive-supply capacity-limit sensible-output supply-enthalpy assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2200 after CP341 and before the unchanged numerical DTO: `SupplyEnthalpy = MixedAirEnthalpy - CoolSensOutput / SupplyMassFlowRate;`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact textual sites are `read-retained-mixed-air-enthalpy-for-supply-enthalpy-difference`, `read-retained-cooling-sensible-output-for-specific-cooling-output-division`, `read-retained-supply-mass-flow-rate-for-specific-cooling-output-division`, `calculate-cooling-sensible-output-divided-by-supply-mass-flow-rate`, `calculate-mixed-air-enthalpy-minus-specific-cooling-output`, and `assign-local-supply-enthalpy`. The three source reads are side-effect-free, so this deterministic Rust/source-text and dependency witness makes no claim about C++ built-in `/`, `-`, or `=` operand-evaluation order; it preserves `MixedAirEnthalpy - (CoolSensOutput / SupplyMassFlowRate)`, division before subtraction, then assignment. Only CP341 `CapacityLimitSensibleOutputMaximumCapacityAssigned` executes all sites. The exact routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyEnthalpyAssigned`. For transitions `T`, inherited `U/N/P/G`, CP340 false fallthroughs `F`, CP342 assignments `H`, CP341 maximum-capacity assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+H`, `A=F+H`, `H=M=CP340 adjustment-body entries`, all three read, two calculation, and one write counters equal `H`, and `source_site_execution_count=6*H`, with witnessed route parity. No invariant equates `H` with `A` or CP321 aggregate maximum-capacity reads. Exact release accepts only `runtime`, selected `system`, and the same-call CP341 predecessor. It requires supplied/latest/private CP341 bit parity and recursive completion, obtains `CoolSensOutput` only from CP341 `resulting_cooling_sensible_output_w` and never CP339's pre-capacity output, and obtains mixed-air enthalpy, supply mass flow, and preexisting supply enthalpy only from the same-call CP339 latest/private witness after exact parity and recursive completion. Caller operands, CP321/model/sizing re-reads, live services, and numerical-DTO inputs are forbidden. The CP340 false route preserves CP339 supply-enthalpy bits without CP342 reads, calculations, or write; the true route executes all six sites. Complete public assignment lineage requires finite mixed-air and preexisting enthalpies, finite strictly positive CP341 cooling output, and finite strictly positive supply flow. Its publicly witnessed nonfinite predecessor uses finite flow: CP339 forms `+infinity`, CP341 caps it to a finite maximum, and CP342 performs the exact divide/subtract from that capped value. A `+infinity` supply flow instead makes the direct lineage CP339 product NaN and reaches the CP340/CP341 false route, so CP342 preserves CP339 `SupplyEnthalpy` without executing an RHS site. The pure transition separately retains raw IEEE binary64 division/subtraction, including signed-zero, NaN, infinity, and overflow characterization without normalization, clamp, fused operation, or derived finite-result coercion. Its `+infinity`-flow positive-zero quotient and overflow-derived `-infinity` are characterization only, not public assignment-reachability claims. Characterized active Rust fields retain nonfinite derived values as `Some(value)`; serde JSON maps their numeric fields to `null` while preserving non-null IEEE bit strings. False-route preexisting/result supply enthalpy remain bit-identical `Some(value)` with unread fields null; inherited skips remain Rust `None` and JSON null. Direct-only lifecycle evidence preserves CP341-to-CP342-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_enthalpy_assignment_lifecycle`; CP342 does not consume, reconcile with, feed, or replace the numerical DTO. Non-direct paths carry no CP342 evidence and reject attached evidence. Physical line 2201 is the first excluded lexical executable: `PurchAir.SupplyTemp = PsyTdbFnHW(SupplyEnthalpy, PurchAir.SupplyHumRat);`; that conversion, line 2203, false continuation at line 2208, and later behavior remain excluded. CP342 adds bounded local supply-enthalpy and lifecycle evidence only and makes no support, run-state, required/forbidden-feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP343 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output supply-temperature assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2201 after CP342 and before the unchanged numerical DTO: `PurchAir.SupplyTemp = PsyTdbFnHW(SupplyEnthalpy, PurchAir.SupplyHumRat);`; the locked raw source SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact sites are `read-local-supply-enthalpy-for-dry-bulb-inversion`, `read-purchased-air-supply-humidity-ratio-for-dry-bulb-inversion`, `evaluate-psy-tdb-fn-h-w`, and `assign-purchased-air-supply-temperature`; both reads are side-effect-free, so the deterministic witness makes no C++ function-argument-evaluation-order claim. Canonical inline `PsyTdbFnHW` is locked to `Psychrometrics.hh` physical lines 743-762 and raw SHA-256 `30C9575BC5A8E73D33D111E0D54A4DA8916AF4534175E9B95071ACA2513AEF45`; it forms `W=max(dW,1.0e-5)` with first-argument NaN semantics and returns exactly `(H - 2.50094e6 * W) / (1.00484e3 + 1.85895e3 * W)`. Rust uses `energyplus_psy_tdb_fn_h_w` with exact grouping and no cache, diagnostic, state, reassociation, normalization, clamp, or fused operation. Only CP342 `CapacityLimitSensibleOutputSupplyEnthalpyAssigned` executes all sites. Routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyTemperatureAssigned`. For `T`, inherited `U/N/P/G`, CP340 false `F`, CP343 assignments `D`, CP342 assignments `H`, CP341 assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+D`, `A=F+D`, `D=H=M=CP340 adjustment-body entries`, each site counter equals `D`, and `source_site_execution_count=4*D`, with witnessed parity; no `D=A` or CP321 aggregate-read invariant is added. Exact release takes only `runtime`, selected `system`, and same-call CP342, requiring supplied/latest/private CP342 bit parity and recursive completion. CP342 `resulting_supply_enthalpy_j_per_kg` solely owns enthalpy, never CP336 pre-capacity enthalpy; same-call CP335 `assigned_supply_humidity_ratio` owns humidity and same-call CP334 `assigned_supply_temperature_c` owns preexisting temperature. Retained CP336 latest/private lineage corroborates those two values without replacing either owner. Caller/model/sizing/Zone re-reads, live services, and numerical-DTO inputs are forbidden. CP340 false preserves CP334 temperature bits without CP343 sites; inherited skips retain null operands/results. Active humidity is finite and `>=0.0` and preexisting temperature is finite, while no new finite gate is added to exact CP342 enthalpy or the helper result. Pure floor, signed-zero, NaN, infinity, overflow, numerator/denominator, and division behavior is defensive characterization, not a new full-public-chain reachability claim. Characterized nonfinite Rust fields stay `Some`; JSON maps numeric values to null and preserves IEEE bits. Direct-only lifecycle evidence preserves CP342-to-CP343-to-unchanged-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_temperature_assignment_lifecycle`; non-direct paths reject CP343 evidence. Line 2202 is commentary; line 2203 is the first excluded executable, `PurchAir.SupplyTemp = min(PurchAir.SupplyTemp, PurchAir.MixedAirTemp);`. CP343 adds only bounded inverse/lifecycle evidence, keeps `routine.psy_tdb_fn_h_w` `state_mapped`, both parent routines `source_mapped`, both parent algorithms `scaffold`/`none`, and makes no support, run-state, feature/evidence, numerical-conformance, capability, count, output, status, conformance, or Roadmap promotion. CP344 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output supply-temperature mixed-air limit at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2203 after CP343 and before the unchanged numerical DTO: `PurchAir.SupplyTemp = min(PurchAir.SupplyTemp, PurchAir.MixedAirTemp);`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact sites are `read-purchased-air-supply-temperature-for-minimum`, `read-purchased-air-mixed-air-temperature-for-minimum`, `apply-source-shaped-two-argument-minimum`, and `assign-purchased-air-supply-temperature`; both reads are side-effect-free, so deterministic Rust/source-text order makes no C++ function-argument-evaluation-order claim. EnergyPlus imports ObjexxFCL's exact two-double `a < b ? a : b` minimum; Rust uses `if left < right { left } else { right }`, so strict true selects the CP343 left operand while ties and unordered comparisons select the CP329 right operand bit-for-bit, with no `f64::min`, total/partial ordering, clamp, normalization, finite coercion, cache, diagnostic, or mutable service state. Only CP343 `CapacityLimitSensibleOutputSupplyTemperatureAssigned` executes all sites. Routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyTemperatureMixedAirLimitExecuted`. For `T`, inherited `U/N/P/G`, CP340 false `F`, CP344 executions `L`, CP343 assignments `D`, CP342 assignments `H`, CP341 assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+L`, `A=F+L`, `L=D=H=M=CP340 adjustment-body entries`, every site counter equals `L`, and `source_site_execution_count=4*L`, with witnessed parity; no `L=A` or CP321 aggregate-read invariant is added. Exact release takes only `runtime`, selected `system`, and same-call CP343, requiring supplied/latest/private CP343 bit parity and recursive completion. CP343 `resulting_supply_temperature_c` solely owns the left/preexisting operand; retained same-call CP329 latest/private `mixed_air_temperature_c` solely owns the finite right operand. CP334 and CP336 temperature evidence remains recursive lineage/corroboration and replaces neither source owner. Caller/model/sizing/Zone re-reads, live services, and numerical-DTO inputs are forbidden. CP340 false preserves CP343 result bits, recursively preserving CP334 temperature, without CP344 sites; inherited skips retain null operands/results. No new finite gate is added to exact CP343 left or selected result. Pure strict-`<` signed-zero, NaN, infinity, tie, and unordered behavior is defensive characterization, not a new full-public-chain reachability claim. Characterized nonfinite Rust fields stay `Some`; JSON maps numeric values to null and preserves IEEE bits. Direct-only lifecycle evidence preserves CP343-to-CP344-to-unchanged-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_temperature_mixed_air_limit_lifecycle`; non-direct paths reject CP344 evidence. Physical lines 2204-2207 are non-executable closing delimiters, blank, and commentary; physical line 2208 is the first excluded executable, `PurchAir.SupplyHumRat = PurchAir.MixedAirHumRat;`. CP344 adds only bounded limit/lifecycle evidence, keeps `routine.psy_tdb_fn_h_w` `state_mapped`, both parent routines `source_mapped`, both parent algorithms `scaffold`/`none`, and makes no support, readiness, run-state, feature/evidence, numerical-conformance, capability, algorithm/routine-count, output, status, conformance, or Roadmap promotion. |
| ideal_loads_finite_limits | ideal_loads | compatibility | supported_compatibility_run | `ZoneHVAC:IdealLoadsAirSystem`<br>`ZoneHVAC:EquipmentConnections`<br>`ZoneHVAC:EquipmentList`<br>`ZoneControl:Thermostat`<br>`ThermostatSetpoint:DualSetpoint` | `OutdoorAir`<br>`Economizer`<br>`HeatRecovery`<br>`Autosizing`<br>`EMS`<br>`PythonPlugin` | `ideal_loads_zone_equipment_purchased_air_source_order`<br>`ideal_loads_air_system` | `ideal_loads_capacity_limit_conformance_001`<br>`ideal_loads_flow_capacity_limit_conformance_001`<br>`ideal_loads_flow_limit_conformance_001` | Arbitrary release compatibility is limited to explicit finite nonnegative hard sizes inside the CP303 exact direct-Zone binding and its state-backed SourceSetpointThresholds loop. Numeric capacity, flow, and flow+capacity conformance evidence remains limited to declared cases; Autosize or unresolved sizes remain unsupported when they trigger blocking validation, while otherwise-valid out-of-binding fixed-demand adapters are diagnostic-only. CP305 additionally requires the exact direct release lane to execute persistent single-unit InitPurchasedAir state, prove one hard-size and BeginEnvrn initialization, and consume its cached finite-flow mass limits; diagnostic fallback still carries no release lifecycle evidence. CP306 additionally builds an immutable TypedModel declaration-order manager plan, rejects active plenum topology before runtime-state mutation, and eagerly snapshots membership outcomes in retained Zone declaration order through each Zone EquipmentConnection and its referenced list entry order, ignoring unreferenced list objects. Until Zone equipment inputs are filled, only the runtime latch and outcome recording are deferred; the first ready call latches before one infallible declaration-order replay. That replay retains membership results, ordered missing-membership diagnostics without fail-fast, immutable plan validation, and selected-unit work afterward. The matched-list ID is Rust diagnostic evidence; this InitPurchasedAir call observes only CheckZoneEquipmentList's Boolean return and does not request optional CtrlZoneNum. Release support remains the exact one-unit direct-Zone binding. Active plenum arrays/barriers, exhaust/multiple-return behavior, Autosize/SizePurchasedAir, multi-unit release dispatch/residual/results, exact message registry, reset/concurrency, broader environment/warmup/adaptive lifecycle, and full Init parity remain outside this claim. CP307 additionally requires an immutable typed selected-unit topology plan and source-ordered one-time transition after the manager sweep. The transition commits its latch before supply validation, preserves invalid-exhaust Severe before return fallback, one-return assignment, the multiple-return warning-without-assignment quirk, zero-return fatal, and the nonfatal-outcome-only OA/economizer advisory as structured lifecycle evidence. Release validation still requires blank IdealLoads exhaust, no Zone exhaust topology, exactly one Zone return, no OA, `SingleZoneReturn`, exact bound identities, one completion, and no topology diagnostic, failure, or advisory. Explicit/invalid exhaust, multiple/zero returns, and economizer branches are direct lifecycle tests only and do not remove `OutdoorAir` or `Economizer` from the forbidden features. The C++ zero-supply bypass is outside this typed required-supply subset, and retained fatal replay is a Rust fail-closed divergence rather than retry parity. Plenum ownership, full sizing/autosizing, multi-unit execution, broader OA behavior, exact messages, wrong-ControlledZone replay, and reset/concurrency remain excluded; capability level and evidence cases are unchanged. CP308 additionally requires the exact release lane to execute the bounded direct-hard-sized `SizePurchasedAir` legacy route after selected-unit topology and before `BeginEnvrn`. The route requires a positive current Zone-equipment slot, no `DesignSpecification:ZoneHVAC:Sizing` binding, no completed Zone sizing run, no `AutoSize` value, and finite nonnegative direct fields. It retains a source-ordered four-field overlay in persistent per-unit state, clears the Init size latch only after normal child return, derives environment flow caches from that overlay, and threads the same capacities into Calc. The source `CurZoneEqNum <= 0` suppression is characterized by direct lifecycle tests but is not release-admitted. This changes lifecycle evidence, not the support envelope or numerical evidence: Autosizing remains forbidden, evidence cases and support level are unchanged, and custom/scalable sizing, design days, Zone sizing arrays and `FinalZoneSizing`, complete sizer calculations, emitted sizing reports and warnings, EMS and shared scratch, child-error and partial-side-effect parity, retry/reset/concurrency, and full `SizePurchasedAir` or `InitPurchasedAir` parity remain excluded. CP309 additionally executes the bounded post-BeginEnvrn supply-temperature diagnostic suffix in cooling-before-heating order, allocates or reuses relative recurring identities, aggregates call count and reported extrema, and exposes the registry through lifecycle JSON. Exact release validation requires every diagnostic identity, event, index, and count to remain zero, so active diagnostics remain direct lifecycle evidence only. Actual message/report sinks, schedule-service effects, exact EnergyPlus process-global registry parity, warmup/sizing counters, broader multi-unit release execution, and reset/concurrency remain excluded; support level, evidence cases, and forbidden features are unchanged. CP310 additionally requires the exact release lane to execute the bounded `CalcPurchAirLoads` entry prefix after each successful Init and before the existing calculation. It records caller-supplied resolved node identities, an exact 12-target zero-result snapshot while actually clearing the three retained fields, heating-then-cooling Remaining demand, availability-manager ordering, unconditional read-site evidence from pre-sampled overall/heating/cooling values, independent mode gates, and the line-2022 `UnitOn` body decision. Release validation requires one prefix call/reset/demand/read-site group plus one normally preallocated ZoneComp `NoAction` visit/Zone write/status copy per coupling, zero `ForceOff`, heating and cooling values/on-counts equal to one/call count, exact bound latest identities, and JSON lifecycle evidence; non-direct runtimes reject this evidence. Line 2023 onward, live schedule/ZoneComp service ownership, OA/EMS/mode selection and all downstream Calc behavior remain outside the claim. Support level, run state, forbidden features, evidence cases, numerical conformance, and full lifecycle status are unchanged. CP311 supersedes CP310's line-2023 exclusion only for the `CalcPurchAirLoads` parent prefix at lines 2023-2040 and the bounded no-OA child route at lines 2781, 2783, 2785, and 2806-2809. The exact release lane consumes the retained CP310 gate in one-for-one order, records a Zone heat-balance reference binding without reading its fields, calls the child and rewrites retained pre-EMS minimum OA flow to zero only for an entered body, reads false EMS and `OutdoorAir` predicates, and records zero sensible and moisture effects; UnitOff skips every body site. Release validation requires reconciled active/UnitOff, child, retained-write, predicate-read, zero-effect, and latest-state evidence plus zero EMS applications, OA effects, and psychrometric calls in direct-only JSON. `OutdoorAir` and `EMS` remain forbidden. Full minimum-OA child services, OA-true/EMS numerics, operating-mode selection at line 2046 and later, support level, run state, evidence cases, and numerical conformance remain unchanged. CP312 supersedes CP311's line-2046 exclusion only for the lines 2046-2047 cooling-entry gate. The exact release lane consumes linked CP310/CP311 snapshots, compares zero minimum-OA sensible effect against finite cooling demand with inclusive `>=`, conditionally records the prevalidated DualHeatCool value after short-circuiting, records no SingleHeat block, and makes a local Cooling assignment exactly when the existing numerical DTO returns Cooling; UnitOff skips the gate. Direct lifecycle JSON reconciles source/UnitOff and cooling/fallthrough partitions, conditional reads, assignments, numerical Cooling counts, and latest state, while non-direct evidence is rejected. This is lifecycle evidence rather than a new numerical or thermostat-family claim. At the CP312 boundary, the cooling body at line 2056, Heat/DeadBand selection at line 2348, live temperature-control service, non-Dual release routes, support level, run state, required or forbidden features, evidence cases, and numerical conformance remain unchanged. CP313 supersedes only that line-2056 exclusion for the lines 2056-2057 cooling OA maximum-flow guard. Exact release preserves nested selector and flow-read short-circuiting across NoLimit, Capacity, FlowRate, and FlowRateAndCapacity; the selected-flow routes compare no-OA `+0.0` with a finite nonnegative initialized maximum, so the excluded warning/reduction body is never entered. Direct lifecycle JSON reconciles CP312 Cooling/UnitOff/non-cooling, selector/read, strict-comparison, and zero-body-entry evidence while non-direct evidence is rejected. This is guard-only lifecycle evidence, not OA, economizer, warning, clamp, numerical, or new finite-limit conformance support. At the CP313 boundary, line 2058 and later behavior, support level, run state, required and forbidden features, evidence cases, and numerical conformance remain unchanged. CP314 supersedes only that line-2058 exclusion for the true body at lines 2058-2078. Direct characterization converts parent outdoor-air mass flow with `StdRhoAir`, retains the zero-initialized first/recurring warning counter and relative index, records first Warning/Continue/timestamp versus later recurring calls with max-only `OAVolFlowRate` and empty maximum units, and clamps local outdoor-air mass flow after either branch. Exact no-OA release never enters the CP313 body, so direct-only lifecycle JSON reconciles zero CP314 density reads, conversions, warnings, indices, and clamps while non-direct evidence is rejected. Line 2082 is the lexical first excluded executable and the dynamic continuation only when CP312 entered cooling and CP313 was false; a CP314-true path instead skips the sibling `else` and next reaches line 2109. This remains structured lifecycle evidence, not actual warning output, OA/economizer behavior, clamp-enabled release numerics, or new finite-limit conformance. Support level, run state, required and forbidden features, evidence cases, and numerical conformance remain unchanged. CP315 supersedes only CP314's line-2082 exclusion for the outer `EconomizerType != NoEconomizer` guard. Public direct release admits exact UnitOff, non-cooling, or active CP313-false fallthrough partitions; the CP313-true/CP314-body skip is pure/internal characterization only. UnitOff and non-cooling skip the guard, while the active fallthrough reads the typed enum once. Exact no-OA release requires `NoEconomizer`, so each active comparison is false and the line-2083 inner condition has zero entries. Internal DifferentialDryBulb/DifferentialEnthalpy characterization stops at that boundary and remains non-release. Direct-only lifecycle JSON reconciles the predecessor partitions, enum reads, comparisons, false results, and zero inner entries; non-direct evidence is rejected. The active true route begins at excluded line 2083, while false and CP314-true routes continue at excluded line 2109. Inner node state, psychrometrics, supply/OA flow effects, `EconoOn`, `TimeEconoActive`, and later Calc numerics remain outside this slice. This does not connect or promote the separate outdoor-air economizer helper: `OutdoorAir` and `Economizer` remain forbidden for the exact direct binding, and support level, run state, required features, evidence cases, and numerical conformance remain unchanged. CP316 supersedes only CP315's line-2083 exclusion for the executable lines 2083-2086 compound cooling economizer condition. Internal characterization preserves the source's repeated typed enum read, left-to-right `&&`/`\|\|` short-circuiting, conditional pre-sampled temperature and stored-enthalpy reads, and strict `<` comparisons. Exact no-OA release always consumes CP315's false `NoEconomizer` result and proves one complete CP316 source-site skip per CP315 transition, with zero selector enum reads, node reads, comparisons, satisfied predicates, or line-2089 body entries. Lines 2087-2088 are non-executable; true continues at the first excluded executable line 2089 and false at line 2109. CP316 does not sample live Node state, recompute enthalpy, call the separate outdoor-air economizer helper, or implement psychrometric and flow effects. This is condition-only lifecycle evidence, not live OA/economizer ownership or numerical support; `OutdoorAir` and `Economizer` remain forbidden, and support level, run state, required features, evidence cases, and numerical conformance remain unchanged. CP317 supersedes only CP316's line-2089 exclusion for the cooling economizer true body at executable lines 2089-2101. Internal characterization preserves all 37 source sites: the Zone humidity-ratio read, canonical `energyplus_psy_cp_air_fn_w` scalar result and explicit `CpAir` assignment; temperature reads, subtraction, `DeltaT` assignment and gate re-read; left-associated divisions with local `CpAir`/`DeltaT` re-reads and initial supply assignment; repeated `CoolingLimit` reads and selector short-circuit; the maximum-flow guard read followed by the clamp-time maximum upper-bound re-read; the inner maximum, outer minimum, and clamped assignment; the resulting supply-flow read followed by outdoor-air flow; and the activation-time supply-flow re-read before ordered `EconoOn`, outdoor-air-flow, system-timestep, and `TimeEconoActive` effects over pre-sampled scalars. This pure psychrometric characterization covers the scalar result only; source static `dwSave`/`cpaSave` state, the initial `dwSave == -100.0` sentinel, cache-hit/miss identity, and concurrent cache lifecycle remain excluded. Exact no-OA release consumes a CP316 predecessor with zero calculation-body entries, so every CP317 transition is a complete all-sites skip with zero reads, calculations, comparisons, clamps, assignments, or timestep effects. Line 2109 is the first excluded executable after the mapped body and intervening closing delimiters/comments. The separate outdoor-air economizer helper remains disconnected. This is lifecycle evidence, not live Node/OA/economizer ownership or numerical support; `OutdoorAir` and `Economizer` remain forbidden, and support level, run state, required features, evidence cases, numerical conformance, and Roadmap state remain unchanged. CP318 additionally requires the exact direct lane to execute the 19-site common Cooling continuation at `PurchasedAirManager.cc` lines 2109-2116 after CP317 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets the candidate to positive zero and exact release proves retained CP310 `CoolOn=true`, consumes retained `QZnCoolSP` plus identity-checked live Zone humidity ratio and temperature, evaluates canonical `PsyCpAirFnW`, applies strict `DeltaT < -1.0e-5`, and preserves left-associated `(QZnCoolSP / CpAir) / DeltaT`, raw IEEE values, and signed zero. Direct-only lifecycle evidence reconciles all sites. The source psychrometric static cache, line 2119 and later humidity/EMS/limit/mixed-air behavior, live schedule or general Node ownership, and numerical-result reconciliation remain excluded. This changes no support level, run state, required or forbidden features, evidence case, or numerical conformance; `OutdoorAir`, `Economizer`, and `EMS` remain forbidden and broad humidity control remains outside the claim. CP319 additionally requires the exact direct lane to execute the 21-site Cooling dehumidification-flow candidate at `PurchasedAirManager.cc` lines 2119-2128 after CP318 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets positive zero, reads retained `CoolOn`, and exact direct `DehumidificationControlType::None` proves selector fallthrough without requesting live moisture-demand or Node-humidity services. Private Humidistat characterization alone preserves local assignments and rereads, strict left-to-right `DeltaHumRat < -0.00025 && MdotZnDehumidSP < 0.0`, short-circuiting, raw division, IEEE values, and signed zero. CP319 does not reuse the broader humidity helper or reconcile its candidate with the numerical DTO. Line 2133 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; broad humidity control remains outside the claim. CP320 additionally requires the exact direct lane to execute the 26-site Cooling humidification-flow candidate at `PurchasedAirManager.cc` lines 2133-2144 after CP319 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling resets positive zero, reads retained same-call `HeatOn`, and exact direct `HumidificationControlType::None` proves the outer selector fallthrough without reading the nested dehumidification selector or requesting live moisture-demand or Node-humidity services. Private Humidistat characterization alone preserves the lazy repeated `DehumidCtrlType == Humidistat \|\| DehumidCtrlType == None` reads, local assignments and rereads, strict left-to-right `DeltaHumRat > 0.00025 && MdotZnHumidSP > 0.0`, short-circuiting, raw division, IEEE values, and signed zero. CP320 does not reuse the broader humidification helper or reconcile its candidate with the numerical DTO. Line 2147 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; broad humidity control remains outside the claim. CP321 additionally requires the exact direct lane to execute the ten-site Cooling capacity-zero flow reset at `PurchasedAirManager.cc` lines 2147-2152 after CP320 and before the unchanged numerical DTO. UnitOff and non-cooling skip the slice; active Cooling preserves lazy repeated `CoolingLimit == Capacity \|\| CoolingLimit == FlowRateAndCapacity` reads, reads retained hard-sized `MaxCoolTotCap` only after a selector match, and compares exact `== 0.0`. A true result assigns positive zero in source order to all three flow candidates; false paths preserve retained same-call CP318 cooling, CP319 dehumidification, and CP320 humidification candidate bits. Exact direct release consumes those three candidate snapshots plus retained model/Init state without live services or numerical-DTO reconciliation. Its pure source transition does not reuse the broader `cooling_capacity_limit_is_zero` helper, perform a finite check, normalize, or clamp; existing public direct admission still rejects unresolved or nonfinite selected sized values before the transition. Line 2155 and later behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP322 additionally requires the exact direct lane to execute the six-site Cooling supply mass-flow maximum at `PurchasedAirManager.cc` executable line 2155 after CP321 and before the unchanged numerical DTO. The lexical sites read retained outdoor-air, cooling, dehumidification, and humidification mass flows, apply the source-shaped five-argument maximum with a positive-zero floor, and assign `SupplyMassFlowRate`; they do not claim a C++ function-argument evaluation order. UnitOff and non-cooling skip the slice. Active Cooling preserves ObjexxFCL's strict-`<` pair tree, left-first tie or unordered selection, and exact signed-zero, NaN, and infinity behavior without Rust floating-point maximum helpers, total ordering, finite checks, normalization, or clamps. Exact direct release consumes post-CP321 candidate results plus retained same-call CP311 pre-EMS outdoor-air state, requiring bitwise positive zero on the no-OA route without duplicate caller scalars, live services, or numerical-DTO reconciliation. Line 2157 and later EMS override, flow-clamp, mixed-air, capacity, and supply-state behavior remain excluded. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP323 additionally requires the exact direct lane to execute the three-site Cooling supply mass-flow EMS override guard at `PurchasedAirManager.cc` executable line 2157 after CP322 and before the unchanged numerical DTO. The sites read `EMSOverrideMdotOn`, evaluate the Boolean guard, and enter the override body when enabled; UnitOff and non-cooling skip the slice. Private characterization preserves both outcomes, while exact direct release validates the completed same-call CP322 snapshot and private witness and requires the false EMS-disabled route, recording one read and evaluation, zero body entries, and no EMS actuator-service request per active transition. Line 2158 is the first excluded executable; the true body at lines 2158-2159, `EMSValueMassFlowRate`, supply and outdoor-air assignments, `min`, the line-2161 flow-limit condition, and later flow behavior remain excluded. `EMS` remains forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP324 additionally requires the exact direct lane to execute the complete six-site Cooling supply mass-flow EMS override body at `PurchasedAirManager.cc` executable lines 2158-2159 after CP323 and before the unchanged numerical DTO. The sites read `EMSValueMassFlowRate`, assign supply flow, read outdoor-air and overridden supply flow as lexical minimum operands, apply the source-shaped two-argument minimum, and assign outdoor-air flow. Private true-body characterization preserves strict-`<` comparison and right-operand selection on a tie or unordered result without claiming C++ function-argument evaluation order. UnitOff and non-cooling skip the slice; exact direct release validates the completed same-call CP323 latest snapshot and private witness and requires its false EMS-disabled route, so active direct transitions also complete-skip all six sites with zero body execution, value or flow reads, assignments, minimum evaluations, or EMS service requests. Line 2161 is the first excluded executable; its cooling flow-limit condition and assignment, the line-2166 very-small-flow clamp, and later mixed-air, capacity, and supply-state behavior remain excluded. No EMS field or service is added and `EMS` remains forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit and Autosize support remain unchanged. CP325 additionally requires the exact direct lane to execute the complete seven-site Cooling supply mass-flow limit guard at `PurchasedAirManager.cc` executable lines 2161-2162 after CP324 and before the unchanged numerical DTO. UnitOff and non-cooling skip every site. Active Cooling preserves lazy repeated `CoolingLimit == FlowRate \|\| CoolingLimit == FlowRateAndCapacity` reads, reads retained `MaxCoolMassFlowRate` only after a selector match, compares strict `> 0.0`, and records whether the deliberately excluded clamp body is entered. Exact direct release validates the completed same-call CP324 latest snapshot and private witness and requires its EMS-disabled complete skip, then obtains the selector from the selected typed system and the maximum from the bit-validated retained Init cache without duplicate caller scalars or live sizing, schedule, Node, psychrometric, EMS, or diagnostic services. A selected positive maximum may record body entry, but CP325 performs no supply-flow read, minimum, assignment, or numerical-DTO reconciliation. Line 2163 is the first excluded executable; the actual supply-flow clamp, line-2166 very-small-flow guard/reset, and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP326 additionally requires the exact direct lane to execute the complete four-site Cooling supply mass-flow limit body at `PurchasedAirManager.cc` executable line 2163 after CP325 and before the unchanged numerical DTO. The sites read retained supply and maximum cooling mass flows as textual minimum operands without claiming C++ function-argument evaluation order, apply ObjexxFCL's source-shaped `a < b ? a : b` two-argument minimum, and assign the result to retained source-local supply flow. UnitOff, non-cooling, and active CP325 guard-false transitions skip every site; only a true body entry executes the body. Private characterization preserves strict-`<` comparison, right-operand selection on tie or unordered values, and exact signed-zero, NaN, and infinity behavior. Exact direct release validates the completed same-call CP325 latest snapshot and private witness, obtains the pre-clamp supply flow from the bit-validated retained CP322 result through unchanged CP323-CP325 lineage, and re-reads the maximum from the retained Init cache without duplicate caller scalars or live sizing, schedule, Node, psychrometric, EMS, or diagnostic services. Coupled and pipeline validation require bit-exact retained CP322-result and Init-cache provenance plus internal CP326 minimum/assignment consistency; they do not reconcile this line-2163 checkpoint with the final numerical DTO because that DTO represents line-2166-and-later downstream work. CP326 does not consume that DTO as input and does not feed or replace it. Line 2166, not line 2167, is the first excluded executable; its very-small-flow guard, line-2167 positive-zero reset, and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP327 additionally requires the exact direct lane to execute the complete four-site Cooling supply mass-flow very-small-flow guard at `PurchasedAirManager.cc` executable line 2166 after CP326 and before the unchanged numerical DTO. The sites read retained supply flow and the `HVAC::VerySmallMassFlow` constexpr as textual relational operands without claiming a C++ built-in relational-operand evaluation order, apply the source `<=`, and record reset-body entry. The operands are side-effect-free, so the observable semantics are only the binary64 `<=` result. UnitOff and non-cooling skip every site; every active Cooling CP326 predecessor evaluates the guard. Equality, both signed zeros, and negative infinity enter the body, while positive infinity and NaN fall through. The threshold is pinned to EnergyPlus 26.1 `DataHVACGlobals.hh:89`, exact `Real64 constexpr VerySmallMassFlow(1.0E-30)` with binary64 bits `0x39b4484bfeebc2a0`. Exact direct release validates the completed same-call CP326 latest snapshot and private witness and obtains the operand from CP326's bit-exact resulting supply flow without a duplicate caller scalar or live sizing, schedule, Node, psychrometric, EMS, or diagnostic service. Coupled and pipeline validation require retained post-CP326 supply provenance plus internal CP327 threshold, comparison, and body-entry consistency; they do not consume, reconcile, feed, or replace the final numerical DTO. Line 2167 is the first excluded executable; its positive-zero assignment and later mixed-air, capacity, and supply-state behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP328 additionally requires the exact direct lane to execute the single-site Cooling supply mass-flow positive-zero reset body at `PurchasedAirManager.cc` executable line 2167 after CP327 and before the unchanged numerical DTO. The one lexical site assigns source `0.0` to retained `SupplyMassFlowRate`, producing exact binary64 positive zero with IEEE bits `0x0000000000000000`; it does not re-read the line-2166 operands, repeat `<=`, normalize, invoke a minimum or maximum helper, or apply another clamp. UnitOff and non-cooling skip the assignment and retain no local supply-flow result. An active CP327 true route performs the assignment, including for `-0.0`, equality, finite below-threshold values, and negative infinity, while an active guard-false route skips it and preserves predecessor bits exactly, including positive infinity or a NaN payload. Exact direct release validates the completed same-call CP327 latest snapshot and private witness and obtains body entry and pre-assignment flow only from that bit-exact predecessor without a duplicate caller scalar or live sizing, schedule, Node, psychrometric, EMS, or diagnostic service. Coupled and pipeline validation require exact predecessor, assignment, and result-bit consistency; they do not consume, reconcile, feed, or replace the final numerical DTO. Line 2168 is an excluded non-executable closing delimiter, and the `CalcPurchAirMixedAir` call beginning at line 2171 is the first excluded executable; mixed-air, capacity, supply-state, and later behavior remain excluded. `EMS` and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, or numerical conformance; finite-limit support remains unchanged. CP329 additionally requires the exact direct lane to execute the complete Cooling `CalcPurchAirMixedAir` call at `PurchasedAirManager.cc` lines 2171-2178 plus only the bounded direct no-OA child fallback. Nine textual caller sites bind/read the state, selected unit, retained no-OA flow, CP328 supply result, three distinct mixed-air output references, and Cooling mode, then invoke and normally return from the child without claiming C++ argument evaluation order. UnitOff and non-cooling skip this Cooling call; every active Cooling predecessor calls once even for zero supply flow. Exact release validates same-call CP328, `OAMassFlowRate=+0.0`, selected identity, Cooling mode, and coherent finite recirculation state. The child performs no OA-node or psychrometric work, copies recirculation T/W and a coherent enthalpy projection to mixed outputs, zeros both heat-recovery outputs, and leaves heat-recovery active time unchanged. The coherent projection does not claim parity for arbitrary independently stored Node enthalpy, torn/late reads, aliases, active OA/recovery and saturation/cache/diagnostics, failure/replay, or concurrency; the broader outdoor-air mixed-state helper is not the exact child. Direct-only lifecycle evidence preserves CP328-to-CP329-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2183 is the first excluded executable after comments at lines 2180-2182; the Heat/DeadBand call at 2454-2461 and later work remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, or Roadmap state; both Calc routines remain `source_mapped`. CP330 additionally requires the exact direct lane to execute the complete three-site Cooling positive-supply guard at `PurchasedAirManager.cc` executable line 2183 after CP329 and before the unchanged numerical DTO. UnitOff and non-cooling skip it. Every active Cooling predecessor reads retained CP329 `SupplyMassFlowRate` and evaluates strict `> +0.0` once, then executes the conditional body-entry site only when true, yielding `2 * active + positive_body_entries` dynamic site executions; the literal is not another site and no C++ relational-operand order is claimed. Raw IEEE semantics make signed zeros, negative finite values, negative infinity, and NaN false and positive finite values and positive infinity true without finite checks, normalization, total ordering, or clamps. Exact release consumes only the same-call CP329 direct no-OA snapshot and witness; CP327/CP328 direct lineage has already canonicalized ordered values at or below `HVAC::VerySmallMassFlow` to positive zero while an unordered NaN can remain bit-exact. Direct-only lifecycle evidence preserves CP329-to-CP330-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2184 is commentary; line 2185 is the first excluded lexical executable and true-body executable. Lines 2185-2337, the 2339-2345 false body whose dynamic next executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, and Autosizing remain forbidden. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, or Roadmap state; both Calc routines remain `source_mapped`. CP329/CP330 fail-closed retained-chain hardening additionally requires lifecycle admission to prove the full retained predecessor chain. CP329's runtime-aware completed proof recursively validates completed CP328 through its latest/private witness and upstream prefix, while completed and pending CP329 UnitOff/non-cooling/active histories must match CP328 exactly; a route-aware checked-arithmetic preflight covers every next counter and caller/child source-site increment before mutation. CP330 completed and pending history must match CP329 exactly and recursively retain that CP328-chain proof, so coordinated CP329/CP330 counter or witness corruption cannot bypass CP328. Chain, history, witness, and overflow failures leave runtime state and private witnesses unchanged. This adds no numerical DTO consumption or reconciliation and changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state. CP331 additionally requires the exact direct lane to execute the single three-site Cooling positive-supply `CpAir` assignment at `PurchasedAirManager.cc` executable line 2185 after CP330 and before the unchanged numerical DTO. The sites read controlled Zone heat-balance `airHumRat`, evaluate canonical `PsyCpAirFnW`, and assign local `CpAir`. UnitOff, non-cooling, and CP330 active guard-false routes skip every site; a CP330 positive-body entry executes all three, so dynamic source-site executions equal `3 * positive_supply_mass_flow_body_entries`. Exact release conditionally reads the identity-checked live Zone humidity and requires its bits to match CP329's retained no-OA recirculation- and mixed-air humidity copies, which are same-call lineage evidence rather than substitute source operands. Positive-route admission requires the live humidity to be finite and `>= 0.0` (both signed zeros are admitted) and the canonical `CpAir` result to be finite. It recursively validates CP330 latest/private-witness and route history through the retained predecessor chain and preflights every conditional source and private-witness increment transactionally. Rust claims only the existing stateless canonical scalar on the admitted physical-domain route. The C++ program-wide `dwSave`/`cpaSave` cache, `-100.0` sentinel and raw-`-100.0` first-call anomaly, hit/miss/reset/interleaving/concurrency behavior, all-`f64` cache equivalence, and full `PsyCpAirFnW` parity remain excluded. Direct-only lifecycle evidence preserves CP330-to-CP331-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Line 2186 is the first excluded lexical executable on the true route; lines 2186-2337, the 2339-2345 false body whose dynamic next executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and supply-temperature/capacity behavior remain unpromoted. This changes no support level, run state, required or forbidden feature, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP332 additionally requires the exact direct lane to execute the single eight-site Cooling positive-supply temperature arithmetic and assignment at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2186 after CP331 and before the unchanged numerical DTO. The sites read retained `QZnCoolSP`, read local CP331 `CpAir`, re-read retained CP330 `SupplyMassFlowRate`, form the parenthesized denominator product, divide the load by that product, read controlled Zone Node temperature, add that temperature, and assign `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ built-in operand-evaluation-order claim. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while each CP331 assignment executes all eight, so dynamic source-site executions equal both `8 * cp_air_assignment_count` and `8 * positive_supply_mass_flow_body_entries`. Raw binary64 `QZnCoolSP / (CpAir * SupplyMassFlowRate) + ZoneTemp` grouping and product/division/addition/assignment bits are preserved without reassociation, reciprocal replacement, `mul_add`, normalization, clamping, minimum, or maximum. Exact release consumes retained CP310 demand, the bit-exact CP331 `CpAir`, retained CP330 flow, and only then conditionally reads identity-checked live Zone temperature; CP318 Zone-temperature and CP329 recirculation-/mixed-air-temperature bits remain same-call lineage evidence, not substitute operands. No duplicate caller scalar, psychrometric recomputation, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP331 latest/private witness and predecessor chain, preserves all four predecessor routes, preflights source and witness increments transactionally, and maintains CP331-to-CP332-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2187 is the first excluded lexical executable and CP333 boundary; its minimum-cooling-temperature maximum, the line-2189 mixed-air minimum, humidity/enthalpy assignments, capacity controls, lines 2187-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, count, readiness, routine, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP333 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply temperature minimum-limit statement at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2187 after CP332 and before the unchanged numerical DTO. The sites read retained CP332 `PurchAir.SupplyTemp`, re-read selected-system `PurchAir.MinCoolSuppAirTemp`, apply ObjexxFCL's source-shaped two-argument maximum, and assign the selected value back to `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ function-argument-evaluation-order claim because both operands are side-effect-free. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while every CP332 assignment executes all four, so dynamic source-site executions equal `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`. The imported two-`double` maximum is exactly `a < b ? b : a`: true selects the right minimum-temperature operand and tie or unordered comparison retains the first CP332 operand, preserving operand-order-sensitive signed zero, NaN, and infinity behavior without Rust `f64::max`, total or partial ordering, a finite check, normalization, another clamp, or a minimum. Exact release takes the first operand only from the bit-exact CP332 result, re-reads the second from the identity-checked selected typed system's `minimum_cooling_supply_air_temperature_c`, and treats CP318's retained minimum only as same-call lineage evidence, not a substitute operand. No duplicate caller scalar, live service, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP332 latest/private witness and full predecessor chain, preserves all four routes, preflights source and witness increments transactionally, and maintains CP332-to-CP333-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2188 is commentary; physical line 2189 is the first excluded lexical executable and CP334 boundary. Its mixed-air minimum, line-2190 humidity assignment, line-2191 enthalpy calculation, capacity controls, lines 2189-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP334 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply temperature mixed-air-limit statement at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2189 after CP333 and before the unchanged numerical DTO. The sites read retained CP333 `PurchAir.SupplyTemp`, read retained CP329 `PurchAir.MixedAirTemp`, apply ObjexxFCL's source-shaped two-argument minimum, and assign the selected value back to `PurchAir.SupplyTemp`; this is a textual inventory and makes no C++ function-argument-evaluation-order claim because both operands are side-effect-free. UnitOff, non-cooling, and CP330 active guard-false routes skip every site, while every CP333 minimum-limit assignment executes all four, so dynamic source-site executions equal `4 * supply_temperature_mixed_air_limit_count`, `4 * supply_temperature_minimum_limit_count`, `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`. The imported two-`double` minimum is exactly `a < b ? a : b`: true selects the first CP333 operand and tie or unordered comparison selects the second CP329 operand, preserving operand-order-sensitive signed zero, NaN, and infinity behavior without Rust `f64::min`, total or partial ordering, a clamp, normalization, or finite coercion. Exact release takes the first operand only from the bit-exact CP333 result and takes the second only from the same-call completed CP329 latest/private witness's finite `mixed_air_temperature_c`. CP329 owns the retained mixed-air source field, and the equal no-OA Zone temperature is same-call lineage evidence, not a substitute operand. No duplicate caller scalar, Zone-state re-read, typed-model value, live service, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains the CP333 and CP329 latest/private witnesses and full predecessor chains, preserves all four routes, preflights source and witness increments transactionally, and maintains CP333-to-CP334-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO; non-direct paths carry no CP334 evidence. Physical line 2190 is the first excluded lexical executable and CP335 boundary. Its mixed-air humidity assignment, line-2191 enthalpy calculation, capacity controls, lines 2190-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature limiting, capacity, enthalpy, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP335 additionally requires the exact direct lane to execute the single two-site Cooling positive-supply humidity-ratio mixed-air assignment at the locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2190 after CP334 and before the unchanged numerical DTO. The exact textual sites are `read-purchased-air-mixed-air-humidity-ratio` and `assign-purchased-air-supply-humidity-ratio`, in that order: they read the same-call retained CP329 `PurchAir.MixedAirHumRat` value and assign the identical binary64 payload to `PurchAir.SupplyHumRat`. The statement contains no arithmetic, psychrometric evaluation, minimum, maximum, clamp, normalization, or finite coercion. UnitOff, non-cooling, and CP330 active guard-false routes skip both sites, while every CP334 mixed-air temperature-limit execution performs both, so dynamic source-site executions equal `2 * supply_humidity_ratio_mixed_air_assignment_count`, `2 * supply_temperature_mixed_air_limit_count`, `2 * supply_temperature_minimum_limit_count`, `2 * supply_temperature_assignment_count`, `2 * cp_air_assignment_count`, and `2 * positive_supply_mass_flow_body_entries`; each per-site counter equals the active assignment count. The pure transition copies signed-zero, NaN, and infinity payloads bit-for-bit. Exact release accepts only the CP334 snapshot predecessor and obtains the active RHS only from the same-call completed CP329 latest/private witness's `mixed_air_humidity_ratio`; CP335's local exact snapshot predicate requires that value finite and `>= 0.0` and preserves negative-zero bits; the complete public release chain cannot currently reach that characterization because retained CP332 operand-lineage proof rejects it first. No duplicate caller scalar, Zone-state re-read, typed-model value, live service, psychrometric recomputation, or numerical-DTO input is admitted. Direct-only lifecycle proof recursively retains CP334 and CP329 latest/private witnesses and full predecessor chains, preserves all four routes including `SupplyHumidityRatioMixedAirAssigned`, preflights source and witness increments transactionally, and maintains CP334-to-CP335-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO; non-direct paths carry no CP335 evidence. Physical line 2191 is the first excluded lexical executable and CP336 boundary. Its `PsyHFnTdbW` enthalpy calculation, capacity controls, lines 2191-2337, the 2339-2345 false body whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, and broader supply-temperature, capacity, enthalpy, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP336 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply enthalpy assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2191 after CP335 and before the unchanged numerical DTO; the locked source-file SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact textual sites are `read-purchased-air-supply-temperature-for-enthalpy`, `read-purchased-air-supply-humidity-ratio-for-enthalpy`, `evaluate-psy-h-fn-tdb-w`, and `assign-local-supply-enthalpy`; the side-effect-free argument reads make no C++ function-argument evaluation-order claim. Only CP335's active `SupplyHumidityRatioMixedAirAssigned` route executes CP336. Exact release accepts only the CP335 predecessor, recursively validates completed CP335 and CP334 latest/private witnesses and retained chains, obtains temperature bit-exactly from same-call CP334 `assigned_supply_temperature_c`, and obtains humidity from CP335 `assigned_supply_humidity_ratio`. It evaluates canonical `energyplus_psy_h_fn_tdb_w` with source `max(W, 1.0e-5)` floor behavior; the legacy IdealLoads `moist_air_enthalpy_j_per_kg` helper, duplicate caller scalars, Zone or typed-model re-reads, live services, and numerical-DTO inputs are forbidden. The pure transition characterizes source-shaped IEEE signed-zero, NaN, infinity, floor, grouping, and overflow behavior. Exact direct admission requires finite temperature, finite `>= 0.0` humidity including negative zero, and finite enthalpy; both signed zeros use the positive floor, while CP335's complete public chain cannot currently deliver negative zero because retained CP332 operand-lineage proof rejects it first. UnitOff, non-cooling, and CP330 active guard-false routes retain null operands and result. Each active assignment executes all four sites, so dynamic source-site executions equal `4 * supply_enthalpy_assignment_count`, `4 * supply_humidity_ratio_mixed_air_assignment_count`, `4 * supply_temperature_mixed_air_limit_count`, `4 * supply_temperature_minimum_limit_count`, `4 * supply_temperature_assignment_count`, `4 * cp_air_assignment_count`, and `4 * positive_supply_mass_flow_body_entries`; every per-site counter equals the active count. Four-route parity and checked-arithmetic preflight make identity, ordinal, operand, provenance, history, witness, replay, corruption, nonfinite-result, and overflow rejection transactional. Direct-only lifecycle evidence preserves CP335-to-CP336-to-numerical order without consuming, reconciling with, feeding, or replacing the unchanged numerical DTO; non-direct paths publish `None` and reject CP336 evidence. Physical line 2192 is blank and lines 2193-2194 are comments; physical line 2195 is the first excluded lexical executable and CP337 boundary. Its capacity-limit guard, lines 2195-2337, the zero-flow `else` at 2339-2345 whose first dynamic executable is 2340, the Heat/DeadBand sibling at 2347-2348, its mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, broader capacity and enthalpy behavior, and output behavior remain unpromoted. This changes no support level, run state, feature boundary, evidence case, numerical conformance, capability, algorithm/routine count, readiness, status, output claim, or Roadmap state; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP337 additionally requires the exact direct lane to execute the complete five-site Cooling positive-supply capacity-limit guard at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2195 after CP336 and before the unchanged numerical DTO; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The textual Rust witness sites are `read-cooling-limit-for-capacity-comparison`, `compare-cooling-limit-equal-to-capacity`, `read-cooling-limit-for-flow-rate-and-capacity-comparison-after-first-false`, `compare-cooling-limit-equal-to-flow-rate-and-capacity`, and `enter-capacity-limit-body-if-compound-condition-satisfied`. Enum literals are not additional sites, and the inventory makes no C++ built-in `==` operand-evaluation-order claim. Source `\|\|` short-circuiting reaches the second textual `CoolingLimit` occurrence only after the Capacity comparison is false. CP337 is active exactly when CP336 assigned supply enthalpy; UnitOff, non-cooling, and CP330 active guard-false routes skip every CP337 site and retain null selector/result evidence. Exact release takes only the same-call CP336 predecessor, recursively validates its latest/private witness and retained chain, and re-reads `cooling_limit` only from the identity-checked selected typed system, with retained initialization and earlier same-call selector records used only as lineage evidence. `LimitCapacity` executes three sites, `LimitFlowRateAndCapacity` five, and `NoLimit` or `LimitFlowRate` four. For active count `A`, Capacity matches `C`, second comparisons `S=A-C`, combined matches `F`, and body entries `B=C+F`, exact counters require active false fallthroughs `A-B` and dynamic source sites `2*A+2*S+B`; checked preflight makes every failure transactional. Direct-only lifecycle evidence preserves CP336-to-CP337-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2196 is the first excluded lexical executable, true-body statement, and CP338 boundary; a false guard next dynamically executes line 2208. The line-2196 capacity body, its operands and lines 2196-2203, false-path line 2208 and later work through 2337, zero-flow `else` at 2339-2345, Heat/DeadBand sibling at 2347-2348, mixed-air call at 2454-2461, and sibling guard at 2465 remain excluded. No maximum-capacity, sizing, psychrometric, supply-state, live-service, or numerical-result ownership is added. `OutdoorAir`, `Economizer`, `HeatRecovery`, `EMS`, Autosizing, broad humidity control, broader capacity/enthalpy/output behavior, support level, run state, feature boundary, evidence cases, numerical conformance, capability, counts, readiness, status, output claims, and Roadmap state remain unchanged; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP338 additionally requires the exact direct lane to execute the single three-site Cooling positive-supply capacity-limit `CpAir` assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2196 after CP337 and before the unchanged numerical DTO; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact sites are `read-purchased-air-mixed-air-humidity-ratio`, `evaluate-psy-cp-air-fn-w`, and `assign-local-cp-air`. Only CP337 `CapacityLimitBodyEntered` executes CP338, so `LimitCapacity` and `LimitFlowRateAndCapacity` execute all three sites. The exact five retained routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, and `CapacityLimitCpAirAssigned`; the first four skip all sites with null operand/result evidence. For active assignments `A`, equal to CP337 body entries `B`, exact state requires `source_site_execution_count=3*A=3*B` and each per-site counter equals `A`. Exact release accepts only the same-call CP337 predecessor, recursively validates its latest/private witness and chain, and reads `PurchAir.MixedAirHumRat` only from the same-call completed CP329 latest/private witness's `mixed_air_humidity_ratio`; equal no-OA Zone humidity is lineage evidence, not a substitute operand. Duplicate caller scalars, Zone/model re-reads, maximum-capacity or sizing values, live services, later body operands, and numerical-DTO inputs are forbidden. Canonical `energyplus_psy_cp_air_fn_w` applies the source humidity floor and its result is assigned to local `CpAir`; mutable C++ cache behavior and broader psychrometric parity remain deferred. Direct-only lifecycle evidence preserves CP337-to-CP338-to-numerical order without consuming, reconciling with, feeding, or replacing the numerical DTO. Physical line 2197 is the first excluded lexical executable and CP339 boundary: `CoolSensOutput = SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy);`; its arithmetic and all later capacity work remain excluded. No support level, readiness, run state, feature boundary, evidence case, numerical conformance, capability, count, output claim, numerical-result ownership, or Roadmap state changes; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP339 additionally requires the exact direct lane to execute the single six-site Cooling positive-supply capacity-limit sensible-output assignment at locked EnergyPlus 26.1 `PurchasedAirManager.cc` physical executable line 2197 after CP338 and before the unchanged numerical DTO: `CoolSensOutput = SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy);`; the locked source-file SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites are `read-retained-supply-mass-flow-rate-for-sensible-output-product`, `read-retained-mixed-air-enthalpy-for-sensible-output-difference`, `read-retained-supply-enthalpy-for-sensible-output-difference`, `calculate-mixed-air-enthalpy-minus-supply-enthalpy`, `calculate-supply-mass-flow-rate-times-enthalpy-difference`, and `assign-local-cooling-sensible-output`; their deterministic Rust ordering makes no C++ built-in operand-evaluation-order claim because the reads are side-effect-free. Only CP338 `CapacityLimitCpAirAssigned` executes CP339. The exact five routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, and `CapacityLimitSensibleOutputAssigned`; skipped routes retain null operands, intermediate, and result. For active assignments `A`, equal to CP338 `capacity_limit_cp_air_assignment_count` and CP337 body entries `B`, exact state requires `source_site_execution_count=6*A=6*B` and every per-site counter equals `A`. Exact release accepts only the same-call CP338 predecessor, recursively proves its latest/private witness and complete retained chain, and reads active operands only from retained CP330 `supply_mass_flow_rate_kg_per_s`, CP329 `mixed_air_enthalpy_projection_j_per_kg`, and CP336 `supply_enthalpy_j_per_kg` latest/private witnesses. It preserves exact `SupplyMassFlowRate * (MixedAirEnthalpy - SupplyEnthalpy)` grouping by subtracting before multiplying, with raw IEEE binary64 behavior and no derived finite-result rejection. CP329 and CP336 enthalpy operands remain finite, while CP330 may retain a permitted `+infinity` flow. Active Rust snapshots keep that possible nonfinite operand and any nonfinite derived difference or product as `Some(value)`. During serde JSON serialization, each such nonfinite numeric field is projected as `null`, while a separate IEEE bit string derived from the retained value remains non-null; skipped Rust snapshots retain corresponding values as `None`, and serialized JSON keeps both numeric and bit-string fields null. CP338 `CpAir`, `MaxCoolTotCap`, model/sizing values, live services, caller scalars, and numerical-DTO consumption, reconciliation, feed, or replacement are excluded. Direct-only lifecycle evidence preserves CP338-to-CP339-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_assignment_lifecycle`; non-direct paths carry no CP339 evidence. Physical line 2198 is the first excluded lexical executable and CP340 boundary, `if (CoolSensOutput >= PurchAir.MaxCoolTotCap) {`; lines 2198-2203, false continuation 2208, and all broader capacity/supply-state/output behavior remain excluded. CP339 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP340 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output guard at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2198 after CP339 and before the unchanged numerical DTO: `if (CoolSensOutput >= PurchAir.MaxCoolTotCap) {`; the locked raw source SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites, in order, are `read-retained-cooling-sensible-output-for-maximum-capacity-comparison`, `read-retained-maximum-total-cooling-capacity-for-sensible-output-comparison`, `compare-cooling-sensible-output-greater-than-or-equal-to-maximum-total-cooling-capacity`, and `enter-cooling-capacity-adjustment-body-if-comparison-satisfied`. This deterministic Rust/source-text witness order makes no C++ built-in `>=` operand-evaluation-order claim because both source reads are side-effect-free. Only CP339 `CapacityLimitSensibleOutputAssigned` executes CP340. The exact six routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputAdjustmentBodyEntered`. For transitions `T`, inherited counts `U/N/P/G`, active assignments `A`, false fallthroughs `F`, and body entries `E`, exact state requires `T=U+N+P+G+F+E=U+N+P+G+A`, `A=F+E=CP338 assignments=CP337 body entries`, both operand reads and comparison equal `A`, body entries equal `E`, `source_site_execution_count=3*A+E`, and witnessed `F/E` parity. Exact release takes only `runtime`, selected `system`, and the same-call CP339 predecessor. It extracts final `cooling_sensible_output_w` only from the retained CP339 latest snapshot after bit-exact parity with the supplied predecessor and private witness plus recursive lineage validation, and extracts finite nonnegative `maximum_total_cooling_capacity_w` only from the retained same-call CP321 latest snapshot after bit-exact private-witness parity and recursive validation; CP321 aggregate reads may exceed `A`. Caller/model/sized-limit re-reads, live services, and numerical-DTO inputs are excluded. Raw IEEE binary64 `>=` makes NaN false, `+infinity` true, and `-infinity` false against the finite nonnegative maximum. Signed-zero capacity bits are characterized, but CP321's zero reset prevents the complete public active chain from reaching either signed zero; active zero forgery is rejected and the real skip retains null capacity. In the direct no-OA lineage, `+infinity` flow has an exact-zero enthalpy difference, so CP339 produces NaN and CP340 takes the false route. The publicly reachable true nonfinite case instead uses finite positive flow and finite enthalpy operands whose subtraction overflows to `+infinity`; CP339 produces `+infinity` and CP340 enters the body. `-infinity` remains a pure-transition characterization only. Active Rust snapshots preserve the possibly nonfinite operand as `Some(value)`; serde JSON projects its nonfinite numeric field to `null` while a separate IEEE bit string remains non-null. Skips retain Rust `None` and JSON null numeric and bit fields. Direct-only lifecycle evidence preserves CP339-to-CP340-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_guard_lifecycle`; non-direct paths carry no CP340 evidence, and CP340 does not consume, reconcile with, feed, or replace the numerical DTO. Physical line 2199 is the first excluded lexical executable, `CoolSensOutput = PurchAir.MaxCoolTotCap;`; that assignment, later adjustment through line 2203, false continuation at line 2208, and broader capacity/supply/output behavior remain excluded. CP340 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP341 additionally requires the exact direct lane to execute the single two-site Cooling positive-supply maximum-capacity assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2199 after CP340 and before the unchanged numerical DTO: `CoolSensOutput = PurchAir.MaxCoolTotCap;`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. The exact source-text sites, in order, are `read-retained-maximum-total-cooling-capacity-for-sensible-output-assignment` and `assign-local-cooling-sensible-output-from-maximum-total-cooling-capacity`. This deterministic Rust right-hand-side-read-to-left-hand-side-write witness makes no general claim about C++ built-in `=` evaluation order. Only CP340 `CapacityLimitSensibleOutputAdjustmentBodyEntered` executes both sites. The exact six routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputMaximumCapacityAssigned`. For transitions `T`, inherited counts `U/N/P/G`, CP340 false fallthroughs `F`, CP341 maximum-capacity assignments `M`, and CP340 active guard evaluations `A`, exact state requires `T=U+N+P+G+F+M`, `A=F+M`, `M=CP340 adjustment-body entries`, both source-site counters equal `M`, `source_site_execution_count=2*M`, and witnessed route-counter parity. No invariant equates `M` with `A` or with CP321 aggregate maximum-capacity reads. Exact release takes only `runtime`, selected `system`, and the same-call CP340 predecessor. It obtains the preexisting cooling sensible output and assignment RHS solely from the retained same-call CP340 latest snapshot after bit-exact parity with the supplied predecessor and private witness plus recursive lineage validation. It never reaches through to CP321 or re-reads caller, typed-model, sizing, live-service, or numerical-DTO values. The false CP340 route preserves predecessor output bits without an RHS read or LHS write; the true route reads the retained maximum and assigns that exact binary64 payload. Complete public active lineage requires the maximum finite and strictly greater than zero. Arbitrary IEEE payload copying by the pure transition is characterization only, with no arithmetic, normalization, clamp, finite coercion, or broader public reachability claim. Active false-route NaN remains `Some(value)` for both preexisting and resulting Rust fields; serde JSON projects both numeric fields to `null` while preserving identical non-null IEEE bit strings and keeps the unread maximum numeric and bit fields null. On a publicly reachable true route with a `+infinity` predecessor, JSON keeps the preexisting numeric field null and its IEEE bits non-null, while the finite positive retained maximum becomes the non-null resulting numeric value with matching result bits. Direct-only lifecycle evidence preserves CP340-to-CP341-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_maximum_capacity_assignment_lifecycle`; CP341 does not consume, reconcile with, feed, or replace the unchanged numerical DTO. Non-direct paths carry no CP341 evidence and reject attached evidence. Physical line 2200 is the first excluded lexical executable: `SupplyEnthalpy = MixedAirEnthalpy - CoolSensOutput / SupplyMassFlowRate;`; that calculation, later adjustment through line 2203, false continuation at line 2208, and broader capacity, supply-state, and output behavior remain excluded. CP341 makes no support, readiness, run-state, feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP342 additionally requires the exact direct lane to execute the single six-site Cooling positive-supply capacity-limit sensible-output supply-enthalpy assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2200 after CP341 and before the unchanged numerical DTO: `SupplyEnthalpy = MixedAirEnthalpy - CoolSensOutput / SupplyMassFlowRate;`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact textual sites are `read-retained-mixed-air-enthalpy-for-supply-enthalpy-difference`, `read-retained-cooling-sensible-output-for-specific-cooling-output-division`, `read-retained-supply-mass-flow-rate-for-specific-cooling-output-division`, `calculate-cooling-sensible-output-divided-by-supply-mass-flow-rate`, `calculate-mixed-air-enthalpy-minus-specific-cooling-output`, and `assign-local-supply-enthalpy`. The three source reads are side-effect-free, so this deterministic Rust/source-text and dependency witness makes no claim about C++ built-in `/`, `-`, or `=` operand-evaluation order; it preserves `MixedAirEnthalpy - (CoolSensOutput / SupplyMassFlowRate)`, division before subtraction, then assignment. Only CP341 `CapacityLimitSensibleOutputMaximumCapacityAssigned` executes all sites. The exact routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyEnthalpyAssigned`. For transitions `T`, inherited `U/N/P/G`, CP340 false fallthroughs `F`, CP342 assignments `H`, CP341 maximum-capacity assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+H`, `A=F+H`, `H=M=CP340 adjustment-body entries`, all three read, two calculation, and one write counters equal `H`, and `source_site_execution_count=6*H`, with witnessed route parity. No invariant equates `H` with `A` or CP321 aggregate maximum-capacity reads. Exact release accepts only `runtime`, selected `system`, and the same-call CP341 predecessor. It requires supplied/latest/private CP341 bit parity and recursive completion, obtains `CoolSensOutput` only from CP341 `resulting_cooling_sensible_output_w` and never CP339's pre-capacity output, and obtains mixed-air enthalpy, supply mass flow, and preexisting supply enthalpy only from the same-call CP339 latest/private witness after exact parity and recursive completion. Caller operands, CP321/model/sizing re-reads, live services, and numerical-DTO inputs are forbidden. The CP340 false route preserves CP339 supply-enthalpy bits without CP342 reads, calculations, or write; the true route executes all six sites. Complete public assignment lineage requires finite mixed-air and preexisting enthalpies, finite strictly positive CP341 cooling output, and finite strictly positive supply flow. Its publicly witnessed nonfinite predecessor uses finite flow: CP339 forms `+infinity`, CP341 caps it to a finite maximum, and CP342 performs the exact divide/subtract from that capped value. A `+infinity` supply flow instead makes the direct lineage CP339 product NaN and reaches the CP340/CP341 false route, so CP342 preserves CP339 `SupplyEnthalpy` without executing an RHS site. The pure transition separately retains raw IEEE binary64 division/subtraction, including signed-zero, NaN, infinity, and overflow characterization without normalization, clamp, fused operation, or derived finite-result coercion. Its `+infinity`-flow positive-zero quotient and overflow-derived `-infinity` are characterization only, not public assignment-reachability claims. Characterized active Rust fields retain nonfinite derived values as `Some(value)`; serde JSON maps their numeric fields to `null` while preserving non-null IEEE bit strings. False-route preexisting/result supply enthalpy remain bit-identical `Some(value)` with unread fields null; inherited skips remain Rust `None` and JSON null. Direct-only lifecycle evidence preserves CP341-to-CP342-to-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_enthalpy_assignment_lifecycle`; CP342 does not consume, reconcile with, feed, or replace the numerical DTO. Non-direct paths carry no CP342 evidence and reject attached evidence. Physical line 2201 is the first excluded lexical executable: `PurchAir.SupplyTemp = PsyTdbFnHW(SupplyEnthalpy, PurchAir.SupplyHumRat);`; that conversion, line 2203, false continuation at line 2208, and later behavior remain excluded. CP342 adds bounded local supply-enthalpy and lifecycle evidence only and makes no support, run-state, required/forbidden-feature, evidence-case, numerical-conformance, capability, algorithm/routine-count, output-ownership, status, conformance, or Roadmap promotion; both parent algorithms remain `scaffold`/`none` and both Calc routines remain `source_mapped`. CP343 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output supply-temperature assignment at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2201 after CP342 and before the unchanged numerical DTO: `PurchAir.SupplyTemp = PsyTdbFnHW(SupplyEnthalpy, PurchAir.SupplyHumRat);`; the locked raw source SHA-256 is `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact sites are `read-local-supply-enthalpy-for-dry-bulb-inversion`, `read-purchased-air-supply-humidity-ratio-for-dry-bulb-inversion`, `evaluate-psy-tdb-fn-h-w`, and `assign-purchased-air-supply-temperature`; both reads are side-effect-free, so the deterministic witness makes no C++ function-argument-evaluation-order claim. Canonical inline `PsyTdbFnHW` is locked to `Psychrometrics.hh` physical lines 743-762 and raw SHA-256 `30C9575BC5A8E73D33D111E0D54A4DA8916AF4534175E9B95071ACA2513AEF45`; it forms `W=max(dW,1.0e-5)` with first-argument NaN semantics and returns exactly `(H - 2.50094e6 * W) / (1.00484e3 + 1.85895e3 * W)`. Rust uses `energyplus_psy_tdb_fn_h_w` with exact grouping and no cache, diagnostic, state, reassociation, normalization, clamp, or fused operation. Only CP342 `CapacityLimitSensibleOutputSupplyEnthalpyAssigned` executes all sites. Routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyTemperatureAssigned`. For `T`, inherited `U/N/P/G`, CP340 false `F`, CP343 assignments `D`, CP342 assignments `H`, CP341 assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+D`, `A=F+D`, `D=H=M=CP340 adjustment-body entries`, each site counter equals `D`, and `source_site_execution_count=4*D`, with witnessed parity; no `D=A` or CP321 aggregate-read invariant is added. Exact release takes only `runtime`, selected `system`, and same-call CP342, requiring supplied/latest/private CP342 bit parity and recursive completion. CP342 `resulting_supply_enthalpy_j_per_kg` solely owns enthalpy, never CP336 pre-capacity enthalpy; same-call CP335 `assigned_supply_humidity_ratio` owns humidity and same-call CP334 `assigned_supply_temperature_c` owns preexisting temperature. Retained CP336 latest/private lineage corroborates those two values without replacing either owner. Caller/model/sizing/Zone re-reads, live services, and numerical-DTO inputs are forbidden. CP340 false preserves CP334 temperature bits without CP343 sites; inherited skips retain null operands/results. Active humidity is finite and `>=0.0` and preexisting temperature is finite, while no new finite gate is added to exact CP342 enthalpy or the helper result. Pure floor, signed-zero, NaN, infinity, overflow, numerator/denominator, and division behavior is defensive characterization, not a new full-public-chain reachability claim. Characterized nonfinite Rust fields stay `Some`; JSON maps numeric values to null and preserves IEEE bits. Direct-only lifecycle evidence preserves CP342-to-CP343-to-unchanged-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_temperature_assignment_lifecycle`; non-direct paths reject CP343 evidence. Line 2202 is commentary; line 2203 is the first excluded executable, `PurchAir.SupplyTemp = min(PurchAir.SupplyTemp, PurchAir.MixedAirTemp);`. CP343 adds only bounded inverse/lifecycle evidence, keeps `routine.psy_tdb_fn_h_w` `state_mapped`, both parent routines `source_mapped`, both parent algorithms `scaffold`/`none`, and makes no support, run-state, feature/evidence, numerical-conformance, capability, count, output, status, conformance, or Roadmap promotion. CP344 additionally requires the exact direct lane to execute the single four-site Cooling positive-supply capacity-limit sensible-output supply-temperature mixed-air limit at pinned EnergyPlus commit `6f2e40d10250a105b49966baa24d843711e61048`, `PurchasedAirManager.cc` physical executable line 2203 after CP343 and before the unchanged numerical DTO: `PurchAir.SupplyTemp = min(PurchAir.SupplyTemp, PurchAir.MixedAirTemp);`; the locked raw source SHA-256 remains `54D960BCBFDF4F424A84BA73BF62040677424AD93E2F9362584898B0B146C005`. Its exact sites are `read-purchased-air-supply-temperature-for-minimum`, `read-purchased-air-mixed-air-temperature-for-minimum`, `apply-source-shaped-two-argument-minimum`, and `assign-purchased-air-supply-temperature`; both reads are side-effect-free, so deterministic Rust/source-text order makes no C++ function-argument-evaluation-order claim. EnergyPlus imports ObjexxFCL's exact two-double `a < b ? a : b` minimum; Rust uses `if left < right { left } else { right }`, so strict true selects the CP343 left operand while ties and unordered comparisons select the CP329 right operand bit-for-bit, with no `f64::min`, total/partial ordering, clamp, normalization, finite coercion, cache, diagnostic, or mutable service state. Only CP343 `CapacityLimitSensibleOutputSupplyTemperatureAssigned` executes all sites. Routes are `UnitOff`, `NonCooling`, `PositiveGuardFalseFallthrough`, `ActiveCapacityLimitGuardFalseFallthrough`, `CapacityLimitSensibleOutputGuardFalseFallthrough`, and `CapacityLimitSensibleOutputSupplyTemperatureMixedAirLimitExecuted`. For `T`, inherited `U/N/P/G`, CP340 false `F`, CP344 executions `L`, CP343 assignments `D`, CP342 assignments `H`, CP341 assignments `M`, and CP340 active evaluations `A`, exact state requires `T=U+N+P+G+F+L`, `A=F+L`, `L=D=H=M=CP340 adjustment-body entries`, every site counter equals `L`, and `source_site_execution_count=4*L`, with witnessed parity; no `L=A` or CP321 aggregate-read invariant is added. Exact release takes only `runtime`, selected `system`, and same-call CP343, requiring supplied/latest/private CP343 bit parity and recursive completion. CP343 `resulting_supply_temperature_c` solely owns the left/preexisting operand; retained same-call CP329 latest/private `mixed_air_temperature_c` solely owns the finite right operand. CP334 and CP336 temperature evidence remains recursive lineage/corroboration and replaces neither source owner. Caller/model/sizing/Zone re-reads, live services, and numerical-DTO inputs are forbidden. CP340 false preserves CP343 result bits, recursively preserving CP334 temperature, without CP344 sites; inherited skips retain null operands/results. No new finite gate is added to exact CP343 left or selected result. Pure strict-`<` signed-zero, NaN, infinity, tie, and unordered behavior is defensive characterization, not a new full-public-chain reachability claim. Characterized nonfinite Rust fields stay `Some`; JSON maps numeric values to null and preserves IEEE bits. Direct-only lifecycle evidence preserves CP343-to-CP344-to-unchanged-numerical order under `purchased_air_calc_cooling_positive_supply_capacity_limit_sensible_output_supply_temperature_mixed_air_limit_lifecycle`; non-direct paths reject CP344 evidence. Physical lines 2204-2207 are non-executable closing delimiters, blank, and commentary; physical line 2208 is the first excluded executable, `PurchAir.SupplyHumRat = PurchAir.MixedAirHumRat;`. CP344 adds only bounded limit/lifecycle evidence, keeps `routine.psy_tdb_fn_h_w` `state_mapped`, both parent routines `source_mapped`, both parent algorithms `scaffold`/`none`, and makes no support, readiness, run-state, feature/evidence, numerical-conformance, capability, algorithm/routine-count, output, status, conformance, or Roadmap promotion. |
| ideal_loads_constant_shr | ideal_loads | compatibility | partial_supported_run | `ZoneHVAC:IdealLoadsAirSystem`<br>`ZoneHVAC:EquipmentConnections`<br>`ZoneHVAC:EquipmentList`<br>`ZoneControl:Thermostat`<br>`ThermostatSetpoint:DualSetpoint` | `OutdoorAir`<br>`Economizer`<br>`HeatRecovery`<br>`Autosizing`<br>`EMS`<br>`PythonPlugin` | `ideal_loads_constant_shr_humidity_control` | `ideal_loads_constant_shr_conformance_001` | ConstantSensibleHeatRatio conformance evidence remains limited to the declared gated case. Arbitrary execution uses only the diagnostic fixed-demand adapter, requires diagnostic mode with partial execution allowed, and cannot become release compatibility or conformance evidence. |
| ideal_loads_humidity_selected_branches | ideal_loads | compatibility | partial_supported_run | `ZoneHVAC:IdealLoadsAirSystem`<br>`ZoneControl:Thermostat`<br>`ZoneControl:Humidistat` | `OutdoorAir`<br>`Economizer`<br>`HeatRecovery`<br>`Autosizing`<br>`EMS`<br>`PythonPlugin` | `ideal_loads_constant_supply_humidity_control`<br>`ideal_loads_constant_supply_humidity_heating_control`<br>`ideal_loads_humidistat_dehumidification_control`<br>`ideal_loads_humidistat_humidification_control` | `ideal_loads_constant_supply_humidity_cooling_annual_meter_conformance_candidate_001`<br>`ideal_loads_constant_supply_humidity_cooling_conformance_candidate_001`<br>`ideal_loads_constant_supply_humidity_heating_annual_meter_conformance_candidate_001`<br>`ideal_loads_constant_supply_humidity_heating_conformance_candidate_001`<br>`ideal_loads_humidistat_dehumidification_annual_meter_conformance_candidate_001`<br>`ideal_loads_humidistat_dehumidification_conformance_candidate_001`<br>`ideal_loads_humidistat_humidification_annual_meter_conformance_candidate_001`<br>`ideal_loads_humidistat_humidification_conformance_candidate_001` | Only declared humidity-control candidate rows retain their case-level evidence. Arbitrary execution uses only the diagnostic fixed-demand adapter, requires diagnostic mode with partial execution allowed, and does not own live moisture demand, Zone humidity feedback, or release compatibility; broad humidity control remains outside the claim. |
| ideal_loads_outdoor_air_selected_branches | ideal_loads | compatibility | partial_supported_run | `ZoneHVAC:IdealLoadsAirSystem`<br>`DesignSpecification:OutdoorAir` | `Autosizing`<br>`EMS`<br>`PythonPlugin`<br>`AirLoopHVAC`<br>`PlantLoop` | `ideal_loads_outdoor_air_design_flow` | `ideal_loads_outdoor_air_air_changes_conformance_candidate_001`<br>`ideal_loads_outdoor_air_co2_dcv_conformance_candidate_001`<br>`ideal_loads_outdoor_air_differential_dry_bulb_economizer_conformance_candidate_001`<br>`ideal_loads_outdoor_air_differential_enthalpy_economizer_conformance_candidate_001`<br>`ideal_loads_outdoor_air_enthalpy_heat_recovery_conformance_candidate_001`<br>`ideal_loads_outdoor_air_flow_area_conformance_candidate_001`<br>`ideal_loads_outdoor_air_flow_person_conformance_candidate_001`<br>`ideal_loads_outdoor_air_flow_zone_conformance_candidate_001`<br>`ideal_loads_outdoor_air_maximum_conformance_candidate_001`<br>`ideal_loads_outdoor_air_occupancy_dcv_conformance_candidate_001`<br>`ideal_loads_outdoor_air_sensible_heat_recovery_conformance_candidate_001`<br>`ideal_loads_outdoor_air_sum_conformance_candidate_001` | Only declared IdealLoads outdoor-air/economizer/heat-recovery branch candidates retain their case-level evidence. Arbitrary execution uses only the diagnostic fixed-demand adapter, requires diagnostic mode with partial execution allowed, and does not claim live OA/weather/schedule/DCV feedback or release compatibility. |

## Unsupported Rules

| ID | Object patterns | Except patterns | Severity | Reason |
|---|---|---|---|---|
| unsupported_hvac_air_loop | `AirLoopHVAC*`<br>`Fan:*`<br>`Coil:*`<br>`Controller:*`<br>`SetpointManager:*`<br>`AirTerminal:*`<br>`OutdoorAir:*` |  | run_blocked | Broad HVAC air-loop semantics are not ported. |
| unsupported_hvac_zone_equipment | `ZoneHVAC:*` | `ZoneHVAC:IdealLoadsAirSystem`<br>`ZoneHVAC:EquipmentConnections`<br>`ZoneHVAC:EquipmentList` | run_blocked | Broad ZoneHVAC equipment semantics outside declared IdealLoads branches are not ported. |
| unsupported_plant | `PlantLoop`<br>`Branch`<br>`BranchList`<br>`Connector:*`<br>`ConnectorList`<br>`Pump:*`<br>`Boiler:*`<br>`Chiller:*` |  | run_blocked | Plant loop semantics are not ported. |
| unsupported_ems_python_airflow | `EnergyManagementSystem:*`<br>`PythonPlugin:*`<br>`AirflowNetwork:*` |  | run_blocked | Runtime-modifying plugin/network features are not ported. |
| unsupported_sizing | `Sizing:*`<br>`ZoneSizing*` |  | run_blocked | Sizing workflows are not ported. |
| unsupported_zone_grouping | `ZoneList`<br>`ZoneGroup` |  | run_blocked | ZoneList target expansion and ZoneGroup list-multiplier runtime semantics are not ported. |
| unsupported_zone_local_environment | `ZoneProperty:LocalEnvironment` |  | run_blocked | Zone-local outdoor-air node conditions and downstream weather consumers are not ported. |
| unsupported_space_partitioning | `Space`<br>`SpaceList` |  | run_blocked | Authored and generated-remainder Space partitioning, SpaceList expansion, and space-level heat-balance consumers are not ported; sole generated whole-zone defaults remain inactive. |
| unsupported_surface_boundary | `FenestrationSurface:*`<br>`Window*`<br>`Daylighting:*`<br>`Shading:*`<br>`Site:Ground*`<br>`Material:RoofVegetation`<br>`MaterialProperty:VariableAbsorptance`<br>`MaterialProperty:PhaseChangeHysteresis`<br>`MaterialProperty:PhaseChange`<br>`MaterialProperty:VariableThermalConductivity`<br>`MaterialProperty:MoisturePenetrationDepth:Settings`<br>`MaterialProperty:HeatAndMoistureTransfer:Settings`<br>`MaterialProperty:HeatAndMoistureTransfer:SorptionIsotherm`<br>`MaterialProperty:HeatAndMoistureTransfer:Suction`<br>`MaterialProperty:HeatAndMoistureTransfer:Redistribution`<br>`MaterialProperty:HeatAndMoistureTransfer:Diffusion`<br>`MaterialProperty:HeatAndMoistureTransfer:ThermalConductivity`<br>`SurfaceProperties:VaporCoefficients`<br>`SurfaceProperty:IncidentSolarMultiplier`<br>`SurfaceProperty:SolarIncidentInside`<br>`ComplexFenestrationProperty:SolarAbsorbedLayers`<br>`Construction:FfactorGroundFloor`<br>`Construction:CfactorUndergroundWall`<br>`Construction:AirBoundary`<br>`Construction:ComplexFenestrationState`<br>`ConstructionProperty:InternalHeatSource`<br>`Construction:WindowEquivalentLayer`<br>`Construction:WindowDataFile` |  | run_blocked | Fenestration, daylighting, shading, and advanced material or surface runtime semantics are not ported. |

## Partial Rules

| ID | Object patterns | Eligible state | Reason |
|---|---|---|---|
| ignored_reporting_objects | `Output:*`<br>`OutputControl:*` | partial_supported_run | Reporting requests can be exported or reported unsupported without changing simulation semantics. |
| inactive_or_unused_raw_objects | `SimulationControl`<br>`SizingPeriod:DesignDay`<br>`HeatBalanceAlgorithm`<br>`ShadowCalculation`<br>`Exterior:Lights` | partial_supported_run | Preserved for diagnostics or staging when inactive for the current runtime subset. |
