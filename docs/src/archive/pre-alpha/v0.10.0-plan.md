---
status: active
claim_level: baseline-only
owner: runtime
last_reviewed: 2026-06-07
---

# v0.10.0 Plan

Purpose: promote thermostat, zone equipment, and IdealLoads intake from
raw-only coverage to a typed model and graph foundation.

## Blocking Smoke Case

`ideal_loads_thermostat_001` is the v0.10 typed-graph gate.

It covers:

- one `Zone`
- one `ThermostatSetpoint:DualSetpoint`
- one `ZoneControl:Thermostat`
- one `ZoneHVAC:EquipmentConnections`
- one `ZoneHVAC:EquipmentList`
- one `ZoneHVAC:IdealLoadsAirSystem`
- one `NodeList` feeding the IdealLoads supply node graph edge
- six opaque `BuildingSurface:Detailed` surfaces with a clean deterministic
  one-zone envelope
- a deterministic internal gain schedule that makes the IdealLoads baseline
  rates nonzero
- hourly thermostat heating and cooling setpoint variables
- hourly IdealLoads total heating and cooling rate variables

The case manifest declares:

- `comparison_class = "smoke"`
- `conformance_claim = false`
- no tolerance rules
- `zone-state` thermostat output variables
- `hvac-state` IdealLoads output variables
- blocking smoke gate: `scripts/dev.cmd ideal-loads-thermostat-smoke`

## Typed Graph Contract

The v0.10 gate requires the generated epJSON to compile with:

- `thermostat_dual_setpoints: 1`
- `zone_thermostats: 1`
- `ideal_loads_air_systems: 1`
- `zone_equipment_lists: 1`
- `zone_equipment_connections: 1`
- `nodes: 3`
- `node_lists: 1`

The execution plan must expose:

- `zone_thermostat_edges: 1`
- `thermostat_setpoint_edges: 1`
- `zone_ideal_loads_edges: 1`
- `node_list_member_edges: 1`
- `ideal_loads_supply_node_edges: 1`
- `zone_air_node_edges: 1`

This makes the control and equipment relationships inspectable before any
IdealLoads numerical solver is claimed.

## v0.10 Hardening Contract

v0.11 work must not start until the v0.10 gate also proves these details:

- the EnergyPlus baseline finishes without severe or fatal errors
- EnergyPlus warnings are either zero or explicitly allowed by the smoke gate
- the fixture does not rely on EnergyPlus auto-fixing surface orientation,
  run-period weekday, or invalid zone volume
- both IdealLoads heating and cooling rate series contain at least one nonzero
  baseline sample
- the report skeleton records per-series `min`, `max`, and `nonzero_count`
- the report skeleton records the EnergyPlus ERR warning summary
- compiler tests cover negative paths for unsupported thermostat/equipment
  types, missing references, invalid HVAC ranges, duplicate zone equipment
  connections, and invalid/duplicate equipment sequences
- the smoke gate checks required steps and graph edges by meaning, not by a
  brittle total step count

## Report Contract

The smoke gate writes baseline-only artifacts under:

- `.runtime/ideal-loads-thermostat/26.1.0/ideal_loads_thermostat_001`
- `.runtime/ideal-loads-thermostat/26.1.0/report-skeleton/ideal_loads_thermostat_001/compare-report.md`
- `.runtime/ideal-loads-thermostat/26.1.0/report-skeleton/ideal_loads_thermostat_001/compare-summary.json`

The report status must remain:

```text
tolerance_policy: none
status: baseline-only
```

The report must also include baseline-only signal checks:

```text
baseline_min
baseline_max
baseline_nonzero_count
energyplus_warnings
```

## Not Claimed

This is not an IdealLoads load-conformance claim.

Not claimed in v0.10:

- IdealLoads heating or cooling rate parity
- thermostat control-loop numerical parity
- HVAC availability, sizing, economizer, heat recovery, ventilation, humidity,
  or fuel-use parity
- system-node mass flow, temperature, or humidity-ratio conformance
- plant, fan, coil, outdoor-air system, or air-loop behavior
- warmup, sizing-period, or unmet-load parity

## Release Gate

`.\scripts\dev.cmd v0.10-verify` must pass before v0.10 can be called ready.
