---
status: active
claim_level: diagnostic
owner: runtime
last_reviewed: 2026-06-07
---

# v0.16.0 Readiness

readiness: diagnostic-ready

v0.16 adds Rust-side plant-state projection artifacts for the v0.15 plant-loop
diagnostic fixture.

This is not a plant numerical conformance claim.

## Completion Checklist

| Requirement | Evidence |
|---|---|
| prerequisite baseline | `plant-loop-diagnostic-smoke` keeps `plant_loop_diagnostic_001` oracle baseline active |
| Rust projection command | `eplus-rs run plant-state-projection <input.epJSON> <output-dir>` |
| projection artifacts | `plant-state-summary.md` and `plant-state-summary.json` |
| output rows | 5 `plant-state` loop series plus 3 `plant-equipment` series |
| policy markers | `algorithm_parity: false`, source-map, timestamp, warmup, and sizing rules |
| smoke gate | `plant-loop-projection-smoke` checks result shape and non-claim boundaries |
| release gate | `scripts/release/v0.16-verify.ps1` checks docs, scripts, tests, smoke gates, false-claim guard, evidence package, and package contents |
| false-claim boundary | no plant, HVAC, node, meter, sizing, or ExampleFiles conformance claim is introduced |

## Projection Outputs

| Key | Variable | Class | Status |
|---|---|---|---|
| `MAIN LOOP` | `Plant Supply Side Cooling Demand Rate` | `plant-state` | projected |
| `MAIN LOOP` | `Plant Supply Side Heating Demand Rate` | `plant-state` | projected |
| `MAIN LOOP` | `Plant Supply Side Inlet Mass Flow Rate` | `plant-state` | projected |
| `MAIN LOOP` | `Plant Supply Side Inlet Temperature` | `plant-state` | projected |
| `MAIN LOOP` | `Plant Supply Side Outlet Temperature` | `plant-state` | projected |
| `PUMP` | `Pump Electricity Rate` | `plant-equipment` | projected |
| `PURCHASED HEATING` | `District Heating Water Rate` | `plant-equipment` | projected |
| `LOAD PROFILE 1` | `Plant Load Profile Heat Transfer Rate` | `plant-equipment` | projected |

## Verification Command

```powershell
.\scripts\dev.cmd v0.16-verify
```

## Boundary

The Rust plant projection remains:

```text
comparison_class: diagnostic-only
conformance_claim: false
algorithm_parity: false
tolerance_policy: none
status: projected
source_map: docs/src/porting-map/plant-source-map.md
```

Future plant conformance still requires source-mapped plant loop algorithms,
Rust result artifacts produced by those algorithms, timestamp, warmup,
design-day, run-period, sizing-period, and environment-period handling,
declared tolerances, and a blocking compare gate.
