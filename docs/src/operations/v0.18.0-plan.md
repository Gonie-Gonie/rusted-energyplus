---
status: planned
claim_level: none
owner: conformance
last_reviewed: 2026-06-07
---

# v0.18.0 Plan

Purpose: add output request injection and oracle baseline pipeline for selected
EnergyPlus ExampleFiles/testfiles.

This milestone prepares official-file evidence. It does not claim Rust
simulation conformance unless a later compare gate promotes a declared case and
variable set.

## Targets

- `conformance patch-outputs <case>` command.
- `Output:Variable` injection.
- `Output:Meter` injection.
- `Output:SQLite` injection.
- `OutputControl:Files` injection.
- duplicate output handling.
- `patch-report.md`.
- `conformance baseline <case>` command hardening.
- EnergyPlus oracle run wrapper stability.
- EnergyPlus ERR severe/fatal parser.
- `baseline-summary.json`.
- `selected_outputs.json`.
- `selected_meters.json`.

## Initial Cases

Tier A baseline candidates:

- `1ZoneUncontrolled.idf`.
- `1ZoneUncontrolled3SurfaceZone.idf`.
- `1ZoneUncontrolled_win_1.idf`.

Tier B baseline candidates:

- `4ZoneWithShading_Simple_1.idf`.
- `5ZoneAirCooled.idf`.

## Gate

- all Tier A baselines generated.
- requested output availability report generated.
- oracle fatal/severe count is zero or every exception has a waiver.

## Boundary

The presence of EnergyPlus baselines and selected outputs is baseline-only
evidence. Rust numerical conformance begins only after compare engine v2 and
case-level tolerances are active.
