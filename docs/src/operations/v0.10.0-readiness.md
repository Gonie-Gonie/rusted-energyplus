---
status: active
claim_level: baseline-only
owner: runtime
last_reviewed: 2026-06-07
---

# v0.10.0 Readiness

Status: typed-graph-ready when `.\scripts\dev.cmd v0.10-verify` passes.

## Evidence

| Evidence | Required artifact |
|---|---|
| case manifest | `data/conformance_cases/ideal_loads_thermostat_001/case.toml` |
| source IDF | `data/conformance_cases/ideal_loads_thermostat_001/ideal_loads_thermostat.idf` |
| output variable | hourly `Zone Thermostat Heating Setpoint Temperature` from ESO |
| output variable | hourly `Zone Thermostat Cooling Setpoint Temperature` from ESO |
| output variable | hourly `Zone Ideal Loads Zone Total Heating Rate` from ESO |
| output variable | hourly `Zone Ideal Loads Zone Total Cooling Rate` from ESO |
| typed object counts | compiler summary from generated `input.epJSON` |
| graph edges | runtime `model plan` summary from generated `input.epJSON` |
| report | `.runtime/ideal-loads-thermostat/26.1.0/report-skeleton/ideal_loads_thermostat_001/compare-report.md` |
| summary | `.runtime/ideal-loads-thermostat/26.1.0/report-skeleton/ideal_loads_thermostat_001/compare-summary.json` |
| blocking gate | `scripts/dev.cmd ideal-loads-thermostat-smoke` |
| release gate | `scripts/dev.cmd v0.10-verify` |

## Claim Boundary

The v0.10 claim is limited to typed intake, reference resolution, graph
connectivity, execution-plan visibility, and baseline-only output availability
for:

```text
ideal_loads_thermostat_001 / ZONE ONE / Zone Thermostat Heating Setpoint Temperature / hourly
ideal_loads_thermostat_001 / ZONE ONE / Zone Thermostat Cooling Setpoint Temperature / hourly
ideal_loads_thermostat_001 / ZONE ONE IDEAL LOADS / Zone Ideal Loads Zone Total Heating Rate / hourly
ideal_loads_thermostat_001 / ZONE ONE IDEAL LOADS / Zone Ideal Loads Zone Total Cooling Rate / hourly
```

This is not an IdealLoads load-conformance claim. The manifest has
`comparison_class = "smoke"`, `conformance_claim = false`, and no tolerance
rules.

## Pass Criteria

The smoke gate must verify:

- `comparison_class: smoke`
- `conformance_claim: false`
- `tolerance_policy: none`
- `status: baseline-only`
- four requested output series
- one typed thermostat dual setpoint
- one typed zone thermostat
- one typed IdealLoads air system
- one typed zone equipment list
- one typed zone equipment connection
- one zone thermostat graph edge
- one thermostat setpoint graph edge
- one zone IdealLoads graph edge

## Required Commands

```powershell
.\scripts\dev.cmd ideal-loads-thermostat-smoke
.\scripts\dev.cmd v0.10-verify
```
