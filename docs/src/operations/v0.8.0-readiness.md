---
status: active
claim_level: conformance
owner: runtime
last_reviewed: 2026-06-07
---

# v0.8.0 Readiness

Status: conformance-ready when `.\scripts\dev.cmd v0.8-verify` passes.

## Evidence

| Evidence | Required artifact |
|---|---|
| case manifest | `data/conformance_cases/heat_balance_nomass_001/case.toml` |
| source IDF | `data/conformance_cases/heat_balance_nomass_001/heat_balance_nomass.idf` |
| output variable | hourly `Zone Mean Air Temperature` from ESO |
| tolerance | `max_abs = 0.000001`, `max_rmse = 0.000001` for `zone-state` |
| report | `.runtime/heat-balance-conformance/26.1.0/heat_balance_nomass_001/compare/compare-report.md` |
| summary | `.runtime/heat-balance-conformance/26.1.0/heat_balance_nomass_001/compare/compare-summary.json` |
| blocking gate | `scripts/dev.cmd compare-heat-balance-conformance` |
| release gate | `scripts/dev.cmd v0.8-verify` |

## Claim Boundary

The v0.8 conformance claim is limited to:

```text
heat_balance_nomass_001 / ZONE ONE / Zone Mean Air Temperature / hourly
```

This is not a dynamic exterior heat-balance claim. Other zone-temperature
comparisons, including `zone_temperature_diagnostic_001`, remain
diagnostic-only unless they declare their own case, variable list, tolerance,
report, and blocking gate.

## Pass Criteria

`compare-summary.json` must contain:

- `comparison_class = conformance`
- `conformance_claim = true`
- `gate.blocking = true`
- `status = pass`
- `samples = 24`
- `heat_balance_timesteps = 96`
- `max_abs_delta_c <= 0.000001`
- `rmse_delta_c <= 0.000001`

## Required Commands

```powershell
.\scripts\dev.cmd compare-heat-balance-conformance
.\scripts\dev.cmd v0.8-verify
```
