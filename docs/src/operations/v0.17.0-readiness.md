---
status: active
claim_level: none
owner: conformance
last_reviewed: 2026-06-07
---

# v0.17.0 Readiness

readiness: schema-v2-ready

v0.17 defines and gates Case Manifest / Output Request Schema v2. It does not add a new numerical conformance claim.

## Completion Checklist

| Requirement | Evidence |
|---|---|
| manifest v2 metadata | `[manifest_v2]` with `schema`, `source_kind`, `source_file`, and `tier` |
| scope metadata | `[scope]` with domains and feature flags |
| output request v2 | every `[[outputs]]` row has `domain` and `level` |
| conformance output boundary | only v0.8/v0.9 cases use `level = "conformance"` |
| meters schema | `MeterRequest` supports v2 meter entries even though no meter case is promoted yet |
| waiver schema | `Waiver` supports reason, owner, and expiry |
| CLI validation | `eplus-rs conformance validate-case-v2 <case.toml>` |
| all-case gate | `scripts/conformance/manifest-validate-all.ps1` |
| release gate | `scripts/release/v0.17-verify.ps1` |

## Gate

```powershell
.\scripts\dev.cmd manifest-validate-all
.\scripts\dev.cmd v0.17-verify
```

## Claim Boundary

The v0.17 milestone claim is:

```text
comparison_class: schema/infrastructure
conformance_claim: false
new_numerical_conformance_cases: none
promoted_conformance_cases: v0.8/v0.9 only
schema: rusted-energyplus.case-manifest.v2
```

v0.17 does not claim ExampleFiles compatibility, plant/HVAC/node/meter
conformance, sizing/autosizing support, or broader heat-balance compatibility.
