# v0.1.0 Readiness

Status: ready when `.\scripts\v0.1-verify.cmd` passes.

## Completion Checklist

| Requirement | Evidence |
|---|---|
| rust-toolchain.toml fixed | `rust-toolchain.toml` pins `1.96.0-x86_64-pc-windows-gnu` |
| Cargo workspace initialized | `Cargo.toml` has `ep_cli`, `ep_raw_model`, `ep_model`, `ep_compiler`, `ep_runtime`, `ep_oracle`, `ep_compare` |
| Rust-only policy documented | `docs/src/architecture/rust-only-policy.md` |
| setup script works | `scripts/setup.ps1` and `.cmd` wrapper |
| portable EnergyPlus binary downloaded/verified | `.runtime/energyplus/26.1.0` and `tools/oracle/energyplus.lock.toml` |
| reference source downloaded/verified | `.reference/energyplus-src/26.1.0` and `source.sha256` |
| config/local.toml generated | created by `scripts/setup.ps1` |
| oracle smoke test passes | `scripts/oracle-smoke.ps1` |
| docs skeleton builds | `scripts/docs-check.ps1` with `mdbook 0.5.3` |
| minimal testcase exists | `data/testcases/minimal/case.toml` |

## Verification Command

```powershell
.\scripts\v0.1-verify.cmd
```

This runs:

- setup artifact checks
- source smoke checks
- Rust format, clippy, and tests
- docs build
- oracle smoke, unless `-SkipOracleSmoke` is supplied

## Reference Source Checkpoints

The reference source is not compiled into the Rust port. It is checked for:

- `testfiles/1ZoneUncontrolled.idf`
- `idd/schema/generate_epJSON_schema.py`
- `idd/embedded/generate_embeddable_epJSON_schema.cpp`
- `src/EnergyPlus/PlantUtilities.cc`
- `src/EnergyPlus/HVACInterfaceManager.cc`
- `src/EnergyPlus/Plant/DataPlant.hh`
- `tst/EnergyPlus/unit/PlantUtilities.unit.cc`
- `tst/EnergyPlus/unit/HVACInterfaceManager.unit.cc`

The packaged runtime is checked for:

- `energyplus.exe`
- `ConvertInputFormat.exe`
- `Energy+.schema.epJSON`
- `ExampleFiles/1ZoneUncontrolled.idf`
- `WeatherData/USA_CO_Golden-NREL.724666_TMY3.epw`

## v0.1.0 Boundary

v0.1.0 does not implement simulation behavior in Rust yet. It proves that the
project has a reproducible Rust workspace, locked oracle, locked reference
source, built docs, and a repeatable oracle smoke case. v0.2.0 starts RawModel
and epJSON inspection.

