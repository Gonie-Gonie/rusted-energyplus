[[object]]
name = "Version"
family = "project"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Accepted for oracle-version tracking; no runtime compatibility claim by itself."

[[object]]
name = "Building"
family = "building"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Typed for model metadata and simple building scope; no global building simulation claim."

[[object]]
name = "Timestep"
family = "time"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Typed for fixed time-axis plumbing. The paired calendar_schedule_hourly_exact_001 and calendar_schedule_weather_leap_policy_no_001 fixtures carry Timestep,4 through the canonical weather-effective calendar state but prove normalized hourly projection only; EnergyPlus invalid/default normalization, zone/system timestep separation, and full timestep-manager parity remain outside the claim. calendar_schedule_compact_zone_timestep_exact_001 separately locks one explicit valid Timestep,4 object as 96 ordered Schedule Value samples and normalized timestamp labels at 15-minute endpoints over one non-actual day. Invalid, missing, or non-divisible timestep-count correction/defaulting, ConductionFiniteDifference adjustment, internal full timestep state/flags, system-timestep separation, and other timestep counts remain outside the claim. calendar_schedule_compact_interpolation_modes_exact_001 separately reuses that one explicit valid Timestep,4 object to gate three 96-sample explicit No/Average/Linear Schedule Value series at the same 15-minute endpoints; it adds no evidence for another timestep count, timestep correction/defaulting, or zone/system timestep separation."

[[object]]
name = "RunPeriod"
family = "time"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Typed for run-period time axes, with paired ordered-exact-unique normalized hourly evidence for the explicit 2016-02-28 through 2016-03-01 range. calendar_schedule_hourly_exact_001 and calendar_schedule_weather_leap_policy_no_001 use the same IDF and 72 raw EPW rows: Leap Year Observed=Yes retains 72 samples ending Tuesday, while No skips February 29 and retains 48 ending simulation Monday. Rust unit tests separately assert March 1 Gregorian/weather/schedule day-of-year state as 61/60/61; the external pair does not prove those ordinal fields. The paired calendar_dst_fixed_date_hourly_exact_001 and calendar_dst_fixed_date_disabled_hourly_exact_001 cases share the exact EPW fixed February 29 through March 1 declaration and differ only in the explicit RunPeriod weather-file DST use flag as Yes versus No. Their pair gate proves 72 ordered-exact-unique zero-tolerance Site Daylight Saving Time Status samples per case: enabled is daily order 0/1/1 with 48 active hours, while disabled is 0/0/0 with zero active hours. The pair gate locks exact EnergyPlus 26.1 `Environment` and `Environment:Daylight Saving` EIO rows; the disabled daylight-saving row is `Environment:Daylight Saving,No,RunPeriod Object`. The Rust summary retains weather_file_period_declared=true while reporting active=false and resolved_period=null. Those Rust fields are summary diagnostics, not additional EIO output. Other periods, years, date rules, or policy combinations beyond the separate exact precedence case; schedule or civil-clock effects, actual-weather execution, Rust raw ESO serialization, and broad WeatherManager behavior remain unclaimed. calendar_dst_epw_idf_precedence_hourly_exact_001 separately uses the exact 2016-02-28 through 2016-03-01 RunPeriod with weather-file DST use No, the shared EPW 2/29 through 3/1 period, and exactly one IDF `RunPeriodControl:DaylightSavingTime` object for 2/28 through 2/29. The input-file source wins independently of the No flag, yielding daily status 1/1/0 and 48 active hours. Its zero-tolerance gate locks 72 ordered-exact-unique EnergyPlus ESO values and timestamp fields, exact EnergyPlus 26.1 `Environment,DST FIXED DATE RUN PERIOD,WeatherFileRunPeriod,02/28/2016,03/01/2016,Sunday,3,Use RunPeriod Specified Day,No,No,No,No,No,Clark and Allen` and `Environment:Daylight Saving,Yes,InputFile,02/28,02/29` rows, and clean 0 Warning/0 Severe completion. Rust `RunPeriodDaylightSavingTime` plus JSON `input_file_period_declared`/`effective_source` and Markdown `input_file_daylight_saving_period_declared`/`daylight_saving_effective_source` fields are summary diagnostics, not additional EIO output. Other fixed, Nth/last, or wrap dates; missing, malformed, or duplicate error parity beyond typed diagnostics; other years, multiple RunPeriods, and policy combinations; schedule/civil-clock effects; actual, cross-year, or DesignDay execution; Rust raw ESO serialization; and broad WeatherManager behavior remain unclaimed. calendar_epw_dst_weekday_rules_hourly_exact_001 adds only the literal EPW boundary pair `4th Monday in February` through `Last Wednesday in February` on the explicit Sunday-start 2032-02-22 through 2032-02-26 RunPeriod with policies No/Yes/No/No/No/No. The pair resolves to days of year 54 through 56 without wrap and proves 120 ordered-exact-unique zero-tolerance samples in daily order 0/1/1/1/0 with exactly 72 active hours, promoted series metadata, raw EnergyPlus oracle ESO timestamp fields and values, the exact WeatherFile EIO row, and clean 0/0 completion. calendar_epw_dst_southern_wrap_hourly_exact_001 adds only the literal EPW pair `Last Sunday in October` through `Last Sunday in March` and its 2032 calendar dates October 31 and March 28. External numerical evidence consists of all 72 EnergyPlus ESO values and timestamp fields over the same-year March 27 through March 29 end-side window, matching daily order 1/1/0 with 48 active and 24 inactive hours at zero tolerance, plus the exact EnergyPlus EIO WeatherFile row with resolved dates 10/31 and 03/28. The Rust summary day-of-year values 305 and 88 and wraps_year=true field are locked only against source-mapped expected values and are not fields emitted by EIO. Cross-year RunPeriod or weather-record traversal, full-year range execution, execution around the October start boundary, generic southern-hemisphere or year-wrap behavior, and other date-rule pairs are not proved. calendar_epw_dst_southern_wrap_start_hourly_exact_001 adds only the same literal EPW pair and its inclusive October 31 start boundary over the same-year October 30 through November 1 window. External numerical evidence consists of all 72 EnergyPlus ESO values and timestamp fields matching daily order 0/1/1 with 48 active and 24 inactive hours at zero tolerance, plus the exact EnergyPlus EIO WeatherFile row with dates 10/31 and 03/28. The Rust summary day-of-year values 305 and 88 and wraps_year=true field are again locked only against source-mapped expected values and are not EIO-emitted fields. This start-side case does not by itself prove full-year or cross-year traversal, the separately gated March end boundary, broader years, months, or date rules, RunPeriod disabling beyond the paired fixed-date Yes/No case, EPW-versus-IDF precedence beyond the exact fixed-date case, DST clock effects beyond the separate exact CP42 detailed-schedule case, actual-weather execution, or broad WeatherManager behavior. calendar_epw_dst_cross_year_start_year_projection_hourly_exact_001 separately locks one non-actual, single-DATA-PERIOD, one-record-per-hour 2031-12-30 through 2032-01-02 RunPeriod with Tuesday start and policies No/Yes/No/No/No/No. Its EPW 1st Thursday in January through 1st Friday in January rules resolve against the 2031 environment-start annual table as January 2 through January 3. The zero-tolerance gate proves 96 ordered-exact-unique Site Daylight Saving Time Status samples in daily order 0/0/0/1 with 24 active and 72 inactive hours, all 96 raw EnergyPlus ESO values and timestamp rows, the exact Environment and Environment:Daylight Saving WeatherFile 01/02 through 01/03 rows, and clean 0 Warning/0 Severe completion. EnergyPlus 26.1 source separately maps the January 2 result to prefetch before the January 1 annual-table reset; that internal buffer order is not an ESO/EIO-emitted field. January 3 and later annual reset/reprojection, multiple boundaries or DATA PERIODS, actual weather, and DST schedule-clock/tomorrow semantics remain unclaimed. Other Nth/last-weekday DST forms beyond that exact range, additional RunPeriod policy branches beyond the paired fixed-date Yes/No case, malformed headers, policy precedence beyond that exact fixed-date case, IDF DST behavior beyond it, DST-shifted schedule lookup and tomorrow rollover beyond the separate exact CP42 detailed-schedule case, and Rust raw ESO serialization remain unit/source evidence or unclaimed. calendar_special_day_cross_year_start_year_projection_hourly_exact_001 separately locks only one non-actual, single-DATA-PERIOD, one-record-per-hour 2031-12-30 through 2032-01-02 traversal. Its explicit Tuesday-start RunPeriod and one input-file 1st Thursday in January Holiday are resolved against the 2031 environment-start annual table and retained across that single boundary, producing 96 ordered-exact-unique zero-tolerance Site Day Type Index samples in daily order 3/4/5/8 with January 2 Holiday. The gate locks 96 raw EnergyPlus ESO values and hourly timestamp rows, exact rows Environment,CROSS YEAR SPECIAL DAY RUN PERIOD,WeatherFileRunPeriod,12/30/2031,01/02/2032,Tuesday,4,Use RunPeriod Specified Day,No,No,No,No,No,Clark and Allen and Environment:Special Days,CROSS YEAR NEW YEAR HOLIDAY,Holiday,InputFile,01/02,  1, and clean 0 Warning/0 Severe completion. A Gregorian 2032 January 1 reprojection is deliberately not claimed. Actual weather, multiple year boundaries or DATA PERIODS, later annual resets/reprojection, DST cross-year semantics, February 29 coupling, EPW holidays, weekend shifting, duration wrap, overlaps, warmup lifecycle parity, records per hour above one, broad WeatherManager behavior, and broad schedule behavior remain unclaimed. calendar_special_day_fixed_date_hourly_exact_001 adds only a fixed IDF February 29 duration-one Holiday projection on the same explicit three-day range: 72 ordered Site Day Type Index samples and normalized timestamps match at zero tolerance as 24 Sunday values, 24 Holiday values, and 24 Tuesday values (1/8/3). Its weather-file holidays, weekend-observation, and DST flags are explicitly No; the case does not prove those policy branches. The paired calendar_special_day_duration_wrap_common_year_hourly_exact_001 and calendar_special_day_duration_wrap_leap_year_hourly_exact_001 cases add two explicit January 1 through January 3 same-year RunPeriods with one fixed December 31 duration-three input-file Holiday and every RunPeriod policy explicitly No. Common-year 2017 resolves the start to day of year 365 and proves 72 ordered-exact-unique zero-tolerance samples in daily order 8/8/3; leap-year 2016 resolves day of year 366 and proves 72 samples in order 8/8/1. Each case contains exactly 48 Holiday=8 samples and locks only its common-year or leap-year same-year annual-table wrap source branch. The paired calendar_special_day_nth_weekday_hourly_exact_001 and calendar_special_day_last_weekday_hourly_exact_001 cases cover only the explicit 2032-02-22 through 2032-03-01 calendar and two exact IDF forms, both with Apply Weekend Holiday Rule=Yes and zero shift: `4th Sunday in February` resolves to 2/22, day of year 53, with 216 ordered-exact-unique zero-tolerance hourly samples in daily-index order 8/2/3/4/5/6/7/1/2; `Last Sunday in February` resolves to 2/29, day of year 60, with 216 samples in order 1/2/3/4/5/6/7/8/2. The calendar_special_day_weekend_rule_enabled_hourly_exact_001, calendar_special_day_weekend_rule_disabled_hourly_exact_001, and calendar_special_day_weekend_rule_blank_hourly_exact_001 cases isolate RunPeriod Apply Weekend Holiday Rule A5 as explicit Yes, explicit No, or genuinely blank around one fixed MonthDay Sunday 2/28 duration-one input-file Holiday on the explicit 2016 three-day calendar. Yes and blank both report apply_weekend_rule=true, resolve Monday 2/29, day of year 60, shift 1, and produce 72 ordered-exact-unique samples as 1/8/3; their oracle values and timestamp rows are identical. No resolves Sunday 2/28, day of year 59, shift 0, and produces 8/2/3. EnergyPlus 26.1's IDD and epJSON schema declare A5 default No, while this exact blank IDF executes as enabled; all three oracle runs complete with exactly 0 Warning and 0 Severe errors. This locks only the narrow fixed-Sunday branch and executable-observed blank-A5 mismatch. Omitted A5, native epJSON default behavior, other blank/default fields, EIO Special Days date semantics, warning text or repetition parity beyond the exact clean counts, and other dates/durations/types/rules remain unclaimed. The paired calendar_special_day_weekend_saturday_enabled_hourly_exact_001 and calendar_special_day_weekend_saturday_disabled_hourly_exact_001 cases isolate the explicit Apply Weekend Holiday Rule Yes/No policy around one fixed MonthDay Saturday 2/27 duration-one input-file Holiday over 2016-02-27 through 2016-02-29. Their 72 zero-tolerance ordered-exact-unique samples and normalized timestamps are enabled 24 Saturday=7, 24 Sunday=1, 24 observed Holiday=8 versus disabled 24 Holiday=8, 24 Sunday=1, 24 Monday=2. This proves only the fixed-Saturday plus-two-day observation branch; the fixed-Sunday plus-one-day branch remains separately evidenced. The paired calendar_epw_holiday_fixed_date_enabled_hourly_exact_001 and calendar_epw_holiday_fixed_date_disabled_hourly_exact_001 cases isolate the RunPeriod weather-file-holiday use flag against one fixed February 29 EPW holiday. Each has 72 zero-tolerance ordered-exact-unique Site Day Type Index samples and normalized timestamps: enabled is 24 Sunday=1, 24 source-exact EPW-holiday Sunday=1, and 24 Tuesday=3; disabled is 24 Sunday=1, 24 Monday=2, and 24 Tuesday=3. calendar_epw_holiday_weekday_rules_hourly_exact_001 adds only the explicit non-actual 2032-02-23 through 2032-02-25 RunPeriod with weather-file holidays enabled. Its EPW header rules `4th Monday in February` then `Last Wednesday in February` resolve in order to days of year 54 and 56 as weather-file Sunday/index 1, producing 72 ordered-exact-unique zero-tolerance samples in daily order 1/3/1 and exactly 0 Warning and 0 Severe errors. The claim excludes corresponding IDF forms, fixed EPW dates, other ordinals/weekdays/months/years/header orderings, overlaps, multiple policy combinations, warning parity beyond clean counts, EIO date semantics, schedule/tomorrow state, actual weather, and cross-year behavior. The paired calendar_special_day_overlap_zulu_then_alpha_hourly_exact_001 and calendar_special_day_overlap_alpha_then_zulu_hourly_exact_001 cases share the explicit 2016-02-28 through 2016-03-01 RunPeriod, one 72-row leap-observed EPW, and two duration-one February 29 definitions named `Zulu Holiday Definition` and `Alpha Custom Day Definition`; their IDFs differ only in those SpecialDays declarations' source order. Zulu then Alpha makes the later CustomDay1 definition win and proves 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3; Alpha then Zulu makes the later Holiday definition win and proves 72 samples in order 1/8/3. This locks only original IDF declaration-order recovery for `RunPeriodControl:SpecialDays` and later-definition overwrite for exactly two definitions on one fixed date. That pair does not itself claim native epJSON ordering, declaration order for other object types, other overlap shapes/counts/durations/types, EPW-versus-IDF precedence, or EnergyPlus warning text or repetition parity. calendar_special_day_epw_idf_precedence_hourly_exact_001 separately uses the explicit 2016-02-28 through 2016-03-01 RunPeriod with weather-file holidays enabled, one fixed February 29 EPW Holiday resolved first as the source-exact Sunday type, and one later duration-one input-file CustomDay1 definition on the same date. Its 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3 lock only weather-file-then-input-file resolution and the later IDF CustomDay1 overwrite for that one mixed collision. Reversed or multiple input definitions, other EPW or IDF rules/types/durations/overlap shapes, warning text/count/repetition parity, schedule lookup, tomorrow state, raw ESO serialization, actual-weather behavior, and cross-year execution or reprojection remain unclaimed. `calendar_special_day_nonexistent_fifth_weekday_failure_001` adds blocking smoke/nonclaim rejection evidence only for the explicit 2016-02-28 through 2016-03-01 `5th Sunday in February` duration-one input-file Holiday. Apply Weekend Holiday Rule is explicitly Yes and does not rescue the rule. EnergyPlus exits 1 from its `SetSpecialDayDates` not-enough-Nths Severe-to-Fatal path with 0 counted warnings, 1 severe error, and zero data rows; the Rust arbitrary-run exits 6 with phase `runtime`, code `RuntimeConvergenceFailure`, the exact special-day rejection, and zero hourly samples. Numeric exit-code equality and numerical Site Day Type Index conformance are not claimed. Other input-file ordinals, weekdays, months, years, or date strings; other EPW weekday rules, header orderings, and calendar rules; multiple errors or definitions; partial-artifact parity; weekend behavior beyond this explicit non-rescue and the declared fixed-Sunday Yes/No/blank and fixed-Saturday Yes/No cases; durations/dates/types or policy values beyond the declared cases; directly ordered typed-vector overwrite; overlap arrangements beyond the exact pair; declaration order beyond SpecialDays; schedule day-type lookup; tomorrow special-day state; raw successful-run ESO serialization; and actual-weather cross-year execution, non-actual cross-year execution beyond the exact single-boundary fixture, or per-year reprojection remain unit/source evidence or unclaimed. weather_record_start_offset_nonactual_001 separately proves a same-year non-actual July 1 through July 2 RunPeriod selecting 48 ordered hourly dry-bulb rows after 24 leading decoy rows from one DATA PERIOD with one record per hour; differing source row years prove only month/day matching. Weather-required heat-balance ep_run setup consumes the same metadata-aware axis and selector before execution, but that arbitrary-run wiring is not independent conformance evidence. Internal Today/Tomorrow source-index transitions, interpolation seeds, the day-local hour-24 solar NextHr, and the one-timestep-per-hour current-only solar branch remain unit evidence. RunPeriodControl:DaylightSavingTime behavior beyond the exact fixed-date precedence case, DST-shifted schedule lookup and hour-24 rollover beyond the separate exact CP42 detailed-schedule case, record selection beyond the declared offset case, runtime consumers outside the stated setup, actual-weather year matching, multiple-data-period execution, records-per-hour greater than one, design-day, sizing-period, warmup, subhourly solar interpolation, complete SetCurrentWeather/solar/WeatherManager conformance, and broad RunPeriod compatibility remain outside the claim. calendar_schedule_compact_through_for_day_type_hourly_exact_001 separately locks only the explicit non-actual 2031-12-30 through 2032-01-03 RunPeriod used by one schedule-only comparison. Its 120 ordered-exact-unique zero-tolerance Schedule Value samples follow daily order 103/104/105/108/199, while the gate separately locks raw schedule/day-type ESO values and timestamp fields, exact Environment, disabled daylight-saving, and January 2 input-file Holiday EIO rows, and clean 0/0 completion. Rust schedule ordinals 365/366/1/2/3 are source-mapped unit-test/summary diagnostics rather than ESO/EIO fields, and Rust EPW record selection is null and unclaimed. Additional RunPeriods, periods, or year boundaries; actual/design-day/warmup execution; DST schedule-clock/hour-24 tomorrow behavior beyond the separate exact CP42 detailed-schedule case; subhourly/interpolation beyond the separate exact CP43 aligned default-No case; internal-gain/HVAC/IdealLoads calendar consumption; Rust raw ESO parity; and broad warning/error parity remain unclaimed. calendar_schedule_dst_hour24_tomorrow_day_type_exact_001 separately locks only the explicit non-actual 2032-10-30 through 2032-11-01 RunPeriod and its source-exact final no-prefetch boundary. The 72 promoted Schedule Value samples are 100x23 then 124, 200x23 then 801, and 800x23 then 901; the gate also locks raw daily DST 0/1/1 and day types 7/1/8, exact Environment/daylight-saving/special-day EIO rows, and clean 0/0 completion. This proves only the detailed-schedule current-DST +1-hour clock, hour-24 next-ordinal/tomorrow-Holiday selection, and the final-run stale Holiday TomorrowVariables type on that one boundary. Other RunPeriods or DATA PERIODS, actual/design-day/warmup execution, subhourly behavior, downstream consumption, EMS/current-value semantics, Rust raw ESO parity, and broad warning/error parity remain unclaimed. calendar_schedule_compact_zone_timestep_exact_001 separately locks only the explicit one-day non-actual 2032-01-01 Thursday RunPeriod with all six calendar/weather policy flags No, exact Environment and disabled daylight-saving EIO rows, and 96 zone-timestep samples. Other RunPeriods, policies, environment kinds, and warmup/design-day/actual-weather execution remain outside this case."

[[object]]
name = "RunPeriodControl:DaylightSavingTime"
family = "time"
status = "typed"
first_evidence = "calendar_dst_epw_idf_precedence_hourly_exact_001"
support_boundary = "Typed only for one unique input-file daylight-saving object with fixed dates. calendar_dst_epw_idf_precedence_hourly_exact_001 uses the exact 2016-02-28 through 2016-03-01 RunPeriod with its weather-file DST flag No, the shared EPW fixed 2/29 through 3/1 declaration, and exactly one input-file 2/28 through 2/29 object. The input-file object wins and proves daily status 1/1/0, exactly 48 active hours, 72 ordered-exact-unique zero-delta EnergyPlus ESO values and timestamp fields, exact EnergyPlus 26.1 Environment and Environment:Daylight Saving rows, and clean 0 Warning/0 Severe completion. Rust retains the typed RunPeriodDaylightSavingTime plus input-file declaration and effective-source summary diagnostics; those diagnostics are not additional EIO output. Missing, invalid, or duplicate objects remain typed diagnostic paths only and do not claim EnergyPlus error text, count, phase, or exit parity. Other fixed dates, Nth/last-weekday or wrap rules, other years, multiple RunPeriods, weather-file enablement with the object, no-EPW execution, schedule/civil-clock effects, actual or cross-year weather, DesignDay environments, Rust raw ESO serialization, and broad WeatherManager behavior remain unclaimed."

[[object]]
name = "RunPeriodControl:SpecialDays"
family = "time"
status = "typed"
first_evidence = "calendar_special_day_fixed_date_hourly_exact_001"
support_boundary = "Typed input and shared calendar/day-type projection, with zero-tolerance ordered-exact-unique external evidence for eleven fixed-date input-file cases, two exact same-year weekday-rule input-file cases, and one exact cross-year start-year weekday-rule case. calendar_special_day_fixed_date_hourly_exact_001 proves one fixed 2/29 duration-one Holiday over 2016-02-28 through 2016-03-01 as exactly 72 Site Day Type Index samples and normalized timestamps: 24 Sunday=1, 24 Holiday=8, and 24 Tuesday=3. Weather-file holidays, weekend observation, and DST are explicitly disabled in that fixture. calendar_special_day_nth_weekday_hourly_exact_001 proves only the explicit 2032 `4th Sunday in February` IDF form: it resolves to 2/22, day of year 53, with Apply Weekend Holiday Rule explicitly Yes, zero shift, and 216 ordered-exact-unique zero-tolerance samples in daily-index order 8/2/3/4/5/6/7/1/2. calendar_special_day_last_weekday_hourly_exact_001 likewise proves only `Last Sunday in February` on the same calendar: it resolves to 2/29, day of year 60, with zero shift and 216 samples in order 1/2/3/4/5/6/7/8/2. calendar_special_day_cross_year_start_year_projection_hourly_exact_001 separately proves only one input-file 1st Thursday in January Holiday resolved against the 2031 environment-start annual table and retained across the single non-actual 2031-12-30 through 2032-01-02 boundary. The 96 exact ordered samples are 3/4/5/8 with January 2 Holiday; exact ESO values/timestamp rows, the Environment and Environment:Special Days EIO rows, and clean 0/0 completion are locked. A Gregorian 2032 January 1 reprojection is not claimed. Actual weather, later annual resets/reprojection, multiple boundaries or DATA PERIODS, DST cross-year semantics, February 29 coupling, and the other manifest nonclaims remain outside this object evidence. The calendar_special_day_weekend_rule_enabled_hourly_exact_001, calendar_special_day_weekend_rule_disabled_hourly_exact_001, and calendar_special_day_weekend_rule_blank_hourly_exact_001 cases use one fixed MonthDay Sunday 2/28 duration-one Holiday over the explicit 2016 three-day calendar and differ only in RunPeriod A5: explicit Yes, explicit No, or genuinely blank. Yes and blank both resolve to Monday 2/29, day of year 60, shift 1, and produce 72 ordered-exact-unique samples as 24 Sunday=1, 24 observed Holiday=8, and 24 Tuesday=3; their oracle values and timestamp rows are identical. No keeps the Holiday on Sunday as 24 Holiday=8, 24 Monday=2, and 24 Tuesday=3. EnergyPlus 26.1's IDD and epJSON schema declare the A5 default as No, while this exact blank IDF executes with apply_weekend_rule=true. All three oracle runs complete with exactly 0 Warning and 0 Severe errors. This proves only the fixed-Sunday plus-one-day executable branch and that narrow blank-A5 mismatch; omitted A5, native epJSON default behavior, other blank/default fields, EIO Special Days date semantics, and warning text or repetition parity beyond the exact clean counts remain unclaimed. The paired calendar_special_day_weekend_saturday_enabled_hourly_exact_001 and calendar_special_day_weekend_saturday_disabled_hourly_exact_001 cases isolate the explicit Apply Weekend Holiday Rule Yes/No policy around one fixed MonthDay Saturday 2/27 duration-one input-file Holiday over 2016-02-27 through 2016-02-29. Their 72 zero-tolerance ordered-exact-unique samples and normalized timestamps are enabled 24 Saturday=7, 24 Sunday=1, 24 observed Holiday=8 versus disabled 24 Holiday=8, 24 Sunday=1, 24 Monday=2. This proves only the fixed-Saturday plus-two-day observation branch; the fixed-Sunday plus-one-day branch remains separately evidenced. The paired calendar_special_day_duration_wrap_common_year_hourly_exact_001 and calendar_special_day_duration_wrap_leap_year_hourly_exact_001 cases add two fixed-date input-file cases with one December 31 duration-three Holiday over explicit January 1 through January 3 same-year annual tables and all RunPeriod policies No. Common-year 2017 resolves day of year 365 and proves 72 ordered-exact-unique zero-tolerance samples in daily order 8/8/3; leap-year 2016 resolves day of year 366 and proves 72 samples in order 8/8/1. Each has exactly 48 Holiday=8 samples. This proves only the common-year and leap-year cyclic annual-table source branches. The paired calendar_special_day_overlap_zulu_then_alpha_hourly_exact_001 and calendar_special_day_overlap_alpha_then_zulu_hourly_exact_001 cases share the explicit 2016-02-28 through 2016-03-01 RunPeriod, one 72-row leap-observed EPW, and two duration-one February 29 definitions named `Zulu Holiday Definition` and `Alpha Custom Day Definition`; their IDFs differ only in those SpecialDays declarations' source order. Zulu then Alpha makes the later CustomDay1 definition win and proves 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3; Alpha then Zulu makes the later Holiday definition win and proves 72 samples in order 1/8/3. This locks only original IDF declaration-order recovery for `RunPeriodControl:SpecialDays` and later-definition overwrite for exactly two definitions on one fixed date. That pair does not itself claim native epJSON ordering, declaration order for other object types, other overlap shapes/counts/durations/types, EPW-versus-IDF precedence, or EnergyPlus warning text or repetition parity. calendar_special_day_epw_idf_precedence_hourly_exact_001 separately uses the explicit 2016-02-28 through 2016-03-01 RunPeriod with weather-file holidays enabled, one fixed February 29 EPW Holiday resolved first as the source-exact Sunday type, and one later duration-one input-file CustomDay1 definition on the same date. Its 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3 lock only weather-file-then-input-file resolution and the later IDF CustomDay1 overwrite for that one mixed collision. Reversed or multiple input definitions, other EPW or IDF rules/types/durations/overlap shapes, warning text/count/repetition parity, schedule lookup, tomorrow state, raw ESO serialization, actual-weather behavior, and cross-year execution or reprojection remain unclaimed. The separate enabled/disabled fixed EPW holiday pair and exact two-rule EPW weekday case contain no RunPeriodControl:SpecialDays object; they prove only their declared RunPeriod policy and weather-file holiday behavior, so they do not widen this object boundary. `calendar_special_day_nonexistent_fifth_weekday_failure_001` adds blocking smoke/nonclaim rejection evidence only for one explicit 2016 duration-one `5th Sunday in February` input-file Holiday. Apply Weekend Holiday Rule=Yes does not rescue it. EnergyPlus exits 1 through the `SetSpecialDayDates` not-enough-Nths Severe-to-Fatal path with 0 counted warnings, 1 severe error, and zero data rows; Rust arbitrary-run exits 6 with phase `runtime`, code `RuntimeConvergenceFailure`, the exact special-day rejection, and zero hourly samples. Exit-number equality and numerical Site Day Type Index conformance are not claimed. Other input-file ordinals, weekdays, months, years, or strings; policy values beyond the exact Yes/No/blank-A5 cases; EPW holidays beyond the one exact mixed collision; durations, dates, or special-day types beyond the paired exact wrap cases; directly ordered typed-vector overwrite; multiple errors or definitions beyond the exact overlap pair; partial-artifact parity; overlap arrangements beyond the exact pair; declaration order beyond SpecialDays; actual-weather cross-year execution, non-actual cross-year execution beyond the exact single-boundary fixture, or per-year reprojection; schedule day-type lookup; tomorrow special-day state; raw successful-run ESO serialization; and broad SpecialDays compatibility remain unit/source evidence or unclaimed."

[[object]]
name = "Site:Location"
family = "site"
status = "typed"
first_evidence = "weather_fields_001"
support_boundary = "Typed for location metadata; weather conformance is limited to declared dry-bulb output."

[[object]]
name = "Material"
family = "material"
status = "typed"
first_evidence = "construction_materials_001"
support_boundary = "Typed for static material and construction evidence; no broad dynamic material algorithm claim."

[[object]]
name = "Material:NoMass"
family = "material"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Typed for declared opaque no-mass heat-balance cases only."

[[object]]
name = "Material:AirGap"
family = "material"
status = "typed"
first_evidence = "material_opaque_variants_001"
support_boundary = "Typed as the positive R-only opaque air-gap variant with fixed source defaults and middle-layer construction validation. material_opaque_variants_001 adds nonblocking diagnostic EnergyPlus 26.1 EIO evidence only for the exact two-gap paired-interzone fixture: grouped layer count, material name, outside-to-inside order, and thermal resistance. Dynamic air-gap heat transfer, window-gap families, CondFD behavior, broad construction use, and a material-family conformance claim remain outside this evidence."

[[object]]
name = "Material:InfraredTransparent"
family = "material"
status = "typed"
first_evidence = "material_opaque_variants_001"
support_boundary = "Typed as the name-only opaque infrared-transparent variant with fixed source values and the intended sole-layer construction invariant. material_opaque_variants_001 adds nonblocking diagnostic EnergyPlus 26.1 EIO evidence only for the exact sole-layer paired-interzone fixture: grouped layer count, source-declared material identity, order, and thermal resistance 0.01 m2-K/W. The generic Material CTF Summary row shape is not treated as an independent object-type discriminator. IRT interzone warnings, the upstream non-first-layer validation gap and exact diagnostics, CondFD behavior, dynamic heat transfer, and conformance remain unclaimed."

[[object]]
name = "WindowMaterial:Glazing:RefractionExtinctionMethod"
family = "material"
status = "typed"
first_evidence = "window_glazing_refraction_extinction_001"
support_boundary = "The complete EnergyPlus 26.1 input object is typed with all five required refraction/extinction fields, five defaulted thermal/dirt/diffusing fields, shared material-name resolution, and source-order normal-incidence optical derivation. The bounded implementation intentionally reproduces the 26.1 visible-back-reflectance assignment to solar-front reflectance. window_glazing_refraction_extinction_001 adds nonblocking diagnostic exact-EIO evidence for thickness plus the derived and emitted material fields, with oracle-only proof of use by one detailed window. The four raw solar/visible refraction-index and extinction-coefficient inputs remain typed-only because EIO omits them. Fenestration surfaces, window runtime optics/heat transfer, broad construction layering, blocking compatibility, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:Glazing:EquivalentLayer"
family = "material"
status = "typed"
first_evidence = "window_glazing_equivalent_layer_001"
support_boundary = "The complete EnergyPlus 26.1 source-effective input object is typed after RefractionExtinctionMethod: all 26 numeric inputs, the SpectralAverage-only mode, six bounded Autocalculate-or-number fields, defaults, shared material-name resolution, and its separate equivalent-layer consumer family. The spectral dataset string is type-checked then discarded because EnergyPlus ignores it for the only supported mode. window_glazing_equivalent_layer_001 adds nonblocking diagnostic exact-EIO evidence for material identity and all 14 emitted solar/infrared numeric fields. The 11 visible optical inputs and thermal resistance remain typed-only because EIO omits them; the exact one-solid Construction:WindowEquivalentLayer and detailed-window rows are oracle-only fixture-integrity evidence. Ordinary Construction rejects this family, and Construction:WindowEquivalentLayer typing, occurrence parity, ASHWAT optics/ratings, fenestration runtime, spectral-dataset input preservation, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:Gas"
family = "material"
status = "typed"
first_evidence = "window_material_gas_001"
support_boundary = "The complete EnergyPlus 26.1 single-gas input object is typed in GetMaterialData source order after equivalent-layer glazing. Gas type and positive thickness are required; Air, Argon, Krypton, and Xenon resolve to the exact built-in source constants and replace valid supplied custom-property values. Custom preserves the three A+B*T+C*T^2 coefficient sets, requires positive viscosity A and specific-heat A plus molecular weight in the schema range [20,200], requires conductivity A+300*B+90000*C greater than zero, and derives nominal resistance at 300 K. A supplied specific-heat ratio must be greater than 1, while a missing Custom ratio is intentionally retained as zero, matching the 26.1 legacy numeric-blank value whose source path omits validation. The single-gas branch of ordinary Construction supports Glass (Gas Glass){0..3}, with ordinary glazing at both ends and at most four panes/three gaps; WindowMaterial:GasMixture separately occupies the same gap positions. window_material_gas_001 adds nonblocking diagnostic EnergyPlus 26.1 EIO evidence for every ordinary-window Construction gas-layer occurrence, including duplicate reuse, across Air, Argon, Krypton, Xenon, and Custom. Occurrence multiplicity, material name, and canonical gas type are exact; thickness is exact after reproducing the EnergyPlus 26.1 {:.3R} EIO serialization policy. Unused gas definitions are excluded. EIO omits Custom coefficient/property values and nominal resistance, so those remain typed-test evidence only. Arbitrary-run assessment explicitly reports every typed WindowMaterial:Gas occurrence as unsupported and blocks execution. Window optics, thermal behavior, runtime execution, broad occurrence ordering, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:Gap:EquivalentLayer"
family = "material"
status = "typed"
first_evidence = "window_material_gap_equivalent_layer_001"
support_boundary = "The complete EnergyPlus 26.1 equivalent-layer single-gas gap input is typed in GetMaterialData source order after WindowMaterial:Gas. The object requires the schema's uppercase AIR, ARGON, KRYPTON, XENON, or CUSTOM token, positive thickness, and exact Sealed, VentedIndoor, or VentedOutdoor mode. Standard gas records use the same source constants as WindowMaterial:Gas and replace valid supplied custom values. CUSTOM preserves all three A+B*T+C*T^2 coefficient sets, validates the source/schema bounds, retains numeric zero for a missing specific-heat ratio, requires positive 300 K conductivity, and derives nominal resistance. The schema's air-only vent note is not promoted to a validation rule because Material.cc does not enforce it. The material belongs to the equivalent-layer family, ordinary Construction rejects it, and arbitrary-run assessment explicitly blocks it. window_material_gap_equivalent_layer_001 adds nonblocking diagnostic exact-EIO evidence for the fixture-ordered Construction:WindowEquivalentLayer gap occurrence sequence across all five gases and all three vent modes, including one reused material and one unused definition excluded by the oracle. Material name, canonical gas and vent types, and source-{:.3R}-normalized thickness are exact. Construction names/declarations are aligned with converted-epJSON canonical order, so arbitrary IDF construction declaration-order parity remains unclaimed. EIO omits Custom coefficients/properties and nominal resistance. Construction:WindowEquivalentLayer typing/ratings, the EnergyPlus 26.1 ASHWAT coefficient-copy/evaluation quirks, its construction-local sub-0.0001 m thickness replacement, window optics/thermal runtime, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:GasMixture"
family = "material"
status = "typed"
first_evidence = "window_material_gas_mixture_001"
support_boundary = "The complete EnergyPlus 26.1 source-effective gas-mixture input is typed immediately after WindowMaterial:Gap:EquivalentLayer. Thickness must be positive and the integer active count is 1..=4. The schema always requires and validates Gas 1 and Gas 2 type/fraction pairs, even when count=1; the bounded compiler then retains only the declared active prefix. Gas 3/4 fields are optional, but an active position requires its type while a missing active optional fraction preserves the source numeric-blank value 0.0. Every explicitly supplied fraction must be >0 and <=1, including inactive fields before valid inactive data is discarded. Components allow only Air, Argon, Krypton, or Xenon, preserve order and duplicates, and use the exact built-in property records. No fraction-sum, uniqueness, or normalization rule is added. Nominal resistance intentionally uses only the first active gas's 300 K conductivity. The material belongs to the ordinary fenestration family and is accepted in the same ordinary-Construction Glass/gap alternation as WindowMaterial:Gas. It is outside the EquivalentLayer family; Construction:WindowEquivalentLayer typing/validation and the later WindowMaterial:Gap complex-fenestration reference consumer remain deferred. Arbitrary-run assessment blocks the mixture. window_material_gas_mixture_001 adds nonblocking diagnostic EnergyPlus 26.1 evidence for six generic Material Details definition rows, including one unused definition and edge-case active prefixes. By-name identity, exactly-one definition multiplicity, MediumRough, source-{:.4R}-serialized thickness, and the fixed zero resistance, conductivity, density, specific-heat, and absorptance fields are exact. The shared WindowMaterial:Gas header is exact and has zero gas data rows. These generic rows expose no component count, species, fractions, order, nominal resistance, or occurrence/use/reuse status. Two seven-layer WindowConstruction rows and the host/window rows are oracle-only fixture locks. Mixture thermal runtime, construction ratings, window behavior, declaration-order parity, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:Shade"
family = "material"
status = "typed"
first_evidence = "window_material_shade_001"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Shade input is typed immediately after WindowMaterial:GasMixture. Five optical inputs are bounded [0,1), infrared hemispherical emissivity is bounded (0,1), thickness and conductivity are positive, and the solar, visible, and infrared input pairs must each sum to less than 1. MediumRough is fixed; solar absorptance is max(0,1-transmittance-reflectance), visible absorptance deliberately remains the source-initialized zero, and nominal resistance is thickness/conductivity. Shade-to-glass distance defaults to 0.05 m in [0.001,1], four opening multipliers default to 0.5 in [0,1], and airflow permeability defaults to 0 in [0,0.8]. The ordinary-Construction subset accepts an exterior or interior shade directly adjacent to one through four panes in the existing Glass/gap alternation, plus only the exact double- and triple-pane between-glass positions. It permits at most one shade and eight total layers, rejects solar-diffusing glass with a shade, and requires exact adjacent five-slot gas type/fraction signatures plus gap widths within 0.0005 m for between-glass use. A single gas retains four inactive Custom/zero slots while a mixture resets inactive slots to Invalid/zero, so those object kinds do not cross-match even with the same active gas. Custom gap polynomial records are intentionally excluded from the source comparison. Rust safely rejects Shade-Gap-Glass and Glass-Gap-Shade end patterns instead of reproducing EnergyPlus 26.1's access-violation and unintended-acceptance paths. Arbitrary-run assessment counts all typed shade definitions, including unused definitions, and blocks execution. window_material_shade_001 adds nonblocking diagnostic EnergyPlus 26.1 evidence for two generic Material Details definition rows and two specialized shade construction-layer occurrences. The generic comparison is by name and exactly-once, includes the unused/defaulted definition, and locks MediumRough, source-{:.4R} thickness, source-{:.3R} conductivity, source-{:.4R} infrared emissivity and derived solar absorptance, plus fixed zero resistance/density/specific heat/visible absorptance. The specialized header is exact; exterior/interior reuse emits two duplicate-aware rows with source-{:.3R} thickness, conductivity, infrared emissivity, solar transmittance, visible transmittance, and solar reflectance, while the unused definition is absent. Three exact WindowConstruction rows and the host/two-window surface rows are oracle-only fixture locks. Visible reflectance, infrared transmittance, placement/opening/permeability inputs, nominal resistance, between-glass reporting, shading controls, ratings, optics/thermal/daylighting runtime, surfaces, broad order, diagnostics, and conformance remain unclaimed."

[[object]]
name = "WindowMaterial:Shade:EquivalentLayer"
family = "material"
status = "typed"
first_evidence = "compiler::tests::window_material_shade_equivalent_layer::shade_equivalent_layer_materializes_all_inputs_and_source_quirks"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Shade:EquivalentLayer input is typed immediately after ordinary WindowMaterial:Shade. The shared beam-beam solar transmittance defaults to 0 in [0,0.8]; the four directional beam-diffuse solar transmittance/reflectance fields are required in [0,1). Three optional visible inputs preserve the source numeric-buffer blank value 0, infrared transmittance defaults to 0.05 in [0,1), and directional infrared emissivities default to 0.91 in (0,1). The compiler enforces the source's five strict sums: shared beam-beam plus each side's solar beam-diffuse transmittance/reflectance, all three visible inputs, and infrared transmittance plus each emissivity must be below 1. EnergyPlus copies the solar beam-beam value to front/back but stores the nominally shared visible inputs only in front TAR slots; Rust exposes the initialized-zero back-visible state rather than inventing symmetry. MediumRough and resistance-only behavior are fixed; directional thermal absorptance equals the corresponding emissivity and thermal transmittance equals infrared transmittance. Base resistance, nominal resistance, thickness, conductivity, density, specific heat, and scalar absorptances remain zero or absent. The material belongs to the equivalent-layer family, ordinary Construction rejects it, Construction:WindowEquivalentLayer remains deferred, and arbitrary-run assessment counts and blocks all typed definitions including unused ones. window_material_shade_equivalent_layer_001 adds nonblocking diagnostic EnergyPlus 26.1 EIO evidence. Its generic Material Details gate compares every definition including the unused one exactly once by normalized name and locks MediumRough plus zero resistance, thickness, conductivity, density, specific heat, and all scalar absorptances. Its exact-header specialized gate compares duplicate-aware Construction:WindowEquivalentLayer occurrences: one defaulted used shade row, two reused high-precision shade rows, and no unused-definition row. It locks the duplicated front/back beam-beam solar value, four directional beam-diffuse solar values, infrared transmittance, and both infrared emissivities with source-{:.4R} serialization. A surface-unused construction, nominally referenced only by a fixture EnergyManagementSystem:ConstructionIndexVariable to suppress the oracle warning, still emits an occurrence, while two surfaces sharing another construction do not multiply it. The three visible inputs are absent from EIO. Equivalent-layer construction typing/packing, arbitrary declaration-order parity, ASHWAT roller-blind and openness-adjusted longwave behavior, visible optical use, ratings, surfaces, EMS behavior, runtime execution, broad diagnostic/order parity, and conformance remain unclaimed."

[[object]]
name = "WindowMaterial:Drape:EquivalentLayer"
family = "material"
status = "typed"
first_evidence = "compiler::tests::window_material_drape_equivalent_layer::drape_equivalent_layer_materializes_all_inputs_defaults_and_source_quirks"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Drape:EquivalentLayer input is typed as source-order object 13 immediately after WindowMaterial:Shade:EquivalentLayer. Its 13 numeric fields preserve these defaults and bounds: shared solar beam-beam transmittance defaults to 0 in [0,0.2]; four required directional solar beam-diffuse transmittance/reflectance fields are in [0,1); three optional visible inputs retain source-buffer blank zero in [0,1); infrared transmittance defaults to 0.05 in [0,1); front/back infrared emissivity defaults to 0.87 in (0,1); and pleated-fabric width/length default to 0 m with minimum 0. Only the three source-owned sums are enforced: front solar and shared visible sums must be below 1, while front infrared fails only above 1 and therefore accepts equality. EnergyPlus performs no back solar or back infrared sum check, so Rust deliberately adds neither. The shared solar beam-beam value is copied to front/back, while visible values are stored front-only and back-visible remains zero. MediumRough and resistance-only behavior are fixed; directional thermal absorptance equals emissivity and thermal transmittance equals infrared transmittance; base/nominal resistance and other scalar thermal/material projections remain zero or absent. Pleat dimensions are retained only when both inputs are present and nonzero; otherwise both effective dimensions are zero and is_pleated is false. The name uses the shared normalized material namespace, the payload belongs to the EquivalentLayer family, ordinary Construction rejects it, Construction:WindowEquivalentLayer remains deferred, and arbitrary-run assessment counts and explicitly blocks all typed definitions including unused ones. window_material_drape_equivalent_layer_001 adds a nonblocking diagnostic EnergyPlus 26.1 EIO gate. Its generic Material Details comparison requires every definition exactly once by normalized name, including the definition unused by every construction, and locks MediumRough plus zero resistance, thickness, conductivity, density, specific heat, and scalar absorptances. Its specialized comparison deliberately preserves the malformed source shape: the exact 14-token header advertises separate front/back beam-beam columns and contains an empty column, while each 12-token data row emits the shared beam-beam value only once. The exact A,Z,Z,P,Q occurrence sequence locks one defaulted drape, two reused high-precision drapes, and two one-sided input cases whose source-effective pleat dimensions are zero; the unused definition is excluded. N1, N2-N5, and N9-N11 use source-{:.4R} serialization and the two effective pleat dimensions use source-{:.5R}. Visible N6-N8 are absent. The specialized header requires Output:Constructions selecting Constructions, at least one drape definition, and at least one window construction; Materials-only emits no header. Surfaces do not multiply construction occurrences, and surface-unused constructions still emit independently of surface use. Fixture-only EMS construction-index references merely suppress oracle unused-construction warnings and are not compared. Equivalent-layer construction typing/packing, arbitrary declaration-order parity, ASHWAT drape coefficients, numerical optics/thermal and pleat behavior, ratings, daylighting, surfaces, EMS behavior, runtime, broad diagnostics/order, and conformance remain unsupported."

[[object]]
name = "WindowMaterial:Screen"
family = "material"
status = "typed"
first_evidence = "compiler::tests::window_material_screen::window_screen_materializes_defaults_and_source_derived_properties"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Screen input is typed as source-order object 14 immediately after WindowMaterial:Drape:EquivalentLayer. Reflected beam transmittance accounting accepts exactly DoNotModel, ModelAsDiffuse, or ModelAsDirectBeam and defaults to ModelAsDiffuse when missing or blank. Diffuse solar and visible reflectance plus screen spacing and diameter are required; reflectances are in [0,1), spacing and diameter are positive, and diameter must be strictly less than spacing. Thermal emissivity defaults to 0.9 in (0,1), conductivity defaults to 221 W/m-K and is positive, screen-to-glass distance defaults to 0.025 m in [0.001,1], four opening multipliers default to zero in [0,1], and map resolution defaults to zero and accepts only 0, 1, 2, 3, or 5 degrees. With r=diameter/spacing, open-area transmittance tau=(1-r)^2, and solid fraction f=1-tau, Rust preserves the source-effective solar/visible reflectances and thermal absorptance scaled by f, derives solar/visible absorptance as max(0,1-tau-effective reflectance), projects visible/thermal transmittance plus airflow permeability to tau, fixes MediumRough/resistance-only behavior, and derives nominal resistance as f*diameter/conductivity. The three source strict-sum checks use adjusted assembly values tau+raw*(1-tau), not the unadjusted tau+raw reflectance; valid schema inputs make the adjusted sums algebraically less than one while explicit checks preserve finite-precision parity. The shared normalized material namespace and GetMaterialData order include Screen, and the payload belongs to the Fenestration family. Ordinary Construction accepts only one exterior Screen directly followed by Glass ((Gas|GasMixture) Glass){0..3}; it rejects interior/between-glass screens, multiple shading devices, solar-diffusing glazing, and the Screen-Gap-Glass source-validation hole that later reaches an unsafe layer-2 glazing cast/assert in EnergyPlus 26.1. Arbitrary-run assessment counts and blocks every typed definition, including unused definitions. General angle-dependent TAR, transmittance-map output, WindowShadingControl and surface behavior, window optics/thermal runtime, daylighting, broad diagnostics/order, and conformance remain unsupported. The separate addendum registers only the bounded static Screen EIO family case and proof variables."
support_boundary_addendum = "window_material_screen_001 adds a nonblocking diagnostic EnergyPlus 26.1 static EIO gate without promoting runtime or conformance. Its exact generic Material Details gate requires one row for each of the Z,M,A definitions, including unused M, and locks MediumRough, zero resistance/density/specific heat, source-{:.4R} wire-diameter thickness, source-{:.3R} conductivity, and source-{:.4R} solid-fraction thermal/solar/visible absorptances. Its exact 12-token specialized header retains the source Screen To GlassDistance typo. The construction-layer occurrence sequence A,Z,Z excludes M and locks source-{:.5R} thickness plus source-{:.3R} conductivity, thermal absorptance, normal-incidence beam transmittance and solar/visible reflectance, quarter-hemisphere diffuse solar/visible reflectance, diameter/spacing ratio, and screen-to-glass distance. The bounded comparator replays the EnergyPlus 26.1 CalcScreenTransmittance normal-incidence path and reverse-order 18 by 18 CalcWindowScreenProperties integration for fixture ExteriorScreen-activated A and Z only. A constant-zero control schedule still initializes both definitions. Two surfaces sharing C do not multiply its Z row; surface-unused D emits another Z row because C initialized the shared material, while a fixture-only EMS construction-index reference only suppresses an oracle warning. The primary, Materials-only, and Constructions-only lanes lock independent generic/specialized header activation and all complete with zero warnings and zero severe errors. DoNotModel specialized optics, arbitrary activation/declaration order, general angle-dependent TAR and map output, opening multipliers, window optics/thermal runtime, shading-control/surface behavior, ratings, daylighting, broad diagnostics, and conformance remain unclaimed."

[[object]]
name = "WindowMaterial:Screen:EquivalentLayer"
family = "material"
status = "typed"
first_evidence = "compiler::tests::window_material_screen_equivalent_layer::screen_equivalent_layer_materializes_all_inputs_defaults_and_source_quirks"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Screen:EquivalentLayer input is typed as source-order object 15 immediately after ordinary WindowMaterial:Screen. Screen beam-beam solar transmittance preserves the default Autocalculate sentinel and otherwise accepts a number in [0,1); the five beam-diffuse solar transmittance/reflectance and beam-beam/beam-diffuse/diffuse-reflectance visible inputs are required in [0,1). Infrared transmittance defaults to 0.02 in [0,1), and infrared emissivity defaults to 0.93 in (0,1). Although the schema advertises positive wire-spacing/diameter defaults of 0.025/0.005 m, GetMaterialData's blank guards leave missing or blank source-effective geometry at the MaterialScreenEQL initial 0/0 m; Rust preserves that quirk. Explicit geometry uses the source greater-than-0.00001 m threshold and diameter-below-spacing relationship. EnergyPlus can emit a Severe message, substitute 0.025 m or 0.005 m, and continue through some violations of those geometry rules; Rust fail-closes those repair branches and does not materialize the post-Severe values. N1-N3 are shared front/back solar state. N4/N5 populate only the front visible beam record, N6 populates the front diffuse-diffuse reflectance slot rather than beam-diffuse reflectance, and back-visible remains initialized to zero. Infrared values are shared front/back; directional thermal absorptance equals emissivity and thermal transmittance equals infrared transmittance. Only numeric N1 plus N3 and N4 plus N6 must be below 1; beam-diffuse transmittances are omitted, and the source's nominal infrared check reads untouched scalar AbsorpThermal zero rather than directional emissivity, so Rust adds no infrared transmittance-plus-emissivity sum. EnergyPlus emits a Severe message and rewrites diameter without setting local ErrorsFound only when numeric N1 exceeds geometry-derived openness by more than one percent; Rust fail-closes that one-sided recovery branch instead of materializing after a Severe diagnostic and does not invent a symmetric closeness rule. MediumRough and resistance-only behavior are fixed; base/nominal resistance, thickness, conductivity, density, specific heat, and scalar absorptance projections remain zero or absent. The material uses the shared normalized namespace and EquivalentLayer family, ordinary Construction rejects it, Construction:WindowEquivalentLayer remains deferred, and arbitrary-run assessment counts and blocks all definitions including unused ones. Equivalent-layer construction typing/packing, CheckAndFixCFSLayer/IS_OPENNESS/ASHWAT behavior, ratings, surfaces, exact diagnostic recovery/text, runtime, and conformance remain unsupported. The separate addendum registers only the bounded static EIO parser/comparator case and its two diagnostic proof variables."
support_boundary_addendum = "window_material_screen_equivalent_layer_001 adds a nonblocking diagnostic EnergyPlus 26.1 static EIO gate and bounded Rust ep_compare/ep_cli comparator without promoting runtime or conformance. The generic Material Details comparison requires exactly one MediumRough/all-zero row for each fixture definition in source Z,M,A order, including M UNUSED EQL SCREEN, which is absent from every construction. The specialized comparison preserves the malformed source shape: its exact header has nine comma-separated tokens while every data row has twelve, and the exact equivalent-layer construction occurrence sequence is A,Z,Z with M excluded. A locks raw Autocalculate as -99999.0000 and blank wire spacing/diameter as 0.00000/0.00000; Z locks high-precision source formatting and reuse. Rows emit shared N1 once, duplicate N2/N3 into front/back solar slots, emit shared N7 once, duplicate N8 into front/back infrared-emissivity slots, omit visible N4-N6, serialize solar/infrared values with source-{:.4R}, and serialize geometry with {:.5R}. Construction B is shared by two surfaces without multiplying its Z row; surface-unused A and C are nominally retained only by fixture EnergyManagementSystem:ConstructionIndexVariable references and emit A and the second Z. The primary lane emits both generic and specialized tables, Materials-only emits generic Z,M,A and maps the expected missing specialized header to an empty occurrence set, and Constructions-only emits specialized A,Z,Z without generic rows; all three oracle runs must be warning- and severe-free. Exact equivalent-layer construction rows and surface/EMS topology are fixture-integrity locks only. Rust EIO serialization, Construction:WindowEquivalentLayer typing/packing, CheckAndFixCFSLayer/IS_OPENNESS/ASHWAT behavior, optics/thermal behavior, ratings, surfaces, EMS behavior, runtime, broad diagnostics/order, and conformance remain unclaimed."

[[object]]
name = "WindowMaterial:Blind"
family = "material"
status = "typed"
first_evidence = "compiler::tests::window_material_blind::blind_projects_all_source_fields_defaults_and_base_state"
support_boundary = "The complete EnergyPlus 26.1 source-effective WindowMaterial:Blind input is typed as source-order object 16 immediately after WindowMaterial:Screen:EquivalentLayer. Slat orientation accepts Horizontal or Vertical and defaults to Horizontal. Slat width and separation are required in (0,1] m; thickness defaults to 0.00025 m in (0,0.1] m; angle defaults to 45 degrees in [0,180]; and conductivity defaults to 221 W/m-K and is positive. Front/back beam-solar reflectance, front/back diffuse-solar reflectance, and beam-visible transmittance are the other required values. All optical inputs are in [0,1): beam/diffuse solar transmittance, diffuse-visible transmittance, and infrared transmittance default to zero; front/back infrared emissivity defaults to 0.9; and the four optional visible reflectances preserve source-buffer blank zero. Blind-to-glass distance defaults to 0.05 m in [0.01,1], the top/bottom/left/right opening multipliers default to 0.5/0.0/0.5/0.5 in [0,1], and minimum/maximum slat angles default to 0/180 degrees with independent [0,180] bounds. The compiler enforces all ten source strict-below-one transmittance-plus-reflectance/emissivity sums and the six beam/diffuse equality rules, rejecting only absolute differences greater than 1e-5. Width below separation remains a materializing warning. Blind-to-glass distance must be at least half the slat width; the inverse-sine geometry limits apply only when separation plus thickness is less than width; and the fixed-angle source branch adds no N26/N27 relationship. N6-N20 project into their source solar, visible, and infrared slat-property slots. Rough/resistance-only behavior is fixed; slat crown, unassigned optical slots, base thickness/conductivity/resistance/scalar absorptances, and nominal resistance remain zero or absent. The shared namespace and Fenestration family permit one safe exterior, interior, double-between, or triple-between Blind in ordinary Construction while enforcing the common maximum-one Shade/Screen/Blind and solar-diffusing exclusions. Between-glass gaps must have the same source five-slot gas type/fraction signature, differ in width by no more than 0.0005 m, and have a combined width at least the slat width; unsafe exterior/interior Blind-Gap-Glass end holes and every placement outside the bounded topologies fail closed. Arbitrary-run assessment counts and blocks every definition including unused ones. The separate addendum bounds its nonblocking static EIO evidence. CalcBlindProperties, beam-beam and other optics, daylighting, shading-control/surface behavior, variable slat-angle and window thermal runtime, exact diagnostic order/text/multiplicity, Construction:WindowEquivalentLayer, WindowMaterial:Blind:EquivalentLayer, and conformance remain unsupported outside that evidence."
support_boundary_addendum = "window_material_blind_001 adds a nonblocking diagnostic EnergyPlus 26.1 static EIO gate and bounded Rust ep_compare/ep_cli comparator without promoting runtime or conformance. The generic Material Details comparison requires exactly one normalized-name row for each fixture definition, including construction-unused M, while the smoke gate locks source declaration order Z,M,A. Every generic row is Rough and has all eight numeric base fields zero. The specialized header and rows each have exactly nine tokens; the exact construction occurrence sequence A,Z,Z excludes M and preserves duplicate reuse. Rows serialize raw input N1 slat width, N2 separation, and N3 thickness with source-{:.4R}, followed by N4 angle, N6 beam solar transmittance, N7 front beam solar reflectance, and N21 glass distance with source-{:.3R}. slatTAR remains raw while CalcBlindProperties writes separate TARs, so no computed blind optics are replayed. Two surfaces sharing construction C do not multiply its Z row; surface-unused D still emits the second Z, and its fixture EMS construction-index reference only suppresses an oracle warning. Primary, Materials-only, and Constructions-only independently lock both report selectors and all complete with zero warnings and zero severe errors; a specialized header with no data rows is valid. The comparator predicts exterior/interior occurrences only with an exact bare companion stack and fails closed for between-glass and missing-bare report behavior. Exact WindowConstruction, surface, control, and EMS rows are fixture-integrity locks only. Rust EIO serialization, CalcBlindProperties, beam-beam and other optics, ratings, daylighting, control/surface/EMS behavior, variable angle, thermal/window runtime, between-glass or missing-bare reporting, broad declaration/diagnostic ordering, Construction:WindowEquivalentLayer, WindowMaterial:Blind:EquivalentLayer, and conformance remain unclaimed."

[[object]]
name = "Construction"
family = "construction"
status = "typed"
first_evidence = "heat_balance_nomass_001"
support_boundary = "Typed for surface/material links and declared static construction evidence. material_opaque_variants_001 separately adds nonblocking diagnostic EnergyPlus 26.1 EIO evidence for the exact EXTERIOR SOLID, MIXED OPAQUE, reversed mixed, and sole-layer IRT construction groups, including every layer count, material name, outside-to-inside order, and thermal resistance. It does not promote general construction, CTF generation, conduction, or heat-balance conformance."

[[object]]
name = "ScheduleTypeLimits"
family = "schedule"
status = "typed"
first_evidence = "schedule_constant_001"
support_boundary = "Typed for lower_limit_value, upper_limit_value, and numeric_type metadata. Limited EnergyPlus 26.1 conformance is promoted only by calendar_schedule_fmu_export_bounded_initial_value_exact_001: Continuous bounds [0.0,0.875], inactive FMU Export Initial Value 0.875 at the inclusive upper endpoint, and exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples, all 0.875, with the exact one-warning/zero-severe EnergyPlus completion. Its paired calendar_schedule_fmu_export_bounded_initial_value_failure_001 changes only the upper bound to 0.5 and contributes blocking smoke/nonclaim rejection evidence: zero EnergyPlus schedule data samples with 0 Warning/1 Severe, plus Rust ScheduleValueOutsideTypeLimits before TypedModel or runtime output. Rust source/unit coverage also applies the two-sided inclusive scalar validator to Schedule:Constant, ExternalInterface:Schedule, and FMU Import To Schedule, but those bound checks are not externally promoted. Compact/File/Year resolved minmax, Day warning and Discrete-integer behavior, one-sided/reversed/blank numeric bounds, unit_type, unknown-reference parity, multiple-violation diagnostic order/text/count/exit behavior, live updates, and cross-family duplicate source-order parity remain unclaimed."

[[object]]
name = "Schedule:Constant"
family = "schedule"
status = "typed"
first_evidence = "schedule_constant_001"
support_boundary_addendum = "CP55 deterministic simulation-owned operation counters cover this scalar family only when referenced by the immutable hour-only OtherEquipment cache. Its entry requires no Compact build evaluation; initialization, warmup, and run-period use indexed reads with zero hot live fallback, legacy schedule-family-chain, or Compact resolution/evaluation operations. Constant-plus-Compact cached/live timestep f64-bit equivalence after one shared cached initialization remains Rust A/B evidence only and does not directly compare initialization paths; remaining OtherEquipment/zone/surface/people iteration, wall-clock speedup, numerical promotion, calendar-aware or unsupported-family consumption, IdealLoads/HVAC/thermostat use, live mutation, and broader completion remain unclaimed."
support_boundary = "Typed and tolerance-gated for declared Schedule Value evidence. Rust-only immutable schedule-cache infrastructure stores each constant once as scalar storage and preserves the legacy numerical Schedule Value series through its adapter. Rust unit and structure evidence now proves only referenced-only hour-only OtherEquipment heat-balance cache consumption for this schedule family; it adds no EnergyPlus numerical heat-balance oracle promotion, IdealLoads consumption, or wall-clock speedup claim."

[[object]]
name = "Schedule:Compact"
family = "schedule"
status = "typed"
first_evidence = "ideal_loads_thermostat_001"
support_boundary_addendum = "CP55 deterministic simulation-owned counters evaluate each referenced calendar-invariant Compact profile exactly 24 times while building its immutable 24-sample OtherEquipment cache entry. Initialization, warmup, and run-period then use indexed reads with zero hot live fallback, legacy schedule-family-chain, Compact profile-resolution, or Compact value-evaluation operations. The Constant-plus-Compact cached/live timestep A/B preserves scoped f64 bits after one shared cached initialization and does not directly compare initialization paths. Remaining OtherEquipment/zone/surface/people iteration, wall-clock speedup, numerical promotion, calendar-aware Compact or unsupported-family consumption, IdealLoads/HVAC/thermostat use, live mutation, and broader completion remain unclaimed."
support_boundary = "Typed for selected AllDays/Until segments, with ordered-exact-unique normalized hourly Schedule Value conformance for the 1-through-24 daily profile in the paired calendar_schedule_hourly_exact_001 and calendar_schedule_weather_leap_policy_no_001 weather-effective calendar cases, plus the no-OA all-days Schedule:Compact IdealLoads fuel-efficiency conformance candidate. The paired cases prove only 72 leap-observed samples versus 48 non-leap-policy samples from the same IDF and raw EPW rows; Through/For day-type expansion beyond the separate exact case, DST and holiday lookup beyond the separate exact CP42 rollover case, zone-timestep lookup beyond the separate exact CP43 aligned default-No and CP44 explicit-mode cases, downstream consumption of the precomputed Schedule Value series, and broad compact-schedule grammar remain outside the claim. calendar_schedule_compact_through_for_day_type_hourly_exact_001 separately proves the exact fixed Month/Day Through 1/1 and 12/31 periods, declared source-ordered For selectors and period-local AllOtherDays complement, and one hourly Until endpoint per profile over one five-day cross-year RunPeriod. Its 120 ordered-exact-unique zero-tolerance Schedule Value samples and timestamps are daily 103/104/105/108/199, each repeated 24 times; the gate also locks raw EnergyPlus schedule/day-type ESO fields, exact Environment/daylight-saving/special-day EIO rows, and clean 0/0 completion. Rust schedule ordinals 365/366/1/2/3 are source-mapped diagnostics, not ESO/EIO fields, and Rust EPW record selection is unclaimed. DST clock/hour-24 tomorrow behavior beyond the separate exact CP42 rollover case, subhourly/interpolation beyond the separate exact CP43 aligned default-No and CP44 explicit-mode cases, other schedule families, more periods/RunPeriods/year boundaries, actual/design-day/warmup execution, downstream internal-gain/HVAC/IdealLoads calendar consumption, Rust raw ESO parity, and broad warning/error parity remain outside the claim. Hour-only OtherEquipment paths fail closed with typed InvalidInternalGainSchedule errors for calendar-varying or unresolved schedule references at public gain-trace, heat-balance initialization, and first-zone boundaries; this is unit safety evidence, not downstream calendar-consumption conformance. calendar_schedule_dst_hour24_tomorrow_day_type_exact_001 separately proves one source-exact EnergyPlus 26.1 detailed Schedule:Compact clock boundary over 2032-10-30 through 2032-11-01. Exactly 72 ordered-exact-unique zero-tolerance Schedule Value samples and timestamps are 100x23 then 124, 200x23 then 801, and 800x23 then 901. Raw DST is 0/1/1 and raw day type is 7/1/8. This locks current-day DST +1-hour lookup, DST hour 24 advancing the schedule ordinal and selecting tomorrow's Holiday profile, and final-run hour 24 advancing the ordinal while retaining the stale final Holiday TomorrowVariables type. The gate also locks exact Environment/daylight-saving/special-day EIO rows and clean 0/0 completion. Schedule-specific DST opt-out, subhourly/interpolation beyond the separate exact CP43 aligned default-No and CP44 explicit-mode cases, other schedule families, additional RunPeriods/DATA PERIODS/boundaries, year-end ordinal wrap beyond unit evidence, actual/design-day/warmup execution, downstream internal-gain/HVAC/IdealLoads calendar consumption, EMS/current-value semantics, Rust raw ESO parity, and broad warning/error parity remain outside the claim. calendar_schedule_compact_zone_timestep_exact_001 separately proves only one aligned default Interpolate:No Schedule:Compact profile with omitted Interpolate directive under Timestep,4. Its 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps are 11/12/13/14 at hour 1's four 15-minute endpoints and 90 for the remaining 92 samples. The gate also locks all raw EnergyPlus values and timestep timestamp fields, exact Environment and disabled daylight-saving EIO rows, and clean 0/0 completion. Rust EPW record selection is null and unclaimed for this schedule-only case. Explicit Interpolate directives, Average/Linear, non-aligned Until warnings and resulting sampling behavior, other intervals/timestep counts/calendars, DST interaction, Schedule:File and the public/input Schedule:Day:*, Schedule:Week:*, and Schedule:Year families, UpdateScheduleVals, EMS/currentVal policy, downstream consumption, Rust raw ESO serialization, and broad warning/error parity remain outside the claim. calendar_schedule_compact_interpolation_modes_exact_001 separately proves only one non-actual 2032-01-01 Thursday with Timestep,4, all six calendar/weather policies No, and three source-ordered AllDays profiles using explicit Interpolate:No, Average, and Linear with the same Until 00:20=10, 01:15=175, and 24:00=175 fields. Its three 96-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series begin No 10/175/175/175/175, Average 10/120/175/175/175, and Linear 10/40/85/130/175; samples 6 through 96 are 175. The gate locks flat first-Linear-interval behavior, the cross-hour day-minute ramp, the Average 15-minute window mean, explicit-No endpoint selection, all 288 raw EnergyPlus values, 96 shared timestamp rows, exact Environment and disabled daylight-saving EIO rows, and the exact EnergyPlus 1 Warning/0 Severe completion with the one non-multiple Until warning attached only to explicit No. Rust expands through an intermediate 1,440-minute lattice and stores an immutable 96-value zone-timestep cache. Rust EPW record selection is null and unclaimed. Other timestep counts, Until 24:MM correction, malformed directives, overlap/zero/incomplete profiles, DST plus subhourly interpolation, mixed multi-profile modes, hourly aggregation, downstream consumption, Schedule:File, public/input Schedule:Day:*, Schedule:Week:*, and Schedule:Year, UpdateScheduleVals, EMS/currentVal, Rust raw ESO serialization, and broad diagnostic or schedule parity remain outside the claim. Typed Rust invalid/duplicate/misplaced directive errors and its warning code are source/unit evidence only. The exact EnergyPlus oracle warning text/count is locked only for this fixture; Rust warning text/count parity remains unclaimed. Rust unit and structure evidence separately proves only referenced-only 24-hour cache consumption for calendar-invariant Compact profiles used by OtherEquipment in the Rust heat-balance initializer, warmup, and run period; it adds no EnergyPlus numerical heat-balance oracle promotion, calendar-aware Compact downstream consumption, IdealLoads consumption, or wall-clock speedup claim."

[[object]]
name = "Schedule:File"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_file_8760_leap_exact_001"
support_boundary = "Typed and limited EnergyPlus 26.1 conformance only for calendar_schedule_file_8760_leap_exact_001: one safe staged flat comma CSV basename with one header and exactly 8760 three-column numeric rows; one-based column 2, one skipped row, declared 8760 hours, explicit Interpolate=No, 60 minutes per item, and Adjust Schedule for Daylight Savings=No. The blocking gate proves 72 ordered-exact-unique zero-tolerance hourly Schedule Value samples and timestamps over 2016-02-28 through 2016-03-01 as 1393..1416, the same 1393..1416 repeated on February 29, then 1417..1440. Byte-identical sidecar staging, raw EnergyPlus values/timestamps, and 0 Warning/0 Severe completion are locked. 8784 rows, row-count mismatches, file diagnostics, subhourly/interpolated/DST-adjusted paths, other separators or shapes, shared caches, shading, arbitrary-run staging, downstream consumption, EMS/currentVal, Rust raw ESO, and broad parity remain unclaimed."

[[object]]
name = "Schedule:File:Shading"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_file_shading_exact_001"
support_boundary = "Typed and limited EnergyPlus 26.1 conformance only for calendar_schedule_file_shading_exact_001: one safe staged comma CSV basename with the exact Date/Time, ALPHA SURFACE, and BETA SURFACE headers plus exactly 35,040 common-year Timestep,4 data rows. The two unique non-timestamp headers generate ALPHA SURFACE_shading and BETA SURFACE_shading schedules. The blocking gate proves two 96-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series on 2013-01-01: alpha 0.01..0.96 and beta 0.96..0.01. Byte-identical sidecar staging, converted epJSON file_name, raw EnergyPlus ESO values/timestamps, exact EIO environment plus default non-Imported shadowing row, and 0 Warning/0 Severe completion are locked. The fixture deliberately has no zones, surfaces, or ShadowCalculation object, so Imported ShadowCalculation, surface lookup or consumption, sunlit fraction, incident solar, and heat-balance effects are unclaimed. JSON/unknown extensions, multiple objects, header and numeric diagnostics, other timestep/day-count branches, timestamp validation, EMS/currentVal, downstream consumption, Rust raw ESO, and broad parity remain unclaimed."

[[object]]
name = "ExternalInterface:Schedule"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_external_interface_initial_value_exact_001"
support_boundary_addendum = "CP55 deterministic simulation-owned operation counters cover this inactive scalar only when referenced by the immutable hour-only OtherEquipment cache. Its entry requires no Compact build evaluation; initialization, warmup, and run-period use indexed reads with zero hot live fallback, legacy schedule-family-chain, or Compact resolution/evaluation operations. Existing family value parity remains Rust unit evidence; the profiled cached/live timestep A/B covers Constant plus Compact only. Remaining iteration, wall-clock speedup, numerical promotion, active exchange/live mutation, broader downstream consumption, and completion remain unclaimed."
support_boundary = "Typed and limited EnergyPlus 26.1 conformance only for calendar_schedule_external_interface_initial_value_exact_001: exactly one schedule named EXTERNAL INITIAL VALUE references the unlimited Any Number type and declares Initial Value 0.375, with no ExternalInterface activation object, over one non-actual 2032-01-01 Thursday at Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all equal to 0.375. It also locks the converted epJSON schedule_type_limits_name and initial_value fields plus activation absence, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact missing-activation EnergyPlus warning once, and 1 Warning/0 Severe completion in ERR and END. Rust represents only the inactive case as an immutable initial value. BCVTB/FMU live exchange, socket/configuration files, mutable updates/currentVal, warmup/sizing exchange lifecycle, EMS priority, generated internal day/week EIO parity, downstream consumers, multiple objects, diagnostic parity including Rust warning text/count, bounded ScheduleTypeLimits validation, and the following FunctionalMockupUnitImport/Export schedule families remain unclaimed. Rust-only immutable schedule-cache infrastructure stores this inactive initial value once as scalar storage and preserves the legacy numerical Schedule Value series through its adapter. Rust unit and structure evidence now proves only referenced-only hour-only OtherEquipment heat-balance cache consumption for this schedule family; it adds no EnergyPlus numerical heat-balance oracle promotion, IdealLoads consumption, or wall-clock speedup claim."

[[object]]
name = "ExternalInterface:FunctionalMockupUnitImport:To:Schedule"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_fmu_import_initial_value_exact_001"
support_boundary_addendum = "CP55 deterministic simulation-owned operation counters cover this inactive scalar only when referenced by the immutable hour-only OtherEquipment cache. Its entry requires no Compact build evaluation; initialization, warmup, and run-period use indexed reads with zero hot live fallback, legacy schedule-family-chain, or Compact resolution/evaluation operations. Existing family value parity remains Rust unit evidence; the profiled cached/live timestep A/B covers Constant plus Compact only. Remaining iteration, wall-clock speedup, numerical promotion, active FMU import/live mutation, broader downstream consumption, and completion remain unclaimed."
support_boundary = "Typed and limited EnergyPlus 26.1 conformance only for calendar_schedule_fmu_import_initial_value_exact_001: exactly one schedule named FMU IMPORT INITIAL VALUE references the unlimited Any Number type, retains missing.fmu, ProbeInstance, and ProbeOutput as inert binding metadata, and declares Initial Value 0.625, with no ExternalInterface activation object or FunctionalMockupUnitImport provider object, over one non-actual 2032-01-01 Thursday at Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all equal to 0.625. It also locks converted epJSON schedule_type_limits_names, the three binding strings, and initial_value fields plus activation/provider absence, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact missing-activation EnergyPlus warning once, and 1 Warning/0 Severe completion in ERR and END. Rust represents only the inactive case as a shared immutable initial value. FMU provider resolution, object-list validation or diagnostics, FMU file opening/unpacking and modelDescription parsing, FMI 1 import binding, From:Variable instances, value-reference or causality checks, live exchange and warmup updates, the activated-without-provider permissive EnergyPlus no-op branch, bounded ScheduleTypeLimits validation, downstream consumers, multiple objects, broad diagnostic parity including Rust warning text/count, actual/design-day/multienvironment execution, and Rust raw ESO serialization remain unclaimed. Rust-only immutable schedule-cache infrastructure stores this inactive initial value once as scalar storage and preserves the legacy numerical Schedule Value series through its adapter. Rust unit and structure evidence now proves only referenced-only hour-only OtherEquipment heat-balance cache consumption for this schedule family; it adds no EnergyPlus numerical heat-balance oracle promotion, IdealLoads consumption, or wall-clock speedup claim."

[[object]]
name = "ExternalInterface:FunctionalMockupUnitExport:To:Schedule"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_fmu_export_initial_value_exact_001"
support_boundary_addendum = "CP55 deterministic simulation-owned operation counters cover this inactive scalar only when referenced by the immutable hour-only OtherEquipment cache. Its entry requires no Compact build evaluation; initialization, warmup, and run-period use indexed reads with zero hot live fallback, legacy schedule-family-chain, or Compact resolution/evaluation operations. Existing family value parity remains Rust unit evidence; the profiled cached/live timestep A/B covers Constant plus Compact only. Remaining iteration, wall-clock speedup, numerical promotion, active FMU export/live mutation, broader downstream consumption, and completion remain unclaimed."
support_boundary = "Typed and limited EnergyPlus 26.1 conformance only for calendar_schedule_fmu_export_initial_value_exact_001: exactly one object's inner schedule_name FMU Export Initial Value references the unlimited Any Number type, retains ProbeInput as inert FMU-variable metadata, and declares Initial Value 0.875, with no ExternalInterface activation or socket/provider configuration, over one non-actual 2032-01-01 Thursday at Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all equal to 0.875. It also locks the converted epJSON synthetic outer key ExternalInterface:FunctionalMockupUnitExport:To:Schedule 1 and exact inner schedule_name, schedule_type_limits_names, fmu_variable_name, and initial_value fields plus activation absence, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact missing-activation EnergyPlus warning once, and 1 Warning/0 Severe completion in ERR and END. Rust represents only the inactive case as a shared immutable initial value. socket.cfg and variables.cfg handling, provider resolution, FMU variable binding, live exchange, warmup handoff, mutable updates/currentVal, termination/final exchange, Ptolemy lifecycle, global ExternalInterface behavior, other ExternalInterface variable/actuator families, exact duplicate diagnostic text, unknown type-limit warning parity, downstream consumers, multiple objects, actual/design-day/multienvironment execution, Rust raw ESO serialization, and broad FMU export behavior remain unclaimed for this unlimited CP53 fixture. The separate calendar_schedule_fmu_export_bounded_initial_value_exact_001 fixture promotes only the Continuous scalar inclusive endpoint [0.0,0.875], with paired calendar_schedule_fmu_export_bounded_initial_value_failure_001 smoke/nonclaim evidence blocking the above-bound [0.0,0.5] input before samples; every other bounded ScheduleTypeLimits branch remains unclaimed. Rust-only immutable schedule-cache infrastructure stores this inactive initial value once as scalar storage and preserves the legacy numerical Schedule Value series through its adapter. Rust unit and structure evidence now proves only referenced-only hour-only OtherEquipment heat-balance cache consumption for this schedule family; it adds no EnergyPlus numerical heat-balance oracle promotion, IdealLoads consumption, or wall-clock speedup claim."

[[object]]
name = "Schedule:Day:Hourly"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_day_hourly_week_daily_year_leap_exact_001"
support_boundary = "Typed as an immutable 24-value hourly profile. Limited EnergyPlus 26.1 conformance covers only the three fully explicit profiles consumed by calendar_schedule_day_hourly_week_daily_year_leap_exact_001: Sunday Week A 101..124, Monday Week A 201..224, and Tuesday Week B 301..324. Blank/default values, ScheduleTypeLimits validation/warnings, other Day schedule families, downstream consumers, and broad diagnostics remain unclaimed."

[[object]]
name = "Schedule:Day:Interval"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_day_interval_modes_exact_001"
support_boundary = "Typed as source-ordered Until/value segments plus blank/default-No, Average, or Linear interpolation in the shared DayScheduleId namespace after Schedule:Day:Hourly. Limited EnergyPlus 26.1 conformance covers exactly three Timestep,4 profiles consumed through one Week:Daily and Year over 2032-01-01 through 2032-01-03, producing one 288-sample ordered-exact-unique zero-tolerance Schedule Value series with exact raw values/timestamps and 0 Warning/0 Severe completion. The separate calendar_schedule_week_compact_day_types_exact_001 case consumes two constant explicit-No Interval profiles through Week:Compact but does not revalidate interpolation. Day:List, other Week:Compact combinations, unaligned-No warnings, other interval/timestep shapes, Hourly aggregation, diagnostic parity, other special-day/DST/tomorrow behavior, downstream consumers, and broad ScheduleManager parity remain unclaimed."

[[object]]
name = "Schedule:Day:List"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_day_list_modes_exact_001"
support_boundary = "Typed as source-ordered values with Minutes per Item and blank/default-No, Average, or Linear interpolation in the shared DayScheduleId namespace after Schedule:Day:Hourly and Schedule:Day:Interval. Limited EnergyPlus 26.1 conformance covers exactly three Minutes-per-Item-20 Timestep,4 profiles consumed through one Week:Daily and Year over 2032-01-01 through 2032-01-03, producing one 288-sample ordered-exact-unique zero-tolerance Schedule Value series. The gate proves the source-actual Day:List behavior that Average computes minute-window means while Linear equals No endpoint sampling, plus exact raw values/timestamps and 0 Warning/0 Severe completion. The separate calendar_schedule_week_compact_day_types_exact_001 case consumes two constant explicit-No List profiles through Week:Compact but does not revalidate interpolation. Other Week:Compact combinations, other minute/timestep shapes, blank-value and malformed-count diagnostics, a documentation-intended Linear ramp, Hourly aggregation, other special-day/DST/tomorrow behavior, downstream consumers, and broad ScheduleManager parity remain unclaimed."

[[object]]
name = "Schedule:Week:Daily"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_day_hourly_week_daily_year_leap_exact_001"
support_boundary = "Typed as 12 resolved Schedule:Day references in EnergyPlus day-type order. The exact case externally consumes only Week A Sunday/Monday and Week B Tuesday over 2016-02-28 through 2016-03-01; the other nine day-type selections, Schedule:Week:Compact, missing-reference diagnostic parity, DST/holiday/tomorrow combinations, design-day execution, and downstream consumers remain unclaimed."

[[object]]
name = "Schedule:Week:Compact"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_week_compact_day_types_exact_001"
support_boundary = "Typed as source-ordered DayType List/Schedule:Day pairs compiled into 12 resolved day pointers in the shared WeekScheduleId namespace after Schedule:Week:Daily. Limited EnergyPlus 26.1 conformance covers exactly one seven-pair Weekdays, Weekends, Holiday, SummerDesignDay, WinterDesignDay, CustomDay1, and CustomDay2 mapping consumed through one full-year Schedule:Year over 2032-01-01 through 2032-01-07 at Timestep,4. The gate proves one 672-sample ordered-exact-unique zero-tolerance Schedule Value series, exact raw values/timestamps, the resolved 12-slot WeekSchedule EIO row, converted epJSON pair order, five special-day rows, and 0 Warning/0 Severe completion. AllDays, AllOtherDays, omitted For prefixes, arbitrary combined lists, selector/reference diagnostic parity, other pair orders, other profiles/calendars, DST/tomorrow behavior, downstream consumers, and broad ScheduleManager parity remain unclaimed."

[[object]]
name = "Schedule:Year"
family = "schedule"
status = "typed"
first_evidence = "calendar_schedule_day_hourly_week_daily_year_leap_exact_001"
support_boundary = "Typed as a top-level ScheduleId backed by an immutable 366-day Week pointer table. Limited EnergyPlus 26.1 conformance covers only source-ordered Week A 1/1-2/28 and Week B 3/1-12/31 ranges whose omitted day 60 copies day 59's Week A pointer, producing exact hourly values 101..124, 201..224, and 301..324 over the three-day leap window. Range wrap, missing/overlap/reference diagnostic parity, additional periods/calendars, actual/design-day/warmup/multiyear execution, EMS/currentVal, downstream consumers, Rust raw ESO, and broad ScheduleManager parity remain unclaimed."

[[object]]
name = "OtherEquipment"
family = "internal_gains"
status = "typed"
first_evidence = "internal_gains_001"
support_boundary = "Typed for nominal EIO evidence and declared convective-gain trace only."

[[object]]
name = "People"
family = "internal_gains"
status = "typed"
first_evidence = "ideal_loads_outdoor_air_flow_person_diagnostic_001"
support_boundary = "Typed for zone design occupant count used by the limited IdealLoads Flow/Person outdoor-air conformance candidate, current People occupancy schedule values used by the limited Flow/Person OccupancySchedule DCV outdoor-air conformance candidate, and the diagnostic Flow/Person design-flow evidence; no people heat-gain numerical conformance claim."

[[object]]
name = "ThermostatSetpoint:DualSetpoint"
family = "thermostat"
status = "typed"
first_evidence = "ideal_loads_thermostat_001"
support_boundary = "Typed for thermostat graph wiring; no HVAC control algorithm conformance."

[[object]]
name = "ZoneControl:Thermostat"
family = "thermostat"
status = "typed"
first_evidence = "ideal_loads_thermostat_001"
support_boundary = "Typed for thermostat references; no load-control numerical conformance."

[[object]]
name = "ZoneControl:Humidistat"
family = "thermostat"
status = "typed"
first_evidence = "ideal_loads_humidistat_dehumidification_diagnostic_001"
support_boundary = "Typed for no-OA Humidistat relative-humidity setpoint schedule references and no-OA ThirdOrder moisture-demand predictor/corrector helpers. The declared short no-OA Humidistat dehumidification/humidification candidates now gate the trace-driven moisture-demand rows as conformance; fully owned zone moisture history/state, finite-limit humidistat behavior, outdoor-air humidity control, EMS/fault offsets, and broad humidity-control conformance remain outside the claim."

[[object]]
name = "ZoneHVAC:IdealLoadsAirSystem"
family = "hvac"
status = "typed"
first_evidence = "ideal_loads_no_oa_sensible_conformance_001"
support_boundary = "Typed input support plus limited no-OA/no-limit IdealLoads sensible conformance evidence, no-OA numeric capacity-limit, flow-limit, and flow-and-capacity-limit conformance evidence for declared thermostat, IdealLoads rate, and supply-node temperature/mass-flow outputs, no-OA ReportPurchasedAir rate-to-TimeStepSysSec and OutputProcessor Sum conformance evidence for declared non-fuel energy rows, no-OA blank fuel-efficiency, constant Schedule:Constant fuel-efficiency, and all-days Schedule:Compact fuel-efficiency conformance evidence for declared fuel-energy rows, no-OA ConstantSensibleHeatRatio cooling conformance evidence for declared cooling total/sensible/latent rate and supply-node humidity outputs, no-OA ConstantSupplyHumidityRatio cooling/heating conformance evidence for declared total/sensible/latent rate, ReportPurchasedAir energy/fuel, and supply-node temperature/mass-flow/humidity outputs, no-OA Humidistat dehumidification/humidification conformance evidence for declared cooling/heating total/sensible/latent rate, paired trace-driven moisture-demand rows, ReportPurchasedAir energy/fuel, and supply-node temperature/mass-flow/humidity outputs, no-OA hourly DistrictHeatingWater:Facility and DistrictCooling:Facility meter conformance evidence in the dedicated meter candidate, no-OA hourly/monthly/run-period meter conformance evidence in the humidity-control candidates, full-year annual meter conformance evidence in the four humidity-control annual meter candidates, plus monthly/annual/run-period meter conformance evidence in the meter-only candidate using RuntimeMeterRegistry request resolution and fuel-energy aggregation proof rows, limited outdoor-air Flow/Zone, Flow/Person, Flow/Person OccupancySchedule DCV, Flow/Person CO2Setpoint DCV, Flow/Area, AirChanges/Hour, Sum, Maximum, Flow/Zone DifferentialDryBulb/DifferentialEnthalpy economizer, and Flow/Zone Sensible/Enthalpy heat-recovery conformance evidence for declared outdoor-air mass/standard-density volume flow, no-humidity report rates, supply-air state, mixed-air state, economizer active-time, heat-recovery rates, and heat-recovery active-time outputs, remaining facility meter oracle-MTR vs Rust aggregated fuel-energy evidence outside the declared meter rows, and remaining diagnostic outdoor-air predecessor evidence for declared output variables only; outdoor-air methods beyond Flow/Zone/Flow/Person/Flow/Person OccupancySchedule DCV/Flow/Person CO2Setpoint DCV/Flow/Area/AirChanges/Hour/Sum/Maximum/DifferentialDryBulb/DifferentialEnthalpy economizer and declared Sensible/Enthalpy heat recovery, broad humidity-control conformance, fully owned humidistat moisture-history closure, heat-recovery saturation-limit branch generality, broader DCV combinations, sizing, fuel-efficiency schedules beyond the declared blank/constant/all-days Schedule:Compact candidates, broad meter conformance beyond the declared no-OA hourly facility meter rows, humidity-control hourly/monthly/run-period facility meter rows, four full-year humidity-control annual facility meter candidates, and monthly/annual/run-period facility meter candidate, annual facility meter rows in the short-run humidity-control candidates, multi-year annual grouping, and broad HVAC compatibility remain outside the claim."

[[object]]
name = "DesignSpecification:OutdoorAir"
family = "hvac"
status = "diagnostic"
first_evidence = "ideal_loads_outdoor_air_design_flow_diagnostic_001"
support_boundary = "Typed for IdealLoads outdoor-air reference/schedule preservation plus limited Flow/Zone, Flow/Person, Flow/Person OccupancySchedule DCV, Flow/Person CO2Setpoint DCV, Flow/Area, AirChanges/Hour, Sum, Maximum, Flow/Zone DifferentialDryBulb/DifferentialEnthalpy economizer, and Flow/Zone Sensible/Enthalpy heat-recovery conformance evidence for declared design-flow/final-flow mass, standard-density volume flow, no-humidity outdoor-air report-rate, supply-air state, mixed-air state, economizer active-time, heat-recovery rate, and heat-recovery active-time rows in ideal_loads_outdoor_air_flow_zone_conformance_candidate_001, ideal_loads_outdoor_air_flow_person_conformance_candidate_001, ideal_loads_outdoor_air_occupancy_dcv_conformance_candidate_001, ideal_loads_outdoor_air_co2_dcv_conformance_candidate_001, ideal_loads_outdoor_air_flow_area_conformance_candidate_001, ideal_loads_outdoor_air_air_changes_conformance_candidate_001, ideal_loads_outdoor_air_sum_conformance_candidate_001, ideal_loads_outdoor_air_maximum_conformance_candidate_001, ideal_loads_outdoor_air_differential_dry_bulb_economizer_conformance_candidate_001, ideal_loads_outdoor_air_differential_enthalpy_economizer_conformance_candidate_001, ideal_loads_outdoor_air_sensible_heat_recovery_conformance_candidate_001, and ideal_loads_outdoor_air_enthalpy_heat_recovery_conformance_candidate_001. No broader DCV combinations, general saturation-limit heat-recovery branch parity, CO2 contaminant-balance conformance, CO2 concentration conformance, or broad numerical OA conformance claim."

[[object]]
name = "NodeList"
family = "hvac"
status = "typed_graph_only"
first_evidence = "air_side_node_diagnostic_001"
support_boundary = "Typed graph and node expansion only; node outputs remain diagnostic."

[[object]]
name = "PlantLoop"
family = "plant"
status = "typed_graph_only"
first_evidence = "plant_loop_diagnostic_001"
support_boundary = "Typed graph and diagnostic projection only; no plant loop simulation conformance."

[[object]]
name = "Pump:ConstantSpeed"
family = "plant"
status = "typed_graph_only"
first_evidence = "plant-loop-skeleton-smoke"
support_boundary = "Typed graph equipment reference only; no pump performance or flow-control algorithm."

[[object]]
name = "Boiler:HotWater"
family = "plant"
status = "typed_graph_only"
first_evidence = "plant-loop-skeleton-smoke"
support_boundary = "Typed graph equipment reference only; no boiler performance algorithm."

[[object]]
name = "Chiller:Electric:EIR"
family = "plant"
status = "typed_graph_only"
first_evidence = "plant-loop-skeleton-smoke"
support_boundary = "Typed graph equipment reference only; no chiller performance algorithm."
