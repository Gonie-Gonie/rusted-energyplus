[[algorithm]]
id = "zone_air_heat_balance"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceManager.cc",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/state.rs::ZoneHeatBalanceState",
  "crates/ep_runtime/src/runtime.rs::simulate_heat_balance",
]
status = "conformance"
first_case = "heat_balance_nomass_001"
first_evidence = "heat_balance_nomass_001"
proof_variables = [
  "Zone Mean Air Temperature",
]
claim_level = "limited-conformance"
support_boundary = "Limited conformance only for declared no-mass zone-air heat-balance proof variables; no general heat-balance, HVAC, warmup, or sizing claim."

[[algorithm]]
id = "official_dynamic_heat_balance_compat_candidate"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceManager.cc",
  "src/EnergyPlus/HeatBalanceSurfaceManager.cc",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/HeatBalanceManager.cc|ManageHeatBalance|GetHeatBalanceInput|GetHeatBalanceInput",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc|ManageZoneAirUpdates|ManageZoneAirUpdates|ManageZoneAirUpdates",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/algorithm.rs::CompatibilityHeatBalanceAlgorithm::SourceOrder1ZoneOpaqueCompat",
  "crates/ep_runtime/src/heat_balance/algorithm.rs::CompatibilityHeatBalanceAlgorithm::runtime_config",
  "scripts/compare/official-dynamic-heat-balance-compat-candidate.ps1",
]
status = "conformance"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
first_evidence = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Site Outdoor Air Drybulb Temperature",
  "Site Outdoor Air Wetbulb Temperature",
  "Site Sky Temperature",
  "Site Horizontal Infrared Radiation Rate per Area",
  "Site Rain Status",
  "Zone Mean Air Temperature",
  "Zone Mean Air Humidity Ratio",
  "Zone Air Heat Balance Internal Convective Heat Gain Rate",
  "Zone Air Heat Balance Outdoor Air Transfer Rate",
  "Surface Inside Face Temperature",
  "Surface Inside Face Adjacent Air Temperature",
  "Surface Outside Face Temperature",
  "Surface Outside Face Outdoor Air Drybulb Temperature",
  "Surface Outside Face Outdoor Air Wetbulb Temperature",
  "Surface Outside Face Outdoor Air Wind Speed",
  "Surface Outside Face Outdoor Air Wind Direction",
  "Surface Inside Face Convection Heat Transfer Coefficient",
  "Surface Outside Face Convection Heat Transfer Coefficient",
  "Surface Outside Face Thermal Radiation to Air Heat Transfer Coefficient",
  "Surface Outside Face Thermal Radiation to Sky Heat Transfer Coefficient",
  "Surface Outside Face Thermal Radiation to Ground Heat Transfer Coefficient",
  "Surface Outside Face Convection Heat Gain Rate",
  "Surface Outside Face Convection Heat Gain Rate per Area",
  "Surface Outside Face Net Thermal Radiation Heat Gain Rate",
  "Surface Outside Face Net Thermal Radiation Heat Gain Rate per Area",
  "Surface Inside Face Convection Heat Gain Rate",
  "Surface Inside Face Net Surface Thermal Radiation Heat Gain Rate",
  "Surface Inside Face Conduction Heat Transfer Rate",
  "Surface Inside Face Conduction Heat Transfer Rate per Area",
  "Surface Outside Face Conduction Heat Transfer Rate",
  "Surface Outside Face Conduction Heat Transfer Rate per Area",
  "Surface Outside Face Incident Solar Radiation Rate per Area",
  "Surface Outside Face Incident Beam Solar Radiation Rate per Area",
  "Surface Outside Face Incident Sky Diffuse Solar Radiation Rate per Area",
  "Surface Outside Face Incident Ground Diffuse Solar Radiation Rate per Area",
  "Surface Outside Face Solar Radiation Heat Gain Rate",
  "Surface Outside Face Solar Radiation Heat Gain Rate per Area",
  "Surface Heat Storage Rate",
  "Surface Heat Storage Rate per Area",
  "Zone Opaque Surface Inside Faces Conduction Rate",
  "Zone Opaque Surface Outside Faces Conduction Rate",
  "Zone Opaque Surface Outside Faces Conduction Heat Gain Rate",
  "Zone Opaque Surface Outside Faces Conduction Heat Loss Rate",
  "Zone Air Heat Balance Surface Convection Rate",
  "Zone Air Heat Balance Air Energy Storage Rate",
  "Surface Inside Face Heat Balance Calculation Iteration Count",
]
claim_level = "limited-official-dynamic-conformance"
support_boundary = "Tolerance-gated official 1ZoneUncontrolled dynamic conformance only for conformance-level variables in the source-order compatibility lane, including zone mean air humidity ratio, wetbulb/rain-status/sky/horizontal-infrared weather alignment, roof surface-local outdoor-air drybulb/wetbulb/wind rows, declared inside adjacent-air temperature rows, the hourly Surface Inside Face Heat Balance Calculation Iteration Count row under surface-iteration-count-state tolerance, named roof/wall/floor conduction per-area rows, declared inside convection/radiation source rows, declared inside/outside convection coefficient rows and roof outside longwave air/sky/ground coefficient rows under surface-coefficient-state tolerance, declared wall/roof exterior convection/net-radiation heat-gain rate rows under surface-exterior-rate-state tolerance, roof exterior convection/net-radiation per-area rows under surface-exterior-flux-state tolerance, named wall/roof incident total and beam solar per-area rows under surface-solar-flux-state tolerance, exact-zero named wall/roof sky and ground diffuse incident solar rows, named wall/roof absorbed solar heat gain rate rows under surface-solar-rate-state tolerance, the roof absorbed solar heat gain per-area row under surface-solar-flux-state tolerance, zone opaque conduction aggregate rows including outside-face gain/loss aggregates, and the floor Surface Heat Storage Rate and Surface Heat Storage Rate per Area under dedicated aggregate/flux/storage/solar-rate/solar-flux/exterior-rate/exterior-flux/coefficient/count tolerances; broad heat-balance compatibility, broad weather processor compatibility, other storage rows, exterior source rows outside the declared wall/roof and roof per-area rows, absorbed/non-incident solar outside the declared rows, exact iteration-count parity beyond the declared count tolerance, fenestration, and non-promoted probe lanes remain outside the claim."

[[algorithm]]
id = "heat_balance_manager_source_order"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceManager.cc",
  "src/EnergyPlus/HeatBalanceManager.hh",
  "src/EnergyPlus/EMSManager.cc",
  "src/EnergyPlus/PluginManager.cc",
  "src/EnergyPlus/PluginManager.hh",
  "src/EnergyPlus/HeatBalanceIntRadExchange.cc",
  "src/EnergyPlus/InternalHeatGains.cc",
  "src/EnergyPlus/HeatBalanceKivaManager.cc",
  "src/EnergyPlus/SurfaceOctree.cc",
  "src/EnergyPlus/DataSurfaces.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/HeatBalanceManager.cc|ManageHeatBalance|GetHeatBalanceInput|GetHeatBalanceInput",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/manager.rs::manage_heat_balance_source_order_stages",
  "crates/ep_compiler/src/compiler/zone.rs::Compiler::parse_zones",
  "crates/ep_compiler/src/compiler/zone_collections.rs::Compiler::mark_nominal_controlled_zones",
  "crates/ep_compiler/src/compiler/zone_collections.rs::Compiler::parse_zone_lists",
  "crates/ep_compiler/src/compiler/zone_collections.rs::Compiler::parse_zone_groups",
  "crates/ep_compiler/src/compiler/zone_local_environment.rs::Compiler::parse_zone_local_environments",
  "crates/ep_compiler/src/compiler/space.rs::Compiler::parse_space_data",
  "crates/ep_compiler/src/compiler/surface_space.rs::Compiler::create_missing_spaces",
  "crates/ep_compiler/src/compiler/variable_absorptance_surface.rs::Compiler::build_variable_absorptance_surface_list",
  "crates/ep_compiler/src/compiler/surface_incident_solar_multiplier.rs::Compiler::parse_surface_incident_solar_multiplier_requests",
  "crates/ep_compiler/src/compiler/surface_solar_incident.rs::Compiler::parse_surface_solar_incidents",
  "crates/ep_compiler/src/compiler/fenestration_solar_absorbed.rs::Compiler::parse_fenestration_solar_absorbed_requests",
  "crates/ep_compiler/src/compiler/scheduled_surface_gains_check.rs::Compiler::check_scheduled_surface_gains_typed_subset",
  "crates/ep_compiler/src/compiler/thermochromic_construction.rs::Compiler::create_thermochromic_construction_projections",
  "crates/ep_compiler/src/compiler/valid_simulation_objects.rs::Compiler::check_valid_simulation_objects_bounded",
  "crates/ep_compiler/src/compiler/construction_use.rs::Compiler::collect_known_construction_use_evidence",
  "crates/ep_compiler/src/compiler/internal_heat_gains.rs::Compiler::parse_bounded_internal_heat_gains_input",
  "crates/ep_compiler/src/compiler/surface_computed_geometry.rs::Compiler::set_bounded_surface_computed_geometry",
]
status = "scaffold"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Zone Mean Air Temperature",
]
claim_level = "none"
support_boundary = "Source-order scaffold for HeatBalanceManager stage barriers plus bounded immutable ProcessZoneData and post-Zone GetZoneData declaration state through nominal control, ZoneList, ZoneGroup, GetZoneLocalEnvData, and GetSpaceData processing while the intervening ZonePreDefRep allocation remains deferred. A later CP97 composite surface-membership slice bounds the GetHTSurfaceData-owned typed BuildingSurface:Detailed space_name lookup through the full pre-remainder Space arena and same-Zone validation while the full routine remains source-mapped, then state-maps CreateMissingSpaces remainder detection and fallback assignment so every retained detailed opaque surface has a final SpaceId. CP98 separately preserves the next parent call after GetBuildingData by invoking `Compiler::build_variable_absorptance_surface_list` after the retained detailed-surface pass; it publishes immutable `VariableAbsorptanceSurfaceBinding` identities for Outdoors surfaces whose construction outside layer owns a typed overlay and emits bounded non-outdoor and inside-layer warnings. Every overlay remains run-blocking, while full `AllHTSurfaceList` membership/source reorder parity, other surface families, exact warning text/order/multiplicity outside typed arenas, `UpdateVariableAbsorptances`, runtime numerics, and conformance remain unclaimed. CP99 types the following `SurfaceProperty:IncidentSolarMultiplier` inputs only as immutable request snapshots: each dense typed identity retains a nonsemantic normalized declaration key, an unresolved normalized window target, an inclusive-[0,1] multiplier defaulting to 1.0, and an optional resolved ScheduleId. Rust fails closed on missing schedules and repeated normalized targets, claims no source order for this unnamed family, and leaves `GetIncidentSolarMultiplier` source-mapped because FenestrationSurface typing, window/exterior/construction/shade validation, per-surface mutation, source duplicate overwrite behavior, schedule evaluation, and every runtime consumer remain deferred; every request run-blocks. CP100 types the first `GetScheduledSurfaceGains` family, `SurfaceProperty:SolarIncidentInside`, as immutable `SurfaceSolarIncident` records. Each record retains its semantic normalized name without a name map, resolves only a typed `BuildingSurface:Detailed` SurfaceId plus any typed ConstructionId and one required ScheduleId, permits repeated semantic names and a construction different from the surface's own construction, and fails closed on a repeated SurfaceId/ConstructionId pair. CP101 types the immediately following `ComplexFenestrationProperty:SolarAbsorbedLayers` family as immutable `FenestrationSolarAbsorbedRequest` snapshots: each dense request retains a semantic normalized name without a name map, an unresolved normalized fenestration target, only a typed complex-fenestration ConstructionId, and outside-to-inside ScheduleIds whose count exactly matches that construction's solid optical layers. Schedule values and type limits are not inspected. Repeated normalized target/ConstructionId pairs fail closed because neither input family claims declaration order. CP102 then state-maps only a monotonic typed-subset slice of the internal `CheckScheduledSurfaceGains` tail: after error-free final Space assignment, a Zone gets a nonblocking warning only when retained detailed opaque surfaces already include both an exact current SurfaceId/ConstructionId pair match and a miss; empty, all-matched, and all-unmatched subsets are silent. It writes no model state and lists only known unmatched surface names. Rust leaves representative-surface mutation, full Zone/Space heat-transfer-surface completeness, windows and other surface families, both pair-lookup routines, schedule evaluation, and every runtime consumer deferred; every definition in either family run-blocks. CP103 records the next inline representative-surface EIO assignment block as an output-only mapped/deferred barrier without inventing a routine entry, then state-maps `CreateTCConstructions` only as separate immutable series/child projections. Those projections preserve master ConstructionId and input-state order, the first-state initial temperature, cloned effective stacks with final-master-layer substitution, and source-shaped `{:.0R}` child-name formatting at pinned boundaries, but receive no global ConstructionId/name/count/graph identity. Exhaustive arbitrary-finite formatter equivalence, WINDOW5-relative append order, full deep copies, collision lookup, active switching, reporting, and runtime remain deferred, and the existing parent blocker is unchanged. CP104 then invokes `Compiler::check_valid_simulation_objects_bounded` immediately after the CP103 projection and, absent prior Errors, emits one model-state-free `InvalidSimulationWithoutZones` Error only for an empty typed Zone arena positively witnessed by raw `Shading:Site:Detailed` or `Shading:Building:Detailed` and no raw occurrence of the eight source-allowed collector/generator families. Full `TotSurfaces > 0 && NumOfZones == 0` parity, other surface families, and exact severe/fatal behavior remain source-mapped; allowed raw presence does not grant typing or runtime support. CP105 then calls `Compiler::collect_known_construction_use_evidence` after the bounded validity check and before the existing fatal barrier. It materializes only sorted/deduplicated positive ConstructionIds in `known_used_constructions` from retained typed surfaces plus resolved references from the six source-ordered raw families, with a separate `known_ctf_used_constructions` subset only for GroundHeatExchanger and EMS references whose typed kind is Opaque or AirBoundary. The latter mirrors only the source non-window `IsUsedCTF` mark and does not assert that AirBoundary owns or requires CTF coefficient state. Missing, blank, wrong-type, and unresolved raw references stay silent, and any prior compile Error leaves both vectors empty. No false flag, unused count/name/warning, `DisplayExtraWarnings`, CTF/CondFD selection, runtime, object-count/graph, support, or conformance claim follows from absence. CP106 maps the immediate post-`CheckUsedConstructions` fatal input-completion barrier without a synthetic routine: Rust final `model = None` is only a coarse fail-closed analogue and does not claim the source line-311 short circuit, accumulated-diagnostic order, fatal text, or side effects. It then adds required source mapping for the line-316 `HeatBalanceIntRadExchange::InitSolarViewFactors` call, whose report options and EIO/debug output, user-factor alignment, complete merged Solar-enclosure/Space/surface topology, AirBoundary exclusion, pointer/global-report order, geometry and inside-absorptance state, zero/one/at-most-three-surface branches, approximate/user matrices, InternalMass-aware fixing, `EnforcedReciprocity`, 401st-iteration behavior, and warning/fatal/report side effects remain deferred. Existing Rust approximate/fix helpers and 1Zone EIO evidence do not promote that routine. CP107 adds a required source-mapped `ManageInternalHeatGains` row plus a state-mapped bounded `GetInternalHeatGainsInput` row for direct-Zone People then OtherEquipment. CP108 adds a non-required source-mapped `setupKivaInstances` row for the conditional `AnyKiva` call. CP109 maps and defers the caller's inline conditional `DoingSizing` assignment from `doSpaceHeatBalanceSizing` into `doSpaceHeatBalance` without a synthetic routine or Rust helper. CP110 adds a non-required source-mapped `SurfaceOctreeCube::init` row for the following double-guarded octree call. CP111 then state-maps a positive-only retained `BuildingSurface:Detailed` slice of the complete Surface `set_computed_geometry` loop: finite, coplanar, nondegenerate Triangles and conservative source-recognized Rectangles receive immutable shape category, source-ordered Newell plane, axis-selected projected vertices and bounds, signed-area tail reversal, wrap edges, and Rectangle side squares on `Surface::computed_geometry`. CP112 then maps and defers the inline line-186 `ManageHeatBalanceGetInputFlag = false` lifecycle boundary without a synthetic routine or persistent Rust flag. CP113 adds a non-required generic source mapping for `EMSManager::ManageEMS` at the following unconditional `BeginZoneTimestepBeforeInitHeatBalance` caller checkpoint, with no Rust target. CP114 adds a required source mapping for the unconditional line-198 `InitHeatBalance` call without promoting the existing Rust stage metadata, identity wrapper, or bounded initialization state. CP115 maps the second unconditional `ManageEMS` caller checkpoint at `BeginZoneTimestepAfterInitHeatBalance`, reusing `routine.manage_ems` without a new row or state claim. CP116 maps the unconditional line-209 `ManageSurfaceHeatBalance(state)` call and expands the existing required `routine.manage_surface_heat_balance` row for its complete source parent order without adding a row, project contract, Rust target, mapped state, count, support, or conformance claim; CP117 maps the inline `ManageSurfaceHeatBalancefirstTime`-guarded `Initializing Surfaces` display at lines 158-160 without a synthetic routine or implementation. `HeatBalanceSurfaceManager.hh` owns the flag with a true member default and true `clear_state()` reset; the same flag remains true through all four progress guards and becomes false only at the successfully reached parent tail, so an abnormal exit can leave it true for a repeated display on re-entry. Rust has no persistent equivalent flag, reset lifecycle, or progress output, and its identity-wrapper closure/nesting is not parity. CP117 adds no project-contract/Rust/state/support/count/conformance entry. CP118 adds the canonical required `routine.init_surface_heat_balance` and project-contract entry for the unconditional parent line-161 call and source lines 272-621. The source driver orders per-Surface outdoor/local-node/EMS weather state, BeginSim allocations and construction properties, variable absorptance, BeginEnvrn histories, movable insulation, ground/window/solar/daylighting/internal-gain/convection initialization, CondFD and CTF/EMPD/source-sink histories, radiant/PV/pool/HVAC resets, sizing/reporting, and its own first-time lifecycle. Existing Rust init-stage metadata and identity wrapper remain intentionally unlisted as an algorithm target and implement only a limited outside-path nesting, not the source driver or state. CP118 adds no Rust/state/support/conformance promotion; complete flags, children, state, lifecycle, outputs, numerics, and conformance remain unclaimed. CP119 maps the inline parent lines 165-167 shared-`ManageSurfaceHeatBalancefirstTime` guard and exact `Calculate Outside Surface Heat Balance` progress output. It is reached only after the unconditional initialization returns and directly precedes unconditional line-168 `CalcHeatBalanceOutsideSurf`. The CP117 default/reset/successful-tail clear remains the only lifecycle state: a pre-display abnormal exit, including failure to return from `InitSurfaceHeatBalance`, suppresses this message, while a post-display/pre-tail abnormal exit can repeat it on same-state re-entry. Rust has no persistent flag or progress output, and existing metadata and identity wrappers are not parity. CP119 adds no synthetic routine, source, project-contract, Rust, state, count, support, or conformance promotion. CP120 adds the unconditional outside-balance call as canonical required `routine.calc_heat_balance_outside_surf` plus its project-contract entry; CP121 maps the shared-first-time inline inside-balance display without a routine/state/count entry; CP122 adds canonical required `routine.calc_heat_balance_inside_surf` and its project-contract entry for the full-building line-172 call and lines 7738-7813 wrapper, without Rust/state/support/conformance promotion; CP123 maps the shared-first-time inline Air-balance display without a routine/state/count entry; CP124 maps the existing required `ManageAirHeatBalance` caller without a duplicate row; CP125 adds `UpdateFinalSurfaceHeatBalance` as a required source routine and project-contract entry; CP126 adds the following `AnyCTF || AnyEMPD`-guarded `UpdateThermalHistories` call and complete history body as a required source routine and project-contract entry; CP127 maps the independent `AnyCondFD` complete-Surface filtered moisture-history loop and its inline `SurfaceDataFD::UpdateMoistureBalance` helper as a non-required source routine; CP128 maps the unconditional false-initialize-only `ManageThermalComfort` call as a non-required source routine; CP129 maps the unconditional `ReportSurfaceHeatBalance` call as a required source routine and project-contract entry; CP130 maps the `ZoneSizingCalc`-guarded `GatherComponentLoadsSurface` call as a non-required source routine; CP131 maps the unconditional `CalcThermalResilience` call as a non-required source routine; CP132 maps the thermal-summary-guarded `ReportThermalResilience` call as a non-required source routine; CP133 maps the independently guarded CO2-summary `ReportCO2Resilience` call as a non-required source routine; CP134 maps the independently guarded visual-summary `ReportVisualResilience` call as a non-required source routine; CP135 maps the inline parent-tail `ManageSurfaceHeatBalancefirstTime = false` lifecycle assignment without a synthetic routine or count change; CP136 maps the unconditional `EndZoneTimestepBeforeZoneReporting` `ManageEMS` call by reusing `routine.manage_ems` without a duplicate row or count change; CP137 adds required `routine.rec_keep_heat_balance` and its project-contract requirement for line 211 and source lines 2971-3057 without Rust/state/support/conformance promotion; CP138 adds required `routine.report_heat_balance` and its project-contract requirement for the unconditional line-217 call and source lines 3321-3418. The source always calls `Sched::ReportScheduleVals`, then selects the normal non-warmup output, effective warmup-reporting, external-interface fallback, or no-further-output path; exact node, Zone output, sizing-log, dictionary/stamp, tabular, and utility ordering remains source-only dependency context, with no child rows or added sources. Existing Rust report-stage, composite-plan, prebinding, and bounded result-store metadata are scaffolding, not implementation. CP138 adds no source file, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP139 maps the unconditional line-219 `EndZoneTimestepAfterZoneReporting` `ManageEMS` call by reusing `routine.manage_ems` without a duplicate row. Generic `ManageEMS` resets the shared caller-owned `anyRan` instead of accumulating the CP136 result, quick-returns when EMS is absent, otherwise initializes this calling point, dispatches callbacks/plugins and exactly matching managers, and applies actuators plus `ReportEMS` only if something ran. Because CP138 reporting has already completed, any commit cannot retroactively change those outputs; later numerical, trend, and plugin visibility is not inferred. CP139 adds no source, project-contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP140 adds non-required `routine.update_ems_trend_variables` for the unconditional line-221 call, `EMSManager.hh` line 122 declaration, and `EMSManager.cc` lines 1444-1479 body. After no-EMS and zero-trend quick returns, the source visits declarations in 1-based order, skips nonpositive Erl pointers or log depths, and pushes the current numeric value to history index 1 without local positive upper-bound validation. Input allocation, `TimeARR`, setup diagnostics, and environment resets remain dependencies; the body emits no direct diagnostics. Existing Rust plan scaffolding has no trend-update implementation or target. CP140 adds no source file, project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 151 routines, split 58 state-mapped plus 93 source-mapped, with 50 required. CP141 adds non-required `routine.update_plugin_values` for the unconditional line-222 call, `PluginManager.hh` line 198 declaration, and `PluginManager.cc` lines 1458-1467 body, adding those two PluginManager sources to the Manager inventory. Without `LINK_WITH_PYTHON` the call is a no-op; the linked body has no runtime manager guard and directly visits stored-order `state.dataPluginManager->trends`, naturally doing no work for an empty vector. It gets each current global, pushes it at index 0, and drops the oldest value while fixed `times` remain unchanged; the update validates neither history length nor deque size. Construction returns before input population without a retained `PythonPlugin:Instance`; retained-instance setup owns globals, trends, handles, positive history length, fixed offsets, diagnostics, and missing-handle fatals. BeginEnvironment zeros globals and trend values while retaining times. The getter fatals for an empty global vector, while a nonempty invalid handle uses unchecked `[]`; dependency failure can prevent CP142. Warmup updates do not test per-instance run-during-warmup. Rust plugin execution remains unsupported. CP141 adds no project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 152 routines, split 58 state-mapped plus 94 source-mapped, with 50 required. CP142 adds required `routine.check_warmup_convergence` and its project-contract entry for the outer `WarmupFlag && EndDayFlag` gate at lines 224-226 and `HeatBalanceManager.cc` lines 3059-3226 body, declared at header line 136. `NumOfZones <= 0` clears warmup immediately. Otherwise the Zone loop uses inclusive temperature-tolerance tests and strict current-load `> 1.0e-4` gates; active heat/cool tests mutate current and previous loads through `abs(max(value, 100.0))`, divide by the clamped current value, and pass inclusively at the load tolerance. No-load paths pass without refreshing their stored test values. Each Zone then copies current values to previous-day state and resets current extrema to `-9999.0`/`1000.0`. At the maximum day while `WarmupFlag` remains true, failures emit per-Zone severe/detail diagnostics with state-wide normal/sizing guidance latches, optionally diagnose a user maximum below the default 25, and finally force warmup false; ordinary convergence still honors the minimum day. Allocation, BeginEnvironment resets, CP137 extrema accumulation, controls, names, diagnostics, and failures remain dependencies. Existing Rust stage metadata and temperature-only diagnostic loop are not this routine. CP142 adds no EnergyPlus source, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP143 maps only the inner line-227 `!WarmupFlag` branch and ordered numeric `DayOfSim` then textual `DayOfSimChr` zero resets at lines 228-229, without a synthetic routine or count change. Despite the source convergence comment, the branch also follows CP142 zero-Zone and maximum-day forced exits, but not a below-maximum failed check. The following line-231 `BeginNewEnvironmentAfterWarmUp` EMS call observes both resets, which persist if that dependency does not return; on the same invocation the later line-235 report gate cannot satisfy `DayOfSim == 1`. Existing Rust warmup metadata and diagnostics implement none of these global mutations or nesting. CP143 adds no source, project contract, Rust/state/support/conformance promotion; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP144 maps the in-branch line-231 `BeginNewEnvironmentAfterWarmUp` call by reusing `routine.manage_ems`, passing the same `anyRan` and an absent manager index after CP143 resets. Generic dispatch overwrites the flag false, quick-returns without EMS, otherwise initializes this calling point, runs callbacks/plugins and exact managers, and uses any execution to gate actuator commit plus `ReportEMS`. The lines 268-271 runtime-language and plugin begin-environment hooks require the distinct `BeginNewEnvironment` point and do not run here. Observers see warmup false and both day values zero; the caller ignores the result. CP143 mutations persist on non-return, which prevents CP145; a successful return still leaves the same-invocation CP145 `DayOfSim == 1` test false. CP144 adds no duplicate routine, source, project contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP145 adds required `routine.report_warmup_convergence` and its project-contract entry for the lines 235-237 four-way first-nonwarmup-day guard and `HeatBalanceManager.cc` lines 3228-3301 body, declared at header line 138. Its internal non-warmup guard can no-op; a one-state-lifetime, positive-Zone EIO header precedes source-order Zone rows. It resets scratch, selects RunPeriod or SizingPeriod text, averages `CountWarmupDayPoints` temperature samples, and normalizes each stored load difference in place only when its maximum is strictly above `1.0e-4`, otherwise zero. Load means and population standard deviations use the mutated ratios although the header says W; rows include four CP142 `PassFail` values and `{:.10R}` reals. Repeated calls renormalize already changed loads. Allocation, BeginEnvironment reset, CP137 producer/count, EIO, formatting, and bounds are dependencies; the body validates no positive count or capacity and emits no direct diagnostics. Existing Rust warmup metadata/summary is not this report. CP145 adds no source file or Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 154 routines, split 58 state-mapped plus 96 source-mapped, with 52 required. CP146 adds required source-mapped `SetPreConstructionInputParameters`. The unconditional `SimulationManager.cc` line-216 call follows constant/state initialization, environment/report checks, and predefined-table setup and precedes time-pointer setup; the routine is declared at `HeatBalanceManager.hh` line 96 and implemented at `HeatBalanceManager.cc` lines 446-492. Each entry overwrites shared `MaxSolidWinLayers` with executable baseline 7 despite the stale comment saying 5; the member defaults to 0, module clear reconstructs 0, and re-entry recomputes from 7. Raw `Construction:ComplexFenestrationState` presence raises the bound to 10 without retrieving an item or erasing any complex-state record from InputProcessor `unusedInputs`. The raw `Construction:WindowEquivalentLayer` count drives 1-based `getObjectItem` calls into all shared alpha/numeric value, blank-marker, and field-name buffers; only the positional `NumAlpha - 1` span is folded into the maximum, including holes/blanks, while alpha contents, numerics, `NumNumber`, `IOStat`, markers, and names are ignored. For each found item, InputProcessor zeros both counts, sets `IOStat` to -1, clears the shared buffers, and erases the requested equivalent-layer record from `unusedInputs` before field traversal; only full completion sets `IOStat` to 1, and CP146 never checks it. Opaque F/C-factor constructions are ignored. Normal successful output is 7 through 11; successful order cannot change the maximum, but source-order retrieval leaves the last completed shared buffers and marks every requested equivalent-layer item used. If a dependency does not return during the current item, the maximum retains only baseline/CFS and previously returned equivalent-layer spans, while that current object may already be marked used and its shared buffers may be partial; later items and downstream work are skipped. The routine validates and diagnoses nothing locally. Construction array allocation plus Surface, SolarShading, SurfaceGeometry, and Window loops are downstream dependencies. Rust dynamic `Vec` storage and separate limits 8 and 11 do not implement the mutable shared bound, scan/call order, buffers, failure prefix, allocations, or reset/re-entry lifecycle. CP146 adds no source inventory, Rust target/state/code, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 155 routines, split 58 state-mapped plus 97 source-mapped, with 53 required. CP147 adds required source-mapped `GetSiteAtmosphereData`, declared at `HeatBalanceManager.hh` line 100, called at `GetHeatBalanceInput` line 264 between line-262 project controls and line-266 spectral input, and implemented at `HeatBalanceManager.cc` lines 1252-1317. It sets `CurrentModuleObject` to `Site:HeightVariation`, uses local alpha-1/numeric-3 arrays, and reads the raw count. The unique-object schema defaults N1/N2/N3 to 0.22, 370.0, and 0.0065 with respective inclusive-zero, strict-positive, and inclusive-zero minima and no maxima. Exactly one object uses `NumNums` to copy only the returned N1/N2/N3 prefix, so absent trailing fields retain Terrain-derived wind or the existing gradient while blank/missing fields inside the prefix propagate schema defaults. InputProcessor zeros counts, sets `IOStat` to -1, clears local values plus shared blank/name metadata, and erases the object from `unusedInputs` before traversal; full completion sets status 1, but CP147 ignores status, alphas, metadata, and all local validation. A non-return can leave used/partial dependency effects before any environment copy or EIO. More than one object reads and marks no item, changes no environment value, emits exact `Too many Site:HeightVariation objects, only 1 allowed.`, then monotonically sets caller `ErrorsFound`; zero objects retain predecessor Terrain wind and reset only gradient to 0.0065. Every normally returned branch unconditionally repeats the EIO header and formatted row, including retained values on duplicates; output failures preserve earlier mutations and can leave a header-only prefix. Environment/module clear restores default 0.22/370/0.0065 and empty current-object state, while re-entry repeats intake and output. Terrain setup, height-corrected dry/wet bulb and wind formulas, Surface bulk paths, Kiva, diagnostics, InputProcessor, and EIO are dependencies. Rust keeps this object raw-only; its Terrain wind mapping and fixed-gradient helper do not implement override/hybrid state, shared lifecycle, source formulas, diagnostics, usage/metadata effects, or output. CP147 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 156 routines, split 58 state-mapped plus 98 source-mapped, with 54 required. CP148 adds required source-mapped `AllocateZoneHeatBalArrays`, declared at `HeatBalanceManager.hh` line 130 and implemented at `HeatBalanceManager.cc` lines 2824-2854. `InitHeatBalance` calls `AllocateHeatBalArrays` under `BeginSimFlag` at lines 2617-2618, and that parent calls CP148 as its first action at line 2863. A false `ZoneIntGain.allocated()` sentinel invokes `AllocateIntGains`, which allocates Zone internal gain records, Space internal gain records, Space device records, then Space power-reduction factors defaulted 1.0; gain values/counts default zero. A true sole sentinel preserves and does not validate the whole companion bundle. On the ordinary path, `GetHeatBalanceInput` line 320 already calls `ManageInternalHeatGains(state, true)`, whose input path normally invokes `AllocateIntGains` at `InternalHeatGains.cc` lines 291-293; CP148 therefore preserves populated internal-gain state, while its fallback chiefly serves direct test and alternate paths. CP148 next unconditionally allocates Zone and Space heat-balance EPVectors even with Space mode false. EPVector sets allocated before resize and fills every element with `T{}`: exact inherited defaults are 23 C for MAT/MRT/ZTAV/ZT/ZTAVComf/XMPT/TMX/TM2/MixingMAT and all XMAT/DSXMAT slots, 0.01 for the five named humidity values, and zero for every remaining member including T1 and ZTM/W histories. Eight `NumOfSolarEnclosures` arrays allocate in QSDifSol, QD, QDforDaylight, DB, DBSSG, DBIntWin, QSWRad, QSWRadLights order; the 1-based loop zeros them in QSDifSol, QD, QDforDaylight, QSWRad, QSWRadLights, DB, DBSSG, DBIntWin order. Zero counts are accepted and set EPVector sentinels without elements. Negative `NumOfZones` or `numSpaces` converts to unsigned EPVector `size_type`, sets allocated true, then generally nonreturns through a huge resize; negative `NumOfSolarEnclosures` instead creates eight empty Objexx 1:0 arrays, skips the loop, and can return. Oversized, inconsistent, and topology-invalid counts receive no local validation or diagnostic. Sequential failures expose completed allocations and per-enclosure writes; allocated-before-resize can poison the Zone sentinel so an uncleared retry skips an incomplete fallback. Successful re-entry preserves internal gains but destructively rebuilds Zone/Space records and enclosure arrays. Owning clears are nonuniform: HeatBalance clear resets the Zone sentinel/Space companions/enclosure arrays, predictor clear resets Zone/Space records, and ViewFactor clear resets the enclosure count, but DaylightingManager clear retains `spacePowerReductionFactor`. The false reset Zone sentinel makes the next fallback re-dimension that retained factor and overwrite it with 1.0; not all participating state becomes uniformly fresh or unallocated. The parent remainder after line 2863, BeginSim lifecycle, predictor/corrector, convection, HVAC, internal-gain/daylighting, SolarShading, and Surface solar consumers are dependencies. Rust Zone vectors and initialization shells do not implement the Space/fallback/enclosure state, exact defaults/order, partial failure, destructive re-entry, or clear/retry lifecycle. CP148 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 157 routines, split 58 state-mapped plus 99 source-mapped, with 55 required. CP149 adds required source-mapped `routine.allocate_heat_bal_arrays` and its project-contract entry for the sole production `InitHeatBalance` BeginSim call, header line 132, and source lines 2855-2963. After the separate CP148 child, the source dimensions ten FanSystem Zone arrays to zero; no-value allocates `ZoneReOrder`, dimensions two flags false, then explicitly zeros the reorder; dimensions thermostat air to 23 C; independently samples each enabled CO2/generic schedule once and dimensions three arrays; resets the exact warmup extrema/history/report bundle; no-value allocates convergence records and two standard-deviation arrays; allocates cold then heat crossed-threshold matrices before explicitly setting cold then heat false; conditionally allocates eleven thermal, three CO2, and three visual vector-report matrices; unconditionally allocates six SET/longest/start matrices using `TotThermalReportPers`; and resets `CountWarmupDayPoints` last. Value-bearing `dimension` always refills. Ordinary release no-value allocation preserves linear cells whenever `resize` reports no reallocation, including same-product reshapes with reinterpreted indices; changed counts within reserved capacity remain a container dependency. Reported reallocation default-constructs vector cells but leaves arithmetic cells without CP149 initialization, while Objexx array-init/debug builds assign trait/debug values even on the no-reallocation path. False feature branches preserve old state on same-state re-entry. Zero Zone count still samples enabled contaminants and reaches the tail; nonpositive report counts skip only conditional families, while crossed and six SET allocations still run with empty negative extents. Effects are sequential with delayed fills, no validation, diagnostic, rollback, or status. The later BeginEnvironment block resets many warmup values but not `LoadZoneSecPrevDay`, convergence records, or standard deviations; owner clears make a fully cleared retry fresh. Rust has no parent analog. CP149 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 158 routines, split 58 state-mapped plus 100 source-mapped, with 56 required. CP150 adds required source-mapped `routine.update_window_face_temps_non_bsdf_win` and its project-contract entry immediately after `rec_keep_heat_balance` and before `report_heat_balance`. Declared at header line 140 and implemented at source lines 3303-3313, it is the unconditional last executable action of `RecKeepHeatBalance` at line 3056 and makes successful endpoint copies visible before same-timestep line-217 reporting. The helper traverses stored `AllHTWindowSurfaceList` order; normal clean-state production appends ascending indices satisfying `HeatTransSurf && Class == Window`, with no BSDF filter. Each entry first dereferences the current mutable `Surface.Construction`, skips only `WindowTypeBSDF`, copies outside history term 1 to `SurfWinFenLaySurfTempFront(SurfNum, 1)`, then copies inside history term 1 to `SurfWinFenLaySurfTempBack(SurfNum, TotLayers)`, where `TotLayers` is not the solid-layer count. Destination matrices are dependency-dimensioned `[TotSurfaces, MaxSolidWinLayers]` to zero; history term arrays are separately allocated/initialized and produced by Surface balance. Standard non-BSDF face-temperature outputs are registered only for exterior-solar front layer 1 and back layer `TotLayers`, but CP150 has no output, warmup, sizing, or kickoff guard. Empty lists no-op; duplicates repeat writes; valid BSDF entries and unlisted/intermediate/obsolete cells retain prior values. Invalid Surface/Construction/history/destination/layer state is unchecked. Effects are stored-order and front-before-back with no diagnostics, status, rollback, math, or finite check, so a back failure can preserve the current front plus earlier entries and prevent ReportHeatBalance. Re-entry overwrites current endpoints but does not clear cells skipped after Construction/list/layer changes. Owner clears reconstruct the list, destinations, histories, and Construction table; BeginEnvironment does not explicitly reset destination matrices. Rust blocks fenestration and has no list/report-matrix/history-handoff analog. CP150 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 159 routines, split 58 state-mapped plus 101 source-mapped, with 57 required. CP151 adds non-required source-mapped `routine.open_shading_file`, declared at header line 144 and implemented at source lines 3422-3438, for the sole production `InitHeatBalance` lines-2696-2698 call under exact `BeginSimFlag && DoWeathSim && ReportExtShadingSunlitFrac`. It first calls `shade.ensure_open` with caller label `OpenOutputFiles` and the independent `outputControl.extshd` selector. A non-good enabled stream opens text output with truncation or fatals on failure; disabled physical output creates a good dev-null sink. A good physical, string, or dev-null stream is retained with its current path/mode/position, so direct re-entry appends another header, while a closed/failed physical stream reopens with truncation. The body writes literal `Surface Name,`, every raw `Surface(1..TotSurfaces).Name` plus a comma in numeric order, then newline. It has no Surface filter, CSV quoting/escaping, status, flush, close, once flag, post-write check, or rollback; nonpositive counts still write the literal line, invalid positive Surface state or stream failure can leave a partial prefix, and only the open failure owns an exact fatal. Later daily shading rows use a separate BeginDay/non-warmup/RunPeriodWeather/report-request gate, which omits CP151 caller conditions `BeginSimFlag` and `DoWeathSim`; for a normal validated positive timestep count they emit `24 * TimeStepsInHour` rows and all `SurfSunlitFrac` values. They are dependency context, not CP151. Rust has no report flag, extshd selector, eplusshading CSV lifecycle, all-Surface export, or header/row writer; existing bounded incident-solar calculation/result state and Schedule:File:Shading input support are not parity. CP151 adds no project-contract requirement, source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 160 routines, split 58 state-mapped plus 102 source-mapped, with 57 required. CP152 adds non-required source-mapped `routine.set_storm_window_control` for the sole production `InitHeatBalance` line-2669 call under outer `TotStormWin > 0` and inner `BeginDayFlag`; only a successful return sets caller-owned `ChangeSet = false`. The body first clears `StormWinChangeThisDay`, then visits StormWindow records in numeric input order: it resolves `BaseWindowNum`, copies current to previous, computes local `DateOff - 1` with zero remapped to 366, uses inclusive/wrapping `BetweenDates(DayOfYear_Schedule, DateOn, adjusted DateOff)` to write current 1 or 0, makes `BeginSimFlag` overwrite previous with current, and latches any mismatch. Input dates and WeatherManager's test day use fixed leap-shaped `OrdinalDay(..., 1)` values, making `DateOn` inclusive and input `DateOff` exclusive; January 1 off wraps through day 366, and malformed equal dates that bypass input rejection are on for all 366 days. The first later non-BeginDay call while `ChangeSet` is false clears the event, snapshots each referenced current flag to previous, then sets `ChangeSet` true. Every positive-total caller pass then visits every unfiltered Zone/Space WindowSurface-span index, including normal Window, GlassDoor, and TDD_Diffuser entries rather than only StormWindow references, selecting the synthesized storm construction only for flag 1 plus Detailed and otherwise the current mutable `Surface.Construction`; this can reflect or overwrite the immediately preceding EMS active-construction decision. Duplicate BaseWindow records are ordered writes: last current wins, later previous sees the earlier new current, and when not BeginSim any intermediate mismatch latches; BeginSim suppresses each mismatch. Normal unique-base same-day re-entry clears an earlier transition. Effects are sequential with no validation, diagnostic, status, or rollback: helper failure preserves prefixes and prevents the `ChangeSet` clear and remainder; remainder failure preserves helper state, false `ChangeSet`, and its active-construction prefix while blocking CP151 and solar work; non-BeginDay snapshot failure leaves the event false, `ChangeSet` false, and a previous-day prefix for retry; and a nonpositive caller total skips the outer block and can leave stale state. Owner clears restore empty/zero Surface storm state, a false event, and true `ChangeSet`; BeginEnvironment does not. Active-construction selection, window starting-temperature guesses, daylighting recalculation, shaded-construction selection, divider behavior, and status output remain downstream dependencies. Rust has no storm-window input, state, controller, active-construction switch, or fenestration analog. CP152 adds no project-contract requirement, source inventory, Rust target/state/code, test, support, output, or conformance promotion; the inventory becomes 32 algorithms and 161 routines, split 58 state-mapped plus 103 source-mapped, with 57 required. CP153 adds required source-mapped `routine.init_conduction_transfer_functions` and its heat-balance project-contract requirement. The sole production `InitHeatBalance` path first enters `BeginSimFlag`, returns from `AllocateHeatBalArrays`, then under `AnyCTF || AnyEMPD` emits `Initializing Response Factors` and calls line 2621; EMPD uses the same thermal generator. The wrapper initializes local error/forced-report flags false, visits every Construction in array order through `calculateTransferFunction`, then after each returned child makes `SimpleCTFOnly` false for more than one history and raises `MaxCTFTerms`; any internal source independently makes the simple flag false. A second pass calls `reportLayers` only for `IsUsedCTF`; `ScanForReports` follows; request or forced error report writes four EIO headers and increments `cCounter` across all Constructions while reporting only used ones; accumulated errors fatal last with the exact wrapper message. The child first zeros all fixed CTF/QTF/source/user-temperature arrays plus timestep, counts, and U, then unused entries return. Used entries scan every material; source high-diffusivity diagnostics latch once without setting the shared bool, thickness over 3 m and resistance below `1.0e-3` set it, boundary/non-ROnly resistance layers receive equivalent-air properties, and interior ROnly layers stay massless. Any already-set shared error makes later used children reset and scan but return before generation. Adjacent massless merges alter local layers and persistently decrement source/user-temperature layer positions. All-resistive constructions use one history/term and `Outside0 = Cross0 = Inside0 = U = 1/R`, with a source/sink setting an error after coefficients are formed. A source-free exact reverse of an earlier used equal-layer Construction reuses its timestep/counts, swaps inside/outside, and retains cross/nonzero flux. New massive generation builds the bounded 1-D/optional-2-D node grid and source nodes, then each adaptive attempt calls exponential matrix, inverse matrix, gammas, and final coefficients in order. More than 18 terms or over-1% series-sum mismatch increments histories and timestep and retries; a zero largest sum fatals immediately, while timestep at least 7 hours emits convergence guidance, forces detailed reporting, sets the error, and breaks. Normal completion stores regular/QTF coefficients and U and deallocates work arrays; singular/nonfinite results have no distinct local validation. Nonfatal errors can leave earlier/full, failing/latest, and later/reset-zero Construction state, still run reachable layer/detail reporting, then fatal; immediate child/output failures bypass the remainder with no rollback. Re-entry recomputes child bundles and reverse reuse sees fresh earlier entries, but the wrapper never resets `SimpleCTFOnly` true or `MaxCTFTerms` zero, warning latches and appended reports persist, and repeated massless merging can decrement persistent source positions again. Owner clears reconstruct state; BeginEnvironment does not regenerate. Rust can consume `EnergyPlusEioSeeded` rows or `RustGeneratedSteady` no-history `1/R` coefficients and has bounded CTF consumers, but no native finite-difference/matrix/adaptive/QTF generator or this lifecycle. Construction helper files remain dependency context with no separate row or source-inventory addition. CP153 adds no Rust target/state/code, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 162 routines, split 58 state-mapped plus 104 source-mapped, with 58 required. CP154 adds non-required source-mapped `routine.gather_for_predefined_report`, declared at `HeatBalanceSurfaceManager.hh` line 99, implemented at `HeatBalanceSurfaceManager.cc` lines 623-1404, and reached only from the `InitSurfaceHeatBalance` lines-475-482 BeginSim branch; the first-time flag controls only the progress display, while the gather is unconditional within BeginSim and independent of whether predefined reports will be shown. The caller has already returned from `InitIntSolarDistribution` and `InitIntConvCoeff`; a successful gather next reaches `AnyCondFD` initialization and conduction-history work. Only `dataGeneral->Constructions` gates Construction EIO headers and rows. Each call zeros class counts and fenestration accumulators, allocates/zeros `computedNetArea`, optionally writes the assembly header, recreates all local dedup/header state, and first traverses stored `AllSurfaceListReportOrder` without an added use or report filter. Only `ExternalEnvironment`, `Ground`, `GroundFCfactorMethod`, and `KivaFoundation` are exterior. Exterior opaque classes are Wall/Floor/Roof; the interior branch also accepts IntMass. Opaque and Door area use Zone times List multipliers, while Window/TDD_Dome also use the Surface multiplier. Rows append construction/Zone/Space, no-film U, outside reflectance, geometry, and orientation; openings subtract from `computedNetArea(BaseSurf)` without validation or clamp. Azimuth is rounded to 0.01 before cardinal classification; `60 <= Tilt < 180` and azimuth at least 315 or below 45 alone marks north, while all other exterior fenestration contributes to non-north totals. Exterior Window/TDD reporting reuses nonzero SummerSHGC/visible cache or, for non-EQL zero cache, calls `CalcNominalWindowCond`, ignores `errFlag`, and stores returned optical values. EQL plus zero cache and dependency error can expose uninitialized locals; a non-EQL error can also poison the cache. Normal `CalcNominalWindowCond` completion also rewrites `SurfWinCoeffAdjRatio` for the complete exterior-Window list. Frames add the exact perimeter area, dividers use their grid formula, and the fully multiplied gross-plus-frame opening drives net subtraction and weighted U/SHGC/VT totals. Framed entries index the fixed NFRC product geometry/vision tables and call assembly helpers whose factory can substitute `SurfWinActiveShadedConstruction` from live `SurfWinShadingFlag`, independently of the supplied construction. An external-library model can overwrite NominalU and local SHGC through its NFRC rating helpers; locally unique construction/frame pairs alone write assembly EIO rows. Shaded-state count comes from the control list while shaded construction indices are read unchecked. Framed NFRC helpers run before pair dedup; unframed states use separate construction dedup plus NFRC width/height table index zero. The same shaded construction with different frames, or across framed/unframed domains, can append duplicate predefined cells keyed only by construction name. Two independent lazy same-tag EIO headers can expose different schemas. Active WindowShadingControl access is unchecked and the control row joins shaded plus storm construction names in stored order. Interior literal `iz-` mirrors skip all individual rows, net subtraction, and totals but still reach class counting. Interior AirBoundary openings contribute area and U to denominators while contributing zero SHGC/VT numerator, and interior legacy zero-cache results are not stored back. Every listed Surface increments its actual class; Window with original GlassDoor or TDD_Diffuser also increments the original class. Raw two-family Overhang and Fin object totals overwrite those buckets. A second report-order pass appends Wall/Floor/Roof net rows, followed by exterior overall/north/non-north and interior area-weighted totals with literal `-` on nonpositive denominators, then fourteen fixed labels in both total and exterior count columns; empty input still appends totals/counts. Every predefined entry appends. Real entries retain formatted text, original SI value, and digit metadata; report writing groups first-seen object order and takes the last duplicate cell, while `RetrievePreDefTableEntry` scans forward and takes the first; shaded construction/frame key collisions can trigger that disagreement within one invocation. Duplicate report-order indices/names therefore repeat counts, totals, subtraction, and entries and can drive net area farther negative. Unchecked topology/NFRC/list indices, ignored nominal errors, helper/output failures, and assertions leave partial predefined, cache, and EIO prefixes. CP154 itself emits no local diagnostic or status result and has no rollback; a non-return blocks the parent remainder even when a dependency diagnosed the cause. Re-entry recreates locals but appends every table row and repeats enabled EIO; nonzero optical cache skips legacy work, physical zero recomputes, and external-library/cache/scratch mutations persist. HeatBalanceSurfaceManager clear resets its own arrays/flags but owns no CP154 table/EIO state and does not remove duplicate predefined entries or emitted output. OutputReportPredefined clear removes definitions/ids/entries and requires `SetPredefinedTables`; owning topology/cache and General clears rebuild their respective state, while EIO stream rewind/truncation belongs to separate output-file lifecycle. Rust bounded opaque geometry, frame typing, and the static comparator are not this routine; Rust has no complete report-order/fenestration topology, predefined arena, NFRC/shading/report gatherer, EIO, duplicate, failure, or re-entry semantics. CP154 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 163 routines, split 58 state-mapped plus 105 source-mapped, with 58 required. CP155 adds required source-mapped `routine.allocate_surface_heat_bal_arrays` and its project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 101, implemented at `HeatBalanceSurfaceManager.cc` lines 1406-2206, and its sole production `src/` call is `InitSurfaceHeatBalance` line 350 inside the lines-349-355 BeginSim branch, after the caller's per-Surface outdoor/local-node weather and EMS overrides and before the `InterZoneWindow` reduction. The allocation phase defines 136 distinct fields across HeatBalSurf (100), FanSystem (15), MoistureBalance (12), HeatBalance (6), Surface (2), and HeatBalanceSurfaceManager (1): 119 ordinary per-Surface arrays, eight rectangular arrays, eight 19-element-outer jagged histories whose inners have extent `S`, and one EPVector. Of those fields, 121 are unconditional and 15 conditional. The source has 144 syntactic allocate/dimension sites, split 125 unconditional plus 19 conditional, because each of the eight jagged fields has separate outer allocation and inner dimensioning; 144 is therefore not the 121-plus-15 distinct-field count. When reached, exactly 133 distinct fields end at zero or zero-valued default construction; `SurfTempEffBulkAir` starts at 23 C, `TCondFDSourceNode` at 15 C, and `SurfRoughnessExt` at `Invalid` (-1). `AnyInternalHeatSourceInInput` gates ten fields and is evaluated separately at lines 1429 and 1555, while `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` gates five fields and is evaluated once at line 1466. False branches neither clear nor deallocate preserved prior state. After the reached allocation phase completes, line 1607 displays exact text `Setting up Surface Reporting Variables`; the body then visits numeric Surface indices `1..S` and skips only non-`HeatTransSurf` entries. It contains 78 syntactic `SetupOutputVariable` call sites: the conditional union of 77 per-Surface sites plus one unconditional site-total setup call, with 64 Average and 14 Sum sites. The exact Surface-side gates are non-Kiva, non-Window, external-environment-or-advanced with nested non-Window, opaque class with nested non-Kiva, `Construct(surface.Construction).SourceSinkPresent`, Window, exact external environment, and advanced-only construction index; the 21 retained-heat-transfer sites have no additional Surface gate. Per-Surface keys use `Surface.Name`; the site-total Sum uses key `Environment` and existing `SumSurfaceHeatEmission` without resetting it. There is no CP155 report-request filter before the dependency calls. OutputProcessor's first setup call initializes that subsystem when `OutputInitialized` is false, before request checking and dictionary insertion; the dictionary deduplicates name plus units, every call increments setup counters, and these no-meter calls append an active `OutVar` only for requested name/key matches. Allocation and setup are sequential and nontransactional, with no local validation, diagnostic, status, rollback, or cleanup. A failing dependency can retain a six-owner/output-setup prefix and blocks the caller's `InterZoneWindow` scan, remainder, and first-time clear. With `S == 0`, active jagged outers still allocate 19 elements, and `surfQRadFromHVAC.allocate(0)` leaves an empty EPVector with its allocated sentinel true; the display and site-total setup call still occur, and only a requested site-total match becomes active. Negative `S` can mutate many Objexx arrays before the EPVector argument converts to an enormous unsigned size; its allocation sets the allocated sentinel before resize fails. Direct re-entry refills reached value-bearing dimensions, may relocate backing storage, and preserves newly false conditional allocations; setup counters advance again, dictionary entries remain deduplicated, and requested matches can append duplicate active records whose earlier pointers reference pre-redimension storage. Five owner clears reconstruct their array owners; HeatBalance clear restores the two allocation flags, HeatBalanceSurfaceManager clear releases its own reference-air array and resets first-time flags, and OutputProcessor clear is separately required for registrations. BeginEnvironment alone is not a full CP155 reset or rerun. Seven unit-test files make 35 direct calls only as downstream setup and provide no dedicated zero-count or re-entry test. Rust has no complete six-owner allocation, history/default/conditional-preservation state, output-setup, or re-entry lifecycle analog. CP155 adds no source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 164 routines, split 58 state-mapped plus 106 source-mapped, with 59 required. CP156 adds required source-mapped `routine.init_thermal_and_flux_histories` and its project-contract entry immediately after `allocate_surface_heat_bal_arrays`. It is declared at `HeatBalanceSurfaceManager.hh` line 103, implemented at `HeatBalanceSurfaceManager.cc` lines 2208-2447, and its sole production `src/` call is `InitSurfaceHeatBalance` line 383 inside the lines-379-384 BeginEnvrn branch; no unit test calls it directly. The caller has already refreshed Surface weather/local-node/EMS state, conditionally run CP155, refreshed construction absorptances, and called `UpdateVariableAbsorptances`. Its first-time flag gates only exact display `Initializing Temperature and Flux Histories`; CP156 itself has no output or diagnostic, and a successful return next reaches movable insulation. CP156 first visits numeric Zones: placement-new reconstructs each ZoneHeatBalanceData with its named temperature/MAT histories and MixingMAT at 23 C, five humidity values initially 0.01, and remaining zero/default state; only `airHumRatAvg` and `airHumRat` are then overwritten by `OutHumRat`, and `TempTstatAir` becomes 23 C. Every enclosure MRT becomes 23 C, and every existing Space heat-balance record is independently placement-new reset regardless of `doSpaceHeatBalance`, with the same two humidity overwrites. Zone order, stored `spaceIndexes`, and inclusive heat-transfer ranges then receive exactly 34 assignments per reached Surface: `SurfTempEffBulkAir`, `SurfTempIn`, and `SurfTempInTmp` become 23 C, `SurfHConvInt` becomes 3.076 W/m2-K, and the other 30 coefficient/report/temperature fields become zero. Independently guarded nonnegative opaque/IntMass ranges zero four conduction/beam fields; guarded Window ranges set six frame/divider current/old/outside temperatures to 23 C and two shading flags to `NoShade`, with no class recheck. A Zone/Space/term-outer `1..19` pass sets both base temperatures to 23 C and both base fluxes to zero. The first line-2339 evaluation of `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` also resets `SurfCurrNumHist`, both master temperatures, and both master fluxes. A separate `AnyInternalHeatSourceInInput` pass, ordered Surface before term, sets four source/user temperature histories to 23 C and two source flux histories to zero. False feature branches preserve prior allocations and contents. Working `CondFDRelaxFactor` then copies its input. The next loop is term outer `1..MaxCTFTerms + 1` and stored AllHT Surface inner. ExternalEnvironment and OtherSideCondModeledExt outside temperatures use the already refreshed local dry bulb, Ground uses BuildingSurface ground temperature, GroundFCfactorMethod uses FCFactor ground temperature, and other boundaries retain 23 C under normal reachable topology. Each reached outside flux term becomes `q = Construct(surface.Construction).UValue * (OutsideTempHist(1) - InsideTempHist(1))`; each inside term copies base outside-flux slot 2. Because term 1 executes before slot 2 is written, normal preceding zero state leaves inside term 1 zero; term 2 first writes slot 2 to q, so inside terms 2 and higher receive q. Global MaxCTFTerms zero still executes term 1 and reads fixed slot 2; normal 18 reaches all 19 slots. An AllHT pass sets each referenced exterior cavity's `TbaffleLast` and `TairLast` to 20 C. Stored AllHTKiva order uses `surfaceConvMap[SurfNum]`, inserting a missing record, then assigns inside constant 3.076, default forced term, and outside constant 0 in `.in` then `.f` then `.out` order. The same SimpleCTF/EMS expression is evaluated again at line 2413. When true, special boundary families overwrite all 19 master outside-temperature terms; other boundaries retain 23 C, and terms `2..Construct.NumCTFTerms + 1` copy base outside-flux slot 2 to both master fluxes while term 1 and higher unused terms retain zero. Positive `TotOSCM` finally sets numeric OSCM TConv/HConv/TRad/HRad to 20/4/20/4 while preserving EMS fields and other members. CP156 is selective and nontransactional: direct re-entry destructively resets every reached record/range/history/factor/list/count target; newly false gates and removed ranges/list/map entries preserve prior state, so changed topology can mix fresh and stale cells. With zero Zones, existing enclosures and Space records, the CondFD factor, AllHT/cavity/Kiva lists, and OSCMs still reset; conventional `0..-1` ranges skip. It assumes CP155/prior allocations and validated counts, ranges, list membership, Construction/cavity indices, and term bounds. It has no return, local validation, warning, status, catch, rollback, or cleanup; nonfinite humidity, boundary temperatures, or U-value propagate. The concrete placement-new default construction is nonthrowing; unchecked index/container access can assert, terminate, or produce undefined behavior after earlier mutations. Kiva map allocation or `std::function` work can throw; a Kiva failure can leave an inserted key with only `.in` or `.in` plus `.f` committed, and any failure blocks the caller remainder and first-time tail. Clears are distributed across predictor/corrector, ViewFactor, HeatBalance, HeatBalSurf, FanSystem, Surfaces, and SurfaceGeometry/Kiva owners; no partial owner clear reproduces CP156, while a reached BeginEnvironment call is its ordinary reset boundary. Rust's bounded optional `HeatBalanceCtfInitialHistoryPolicy::EnergyPlusSurfInitial` helper accepts configurable initial temperature, selects a typed boundary temperature, computes steady `1/R` flux, and fills variable-length base histories with the same q on both sides. It does not implement EnergyPlus's fixed 19-slot current/prior shell and term-1 inside-flux asymmetry, Zone/Space and 34-plus-4-plus-8 Surface reset, master/source histories, cavity/Kiva/OSCM state, or selective failure/re-entry lifecycle. CP156 adds no source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 165 routines, split 58 state-mapped plus 107 source-mapped, with 60 required. CP157 adds non-required source-mapped `routine.eval_outside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 105 and implemented at `HeatBalanceSurfaceManager.cc` lines 2449-2481. Its sole production `src/` call is `InitSurfaceHeatBalance` line 388, the first child of the shared lines-387-390 `AnyMovableInsulation` block; CP158 follows at line 389, so CP157 failure blocks inside evaluation while a later CP158 failure preserves completed exterior effects. One unit test makes three direct calls with an always-on Schedule: Regular verifies solar absorptance, conductance, thermal absorptance, and roughness, while Glass and GlassEQL verify only solar absorptance; no inactive case is tested. CP157 visits stored `extMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `extMovInsuls(SurfNum)` and calls its Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads the current `SurfActiveConstruction` and its exterior `LayerPoint(1)` material, then copies raw `AbsorpSolar`, `AbsorpThermal`, and `Roughness` to the three exterior Surface fields in that order. This branch preserves stale `H` and exact `presentPrevTS` and does not use `movInsul.matNum`. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL then dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; the comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, preserves positive infinity, and has no upper clamp. Other groups copy raw `AbsorpSolar`. Both active material paths next copy raw thermal absorptance and roughness, and exact `presentPrevTS` remains untouched. Both successful branches overwrite the caller's just-refreshed construction and variable-absorptance exterior values. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-construction properties while preserving H, and removed entries receive no CP157 mutation. Under global `AnyMovableInsulation`, CondFD later reads `extMovInsuls(Surf).H` without checking `present` or exterior-list membership, so a stale positive active-to-inactive H can still select its movable-insulation branch. CP157 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/layer/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and a current inactive-present/property or active-present/H/property prefix. `SurfacesData::clear_state` reconstructs the records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied property arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no exterior movable-insulation input/state, traversal, Schedule controller, active-construction property restoration, glass optical path, conductance/CondFD handoff, or matching failure/re-entry analog. CP157 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 166 routines, split 58 state-mapped plus 108 source-mapped, with 60 required. CP158 adds non-required source-mapped `routine.eval_inside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 107 and implemented at `HeatBalanceSurfaceManager.cc` lines 2483-2513. Its sole production `src/` call is `InitSurfaceHeatBalance` line 389, the second child of the shared lines-387-390 `AnyMovableInsulation` block after CP157; a CP158 failure preserves completed exterior effects and blocks the caller remainder, after a successful CP158 return the caller resumes its intervening ground/shading/radiation work, and reaches unconditional CP159 at line 457 only if those dependencies also return. One unit test makes three positive always-on calls while reusing the same concrete `MaterialShade` record and mutating only its group tag Regular to Glass to GlassEQL. The first verifies solar absorptance, H, present, and thermal absorptance; the later two verify only solar absorptance, and no inactive case is tested. CP158 visits stored `intMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `intMovInsuls(SurfNum)` and calls Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads current `SurfActiveConstruction`, then copies that Construction's `InsideAbsorpSolar` followed by `InsideAbsorpThermal` to the two inside Surface fields. This branch preserves stale `H` and exact `presentPrevTS`, does not use `movInsul.matNum`, and writes no roughness. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; Objexx's comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, retains positive infinity, and applies no upper clamp. Other groups copy raw `AbsorpSolar`; both active paths then copy raw `AbsorpThermal`. Exact `presentPrevTS` and roughness remain untouched. Both successful branches overwrite the current inside absorptance values, including the caller's construction-derived refresh when its earlier gate ran; `UpdateVariableAbsorptances` writes only exterior arrays and contributes no inside value. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-Construction absorptances while preserving H, and removed entries receive no CP158 mutation. Unlike CP157's CondFD path, standard and optimized inside-balance H use is gated by the global flag plus `intMovInsuls(SurfNum).present`, though not by list membership: stale inactive H alone is suppressed, but a removed formerly-active record retaining `present = true` and H can still be consumed. After caller line-417 `CheckGlazingShadingStatusChange`, line-444 passes `SurfIterations = 0` into `CalcInteriorRadExchange`; outside BeginEnvironment, reached non-window enclosure Surfaces can invoke `UpdateMovableInsulationFlag`, which compares `present != presentPrevTS` then tests strict `abs(base Surface.Construction InsideAbsorpThermal - movable AbsorpThermal) > 0.01`. A NaN difference is false, while BeginEnvironment forces recalculation separately. End-zone-timestep `RecKeepHeatBalance` copies `present` to `presentPrevTS` at line 3051. CP158 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and an inactive present/solar or active present/H/solar prefix. `SurfacesData::clear_state` reconstructs records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied absorptance arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no interior movable-insulation input/state, traversal, Schedule controller, active-Construction absorptance restoration, glass optical branch, conductance/inside-balance handoff, long-wave change detection, or matching failure/re-entry analog. CP158 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 167 routines, split 58 state-mapped plus 109 source-mapped, with 60 required. CP159 adds required source-mapped `routine.init_solar_heat_gains` and its project-contract entry immediately after `init_thermal_and_flux_histories` and before `calc_heat_balance_outside_surf`. It is declared at `HeatBalanceSurfaceManager.hh` line 109, implemented at `HeatBalanceSurfaceManager.cc` lines 2515-3776, and called only as unconditional `InitSolarHeatGains(state)` inside `InitSurfaceHeatBalance` line 457; the caller first-time flag gates only its preceding progress text, a CP159 failure blocks daylighting/internal gains/CP160, and only the successful caller tail clears that flag. Exactly four direct calls occur in three unit-test contexts: the incident-multiplier fixture calls twice under positive solar and checks only exact half-versus-whole transmitted solar, WindowFrameTest exercises BeginEnvironment reset with zero solar and asserts only a downstream heat-loss relation, and the CFS fixture calls under positive solar before mutating back-surface state/rerunning distribution and asserts only downstream beam absorption. Every call unconditionally zeros Zone window/opaque conduction rate and energy reports, every enclosure initial-diffuse-reflection term, stored opaque-range conduction/absorbed/report fields, stored Window-range frame/divider/shade/convection/gain/energy fields, and `SurfWinQRadSWwinAbs` through `CFSMAXNL + 1`. While first-time remains true it seeds both ground-reflection factors from each Surface ground view factor. It defines positive solar by `SunIsUp && signed beam+ground+diffuse > 0`, derives sunset and sun-up/no-radiation plus BeginEnvironment reset, then commits `PreviousSolRadPositive` before any broad reset; NaN makes the comparison false, and a failed sunset-only reset loses its edge on retry while the other two reset causes persist. Positive-or-reset calls clear all-Surface inside-beam/incident/reflection and sky/ground arrays, enclosure transmitted reports/energies, stored-Window shade/transmission/reveal/BSDF/absorbed-layer fields; reset-only calls additionally clear only `EnclSolQD`/`EnclSolQDforDaylight`, TDD transmittances and selected dome rate/layer fields, optional three reflection factors, and selected all-Surface reports/profile/system optics. They do not clear `EnclSolQSDifSol`, either complex-window ground split, `SurfSkyDiffReflFacGnd`, positive-only `IncSolMultiplier`, custom-only `GndReflSolarRad`, `AbsDiffWin*`, scheduled-mutated `SurfWinA`, external-library total energy, or all TDD dome W/energy state. The positive branch asserts reflection-table dimensions even when reflection calculation is disabled, computes `max(beam*SOLCOS(3)+diffuse,0)` with the expression first so NaN persists, samples/stores one multiplier for every numeric Surface using same-index request alignment, computes sky and ground incidence, and, when enabled, writes the complex ground split before interpolating the beam ground factor, then interpolates obstruction/ground factors and rebuilds main sky/ground incidence. It calls profile angles, optional reveal reflection, then either the external-library simplified opaque plus interior distribution path or ordinary interior distribution. It forms daylight and normal enclosure diffuse power, copies daylight power into `EnclSolQSDifSol`, adds only flagged off-diagonal interzone contributions, then applies diagonal fraction and `solVMULT`. Shading-range reports omit obstruction additions; stored AllExt reports add them; TDD diffuser overrides use active diffuser `TransDiff` and dome angle/sunlit while retaining earlier ground-component reports; shelf ground overrides happen after incident totals and do not refresh them. Opaque absorption uses base Construction and the first exact scheduled Surface/base-Construction pair, with unscheduled beam scaling but scheduled bare `SurfOpaqAI`; duplicate opaque ranges overwrite outside/report assignments but repeat inside `+=`. The Window loop dereferences SurfaceWindow before its solar/TDD guard and uses active Construction except base EQL layer count; Detailed optics cover shade/screen/blind horizontal sky-ground splitting and switchable interpolation, BSDF uses the first exact scheduled Surface/active-Construction pair and mutates `SurfWinA`, EQL uses base-system count with active optics, and external-library layers never write total energy. Shaded-construction array/member ownership differs unchecked. Common frame/divider work preserves projection, reveal, switchable, suspended-divider, ExtBlind/ExtShade/ExtScreen formulas; between-glass enters alternate incidence but no final absorber case. A zeroed local beam array is populated by earlier lists/overrides and consumed later, so malformed list/range alignment can yield zero beam; repeated windows reset Detailed/EQL totals locally but can accumulate BSDF/external totals. Final TDD dome absorption uses base Construction and writes energy inside its layer loop. Representative averaging uses raw constituent-area divides with no zero/topology guard, can consume earlier representatives in traversal order, uses active representative `TotGlassLayers` regardless model, and overwrites only per-area opaque/window-layer/frame/divider fields without recomputing Window layer W, totals, energy, or reports. CP159 emits no direct diagnostic and has no validation, catch, rollback, or cleanup; dimension/material assertions, unchecked blind casts, indices, TDD/area/reflection denominators, trig/nonfinite inputs, child failures, or allocation can leave an ordered prefix. Six mutated owners require coordinated clear: HeatBalance, HeatBalSurf, Surfaces, Environment, DaylightingDevices placement-new reconstruction plus `HeatBalSurfMgr::clear_state`; the latter rearms first-time and recreates scratch arrays, Environment restores the previous-solar latch false, and dependency-owner clears alone do not replay CP159. Rust incident-solar forcing diagnostics and bounded opaque absorption, plus run-blocked multiplier/scheduled-inside declarations, are not this latch/distribution/window/report lifecycle. CP159 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 168 routines, split 58 state-mapped plus 110 source-mapped, with 61 required. CP160 adds required source-mapped `routine.init_int_solar_distribution` and its project-contract entry immediately after `init_solar_heat_gains` and before `compute_int_thermal_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 111, implemented at `HeatBalanceSurfaceManager.cc` lines 3778-4177, and called only as unconditional `InitIntSolarDistribution(state)` inside `InitSurfaceHeatBalance` line 468; the first-time flag gates only preceding progress text, and a CP160 failure blocks interior convection and the successful parent-tail flag clear. The only direct unit context calls twice: with InterZone disabled and beam report 10 W, the night call observes an already-zero report remain zero, while the sun-up call checks only 10/6 = 1.666667 W/m2. SunIsUp gates only that AllHT, non-Shading beam-report traversal; at night CP160 still distributes internal short wave and leaves prior beam intensity/amount/energy untouched. Every call assigns each Solar enclosure raw `EnclSolQD + sum(space QLTSW)` and lights-only `sum(QLTSW)`; an InterZoneWindow receiver/source double loop requires both receive flags, excludes the diagonal, recomputes source QLTSW, adds raw matrix-weighted total/lights/diffuse power, and overwrites diffuse-report energy after each qualifying source while a no-source receiver gets no energy write. The enclosure values are then multiplied by `solVMULT` and, only for InterZoneWindow, the diagonal fraction; there is no normalization or range/finite guard. Sun-up beam reporting divides Zone beam W by raw enclosure total area, then multiplies by Surface area plus divider area and timestep seconds. Zone/stored-Space traversal visits inclusive opaque then Window ranges without sorting, deduplication, class checks, or bound validation. Opaque total short-wave absorption uses current `SurfAbsSolarInt`, lights-only absorption uses base Construction `InsideAbsorpSolar`, and initial diffuse is added last. Under global movable insulation, every reached opaque record marked present first writes `SurfQRadSWOutMvIns = prior outside absorption * current exterior absorptance / base outside-material absorptance`, then overwrites outside absorption through an optional `MaterialFen.Trans` and a second division by current absorptance; a failed cast supplies zero transmittance and both denominators are unchecked. Direct retry feeds the overwritten outside value back, while a false global/present gate leaves that value and `SurfQRadSWOutMvIns` stale. Every Window dereferences current SurfaceWindow, radiant/Solar enclosures, and active Construction with no exterior-solar or class gate. Both EQL and non-EQL overwrite Window internal radiant gain from enclosure long wave, `radThermAbsMult`, and current thermal absorptance; a load-component pulse adds exactly 0.01 times radiant-enclosure floor area, but common frame/divider formulas keep the unpulsed value plus HVAC radiation. Non-EQL has no separate external-library branch and uses active conventional optics: unshaded layers add back diffuse absorption; shade/screen layers use shaded back optics; only IntBlind/ExtBlind interpolate pane absorption with unclamped `Interp`, so BGBlind skips pane addition while still receiving the later any-blind shade absorption. Only interior shade/blind writes shade long-wave absorption; exterior shade/blind/screen scales shade short wave by glazed fraction. Switchable layers use `InterpSw`, which clamps ordered factors including infinities to [0,1] but propagates NaN, and dereferences the shaded Construction without a zero-index guard. Zero-index or conditional/invalid shading can skip main layer work and later fail in the Detailed initial-diffuse tail. Positive frame/divider areas add Solar-enclosure short wave plus unpulsed internal/HVAC long wave with raw projection corrections; suspended dividers assert a last-layer MaterialGlass and use an unchecked multiple-reflection denominator, while IntShade asserts MaterialFen and IntBlind applies current solar/IR transmission. EQL uses active EQL NL and back diffuse optics and omits frame/divider work. For every positive adjacent link, a conventional target uses the current/source active layer count but adjacent base `AbsDiff`; an EQL target uses adjacent base EQL NL but current/source active `AbsDiffFrontEQL`. The final Detailed/BSDF/EQL tail adds prior initial-diffuse layer values, and Detailed shade/screen/blind also adds initial shade absorption. All CP160 Window additions mutate only per-area `SurfWinQRadSWwinAbs` and never refresh CP159 layer W, total W, or total energy; frame/divider additions likewise have no separate W/energy derivation here. The unconditional final TDD child is declared at `DaylightingDevices.hh` line 95, implemented at `DaylightingDevices.cc` lines 1506-1559, called only by CP160 line 4176, and has no direct unit-test call. It uses diffuser base `TransDiff`, stale CP159 diffuser total W, normally prior CP159/dependency dome layer one, and newly CP160-mutated diffuser layer one; its raw formula applies diffuser area even to the dome term and divides by `TransDiff`. Zero-first `max(0, rawGain)` maps negative, negative infinity, and NaN report values to zero while retaining positive infinity, but transition-zone gains use unclamped signed/nonfinite rawGain times length divided by unchecked total length. The current pipe report is committed before its transition loop, so failure can retain that report and a partial current `TZoneHeatGain` prefix after complete earlier pipes. Preceding `ManageInternalHeatGains`/`FigureTDDZoneGains` can zero current transition gains on BeginEnvironment but not the pipe report, and its current internal-gain update completes before CP160 writes new transition values. CP160 has no direct diagnostic, validation, return status, catch, rollback, or cleanup; only the suspended-divider and IntShade casts assert, while invalid topology, arrays, indices, layer mismatches, denominators, and nonfinite inputs can leave an ordered prefix. Re-entry mixes assigned enclosure/radiant/TDD state with additive Zone diffuse, opaque, layer, adjacency, frame/divider, and shade state; Sun-down, no-source, existing no-longer-reached range members, reduced transition-zone counts, and changed gates can preserve stale values, and normal parent order merely relies on preceding CP159 resets. Four directly mutated owners clear by placement-new: HeatBalance, HeatBalSurf, Surfaces, and DaylightingDevices; HeatBalSurfMgr clear only rearms the caller display flag. Rust separately consumes initialized/supplied opaque inside-short-wave state and has a bounded typed-Zone radiant-gain distributor whose ordered `<= 0` gates admit NaN, but no CP160 Solar-enclosure/interzone, Window/shade/frame/divider, adjacency, movable-insulation, TDD, report, failure, or re-entry state machine. CP160 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 169 routines, split 58 state-mapped plus 111 source-mapped, with 62 required. CP161 adds required source-mapped `routine.compute_int_thermal_absorp_factors` and its project-contract entry immediately after `init_int_solar_distribution` and before `compute_int_sw_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 113, implemented at `HeatBalanceSurfaceManager.cc` lines 4179-4295, and called only by unconditional `InitSurfaceHeatBalance` line 427; runtime execution is after shading-status production and before CP162, CP159, and CP160. The caller first-time flag gates only progress text plus the separate preceding `InitInteriorRadExchange`, not CP161, and a CP161 failure blocks every later child and the successful caller-tail flag clear. Its sole direct unit test uses one 1 m2 `IntBlind` Window with 0.1 shade plus 0.1 glass effective emissivity and checks only `SurfAbsThermalInt = 0.2` and `radThermAbsMult = 5`. Pass one scans the full allocated `EnclRadInfo` container, skips false `radReCalc`, then visits stored enclosure Space Window ranges: `IntShade`/`IntBlind` assign effective shade-plus-glass emissivity and every other flag assigns active-Construction inside thermal absorptance. Pass two, independently of `radReCalc`, scans `SurfMovSlatsIndexList` only when `AnyMovableSlat` and rewrites only currently interior shade/blind entries. Pass three again scans the full allocated enclosure container and each flagged `SurfacePtr`: nonswitchable area uses current `SurfAbsThermalInt`, while switchable area uses clamped-factor `InterpSw` between declared base-Construction and `activeShadedConstruction` absorptance. Positive frames add area times `(1 + 0.5*projection)` times emissivity. Positive dividers start with divider emissivity, substitute base-Construction absorptance when suspended, and either use the projected term or, for an interior device with a declared layer, assert the active shaded Construction last material as `MaterialFen` and add shade effective emissivity plus divider absorptance times shade thermal transmittance or current blind back-IR transmittance; the shaded Construction is dereferenced before the layer-present test. This preserves deliberate active/base/runtime-optics asymmetries. The first pass visits full Window ranges, but normally initialized `SurfacePtr` holds representative surfaces only; CP161 ignores constituent-aggregated `EnclRadInfo.Area` and uses raw representative Surface/frame/divider areas, creating a representative-extent mismatch. The producer bulk paths use separate Solar and radiant enclosure counts, while its shading-change loop is Solar-count-bounded but reads same-index radiant `SurfacePtr`; CP161 range-for can see a retained radiant-container tail and never clears `EnclRadInfo.radReCalc`. CP162 reads the corresponding `EnclSolInfo.radReCalc`, not the same flag object. Normal movable slats are observed by the producer through `SurfacePtr`, while direct or malformed topology can still let the independent list update an unflagged enclosure. More importantly, CP158 can change opaque inside thermal absorptance without that movable-insulation transition itself setting `radReCalc`, leaving a normal-path multiplier stale absent another producer cause. Stored orders have no local sort, deduplication, class/model, membership, or bounds validation. `radThermAbsMult = 1/SUM1` is committed only after a complete flagged enclosure with no zero, sign, minimum, or finite guard: zero becomes positive infinity, signed infinity becomes signed zero, negative stays negative, and NaN propagates; `InterpSw` clamps ordered factors including infinities but preserves NaN and raw nonfinite endpoint arithmetic. CP161 has no direct diagnostic, return status, allocation, catch, rollback, or cleanup and only the divider material assertion. Failure can leave a first-pass Window prefix, then a movable-list prefix, or complete earlier enclosure multipliers while the current/later multipliers remain old; false `radReCalc`, no-longer-reached range/list entries, malformed independent slat updates, and direct retry can preserve selective stale state, while a nonpositive frame/divider area simply drops from a recomputed flagged sum. Exactly two direct owners have mutated targets: HeatBalSurf owns the written `SurfAbsThermalInt` and separately supplies the read-only movable-slat list, while ViewFactor owns the written `radThermAbsMult` and separately supplies read-only enclosure flags/topology; Surfaces, HeatBalance topology, Construction, Material, Window, SolarShading, and interior-radiation state are dependencies only. Rust has only a bounded typed-Zone radiant-gain distributor that applies `.max(0.0)` to absorptance but whose ordered `<= 0` gain/sum gates admit NaN, not this recalculation/shading/representative/frame-divider/raw-reciprocal state machine. CP161 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 170 routines, split 58 state-mapped plus 112 source-mapped, with 63 required. CP162 adds required source-mapped `routine.compute_int_sw_absorp_factors` and its project-contract entry immediately after `compute_int_thermal_absorp_factors` and before `compute_dif_sol_exc_zones_wiz_windows` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 115, implemented at `HeatBalanceSurfaceManager.cc` lines 4297-4471, and called only by unconditional `InitSurfaceHeatBalance` line 433 immediately after CP161; the caller first-time flag gates only the preceding diffuse-solar progress text, and a CP162 failure blocks conditional interzone diffuse exchange, daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. There is no direct unit-test call. CP162 scans the full allocated `EnclSolInfo` container, skips false `radReCalc`, and accumulates each flagged enclosure over stored `SurfacePtr` order without local sort, deduplication, class, membership, index, or finite validation. Normal Solar topology contains every stored Space Surface except AirBoundary, including nonrepresentatives, whereas the shading-change producer is Solar-count-bounded but observes same-index representative-only radiant `SurfacePtr`; this records an observer/calculation-domain asymmetry without claiming a normal unflagged constituent. Only direct, malformed, representative-misaligned topology or count/container mismatch can turn that asymmetry into an unflagged Solar enclosure. Each Surface selects its active Construction and is opaque only when active `TransDiff <= 0`; opaque area uses current `SurfAbsSolarInt`, while NaN transmittance enters the Window branch. Conventional versus equivalent-layer selection then uses the declared base Construction `WindowTypeEQL`. Conventional Windows sum active-layer back absorptances and active diffuse transmission; only a nonzero storm-aware `SurfWinActiveShadedConstruction` enables shade/screen/blind optical substitution: shade/screen optics come from that Construction, blinds raw-linearly interpolate current slat endpoints, and a shade/blind flag with index zero silently retains bare active optics and zero shade absorption. Switchable glazing alone has two separate positive-shaded-Construction assertions and clamped-factor `InterpSw` for layer absorption and transmission. Their raw Surface-area term is `TransDiffWin + AbsDiffTotWin + DiffAbsShade`; positive frames add area times solar absorptance times `(1 + 0.5*projection)`. Positive dividers use current divider solar absorptance; suspended dividers assert the active Construction last layer as `MaterialGlass` and apply an unchecked glass/divider multiple-reflection denominator. Interior shade/blind dividers omit projection and add `DividerAbs + DiffAbsShade`; all others use `(1 + projection)*DividerAbs`. The base-EQL branch instead consumes the active Construction EQL pointer, layer count, diffuse transmission, and back absorptances and ignores shading, switching, frames, and dividers. After a complete enclosure, only strict `SUM1 > 0.01` writes `solVMULT = 1/SUM1`; positive infinity becomes positive zero, while exactly 0.01, smaller/negative values, negative infinity, and NaN enter the bad-sum branch, issue the exact misspelled `ComputeIntSWAbsorbFactors` warning only while `solAbsFirstCalc` is true, set that latch false after the warning returns, and write zero. A later good sum or BeginEnvironment does not rearm the latch. False recalculation preserves both targets; failures can leave earlier enclosure commits and the current/later multipliers and latch old, while warning-pipeline failure can leave partial diagnostic side effects before either target is committed. CP158 can change opaque `SurfAbsSolarInt` without itself producing a recalculation flag, giving the normal-path stale-multiplier case; direct or malformed Solar/radiant observer misalignment can also preserve stale state. ViewFactor is the sole direct mutation owner for `solVMULT` and `solAbsFirstCalc`; warning counters/streams/SQLite/callbacks are child diagnostic side effects, while Surface, HeatBalSurf, Construction, Material, equivalent-layer, Window, SolarShading, global, and topology state are dependencies. Rust has no `ComputeIntSWAbsorpFactors`, Solar multiplier, latch, or post-initialization production writer for `inside_shortwave_absorbed_w_per_m2`; its separate typed-Zone thermal-radiant helper has ordered `<= 0` gain/sum gates that admit NaN and is not this short-wave routine. CP162 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 171 routines, split 58 state-mapped plus 113 source-mapped, with 64 required. CP163 adds required source-mapped `routine.compute_dif_sol_exc_zones_wiz_windows` immediately after `compute_int_sw_absorp_factors` and before `init_ems_controlled_surface_properties` in full source-definition order; its project-contract entry remains after `compute_int_sw_absorp_factors` and before `calc_heat_balance_outside_surf` in required-routine order. It is declared at `HeatBalanceSurfaceManager.hh` line 117, implemented at `HeatBalanceSurfaceManager.cc` lines 4473-4644, and called only at `InitSurfaceHeatBalance` line 439 inside the lines-435-440 `InterZoneWindow` block; that parent flag gates both progress-and-call, while the nested first-time flag gates only the exact progress text. A guarded CP163 failure blocks daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. Exactly three direct calls occur in one unit fixture: with three enclosures and two reciprocal 1 m2, 0.1-transmittance Windows, the first call leaves receiver `HasInterZoneWindow` false and checks only identity diagonals plus all receive flags false; before the second call the fixture enables enclosures 1 and 2 and checks only flags true, true, false; before the third it enables `KickOffSimulation` and again checks only identity diagonals plus false flags. No test asserts a matrix coefficient or covers sizing kickoff, denominators, multi-enclosure paths, nonfinite values, allocation incoherence, or either assertion's failure. Allocation is gated solely by an unallocated `ZoneFractDifShortZtoZ`: that path allocates the target square matrix, receive-flag vector, then separate `DiffuseArray` scratch. Every call resets flags false, target identity, then scratch identity before either kickoff flag can return; already allocated dimensions are not repaired. Using the consumers' row-receiver/column-source convention, a stored-order `AllHTWindowSurfaceList` pass skips nonpositive adjacency, Surface-self adjacency, declared/base `TransDiff <= 0`, and a false receiver `HasInterZoneWindow`, then adds `A(receiver, source) += TransDiff * receiver.solVMULT * Area`; it does not use active/shaded/storm optics, validate the adjacent endpoint, sort, deduplicate, or validate indices, class, sign, range, or finiteness. The receiver flag is set when `solVMULT != 0`, independently of the coefficient. Same-enclosure paired Surfaces can modify raw diagonal state that the next transform discards. With raw matrix `A` frozen and identity scratch `D`, every distinct pair computes `D(R,S) = A(R,S)/(1-A(R,S)*A(S,R))`, and `D(N,N) = 1 + sum[M != N](A(N,M)*D(M,N))`; there is no denominator or finite guard. The copied target is followed by exactly two dimension assertions. A later scan only ORs flag `N` when some distinct receiver has `D(M,N) > 0`, making that scan a positive source-column witness rather than the direct pass's receiver witness. Frozen `D` then contributes every flagged, node-distinct simple path of exactly two, three, and four edges with products `D(J,K)*D(I,J)`, `D(K,L)*D(J,K)*D(I,J)`, and `D(L,M)*D(K,L)*D(J,K)*D(I,J)` into receiver/source cells `Z(I,K)`, `Z(I,L)`, and `Z(I,M)`; additions never feed later products, diagonals are not updated, node revisits and paths beyond four edges are omitted. Edge gates skip only exact signed zero, so negative, infinite, and NaN coefficients enter; ordered `> 0` flagging rejects negative and NaN but accepts positive infinity. CP163 emits no diagnostic and has no status, catch, rollback, cleanup, or local validation. Allocation, reset, direct-pass, pair-copy, flag-scan, or path failure can leave the corresponding ordered prefix; a coherent direct retry first erases all three objects, while kickoff commits false/identity state and returns. When production `InterZoneWindow` is false, CP163 is not entered and prior target/flag/scratch state remains dormant stale; CP159 and CP160 gate their normal interzone consumers on the same flag. Exactly two direct owners mutate: HeatBalSurf owns the target matrix and flags and clears them by placement-new and also supplies the read-only caller gate `InterZoneWindow`; CP163 never writes that gate and placement-new restores it false. HeatBalSurfMgr owns and explicitly clears scratch. Independent owner clears can desynchronize the single-guard allocation assumption in either direction. Rust has adjacent-zone opaque heat-transfer links and window-optical declarations but no Solar-enclosure exchange matrix/flags, bilateral transform, fixed simple-path expansion, kickoff reset, or matching failure/re-entry state. CP163 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 172 routines, split 58 state-mapped plus 114 source-mapped, with 65 required. CP164 adds non-required source-mapped `routine.init_ems_controlled_surface_properties` immediately after `compute_dif_sol_exc_zones_wiz_windows` and before `init_ems_controlled_constructions` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 119, implemented at `HeatBalanceSurfaceManager.cc` lines 4646-4720, and called only from `HeatBalanceManager.cc` line 2663 under `AnyEnergyManagementSystemInModel`; the same guard calls definition-next CP165 at line 2662 first, so CP165 is CP164's runtime predecessor, not successor. `ManageHeatBalance` calls the before-init EMS point before `InitHeatBalance`/CP164 and the after-init EMS point afterward, so CP164 observes actuator state as it stands after the before-init EMS point while after-init changes wait for a later entry. No unit test calls CP164 directly or references its three override-on fields. Each entered call first sets `dataGlobal->AnySurfPropOverridesInModel` false, then scans stored Material pointers until the first Regular material with any Solar, Thermal, or Visible override active; non-Regular flags cannot set the global, and no active Regular takes an immediate return. That last-off return does not restore prior working Material absorptances or Construction face copies, and the false global suppresses the normal non-BeginSim Surface refresh and override-caused enclosure recalculation absent another cause. With any active Regular override, a second full Material pass recomputes every Regular material in Solar, Thermal, Visible order: active channels use `max(min(value,0.9999),0.0001)` and inactive channels restore raw input, while non-Regular materials remain untouched. ObjexxFCL's ordered ternaries clamp negative infinity to 0.0001 and positive infinity or NaN to 0.9999; inactive input values are unguarded. The Construction pass skips Windows and exactly zero layers, then for every other layer count copies a nonzero last-layer endpoint into inside Visible, Solar, Thermal and a nonzero first-layer endpoint into outside Visible, Solar, Thermal. Window, zero-layer, and zero-endpoint faces retain old values; endpoint materials need not be Regular, and negative/oversized counts or indices, null pointers, and topology are unchecked. CP164 has no allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. A malformed detection pointer can fail after the initial global write but before any current-material target write; a second-pass pointer failure leaves only complete prior Regular-material triples, and invalid endpoint access fails before the current face's first Visible copy although an outside-face failure can follow a complete inside-face triple; active re-entry overwrites reached state, last-off or non-Regular-only re-entry preserves stale values, and a false outer EMS gate preserves everything without entry. Exactly three direct mutation owners are DataGlobal, MaterialData, and ConstructionData; their independent clears do not replay a coherent projection. Rust immutable material absorptances and run-blocked variable-absorptance declarations are not this EMS actuator/global-flag/mutable endpoint lifecycle. CP164 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 173 routines, split 58 state-mapped plus 115 source-mapped, with 65 required. CP165 adds non-required source-mapped `routine.init_ems_controlled_constructions` immediately after `init_ems_controlled_surface_properties` and before `update_intermediate_surface_heat_balance_results` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 121, implemented at `HeatBalanceSurfaceManager.cc` lines 4722-4940, and called only from `HeatBalanceManager.cc` line 2662 under `AnyEnergyManagementSystemInModel`, immediately before CP164 at line 2663. It observes actuator state as it stands after the before-init EMS point; after-init changes wait for a later `InitHeatBalance` entry. No unit test calls CP165 directly. Three `HeatBalanceManager` integration fixtures exercise it indirectly: the line-1373 Window fixture checks morning/afternoon Construction plus downstream transmittance/daylight after repeated manager calls, while the line-1974 Window and line-2181 CondFD fixtures check only final current Construction equal to the override value and the ON flag true. They do not cover no-ON last-off state, ON-nonpositive/OFF restoration, Window cache overwrite, cached-bad retention, first-versus-next-call asymmetry, CTF/EMPD warning or source-loss branches, CondFD node or source-loss branches, HAMT, Kiva, another algorithm, invalid targets, exact diagnostics, partial failure, or retry. Each entered call first sets `dataGlobal->AnyConstrOverridesInModel` false and scans only the Surface ON flags. Any ON flag, including one paired with a zero or negative integer value, sets it true and enters the full pass; zero ON flags take a stale quick return without restoring current/active Construction, `IsUsed`, or cache state. In the full pass, every OFF Surface and every ON Surface with a nonpositive integer value restores `Surface.Construction` and `SurfActiveConstruction` from `ConstructionStoredInputValue` but does not clear `IsUsed` or either compatibility cache. A positive value is used as an unchecked Construction/cache index. A Window target ignores Surface class and current heat-transfer algorithm, overwrites even a cached rejection to checked/okay before testing the pair, and fast-applies the target in the same call; an already checked/okay pair likewise writes current Construction, marks target `IsUsed`, and writes active Construction. A cached-bad pair does nothing and preserves whatever current/active Construction was already present. An unchecked CTF/EMPD pair premarks okay/checked, then orders a histories Warning plus three Continue calls, a terms Warning plus three Continue calls, and a one-way current-source/sink to target-without-source/sink Severe plus three Continue calls, for 12 possible diagnostics; only source loss rejects. CondFD similarly premarks, orders a node-mismatch Severe plus three Continue calls before an independent source-loss Severe plus three, and can emit eight; either mismatch rejects. HAMT and Kiva each emit Severe plus one Continue and cache rejection. Every other algorithm remains unchecked, silent, and unchanged, while Window, cached-good, cached-bad, and restore branches emit no CP165 diagnostic. A successful first unchecked CTF/EMPD or CondFD evaluation writes only `Surface.Construction`; only a later checked/okay call also marks `IsUsed` and updates `SurfActiveConstruction`. The historical cache is keyed only by target Construction and Surface, while compatibility reads the mutable current Construction, so target switching can validate against a prior override and later cache hits never revalidate changed current state. An invalid positive target fails at the initial `TypeIsWindow` dereference before premarking. CTF/EMPD warning interruption leaves a true cache and skips the remaining checks on retry, while interruption of its source-loss rejection can fast-accept a forbidden target. A failed CondFD node read or interrupted node diagnostic before the false write can likewise fast-accept, but after a complete node rejection has written false, a later source-loss diagnostic failure retries cached-bad; when node counts match, interruption of the sole source-loss rejection can fast-accept. Interrupted HAMT/Kiva diagnostics leave the pair available to diagnose again. CP165 performs no body allocation, status return, assertion, catch, rollback, cleanup, or index/topology validation, and cache/diagnostic/current/`IsUsed`/active failures can leave ordered prefixes. `AnyConstrOverridesInModel` independently disables the `SimpleCTFOnly` history fast path and forces Solar/radiant-enclosure recalculation even when the sole ON value is nonpositive. The four direct mutation owners are DataGlobal, SurfacesData, ConstructionData, and RuntimeLanguageData; diagnostic counters/streams/SQLite/callbacks are child side effects, and independent clears cannot replay a coherent current/active/cache/use projection. Rust typed Constructions, immutable surface references, positive construction-use evidence, and diagnostic CTF-coefficient overrides provide no EMS Construction actuator, mutable current/active selection, compatibility cache, `IsUsed` lifecycle, or matching diagnostics. CP165 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 174 routines, split 58 state-mapped plus 116 source-mapped, with 65 required. CP166 adds required source-mapped `routine.update_intermediate_surface_heat_balance_results` after `init_ems_controlled_constructions` and before CP167's `update_non_representative_surface_results` in the mapped Surface-manager ledger; its project-contract requirement is after `calculate_zone_mrt` and before `manage_air_heat_balance`, preserving the inside-parent/general-or-optimized-child/MRT/tail grouping. It is declared at `HeatBalanceSurfaceManager.hh` line 132, implemented at `HeatBalanceSurfaceManager.cc` lines 4951-5020, and called in production only from `CalcHeatBalanceInsideSurf` line 7812 immediately after `CalculateZoneMRT`; failure always blocks the wrapper return and, on the main Surface-manager path, downstream Air balance. Absent `ZoneToResimulate`, three Zone/Space passes use inclusive bounds 1 through `NumOfZones`; when present, both bounds become the unchecked supplied integer. The sole production partial caller normally dereferences that Zone's lists before CP166, while a direct present zero, negative, or greater-than-`NumOfZones` value reaches CP166's invalid Zone dereference; if an `INT_MAX` body completed, loop increment would overflow. With an absent nonpositive `NumOfZones`, CP166's three Zone loops and CP167's single Zone/Space traversal are empty but the global Kiva pass still runs. One direct unit fixture calls CP166 four times with the optional argument absent at lines 8694, 8702, 8710, and 8718 and asserts only `SurfQdotRadSolarInRepPerArea = SurfOpaqQRadSWInAbs - SurfQdotRadLightsInPerArea` for exact scenarios 0, 3, -0.1, and 0.00001 within tolerance; its Window and convective ranges are empty, representative mode is false, and the Kiva list is empty. The first pass visits each selected Zone's stored Space order and inclusive Window range, adding only `ExtSolar` `SurfWinHeatGain` values into the existing `ZoneWinHeatGain` without resetting it. After every Space, `>= 0` writes only gain rate then gain energy, while the negative branch writes only negated loss rate then loss energy; the opposite sign pair remains stale, zero and negative zero use gain, positive infinity uses gain, and negative infinity or NaN use loss. `InitSolarHeatGains` normally resets these five Zone arrays earlier, but direct or repeated CP166 entry accumulates Window gain again, and a multi-Space failure can leave reports lagging a newer partial sum. When `UseRepresentativeSurfaceCalculations` is true, CP166 then calls definition-next CP167 with the same optional argument; because this is after Window accumulation, the current call can sum pre-child nonrepresentative Window gain, while CP167 mutations feed later assignments and later calls. The second selected-Zone pass overwrites each inclusive opaque-or-Window range with `SurfQdotConvInPerArea = -SurfHConvInt * (SurfTempIn - RefAirTemp)`. The third overwrites each opaque-or-internal-mass range with the tested solar-minus-lights expression. Repeated Space/range membership overwrites the same targets again, while empty, inverted, or unreached ranges preserve prior per-Surface values. Finally, regardless of optional Zone bounds or `NumOfZones`, CP166 visits the global stored `AllHTKivaSurfaceList` and overwrites each `SurfOpaqInsFaceCondFlux` with the negated ordered sum of inside convection, net long-wave, HVAC radiation, internal-gain radiation, and absorbed short-wave terms; a nonselected Kiva Surface can therefore consume stale convection and still be mutated. All arithmetic is raw: there is no finite, sign, range, topology, duplicate, or overflow guard, so infinities and NaN follow IEEE operations and duplicate Window membership repeats additions. CP166 has no body allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. Failure can leave ordered prefixes of Window additions, per-Space sign-report rate/energy pairs, CP167 child effects, convective assignments, solar-minus-lights assignments, and global Kiva assignments; scalar expressions finish their reads before the current target write. Re-entry accumulates Window gains but overwrites the three per-Surface results from current inputs, preserving stale opposite-sign Zone reports. Exactly two direct owners mutate: HeatBalanceData owns the five Zone arrays and HeatBalSurfData owns convection, solar-minus-lights, and Kiva flux targets; both clear by placement-new. CP167 can indirectly mutate those plus placement-new-cleared SurfacesData and explicitly cleared HeatBalSurfMgr state. Independent clears cannot replay the projection. Rust has a separate bounded inside-convection report formula and typed surface state, but no CP166 Zone/Space/window-report orchestration, representative child, solar-minus-lights report state, global optional-zone-independent Kiva pass, or matching failure/re-entry lifecycle. CP166 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 175 routines, split 58 state-mapped plus 117 source-mapped, with 66 required. CP167 adds non-required source-mapped `routine.update_non_representative_surface_results` immediately after `update_intermediate_surface_heat_balance_results` and before `gather_for_predefined_report` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 134, implemented at `HeatBalanceSurfaceManager.cc` lines 5022-5174, and called only by CP166 line 4985 after CP166's additive Window pass and under `UseRepresentativeSurfaceCalculations`; CP167 itself does not test that flag, so a direct call bypasses it. No unit test calls CP167, and CP166's sole direct fixture explicitly disables representative calculations; a separate topology-only `DataSurfaces.unit.cc` fixture proves that a zero-area constituent can map to an area-20 representative but does not execute CP167. An absent optional Zone selects inclusive 1 through `NumOfZones` while a present raw integer selects one unchecked Zone; one outer traversal follows stored Zone/Space order and executes each reached Space's inclusive HT, opaque/internal-mass, then Window ranges before the next Space or Zone, without sorting, deduplication, class, membership, bounds, finite, or topology validation. Inverted ranges are empty, malformed 0-through-0 enters index zero, repeated ranges overwrite again, self representatives are skipped, and non-self chains copy one raw hop whose state may already have been projected earlier in traversal. The per-Space HT range performs at most 21 ordered writes: `SurfTempIn`, `SurfTempOut`, `SurfTempEffBulkAir`, `SurfHConvInt`, `SurfQdotRadNetLWInPerArea`, `SurfQdotRadIntGainsInPerArea`, `SurfQdotRadHVACInPerArea`, `SurfQdotConvOutPerArea`, `SurfHConvExt`, `SurfQdotRadOutRepPerArea`, `SurfHAirExt`, `SurfHSkyExt`, `SurfHGrdExt`, `SurfTAirRef`, conditional `SurfTAirRefRpt`, exterior-convection `hfModelEq` then `hnModelEq`, both additional heat-source values, `SurfTempInTmp`, and `RefAirTemp`. The report flag is recomputed from the copied enum only when it is not Invalid, leaving an Invalid target report stale and allowing another malformed enum to fail its unchecked table lookup; only the two named exterior-convection members are copied, and the `#if 0` diagnostic block is not built. The following per-Space opaque/internal-mass range copies `SurfQdotRadLightsInPerArea`, `SurfOpaqQRadSWOutAbs`, then `SurfOpaqQRadSWInAbs`. The final per-Space Window range computes target Area divided by the same target Area, so every finite nonzero value including a negative gives one while zero, infinity, or NaN gives NaN; glazing gains always use that ratio. Positive target frame/divider areas separately divide by unchecked representative areas and copy gain, loss, inside temperature, then outside-surface temperature; false branches preserve those four fields and contribute local zero. The combined frame/divider gain is always overwritten and whole gain becomes representative whole gain minus representative aggregate times the self-area ratio plus the new target aggregate, so two false branches preserve eight detailed fields but zero the aggregate. Raw nonfinite arithmetic, zero denominators, overflow, malformed chains, and repeat-call scaling are unguarded. CP167 has no body allocation, enabled diagnostic/assertion, status, catch, rollback, cleanup, or validation; failures can leave complete earlier-Space projections followed by ordered current-Space HT, opaque, glazing, frame/divider, aggregate, and whole-gain prefixes, while retry overwrites reached fields but preserves self, false-branch, empty, no-longer-reached, and Invalid-report stale state and can compound traversal-dependent chains. HeatBalSurfData, HeatBalanceData, and SurfacesData clear their direct mutations by placement-new, while HeatBalSurfMgr explicitly clears `RefAirTemp`; independent clears cannot replay the four-owner projection. Rust has bounded opaque `SurfaceHeatBalanceState` and typed `WindowFrameAndDivider` declarations but no representative flag/topology/ID projection, mutable matching Window gain/report lifecycle, or CP167 runtime/failure/re-entry behavior, and runtime Surface indexes leave fenestration empty. CP167 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 176 routines, split 58 state-mapped plus 118 source-mapped, with 66 required. CP168 adds required source-mapped `routine.calculate_zone_mrt` immediately after `update_thermal_histories` in the Surface-manager routine ledger; its project-contract requirement is after `calc_heat_balance_inside_surf_2_ctf_only` and before `update_intermediate_surface_heat_balance_results`. It is declared at `HeatBalanceSurfaceManager.hh` lines 140-141, implemented at `HeatBalanceSurfaceManager.cc` lines 5583-5699, and called in production only from `CalcHeatBalanceInsideSurf` line 7811 after the selected solve and immediately before CP166. One `SurfaceGeometry.unit.cc` fixture calls it three times with the optional Zone absent: all-five-Surface 10 C checks Zone/enclosure/Space MRT 10; Zone Surface temperatures 10/20/30 check Zone MRTs plus shared enclosure 16.667; and, after zeroing five Surface areas and manually rearming first-time state, Zone MAT fallbacks 20/25/28 and low-AE enclosure results 21.5/22 derived from Space MATs. The fixture does not assert Space Volume or which volume subbranch executes, and those expected outputs do not independently distinguish the strict volume-weighted branch from the arithmetic fallback; no test covers warnings, optional selection, cache staleness, exact thresholds, nonfinite/topology errors, failure, or retry. While `CalculateZoneMRTfirstTime` is true, CP168 allocates `SurfaceAE(TotSurfaces)` and `ZoneAESum(NumOfZones)`, zeros both plus every enclosure `sumAE`, then globally caches `Area * current Construction.InsideAbsorpThermal` only for numeric `HeatTransSurf` entries; positive-only Zone and radiant-enclosure index gates accumulate the same raw value without upper, finite, Construction, or topology validation. A partial first call still builds the global cache, which remains stale across later dimension/property/topology changes until HeatBalSurfMgr clear rearms the flag; only the successful routine tail clears it. A present raw Zone directly visits stored Spaces, zeroes each referenced enclosure `sumAET`, then sets `reCalcMRT` true; an absent argument sets every enclosure flag true without pre-zeroing. No source path writes those flags false, so a full call makes every later partial call recalculate all enclosures. Even a first partial call recomputes a marked shared enclosure from its complete `SurfacePtr` and writes every `spaceNums` MRT, including nonselected Zones. The selected-Zone loop uses strict cached `ZoneAESum > 0.01` and stored Space inclusive HT ranges: each `surfAET = SurfaceAE * SurfTempIn` enters the local numerator and unconditionally adds through the Surface's raw radiant-enclosure index, then `Zone.MRT = sumAET / ZoneAESum`. Exactly 0.01, negative, or NaN denominators take first-time Warning-plus-Continue then own-MAT fallback; later fallbacks are silent. Every true-flag enclosure uses strict `sumAE > 0.01`, resets `sumAET`, recomputes over complete `SurfacePtr`, and divides by `sumAE`, discarding prior Zone-loop additions. Low `sumAE` preserves `sumAET`, warns/continues only first-time, and computes stored-Space MAT from `sumMATVol/sumVol` when raw `sumVol > 0.01`, otherwise `sumMAT/(int)spaceNums.size()` with no empty or cast guard; either result is copied to every listed Space. Duplicate/raw lists and ranges repeat work, stale numerators can mismatch cached denominators, malformed 0-through-0 ranges enter index zero, and signs, exact thresholds, infinities, NaN, overflow, empty division, and invalid indices are unguarded. CP168 has no status, assertion, catch, rollback, cleanup, or local validation; allocations/cache writes, optional zero/flag pairs, Zone enclosure additions and MRTs, Warning/Continue effects, enclosure reset/add/MRT, and Space-copy prefixes can remain. A failed first-time retry rebuilds and rewarns but does not globally reset flags, `sumAET`, or prior MRT; later failures reuse stale caches and suppress fallback warnings. HeatBalSurfMgr explicitly owns/clears the two caches and flag, ViewFactorInfoData explicitly owns/clears enclosure sums/MRT/flags, and ZoneTempPredictorCorrectorData placement-new clears Zone/Space MRT; independent clears cannot replay the projection. Rust has bounded Surface area, inside thermal absorptance/temperature, and separate radiant helpers, but no Zone/Space/enclosure MRT state, cached weighting topology, recalculation flags, MAT fallback/diagnostic lifecycle, or CP168 analog. CP168 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 177 routines, split 58 state-mapped plus 119 source-mapped, with 67 required. CP169 adds non-required source-mapped `routine.report_non_representative_surface_results` immediately after `report_surface_heat_balance` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 151, implemented at `HeatBalanceSurfaceManager.cc` lines 6893-6930, and called in production only by `ReportSurfaceHeatBalance` line 6618 under `UseRepresentativeSurfaceCalculations`, immediately after `SolarShading::ReportSurfaceShading`; CP169 itself has no guard, so a direct call bypasses representative-mode selection. No unit test calls CP169 directly, and the seven direct unit calls to its parent leave representative mode false; the separate representative-topology fixture does not execute reporting. CP169 scans numeric Zones, stored Spaces, and each inclusive HT range, and every non-self target copies the representative's inside then outside `convClassRpt` integer without validation. After each Space's HT pass, `DisplayAdvancedReportVariables` independently gates its inclusive Window range. Every non-self Window computes `Surface(surfNum).Area / Surface(surfNum).Area`, so finite nonzero target areas including negative values yield one while zero, infinity, or NaN yields NaN; it then copies representative convective glazing gain, infrared glazing gain, and shortwave loss in that order through the raw ratio. It does not copy energies, frame/shade/gap terms, or whole Window heat gain and does not recompute any dependent report, so a false advanced gate and self, empty, inverted, or unreached ranges preserve prior values. This later report pass can overwrite CP167's earlier two glazing-component projections and adds the shortwave-loss projection only when advanced reporting is enabled. Raw Zone/Space/range/representative indices, class and membership, duplicates, chains, finiteness, and loop overflow are unchecked; 0-through-0 ranges enter index zero, duplicate or chained targets can consume earlier projections in traversal order, and all arithmetic follows IEEE behavior. CP169 has no allocation, child call, diagnostic, status, assertion, catch, rollback, cleanup, or local validation. Failure can leave complete earlier Spaces and earlier Surfaces within the current Space plus an inside-class-only HT prefix or one/two Window-component writes; coherent retry overwrites reached fields from current representative state but preserves skipped stale state and remains traversal-dependent for chains. SurfacesData alone owns all five targets and clears them by placement-new; Rust has no representative topology, convection-class report state, matching Window component lifecycle, advanced-report gate, or CP169 analog. CP169 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 178 routines, split 58 state-mapped plus 120 source-mapped, with 67 required. CP170 adds non-required source-mapped `routine.report_int_mov_ins_inside_surf_temp` immediately after `report_non_representative_surface_results` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 153, implemented at `HeatBalanceSurfaceManager.cc` lines 6932-6940, and called in production only by `ReportSurfaceHeatBalance` line 6741 under `AnyMovableInsulation`, after the parent Window reports and before opaque reporting. CP170 itself never reads that flag, so direct calls bypass the guard, while exterior-only movable insulation can make the parent call it even with an empty interior list. One direct unit fixture allocates two entries and calls CP170 once: a listed `present = false` Surface changes its prior report from 1.23 to the base `SurfTempIn` 23.0, and a listed `present = true` Surface changes 1.23 to `SurfTempInTmp` 12.3, both within 0.00001; it does not cover the parent gate, empty or malformed lists, duplicates, dimensions, nonfinite values, failure, or retry. Every entered call first assigns the complete `SurfTempIn` array to `SurfTempInMovInsRep`, then visits `intMovInsulSurfNums` in stored order and overwrites only raw indices whose `intMovInsuls(SurfNum).present` is true with `SurfTempInTmp(SurfNum)`. An empty list therefore leaves the full base copy; unlisted and listed-false entries keep the base value, and duplicates re-read current presence/input and repeat the same indexed overwrite. There is no arithmetic, clamp, finite filter, list repair, deduplication, bounds or dimension validation, child call, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. The first ObjexxFCL Array1D assignment is allocation-capable: a nonconformable target receives the source index range and is resized, which can allocate, deallocate, or reconstruct storage before element copy; a conformable target keeps its shape and overwrites all elements. Resize/allocation/range or copy failure reaches no list work and has no CP170 rollback guarantee; after a successful full copy, a bad raw index or input access can leave the complete base copy plus every earlier successful present override. A retry repeats the shape-conforming or shape-rebuilding assignment from current `SurfTempIn` and then reevaluates the current list, presence flags, and temporary temperatures, so a successful retry erases stale prior overrides for removed or now-false entries while remaining allocation- and raw-index-failure-prone. HeatBalSurfData alone owns the direct target and clears it by placement-new; normal setup dimensions it to `TotSurfaces` and BeginEnvironment zeroes reached entries, while SurfacesData separately owns/defaults/clears the gate, list, and insulation records. Rust has no movable-insulation topology/state, interior movable-insulation temperature report, conditional override pass, or matching failure/re-entry lifecycle. CP170 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 179 routines, split 58 state-mapped plus 121 source-mapped, with 67 required. CP171 remains immediately after `calc_heat_balance_outside_surf` in the Surface-manager definition ledger; CP175 now interposes `calc_outside_surf_temp` before CP171 in the heat-balance project execution list. It is declared at `HeatBalanceSurfaceManager.hh` line 173, implemented at `HeatBalanceSurfaceManager.cc` lines 7723-7736, and called in production only on the right-hand side of the `CalcHeatBalanceOutsideSurf` line-7717 common-tail assignment. The parent reaches that tail through Zone order, stored Space order, and inclusive HT ranges, but skips every Window, optional-zone-unrelated Surface, and line-7440 ExternalEnvironment EcoRoof; every other surviving boundary path reaches CP171. The helper binds raw `Surface(SurfNum)` and copies `OSCMPtr`; a strict positive pointer returns `-OSCM.HConv * (SurfOutsideTempHist(1) - OSCM.TConv)` and wins even when rain is true, ignoring `SurfHConvExt` and both weather temperatures. Zero or negative pointers fall through: rain uses `-SurfHConvExt * (SurfOutsideTempHist(1) - SurfOutWetBulbTemp)` and no rain uses the same expression with `SurfOutDryBulbTemp`. CP171 repeats no Area, class, boundary, wind, or parent-skip gate. One direct fixture makes exactly two default-zero-pointer calls: with surface history 6.71793958923051, wet bulb 6.66143784594778, coefficient 1000, and rain true it expects the wet formula within 0.01; after rain becomes false, coefficient becomes 5.65361106051348, and dry bulb is 7.2 it expects the dry formula within 0.01. The fixture's Area 58.197 is irrelevant. No test covers a positive OSCM pointer or its rain precedence, negative or invalid pointers, caller skips/store, boundary or wind independence, malformed indices/dimensions, nonfinite inputs, failure, or retry. The body is read-only and allocation-free, with no finite, sign, range, index, pointer, dimension, or topology validation, clamp, normalization, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. Raw negation, subtraction, multiplication, signed zero, infinities, NaN, overflow, and invalid-operation results follow underlying IEEE behavior, and a positive invalid pointer is indexed unchecked. Because the helper must return before the caller assignment, helper failure leaves the current `SurfQdotConvOutPerArea` old while prior parent effects for the current Surface and successful earlier-Surface writes persist. After a successful scalar return, an invalid or unallocated caller target access can still fail on the left-hand side and likewise leave the current target stale; a successful nonfinite result is stored only when that write succeeds. Direct retry rereads current inputs, while a full parent retry also repeats boundary work and overwrite-shaped storage without repairing skipped stale targets. HeatBalSurfData owns, dimensions, initializes, reports, and placement-new-clears the caller target; CP167 can later copy it to nonrepresentative Surfaces, WindowManager separately writes it for Windows, and reporting plus PlantPipingSystems consume the shared field. Rust's bounded `surface_exterior_report_terms_from_balance` has the same final `-coefficient * (outside_face_temperature - reference_temperature)` algebra and per-area report slot, with upstream typed wet-weather context, but no generic raw OSCM pointer and strict precedence, exact history-term lookup, full parent traversal and boundary topology, common-tail skips/store, or matching failure/re-entry lifecycle. CP171 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 180 routines, split 58 state-mapped plus 122 source-mapped, with 68 required. CP172 adds required source-mapped `routine.calc_heat_balance_inside_surf_2` between `calc_heat_balance_inside_surf` and the distinct optimized `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger; CP173 now follows the CTF-only child in that definition ledger and interposes `sum_surf_qdot_rad_hvac` between the wrapper and CP172 in the project execution list. It is declared at `HeatBalanceSurfaceManager.hh` lines 179-184, implemented at `HeatBalanceSurfaceManager.cc` lines 7815-8656, and called in production only by the canonical wrapper lines 7797 and 7809. An absent optional Zone reaches CP172 only when cached `AllCTF` is false and supplies the four global Surface lists; a present Zone always reaches CP172 with the four raw Zone lists regardless of `AllCTF`. Those vectors independently govern reference-air/history, interzone, non-window, and Window work without topology validation, while the optional Zone is forwarded only to interior-radiation and convection-coefficient children. The prologue skips TDD domes only in reference-air setup, zeros exactly 13 selected fields for every supplied Window, resets the global iteration count, samples the global scheduled inside-source list, and conditionally advances all Kiva instances. Each at-least-one convergence pass takes a whole-array inside-temperature snapshot, temporarily substitutes every Kiva map entry with `Trad - Kelvin` around interior radiation, restores from the snapshot, refreshes convection at pre-increment counts 30 through 480 on a maximum-length run, executes EMPD/HAMT setup and the representative-filtered non-window algorithm branches, runs TDD diffusers every pass but regular Windows only on pass one, commits histories/TDD/interzone updates, and increments the count. The post-increment `> 500` check therefore warns outside warmup and breaks after pass 501 even if that pass first met convergence; global `AnyCondFD` controls tolerance and relaxation. The EMPD/HAMT tail globally zeros all Zone moisture sums and repopulates only the supplied non-window subset. All five literal unit calls to the wrapper omit Zone and retain cached `AllCTF = true`, including the fixture that later changes one Surface algorithm to Kiva, so they dispatch to the CTF-only child; CP172 has no direct or known exercised unit path. Raw vectors, duplicates, indices, algorithms, dimensions, arithmetic, Kiva map insertion, child failures, and diagnostics are unchecked locally; there is no status, catch, rollback, or cleanup. Failure can leave selected-window resets, global snapshots and sources, Kiva substitutions or partial restores, solve/history/coefficient prefixes, diagnostics, relaxation, and globally zeroed or partly rebuilt moisture sums, while retry resets only the early selected state and iteration count and otherwise inherits the partial graph. HeatBalSurfMgr, HeatBalanceData, SurfacesData, HeatBalSurfData, HeatBalFanSysData, MoistureBalanceData, ZoneTempPredictorCorrectorData, SurfaceGeometryData, DataTimingsData, and the called algorithm managers separately own this state; independent clears cannot replay the routine. Rust has bounded inside-pass scaffolds and a distinct CTF-only mapping but no CP172 general solver, raw four-list/partial topology, non-CTF/window/Kiva/moisture lifecycle, 501-pass behavior, or matching failure/re-entry semantics. CP172 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 181 routines, split 58 state-mapped plus 123 source-mapped, with 69 required. CP173 adds required source-mapped `routine.sum_surf_qdot_rad_hvac` after `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger and after `calc_heat_balance_inside_surf` before `calc_heat_balance_inside_surf_2` in the heat-balance project contract. It is declared at `HeatBalanceSurfaceManager.hh` line 171, implemented at `HeatBalanceSurfaceManager.cc` lines 9277-9285, and called in production only by the canonical wrapper line 7788 before full, optimized, or partial dispatch. Every call, including optional-Zone resimulation, traverses the complete raw `allGetsRadiantHeatSurfaceList`. Five equipment input paths append nonzero Surface pointers without sorting or deduplication. For each stored index, CP173 binds `surfQRadFromHVAC` and overwrites `SurfQdotRadHVACInPerArea` with the exact left-associated `HTRadSys + HWBaseboard + SteamBaseboard + ElecBaseboard + CoolingPanel` sum. An empty list and unlisted targets preserve stale values; duplicates repeat the same overwrite rather than accumulating again. All five literal wrapper calls in `HeatBalanceSurfaceManager.unit.cc` leave the raw list default-empty and exercise only CP173's no-op. Four `SizingManager.unit.cc` `ManageSimulation` fixtures beginning at lines 2465, 2929, 3393, and 3818 include electric radiant baseboard input, populate the list, and indirectly execute CP173, but assert only downstream sizing; no test calls CP173 directly, observes the aggregate, or isolates arithmetic, order, duplicates, failure, or lifecycle. The routine has no validation, diagnostic, allocation, catch, rollback, cleanup, or latch; raw indices and IEEE addition are unchecked. Source binding precedes the assignment, whose complete right-hand side precedes target lookup and store, so failure can retain an earlier-list write prefix and leave the current target old; retry rereads the current list and component record without clearing skipped state. SurfacesData owns the raw list, HeatBalFanSysData owns the component records, and HeatBalSurfData owns the aggregate; their independent placement-new clears and initialization/distribution resets do not form a CP173 transaction. Rust has a bounded `inside_radiant_hvac_w_per_m2` destination consumed by its surface balance and reports, but no five-source producer, global list, or matching lifecycle. CP173 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 182 routines, split 58 `state_mapped` plus 124 `source_mapped`, with 70 required. CP175 adds required source-mapped `routine.calc_outside_surf_temp` after `test_surf_temp_calc_heat_balance_inside_surf` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, and after `calc_heat_balance_outside_surf` before `get_qdot_conv_out_per_area` in the heat-balance project contract. `CalcOutsideSurfTemp` is declared at `HeatBalanceSurfaceManager.hh` lines 195-202 and implemented at `HeatBalanceSurfaceManager.cc` lines 9470-9763. Its only direct production calls are the parent’s `OtherSideCoefCalcExt` CTF/EMPD path at line 7355, `OtherSideCondModeledExt` CTF/EMPD path at line 7419 after any exterior-vented-cavity update, and `ExternalEnvironment` CTF/EMPD-or-TDD path at line 7626; the first two retain zero movable insulation, while the exterior path alone samples exterior-insulation state. The reusable parent supplies complete or optional-Zone traversal, skips Windows, unrelated Surfaces, EcoRoof, and ineligible boundary/algorithm routes, then fatals immediately when CP175 sets its shared flag, before the CP171 common-tail convection store. CP175 uses strict `HMovInsul > 0.0` and `CTFCross[0] > 0.01` decisions and computes quick coupling as `CTFCross[0] / (CTFInside[0] + SurfHConvInt)`. Sky and ground start from global sky and outdoor dry-bulb temperatures; optional surrounding-property schedules override them, the Surface supplies its precomputed surrounding temperature, and the ground-surface property then wins over any scheduled ground value. A TDD dome resolves raw pipe, diffuser, and diffuser-Space topology, uses `Ueff = 1 / Reff`, and couples dome exterior forcing to diffuser shortwave, radiant, convection, and MAT terms without embedded-source terms. The remaining exhaustive paths separate no-movable slow/quick and movable slow/quick conduction. With no movable insulation, `OSCMPtr == 0` uses exterior shortwave, convection, air/sky/ground/surrounding radiation, additional source, and optional outside embedded-source forcing; quick conduction substitutes the coupled inside balance and denominator correction, but its no-OSCM source branch notably omits the inside embedded-source term. Any nonzero OSCM pointer, including a malformed negative value, indexes raw `TRad`/`HRad`, drops the separated solar/air/sky/ground/surrounding forcing, and its quick source branch includes both outside and coupled inside embedded-source terms. Movable paths ignore OSCM radiation, use `F2 = HMovInsul / (HMovInsul + HConvExt + HAirExt + HSkyExt + HGrdExt + HSrdSurfExt)`, couple exterior forcing through F2, and include no embedded-source terms because that combination is rejected later. After writing `SurfOutsideTempHist(1)`, CP175 overwrites `SurfQdotRadOutRepPerArea` with negative sky, air-radiation, ground, and surrounding exchanges while excluding exterior convection and the parent’s nonlinear surrounding flux. With an embedded source plus movable insulation, those two writes precede four diagnostics, raw insulation-material lookup, `ErrorFlag = true`, and return; radiant-system coefficients remain stale and a successful call never clears a preexisting flag. With an embedded source and no movable insulation, constant, inside-temperature, and source coefficients are written in order; the divisor preserves the source anomaly that `SurfHSrdSurfExt` appears twice, and the constant coefficient omits the additional outside source. A non-source call does not clear old coefficients. The one direct fixture covers only a non-OSCM Wall with positive insulation, zero-cross slow conduction, and invalid embedded source; it checks the flag and four diagnostics but not independent temperature or radiation numerics. Four direct CTF parent fixtures incidentally reach CP175 but assert only parent convection, surrounding/ground, or scheduled-source state, while a fifth CondFD fixture does not call it. No focused test covers valid slow/quick equations, exact 0/0.01 thresholds, OSCM, TDD, temperature precedence, coefficient anomalies, an input-true flag, malformed topology, nonfinite arithmetic, failure prefixes, or retry. There is no finite, denominator, index, dimension, topology, schedule, property, TDD, Space, or material validation, catch, rollback, cleanup, or local latch. Threshold equality and NaN select absent/slow, raw floating-point exceptional results propagate, and failure can preserve no write, only outside history, history plus radiation report, a diagnostic prefix with the flag still false, or a one/two-coefficient prefix. Direct retry rereads live inputs but does not clear a caller-supplied flag or untouched coefficients; a fresh parent call reinitializes its local flag but repeats earlier parent work. HeatBalSurfData owns the temperature history and radiant report, HeatBalFanSysData owns the three coefficients, the parent stack owns the shared flag, and the Surface, Construction, environment, schedule, daylighting, Space/MAT, material, and error owners reset independently rather than transactionally. Rust has bounded slow/quick typed Outdoors CTF and report helpers, but epsilon fallbacks, clamps, shortcuts, and a collapsed environment do not implement CP175’s TDD, OSCM, movable insulation, property precedence, embedded-source coefficient/error lifecycle, exact reports, raw topology, or failure/re-entry behavior, so no Rust target, support, numerical, or conformance promotion follows. The inventory becomes 32 algorithms and 184 routines, split 58 `state_mapped` plus 126 `source_mapped`, with 72 required. CP176 adds non-required source-mapped `routine.calc_exterior_vented_cavity` immediately after `calc_outside_surf_temp` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `CalcExteriorVentedCavity` is declared at `HeatBalanceSurfaceManager.hh` line 204 and implemented at `HeatBalanceSurfaceManager.cc` lines 9765-9850. Its only direct production calls are the `CalcHeatBalanceOutsideSurf` `OtherSideCondModeledExt` CTF/EMPD path at line 7416 and CondFD/HAMT path at line 7427, both guarded by the trigger Surface's `SurfExtCavityPresent`. Parent Window and optional-Zone skips, every other boundary, and every other heat-transfer algorithm cannot trigger CP176, but a reached cavity forwards its complete raw `SurfPtrs` group without Window or Zone filtering. Before either call, the parent applies EMS overrides to the trigger Surface's OSCM and snapshots its existing `TConv/HConv` into local `TempExt`, `SurfHConvExt`, and any CondFD/HAMT moisture boundary. CP176 writes the cavity's separate raw `OSCMPtr`, which neither the wrapper nor input loader checks against each Surface's `OSCMPtr`. The CTF/EMPD path then calls CP175 with the pre-CP176 convective snapshot but, when both pointers match, CP176's new live `TRad/HRad`; common-tail CP171 likewise reads the new `TConv/HConv` only when those pointers match. CP176 obtains `CavNum` from the trigger Surface, computes trigger humidity ratio and density from that Surface's dry/wet bulb and global pressure, forms `holeArea = ActualArea * Porosity` and `AspRat = 2 * HdeltaNPL / PlenGapThick`, and seeds temporary baffle and cavity-air temperatures from `TbaffleLast/TairLast`. It then calls deferred dependency `TranspiredCollector::CalcPassiveExteriorBaffleGap` exactly three times with no convergence check, carrying each pass's two updated temperatures into the next. The child area-weights weather, wind, current outside history, incident solar, and heat-transfer coefficients over the whole raw group, invokes exterior-convection setup for every member, applies rain and optional ICS branches, and returns the final gap coefficients, incident term, total mass flow, and wind/buoyancy volume flows. After all three passes return, CP176 stores `Isc`, `TAirCav`, `Tbaffle`, `HrPlen`, `HcPlen`, `PassiveACH = (MdotVent / rho_trigger) / (ProjArea * PlenGapThick) * 3600`, total/wind/buoyancy passive mass reports, both last-temperature seeds, then OSCM `TConv/HConv/TRad/HRad` in that order; it never writes the separate cavity `MdotVent` member. The child total mass flow uses group-weather density, while ACH and the two component mass reports use trigger-Surface density, so component sums and the total can diverge when member weather differs. There is no cavity-number deduplication: each eligible member trigger recomputes the complete group through three more passes and immediately advances shared last temperatures, making full and optional-Zone traversal order visible and a successful direct retry non-idempotent. Input accepts duplicate Surface references within and across cavity objects: repeated raw entries are weighted repeatedly, a later cavity overwrites a Surface's trigger cavity number while an earlier raw group retains it, and the three-times-N call count uses distinct traversed trigger Surfaces rather than duplicate list entries. No test calls CP176 or enables its parent gate. One `ICSCollector.unit.cc` fixture calls the shared child only once and checks baffle temperature plus Hc, Hr, and total mass flow, not the wrapper loop, air temperature, report/history/OSCM stores, density split, multi-member order, or retry. CP176 has no local index, dimension, topology, pointer-consistency, finite, sign, range, or denominator validation, diagnostic, status, convergence test, catch, rollback, cleanup, cache, or latch. Psychrometric and convection dependencies may diagnose or mutate their own state; a failure before or within the three child passes leaves wrapper-owned cavity results, both seeds, and OSCM fields old but can retain dependency effects. After all three child calls return, straight-line non-trapping scalar work commits every cavity/report field and both seeds before the first new raw-index risk at the initial OSCM access; an invalid pointer leaves all four OSCM fields old, while a valid stable index lets all four stores complete. Retry therefore starts from either both old or both newly committed seeds, never a CP176-created split pair. `HeatBalanceData` owns the cavity topology, inputs, results, reports, and seeds; its constructor starts the two seeds at 20 C and explicitly initializes the listed result/report/source scalars at zero, but omits the separate never-written `MdotVent` member and leaves it indeterminate. BeginEnvironment initialization resets only the two seeds to 20 C. `SurfacesData` separately owns membership, OSCM/EMS state, and member weather/geometry; BeginEnvironment resets each OSCM to 20 C and 4 W/m2-K but leaves cavity results and PV-fed `QdotSource` stale until recomputation. General-routines ICS state, solar collectors, PV, construction/material, convection, environment, outside histories, and incident solar have independent owners and clears. Rust collapses `OtherSideConditionsModel` to `OutsideBoundaryCondition::Other`, excludes it from the exterior list, and preserves the existing outside-face temperature; it has no cavity object/group, OSCM state, passive-gap/Nusselt/flow solve, three-pass seeds, ICS/PV coupling, six registered outputs, or matching failure/re-entry lifecycle. CP176 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 185 routines, split 58 `state_mapped` plus 127 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP177 adds non-required source-mapped `routine.gather_component_loads_surf_abs_fact` immediately after `calc_exterior_vented_cavity` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GatherComponentLoadsSurfAbsFact` is declared at `HeatBalanceSurfaceManager.hh` line 206 and implemented at `HeatBalanceSurfaceManager.cc` lines 9852-9880. Its only direct production call is `InitSurfaceHeatBalance` line 614 under the caller's `ZoneSizingCalc` gate; the body additionally requires `CompLoadReportIsReq && !isPulseZoneSizing` and checks neither `WarmupFlag`, `KickOffSizing`, `doLoadComponentPulseNow`, nor the parent first-time flag. That sole call can be reached through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; if the effective gates remain true, either path overwrites the same current slot and the last successful call wins. On an ordinary component-report sizing path, the preceding `ManageInternalHeatGains(state, false)` call lazily allocates zero-initialized `surfCompLoads` storage for `(TotDesDays + TotRunDesPersDays) * (24 * TimeStepsInHour) * TotSurfaces` records. CP177 computes `TimeStepInDay = (HourOfDay - 1) * TimeStepsInHour + TimeStep`, selects zero-based day and timestep slots from one-based `CurOverallSimDay` and `TimeStepInDay`, then traverses numeric Surfaces 1 through `TotSurfaces`. It skips exactly when `!HeatTransSurf` or `Zone == 0`, then skips `TDD_Dome`; a malformed negative Zone passes, and there is no other class, Space, representative, list, or deduplication filter, so eligible Windows and TDD diffusers are included. For every reached Surface it first overwrites `ITABSFseq` with `SurfAbsThermalInt`, then overwrites `TMULTseq` with `EnclRadInfo(RadEnclIndex).radThermAbsMult`. It copies the two live factors separately without multiplying, clamping, normalizing, recomputing, or clearing omitted records; a false gate or newly skipped Surface preserves the prior or default slot. Because there is no kickoff or warmup guard, ordinary `SetupZoneSizing` can write timestep 1 twice and the final timestep once per accepted environment before the sizing iterations, the pulse iteration writes neither field, and nonpulse warmup and normal calls overwrite whatever slots they reach. An unchanged successful retry is overwrite-idempotent. `OutputReportTabular::GetDelaySequences` is the only source reader of these fields and later forms `TMULTseq * ITABSFseq * Surface.Area * decayCurve`; the snapshot feeds Zone, Space, AirLoop, and Facility component-load tabular reporting but never feeds the heat-balance solver. No test calls CP177 directly. The upstream CP161 test checks only absorptance 0.2 and multiplier 5, the focused delay-sequence test manually seeds both fields, and full sizing/report fixtures exercise the chain without asserting the CP177 pair, gates, filters, or write order. CP177 performs no allocation, finite, sign, range, index, dimension, topology, enclosure-membership, or time-coordinate validation and has no diagnostic, status, assertion, catch, rollback, cleanup, or local latch. Invalid day, timestep, destination-Surface, or enclosure indexing is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build, a diagnosed day or timestep lookup failure precedes every Surface store; after traversal begins an earlier-Surface prefix may remain, and an invalid `RadEnclIndex` can be diagnosed after `ITABSFseq` commits but before `TMULTseq`, leaving a split new/old pair. A successful retry overwrites only currently reached eligible records and does not repair skipped stale cells. `OutputReportTabularData` owns the destination and clears it plus rearms its allocation latch on `clear_state`; normal post-sizing deallocation removes only pulse arrays and retains `surfCompLoads` for report consumption. `HeatBalSurfData`, `ViewFactorInfoData`, `SurfacesData`, `SizingData`, and global timing/gate state own the inputs independently, so their resets do not form a CP177 transaction. In a bounds-checked execution where an indexing failure propagates, the parent does not reach the following initialization-complete display or first-time-flag clear, and prior parent effects plus any committed CP177 prefix remain observable if control recovers; release-build undefined behavior provides no corresponding guarantee. Rust's retained inside thermal absorptance and transient typed-Zone radiant-gain normalization are adjacent primitives only. Rust has no sizing/design-day component-load lifecycle, radiant-enclosure multiplier state, day/timestep/Surface ITABSF/TMULT storage, delay-report consumer, or matching filtering, failure-prefix, reset, and re-entry behavior. CP177 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 186 routines, split 58 `state_mapped` plus 128 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP178 adds non-required source-mapped `routine.get_surf_incident_solar_multiplier` immediately after `gather_component_loads_surf_abs_fact` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurfIncidentSolarMultiplier` is declared at `HeatBalanceSurfaceManager.hh` line 208 and implemented at `HeatBalanceSurfaceManager.cc` lines 9882-9890. Its only direct production call is the numeric all-Surface loop at `InitSolarHeatGains` line 2820, reached only when `currSolRadPositive` is true: `SunIsUp` and the signed `BeamSolarRad + GndSolarRad + DifSolarRad` sum is greater than zero. The unconditional line-457 parent call can arrive through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; no first-time, warmup, kickoff, sizing, pulse, class, heat-transfer, exterior-solar, representative, or deduplication filter narrows CP178 after the positive-solar branch is entered. The `resetSolar` condition alone does not call it: sunset and sun-up/no-radiation paths skip CP178, and BeginEnvironment skips it only when the independent `currSolRadPositive` gate is false; a positive-solar BeginEnvironment call refreshes every Surface cache, while skipped calls preserve prior caches. For each numeric Surface, CP178 first reads same-index `Surface.hasIncSolMultiplier`. False returns exact 1.0 without touching the optional record vector. True with a nonnull same-index schedule returns `sched->getCurrentVal() * Scaler`, where the schedule getter selects `EMSVal` when actuated and `currentVal` otherwise; true with a null schedule returns `Scaler`. CP178 does not clamp, normalize, validate, cache, diagnose, or consult the record `Name`, `SurfaceIdx`, total-object count, prior cached value, Surface class, boundary, exposure, construction, shading, or any typed list. The caller stores a completed return into same-index `Surface.IncSolMultiplier` before downstream sky/ground, beam/diffuse, obstruction and reflected-solar contributions, TDD, shelf, opaque/window, frame/divider, daylighting, interior-distribution, solver, and report consumers. No test calls CP178 directly. The focused Surface-manager fixture manually sets `hasIncSolMultiplier`, a null schedule, and scalers 0.5 then 1.0, calls the parent twice under positive solar, and proves only an exact half-versus-whole transmitted-solar result; input tests inspect scaler and schedule state but never assert the scheduled product, default branch, EMS precedence, solar gate, failure prefix, or retry. CP178 has no finite, range, index, dimension, alignment, pointer, or lifecycle validation and no diagnostic, status, allocation, catch, rollback, cleanup, or latch. Invalid vector or pointer access is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build the helper itself has no writes; if a lookup failure propagates during the caller loop, only earlier Surface cache stores can remain while the current and later caches stay old. IEEE nonfinite results are returned and stored rather than diagnosed. A successful positive-solar retry re-reads live flags, scaler, and schedule or EMS state and overwrites every numeric Surface; a retry whose solar gate is false preserves any prior cache. `SurfacesData` owns the flag, same-index record vector, and cached multiplier; the Surface constructor defaults the flag and cache to false and 1.0, record defaults are scaler 1.0 and null schedule, placement-new `clear_state` replaces the visible owner with empty Surface and record vectors, and subsequently allocated entries receive those defaults, while BeginEnvironment does not reset an existing cache. `ScheduleManagerData` separately owns and deletes schedule objects, while `EnvironmentData` owns the solar gate and CP159 mutates preceding state across several owners, so independent clears and a failed parent are not a CP178 transaction. Rust retains only the CP99 immutable request snapshot with an unresolved fenestration target, bounded scalar, and optional `ScheduleId`; every definition remains run-blocked. Rust has no fenestration binding, per-Surface flag or cache, same-index record alignment, request-specific current or EMS schedule sampling, positive-solar traversal, or matching window, TDD, shelf, reflection, distribution, solver, and reporting consumers. Its request-free opaque incident-solar diagnostic and generic schedule infrastructure do not implement CP178. CP178 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 187 routines, split 58 `state_mapped` plus 129 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP179 adds non-required source-mapped `routine.init_surface_property_view_factors` immediately after `get_surf_incident_solar_multiplier` and before `update_final_surface_heat_balance`, with no project-contract entry. `InitSurfacePropertyViewFactors` is declared at `HeatBalanceSurfaceManager.hh` line 210, implemented at `HeatBalanceSurfaceManager.cc` lines 9893-9993, and called only by `InitHeatBalance` line 2623 inside its `BeginSimFlag` block after array allocation and optional CTF initialization but before Window, daylighting, and solar initialization. The body quick-returns unless `AnyLocalEnvironmentsInModel` and the separately owned `InitSurfaceHeatBalancefirstTime` latch are both true, then visits every numeric Surface whose surrounding or ground property flag is set. It sums a strictly positive stored sky value, every enumerated surrounding factor, and the selected ground value, with a separate GroundSurfaces object taking precedence over a SurroundingSurfaces ground field; a total above one emits severe/continue diagnostics but does not stop, clamp, or skip mutation. Both-set, sky-only, ground-only, and neither-set branches overwrite or scale Surface sky/ground IR factors, backfill property values and presence flags, and conditionally invoke the separately CP182-mapped `ReSetGroundSurfacesViewFactor` child, which replaces the ground-object sum and either disables zero-factor ground use or writes the whole inferred factor into its first entry. Shared property records make numeric Surface alias order observable, and a sky-only SurroundingSurfaces path without a separate ground object can alternate its inferred ground value across direct retries because the value is backfilled without setting its presence flag. Focused fixtures cover the four ordinary branches and selected long-wave consumers but not both quick returns, production latch order, shared aliases, sum-above-one continuation, malformed topology, failure prefixes, or retry. There is no local finite/index/topology validation, status, catch, rollback, cleanup, transaction, or latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface prefix and leave the later first-time clear unreached. Rust ordinary ground/sky complement helpers and run-blocked Zone local-environment records do not implement the Surface local-environment property arenas, mutable flags/factors, diagnostics, reset child, alias order, or lifecycle. CP179 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 188 routines, split 58 `state_mapped` plus 130 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP180 adds non-required source-mapped `routine.get_ground_surfaces_temperature_average` immediately after `init_surface_property_view_factors` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 212 and implemented at `HeatBalanceSurfaceManager.cc` lines 9995-10035. Its only direct production call is the unconditional first child of `CalcHeatBalanceOutsideSurf` at line 7016, before the separately CP183-mapped surrounding-temperature child, internal-source transfer, optional-Zone filter, and every outside-Surface calculation. All full-model and zone-limited parent invocations, including normal Surface management, final-radiant recalculation, SimulationManager resimulation, and radiant/HVAC retries, therefore enter CP180 before the optional-Zone filter; when `AnyLocalEnvironmentsInModel` passes, CP180 traverses the complete numeric Surface array. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state. Otherwise CP180 filters only `IsSurfPropertyGndSurfacesDefined`, follows the Surface property index, and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfTemp` while preserving the prior shared average. For a nonzero sum it skips exact-zero view factors and null temperature schedules; every other entry samples the live schedule with EMS precedence and contributes `view_factor * (temperature + Kelvin)^4`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsTempAvg` with zero and then disables only the current Surface use flag; otherwise CP180 stores `root_4(numerator / SurfsViewFactorSum) - Kelvin` and never enables a false use flag. Shared-property aliases on a nonzero-sum path repeat the schedule reads and shared average store in numeric Surface order while use flags remain per Surface. Stable successful inputs are numerically overwrite-idempotent, but either zero branch latches an affected use flag false until Surface-property input relinking restores it, and later nonzero schedules can refresh the report average without restoring solver consumption. One focused unit fixture makes four direct calls and checks a three-entry radiative average plus each one-entry selection; one outside-long-wave fixture indirectly consumes the ordinary average. They do not cover the global return, parent/optional-Zone order, zero-sum or zero-numerator stores, missing schedules, EMS values, aliases, use-flag latching, malformed topology, failure prefixes, or retry. Beyond its explicit global/property/equality/null checks, CP180 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust ordinary ground temperature, schedule, long-wave, and view-factor primitives do not implement the SurfaceProperty ground list, shared radiative-temperature average, per-Surface use flag, complete-Surface zone-resimulation order, output-variable binding, or matching failure/re-entry lifecycle. CP180 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 189 routines, split 58 `state_mapped` plus 131 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP181 adds non-required source-mapped `routine.get_ground_surfaces_reflectance_average` immediately after `get_ground_surfaces_temperature_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesReflectanceAverage` is declared at `HeatBalanceSurfaceManager.hh` line 214 and implemented at `HeatBalanceSurfaceManager.cc` lines 10037-10072. Its only direct production call is unconditional `InitSurfaceHeatBalance` line 395, after variable absorptance, optional environment-history and movable-insulation work but before complex-fenestration, anisotropic-sky, shading, radiation, daylighting, and solar-gain initialization. Both normal Surface management and `SimulationManager::Resimulate` under `ResimHB` reach it; no first-time, BeginEnvironment, day, hour, timestep, warmup, sizing, solar, Surface-class, or boundary gate narrows CP181 itself. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state; when that gate passes, CP181 traverses the complete numeric Surface array and filters only `IsSurfPropertyGndSurfacesDefined`. It follows the Surface property index and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfRefl` while preserving the prior shared average. For a nonzero sum it traverses every stored ground entry and skips only null reflectance schedules: even an exact-zero view factor still samples a nonnull schedule. Every sampled schedule uses EMS precedence and contributes `view_factor * current_reflectance`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsReflAvg` with zero and then disables only the current Surface use flag; otherwise CP181 stores `numerator / SurfsViewFactorSum` and never enables a false flag. Thus a valid zero-reflectance schedule selects the disable branch, and later solar consumers use their environment fallback rather than retaining a custom zero. Shared-property aliases on a nonzero-sum path repeat schedule reads and the shared store in numeric Surface order while use flags remain per Surface. Stable successful inputs are overwrite-idempotent, but either zero branch latches an affected flag false until Surface-property input relinking restores it; later nonzero schedules can refresh the output average without restoring custom solar consumption. One focused unit fixture makes four direct calls and checks a three-entry weighted average plus each one-entry result. One long-wave fixture incidentally executes the all-null-schedule disable path but asserts no reflectance state, and no test connects the average to its solar or output consumers. Missing coverage includes the global return, zero-sum stale average, partial-null denominator rule, exact-zero schedule meaning, EMS values, aliases, flag latching, malformed topology, failure prefixes, and retry. Beyond its explicit global/property/equality/null checks, CP181 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust's generic schedules, ordinary ground view factor, and fixed 0.2 ground-reflectance solar path do not implement the SurfaceProperty ground list, scheduled shared average, per-Surface use flag, EMS sampling, fallback transition, alias order, output binding, or matching failure/re-entry lifecycle. CP181 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 190 routines, split 58 `state_mapped` plus 132 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP182 adds non-required source-mapped `routine.re_set_ground_surfaces_view_factor` immediately after `get_ground_surfaces_reflectance_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `ReSetGroundSurfacesViewFactor` is declared at `HeatBalanceSurfaceManager.hh` line 216, implemented at `HeatBalanceSurfaceManager.cc` lines 10074-10092, and called in production only by `InitSurfacePropertyViewFactors` line 9989. CP182 itself has no global or first-time gate: its sole quick return tests the current Surface ground-property flag. The production parent supplies broader `AnyLocalEnvironmentsInModel` and `InitSurfaceHeatBalancefirstTime` gates and calls CP182 only for a numeric property-bearing Surface whose sky-only or neither-set branch inferred a ground factor while a separate GroundSurfaces object exists; before calling, the parent overwrites `Surface.ViewFactorGroundIR` and marks the shared property's `IsGroundViewFactorSet` true. CP182 follows the Surface property index and first overwrites shared `SurfsViewFactorSum` with that current Surface ground IR factor. An exact zero, including negative zero, then clears the current Surface reflectance-use flag before its temperature-use flag and returns without touching any entry. Every nonzero value, including negative, NaN, or infinity, is copied as the whole sum into only `GndSurfs(1).ViewFactor`. The helper does not validate or clamp the value, clear later entries, change either shared average, change `Surface.ViewFactorGroundIR`, alter entry metadata or count, write `IsGroundViewFactorSet`, or enable a false use flag. Consequently nonzero-to-zero direct retry leaves the old first entry behind, zero-to-nonzero retry cannot restore the two flags, and a pre-mutated multi-entry property can retain an entry total inconsistent with the stored sum. Shared properties make order observable: in the ordinary parent loop the first blank-property alias marks the object set and later aliases normally consume that state without re-entering CP182, while every standalone call overwrites the shared sum, only nonzero calls overwrite the first entry, and a zero call preserves that entry while disabling only its own Surface flags. CP181 reflectance and CP180 temperature averaging later consume the current shared denominator and entries; a zero CP182 branch selects their solver fallbacks through the Surface flags while leaving report averages stale until separately recomputed. No test calls CP182 directly. Four parent fixtures reach it for six Surfaces; two fixtures assert the positive first-entry backfill for four of them, while the other two assert only parent or downstream results. The property quick return, exact-zero path and flag-write order, sum assignment, stale-first-entry and sticky-flag retries, aliases, malformed values/topology, and failure prefix remain untested. CP182 has no finite, sign, range, index, allocation, count, topology, alias, or lifecycle validation and emits no diagnostic, status, catch, rollback, cleanup, transaction, or latch. In a bounds-checked failure, an invalid Surface or property index precedes the shared-sum write, whereas a missing first entry on the nonzero path can fail after that sum commits; the zero path never accesses the first entry and can still clear both flags. Release unchecked malformed access can be undefined, and a failure reached from the parent retains its earlier Surface-factor and set-flag writes. Rust has no SurfaceProperty ground arena, shared sum/first-entry mutation, per-Surface use flags, parent branch/latch behavior, alias semantics, or matching failure/re-entry lifecycle; ordinary ground/sky factors, fixed ground reflectance, schedules, and run-blocked Zone local-environment state are adjacent primitives only. CP182 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 191 routines, split 58 `state_mapped` plus 133 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP183 adds non-required source-mapped `routine.get_surrounding_surfaces_temperature_average` immediately after `re_set_ground_surfaces_view_factor` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurroundingSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 218, implemented at `HeatBalanceSurfaceManager.cc` lines 10094-10118, and is the final routine before those namespaces close. Its sole direct production call is unconditional `CalcHeatBalanceOutsideSurf` line 7019, after CP180 ground-temperature averaging and before internal-source transfer, optional-`ZoneToResimulate` handling, and every outside-Surface calculation. A CP180 failure prevents CP183, but every parent call that passes that sibling enters CP183 before any full-model/Zone filter; even a Zone-limited radiant or HVAC retry can refresh every eligible allocated Surface. CP183 first returns when `AnyLocalEnvironmentsInModel` is false, preserving every prior average. Otherwise it range-traverses the complete allocated Surface vector and skips only records whose `SurfHasSurroundingSurfProperty` flag is false. For each eligible Surface it follows `SurfSurroundingSurfacesNum`, zeros a local sum, visits entries 1 through `TotSurroundingSurface` in stored order, unconditionally dereferences every temperature schedule even for an exact-zero factor, samples current or EMS-actuated value, adds Kelvin, and accumulates `ViewFactor * temperature^4`. It then divides by the Surface-local cached `ViewFactorSrdSurfs` rather than the property's live `SurfsViewFactorSum`, takes the fourth root, subtracts Kelvin, and performs the routine's only persistent write to that Surface's `SrdSurfTemp`. CP183 does not clear, disable, clamp, renormalize, diagnose, or mutate the property, entry list, factors, denominator cache, presence flag, schedules, sky/ground fields, or any shared average. Normal input processing requires at least one complete entry and a resolved schedule, constrains each factor to [0,1], accumulates the property sum, copies it into the Surface cache, and clears the presence flag on an exact-zero sum, but does not constrain the aggregate to one; none of those dependency guarantees is revalidated here. A schedule of exactly -273.15 C yields -273.15 C rather than a disable branch, temperatures below absolute zero lose their Kelvin sign through the fourth power, and a zero-factor NaN or infinity still poisons the sum. Nonfinite inputs, overflow, zero or mismatched denominators, and a negative quotient can store NaN or infinity without a diagnostic. Shared-property aliases repeat every schedule read and write separate Surface averages; matching cached denominators produce the same result, while independently mutated caches can diverge. Stable inputs are overwrite-idempotent, schedule or EMS changes appear on the next call, and false global/property gates preserve stale values. Three direct fixtures make three calls, write four property-bearing Surfaces, and traverse eight entries: two directly prove mixed-temperature fourth-power means, while one proves a single-entry result only through Window long-wave output. Five direct-parent fixtures add a global return, a per-Surface skip, and nine eligible writes; only the CondFD fixture genuinely checks an average-derived coefficient, while other assertions can use the parent's separate raw-entry long-wave loop. No test covers optional-Zone all-Surface refresh, gate-preservation sentinels, aliases, retries after schedule change, cached-denominator mismatch, zero/negative/nonfinite arithmetic, malformed topology, or failure prefixes. CP183 has no finite, sign, range, index, allocation, count, pointer, topology, alias, or lifecycle validation and no status, diagnostic, catch, rollback, cleanup, transaction, or latch. In a bounds-checked or otherwise diagnosed malformed-access failure, an invalid property, entry, or schedule can leave the current Surface old because its sole store follows the complete inner loop, while earlier eligible Surfaces and preceding CP180 writes remain committed; unchecked release access can be undefined. Downstream CTF/CondFD exterior balances, surrounding-radiation coefficients, and ordinary, complex, and equivalent-layer Window paths consume `SrdSurfTemp`, while the separate outside-Surface raw surrounding-entry radiation sum does not. Rust has no typed Surface surrounding-property arena, entry list, denominator cache, per-Surface radiative average, current/EMS property binding, surrounding long-wave term, parent traversal, alias order, or matching failure/re-entry lifecycle; generic schedules, ordinary sky/air/ground long-wave terms, and run-blocked Zone local-environment state are adjacent primitives only. CP183 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 192 routines, split 58 `state_mapped` plus 134 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP184 adds required source-mapped `routine.get_air_heat_balance_input` immediately after `routine.manage_air_heat_balance` in the Air-manager definition ledger and adds `get_air_heat_balance_input` after `manage_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirHeatBalanceInput` is declared at `HeatBalanceAirManager.hh` line 67, implemented at `HeatBalanceAirManager.cc` lines 163-189, and called in production only by `ManageAirHeatBalance` line 150 while the caller-owned `ManageAirHeatBalanceGetInputFlag` is true. The manager writes that flag false only after CP184 returns, then calls initialization, Air calculation, and mean-air reporting; a CP184 fatal or earlier non-return therefore leaves the flag true and blocks those later children on that attempt, while a successful return makes later manager calls skip only CP184. The flag defaults true, `HeatBalanceAirMgrData::clear_state` rearms it and clears only that manager's uniqueness sets, and the supported clean retry boundary is the broader `EnergyPlusData::clear_state` that also resets child-owned heat-balance and room-air state. CP184 itself has no guard or persistent latch. Every direct entry creates local `ErrorsFound = false`, then unconditionally calls `GetAirFlowFlag(state, ErrorsFound)`, `SetZoneMassConservationFlag(state)`, and `GetRoomAirModelParameters(state, ErrorsFound)` in that order before testing the aggregate once. `GetRoomAirModelParameters` owns another local flag and only raises the shared flag on its own errors; it never clears an earlier airflow error, so the result is monotonic OR accumulation. A normally returned first-child error does not suppress either later child, whereas any child non-return prevents its successors and the final wrapper check. When the aggregate is true, CP184's only diagnostic is terminal `ShowFatalError` with exact text `GetAirHeatBalanceInput: Errors found in getting Air inputs`; it does not derive the predicate from the global severe count. CP184 writes no persistent domain state itself and provides no validation, catch, rollback, cleanup, transaction, or idempotence. Its child context is observably stateful: `GetAirFlowFlag` sets `AirFlowFlag`, parses and allocates simple airflow/report state, and can append an AirFlow EIO row even with the aggregate already true; `SetZoneMassConservationFlag` monotonically marks receiving then source Zone flags and can expose a partial prefix after malformed parsed topology; `GetRoomAirModelParameters` allocates and populates per-Zone room-air state and writes its EIO header/rows before propagating its local error. A final fatal preserves all returned-child mutations, registrations, diagnostics, EIO output, and uniqueness effects while leaving the parent latch true; same-state retry revisits them without rollback. A direct call never changes the parent latch, so a later manager call can repeat the complete input wrapper, whereas any successfully returned production call suppresses it for that state even if a later manager child fails. The only literal direct test call is PTAC integration setup after heat-balance input and array allocation; it asserts later PTAC node and flow results, not CP184 state, ordering, error, fatal, latch, output, or retry. No test directly calls `ManageAirHeatBalance`; eight fixtures contain explicit `ManageHeatBalance` paths but assert daylighting, EMS, construction, plant, or Window behavior. Full-harness simulations provide broader transitive smoke, including one fixture that checks parsed Mixing/CrossMixing records, but not the wrapper contract. Separate child tests prove one airflow error flag and one room-air error/severe stream, while no test proves aggregate fatal text, three-child continuation, parent latch retention, partial effects, direct-call duplication, or clean/reset retry. Rust's `get_air_heat_balance_input_compat` is only an identity closure alias whose sole use wraps a per-Zone air-temperature coefficient calculation; the timestep manager path does not nest it, Rust owns none of the three input children or caller latch, and its heat-balance state has no matching lifecycle. CP184 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 193 routines, split 58 `state_mapped` plus 135 `source_mapped`, with 73 required; the heat-balance project list becomes 42. CP185 adds required source-mapped `routine.get_air_flow_flag` immediately after `routine.get_air_heat_balance_input` in the Air-manager definition ledger and adds `get_air_flow_flag` after `get_air_heat_balance_input` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirFlowFlag` is declared at `HeatBalanceAirManager.hh` line 69, implemented at `HeatBalanceAirManager.cc` lines 191-214, and called in production only as CP184's unconditional first child at line 179. Every entry first overwrites the persistent `AirFlowFlag` true, then calls complete `GetSimpleAirModelInputs(state, ErrorsFound)`, then on normal child return sums `TotInfiltration`, `TotVentilation`, `TotMixing`, `TotCrossMixing`, and `TotRefDoorMixing`. A strictly positive sum appends exact two-line EIO text `! <AirFlow Model>, Simple` followed by ` AirFlow Model, Simple`; zero writes no CP185 summary even though the flag remains true. `TotZoneAirBalance` and auxiliary EarthTube, CoolTower, and ThermalChimney presence are not in that output predicate. CP185 neither initializes, reads, tests, clears, nor directly writes the caller's `ErrorsFound` reference. The parser only raises that shared flag on its diagnosed paths, so an incoming true remains true and a returned child error does not suppress the count test or positive-total summary. CP185 emits no diagnostic itself. The child remains separate source dependency context: it allocates Zone and optional Space air-report records, registers output/EMS state, parses the nine simple-airflow object families, populates counts/records/mass-conservation data, and writes detailed EIO before returning. `AirFlowFlag` defaults false in `HeatBalanceData`, CP185 is its only source writer, and `HeatBalanceData::clear_state` placement-new restores false and clears its counts and arenas. The flag gates `InitSimpleMixingConvectiveHeatGains` simple mixing/cross-mixing/refrigeration-door initialization and, after its own accumulator resets, `ZoneEquipmentManager::CalcAirFlowSimple` before the AirflowNetwork control gate and simple airflow/auxiliary calculations. Count and record state has further HVAC, hybrid-ventilation, SQLite, mass-conservation, nominal-report, and output consumers. CP185 has no guard, validation, catch, rollback, cleanup, transaction, or one-shot latch. A parser non-return preserves the already-true flag plus its child prefix and skips CP185's summary; a count-access or EIO failure preserves the flag and completed parser effects. On normal error return, CP184 still runs its mass-conservation and room-air children and can later fatal; because the manager clears its input latch only after CP184 returns, that fatal leaves the latch true and same-state retry re-enters CP185 with prior parser/output state. The flag write is value-idempotent after the first entry, but the parser, registrations, uniqueness checks, diagnostics, and EIO are not a repeat-safe transaction. Direct calls always re-enter the child, while a successful production input pass suppresses later CP185 calls through the parent latch. Air-manager-only clear rearms that latch and clears its uniqueness sets but does not reset `HeatBalanceData` or output state; the broader whole-state clear is the clean retry boundary. One direct unit fixture supplies two same-Zone `ZoneAirBalance:OutdoorAir` objects with missing schedules and asserts only that the shared error flag is true; it neither isolates the prior `GetZoneData` status nor checks CP185's flag, zero-total no-summary branch, diagnostics, or parsed state. One PTAC fixture indirectly executes a clean five-infiltration positive-total path through CP184 but asserts only later node and mass-flow behavior. Parser tests bypass CP185, and a consumer test manually toggles `AirFlowFlag`. No test isolates false/true overwrite, incoming-error preservation, exact summary text or absence, error-plus-positive-summary behavior, repeated calls, partial failure, parent latch, or reset/retry. Rust has no `GetAirFlowFlag` alias, selector, parser, five totals, simple-airflow arenas, or matching EIO path. Its non-system `sum_mcp*` fields stay zero without a simple-airflow producer; typed `Construction:AirBoundary` mixing metadata is explicitly run-blocked and generates no `ZoneCrossMixing`, AirflowNetwork is unsupported, and Ideal Loads outdoor air is a distinct subsystem. CP185 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 194 routines, split 58 `state_mapped` plus 136 `source_mapped`, with 74 required; the heat-balance project list becomes 43. CP186 adds required source-mapped `routine.set_zone_mass_conservation_flag` immediately after `routine.get_air_flow_flag` in the Air-manager definition ledger and adds `set_zone_mass_conservation_flag` after `get_air_flow_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `SetZoneMassConservationFlag` is declared at `HeatBalanceAirManager.hh` line 71, implemented at `HeatBalanceAirManager.cc` lines 216-233, and called in production only as CP184's unconditional second child at line 181, after CP185 returns and before the room-air child. It receives no error reference or status parameter. Every entry first reads `ZoneAirMassFlow.EnforceZoneMassBalance` and then tests `ZoneFlowAdjustment` against exact `AdjustmentType::NoAdjustReturnAndMixing`. A false first guard or exact no-adjust value performs no writes; otherwise it visits expanded `Mixing` records from 1 through `TotMixing` in stored order and monotonically writes each receiving `ZonePtr` flag true before its source `FromZone` flag true. Zero or negative totals naturally visit no records, duplicate or self links repeat true stores, and any enum value other than exact no-adjust passes the second guard. CP186 never writes false and does not include `CrossMixing`, AirBoundary-generated cross mixing, refrigeration-door mixing, infiltration, or ventilation. `GetProjectControlData` owns the two controls, CP185's parser owns `TotMixing` and `Mixing`, and ordinary `AllocateHeatBalArrays` dimensions `ZoneMassBalanceFlag` false before the production Air pass. CP186 is the only production true writer. `CalcZoneMassBalance` reads the flag on each controlled-Zone visit of the mass-balance iteration loop to include mixing/return flow in mass continuity, while `CalcAirFlowSimple` reads it in all three delta-temperature branches when adjusted mixing is requested; resulting effects can reach return, infiltration, heat, moisture, contaminant, resimulation, and report state. CP186 has no allocation, enum, count, index, topology, or result validation and emits no diagnostic or output. In direct or externally mutated state, a malformed receiving endpoint can fail before the current record changes, while a valid receiving endpoint followed by an invalid source can leave that first write plus every prior record committed; unchecked access can be undefined. Stable valid re-entry is value-idempotent, but false guards, zero totals, removed links, or changed topology preserve stale true flags. `HeatBalFanSysData::clear_state` discards the flag array for later false reallocation; HeatBalanceData or Air-manager-only clear does not reset this separate owner, so whole-state clear and reinitialization is the clean boundary. A CP185 error that returns normally does not suppress CP186, and CP186 cannot alter CP184's aggregate; a CP186 non-return prevents room-air input and the final fatal while leaving the parent latch true. Seven direct fixtures call CP186. One explicitly exercises the second-guard no-op with enforcement true and no-adjust, five active-adjustment fixtures transitively exercise downstream mass balance, and one active fixture asserts only parser-owned controls and `ZoneReOrder`; none directly asserts `ZoneMassBalanceFlag`. No test isolates the first guard, active zero count, exact endpoint write order, sticky/repeated topology, excluded families, malformed partial writes, parent retry, or reset. Rust has no `SetZoneMassConservationFlag` alias, `ZoneAirMassFlowConservation` controls, typed `ZoneMixing` topology, endpoint flag array, mass-balance solver, or adjusted simple-mixing consumer. Its zero-seeded `sum_mcp*` state, run-blocked AirBoundary metadata, and Ideal Loads airflow are adjacent but distinct. CP186 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 195 routines, split 58 `state_mapped` plus 137 `source_mapped`, with 75 required; the heat-balance project list becomes 44. CP187 adds required source-mapped `routine.get_simple_air_model_inputs` immediately after `routine.set_zone_mass_conservation_flag` in the Air-manager definition ledger and adds `get_simple_air_model_inputs` after `set_zone_mass_conservation_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `GetSimpleAirModelInputs(EnergyPlusData &state, bool &ErrorsFound)` is declared at `HeatBalanceAirManager.hh` line 73, implemented at `HeatBalanceAirManager.cc` lines 235-4244, and called in production only by CP185 `GetAirFlowFlag` line 207, unconditionally after that wrapper writes `AirFlowFlag = true`. It first dimensions local report-selection state, allocates Zone plus sizing-or-simulation Space air-report records, and registers baseline Zone, simulation-only Space, Site, and conditional advanced outputs; it then sizes shared input scratch arrays from exactly nine direct schemas and processes `ZoneAirBalance:OutdoorAir`, all three `ZoneInfiltration` families, both `ZoneVentilation` families, `ZoneMixing`, `ZoneCrossMixing`, and `ZoneRefrigerationDoorMixing` in that order. Count-driven arenas and expanded Space/Zone records feed output and EMS registrations; `ZoneMixing` additionally constructs per-Zone `MassConservation` topology and receiving/source `ZoneReOrder` state, while `TotCrossMixing` appends generated Construction:AirBoundary records after explicit CrossMixing input without making AirBoundary a tenth parser family. The tail emits nominal infiltration, ventilation, mixing, cross-mixing, and refrigeration-door EIO rows, stores per-Zone nominal totals, conditionally retains the last encountered infiltration pointer per Zone when mass-balance enforcement is enabled, registers mass-balance outputs, and deliberately excludes refrigeration-door flow from `TotMixingFlow`. CP187 never initializes, clears, tests, or terminally barriers the supplied `ErrorsFound` reference: incoming true does not suppress any phase, diagnosed paths only raise it, and normal return lets CP185 perform its count summary regardless of the value. A blank ZoneAirBalance induced-air schedule and an enforced-balance source-only Mixing Zone without Infiltration each emit severe diagnostics without raising the reference; malformed endpoints or schedules can instead fail before normal return, so diagnosed continuation is not a return guarantee. The routine has no guard, transaction, rollback, or cleanup. Same-state repeat normally encounters already allocated HeatBalance arrays and can repeat registrations, diagnostics, or EIO; retained `UniqueZoneNames` can turn a partially cleared ZoneAirBalance retry into duplicate errors, whereas `UniqueInfiltrationNames` is only reserved and has no semantic use. Air-manager-only and HeatBalance-only clears each leave other owners stale, so coordinated whole-state reset plus normal reinitialization is only the clean domain-state retry boundary; an output-clean replay additionally requires a fresh or reset EIO stream. Fifteen actual direct unit calls cover selected design-flow infiltration/ventilation expansion, all three infiltration models, explicit CrossMixing, several Mixing mass-balance paths, one `ZoneReOrder` assertion, and mass-balance output ordering; no direct parser fixture covers ZoneAirBalance, WindAndStack ventilation, refrigeration-door mixing, or AirBoundary-generated CrossMixing, and none isolates incoming-true behavior, either unflagged severe, exact nominal EIO, partial failure, repeat, or reset. Rust has no typed records, counts, topology, outputs, EMS, EIO, or runtime path for any of the nine families; generic Zone/schedule state, zero-seeded `sum_mcp*` fields, run-blocked AirBoundary metadata, and Ideal Loads outdoor air are adjacent dependencies only. CP187 adds no Rust target, code, state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 196 routines, split 58 `state_mapped` plus 138 `source_mapped`, with 76 required; the heat-balance project list becomes 45. CP188 adds required source-mapped `routine.get_room_air_model_parameters` immediately after `routine.get_simple_air_model_inputs` in the Air-manager definition ledger and adds `get_room_air_model_parameters` after `get_simple_air_model_inputs` and before `manage_zone_air_updates` in the heat-balance project contract. `GetRoomAirModelParameters(EnergyPlusData &state, bool &errFlag)` is declared at `HeatBalanceAirManager.hh` line 75, implemented at `HeatBalanceAirManager.cc` lines 4246-4492, and called in production only as CP184 `GetAirHeatBalanceInput` line 184's unconditional third child after CP185 and CP186 return. Every entry resizes and default-fills one `AirModelData` record per Zone as Mixing, Direct, and not simulated, initializes only a local error false, counts the sole direct schema `RoomAirModelType`, diagnoses but continues when its object count exceeds the Zone count, and false-dimensions three downstream per-Zone model arrays only when at least one object exists. The object loop resolves a Zone, maps eight model selectors and Direct/Indirect coupling, sets per-Zone selector/coupling/`SimAirModel` plus monotonic global use flags, validates only the matching Zone-name presence of five `RoomAirSettings:*` families, and for AirflowNetwork checks only whether `AirflowNetwork:SimulationControl` exists. UserDefined details, RoomAir nodes and patterns, AirflowNetwork intrazone topology, and all settings fields remain later `GetAirModelDatas` dependencies. Invalid model and coupling keys emit warnings and fall back to Mixing and Direct without raising the local error; too many objects, a missing Zone or companion, a missing AirflowNetwork control object, and any non-Mixing model combined with Space heat-balance sizing or simulation raise it. The nominal duplicate-Zone check reads `AirModel.Name` before the loop ever writes that field, so every clean entry sees empty names: repeated declarations for one Zone overwrite model and coupling in input order without that diagnostic, while a prior declaration's true `SimAirModel` or global used flag can remain sticky. CP188 never retains the authored object name and never sets `AirModelData::ZonePtr`. After the object loop it overwrites every Zone's record name with `MIXING AIR MODEL FOR <Zone>` regardless of selected model, refreshes its canonical Zone name, and monotonically sets `anyNonMixingRoomAirModel`. Even with zero objects, it materializes default Mixing records and, on every normally reached tail, writes the RoomAir EIO header plus one Zone-order row per Zone before any local error summary or outer-flag update. The outer `errFlag` is never read or cleared; a local error emits `Errors found in processing input for RoomAirModelType` and assigns only true after EIO, while an incoming true with locally clean input adds no CP188 summary and still executes every phase. An EIO or earlier non-return can preserve assigned state and local diagnostics before outer propagation. CP188 has no latch, transaction, rollback, or output cleanup. Direct repeat resets the per-Zone arena but repeats EIO and retains monotonic manager flags; `RoomAirModelData::clear_state` clears CP188 arenas and used flags but does not rearm `GetAirModelData`, `MyOneTimeFlag`, or `MyEnvrnFlag`, the parent Air-manager latch is separate, and `EnergyPlusData::clear_state` does not reset EIO. End-to-end clean retry therefore requires fresh or explicitly rearmed room-air and parent lifecycle state plus a fresh or reset EIO stream. Exactly two direct unit calls exist: one proves two missing-AirflowNetwork-control diagnostics and the outer true result, while one proves a valid AirflowNetwork false result only through later intrazone Zone indexing. No direct test covers zero-object defaults or EIO, count overflow, invalid Zone, duplicate overwrite, invalid keys, Indirect coupling, any non-AirflowNetwork active model, Space incompatibility, state fields, incoming true, partial failure, repeat, or reset. Rust has no `GetRoomAirModelParameters` alias, typed `RoomAirModelType` or `RoomAirSettings` records, per-Zone selector/coupling state, non-Mixing flags, RoomAir or intrazone-node topology, dependency validation, model dispatch, or RoomAir EIO. Those room-air objects remain RawOnly and run-blocking, AirflowNetwork remains explicitly unsupported, and typed Zone/Space identities, the single Zone mean-air state, numerical zone-air selectors, HVAC Zone-air-node edges, surface adjacent-air state, and oracle-side Mixing EIO are adjacent but non-equivalent. CP188 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 197 routines, split 58 `state_mapped` plus 139 `source_mapped`, with 77 required; the heat-balance project list becomes 46. CP189 adds required source-mapped `routine.init_air_heat_balance` immediately after `routine.get_room_air_model_parameters` in the Air-manager definition ledger and adds `init_air_heat_balance` after `get_room_air_model_parameters` and before `manage_zone_air_updates` in the heat-balance project contract. `InitAirHeatBalance(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 77 and implemented at `HeatBalanceAirManager.cc` lines 4494-4507. The body owns no condition, loop, local persistent state, status, diagnostic, output, catch, cleanup, or latch; every entry forwards the unchanged state exactly once to CP190 `InitSimpleMixingConvectiveHeatGains` line 4506 and returns only after that child returns. Two production call sites exist. The ordinary `ManageAirHeatBalance` path calls CP189 unconditionally at line 154 after its optional input block and before `CalcHeatBalanceAir` and `ReportZoneMeanAirTemp`; its input latch has already been cleared after successful input, so a CP189 non-return preserves the child's partial effects, suppresses both later children, and a same-state manager retry skips input and enters CP189 again. `SimulationManager::Resimulate` conditionally calls CP189 at line 2908 when `ResimHB` is true after Surface initialization and both Surface balances; a non-return prevents refrigeration-rack work, the heat-balance iteration increment, and the line-2912 assignment forcing `ResimHVAC` true, while prior Surface and child effects remain. This second path intentionally can execute multiple times without advancing simulation time and bypasses the Air-manager input latch and its `CalcHeatBalanceAir` and report siblings. CP189 itself neither makes child re-entry safe nor adds a wrapper-specific reset obligation; mutation, failure, retry, and owner-clear behavior are exactly those of CP190 and its dependencies. As bounded dependency context rather than parity, the child's enforced mass-balance fraction loop uses each record's receiving count against the global `Mixing(1..N)` prefix and raw `DesignLevel`, not that Zone's receiving pointers or schedule/EMS-scaled flow. Five direct unit calls all use CP189 as setup for ZoneEquipment mass-balance tests and assert downstream airflow or mass-balance results rather than the wrapper's one-child cardinality, either production caller order, resimulation, failure prefix, retry, or reset; four direct CP190 calls in two fixtures bypass the wrapper. Rust's `init_air_heat_balance_compat` is an identity closure invoked with an empty body in the timestep scaffold. It has no CP190 alias, typed mixing, cross-mixing, or refrigeration-door arenas, schedule or EMS refresh, mass-conservation fractions, demand-manager resimulation path, diagnostics, or outputs; zero-seeded `sum_mcp*` state and run-blocked AirBoundary SimpleMixing metadata are adjacent but non-equivalent. CP189 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 198 routines, split 58 `state_mapped` plus 140 `source_mapped`, with 78 required; the heat-balance project list becomes 47. CP190 adds required source-mapped `routine.init_simple_mixing_convective_heat_gains` immediately after `routine.init_air_heat_balance` in the Air-manager definition ledger and adds `init_simple_mixing_convective_heat_gains` after `init_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `InitSimpleMixingConvectiveHeatGains(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 79, implemented at `HeatBalanceAirManager.cc` lines 4509-4588, and called in production only as CP189 `InitAirHeatBalance` line 4506's unconditional child. Every entry first tests `AirFlowFlag`; false leaves every target untouched. True executes four ordered phases. The actual Mixing vector, not `TotMixing`, receives `DesignLevel * sched->getCurrentVal()`, then an optional object EMS override, then the final value in both `DesiredAirFlowRate` and `DesiredAirFlowRateSaved`. Under `EnforceZoneMassBalance` the actual MassConservation vector is traversed; for each record's receiving count `N`, the first pass adds global `Mixing(1..N).DesignLevel` before zeroing the corresponding fraction, and a strictly positive sum triggers normalization from that same raw global prefix. This ignores `ZoneMixingReceivingPtr` plus schedule- and EMS-scaled desired flows, so zero, negative, or NaN sums leave touched fractions zero and each Zone can receive weights derived from unrelated records. The actual CrossMixing vector, not `TotCrossMixing`, then gets the same schedule-before-object-EMS desired flow but no saved-flow write. Finally, `TotRefDoorMixing > 0` acts only as a gate over Zone indices 1 through `NumOfZones - 1`; a flagged record whose `ZonePtr` equals that index has every declared connection volume zeroed and then replaced only when its door EMS override is on. Open schedules and physical door flow remain later `CalcAirFlowSimple` work. CP190 does not initialize infiltration, ventilation, report flags, Mixing mass flow, CrossMixing saved flow, counts, or heat-gain accumulators, and it contains no AirflowNetwork gate, allocation, validation, diagnostic, EIO, status, latch, catch, rollback, or cleanup. Null schedules or mismatched vectors, counts, fractions, Zone-indexed records, or connection arrays can stop the routine after an ordered Mixing, fraction, CrossMixing, or door prefix. Normal repeat resamples current schedule and EMS state, restores Mixing baselines, rebuilds touched fractions, and resets reached door volumes, while false guards, shortened counts, or skipped records preserve untouched values. `HeatBalanceData::clear_state` reconstructs all CP190-owned records; Air-manager-local clearing only rearms the separate input lifecycle, so clean production replay needs coordinated dependency reinitialization, but CP190 itself has no output-stream reset requirement. Four direct calls in two fixtures cover only an `AirFlowFlag = false` fraction sentinel, zero and 100/300 raw-DesignLevel fractions, and downstream CrossMixing airflow effects. They do not directly assert Mixing desired/saved flow, non-unit schedules, schedule or object EMS, count-versus-vector behavior, the ignored receiving pointers, negative/nonfinite arithmetic, CrossMixing state, any refrigeration-door branch, partial failure, repeat, resimulation, or reset. Rust has no CP190 alias, typed ZoneMixing, ZoneCrossMixing, or ZoneRefrigerationDoorMixing runtime records, schedule/EMS flow refresh, mass-conservation fraction topology, door state, or consumers. The CP189 identity closure runs an empty body; generic schedule machinery, zero-seeded `sum_mcp*` fields, and typed but run-blocked `Construction:AirBoundary` SimpleMixing metadata are adjacent but non-equivalent. CP190 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 199 routines, split 58 `state_mapped` plus 141 `source_mapped`, with 79 required; the heat-balance project list becomes 48. CP191 adds required source-mapped `routine.calc_heat_balance_air` immediately after `routine.init_simple_mixing_convective_heat_gains` in the Air-manager definition ledger and adds `calc_heat_balance_air` after `init_simple_mixing_convective_heat_gains` and before `manage_zone_air_updates` in the heat-balance project contract. `CalcHeatBalanceAir(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 83, implemented at `HeatBalanceAirManager.cc` lines 4590-4604, and called in production only by `ManageAirHeatBalance` line 158 after CP189 returns and before `ReportZoneMeanAirTemp`. Each entry tests the current `state.dataGlobal->externalHVACManager`. A configured callback selects the external branch: when `externalHVACManagerInitialized` is false it first calls `initializeForExternalHVACManager`, then invokes the callback with `&state`; a true flag skips only that initializer. An absent callback ignores the flag and calls `HVACManager::ManageHVAC`, so the external branch bypasses the entire standard HVAC manager. The initializer presently calls only `InitZoneAirSetPoints`, while neither CP191, that child, nor the runtime registration APIs set `externalHVACManagerInitialized` true; source-wide references contain only the false default and CP191 read, so a normally registered callback repeats the initializer call on every entry unless external code mutates the flag. The child's own one-time flag can suppress only its internal one-time block; it is not CP191's initialized flag. CP191 owns no direct mutation, status, diagnostic, output, catch, cleanup, rollback, or transaction. An initializer non-return suppresses the callback, and any initializer, callback, or `ManageHVAC` effects before a non-return remain while the parent report and later Surface-manager work are suppressed. Normal repeat re-evaluates the callback and executes one branch every time; child work is stateful rather than idempotent. `DataGlobal::clear_state` placement-new removes the callback and resets the flag false; Air-manager-local clear touches neither, so external-mode replay needs callback re-registration plus dependency reset. There are no direct unit calls to CP191 or its initializer and no external-manager API test; ordinary simulation tests only transitively cover the default branch. Rust's `calc_heat_balance_air_compat` is an identity closure used once around a bounded `ManageZoneAirUpdates(PredictStep)` shell and zone-temperature work. It has no external callback, flag, initializer, `ManageHVAC`, complete predictor/corrector and system-timestep loop, HVAC equipment/air/plant dispatch, demand-manager work, or report topology; separate Ideal Loads and execution-plan scaffolds are not CP191 parity. CP191 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 200 routines, split 58 `state_mapped` plus 142 `source_mapped`, with 80 required; the heat-balance project list becomes 49. CP192 adds non-required source-mapped `routine.initialize_for_external_hvac_manager` immediately after `routine.calc_heat_balance_air` in the Air-manager definition ledger and adds no project-contract requirement. `initializeForExternalHVACManager(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 81, implemented at `HeatBalanceAirManager.cc` lines 4608-4613, and called in production only by CP191 line 4598 when an external callback exists and `externalHVACManagerInitialized` is false. The standard `ManageHVAC` branch and an external branch whose flag is true both bypass CP192. Its body has no local guard or write and forwards the unchanged state exactly once to `ZoneTempPredictorCorrector::InitZoneAirSetPoints` line 4612; it does not acquire Zone setpoint input, calculate setpoints, invoke the external callback, or set CP191's initialized flag. The child is independently called by `ManageZoneAirUpdates` line 220 and owns a one-time allocation/output-registration block, begin-environment and day latches, controlled-Zone and demand-limit work, sticky error state, a fatal tail, and several other owners' arrays. A child non-return preserves its ordered prefix and prevents the CP191 callback, Air report, and later Surface-manager work. Failure before the child's one-time flag clear can repeat partial allocation or registration on retry, while failure after that clear skips the one-time prefix; a retained environment flag or sticky error can likewise repeat reset work or fatal again. Because CP191 never sets its global initialized flag, normal external-mode entry calls CP192 before every callback, although the child's separate one-time flag suppresses its successful one-time block. Coordinated owner reset is required for a clean child replay, and `DataGlobal::clear_state` removes the external callback so whole-state replay also requires re-registration. CP192 has no direct test and no external-manager registration test; four direct `InitZoneAirSetPoints` calls in four HVAC/report fixtures bypass the wrapper and use the child only as setup. Rust has no CP192 or external-manager adapter. Its `init_zone_air_set_points_compat` is an identity closure used once around an empty setpoint-calculation shell and implements none of the source child's allocation, output, environment, demand, error, or reset lifecycle. CP192 adds no EnergyPlus source inventory, Rust target, code, state, test, project requirement, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 201 routines, split 58 `state_mapped` plus 143 `source_mapped`, with 80 required; the heat-balance project list remains 49. CP193 adds required source-mapped `routine.report_zone_mean_air_temp` immediately after `routine.initialize_for_external_hvac_manager` in the Air-manager definition ledger and adds `report_zone_mean_air_temp` after `calc_heat_balance_air` and before `manage_zone_air_updates` in the heat-balance project contract. `ReportZoneMeanAirTemp(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 85, implemented at `HeatBalanceAirManager.cc` lines 4615-4687, and called in production only by `ManageAirHeatBalance` line 160, unconditionally after CP191 returns. Its one-time phase scans the actual OutputProcessor request vector for exact Zone or Space Wetbulb Globe Temperature names, fans a stored empty key over the declared Zone or Space count, otherwise performs a literal name lookup, then scans EMS sensors `1..NumSensors`; Space handling is entirely guarded by `doSpaceHeatBalanceSimulation`, and only the Output:Variable keys are uppercased here. Reached matches monotonically set per-report `ReportWBGT` true, and only a completely returned scan clears `CalcExtraReportVarMyOneTimeFlag`. Every call then traverses Zones `1..NumOfZones`, calls CP194 `calcMeanAirTemps` once with each Zone's averaged temperature, humidity ratio, MRT, and report record, and, when Space simulation is active, visits that Zone's stored `spaceIndexes` in order and calls the same child with each Space's averaged state but the owning Zone number for operative-control metadata. CP194 owns the mean temperature and humidity copies, 50/50 operative temperature, dew-point psychrometrics, conditionally retained thermostat-operative value, and conditionally retained WBGT calculation; CP193 itself neither emits output nor allocates or validates these arenas. A request or sensor scan non-return preserves a true-flag prefix and the still-true latch, while a later non-return retains the false latch plus the completed Zone/Space and child-write prefix; there is no status, catch, cleanup, rollback, or transaction. Air-manager clear rearms CP193's scan but leaves its report state intact, while HeatBalance, predictor/corrector, OutputProcessor, EMS, Zone-control, and environment owners separately hold report flags and values, averaged inputs, requests, sensors, control metadata, and pressure, so clean replay requires coordinated reset and input reconstruction. No C++ unit test calls CP193 or CP194 directly or names the WBGT targets and report-only fields. Rust's `report_zone_mean_air_temp_compat` is an identity closure used only to choose one bounded Zone temperature sample during run-period aggregation; existing MAT/humidity result series and a CLI trace label provide neither the request/EMS scan, Space traversal, report bundle, operative/dew-point/WBGT calculations, failure lifecycle, nor CP193 call topology. CP193 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 202 routines, split 58 `state_mapped` plus 144 `source_mapped`, with 81 required; the heat-balance project list becomes 50. CP194 adds required source-mapped `routine.calc_mean_air_temps` immediately after `routine.report_zone_mean_air_temp`, closing the HeatBalanceAirManager definition ledger, and adds `calc_mean_air_temps` after `report_zone_mean_air_temp` and before `manage_zone_air_updates` in the heat-balance project contract. `calcMeanAirTemps(EnergyPlusData &state, Real64 ZTAV, Real64 airHumRatAvg, Real64 MRT, AirReportVars &thisAirRpt, int zoneNum)` is declared at `HeatBalanceAirManager.hh` lines 87-92, implemented at `HeatBalanceAirManager.cc` lines 4689-4728, and called only by CP193 for each Zone at line 4678 and each active stored Space membership at lines 4682-4683; Space inputs use that Space's averages but the owning Zone number for control metadata. Every entry first writes `MeanAirTemp = ZTAV`, the raw averaged humidity ratio, ordinary `0.5 * (ZTAV + MRT)` operative temperature, and dew point from current outdoor barometric pressure in that order. `PsyTdpFnWPb` floors only its local humidity input to `1e-5` before saturation work, so the stored report humidity remains raw. Thermostat-operative output updates only under `AnyOpTempControl`, controlled-Zone, and non-`None` guards. Only exact `Scheduled` reads the schedule's current value, including an EMS override; every other non-`None` value takes `FixedRadiativeFraction`. No index, pointer, finite-value, or `[0,1]` fraction check is local, the ordinary operative value is redundantly restored, and `ThermOperativeTemp = (1-f) * ZTAV + f * MRT` can extrapolate. A false guard retains its prior/default value. Only `ReportWBGT` updates `WetbulbGlobeTemp` as `0.7 * PsyTwbFnTdbWPb(ZTAV, airHumRatAvg, OutBaroPress) + 0.3 * OperativeTemp`, using raw humidity and the ordinary 50/50 operative value rather than the thermostat-weighted value; a false flag retains the old/default WBGT. The default psychrometric build uses quantized saturation and wet-bulb caches plus raw memo, warning, and recurring-error state, so normal numeric writes are not a claim of pure whole-routine idempotence or cold retry. CP194 owns no allocation, validation, diagnostic conversion, status, latch, catch, cleanup, rollback, or transaction: a dew-point non-return leaves the first three writes, schedule failure leaves the first four, and wet-bulb failure leaves all preceding reached writes while the final target remains retained. Clean replay spans separately owned HeatBalance report arenas, predictor averages, Zone controls, environment pressure, and psychrometric/cache state. No C++ unit test directly calls CP194 or CP193 or asserts any target report field; focused psychrometric tests do not cover the report composition, guards, Space topology, partial effects, repeat, or reset. Rust has no CP194 alias, integrated Air-report record, MRT/operative/control/WBGT state, Space report path, or W-input cached wet-bulb counterpart. Its isolated dew-point projection and RH-input outdoor wet-bulb helper are not wired to this source call topology. CP194 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list becomes 51. CP195 expands the existing required source-mapped `routine.manage_zone_air_updates` without adding a routine or project-contract entry. `ManageZoneAirUpdates(EnergyPlusData &state, PredictorCorrectorCtrl UpdateType, Real64 &ZoneTempChange, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 264-270 and implemented at `ZoneTempPredictorCorrector.cc` lines 195-244. Every entry first tests the shared `GetZoneAirStatsInputFlag`; true calls CP196 `GetZoneAirSetPoints` and clears the latch only after that child returns. It then calls `InitZoneAirSetPoints` unconditionally before dispatching. The six recognized selectors call `CalcZoneAirTempSetPoints`, `PredictSystemLoads` with all three timestep arguments, `correctZoneAirTemps` with only the history selector, or the three revert/Zone-push/system-push history children. `Invalid`, `Num`, and out-of-range casts reach a silent default only after the common input/init prefix. Only a normally returned `CorrectStep` assigns its child result into caller-owned `ZoneTempChange`; every other selector and a correct-child non-return preserve the caller's prior value. Seven `HVACManager::ManageHVAC` sites execute Get, initial Predict and Correct, optional shortened-step Predict/Correct/PushSystem repetitions, and final PushZone; two `SimulationManager::Resimulate` sites execute only Get then Predict when HVAC resimulation is requested. No production caller selects Revert. CP195 owns no selector validation, status, diagnostic conversion, catch, cleanup, rollback, or transaction. A Get failure retains its parser prefix and true latch while suppressing Init and dispatch; an Init failure retains the now-false latch and its prefix; branch failure retains both common phases and the reached branch prefix. Normal repeat skips successful one-time input but always reruns Init and the selected stateful child, while push/revert repetition is not idempotent. Clean replay requires coordinated ZoneControls, predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, RoomAir, node, schedule, environment, and output-owner reset. The C++ unit tree directly calls CP195 zero times; direct child calls exercise Get 9 times, Init 4, setpoint calculation 21, Predict 16, and Correct 5, but none of the three history children, and none proves wrapper order, latch timing, one-branch dispatch, default behavior, output-reference retention, partial failure, retry, or reset. Rust's stage object is metadata and `manage_zone_air_updates_compat` ignores its selector while passing one arbitrary closure through. Live Rust invokes only Predict and Correct selector shells, manually composes empty/common and history wrappers in a topology that is not the source dispatch, and has no shared input latch, unconditional Init contract, `ZoneTempChange` reference, invalid/default selector, or coordinated owner lifecycle. Existing bounded MAT, coefficient, correction, history, execution-plan, and promoted output evidence does not establish CP195 parity. CP195 adds no EnergyPlus source inventory, routine count, project entry, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory remains 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list remains 51. CP196 adds required source-mapped `routine.get_zone_air_set_points` immediately after `routine.manage_zone_air_updates` in the predictor/corrector definition ledger and adds `get_zone_air_set_points` after `manage_zone_air_updates` before `update_final_surface_heat_balance` in the heat-balance project contract. `GetZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 272 and implemented at `ZoneTempPredictorCorrector.cc` lines 246-2174 with no explicit return or internal latch. It executes ordinary thermostat and four setpoint families, humidistat, thermal-comfort and four Fanger families, unconditional HybridModel input, Zone capacitance assignment and EIO, operative/adaptive comfort, temperature-and-humidity overcool, and staged-dual input before one accumulated-error fatal. It expands ZoneLists, allocates and links control and schedule state, mutates Zone indices and multipliers, registers operative-temperature outputs, preserves warning and severe paths that do not set the local fatal flag, and can exit earlier through Hybrid or adaptive helper fatals. The staged precheck reads the routine-wide error flag, the capacitance ZoneList loop omits its final member, and no rollback or re-entry guard exists. Three production call sites share `GetZoneAirStatsInputFlag` and clear it only after return; a non-return retains partial state and the true latch, so same-state retry is unsafe. Nine direct C++ unit calls cover selected valid fields and two fatal diagnostics but not the complete family order, shared latch, valid staged, overcool, ZoneList, or capacitance-output paths, partial failure, retry, or reset. Rust types only direct-Zone DualSetpoint thermostat and humidistat subsets and exposes an identity `get_zone_air_set_points_compat` closure; it does not implement the complete 16-family transaction, ZoneList expansion, diagnostic and output order, latch, partial effects, or owner lifecycle. CP196 adds no new EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 204 routines, split 58 `state_mapped` plus 146 `source_mapped`, with 83 required; the heat-balance project list becomes 52. CP197 adds non-required source-mapped `routine.calculate_monthly_running_average_dry_bulb` immediately after `routine.get_zone_air_set_points`; the heat-balance project contract remains unchanged. `CalculateMonthlyRunningAverageDryBulb(EnergyPlusData &state, Array1D<Real64> &runningAverageASH, Array1D<Real64> &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` line 284 and implemented at `ZoneTempPredictorCorrector.cc` lines 2176-2275. Its only two production calls are CP196 operative-temperature branches at lines 1661 and 1731, each under the shared adaptive initialized guard and immediately before CP198; callers allocate fresh zeroed `NumDaysInYear` arrays, while CP197 neither reads nor sets the guard and only CP198 sets it true. CP197 checks and opens the weather path, discards nine lines, blindly reads `NumDaysInYear * 24` records, strips six comma fields, and averages field 7 into positional days before its ASH and CEN windows. The canonical eight-header mismatch shifts daily buckets by one hour; EOF and conversion status are ignored, `StrToReal` silently supplies -99999 on invalid text, and the corrupted final bucket is never consumed. The regular ASH path sums 31 inclusive days and divides by 30, the regular CEN path sums 8 and divides by 7, both wrap paths exclude `dailyDryTemp(N)`, and each caller cell self-accumulates as `x = x + avgDryBulb` without local clearing before `/=`. Missing or unreadable weather fatals before output writes, while malformed readable input can flow into CP198 and become latched. No C++ unit test executes CP197; the adaptive test calls CP198 with synthetic arrays. Rust has a typed EPW parser, while its legacy dry-bulb loader skips eight headers and reports numeric conversion errors; neither provides CP197 averaging, adaptive state, or an operative-control path. CP197 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 205 routines, split 58 `state_mapped` plus 147 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP198 adds non-required source-mapped `routine.calculate_adaptive_comfort_set_point_schl` immediately after `routine.calculate_monthly_running_average_dry_bulb`; the heat-balance project contract remains unchanged. `CalculateAdaptiveComfortSetPointSchl(EnergyPlusData &state, Array1D<Real64> const &runningAverageASH, Array1D<Real64> const &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` lines 286-287 and implemented at `ZoneTempPredictorCorrector.cc` lines 2277-2348. Its only two production calls are CP196 lines 1662 and 1732, immediately after CP197 and under the same external false-initialized guard; CP198 has no local guard. It scans all stored design days, uses only day type 9, derives `MaxDryBulb - DailyDBRange / 2`, and conditionally writes shared ASH slots 0-2 for strict `(10, 33.5)` and CEN slots 3-6 for strict `(10, 30)`. Later qualifying design days overwrite each family independently, while nonqualifying values retain earlier/default slots; the C++ declaration `std::array<Real64, 7> = {-1}` initializes the vector as `[-1, 0, 0, 0, 0, 0, 0]`. It then allocates seven `NumDaysInYear` arrays and overwrites every day with three `0.31 * T + offset` ASH values or `-1`, followed by four `0.33 * T + offset` CEN values or `-1`. Exact bounds and nonfinite daily inputs take the `-1` branches. Input shape is trusted, the const input arrays are untouched, and the shared `initialized` flag is set only after all design, allocation, and daily work returns. There is no status, diagnostic, catch, cleanup, or rollback; abnormal non-return can retain design writes, an allocation/day prefix, and the prior flag, while complete direct re-entry runs even when the flag is already true. A later CP196 fatal retains successfully committed schedules and causes same-state input retry to skip both adaptive helpers; only full predictor/corrector owner reconstruction resets them. One C++ fixture calls CP198 directly three times with uniform 0, 40, and 25 arrays, asserting day 1, all seven daily formulas for 25, the flag, and only two design slots; strict bounds, nonfinite and shaped inputs, remaining days, multiple/default design days, partial failure, guarded integration, retry, and reset remain uncovered. Rust has no operative-temperature control object, adaptive model/formulas, schedule state, or consumer; DualSetpoint thermostat parsing and execution-plan labels are not parity. CP198 adds no source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 206 routines, split 58 `state_mapped` plus 148 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP199 adds required source-mapped `routine.init_zone_air_set_points` immediately after `routine.calculate_adaptive_comfort_set_point_schl` and adds `init_zone_air_set_points` after `get_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `InitZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 274 and implemented at `ZoneTempPredictorCorrector.cc` lines 2350-2816. CP195 line 220 calls it on every selector after optional CP196 input and before dispatch; CP192 line 4612 also calls it directly for external-HVAC initialization without reproducing that input prefix. The default-true one-time block sizes thermostat setpoints, load correction, control/report, optional comfort/Fanger, setback/deadband, ZoneList/ZoneGroup load, six hybrid-history, Zone demand, and simulation-or-sizing Space demand state. It warning-checks each Zone's heat-transfer-surface reference-air consistency, registers Zone and simulation-Space heat-balance and sensible/moisture demand bundles, six direct thermostat/correction outputs, optional comfort outputs, and ZoneList/ZoneGroup outputs, and clears its one-time latch only after that complete prefix. A begin-environment gate then calls CP200 for every Zone and active Space, zeros four current thermostat setpoints, sets load correction to one and temperature control to Uncontrolled, resets Zone and active-Space demand helpers, `DeadBandOrSetback`, `NoHeatToReturnAir`, and hybrid histories, and clears `MyEnvrnFlag` only at its tail. It intentionally does not directly reset averaged setpoints, temperature-control report, comfort control/report/Fanger state, `Setback`, `CurDeadBandOrSetback`, or ZoneList/ZoneGroup totals; seeing `BeginEnvrnFlag` false rearms the gate. The begin-day branches only toggle `MyDayFlag`. Every call then scans ordinary temperature controls followed by comfort controls. Once Zone-equipment input is filled and before `ControlledZonesChecked`, missing Zone configuration emits Severe plus Continue diagnostics and sets the routine-owned sticky `ErrorsFound`. Independent demand-management branches clamp ordinary controls with strict `>`/`<` comparisons and comfort controls with inclusive `>=`/`<=` comparisons; selected single heat/cool and single-heat-cool comfort paths also rewrite the ordinary control type/report, and the later comfort loop can overwrite ordinary-loop state. The fatal check occurs after both loops and their reached clamp mutations; only a normally reached final tail sets `ControlledZonesChecked = true`. No entry clears `ErrorsFound` and no status, catch, cleanup, rollback, shape reconciliation, or transaction exists. A non-return can retain an allocation/output, environment-reset, diagnostic, or clamp prefix according to the already committed latches; same-state error retry can repeat validation diagnostics and fatal, while clean replay requires coordinated predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, ZoneControls, ZoneEquipment, Surface, environment, and OutputProcessor reset. Four direct C++ unit calls use CP199 only as setup with begin-environment, comfort, demand, and validation behavior inactive and assert none of its state; focused boundary, warning, output-bundle, partial-failure, retry, and reset coverage is absent. Rust's `init_zone_air_set_points_compat` is an identity closure used only in the Predict scaffold and omitted from its Correct shell. Its separate constant-DualSetpoint heating/cooling observed series and limited `ZoneSysEnergyDemand` snapshots implement neither CP199 allocation, output binding, lifecycle, validation, demand clamps, nor selector-independent topology. CP199 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 207 routines, split 58 `state_mapped` plus 149 `source_mapped`, with 84 required; the heat-balance project list becomes 53. CP200 adds required source-mapped `routine.zone_space_heat_balance_begin_environment_init` immediately after `routine.init_zone_air_set_points` and adds `zone_space_heat_balance_begin_environment_init` after `init_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::beginEnvironmentInit(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 213 and implemented at `ZoneTempPredictorCorrector.cc` lines 2818-2836. Its only two direct call expressions are CP199 lines 2624 and 2628 inside `MyEnvrnFlag && BeginEnvrnFlag`: every stored Zone record runs first, then every stored Space record only when current `doSpaceHeatBalance` is true. The helper owns no identity, count, gate, or latch. For each fixed index 0 through 3 it writes `ZTM[i] = 0`, `WPrevZoneTS[i] = OutHumRat`, `DSWPrevZoneTS[i] = OutHumRat`, and `WPrevZoneTSTemp[i] = 0` in that order; it then writes `WTimeMinusP`, `W1`, `WMX`, and `WM2` from `OutHumRat` before zeroing `airHumRatTemp`, `tempIndLoad`, `tempDepLoad`, `airRelHum`, `AirPowerCap`, and `T1`. These are 26 overwrites: 12 outdoor-humidity values and 14 zeros. CP200 reads no prior target value and leaves current/average air humidity, MAT/MRT/ZT and their other histories, coefficients outside the two load scalars, and every unlisted record field unchanged. All four indexed targets are fixed `std::array<Real64, 4>` values. Outdoor humidity is copied without finite, sign, range, or consistency validation, so negative, infinite, and quiet-NaN values propagate to all 12 humidity targets without a clamp or diagnostic. There is no allocation, checked access, status, diagnostic, child call, catch, cleanup, or rollback, and valid state has no ordinary catchable failure path. Complete repeat with unchanged outdoor humidity is overwrite-idempotent; a changed value replaces all 12 humidity targets. CP199 alone controls frequency and clears its environment latch after the entire parent reset, so a Space mode that becomes true later in the same uninterrupted BeginEnvironment interval does not replay skipped Space records. No C++ unit test calls CP200 directly; the four direct CP199 fixtures have `BeginEnvrnFlag` false. Of 56 active full-simulation tests that reach CP199, 55 execute at least one Zone CP200 call; `WeatherManager_SetRainFlag` has zero Zones and executes none. Seven `SizingManager` tests also reach sizing-Space and one `HeatBalanceAirManager` test reaches simulation-Space, but none asserts CP200 fields. Rust creates only Zone state at run initialization with configurable three-slot temperature histories and fixed-default three-slot humidity histories; when a weather series exists, it performs a one-time first-weather-sample seed of current and averaged humidity plus both three-slot histories. That seed touches a different field set and is not CP200's current-`OutHumRat` four-slot per-environment Zone/Space reset. Rust lacks the exact four-slot `ZTM`, `WPrevZoneTS`, `DSWPrevZoneTS`, and `WPrevZoneTSTemp` set, Space records, 26-field touched-versus-retained boundary, and environment gate. Its coefficient representation is initialized and computed through a different path; these adjacent histories and coefficients are not CP200 parity. CP200 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 208 routines, split 58 `state_mapped` plus 150 `source_mapped`, with 85 required; the heat-balance project list becomes 54. CP201 adds required source-mapped `routine.zone_space_heat_balance_set_up_output_vars` immediately after `routine.zone_space_heat_balance_begin_environment_init` and adds `zone_space_heat_balance_set_up_output_vars` after `zone_space_heat_balance_begin_environment_init` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::setUpOutputVars(EnergyPlusData &state, std::string_view prefix, std::string const &name)` is declared at `ZoneTempPredictorCorrector.hh` line 215 and implemented at `ZoneTempPredictorCorrector.cc` lines 2838-2868. Its only production call expressions are CP199 line 2443 for every Zone and lines 2446-2447 for each stored Space membership only when `doSpaceHeatBalanceSimulation` is true; Zone and nested Space calls follow parent Zone order, while sizing-only Space mode does not enter this child. Each call registers, in order, `{Zone|Space} Air Temperature` bound to `ZT` as C/System/Average, `Air Humidity Ratio` bound to `airHumRat` as Units::None/System/Average, `Air Relative Humidity` bound to `airRelHum` as percent/System/Average, and `Mean Radiant Temperature` bound to `MRT` as C/Zone/Average, keyed by the source Zone or Space name. The optional meter fields remain invalid or empty, multipliers remain one, SQL index remains -999, and the report-frequency argument defaults to Hour before any matching request overrides its frequency and schedule. CP201 writes none of the four members, but OutputProcessor initialization, request parsing, dictionary insertion, setup/total counters, requested keyed OutVar pointers, report identifiers, and report-dictionary sinks are persistent side effects. Rows with neither a report request nor a DataOutputs variable-list match still create or reuse dictionary rows and advance setup/total counters without an OutVar. A variable-list-only match appends a keyed dummy pointer, DD link, and report identifier without emitting a report dictionary row; a report request additionally marks the entry for reporting and writes its dictionary sinks. The dictionary deduplicates case-insensitive name plus units only, while direct re-entry repeats the applicable counter, dummy or requested entry, link, identifier, and sink effects. No identity, membership, prefix, key, lifecycle, guard, status, catch, cleanup, or rollback is local. A formatting, initialization, request-input, allocation, or report-write non-return can retain any completed prefix while CP199's one-time latch remains true until line 2618; safe replay requires coordinated predictor/corrector record and OutputProcessor ownership rather than either reset alone. There is no direct CP201 unit call or positive registration assertion. Four direct CP199 fixtures and 55 active full-simulation tests execute at least one Zone setup indirectly; one additional no-Zone `WeatherManager_SetRainFlag` simulation reaches CP199 but executes no CP201 call. Only `HeatBalanceAirManager_GetMixingAndCrossMixing` reaches simulation-Space setup, for three stored Spaces, while seven sizing-Space tests do not. Rust's registry registers adjacent `Zone Mean Air Temperature`, while its heat-balance ResultStore separately stores `Zone Mean Air Temperature` and `Zone Mean Air Humidity Ratio`; neither registers or stores CP201's exact eight-name Zone/Space output set, four member bindings, System-versus-Zone timestep metadata, Space records, or registration lifecycle. Exact `Zone Air Temperature` and `Zone Air Humidity Ratio` constants in the IdealLoads CLI are ESO oracle input identities; the humidity name is also reused for a diagnostic comparison label, but neither is a RuntimeOutputRegistry or ResultStore production definition. CP201 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 209 routines, split 58 `state_mapped` plus 151 `source_mapped`, with 86 required; the heat-balance project list becomes 55. CP202 adds required source-mapped `routine.predict_system_loads` immediately after `routine.zone_space_heat_balance_set_up_output_vars` and adds `predict_system_loads` after `zone_space_heat_balance_set_up_output_vars` before `update_final_surface_heat_balance` in the heat-balance project contract. `PredictSystemLoads(EnergyPlusData &state, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 276-280 and implemented at `ZoneTempPredictorCorrector.cc` lines 2870-3145. Its only production call expression is CP195 line 227; the initial and downstepped `HVACManager` PredictStep sites plus `SimulationManager::Resimulate` enter through that wrapper after CP196 and CP199 return. Four phases execute in order. The staged loop samples both base schedules, uses `MAT` or shortened `XMPT`, increments warning or recurring-warning state and clamps heat to cool minus 0.1 when heat is not below cool, chooses the last qualifying negative cooling or positive heating `StageNum`, and applies the source-literal throttling setpoint. The heating branch notably compares its offset with half `CoolThroRange` while shifting by half `HeatThroRange`. Active Space heat balance copies the Zone stage to every stored Space membership. The on/off prefix is gated by `NumOnOffCtrZone` but scans every temperature-controlled Zone having positive `DeltaTCutSet`, saves or restores last-mode flags according to `ShortenTimeStepSys`, clears both off flags, selects `MAT` or shortened `XMPT` only for ThirdOrder and `T1` otherwise, and revises SingleHeat, SingleCool, or both DualHeatCool setpoints with the literal strict comparisons around 0.02 C; `SingleHeatCool` and other switch values receive no setpoint write. An inclusive revised dual lower-greater-than-or-equal-upper collision emits severe and continuation diagnostics and then fatals before prediction dispatch. Every Zone then calls the still-source-mapped CP203 `ZoneSpaceHeatBalanceData::predictSystemLoad` with all three timestep arguments. Each stored Space calls that child when `doSpaceHeatBalance` is true; otherwise a shortened call copies only the returned Zone `MAT` and `airHumRat` to the Space, while a nonshortened call leaves it unchanged. `UseZoneTimeStepHistory` and `PriorTimeStep` are only forwarded locally. The final on/off pass derives `CoolModeLast` and `HeatModeLast` from the off flags and inclusive sign tests on child-produced `TotalOutputRequired`; zero can set both when both flags are true, while NaN clears both. There is no local latch, status, finite or topology validation, catch, cleanup, or rollback. Staged warnings and any completed setpoint, stage, save/restore, child, or Space-copy prefix persist across a fatal or child non-return; same-state retry samples schedules again, advances staged recurrence state, and repeats child effects. Two direct C++ fixtures make 16 calls and 24 setpoint assertions, covering selected Euler and ThirdOrder normal/shortened on/off cases only; global `NumOfZones` remains zero, so no Zone/Space child, forwarding, staged path, final-mode result, failure, retry, or reset contract is asserted. Of 56 active full simulations that reach CP202, 55 execute a Zone child and eight enable a Space child, but none is positive staged/on-off or direct CP202 state coverage. Rust retains the cutout delta only in typed thermostat input, never consumes it at runtime, has no staged control, `StageNum`, CP202 off/mode memory, sensible `ZoneSysEnergyDemand::TotalOutputRequired` field, or Space heat-balance record, and supplies only an identity `predict_system_loads_compat` wrapper around a different bounded Zone-temperature update. Oracle-derived IdealLoads demand snapshots and a separate `predict_step_source_order_path` wrapper-order test do not implement this transaction. CP202 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 210 routines, split 58 `state_mapped` plus 152 `source_mapped`, with 87 required; the heat-balance project list becomes 56. CP203 adds required source-mapped `routine.zone_space_heat_balance_predict_system_load` immediately after `routine.predict_system_loads` and adds `zone_space_heat_balance_predict_system_load` after `predict_system_loads` before `calc_zone_air_temp_set_points` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::predictSystemLoad(EnergyPlusData &state, bool shortenTimeStepSys, bool useZoneTimeStepHistory, Real64 priorTimeStep, int zoneNum, int spaceNum)` is declared at `ZoneTempPredictorCorrector.hh` lines 217-222 and implemented at `ZoneTempPredictorCorrector.cc` lines 3146-3257. Its only production call expressions are CP202 line 3116 for every Zone and lines 3119-3120 for every stored Space only while `doSpaceHeatBalance` is true. After a positive-`zoneNum` debug assertion, CP203 first calls the still-deferred `updateTemperatures` child with all timestep/history arguments and both identities. It then selects Space volume only for positive `spaceNum`, otherwise Zone volume, and writes `AirPowerCap` from that volume, the parent Zone's sensible capacitance multiplier, current pressure/MAT/humidity psychrometrics, and unguarded `TimeStepSysSec`. After `calcZoneOrSpaceSums(state, false, zoneNum, spaceNum)` returns, a Zone-only hybrid branch clears and refreshes `SumIntGainExceptPeople`. CP203 directly derives `TempDepCoef`, `TempIndCoef` including `SysDepZoneLoadsLagged`, the fixed three-history `TempHistoryTerm`, and ThirdOrder-shaped `tempDepLoad`/`tempIndLoad`. A used RoomAir AirflowNetwork control node under the state-wide nonmixing flag first runs `LoadPredictionRoomAirModelAFN`, then overwrites those coefficient, capacity, history, and load values and optionally obtains `HVAC(1).SupplyFraction` as local `RAFNFrac`; this Zone-keyed override is not suppressed for a Space record. CP203 next clears shared `ShortenTimeStepSysRoomAir`. ThirdOrder performs no further direct history rewrite. Every other solution algorithm uses `shortenTimeStepSys && TimeStepSys < TimeStepZone` to select this record's `TM2`/`WM2` when shared `PreviousTimeStep < TimeStepZone`, otherwise `TMX`/`WMX`, sets corresponding AirflowNetwork-node T1 histories, and sets the shared flag true; the other path copies current `ZT`/`airHumRat` and current AFN-node values while leaving it false. These later AFN history copies test only the Zone model type, without the earlier nonmixing or `IsUsed` guards. Non-ThirdOrder finally replaces both load scalars with the plain coefficients. The last ordered calls are `calcPredictedSystemLoad` and then `calcPredictedHumidityRatio` with `RAFNFrac` and both identities. Their thermostat, sensible-demand, humidity-control, moisture-demand, diagnostic, and reporting behavior, together with temperature updating, sum assembly, psychrometrics, hybrid gain collection, and AFN load prediction, remains child dependency behavior rather than CP203 implementation credit. Apart from the debug assertion, CP203 has no local identity, upper-bound, membership, finite, timestep, volume, multiplier, node, or history validation and no local diagnostic, latch, status, catch, cleanup, or rollback. An abnormal child return can retain any completed history, capacity, hybrid clear, coefficient, AFN, shared-flag, record-history, node-history, or sensible-demand prefix; a moisture-child failure follows the already committed sensible child. Same-state retry reruns every child and direct write, and clean replay requires coordinated Zone/Space records, HVAC timestep/global state, HeatBalFanSys and node histories, RoomAir/AFN, HybridModel/internal gains, sensible/moisture demand, diagnostics, and every child owner. No C++ test calls CP203 directly; CP202's 16 focused calls retain zero Zones and never enter it. Of 56 active full simulations reaching CP202, 55 execute at least one Zone CP203 and eight enable Space CP203, but their downstream assertions do not isolate this transaction and none covers its AFN override. Rust's identity `predict_system_loads_compat` encloses a different Zone-only temperature update. Existing coefficient, moist-air-capacitance, analytical/ThirdOrder, history-interpolation, and no-OA ThirdOrder moisture helpers cover adjacent formulas only; their guarded ownership omits CP203's Space path, lagged/hybrid/AFN overrides, shared room-air shortening flag, exact T1/W1 and AFN-node history transaction, and ordered sensible-then-moisture demand children. CP203 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 211 routines, split 58 `state_mapped` plus 153 `source_mapped`, with 88 required; the heat-balance project list becomes 57. CP204 adds required source-mapped `routine.calc_zone_air_temp_set_points` immediately after `routine.zone_space_heat_balance_predict_system_load` and adds `calc_zone_air_temp_set_points` after `zone_space_heat_balance_predict_system_load` before `zone_space_heat_balance_calc_predicted_humidity_ratio` in the heat-balance project contract. `CalcZoneAirTempSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 282 and implemented at `ZoneTempPredictorCorrector.cc` lines 3259-3460. Its only production call expression is CP195 line 224 for the `GetZoneSetPoints` selector; both ordinary HVAC timestep setup and HVAC resimulation enter through that wrapper after CP196 and CP199 return. Every call first assigns the complete `TempControlType` array to Uncontrolled, conditionally allocates but never resizes the two occupied-setpoint arrays, then fills every existing heating entry with zero and cooling entry with 100. The local `DeltaT` is assigned zero and never consumed, while `TempControlTypeRpt` and the thermostat setpoint fields receive no global reset. In stored temperature-control order, CP204 trusts `ActualZoneNum` and schedule pointers, samples and casts the control-type schedule, writes the enum plus integer report, and dispatches five nominal cases. Uncontrolled leaves all setpoint fields stale. SingleHeat alone guards its family with `isUsed`, samples the heating schedule into generic `setpt`, snapshots the raw low thermostat value, applies operative-temperature adjustment, and writes only `setptLo`. SingleCool samples its cooling schedule without an `isUsed` guard, snapshots the raw high value, optionally applies adaptive comfort and snapshots `setptAdapComfortCool`, then applies operative-temperature and temperature-and-humidity overcool adjustments while leaving `setptLo` stale. SingleHeatCool samples the family's heating schedule, optionally applies adaptive comfort, applies operative control, copies the result to both bounds, and under allocated optimum-start state may replace generic `setpt` from the SingleHeat family day array at `(ceil(OccStartTime) + 1) * TimeStepsInHour` before an independent `OptStartFlag` copies it to both bounds. DualHeatCool samples and snapshots cooling first, optionally applies adaptive comfort, applies operative control, then samples, snapshots, and operative-adjusts heating; a matching optimum-start identity fills the globally reset occupied arrays from the dual day arrays, an independent flag copies those values into the bounds, and overcool runs last. The invalid-control default emits one Severe diagnostic and continues without a local fatal. After every ordinary record branch, an enabled non-warmup, non-sizing, non-kickoff thermostat-fault scan stops at the first case-insensitive thermostat-name match even when its availability is nonpositive; an available match samples optional severity and subtracts the resulting offset from generic, low, and high setpoints without changing raw or adaptive snapshots. This can accumulate on fields the selected branch did not rewrite. After the ordinary loop, comfort setpoints run conditionally and can overwrite ordinary/fault results, then the EMS override runs unconditionally with final precedence. Operative, adaptive, overcool, comfort, EMS, schedule, and fault helpers remain dependency behavior rather than CP204 implementation credit. CP204 has no Space path, local latch, status, catch, cleanup, rollback, topology/shape/index/schedule/enum/time/finite validation, or allocation reconciliation. Any allocation, schedule, diagnostic, or child non-return retains the reached reset, allocation, record, fault, comfort, or EMS prefix; same-state retry resamples schedules, resets occupied arrays, repeats children and diagnostics, and can compound offsets in untouched fields. Clean replay requires coordinated ZoneControls, HeatBalFanSys setpoint/type/report, Availability optimum-start, schedule, FaultsManager, global/environment/weather, Zone heat-balance MRT/RH, comfort/ThermalComfort, EMS, diagnostic, and child-owned state. Four C++ fixtures make 21 direct calls and 33 thermostat-field post-call assertions: the optimum-start fixture contributes one call/two assertions, the reporting fixture four/seven, and two cutout/prediction fixtures eight/twelve each. The latter calls are composed with CP202, and helper-only tests cover adaptive adjustment and EMS override; no direct helper test covers operative adjustment, overcool, or comfort calculation. Of 57 active full simulations, one expected EMS fatal stops before CP204 and the other 56 reach it; 38 contain a thermostat declaration by static input evidence, but none directly asserts post-CP204 setpoint or control state and none contains comfort, operative-temperature, overcool, optimum-start, or thermostat-fault input. Rust's `calc_zone_air_temp_set_points_compat` is an identity closure invoked once around an empty closure inside the Predict scaffold, rather than the source `GetZoneSetPoints` branch, and has no direct test. Rust types only a direct-Zone DualSetpoint thermostat subset; its IdealLoads diagnostic helper selects the first control, ignores the control-type schedule, and repeats constant schedule values outside the heat-balance runtime. It implements none of CP204's control-family selection, adjustments, optimum-start, fault, comfort, EMS, mutation order, partial effects, or owner lifecycle. CP204 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 212 routines, split 58 `state_mapped` plus 154 `source_mapped`, with 89 required; the heat-balance project list becomes 58. CP205 adds required source-mapped `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` immediately after `routine.calc_zone_air_temp_set_points` and adds `zone_space_heat_balance_calc_predicted_humidity_ratio` after `calc_zone_air_temp_set_points` before `correct_zone_air_temps` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::calcPredictedHumidityRatio(EnergyPlusData &state, Real64 RAFNFrac, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 243 and implemented at `ZoneTempPredictorCorrector.cc` lines 3462-3815. Its only production call expression is CP203 line 3256, after the sensible-demand child; every Zone and every active Space therefore runs CP205 in parent order. CP205 first derives two local RH setpoints and a controlled flag from the parent Zone. A positive `humidityControlZoneIndex` samples humidifying then dehumidifying schedules, applies independent EMS overrides, then scans the first name-matching humidistat fault outside warmup, sizing, and kickoff. A thermostat-dependent fault searches thermostat faults by object name, fatals when none exists, and only for a nonzero available severity-scaled thermostat offset converts both RH values through humidity ratio at record `MAT + offset` and back at `MAT` before clamping to [0,100]. Every other humidistat-fault type samples its own availability/severity, subtracts the offset, and clamps only when available. The first matching humidistat or thermostat fault stops its scan even when unavailable. A humidifying value above dehumidifying emits first-occurrence plus recurring warnings through the control-owned `ErrorIndex` and is reduced to the dehumidifying value; exact equality marks a single setpoint. Without a humidistat, `DoingSizing && DoLatentSizing` scans Zone equipment configurations only to the first controlled entry, does not match it to `zoneNum`, uses its matching `Sizing:Zone` or falls back to the first sizing input, and, when latent sizing is enabled there, samples or reads its RH values, silently reduces humidifying to dehumidifying, and marks the record controlled. Thus the same first controlled sizing input can drive every otherwise uncontrolled Zone and Space invocation. Controlled work adds record `latentGain` to Zone-indexed radiant-system and pool latent gains, computes moist-air density and vapor enthalpy, and assembles `A` and `B` either from Zone-indexed AirflowNetwork multizone exchange plus record surface-moisture terms or from record outdoor, ventilation, exhaust, cross-mixing, mixing, outdoor-air, and surface-moisture fields. `C` uses Space volume only for positive `spaceNum`, otherwise Zone volume, together with the parent Zone moisture-capacitance multiplier and unguarded system-timestep seconds. A parent Zone RoomAir AirflowNetwork model then replaces `A`, `B`, `C`, and vapor enthalpy from its control node without the earlier CP203 nonmixing or `IsUsed` gates, including during a Space invocation. For each RH setpoint CP205 converts record `ZT` and pressure to humidity ratio, then computes ThirdOrder, Analytical, or Euler load. ThirdOrder uses the first three `WPrevZoneTSTemp` values. Analytical uses the exact `A == 0` branch or reuses `exp(min(700, -A/C))` from the humidifying calculation for dehumidifying. Euler uses `C * (Wsp - W1) + A * Wsp - B`. Any positive `RAFNFrac` divides each load; zero, negative, or NaN does not. An unknown solution enum leaves both initialized loads zero. Exact-equal setpoints select humidifying load. Otherwise two positive loads select humidifying, two negative select dehumidifying, and the inclusive humidifying-nonpositive/dehumidifying-nonnegative deadband selects zero; every other combination emits Severe/Continue diagnostics and fatals before demand commit. The final identity branch selects Space demand only for positive `spaceNum`. Controlled state calls `reportMoistLoadsZoneMultiplier` with the parent Zone identity; that dependency stores three unmultiplied predicted rates, three Zone-multiplied demand values, and conditionally initializes sequenced equipment demand. Uncontrolled state directly zeroes only `TotalOutputRequired`, `OutputRequiredToDehumidifyingSP`, and `OutputRequiredToHumidifyingSP`, leaving raw predicted and sequenced fields stale. Counts, indices, membership, pointers, schedule/EMS/fault/sizing values, setpoint ranges, pressure, temperature, humidity, histories, volume, multiplier, timestep, flows, AFN topology, `RAFNFrac`, and finite values are otherwise trusted. Apart from the humidity-control debug assertion, CP205 has no local validation, latch, status, catch, cleanup, or rollback. A missing dependent thermostat or impossible load combination fatals before final demand write; schedule, psychrometric, allocation, diagnostic, or report-child non-return retains any earlier diagnostic, recurrence-index, or partial demand prefix. Zone demand can commit before a later Space failure. Same-state retry resamples schedules and faults, repeats calculations, advances recurring warnings, and repeats child writes; clean replay requires coordinated Zone/Space heat-balance records, ZoneControls and schedules/EMS, FaultsManager, sizing and Zone equipment, HVAC timestep, environment and psychrometrics, AFN/RoomAir, Zone/Space moisture demand including sequences, multipliers, and diagnostics. No C++ test calls CP205 directly. One helper-only fixture calls `ZoneSystemMoistureDemand::reportMoistLoadsZoneMultiplier` four times with 21 assertions, covering only Zone raw and multiplied values, not Space or sequenced output. Of 57 active full simulations, one expected EMS fatal stops before prediction and one zero-Zone weather case reaches CP202 without CP205; the other 55 execute Zone CP205 and eight also execute Space CP205. Their enclosing inputs contain no humidistat, humidistat fault, EMS humidistat override, or RoomAir AFN. Only one ThirdOrder latent-sizing configuration makes the controlled calculation positive for one Zone and three Spaces, and its assertions are downstream; six Analytical configurations and every other reached record take the uncontrolled zero path, while no Euler configuration exists. Rust has no heat-balance CP205 wrapper, call, Zone/Space moisture-demand owner, or runtime mutation. The separate `calc_no_oa_third_order_moisture_demand_compat` implements a guarded Zone-only no-outdoor-air `A = 0` ThirdOrder subset with record density/capacity, internal latent gain, two RH conversions, three-history term, sign/deadband choice, and one combined Zone multiplier. It has two non-test callers, two direct predictor tests, and one adjacent history-term test; a transactional fixed-one-step IdealLoads Humidistat wrapper adds seven tests and eight call expressions around predictor, PurchasedAir, corrector, and history push. That subset rejects invalid inputs through `Option`, clamps both RH fractions, and returns only multiplied loads. It omits source schedule/EMS/fault/sizing ownership, radiant/pool gains, airflow and surface moisture, AFN/RoomAir/RAFN scaling, Space state, Analytical/Euler, diagnostics and partial effects, uncontrolled zero writes, raw predicted values, and sequenced demand. Its CLI path still consumes EnergyPlus-seeded histories, temperatures, sensible demand, and pressure; existing manifest-specific no-OA IdealLoads evidence does not state-map CP205. CP205 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 213 routines, split 58 `state_mapped` plus 155 `source_mapped`, with 90 required; the heat-balance project list becomes 59. CP206 adds required source-mapped `routine.correct_zone_air_temps` immediately after `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` and adds `correct_zone_air_temps` after `zone_space_heat_balance_calc_predicted_humidity_ratio` before `zone_space_heat_balance_correct_air_temp` in the heat-balance project contract. `correctZoneAirTemps(EnergyPlusData &state, bool useZoneTimeStepHistory)` is declared at `ZoneTempPredictorCorrector.hh` lines 289-291 and implemented at `ZoneTempPredictorCorrector.cc` lines 3817-3861. Its only production direct call is CP195 line 230, whose CorrectStep arm assigns the returned value to caller-owned `ZoneTempChange` only after CP206 returns. The ordinary HVAC parent runs initial Get, Predict, HVAC simulation, and then Correct. It selects adaptive downstepping only when the initial returned maximum exceeds `MaxZoneTempDiff` and `KickOffSimulation` is false; otherwise it uses one system step and Zone-timestep history. Each fine step repeats Predict, HVAC simulation, Correct, contaminant correction, and system-history push without recalculating the chosen count from later returns. Demand resimulation calls Get and Predict but not Correct. CP206 initializes its local maximum to zero and traverses Zones in ascending identity. For each Zone it first calls `ZoneSpaceHeatBalanceData::correctAirTemp` with the history selector and saves the result without folding it. It then visits every stored Space identity regardless of Space-HB flags. `doSpaceHeatBalanceSimulation && !DoingSizing` calls the Space correction child and immediately folds that result in Space order. Every other case may copy the controlled Zone system node Temp, HumRat, and Enthalpy to the Space node when `doSpaceHeatBalanceSizing && Zone.IsControlled`, a condition with no direct `DoingSizing` test, and then always copies Zone `ZT`, complete `ZTM`, `MAT`, `airHumRat`, and `airRelHum` into the Space record while deliberately omitting `ZTAVComf`. Only after all Spaces does CP206 fold the saved Zone result. It then always calls `CalcZoneComponentLoadSums` for the Zone, and when simulation Space HB is enabled it makes a second stored-Space traversal and calls that report child even during sizing when Space correction was bypassed. The history selector is only forwarded; shortening and prior-timestep inputs never enter CP206. The wrapper applies no absolute value, tolerance, or finite guard. Normal child returns are nonnegative absolute changes, but with the accumulator as the first `std::max` argument and every candidate second, negative and NaN candidates are ignored, positive infinity can be retained, and nonpositive Zone count returns zero. CP206 directly persists only the optional three Space-node values and five mirrored Space-HB values; correction, moisture, hybrid, and component-report mutations remain child behavior. It has no local assertion, count, identity, membership, arena, node, finite, or topology validation, diagnostic, stop flag, status, latch, catch, cleanup, transaction, or rollback. A Zone-child non-return suppresses that Zone remainder and later Zones. A Space-child failure retains the completed Zone child and earlier Spaces but prevents the Zone fold and reports. Node/mirror failure can retain a direct-write prefix, and report failure retains completed correction, copies, and earlier report effects. The local maximum is not externally visible on failure, and CP195 leaves the old caller value because its assignment has not completed. Same-state retry restarts at Zone one and repeats all children, copies, and reports; clean replay requires coordinated predictor/corrector, Zone/Space heat balance, node, topology, RoomAir, HVAC, report, diagnostic, and child-owner reset. One C++ HybridModel fixture makes five direct calls, all with Zone history true, one Zone, one stored Space, and false Space-HB flags. It therefore reaches Zone correction and five-field Space mirroring, but its five assertions check only hybrid child effects and never the returned maximum, mirror state, component reports, false-history path, failure, retry, or reset. Of 57 active full simulations, one expected EMS fatal stops before CP206, one zero-Zone weather case reaches the zero return, and 55 execute Zone correction. One Analytical configuration positively reaches active simulation-Space correction and Space reporting for two Zones and three Spaces, while seven sizing configurations reach the controlled node triplet and five-field mirror for one Zone and three Spaces each; their assertions are downstream topology or sizing evidence. One thermal-comfort simulation checks later Zone averages, but no test isolates CP206 maximum folding, adaptive step selection, component-report dispatch, failure, retry, or reset. Rust defines `correct_zone_air_temps_compat` only as a generic identity alias over `correct_step_source_order_path`. Its one non-test call and zero direct tests wrap a closure that returns unit, performs an all-Zone temperature pass followed by an all-Zone humidity pass, then optionally performs per-Zone adaptive correction or local history synchronization. It has no Space HB or node state, AirReportVars, component-report child, source history-selector input, or returned global maximum. The lower CorrectStep identity has additional calls during surface iteration and adaptive work that bypass the named CP206 wrapper. Rust chooses adaptive substeps independently per Zone from a hard-coded temperature threshold and repeats only local temperature/humidity work rather than the source global maximum and full Predict-HVAC-Correct retry. Existing coefficient, ThirdOrder, Analytical, history, storage-report, and bounded official 1ZoneUncontrolled evidence belongs to adjacent Zone-only child formulas and case-specific outputs, not CP206 topology or lifecycle parity. Authored Space execution, sizing, EMS, Python, and AirflowNetwork remain run-blocked. CP206 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 214 routines, split 58 `state_mapped` plus 156 `source_mapped`, with 91 required; the heat-balance project list becomes 60. CP207 adds required source-mapped `routine.zone_space_heat_balance_correct_air_temp` immediately after `routine.correct_zone_air_temps` and adds `zone_space_heat_balance_correct_air_temp` after `correct_zone_air_temps` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctAirTemp(EnergyPlusData &state, bool useZoneTimeStepHistory, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` lines 236-239 and implemented at `ZoneTempPredictorCorrector.cc` lines 3863-4165. Its only production direct call expressions are CP206 line 3825 for each Zone with default zero Space identity and line 3831 for positive active Spaces under `doSpaceHeatBalanceSimulation && !DoingSizing`; the Zone always precedes eligible Spaces. CP207 starts its local delta at zero, debug-asserts only positive `zoneNum`, reads the parent Zone multiplier product and system-timestep seconds, then copies complete `DSXMAT`/`DSWPrevZoneTS` histories when the selector is false or `XMAT`/`WPrevZoneTS` when true. Positive `spaceNum` alone selects Space volume; otherwise Zone volume. It overwrites `AirPowerCap` from that volume, parent sensible-capacitance multiplier, density at pre-correction `MAT` and `airHumRat`, specific heat, and unguarded timestep seconds. Exact zero alone calls `RoomAir::ManageAirModel`, then every identity calls `calcZoneOrSpaceSums` with the corrector flag true. `FlagHybridModel_PC` refreshes the current record's `SumIntGainExceptPeople` from the parent Zone without a Space guard. Positive Space alone selects its system node; a positive node, not `Zone.IsControlled`, selects the controlled branch. Controlled `TempDepCoef` is `SumHA + SumMCp + SumSysMCp`, while `TempIndCoef` adds internal and surface terms, non-system and system temperature terms, divided non-air response, lagged system load, and optional parent-Zone AFN sensible exchange and duct loss. The uncontrolled branch omits system flow, non-air response, and lagged system load but retains optional AFN and duct additions. Both branches dispatch unguarded ThirdOrder, Analytical, or Euler equations. Analytical tests exact zero dependence and caps its exponent with source-ordered `min(700, candidate)`; an unknown enum silently retains old `ZT` and continues. In the controlled node stage, a globally mixed or inactive model writes node temperature, Zone-only thermostat temperature, and correction factor one. Three-node displacement or UFAD has precedence and, like active UserDefined or one-node displacement, leaves node and thermostat untouched while conditionally computing the Zone-shared correction factor from supply temperature and existing node temperature; ordered `max(-3, raw)` then `min(3, value)` maps NaN raw to -3 and clamps infinities. AirflowNetwork replaces `ZT` from the parent control node and performs mixed writes; fallback also performs mixed writes. A Space uses its own node but parent RoomAir flags and can overwrite the Zone-shared correction factor after the Zone child, while never writing the thermostat. Controlled `SNLoad` uses `SumSysMCpT`, node mass flow and temperature divided by the Zone multiplier, record humidity specific heat, non-air response, and lagged load. The uncontrolled branch leaves zero load and shared node/control fields stale; only an exact uncontrolled Zone under global nonmixing and the AirflowNetwork enum replaces `ZT`, without testing `SimAirModel`. Exact Zone identity next conditionally calls `InverseModelTemperature` for enabled temperature inverse modes outside warmup and sizing. This occurs after node and load calculation, can replace `ZT` with measured temperature, and reads the global history flag rather than CP207's argument, so later `MAT`, humidity, RH, and delta can disagree with earlier node, thermostat, and load state. CP207 then writes `MAT = ZT`, reports sensible load to Space demand only for positive identity and otherwise Zone demand, calls the later-defined `correctHumRat` child, copies `airHumRatTemp` to `airHumRat`, and computes `airRelHum` through `PsyRhFnTdbWPb` without a caller-side clamp. Sensible demand commits before humidity. Returned change treats every Space as mixed. Exact Zone nonmixed classification recognizes only unmixed three-node displacement or UFAD flags. ThirdOrder compares `ZT` with `ZTM[0]` or the two room-air histories; Analytical and Euler compare with `T1` or the corresponding room-air one-step histories. Unknown enum returns zero. The outer `std::max` has zero first, so NaN is suppressed and positive infinity retained; in a nested nonmixed fold, NaN in the first child suppresses a finite second child while a finite first child survives a NaN second child. A malformed negative Space identity is accepted and combines Zone volume, node, and demand with skipped exact-zero RoomAir, thermostat, hybrid, AFN override, and nonmixed classification; production never supplies it. Direct record writes are the selected histories, capacitance, optional except-People sum, coefficients, `ZT`, `MAT`, humidity, and RH; direct shared writes are applicable node temperature, Zone thermostat, and Zone-shared correction factor. RoomAir, sum assembly, hybrid, sensible demand, humidity, and psychrometric diagnostics remain dependencies. Apart from the Zone assertion, CP207 has no local identity, bound, membership, arena, node, history, enum, topology, multiplier, volume, time, denominator, or finite validation, diagnostic, latch, status, catch, cleanup, transaction, or rollback. A failure can retain any ordered prefix through history, capacity, sums, coefficients, node/control, hybrid, `MAT`, sensible demand, humidity, or final RH; any non-return prevents CP206 from folding the delta. Same-state retry recomputes from already-mutated record, node, RoomAir, correction-factor, demand, and hybrid history state, and clean replay requires coordinated reset of all those owners plus AFN, duct, HVAC/environment, diagnostics, and children. No C++ test calls CP207 directly. One HybridModel fixture reaches its controlled, mixed, default-ThirdOrder Zone path five times with history true and asserts only hybrid child outputs. Helper-only tests call `correctHumRat` ten times and `calcZoneOrSpaceSums` five times but do not compose CP207. Of 57 active full simulations, one EMS fatal stops before it and one has zero Zones; the other 55 reach 81 statically declared Zone records, split 55 controlled and 26 uncontrolled. Forty-nine configurations select ThirdOrder for 74 records, six select Analytical for seven, and none selects Euler. One Analytical configuration reaches two uncontrolled Zones and three uncontrolled Spaces, but no controlled Space. One AFN-distribution and one duct-loss configuration reach their optional terms without asserting CP207 coefficients. No full simulation declares RoomAir or HybridModel input, and no focused assertion covers false history, solver output, node/control writes, sensible demand, RH, returned delta, failure, retry, or reset. Rust has no named `correct_air_temp` wrapper or direct CP207 test. Its all-Zone surface-driven temperature corrector has four non-test calls and no direct test; its adaptive single-Zone corrector has one non-test call and no direct test. The live Correct closure performs one all-Zone temperature pass, then a separate all-Zone humidity pass, then project-specific adaptive/history work instead of CP207's per-record transaction. The coefficient helper has five non-test calls and one direct fixture; the public ThirdOrder helper has no production call and one direct fixture, its coefficient solver has four non-test calls and no direct test, and the Analytical helper has three non-test calls and one direct fixture. There is no Euler helper. Rust preserves limited normal-finite Zone coefficient, ThirdOrder-history, and Analytical algebra, but combines system and non-system flow without a controlled-node decision; those runtime flow fields have no production writers and surface `SumHATref` is fixed zero. It omits exact sum assembly, non-air and lagged loads, AFN, duct, Space state, nodes, RoomAir, thermostat, correction factor, sensible demand, hybrid, distinct `ZT`/`MAT`/`T1`, RH, and returned delta. Rust capacity omits the sensible-capacitance multiplier and Space selection, can retain stale state on rejected weather/psychrometrics, zeroes AirPowerCap for nonpositive inputs, guards a near-zero ThirdOrder denominator, uses epsilon rather than exact Analytical zero, and has no source unknown-enum behavior. Its history selector is recorded but not consumed, and its separate saturation-clamped/defaulting humidity approximation is not `correctHumRat`. Existing formula tests and bounded official `1ZoneUncontrolled` MAT evidence remain adjacent case-specific evidence. Authored Space, sizing, EMS, Python, and AFN paths remain run-blocked. CP207 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 215 routines, split 58 `state_mapped` plus 157 `source_mapped`, with 92 required; the heat-balance project list becomes 61. CP208 adds required source-mapped `routine.push_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_correct_air_temp` and adds `push_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 293 and implemented at `ZoneTempPredictorCorrector.cc` lines 4167-4185. Its sole built-in direct call is CP195 line 236; the sole built-in selector request is `HVACManager::ManageHVAC` lines 579-584 after all system steps and Zone/Space comfort-average snapshots, and before the separate contaminant push, last-system-step-count commit, and demand-manager update. The dispatcher common input/init prefix runs first, but the CP208 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP208 aliases the predictor/corrector state, visits Zones 1 through `NumOfZones`, calls the Zone record child first, and only under the current aggregate `doSpaceHeatBalance` flag visits that Zone's stored `spaceIndexes` in container order and calls the Space child with both identities. It does not independently scan all Spaces, sort, deduplicate, or validate counts, arenas, membership, or identity consistency; a nonpositive Zone count is a no-op. Sizing can set the aggregate flag from `doSpaceHeatBalanceSizing` and normal simulation resets it from `doSpaceHeatBalanceSimulation`. CP208 itself reads traversal state but performs no direct history or numerical writes; all four-slot record, psychrometric, RoomAir, and AFN mutations belong to CP209. It has no local assertion, diagnostic, status, latch, catch, cleanup, transaction, or rollback. A child non-return retains every earlier child and the failing child's prefix, suppresses later traversal, and same-state retry shifts already-shifted histories again. No C++ unit calls CP208, its record child, `ManageZoneAirUpdates`, or `ManageHVAC` directly. Of 57 active `ManageSimulation` call expressions, one expected EMS fatal stops before CP208; the other 56 reach the wrapper, including one zero-Zone case, while the remaining 55 collectively span a static census of 81 Zone identities. Eight configurations collectively enable a static census of 24 Space identities, one in simulation and seven during sizing, but no assertion isolates the Zone/Space history shift, order, failure, retry, or reset. Rust has the selector name and identity source-order/compat wrappers, but production never passes the PushZone selector and the dispatcher ignores its selector. The one live compat call runs at the start of the Predict closure, shifts only three Zone temperature and humidity slots, chooses average versus current input by a project-specific adaptive flag, and encloses predictor work rather than running after the HVAC system loop. Rust has no Space heat-balance arena, membership traversal, fourth history slot, or CP209 RoomAir/AFN and auxiliary record state. One generic wrapper test checks only the wrapper call label and order in a larger vector; one uniform-temperature timestep assertion cannot distinguish slot movement, and there is no direct compat or nonuniform humidity-history test. Separate IdealLoads humidity history is not CP208 parity. CP208 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 216 routines, split 58 `state_mapped` plus 158 `source_mapped`, with 93 required; the heat-balance project list becomes 62. CP209 adds required source-mapped `routine.zone_space_heat_balance_push_zone_timestep_history` immediately after `routine.push_zone_timestep_histories` and adds `zone_space_heat_balance_push_zone_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushZoneTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 245 and implemented at `ZoneTempPredictorCorrector.cc` lines 4187-4275. Its only direct production expressions are CP208 lines 4178 and 4181, with every Zone first and positive eligible Spaces afterward. After a debug-only positive-Zone assertion, it aliases the parent AirModel before any mutation. It interleaves descending four-slot `XMAT` and `WPrevZoneTS` shifts, inserts `ZTAV` and `airHumRatAvg` at slot zero, writes `XMPT = ZT`, commits `airHumRat` and `WTimeMinusP` from `airHumRatTemp`, then writes percent `airRelHum` from `PsyRhFnTdbWPb` using `ZT`, the newly committed humidity, barometric pressure, and diagnostic label `pushTimestepHistories`. The helper floors only its local working humidity to 1e-5, can clamp a comparison-detected out-of-range RH, and can propagate NaN; CP209 stores the original humidity and applies no caller-side finite or range guard. Exact zero Space identity alone enables RoomAir. Three-node displacement and UFAD shift four-slot Floor, occupied, and mixed temperature histories and copy current temperatures to their MAT fields. AirflowNetwork shifts four-slot temperature and humidity histories for every stored node. These branches test the enum without an activity guard. Every solution enum except exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` and `WM2/WMX` from averages; exact-Zone stratified RoomAir advances its three M2/MX pairs and AFN advances per-node T2/TX temperature and humidity pairs. ThirdOrder still performs the common record and Zone RoomAir/AFN four-slot work. Any nonzero Space identity, including a malformed negative value, skips shared RoomAir while retaining common and non-ThirdOrder record writes. CP209 has no upper-bound, record-kind, membership, allocation, enum, topology, pressure, or finite validation and no local diagnostic beyond the psychrometric dependency, status, catch, cleanup, transaction, or rollback. An abnormal non-return after record mutation, for example from malformed psychrometric dependency or RoomAir/AFN indexing state, can leave the common prefix through `WTimeMinusP` with stale RH or retain later record and RoomAir prefixes. Retry shifts already shifted histories again, and the later revert routine is not a full inverse. No C++ test directly calls CP209 or asserts any destination. Fifty-five completing nonzero-Zone configurations collectively span 105 static record identities: 95 ThirdOrder and 10 Analytical, with no Euler. All 81 Zone identities use Mixing RoomAir; 24 Space identities skip RoomAir, so every special RoomAir/AFN branch has zero active full-simulation reach. Tracked official one-Zone outputs are downstream-only and do not isolate this commit. Rust has no singular record helper, call, or test. Its one live plural-wrapper closure shifts only three Zone temperature and humidity slots before predictor work, conditionally chooses average versus current input, and lacks Space, the fourth slot, `XMPT`, temporary/previous-system humidity scalars, RH, non-ThirdOrder record state, and all RoomAir/AFN histories. One uniform history assertion cannot identify the shift; separate system-step and IdealLoads histories are not CP209 parity. CP209 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 217 routines, split 58 `state_mapped` plus 159 `source_mapped`, with 94 required; the heat-balance project list becomes 63. CP210 adds required source-mapped `routine.push_system_timestep_histories` immediately after `routine.zone_space_heat_balance_push_zone_timestep_history` and adds `push_system_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushSystemTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 295 and implemented at `ZoneTempPredictorCorrector.cc` lines 4277-4295. Its only built-in direct call is CP195 lines 238-240; its only built-in selector request is `HVACManager::ManageHVAC` lines 388-393 inside each system iteration's strict `TimeStepSys < TimeStepZone` branch, after Predict/HVAC/Correct and before the separate contaminant push, `PreviousTimeStep` commit, and current-step average accumulation. Equal-step and kickoff paths do not request it. The dispatcher common prefix runs first, but the CP210 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP210 visits Zones in ascending order, calls each Zone child before stored eligible Spaces under the current aggregate `doSpaceHeatBalance` flag, and does not independently scan, sort, deduplicate, or validate topology. It performs no direct history write; CP211 owns downstepped record, RoomAir/AFN, and non-ThirdOrder mutations. It has no local assertion, diagnostic, status, catch, cleanup, transaction, or rollback. An abnormal child non-return retains completed and failing prefixes, suppresses later traversal and the parent's following contaminant/time/average work, and retry shifts completed system histories again; Zone-timestep revert does not restore them. No C++ test directly calls CP210 or its child, and no destination assertion or tracked oracle proves the numerical adaptive gate; 55 completing nonzero-Zone configurations provide only potential topology of 81 Zone and 24 eligible Space identities, not execution evidence. Rust has the selector name and generic wrappers, but production never dispatches the PushSystem selector. Production has exactly three lexical compat sites. The feature-disabled site is exclusive with the adaptive helper; when adaptive correction is enabled, each Zone independently takes either the count-one or count-greater-than-one site, so both adaptive sites can execute in one multi-Zone timestep. Exactly one site executes per Zone, rebuilding three slots even at a full Zone step or committing after all locally chosen adaptive substeps rather than performing the source per-fine-step global Zone/Space pass. Rust lacks Space topology, a fourth slot, and CP211 RoomAir/AFN and auxiliary state; the source-order wrapper has one direct label/order test, while the compat wrapper and PushSystem selector dispatch have none. One indirect count-one three-slot sync test does not prove CP210. CP210 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 218 routines, split 58 `state_mapped` plus 160 `source_mapped`, with 95 required; the heat-balance project list becomes 64. CP211 adds required source-mapped `routine.zone_space_heat_balance_push_system_timestep_history` immediately after `routine.push_system_timestep_histories` and adds `zone_space_heat_balance_push_system_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushSystemTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 247 and implemented at `ZoneTempPredictorCorrector.cc` lines 4297-4370. Its only production calls are CP210 lines 4288 and 4291 for each selected Zone and eligible stored Space. After a debug-only positive-Zone assertion, CP211 interleaves descending four-slot `DSXMAT` and `DSWPrevZoneTS` shifts and inserts current `MAT` and `airHumRat`. Exact Zone identity under the global nonmixing flag also advances applicable Floor/occupied/mixed four-slot temperature histories or each stored AirflowNetwork node's four-slot temperature and humidity histories. Every solution value other than exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` from `MAT` and `WM2/WMX` from distinct `airHumRatTemp`; exact Zones additionally advance applicable stratified or AirflowNetwork two-slot state. ThirdOrder skips only this final stage. CP211 has no upper-bound, Space-sign, membership, record-kind, topology, node-count, enum, or finite validation and no diagnostic, status, catch, cleanup, transaction, or rollback. A late abnormal non-return retains an ordered record or RoomAir/AFN prefix, blocks the rest of CP210 and its parent's following contaminant/time/average work, and same-state retry shifts completed histories again; the following Zone-timestep revert does not restore this downstepped state. No C++ test directly calls CP211 or asserts a destination. Fifty-five nonzero-Zone configurations provide only conditional potential topology if one CP210 pass is selected: 105 records split 95 ThirdOrder and 10 Analytical, with no Euler; all 81 Zones are Mixing and 24 Spaces skip shared RoomAir, so special RoomAir/AFN branches have zero corpus potential. Rust has no singular record helper, Space arena, fourth slot, `airHumRatTemp`, non-ThirdOrder scalar state, RoomAir, or AFN histories. Its count-one and feature-disabled helpers rebuild three Zone slots from current and Zone-timestep histories even when source CP211 is absent, while adaptive count-greater-than-one shifts local three-slot arrays after each Zone-local correction outside the named wrapper and commits them only once after all local substeps. One source-order label test and one indirect count-one rebuild test do not establish CP211; there is no adaptive-count-greater-than-one commit test. CP211 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 219 routines, split 58 `state_mapped` plus 161 `source_mapped`, with 96 required; the heat-balance project list becomes 65. CP212 adds required source-mapped `routine.revert_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_push_system_timestep_history` and adds `revert_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `RevertZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 297 and implemented at `ZoneTempPredictorCorrector.cc` lines 4372-4389. Its only direct thermal source call is the CP195 dispatcher arm at lines 232-234, while all nine built-in `ManageZoneAirUpdates` call sites and the test tree request other selectors; EnergyPlus 26.1 therefore has no built-in CP212 timing or runtime gate. An external dispatcher request would still run the common input/init prefix, ignore shortening, history-selector, and prior-step arguments, and preserve `ZoneTempChange`. CP212 visits Zones ascending, calls each Zone child first, then under the current aggregate `doSpaceHeatBalance` flag visits stored Spaces in container order. It does not scan, sort, deduplicate, validate topology, or write history directly; CP213 owns the `XMAT`/`WPrevZoneTS` and optional RoomAir/AFN mutations. It never restores CP211 `DSXMAT`/`DSWPrevZoneTS`, non-ThirdOrder scalars, or downstepped RoomAir/AFN state. CP212 has no assertion, diagnostic, status, completion count, catch, cleanup, transaction, or rollback. A child abnormal non-return retains completed and failing prefixes and suppresses later traversal; same-state retry reapplies CP213's forward copies and is destructive. No C++ test directly calls the wrapper or child or asserts a destination, and all 57 active `ManageSimulation` expressions execute CP212 zero times. The 81 Zone plus 24 eligible Space records are only counterfactual once-requested topology, not full-simulation reach evidence; special RoomAir/AFN topology remains absent. Rust has the selector label and one source-order/compat identity pair, but its dispatcher ignores the selector. Its sole live compat site runs per Zone only on the adaptive count-greater-than-one path and conditionally overwrites current mean temperature and humidity when the new count differs; it shifts no Zone history, has no Space traversal, and can run despite the source branch being dormant. The one source-order label test has no state assertion, and the only focused adaptive test takes count one and never reaches the site. CP212 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 220 routines, split 58 `state_mapped` plus 162 `source_mapped`, with 97 required; the heat-balance project list becomes 66. CP214 adds required source-mapped `routine.zone_space_heat_balance_correct_hum_rat` immediately after `routine.zone_space_heat_balance_revert_zone_timestep_history` and adds `zone_space_heat_balance_correct_hum_rat` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctHumRat(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 241 and implemented at `ZoneTempPredictorCorrector.cc` lines 4433-4619. Its sole production expression is CP207 line 4128, after temperature, `MAT`, and sensible reporting but before record humidity/RH commit and returned delta. Each correction pass visits every Zone and only active non-sizing simulation Spaces; initial and adaptive fine-step HVAC corrections can repeat it. CP214 selects controlled, return-plenum, supply-plenum, or zero primary flow, then independently adds positive parallel-PIU leakage. The literal PIU loop uses global ordinals one through the stored list size rather than the identities in `leakageParallelPIUNums`. It builds latent gain, density, vapor enthalpy, default moisture `A/B`, optionally replaces them with AFN multizone exchange, uses parent-Zone volume for `C` even on Space calls, then independently adds AFN distribution and duct latent terms. ThirdOrder, Analytical, and Euler equations write `airHumRatTemp`; an unmatched enum retains its prior value. Strict negative and saturation clamps run before a parent-Zone RoomAir AFN control-node override that also affects Spaces and is not reclamped. Exact-Zone hybrid inference can mutate measured humidity state without changing the temporary solution; a positive selected node receives humidity before enthalpy, and latent sizing reports raw latent gain with earlier sensible demand. Positive Spaces retain parent Zone equipment, multiplier, capacity, AFN, duct, radiant/pool, and RoomAir context; malformed negative Space identities use Zone node/demand state while skipping only the exact-Zone hybrid gate. CP214 has only a debug positive-Zone assertion and no bounds, membership, topology, allocation, multiplier, timestep, pressure, denominator, enum, or finite validation, status, catch, transaction, or rollback. Failure can retain solved humidity, hybrid, node, or demand prefixes after the parent `MAT` and sensible writes while blocking final record humidity/RH and later traversal; retry can resample schedules and repeat histories, reports, and diagnostics. Ten direct C++ calls plus five indirect focused calls cover only Euler node humidity and hybrid inference slices. Fifty-five nonzero-Zone corpus configurations provide a static one-pass census of 81 Zone plus three active Space records, split 74 ThirdOrder and ten Analytical with no Euler. Fifty-five records are controlled and 29 have no controlled primary flow; plenum/PIU topology is absent, while AFN replacement can reach five Zones, AFN distribution three, and duct latent addition three without an isolating assertion. Runtime cadence is larger and no assertion isolates the full transaction. Rust has no singular Zone/Space helper. Its main and adaptive heat-balance humidity paths use three-slot history only, directly write Zone humidity, enforce a `1e-5` floor and `0.008` invalid fallback, and omit source moisture coefficients and lifecycle effects. The separate `correct_no_oa_third_order_humidity_ratio_compat` is a guarded single-Zone IdealLoads subset for one purchased-air supply with exact ThirdOrder `A/B/C` and clamps, but omits all other branches, Space/node/RH/hybrid/sizing ownership, and source failure semantics. Existing case-bounded humidity claims remain unchanged. CP214 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 222 routines, split 58 `state_mapped` plus 164 `source_mapped`, with 99 required; the heat-balance project list becomes 68. CP215 adds required source-mapped `routine.down_interpolate_4_history_values` immediately after `routine.zone_space_heat_balance_correct_hum_rat` and adds `down_interpolate_4_history_values` before `update_final_surface_heat_balance` in the heat-balance project contract. The first scalar-output `DownInterpolate4HistoryValues` overload is declared at `ZoneTempPredictorCorrector.hh` lines 299-308 and implemented at `ZoneTempPredictorCorrector.cc` lines 4621-4702. It snapshots three inputs by value, writes five referenced outputs in order, tests strict ratio-two then ratio-three tolerance bands, and uses a sequential recurrence for every other ratio without validating timesteps, shortening direction, integer ratio, finite values, or aliasing. Its only production expressions are the CO2 then generic-contaminant calls in `PredictZoneContaminants`; each Zone reaches them only on the first shortened adaptive predictor when the system-step count changed and the matching species is enabled. One direct C++ ratio-two call asserts five outputs. Three contaminant predictor fixtures keep shortening false, and all 57 active full-simulation expressions have contaminant simulation disabled, so focused indirect and corpus reach are zero. Rust's nearest helper returns only the first three thermal Zone history values, rejects nonpositive timesteps, has two adaptive temperature/humidity call sites and ratio-two/three/four tests, and owns no contaminant state or five-output reference transaction. CP215 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required; the heat-balance project list becomes 69. CP216 expands that same logical routine mapping to the independent array-return `DownInterpolate4HistoryValues` overload at `ZoneTempPredictorCorrector.cc` lines 4704-4736. It computes the raw old/new timestep ratio before output, writes four array elements in strict ratio-two, then ratio-three, then fallback order, and returns `oldVals[0]` only after the writes. The fourth input is never read, the third is used only for the ratio-two final output, and no timestep, finite-value, shortening, integer-ratio, or input/output alias validation is performed. A same-array call exposes live-reference aliasing and makes retry generally non-idempotent, while stable distinct-array replay overwrites deterministically. Seven production expressions in `ZoneSpaceHeatBalanceData::updateTemperatures` provide two base temperature/humidity calls per eligible Zone or active Space, three exact-Zone displacement/UFAD calls, and two calls per exact-Zone AFN node. They require shortening and a system-step count different from the prior Zone timestep, so normal HVAC timing permits only the first shortened fine-step candidate; a matching count reuses prior downstepped arrays. One direct C++ ratio-two call asserts the return, all four outputs, and four unchanged inputs. Focused wrapper calls have zero Zones, while the 55 completing nonzero-Zone corpus configurations leave adaptive entry unobserved; their conditional one-pass topology is 81 Zones plus 24 eligible Spaces, or 210 base calls, with zero stratified/AFN potential because all Zones are Mixing. Rust's nearest helper returns only three values by value, rejects nonpositive timesteps, and has Zone-only adaptive temperature/humidity ownership without the fourth output, separate scalar return, Space/RoomAir/AFN topology, node rollback, or alias behavior. CP216 adds no second routine, project-contract item, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion; counts remain 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required, and the heat-balance project list remains 69. CP217 adds required source-mapped `routine.inverse_model_temperature` and project-contract item after `down_interpolate_4_history_values`. `InverseModelTemperature`, declared at `ZoneTempPredictorCorrector.hh` lines 313-325 and implemented at `ZoneTempPredictorCorrector.cc` lines 4737-4951, samples measured temperature and resets the current mass multiplier, conditionally overwrites `ZT`, runs independent infiltration, internal-mass, and people inverse branches under global Zone-history selection, then shifts three measured histories even outside the date window or during adaptive fine-step calls. Its sole exact-Zone production expression is inside `correctAirTemp`; five indirect focused calls assert only one multiplier, two infiltration rates, and two zero people counts, while all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed `HybridModel:Zone`, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP217 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 224 routines, split 58 `state_mapped` plus 166 `source_mapped`, with 101 required, and the heat-balance project list becomes 70. CP218 adds required source-mapped `routine.process_inverse_model_multp_hm` and project-contract item after `inverse_model_temperature`. `processInverseModelMultpHM`, declared at `ZoneTempPredictorCorrector.hh` lines 327-333 and implemented at `ZoneTempPredictorCorrector.cc` lines 4953-4991, clamps only values below one, warns but does not cap values above 30, includes every value above one in persistent sum/count/average state, and owns a per-Zone first/recurring diagnostic index. Its sole production call is CP217 internal-mass inference; one direct five-call fixture has 26 assertions and proves an uncapped 50 is aggregated, one indirect call asserts only current multiplier 15.13, and all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed HybridModel object, inferred multiplier, aggregates, recurring index, exact output/table, runtime path, or focused test, and the raw object remains run-blocking. CP218 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 225 routines, split 58 `state_mapped` plus 167 `source_mapped`, with 102 required, and the heat-balance project list becomes 71. CP219 adds required source-mapped `routine.inverse_model_humidity` and project-contract item `inverse_model_humidity` immediately after `process_inverse_model_multp_hm`. `InverseModelHumidity`, declared at `ZoneTempPredictorCorrector.hh` lines 335-343 and implemented at `ZoneTempPredictorCorrector.cc` lines 4993-5131, unconditionally samples measured humidity, conditionally runs infiltration and people inverse balances, and always shifts measured histories. Its sole exact-Zone production call is inside `correctHumRat`; four indirect fixture calls assert only two ACH and two people results, while all 57 active full simulations configure no HybridModel. Rust has no typed `HybridModel:Zone`, measured humidity history, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP219 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 226 routines, split 58 `state_mapped` plus 168 `source_mapped`, with 103 required, and the heat-balance project list becomes 72. CP220 adds required source-mapped `routine.zone_space_heat_balance_calc_zone_or_space_sums` and project-contract item `zone_space_heat_balance_calc_zone_or_space_sums` after `inverse_model_humidity`. `ZoneSpaceHeatBalanceData::calcZoneOrSpaceSums`, declared at `ZoneTempPredictorCorrector.hh` lines 226-230 and implemented at `ZoneTempPredictorCorrector.cc` lines 5133-5281, reconstructs internal, non-system, system, and virtual surface sums for predictor and correction passes. It uses parent-Zone AFN/equipment/plenum/PIU context for Space records, includes the literal PIU-ordinal anomaly, volume-scales only uncontrolled-Space system sums, and can repeat child Window side effects. One five-call C++ Zone fixture has 12 bounded assertions; the 55 nonzero-Zone corpus configurations provide static one-pass censuses of 105 prediction and 84 correction records but no controlled-Space, plenum, or PIU topology. Rust has only adjacent Zone opaque-surface and OtherEquipment subsets with zero-initialized flow fields, no matching routine or Space/airflow topology, and no promotion follows. The inventory becomes 32 algorithms and 227 routines, split 58 `state_mapped` plus 169 `source_mapped`, with 104 required; the heat-balance project list becomes 73. CP221 adds required source-mapped `routine.zone_heat_balance_calc_sum_hat` and project-contract item `zone_heat_balance_calc_sum_hat` after `zone_space_heat_balance_calc_zone_or_space_sums`. `ZoneHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 254 and implemented at `ZoneTempPredictorCorrector.cc` lines 5283-5298, debug-asserts Zone identity, zero-initializes four return fields, and folds every stored Space child in container order without gating, sorting, filtering, deduplication, or membership validation. It owns no persistent write, but child Window effects can survive failure and repeat on retry. One CP220 fixture reaches it five times and asserts six aggregate surface values. The 55 nonzero-Zone corpus configurations give static prediction and correction censuses of 81 Zone aggregates dispatching 99 stored children per pass. Rust directly folds Zone opaque-Surface indexes into only HA/HATsurf/HATref=0, with no four-field result, Space topology, child dispatch, or exact test; existing one-Zone output evidence does not promote this routine. The inventory becomes 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list becomes 74. CP222 expands the existing required source-mapped `routine.zone_heat_balance_calc_sum_hat` to the independent Space override without a second routine or project-contract item because both C++ definitions share the unqualified source identifier `calcSumHAT`. `SpaceHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 259 and implemented at `ZoneTempPredictorCorrector.cc` lines 5300-5413, debug-asserts positive identities, zero-initializes four fields, visits the selected Space inclusive Surface range without filtering, applies ordered Window gains/report mutations and frame/divider/base convection, and dispatches ZoneMean, Adjacent, ZoneSupply, or silent-default reference-air logic using parent-Zone supply state. Its uncontrolled ZoneSupply branch is fatal, and its airflow/no-return `SurfWinHeatGain +=` side effect makes replay and nested-plus-explicit duplicate visits generally non-idempotent. No direct test exists; one CP220 fixture reaches a one-Space, three-non-Window child five times and asserts six surface aggregates, while the static corpus census is 123 prediction and 102 correction calls, or 225 combined. Rust's nearest Zone-only opaque Surface fold returns only HA/HATsurf/HATref=0 and has no Space result, Window runtime, reference switch, or side effects; existing one-Zone evidence does not promote this routine. Counts remain 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list remains 74. CP223 adds required source-mapped `routine.calc_zone_component_load_sums` and project-contract item `calc_zone_component_load_sums` after `routine.zone_heat_balance_calc_sum_hat`. `CalcZoneComponentLoadSums`, declared at `ZoneTempPredictorCorrector.hh` lines 345-348 and implemented at `ZoneTempPredictorCorrector.cc` lines 5414-5677, is the correction-only component-report update sequence. It zeroes ten target report fields, combines Zone-wide internal gains and parent-Zone airflow/equipment/plenum/PIU/non-air topology with receiver-HB temperatures, coefficients, and storage state, re-walks every parent-Zone Space Surface range for every Zone or active simulation-Space report, accumulates Window convection and CondFD enthalpy, computes ThirdOrder, Analytical, or Euler air storage, and conditionally mutates a parent-Zone shared imbalance-warning index. Controlled inlet work can overwrite four ADU report fields, repeated Space reports can leave the last Space calculation on shared ADUs, and the literal PIU loop again uses global ordinals rather than stored identities. No direct C++ test exists; five HybridModel wrapper calls reach only a Zone report and assert unrelated hybrid state. The 55 nonzero-Zone corpus configurations give a static one-correction census of 81 Zone plus three Space reports, or 84 calls, with 104 stored-Space range walks; no full-simulation assertion isolates the transaction. Rust has separate Zone-only internal-gain, surface-convection, and air-storage report helpers plus a hard-coded zero outdoor-transfer series, but no ten-field record, Space report arena, complete airflow/equipment/Window/PCM/diagnostic transaction, or failure lifecycle. Existing four-row one-Zone conformance remains bounded output evidence and does not promote this routine. CP223 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 229 routines, split 58 `state_mapped` plus 171 `source_mapped`, with 106 required; the heat-balance project list becomes 75. CP224 adds required source-mapped `routine.verify_thermostat_in_zone` and project-contract item `verify_thermostat_in_zone` after `routine.calc_zone_component_load_sums`. `VerifyThermostatInZone`, declared at `ZoneTempPredictorCorrector.hh` line 350 and implemented at `ZoneTempPredictorCorrector.cc` lines 5679-5700, conditionally executes CP196 under the shared `GetZoneAirStatsInputFlag`, clears the latch only after normal return, gates lookup on positive `NumTempControlledZones`, then exact-string scans the full allocated `TempControlledZone.ZoneName` arena and returns only membership. Its sole production call is `SetUpZoneSizingArrays` for each record whose cooling or heating method is exactly `FromDDCalc` once any equipment configuration is controlled; a false result only makes the caller issue a non-pulse warning. CP224 itself owns no diagnostic or mutation after input acquisition. A CP196 non-return retains its partial state and the true latch, so same-state retry can re-enter the non-idempotent loader. No direct C++ test exists; one focused setup fixture reaches two false results without an isolating assertion. Thirty-four of 57 active full simulations provide 48 direct-Zone DesignDay sizing records with matching thermostats and equipment connections, hence a static first-setup census of 48 true calls, but no assertion isolates the predicate, lazy latch, exact-name mismatch, warning, failure, retry, or reset. Rust has bounded normalized direct-Zone DualSetpoint records, graph edges, an IdealLoads diagnostic, and plan metadata, but no exact helper, full CP196 arena or lazy input, executable `Sizing:Zone` caller, or source lookup and failure lifecycle. CP224 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 230 routines, split 58 `state_mapped` plus 172 `source_mapped`, with 107 required; the heat-balance project list becomes 76. CP225 adds required source-mapped `routine.verify_controlled_zone_for_thermostat` and project-contract item `verify_controlled_zone_for_thermostat` after `routine.verify_thermostat_in_zone`. `VerifyControlledZoneForThermostat`, declared at `ZoneTempPredictorCorrector.hh` line 352 and implemented at `ZoneTempPredictorCorrector.cc` lines 5702-5713, directly exact-string scans the full allocated `ZoneEquipConfig.ZoneName` arena and returns only positive membership. It performs no input load, normalization, `IsControlled`, equipment-list, Zone-identity, count, or Space check and owns no write or diagnostic; an exact default-sentinel or manually corrupted uncontrolled-slot match can therefore return true. Its only call expressions are CP199 ordinary then comfort control loops, each deferred until Zone-equipment input is filled and gated by a state-lifetime checked latch. False results produce caller-owned Severe/Continue diagnostics and sticky `ErrorsFound`; both loops finish before fatal, and only a normal input-filled return commits the latch. CP225 itself is read-only and deterministic, but caught parent retry repeats missing diagnostics and fatal because the error remains true, the checked latch remains false, and earlier CP199 effects persist. No direct C++ test exists; four direct CP199 fixture calls are gated off. Thirty-eight of 57 active full simulations provide 52 matching direct-Zone ordinary thermostat/equipment records, yielding 52 true calls, zero false calls, and zero comfort calls without an isolating assertion. Rust independently normalizes a raw nominal-controlled marker, direct-Zone DualSetpoint thermostats, and typed ZoneId equipment connections, plus an IdealLoads-only dispatch validator, but has no preserved-name helper, cross-family ordinary/comfort check, deferred latch, or source diagnostic/failure lifecycle. CP225 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 231 routines, split 58 `state_mapped` plus 173 `source_mapped`, with 108 required; the heat-balance project list becomes 77. CP226 adds required source-mapped `routine.detect_oscillating_zone_temp` and project item `detect_oscillating_zone_temp` after `routine.verify_controlled_zone_for_thermostat`. `DetectOscillatingZoneTemp`, declared at `ZoneTempPredictorCorrector.hh` line 354 and implemented at `ZoneTempPredictorCorrector.cc` lines 5715-5861, performs clear-on-success one-time allocation and registration, latches calculation from six output names or `PerformancePrecisionTradeoffs`, then on every enabled accepted system timestep shifts an independent zero-seeded four-slot Zone history and detects only strict alternating three-difference swings beyond `0.15 C`. It publishes Zone and Facility-any durations, separately classifies allocated ASH55 occupancy and unguarded deadband state, and accumulates three state-lifetime performance-log scalars without an environment reset. Setup and mid-loop failure are non-transactional, and duplicate enabled calls shift and can accumulate again. No C++ test references CP226 state or output. Fifty-six of 57 active full simulations reach setup and statically register 411 values across 81 nonzero-Zone records plus one zero-Zone configuration, but none requests the variables, monthly report, or performance tradeoff, so the calculation body has zero corpus reach. Rust has adjacent three-slot MAT history, adaptive `0.3 C` step selection, IdealLoads-only occupancy/deadband concepts, and hourly MAT/debug output, but no exact helper, four-slot detector, activation latch, six output-name families, annual/perflog state, HVAC-manager caller, or focused test. CP226 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 232 routines, split 58 `state_mapped` plus 174 `source_mapped`, with 109 required; the heat-balance project list becomes 78. CP227 adds required source-mapped `routine.adjust_air_set_points_for_op_temp_cntrl` and project item `adjust_air_set_points_for_op_temp_cntrl` after `routine.detect_oscillating_zone_temp`. `AdjustAirSetPointsforOpTempCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 356 and implemented at `ZoneTempPredictorCorrector.cc` lines 5863-5897, returns first on a false global operative-control flag and then only on exact per-record None; otherwise it selects a scheduled or fixed MRT fraction, copies the supplied Zone MRT, and overwrites the referenced target with `(setpoint - f * MRT) / (1 - f)`. Normal input validates `f` in `[0.0, 0.9)`, but the routine has no identity, pointer, denominator, finite, or range checks; untargeted records default to Invalid/fixed zero rather than None once another Zone raises the global flag. Five parent call expressions cover one-call single modes and cooling-then-heating Dual mode before later optimum, humidity, fault, comfort, and EMS effects. Built-in execution occurs once per Zone timestep before system substeps, while demand resimulation can add passes; full-parent replay reloads raw schedules, but a direct duplicate routine call generally double-transforms. No direct C++ test exists. Twenty-one direct parent calls produce 46 global-off entries, and the 57-expression full corpus contains no operative object: its static 101 active-sweep entry opportunities all return before record, MRT, or formula work. Rust has bounded DualSetpoint graph and raw-schedule IdealLoads evidence but no typed operative object, global/per-record control state, fraction binding, Zone MRT, exact helper, live caller, or focused test; the raw operative object run-blocks. CP227 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 233 routines, split 58 `state_mapped` plus 175 `source_mapped`, with 110 required; the heat-balance project list becomes 79. CP228 adds required source-mapped `routine.adjust_operative_set_points_for_adap_comfort` and project item `adjust_operative_set_points_for_adap_comfort` after `routine.adjust_air_set_points_for_op_temp_cntrl`. `AdjustOperativeSetPointsforAdapComfort`, declared at `ZoneTempPredictorCorrector.hh` line 358 and implemented at `ZoneTempPredictorCorrector.cc` lines 5899-5964, consumes one of seven adaptive daily arrays outside DesignDay/HVACSizeDesignDay or a shared summer-design-day slot for those two environment kinds, then applies the source integer-truncated baseline and exact `-1` fallback before CP227 converts the operative target to an air setpoint. Three guarded parent expressions cover SingleCool, SingleHeatCool, and Dual high only and snapshot the pre-CP227 operative result. Four direct C++ calls prove two central-model values and two lower-than-baseline sentinel restorations without isolating the later exact-sentinel branch, while 21 parent calls and all 57 active full simulations yield zero production entries. Rust has no typed operative object, adaptive state, selector, environment branch, output, live caller, or test, and the raw object run-blocks. CP228 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 234 routines, split 58 `state_mapped` plus 176 `source_mapped`, with 111 required, and the heat-balance project list becomes 80. CP229 adds required source-mapped `routine.calc_zone_air_comfort_set_points` and project-contract item `calc_zone_air_comfort_set_points` after `adjust_operative_set_points_for_adap_comfort`. `CalcZoneAirComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` line 360 and implemented at `ZoneTempPredictorCorrector.cc` lines 5966-6329, performs first-use ThermalComfort initialization, resets and samples comfort control types, selects PMV schedules, executes NO/SPE/OBJ/PEO People dispatch through the next child, clamps and overwrites ordinary dry-bulb setpoints, and leaves EMS with final precedence. Function-scope object and occupant counts bias later Zones, OBJ/PEO SingleCool use the low `-999` sentinel, child endpoint equality can retain stale outputs, and warnings plus partial Fanger/People/setpoint writes survive retry. No direct C++ call or active full-simulation expression reaches it; two raw setup fixtures do not call the parent, and the one installed comfort ExampleFile is not adopted by the repository. Rust has no typed comfort thermostat/setpoint, PMV/Fanger state, inverse child, exact outputs, live caller, or test, and active raw comfort input run-blocks. CP229 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 235 routines, split 58 `state_mapped` plus 177 `source_mapped`, with 112 required, and the heat-balance project list becomes 81. CP230 adds required source-mapped `routine.get_comfort_set_points` and project-contract item `get_comfort_set_points` after `calc_zone_air_comfort_set_points`. `GetComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` lines 362-367 and implemented at `ZoneTempPredictorCorrector.cc` lines 6331-6415, always evaluates the impure forward Fanger model at the stored minimum then maximum dry-bulb bounds. Only a strict interior PMV target enters configurable global `SolveRoot` with residual tolerance 0.001 and limit 500; below/above targets select a bound; with ordered or equal endpoint PMVs exact equality preserves the referenced output, while reversed equality can select the opposite temperature bound, and a NaN target or any case making all three comparisons false also preserves the output. The solver repeats both endpoints, can evaluate 501 candidates because convergence precedes the strict limit check, returns the last evaluated candidate on an iterated `-1` but Tmin on a zero-candidate width `-1` or `-2`, and yields two, four, or five-through-505 total Fanger calls by path. Each trial can resample People schedules and room-air state, overwrite ThermalComfort scratch plus People/Fanger report state, and repeat child diagnostics; lower clamp and `-2` can return Tmin while the final report state reflects Tmax. SurfaceWeighted MRT first-use can mutate Surface `AE`/`enclAESum` and its latches, and a bad sum returns a MAT/surface average on the first trial but zero later, providing a concrete stateful path from the initial bracket to `-2`. The two solver warning count/index pairs are shared across all comfort Zones and People, persist across environments, and are suppressed during warmup even though calculation and child effects continue. All 12 production expressions are inside CP229, with one/two NO-SPE calls, N/2N OBJ or positive-PEO calls, and 2N/4N nonpositive-PEO-plus-fallback calls. There is no status, transaction, rollback, or latch, so partial child/output/diagnostic state survives failure and retries repeat effects. No unit test reaches CP230, all 21 direct parent calls skip comfort, and the 57 active full simulations contain no comfort input. Separate forward-Fanger and generic-root tests do not compose this inverse. A manual stock-26.1 ExampleFile run supplies only uninstrumented diagnostic evidence and is not adopted as a repository case. Rust has no comfort type, PMV/Fanger state, generic configurable root solver, diagnostics, live caller, or composed test; RawOnly comfort input still run-blocks, and an unrelated private IdealLoads bisection does not promote the boundary. CP230 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 236 routines, split 58 `state_mapped` plus 178 `source_mapped`, with 113 required, and the heat-balance project list becomes 82. CP231 adds required source-mapped `routine.adjust_cooling_set_point_for_temp_and_humidity_control` and project item `adjust_cooling_set_point_for_temp_and_humidity_control` after `get_comfort_set_points`. `AdjustCoolingSetPointforTempAndHumidityControl`, declared at `ZoneTempPredictorCorrector.hh` lines 369-372 and implemented at `ZoneTempPredictorCorrector.cc` lines 6417-6458, aliases both indexed records before its global and exact-None guards, chooses a scheduled or constant maximum range, caps only for a strictly positive high-low gap, samples Zone RH and an independent dehumidifying schedule, and under strict positive excess/ratio gates subtracts only from `setptHi`. Default Invalid records do not return; the object-count global flag plus null untargeted schedules creates a mixed-record failure dependency, while zero/reversed gaps, ObjexxFCL second-argument-on-unordered `min`, malformed values, and direct duplicate calls retain literal nonfinite, deadband, and non-idempotent behavior. The two SingleCool/Dual parent expressions run after adaptive/operative and optional optimum-start work and before fault, comfort, and final EMS overrides; SingleCool load still consumes the untouched scalar, and a later positive cutout path can rebuild high from the earlier raw snapshot. No direct C++ test exists; 20 indirect unit entries and all active full-corpus entries return at the false global guard because no exact modifier input is present. The sole installed exact ExampleFile is an upstream Constant-path simulation test, but this repository does not adopt it and the case neither requests the adjusted cooling-setpoint output nor directly asserts CP231. Rust has only adjacent DualSetpoint, Humidistat, psychrometric, and IdealLoads moisture paths, with no typed modifier, percent-RH/range/ratio state, mutable setpoint triple, exact helper, live caller, or test; the raw object run-blocks. CP231 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 237 routines, split 58 `state_mapped` plus 179 `source_mapped`, with 114 required; the heat-balance project list becomes 83. CP232 adds required source-mapped `routine.override_air_set_points_for_ems_cntrl` and project item `override_air_set_points_for_ems_cntrl` after `adjust_cooling_set_point_for_temp_and_humidity_control`. `OverrideAirSetPointsforEMSCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 374 and implemented at `ZoneTempPredictorCorrector.cc` lines 6460-6555, traverses every ordinary record then every comfort record, with heating before cooling. Single modes write scalar plus one bound, SingleHeatCool leaves scalar at cooling when both flags are active, and Dual writes only low/high; dispatch uses live per-Zone types, so record collisions and the later comfort loop resolve field-by-field without validation. Ordinary actuators bind under `Zone Temperature Control` with `[C]`, while comfort actuators bind under `Zone Comfort Control` with `[]` yet are copied directly into Celsius-backed fields. The unconditional parent-tail call follows ordinary modifiers, fault, and comfort processing, while later staged or positive-cutout prediction can replace fields from other/raw state. One direct unit test proves only ordinary and comfort Dual low/high writes; all indirect unit and active full-corpus visits retain false flags, and installed testfiles provide no exact actuator-active ExampleFile. Rust has no EMS engine, actuator state, comfort type, mutable setpoint triple, helper, or live caller, and all EMS input run-blocks. CP232 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 238 routines, split 58 `state_mapped` plus 180 `source_mapped`, with 115 required; the heat-balance project list becomes 84. CP233 adds required source-mapped `routine.fill_predefined_table_on_thermostat_setpoints` and project item `fill_predefined_table_on_thermostat_setpoints` after `override_air_set_points_for_ems_cntrl`. `FillPredefinedTableOnThermostatSetpoints`, declared at `ZoneTempPredictorCorrector.hh` line 376 and implemented at `ZoneTempPredictorCorrector.cc` lines 6558-6672, visits all authored SingleHeating, SingleCooling, SingleHeatingOrCooling, then DualSetpoint definitions and lets the first occurrence of each numeric Schedule ID own its winter, summer, or split synthetic rows in the LEED temperature-schedule table. Detailed schedules sample the first Wednesday at hours 11 and 23 with one selected-date DST shift and count exact matching Wednesday-profile values across 365/366 calendar-day rules; constants return final `currentVal` and every day. Every cell appends without a transaction or once latch; valid parsed names avoid case-sensitive base/synthetic collisions, while manual mixed-case state can still collide, and retry can diverge between last-wins rendering and first-wins retrieval. The sole end-simulation tabular caller runs even when table files are disabled. No C++ test calls CP233; 56 successful full simulations give unasserted static reach, while a helper-only test makes nine calls and 21 assertions. Rust has only adjacent DualSetpoint graph state, calendar-aware schedule series, and constant-schedule IdealLoads diagnostics; it has no four-family arena, source ID deduplication, seasonal query, predefined table store, exact helper, caller, or composed test, and summary-report input stays ignored. CP233 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 239 routines, split 58 `state_mapped` plus 181 `source_mapped`, with 116 required; the heat-balance project list becomes 85. CP234 adds required source-mapped `routine.fill_predefined_table_on_thermostat_schedules` and project item `fill_predefined_table_on_thermostat_schedules` after CP233. `FillPredefinedTableOnThermostatSchedules`, declared at `ZoneTempPredictorCorrector.hh` line 378 and implemented at `ZoneTempPredictorCorrector.cc` lines 6674-6766, visits materialized ordinary TempControlledZone records in stored order, always appends thermostat/control-schedule plus independently joined control-type/name cells, and conditionally appends heating/cooling schedule cells. Nonempty control-object names alone select fixed type slots; tuple-lexical sorting and per-column empty filtering can break positional alignment. The five-slot resize followed by move-append relies on valid-unspecified moved-from strings being empty in observed libraries. Append-only predefined entries have partial-failure, duplicate-retry, last-wins-render versus first-wins-retrieval, and separate reset behavior. One direct call has 24 assertions only for one type per Zone; 56 successful full simulations provide unasserted static reach across 52 records and 312 cells. Rust has only adjacent direct-Zone DualSetpoint graph state and no complete control arena, exact sort/joins, predefined System Summary store, caller, serializer, or comparator; summary-report input remains ignored. CP234 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 240 routines, split 58 `state_mapped` plus 182 `source_mapped`, with 117 required; the heat-balance project list becomes 86. CP235 next maps `ZoneSpaceHeatBalanceData::updateTemperatures`, declared at `ZoneTempPredictorCorrector.hh` lines 233-234 and implemented at `ZoneTempPredictorCorrector.cc` lines 6768-6833. GetBuildingData, complete GetZoneData, SetupZoneGeometry, GetSurfaceData, full GetHTSurfaceData, GetIncidentSolarMultiplier, and GetScheduledSurfaceGains remain source-mapped wrappers; local-weather consumers, other surface families, opposite-surface generation, surface reordering and Space surface lists, geometry realization, list-target expansion, comprehensive multiplier and scheduled-surface-gain consumption, space heat balance, reporting, and runtime convection remain deferred. Zone grouping, every local-environment definition, every authored Space or SpaceList, and every generated remainder Space fail closed before runtime; a sole generated whole-zone default remains inactive when no remainder is needed, including when every valid detailed surface explicitly references it. This entry does not add a numerical conformance claim."
support_boundary_addendum = "CP107 promotes the parent `ManageInternalHeatGains(state, true)` checkpoint only to required source mapping and state-maps a bounded `GetInternalHeatGainsInput` slice. After an error-free entry, `Compiler::parse_bounded_internal_heat_gains_input` preserves the source family order for direct-Zone `People` followed by direct-Zone `OtherEquipment`; a People diagnostic does not suppress the OtherEquipment scan within the same pass. Existing typed People and OtherEquipment arenas/name maps are the complete mapped state. Lights, ElectricEquipment, GasEquipment, HotWaterEquipment, SteamEquipment, ITE, Baseboard, CO2, ZoneList/Space/SpaceList expansion, generated instances, Space/Zone occupant totals, `setDesignLevel`, schedule-minimum validation, reporting, meters, EMS/output registration, runtime gain updates, persistent source input flags, re-entry behavior, exact global compiler short-circuiting after errors created inside this pass, numerical parity, and conformance remain unclaimed. CP108 maps only the `HeatBalanceManager.cc` lines 322-325 `AnyKiva` guard and `setupKivaInstances` call; the caller ignores the returned `ErrorsFound` boolean. Rust implements no Kiva setup target. `Foundation:Kiva`, `Foundation:Kiva:Settings`, regular material and Construction intake, complete Surface/Foundation topology, `SurfaceProperty:ExposedFoundationPerimeter` geometry, Kiva instance/aggregator/surface-map ownership, zone setpoint acquisition, weather/site/ground boundary state, convection algorithms, mesh and solver initialization, EIO/output/EMS state, diagnostic and early-return/return-value parity, subsequent runtime calculations, and conformance remain unclaimed. Existing typed `OutsideBoundaryCondition::Foundation` state and ground-like Rust runtime handling are not Kiva instance setup and do not promote this routine; CP108 intentionally mixes in no support-gate repair. CP109 maps and defers only the `ManageHeatBalance` lines 169-171 branch inside the once-only outer `ManageHeatBalanceGetInputFlag`: after `GetHeatBalanceInput` returns, `DoingSizing=true` copies `doSpaceHeatBalanceSizing` into mutable `doSpaceHeatBalance`. `doSpaceHeatBalanceSizing` originates from `GetProjectControlData` reading `ZoneAirHeatBalanceAlgorithm` alpha 2 with false default; the outer get-input flag remains set through the following octree and computed-geometry work and is cleared later at line 186. The separate `SimulationManager` post-sizing assignment from `doSpaceHeatBalanceSimulation` is not this checkpoint. Input typing/default/error parity, the sizing lifecycle, mutable mode consumption, flag clearing or re-entry, Space heat-balance arrays, HVAC/results, octree, computed geometry, EMS, runtime numerics, and conformance remain unclaimed. CP110 source-maps only the `ManageHeatBalance` lines 173-180 double guard: complete `TotSurfaces >= Dayltg::octreeCrossover` with crossover exactly 100, plus raw `Daylighting:Controls` count above zero, before passing the complete mutable Surface array to `surfaceOctree.init`. Source `init` retains only surfaces with at least three vertices and `IsTransparent=false`, uses a zeroed empty root when none survive, otherwise expands componentwise bounds to a centered uniform cube, recursively branches nodes holding more than 10 surfaces to maximum depth 255, keeps a surface in its parent when its bounds span a child boundary, and stores live Surface pointers. Rust has no octree target. Complete Surface count/order/families and generated surfaces, Daylighting:Controls typing, transparency schedules/EMS/plugin mutation, tree ownership/rebuild/clear/reference validity, traversal/intersections/PierceSurface, the following computed-geometry loop, daylighting/shading/solar behavior, runtime performance/numerics, and conformance remain unclaimed. Existing daylighting run-blocking and typed opaque surfaces do not promote this routine, and CP110 changes no object support, capability, graph, or runtime admission. CP111 invokes `Compiler::set_bounded_surface_computed_geometry` after the CP107 executable compiler slice while the intervening Kiva, sizing-mode, and octree checkpoints remain mapped or deferred. Any prior Error suppresses the pass. Each retained `BuildingSurface:Detailed` independently receives `Some(SurfaceComputedGeometry)` only for a finite, coplanar, nondegenerate three-vertex Triangle or conservative four-vertex Rectangle; every excluded Surface receives `None` without a diagnostic and does not suppress later eligible entries. The Rectangle gate preserves the strict 0.020 m diagonal-difference and inclusive cosine-of-89-degrees tests while adding positive finite wrap-edge requirements; every candidate also requires a finite nonzero source-ordered Newell normal, conservative relative coplanarity, and finite nonzero projected signed area. The attachment retains `SurfaceShapeCategory`, a nonnormalized four-coefficient plane, X/Y/Z projection with source tie precedence, projected vertices/bounds, strict negative-area tail reversal, wrap edges, and Rectangle projected edge-1/edge-4 squares. Source `SurfaceGeometry.cc::GetVertices` world-coordinate assignment, `ProcessSurfaceVertices` shape assignment, and `isRectangle` are dependencies only. CP111 adds no identity, name map, object count, graph edge, support/capability, runtime consumer, comparator, proof variable, or conformance. Complete Surface array/order/families/generated state, Convex/Nonconvex and slabs, full geometry validation/recovery and `isRectangle` lifecycle, exact partial mutation/defaults, one-time flag state, octree/PierceSurface consumers, runtime numerics/performance, reporting, and conformance remain unclaimed. CP112 maps and defers only the inline `ManageHeatBalanceGetInputFlag = false` assignment at `ManageHeatBalance` line 186. Source `HeatBalanceMgrData` defaults that flag true and `clear_state()` resets it true; it remains true through `GetHeatBalanceInput`, CP109 sizing, CP110 octree, and CP111 complete computed geometry, becomes false only on successful reach after that loop, and makes later calls on the same state skip the entire input tail. An earlier fatal or abnormal exit does not reach the clear. CP112 adds no synthetic routine row, Rust helper, model or runtime state, support/capability, manifest, comparator, proof variable, or conformance; ordinary one-shot Rust compilation and final `model = None` failure are not persistent per-state flag parity. Exact flag ownership/default/reset/re-entry behavior, abnormal/fatal semantics and partial side effects, input-tail ordering and effects, sizing and simulation environments, repeated calls, the following EMS and initialization sequence, timestep/warmup/runtime behavior, reporting, performance, numerics, and conformance remain unclaimed. CP113 source-maps the canonical generic `EMSManager.cc::ManageEMS` routine as non-required, with no Rust target or project-contract promotion. At `ManageHeatBalance` lines 189-194 the caller unconditionally dispatches it after the CP112 block and before `InitHeatBalance`, passing `BeginZoneTimestepBeforeInitHeatBalance`, caller-owned `anyRan`, and an explicitly absent optional program-manager index. EMS input/setup, the runtime language, programs and program-call managers, sensors, actuators, internal variables, trends, Erl, plugins, external interfaces, registered callbacks, calling-point gating and optional-index behavior, `anyRan` and run-flag values, mutations/reporting/diagnostics and exact execution order, repeated calls, sizing/simulation environments, timestep/warmup/runtime behavior, numerics, performance, and conformance remain deferred. CP113 adds no executable wrapper, model/runtime state, support/capability, manifest, comparator, proof variable, result, or conformance. CP114 records the unconditional line-198 `InitHeatBalance(state)` call and canonical `HeatBalanceManager.cc::InitHeatBalance` routine as required but only `source_mapped`. Its lines 2594-2821 use simulation-start, environment-start, begin-day, warmup, weather/report, detailed-solar, local-environment, and EMS flags to allocate/reset heat-balance state and order response-factor, surface-view-factor, window, daylighting, solar, storm-window, outdoor-condition, and EMS-controlled initialization. Existing Rust `init_heat_balance_stage` metadata and `init_heat_balance_source_order_path` identity wrapping only surround separately bounded schedule-cache, typed Zone/opaque-Surface, construction/CTF, temperature/humidity, and selected history state; they do not implement or promote the source routine. Complete first-call/environment/day/timestep/warmup/report lifecycles; Zone/Space/Surface/window/enclosure/mass/conduction/history arrays; daylighting/windows/ground/Kiva/EMS coupling; every child call and exact order; diagnostics, files, partial side effects, runtime numerics, performance, and conformance remain unclaimed. CP114 adds no Rust target, mapped state, capability/support change, comparator, proof variable, result, or conformance. CP115 maps the unconditional `ManageEMS(state, BeginZoneTimestepAfterInitHeatBalance, anyRan, absent)` caller checkpoint at lines 199-200 by reusing `routine.manage_ems`; it adds no new routine/source/project-contract/Rust/state/count entry. The caller passes the same line-189 `anyRan` used before initialization, and `ManageEMS` overwrites that referenced result for this calling point on every reached `ManageHeatBalance` invocation; the optional program-manager index is explicitly absent. EMS input/setup, Erl, programs/managers, sensors, actuators, internal variables, trends, plugins, callbacks, external interfaces, calling-point and optional-index semantics, exact run flags, the relationship between pre/post calls, initialized-state observation/mutation, side effects/diagnostics/order, other calling points, repeated-call/environment/timestep/warmup lifecycle, runtime numerics, performance, outputs, and conformance remain unclaimed. CP115 adds no capability/support, manifest, comparator, proof variable, result, or conformance. CP116 maps that unconditional line-209 `ManageSurfaceHeatBalance(state)` call and expands the existing required `routine.manage_surface_heat_balance` row for the lines 145-230 parent-driver order, with no new routine/source/project-contract/Rust/state/count/support or conformance entry. Current Rust stage metadata and identity wrappers retain only their separately bounded paths and do not match or close the complete source nesting/order; first-time/display state, complete child implementations, Surface/Construction/CTF/EMPD/CondFD and moisture state, thermal comfort, sizing, resilience, reporting, side effects, runtime numerics, performance, outputs, and broad conformance remain unclaimed. CP117 maps that inline lines-158-160 first-time `Initializing Surfaces` guard/output without a synthetic routine row. `HeatBalanceSurfaceManager.hh` line 239 defaults the shared flag true and `clear_state()` line 291 resets it true; it remains true through all four parent progress guards and is cleared only at successful parent-tail line 229, so an abnormal exit before the tail can preserve true and allow repeat output. Rust owns no persistent flag, reset/re-entry lifecycle, or progress output; current one-closure wrapper execution and nesting are not parity. Exact display routing/formatting/callbacks, the still-unmapped inside- and air-balance guards, implementation of the outside guard mapped by CP119, fatal/exception behavior, child effects, numerics, performance, output behavior, and conformance remain unclaimed. CP117 adds no routine/project-contract/Rust/state/support/count/comparator/proof/result/conformance entry. CP118 adds required `routine.init_surface_heat_balance` and the project-contract entry for the unconditional line-161 call and source lines 272-621. The source order covers first-time progress plus per-Surface outdoor weather, local-node, and EMS state; BeginSim allocations/construction properties; variable absorptance; BeginEnvrn histories; insulation, ground, fenestration, sky, shading, radiation, daylighting, airflow-window, solar/internal-gain/convection initialization; reporting, CondFD, CTF/EMPD/source-sink constants; radiant/PV/pool/HVAC-source resets; sizing; and first-time completion. Rust init-stage metadata/identity wrapping remains intentionally outside `rust_target` and only surrounds a limited outside closure, so no state or implementation promotion occurs. Exact flags, children, state writes/resets, errors, lifecycle, outputs, numerics, performance, and conformance remain deferred. CP119 maps only the inline parent lines 165-167 shared-first-time guard and exact `Calculate Outside Surface Heat Balance` progress message after `InitSurfaceHeatBalance` returns and immediately before unconditional line-168 `CalcHeatBalanceOutsideSurf`. The CP117 flag default, reset, and successful-tail clear apply unchanged: an abnormal exit before this point suppresses the message, including initialization failure, while an exit after display but before the tail can permit repetition on same-state re-entry. Rust has no progress output or persistent equivalent flag; stage metadata and identity wrappers are not parity. Exact output routing and behavior, failure side effects, later guards, runtime, and conformance remain deferred. CP119 adds no routine, source, project-contract, Rust, state, support, capability, count, comparator, proof, result, or conformance entry. CP120 adds canonical required `routine.calc_heat_balance_outside_surf` and its project-contract entry; CP121 maps the shared-first-time inline inside-balance display without a routine/state/count entry; CP122 adds canonical required `routine.calc_heat_balance_inside_surf` and its project-contract entry for the full-building line-172 call and lines 7738-7813 wrapper without Rust/state/support/conformance promotion; CP123 maps the shared-first-time inline Air-balance display without a routine/state/count entry; CP124 next maps the existing required `ManageAirHeatBalance` caller without a duplicate row."
routine.manage_heat_balance.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.manage_heat_balance.source_routine = "ManageHeatBalance"
routine.manage_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_heat_balance.completion_status = "source_mapped"
routine.manage_heat_balance.required_for_full_domain = true

routine.manage_ems.source_file = "src/EnergyPlus/EMSManager.cc"
routine.manage_ems.source_routine = "ManageEMS"
routine.manage_ems.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_ems.completion_status = "source_mapped"
routine.manage_ems.required_for_full_domain = false

routine.init_heat_balance.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.init_heat_balance.source_routine = "InitHeatBalance"
routine.init_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_heat_balance.completion_status = "source_mapped"
routine.init_heat_balance.required_for_full_domain = true

routine.rec_keep_heat_balance.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.rec_keep_heat_balance.source_routine = "RecKeepHeatBalance"
routine.rec_keep_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.rec_keep_heat_balance.completion_status = "source_mapped"
routine.rec_keep_heat_balance.required_for_full_domain = true

routine.update_window_face_temps_non_bsdf_win.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.update_window_face_temps_non_bsdf_win.source_routine = "UpdateWindowFaceTempsNonBSDFWin"
routine.update_window_face_temps_non_bsdf_win.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_window_face_temps_non_bsdf_win.completion_status = "source_mapped"
routine.update_window_face_temps_non_bsdf_win.required_for_full_domain = true

routine.open_shading_file.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.open_shading_file.source_routine = "OpenShadingFile"
routine.open_shading_file.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.open_shading_file.completion_status = "source_mapped"
routine.open_shading_file.required_for_full_domain = false

routine.set_storm_window_control.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.set_storm_window_control.source_routine = "SetStormWindowControl"
routine.set_storm_window_control.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.set_storm_window_control.completion_status = "source_mapped"
routine.set_storm_window_control.required_for_full_domain = false

routine.report_heat_balance.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.report_heat_balance.source_routine = "ReportHeatBalance"
routine.report_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_heat_balance.completion_status = "source_mapped"
routine.report_heat_balance.required_for_full_domain = true

routine.update_ems_trend_variables.source_file = "src/EnergyPlus/EMSManager.cc"
routine.update_ems_trend_variables.source_routine = "UpdateEMSTrendVariables"
routine.update_ems_trend_variables.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_ems_trend_variables.completion_status = "source_mapped"
routine.update_ems_trend_variables.required_for_full_domain = false

routine.update_plugin_values.source_file = "src/EnergyPlus/PluginManager.cc"
routine.update_plugin_values.source_routine = "updatePluginValues"
routine.update_plugin_values.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_plugin_values.completion_status = "source_mapped"
routine.update_plugin_values.required_for_full_domain = false

routine.check_warmup_convergence.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.check_warmup_convergence.source_routine = "CheckWarmupConvergence"
routine.check_warmup_convergence.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_warmup_convergence.completion_status = "source_mapped"
routine.check_warmup_convergence.required_for_full_domain = true

routine.report_warmup_convergence.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.report_warmup_convergence.source_routine = "ReportWarmupConvergence"
routine.report_warmup_convergence.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_warmup_convergence.completion_status = "source_mapped"
routine.report_warmup_convergence.required_for_full_domain = true

routine.set_pre_construction_input_parameters.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.set_pre_construction_input_parameters.source_routine = "SetPreConstructionInputParameters"
routine.set_pre_construction_input_parameters.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.set_pre_construction_input_parameters.completion_status = "source_mapped"
routine.set_pre_construction_input_parameters.required_for_full_domain = true

routine.get_site_atmosphere_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_site_atmosphere_data.source_routine = "GetSiteAtmosphereData"
routine.get_site_atmosphere_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_site_atmosphere_data.completion_status = "source_mapped"
routine.get_site_atmosphere_data.required_for_full_domain = true

routine.allocate_zone_heat_bal_arrays.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.allocate_zone_heat_bal_arrays.source_routine = "AllocateZoneHeatBalArrays"
routine.allocate_zone_heat_bal_arrays.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.allocate_zone_heat_bal_arrays.completion_status = "source_mapped"
routine.allocate_zone_heat_bal_arrays.required_for_full_domain = true

routine.allocate_heat_bal_arrays.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.allocate_heat_bal_arrays.source_routine = "AllocateHeatBalArrays"
routine.allocate_heat_bal_arrays.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.allocate_heat_bal_arrays.completion_status = "source_mapped"
routine.allocate_heat_bal_arrays.required_for_full_domain = true

routine.init_conduction_transfer_functions.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.init_conduction_transfer_functions.source_routine = "InitConductionTransferFunctions"
routine.init_conduction_transfer_functions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_conduction_transfer_functions.completion_status = "source_mapped"
routine.init_conduction_transfer_functions.required_for_full_domain = true

routine.get_frame_and_divider_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_frame_and_divider_data.source_routine = "GetFrameAndDividerData"
routine.get_frame_and_divider_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_frame_and_divider_data.completion_status = "state_mapped"
routine.get_frame_and_divider_data.required_for_full_domain = false
routine.get_frame_and_divider_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_frame_and_divider_data.read_state = [
  "EnergyPlus source places `GetFrameAndDividerData` after `Material::GetHysteresisData` and before `GetConstructData`; Rust deterministically reads `WindowProperty:FrameAndDivider` after base `parse_materials` and before `parse_constructions`, while its separate `parse_material_phase_change_hystereses` pass remains later; every definition is read eagerly, including an all-default, reveal-only, or unused record",
  "one required nonblank outer-key name in an independent normalized namespace; Divider Type defaults to `DividedLite` and accepts `DividedLite` or `Suspended`, while NFRC Product Type defaults to `CurtainWall` and accepts `CasementDouble`, `CasementSingle`, `DualAction`, `Fixed`, `Garage`, `Greenhouse`, `HingedEscape`, `HorizontalSlider`, `Jal`, `Pivoted`, `ProjectingSingle`, `ProjectingDual`, `DoorSidelite`, `Skylight`, `SlidingPatioDoor`, `CurtainWall`, `SpandrelPanel`, `SideHingedDoor`, `DoorTransom`, `TropicalAwning`, `TubularDaylightingDevice`, or `VerticalSlider`",
  "all 23 numeric fields as finite values with source-effective defaults and bounds: frame width [0,1] default 0, two frame projections [0,0.5] default 0, nonnegative frame conductance default 0 without upper bound, frame edge/center ratio (0,4] default 1, frame solar/visible absorptance [0,1] default 0.7, frame emissivity greater than 0 without upper bound default 0.9; divider width [0,0.5] default 0, two counts default 0, two divider projections [0,0.5] default 0, nonnegative divider conductance default 0 without upper bound, divider edge/center ratio (0,4] default 1, divider solar/visible absorptance [0,1] default 0, divider emissivity (0,1) default 0.9; three reveal absorptances [0,1] default 0 and two reveal depths [0,2] default 0",
  "horizontal and vertical divider counts accept finite values in [0,2147483648), truncate toward zero into nonnegative integer state, and have no cross-field validity requirement; no material, construction, surface, schedule, geometry, or usage dependency is read",
]
routine.get_frame_and_divider_data.write_state = [
  "a separate deterministic `WindowFrameAndDivider` arena and normalized name map; each record owns a typed ID, normalized name, nested `WindowFrameProperties`, `WindowDividerProperties`, and `WindowRevealProperties`, and one `WindowNfrcProductType`",
  "source correction order first clears both frame projections when frame width is zero, then clears both divider projections when divider width is zero or type is `Suspended`, then warns and resets a still-positive divider width when both effective counts are zero without revisiting the prior projection result, and finally warns and raises an inside sill shallower than the inside reveal to the reveal depth",
  "source-fixed frame and divider edge widths of 0.06355 m; no WINDOW 5 mullion orientation or synthesized frame/divider state is invented",
  "two nonblocking typed diagnostics identify the positive-divider-width/zero-count reset and the inside-sill-depth reset without claiming EnergyPlus warning text, severity, order, or multiplicity",
  "compile failure before typed ID or normalized-name reservation for a blank name, malformed or out-of-range numeric, count outside the finite [0,2147483648) boundary, invalid enum, or case-insensitive duplicate name; normalized duplicate rejection and invalid-enum rejection deliberately fail closed relative to source case-collision lookup and invalid-token fallback behavior",
]
routine.get_frame_and_divider_data.history_state_ownership = "TypedModel owns immutable frame/divider/reveal descriptors; this checkpoint allocates no mutable window history"
routine.get_frame_and_divider_data.unsupported_state = [
  "fenestration-surface and legacy Window or GlazedDoor reference binding, first-match case-collision behavior, geometry and projected area state, frame/divider edge areas, reveal and sill geometry, and surface-local frame/divider pointers",
  "WINDOW 5 synthesized records and mullion orientation, between-glass shading mutation of shared divider width, window optical and thermal state, NFRC assembly calculations, reports, output variables, and runtime numerical state",
]
routine.get_frame_and_divider_data.inactive_branches = [
  "zero frame width silently leaves both frame projections at zero; zero divider width or `Suspended` silently leaves both divider projections at zero; a positive divider width with zero effective counts resets only the width after projection correction, so positive divided-lite projections intentionally remain possible beside the recovered zero width",
  "all-default, reveal-only, unused, non-fenestration-referenced, and names colliding with Material, Construction, or BuildingSurface namespaces remain valid typed records because this input has no cross-object dependency",
]
routine.get_frame_and_divider_data.unsupported_active_branches = [
  "every valid `WindowProperty:FrameAndDivider` definition is typed but reported as `UnsupportedSurfaceBoundary` and `RunBlocked`, including all-default and unused definitions; no partial run is allowed",
  "valid frame emissivity above 1, fractional divider counts truncated toward zero, source-recovered positive-width/zero-count input, and source-recovered shallow-sill input remain typed but do not activate a runtime or reporting consumer",
]
routine.get_frame_and_divider_data.not_claimed_branches = [
  "Hysteresis-relative Rust compiler pass order and complete `GetHeatBalanceInput` order parity, native-epJSON canonical enum casing, source case-colliding name lookup and declaration ordering, invalid-enum fallback, exact diagnostic text/severity/order/multiplicity, fenestration binding, geometry, WINDOW 5 synthesis, between-glass shading mutation, optics, thermal execution, NFRC assembly calculations, EIO/SQLite or other reporting, runtime numerics, and conformance",
]

routine.get_construct_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_construct_data.source_routine = "GetConstructData"
routine.get_construct_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_construct_data.completion_status = "source_mapped"
routine.get_construct_data.required_for_full_domain = true
routine.get_construct_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_construct_data.read_state = [
  "EnergyPlus calls `GetConstructData` after `GetFrameAndDividerData` and before `GetBuildingData`; within it, every ordinary `Construction` is read before the separately state-mapped F-factor then C-factor generation pass, AirBoundary pass, and SetupComplexFenestrationStateInput pass, followed by the bounded InternalHeatSource overlay, bounded WindowEquivalentLayer family, and bounded request-only WindowDataFile front end whose external-file expansion remains deferred",
  "one required nonblank outer-key name in an independent normalized construction namespace, one required outside-layer material name, and optional layers 2 through 10 that must form a contiguous populated prefix; every populated layer resolves through the typed material namespace",
  "every typed `WindowMaterial:GlazingGroup:Thermochromic` parent encountered in layer order contributes its first ordered glazing state to the effective layer stack; the existing typed parent contract guarantees at least one state",
  "a `WindowMaterial:SimpleGlazingSystem` material is accepted only when it is the sole ordinary-Construction layer, matching the intended whole-system input while excluding EnergyPlus 26.1's multi-layer validation holes",
  "all layer references, contiguous-prefix rules, bounded opaque/fenestration family and topology checks, unsupported-consumer checks, and normalized duplicate checks complete before a `ConstructionId` or name is reserved",
  "the `ConstructionProperty:InternalHeatSource` epJSON instance map is read directly in case-sensitive lexical outer-key order and deliberately remains outside staged IDF declaration-order recovery; its required nonblank outer key is a normalized diagnostic snapshot without a typed ID or name map, so case variants may coexist when they target different constructions",
  "each internal-source record has a required nonblank case-insensitive shared-Construction reference restricted by the bounded contract to an ordinary opaque construction with at least two effective layers; both required one-based layer interfaces are positive integers in `1..TotLayers-1`, need not match, and have no ordering relationship",
  "internal-source CTF dimensions are exactly integer 1 or 2, required finite tube spacing is in `[0.01,1.0]` m and derives a retained perpendicular half-spacing, and the optional finite perpendicular temperature position defaults to 0 in `[0,1]` and remains retained but inactive for a 1-D declaration",
  "each `Construction:WindowEquivalentLayer` has a required nonblank name in the shared case-insensitive Construction namespace, one required outside layer, and optional layers 2 through 11 forming a contiguous one-to-eleven-layer prefix; every reference resolves case-insensitively to an already typed `MaterialFamily::EquivalentLayer` glazing, shade, drape, blind, screen, or gap definition",
  "EnergyPlus reads equivalent-layer constructions through `getObjectItem`; staged IDF therefore preserves family-local declaration order through the explicit order overlay, while native epJSON without that overlay retains case-sensitive lexical instance-key order",
  "each `Construction:WindowDataFile` request has a required nonblank WINDOW5 entry name and optional retain-case `file_name`; missing or blank input selects the explicit current-working-folder default `Window5DataFile.dat`, while nonblank text is retained without path resolution or file access",
  "EnergyPlus also reads WindowDataFile requests through `getObjectItem`; staged IDF preserves family-local declaration order through its explicit order overlay, while native epJSON without that overlay retains case-sensitive lexical instance-key order",
]
routine.get_construct_data.write_state = [
  "the deterministic `Construction` arena and independent normalized name map retain each valid ID, normalized name, bounded opaque-or-fenestration kind, effective outside layer, and ordered effective outside-to-inside material stack",
  "every thermochromic parent layer is replaced by its first-state glazing `MaterialId`; the final parent encountered overwrites the source-shaped zero-based parent-material-layer and glazing-layer metadata slots while earlier parent substitutions remain in the effective stack",
  "a sole-layer SimpleGlazingSystem construction is retained as Fenestration with the original material ID in outside-layer, ordered-layer, and construction/material graph state; no detailed-glazing child is synthesized",
  "construction/material model-graph edges are emitted from the effective material stack, so thermochromic edges target first-state glazing materials rather than parent group descriptors",
  "runtime construction/material resolution and opaque construction thermal-cache building use checked direct-index lookups; these structural paths do not enable thermochromic or other window execution",
  "one immutable internal-source descriptor on each valid unique target retains its normalized diagnostic source name, one-based source and requested-temperature interfaces, 1-D or 2-D selector, authored tube spacing, derived perpendicular half-spacing, and perpendicular temperature position without changing Construction identity, kind, layers, other construction-family metadata, material state, or graph edges",
  "all internal-source fields and target constraints are validated before attachment; a malformed or missing earlier lexical record reserves no target, the first valid record attaches, and a later valid record for the same `ConstructionId` fails closed without overwriting the first descriptor",
  "one dedicated `ConstructionKind::WindowEquivalentLayer` record per valid equivalent-layer definition after the InternalHeatSource overlay, retaining an explicit outside material, ordered material stack and construction/material graph edges, plus a zero-based family source index corresponding to EnergyPlus's one-based `EQLConsPtr` ordinal",
  "all equivalent-layer fields, references, material families, contiguity, and shared-name duplication are validated before identity reservation; GetConstructData's family-only acceptance is preserved without inventing alternating solid/gap topology or downstream ASHWAT semantics",
  "one immutable `ConstructionWindowDataFileRequest` record per valid final-family input retains its normalized search-name snapshot, default-or-explicit retain-case file selector, and zero-based source ordinal in a separate arena",
  "request publication creates no `ConstructionId`, shared construction-name entry, material, frame/divider, graph edge, or optical state; name collisions remain request data until the deferred file parser can validate and materialize source identities",
]
routine.get_construct_data.history_state_ownership = "TypedModel owns immutable construction layer stacks, optional thermochromic master metadata, optional internal-source declaration metadata, equivalent-layer declaration ordinals, and WINDOW5 request selectors; this checkpoint creates no thermochromic child constructions, global source-presence flag, CTF/QTF or source-node history, equivalent-layer ASHWAT state, WINDOW5 synthesized state, active-state history, or mutable window/surface state"
routine.get_construct_data.unsupported_state = [
  "multi-layer or shaded ordinary `Construction` consumption of `WindowMaterial:SimpleGlazingSystem`",
  "InternalHeatSource lookup or overlay of F-factor, C-factor, AirBoundary, window, complex-fenestration, later equivalent-layer, or WindowDataFile constructions; source warning recoveries for invalid layer interfaces, dimensions, spacing, or perpendicular position; and thermochromic child propagation",
  "`SetPreConstructionInputParameters` maximum-layer projection, `CheckAndFixCFSLayer`, `FinalizeCFS`, `SetEquivalentLayerWindowProperties`, ASHWAT optical and thermal calculations, nominal equivalent-layer resistance, U-factor/SHGC/rating state, and every equivalent-layer surface or window consumer",
  "`SearchWindow5DataFile` path lookup and fatal behavior, Unicode/header/EOF and entry matching, one- or two-glazing-system parsing, W5 glass/gas material and construction generation, nominal resistance and U state, angular optical arrays and polynomial fits, frame/divider and mullion synthesis, source collision behavior, and every generated surface, reporting, or runtime consumer",
  "the global `AnyInternalHeatSourceInInput` and `SimpleCTFOnly` flags, resistance-layer merging and source-node remapping, CTF/QTF generation and histories, source/sink heat fluxes and interior temperatures, radiant-system, ventilated-slab, surface-ground-heat-exchanger, representative-surface, and other downstream consumers",
  "`CreateTCConstructions` child allocation and copying, source-formatted child names, surface-active construction switching, temperature-driven state selection, fenestration binding, optics, thermal calculations, daylighting, shading, nominal-U adjustment, EIO or other construction reporting, and runtime or conformance behavior",
]
routine.get_construct_data.inactive_branches = [
  "ordinary constructions without a thermochromic parent retain their existing effective material stacks and bounded opaque/fenestration classification; for these records the input and effective stacks are identical",
  "a sole-layer SimpleGlazingSystem construction retains its one source material ID and has no thermochromic metadata; no multi-layer source quirk is materialized",
  "when more than one thermochromic parent is present, every parent is first-state substituted but only the final parent owns the zero-based master metadata, preserving the source overwrite behavior without generating child constructions",
  "when no valid `ConstructionProperty:InternalHeatSource` targets an ordinary opaque construction, every construction retains absent internal-source metadata; a retained nonzero perpendicular position on a 1-D declaration has no active consumer",
  "when no `Construction:WindowEquivalentLayer` definition exists, the construction arena and graph gain no equivalent-layer state; when definitions exist, any one-to-eleven-layer contiguous family-only pack remains declaration data without topology repair or an executable window consumer",
  "when no `Construction:WindowDataFile` request exists, the request arena is empty; missing or blank file names retain the default selector, and explicit names remain inert retain-case text even when the referenced file does not exist",
]
routine.get_construct_data.unsupported_active_branches = [
  "every typed thermochromic parent remains an all-definition runtime blocker through its existing parent-material capability rule, including an unused parent and a parent consumed by a valid ordinary `Construction`; direct-index structural lookup does not weaken that block",
  "every typed SimpleGlazingSystem definition remains an all-definition runtime blocker, including an unused definition and one consumed by a valid sole-layer ordinary `Construction`",
  "valid bounded fenestration constructions remain typed graph state only and do not enter the opaque runtime thermal cache or acquire window execution",
  "every valid `ConstructionProperty:InternalHeatSource` definition, including one attached only to an otherwise unused construction, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked` with `RuntimeClass::None`; a `BuildingSurface:Detailed` may retain the still-ordinary opaque target identity, but no partial or compatibility runtime is admitted",
  "every valid `Construction:WindowEquivalentLayer` definition, including every unused definition, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked` with `RuntimeClass::None`; supported equivalent-layer materials and graph edges do not admit surface binding, reporting, or partial window runtime",
  "every valid `Construction:WindowDataFile` request, including every unused, defaulted, or missing-file request, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked` with `RuntimeClass::None` before file I/O; no synthesized construction identity or partial runtime is admitted",
]
routine.get_construct_data.not_claimed_branches = [
  "complete `GetConstructData` parity, source case-collision and exact duplicate-key behavior, invalid-object and mark-used side effects, exact diagnostics/order/multiplicity, multi-layer or shaded SimpleGlazingSystem source quirks, thermochromic child generation/naming/state selection, `SearchWindow5DataFile` external-file expansion and generated state, broad InternalHeatSource target and recovery quirks, equivalent-layer topology repair and ASHWAT consumers, global flags, CTF/QTF calculations, nominal-U, EIO/SQLite and other reporting, window, air-boundary, ground, radiant, or source/sink physics, runtime numerics, and conformance",
]

routine.search_window5_data_file.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.search_window5_data_file.source_routine = "SearchWindow5DataFile"
routine.search_window5_data_file.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.search_window5_data_file.completion_status = "source_mapped"
routine.search_window5_data_file.required_for_full_domain = false

routine.create_fc_factor_constructions.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.create_fc_factor_constructions.source_routine = "CreateFCfactorConstructions"
routine.create_fc_factor_constructions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_fc_factor_constructions.completion_status = "state_mapped"
routine.create_fc_factor_constructions.required_for_full_domain = false
routine.create_fc_factor_constructions.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_fc_factor_constructions.read_state = [
  "raw `Construction:FfactorGroundFloor` and `Construction:CfactorUndergroundWall` counts allocate source-ordered private material slots: public `Material`, common `~FC_Concrete`, public `Material:NoMass`, then one `~FC_Insulation_n` for every raw F object followed by every raw C object; staged IDF declaration-order overlays preserve ordinary, F-family, and C-family ordinals independently",
  "each F-factor record has a required nonblank shared Construction name plus required finite `f_factor` and `area` greater than zero and exact epJSON `perimeterexposed` greater than or equal to zero; positive perimeter derives `Reff = area/(perimeter*f_factor)-0.135-0.03`, while zero perimeter derives `Reff = 177`",
  "each C-factor record has a required nonblank shared Construction name plus required finite `c_factor` and `height` greater than zero; equivalent soil resistance is 0.12 at height less than or equal to 0.25 m, 0.92 at height greater than or equal to 2.5 m, and `0.0607+0.3479*height` between, with `Reff = 1/c_factor+soil resistance`",
  "the common MediumRough concrete has thickness 0.15 m, conductivity 1.95 W/m-K, density 2240 kg/m3, specific heat 900 J/kg-K, and default opaque absorptances; each MediumRough resistance-only insulation has all three absorptances zero and derives `Rfic = Reff-(0.15/1.95)`, which must be finite and greater than zero together with finite `Reff`",
]
routine.create_fc_factor_constructions.write_state = [
  "one typed opaque `Construction` per valid record after every ordinary Construction and in all-F-then-all-C order, with a distinct F-factor or C-factor metadata discriminant retaining source inputs, derived effective resistance, generated insulation resistance, and C-factor soil resistance where applicable",
  "each generated construction owns the outside-to-inside layer stack `[private insulation, private concrete]`; the construction/material graph emits both ordered edges and the raw-ordinal insulation slot receives the derived resistance",
  "internal materials live in the typed material arena but are deliberately absent from public `material_names` and every public attachment-target lookup; exact normalized generated-name collisions from user material families fail closed, while nonidentical names such as `~FC_Insulation_01` remain public",
  "invalid fields, nonpositive or nonfinite derived insulation resistance, and duplicate Construction names reserve no Construction ID or name; raw-count internal slots and later-family ordinals remain stable, while exact EnergyPlus invalid-object and collision side effects are not reproduced",
]
routine.create_fc_factor_constructions.history_state_ownership = "TypedModel owns immutable generated ground-factor construction metadata and layer stacks; no surface-local ground-temperature, CTF, CondFD, HAMT, or mutable heat-balance history is allocated"
routine.create_fc_factor_constructions.unsupported_state = [
  "`GroundFCfactorMethod` surface-type pairing, detailed-surface and rectangular-surface boundary conversion, F area/perimeter and C height geometry checks, `Site:GroundTemperature:FCfactorMethod`, EPW fallback, monthly ground state, CTF/CondFD/HAMT calculation, and ground heat-balance execution",
  "public attachment targeting of private internal materials, EMS mutation, bounded-out `ConstructionProperty:InternalHeatSource` targeting of generated F/C constructions, nominal-U adjustment, generic material/construction EIO timing, envelope/SQLite reporting, exact diagnostics/order/multiplicity, and conformance evidence",
]
routine.create_fc_factor_constructions.inactive_branches = [
  "when no raw F-factor or C-factor definition exists, no internal concrete or insulation material is injected and ordinary material/construction behavior is unchanged",
  "F-factor exposed perimeter exactly zero uses the source 177 m2-K/W effective-resistance branch; C-factor heights exactly 0.25 m and 2.5 m use the lower and upper constant soil-resistance branches respectively",
]
routine.create_fc_factor_constructions.unsupported_active_branches = [
  "every valid F-factor or C-factor definition, including every unused definition, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked`; no partial or compatibility runtime is admitted",
  "`BuildingSurface:Detailed` deliberately rejects a generated F/C-factor construction until the downstream `GroundFCfactorMethod` pairing and validation checkpoint is ported; reporting and runtime consumers likewise filter generated stacks from ordinary opaque constructions",
]
routine.create_fc_factor_constructions.not_claimed_branches = [
  "surface binding and geometry validation, ground-temperature and CTF/runtime behavior, internal-material public attachment behavior, source collision and invalid-object side effects, no-thermal-mass flags, nominal-U and reporting parity, exact diagnostics/order/multiplicity, runtime numerics, EIO/SQLite, and conformance",
]

routine.create_air_boundary_constructions.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.create_air_boundary_constructions.source_routine = "CreateAirBoundaryConstructions"
routine.create_air_boundary_constructions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_air_boundary_constructions.completion_status = "state_mapped"
routine.create_air_boundary_constructions.required_for_full_domain = false
routine.create_air_boundary_constructions.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_air_boundary_constructions.read_state = [
  "the `Construction:AirBoundary` epJSON object map after every ordinary, F-factor, and C-factor construction; EnergyPlus iterates that map directly, so records materialize in case-sensitive lexical key order rather than staged source-IDF declaration order, and Rust preserves the same family-local order",
  "one required nonblank outer-key name in the shared case-insensitive Construction namespace; `air_exchange_method` missing or blank defaults to `None` and otherwise accepts case-insensitive `None` or `SimpleMixing` within the Rust parser convention",
  "a supplied nonblank `simple_mixing_air_changes_per_hour` must be finite and nonnegative without an upper bound; `SimpleMixing` applies the source default 0.5 when it is missing or blank, while `None` retains source-effective zero mixing state",
  "only `SimpleMixing` consumes `simple_mixing_schedule_name`: missing or Rust-normalized blank input selects an explicit always-on sentinel, while a nonblank name resolves case-insensitively through the existing typed top-level schedule namespace after Rust schedule materialization; `None` ignores the schedule field and creates no schedule dependency",
]
routine.create_air_boundary_constructions.write_state = [
  "one dedicated zero-layer air-boundary construction descriptor per valid record, appended after every C-factor construction with a shared `ConstructionId` and normalized Construction name but no outside material, material layer, thermochromic or ground-factor metadata, or construction/material graph edge",
  "`None` retains false simple-mixing state, zero air changes per hour, and no schedule selector; inactive supplied mixing fields do not create executable state",
  "`SimpleMixing` retains true mixing state, the effective nonnegative air-changes-per-hour value, and either a resolved user `ScheduleId` or an explicit always-on selector without synthesizing another typed Schedule object",
  "blank names, malformed or out-of-range numeric input, invalid enums, unresolved active schedule references, and normalized duplicate Construction names fail before Rust ID or name reservation; this deliberately does not reproduce source partial-slot and name-reservation side effects after an active schedule lookup failure",
]
routine.create_air_boundary_constructions.history_state_ownership = "TypedModel owns immutable zero-layer air-boundary and optional simple-mixing descriptors; no surface pair, enclosure, cross-mixing object, schedule current value, or mutable heat-balance history is allocated"
routine.create_air_boundary_constructions.unsupported_state = [
  "base-surface or fenestration-surface binding, `Surface` or `Zone` outside-boundary validation, paired nonadiabatic interzone checks, child-fenestration exclusion, `AirBoundaryNoHT` surface conversion, and surface heat-transfer suppression",
  "solar, radiant, and daylighting enclosure grouping, `AnyAirBoundary`, view-factor and enclosure remapping, and every geometry-dependent surface mutation",
  "space-pair deduplication, minimum-space-volume times air-changes-per-hour divided by 3600 flow derivation, AirflowNetwork suppression, generated `ZoneCrossMixing`, schedule current-value consumption, zone mixing calculations, and mixing output variables",
  "nominal-U and CTF skip behavior beyond the typed zero-layer flag, construction or surface EIO/SQLite reporting, exact diagnostics, and conformance evidence",
]
routine.create_air_boundary_constructions.inactive_branches = [
  "when no `Construction:AirBoundary` definition exists, ordinary, F-factor, and C-factor construction behavior is unchanged",
  "`None` leaves simple mixing disabled with zero air changes and no schedule selector even when inactive mixing fields are supplied; their schedule name is not resolved",
  "`SimpleMixing` with no effective schedule name retains only the explicit always-on selector; no schedule sampling or downstream mixing state is activated",
]
routine.create_air_boundary_constructions.unsupported_active_branches = [
  "every valid `Construction:AirBoundary` definition, including every unused `None` or `SimpleMixing` definition, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked`; no partial or compatibility runtime is admitted",
  "`BuildingSurface:Detailed` deliberately rejects an air-boundary construction until interzone pairing, enclosure grouping, no-heat-transfer surface conversion, and optional simple-mixing consumers are ported; a supported referenced schedule does not weaken this block",
]
routine.create_air_boundary_constructions.not_claimed_branches = [
  "complete `GetConstructData` or Rust compiler pass-order parity, source case-collision lookup and invalid-after-reservation side effects, explicit-empty native-epJSON behavior, canonical enum casing, exact diagnostics/order/multiplicity, surface and enclosure behavior, schedule sampling, AirflowNetwork interaction, zone mixing, reporting, runtime numerics, EIO/SQLite, and conformance",
]

routine.setup_complex_fenestration_state_input.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.setup_complex_fenestration_state_input.source_routine = "SetupComplexFenestrationStateInput"
routine.setup_complex_fenestration_state_input.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.setup_complex_fenestration_state_input.completion_status = "state_mapped"
routine.setup_complex_fenestration_state_input.required_for_full_domain = false
routine.setup_complex_fenestration_state_input.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.setup_complex_fenestration_state_input.read_state = [
  "the `Construction:ComplexFenestrationState` family after every ordinary, F-factor, C-factor, and AirBoundary construction; staged IDF input preserves family-local declaration order, while native epJSON without that overlay retains lexical object-map order",
  "one required nonblank name in the shared case-insensitive Construction namespace; missing or blank basis fields default to `LBNLWINDOW` and `None`, and only that combination is admitted while `UserDefined` and `Axisymmetric` fail closed",
  "when at least one complex state exists, every raw-only `WindowThermalModel:Params` definition is validated before state inspection; `Matrix:TwoDimension` loading remains lazy until one nonblank state name passes the shared Construction-name collision gate, then every matrix definition including unused entries is validated before any surviving state identity is materialized",
  "positive finite matrix shapes and effective row-major prefixes drive a one- or two-column LBNL basis, four square global optical matrices, and one-row front/back absorptance matrices per solid; layers form a contiguous alternating one-to-five-solid and zero-to-four-gap pack ending in a solid, accepting only SpectralAverage glazing or ComplexShade at solid positions and WindowMaterial:Gap at gap positions with reserved gap matrix fields blank",
]
routine.setup_complex_fenestration_state_input.write_state = [
  "one `ConstructionKind::ComplexFenestration` record per valid definition after every AirBoundary record, with shared `ConstructionId`, normalized name, outside layer, ordered material stack, no thermochromic, ground-factor, or air-boundary metadata, and staged-IDF family order when available",
  "immutable `ConstructionComplexFenestrationState` metadata retains LBNL/None selectors, the resolved thermal-model snapshot, basis matrix and derived basis length, four global optical matrix snapshots, and ordered solid-layer front/back absorptance snapshots; thermal and matrix lookup plus duplicate detection are normalized case-insensitively, retained matrix snapshots preserve original spelling, and raw helper families remain outside public typed object coverage",
  "ordered construction/material graph edges cover every solid and gap layer, and material-family validation prevents ordinary, equivalent-layer, simple-glazing, thermochromic, or unsupported BSDF glazing variants from entering the pack",
  "all helper catalogs, references, matrix dimensions, layer topology, reserved fields, and shared-name duplication are validated before identity reservation; an invalid earlier state does not consume the shared name needed by a later valid case-variant definition",
]
routine.setup_complex_fenestration_state_input.history_state_ownership = "TypedModel owns immutable declaration, helper snapshots, and ordered layer graph state; no BSDF basis cache, surface-local optical/thermal state, deflection history, TARCOG/WCE state, or mutable window history is allocated"
routine.setup_complex_fenestration_state_input.unsupported_state = [
  "`UserDefined` bases, `Axisymmetric` symmetry, regular-glazing `BSDF`, Spectral and SpectralAndAngle glazing consumers, nonblank reserved gap matrix fields, unsupported solid/gap material families, and any layer topology outside the bounded alternating pack",
  "fenestration-surface binding, complex-window initialization, basis expansion beyond retained input snapshots, BSDF optical calculations, deflection and support-pillar algorithms, TARCOG/WCE thermal calculations, shading, ratings, daylighting, nominal-U or CTF state, output variables, and EIO/SQLite or other reporting",
]
routine.setup_complex_fenestration_state_input.inactive_branches = [
  "when no `Construction:ComplexFenestrationState` definition exists, raw thermal-model and matrix helpers are not parsed by this pass; when definitions exist but every nonblank name collides with the shared Construction namespace, thermal helpers are still validated but the matrix catalog remains untouched",
  "a one-column basis uses its row count directly; a two-column basis uses the EnergyPlus count projection retained by the bounded LBNL branch, while extra finite matrix values beyond rows times columns are ignored",
  "a single solid needs no gap, while each higher populated position must preserve solid-gap alternation and every omitted trailing layer leaves no typed or graph state",
]
routine.setup_complex_fenestration_state_input.unsupported_active_branches = [
  "every valid `Construction:ComplexFenestrationState` definition, including every unused definition, is reported as `UnsupportedSurfaceBoundary` and `RunBlocked` with `RuntimeClass::None`; no partial or compatibility runtime is admitted",
  "`BuildingSurface:Detailed` deliberately rejects a complex-fenestration construction until the dedicated fenestration-surface, BSDF optical, and complex-window thermal consumers are ported; supported material and helper declarations do not weaken that block",
]
routine.setup_complex_fenestration_state_input.not_claimed_branches = [
  "complete `GetConstructData` parity, source partial-slot allocation and invalid-object side effects, broad IDF/native-epJSON order parity, source case-collision behavior, exact diagnostics/order/multiplicity, custom or axisymmetric bases, broad matrix semantics, surface and window behavior, optics, thermal calculations, ratings, daylighting, reporting, runtime numerics, EIO/SQLite, and conformance",
]

routine.get_building_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_building_data.source_routine = "GetBuildingData"
routine.get_building_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_building_data.completion_status = "source_mapped"
routine.get_building_data.required_for_full_domain = true

routine.get_incident_solar_multiplier.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_incident_solar_multiplier.source_routine = "GetIncidentSolarMultiplier"
routine.get_incident_solar_multiplier.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_incident_solar_multiplier.completion_status = "source_mapped"
routine.get_incident_solar_multiplier.required_for_full_domain = false

routine.get_scheduled_surface_gains.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_scheduled_surface_gains.source_routine = "GetScheduledSurfaceGains"
routine.get_scheduled_surface_gains.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_scheduled_surface_gains.completion_status = "source_mapped"
routine.get_scheduled_surface_gains.required_for_full_domain = false

routine.check_scheduled_surface_gains.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.check_scheduled_surface_gains.source_routine = "CheckScheduledSurfaceGains"
routine.check_scheduled_surface_gains.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_scheduled_surface_gains.completion_status = "state_mapped"
routine.check_scheduled_surface_gains.required_for_full_domain = false
routine.check_scheduled_surface_gains.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_scheduled_surface_gains.read_state = [
  "the internal tail helper called by `GetScheduledSurfaceGains` for every EnergyPlus Zone only when either scheduled-gain input family has at least one source record; the enclosing routine remains source-mapped, and its next top-level sibling is the representative-surface EIO assignment block",
  "only the error-free retained typed `BuildingSurface:Detailed` subset after final Space assignment: deterministic `Zone::spaces`, each retained surface's final SpaceId and current resolved ConstructionId, and the immutable `SurfaceSolarIncident` exact SurfaceId/ConstructionId pairs",
  "for each retained typed opaque surface, a pair is scheduled only when its current construction exactly matches one typed first-family record; complex-fenestration requests remain unresolved and cannot match this subset",
]
routine.check_scheduled_surface_gains.write_state = [
  "no model state is written; a Zone receives one nonblocking compile warning only when its retained typed opaque subset already contains both at least one exact-pair match and at least one miss",
  "the bounded diagnostic identifies the Zone and lists only the known retained typed surface names whose current SurfaceId/ConstructionId pair is unscheduled; it does not invent names or outcomes for deferred surfaces",
  "the warning predicate is monotonic under completion of the full EnergyPlus heat-transfer surface set: once a retained subset contains both a match and a miss, adding omitted surfaces cannot make the full set uniformly scheduled or uniformly unscheduled",
  "any relevant typed surface-input or scheduled-surface-gain input error suppresses the entire check so diagnostics are not inferred from partial compiler arenas",
]
routine.check_scheduled_surface_gains.history_state_ownership = "the compiler owns no new persistent state for this diagnostic-only pass; it allocates only local match/miss observations and warning text, with no schedule values, surface flags, lookup cache, or runtime history"
routine.check_scheduled_surface_gains.unsupported_state = [
  "the complete EnergyPlus heat-transfer surface population: windows and other fenestration, legacy detailed surface families, InternalMass, generated opposite surfaces, source surface reordering, and representative/constituent-surface mutation",
  "EnergyPlus `WindowScheduledSolarAbs` matching, complex-fenestration current or active construction selection, `SurfaceScheduledSolarInc` and `WindowScheduledSolarAbs` runtime consumers, schedule sampling, solar replacement, BSDF absorption, and reporting",
]
routine.check_scheduled_surface_gains.inactive_branches = [
  "an empty retained subset, an all-matched retained subset, or an all-unmatched retained subset emits no warning because omitted full-domain surfaces could change those classifications; this also avoids claiming the source empty-Zone flag quirk",
  "a Zone with no typed detailed opaque surfaces emits no warning, and unresolved CP101 fenestration requests cannot create a typed-subset match",
  "surface or scheduled-input errors suppress the pass rather than emitting warnings from partially published state",
]
routine.check_scheduled_surface_gains.unsupported_active_branches = [
  "the bounded warning is diagnostic-only and nonblocking; it does not weaken the all-definition `UnsupportedSurfaceBoundary` and `RunBlocked` boundary for either scheduled-gain object family",
  "Zones whose full source surface set is mixed but whose retained typed subset is empty, all matched, or all unmatched intentionally receive no warning until full completeness is available",
]
routine.check_scheduled_surface_gains.not_claimed_branches = [
  "complete Zone/Space heat-transfer surface membership, windows, legacy/InternalMass/generated/representative surfaces, active-construction semantics, full scheduled-gain completeness, or the EnergyPlus empty-Zone behavior",
  "exact EnergyPlus warning severity text, continuation text, surface text, order, punctuation, or multiplicity; pair-lookup routines, schedule evaluation, runtime numerics, reporting, graph edges, manifests, comparators, proof variables, and conformance",
]

routine.create_tc_constructions.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.create_tc_constructions.source_routine = "CreateTCConstructions"
routine.create_tc_constructions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_tc_constructions.completion_status = "state_mapped"
routine.create_tc_constructions.required_for_full_domain = false
routine.create_tc_constructions.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.create_tc_constructions.read_state = [
  "EnergyPlus calls this routine immediately after the inline representative-surface EIO assignment block; bounded Rust enters after the CP102 scheduled-gain check and reads only immutable typed-model state, while the intervening output block remains mapped and deferred",
  "every existing typed Construction is visited once in dense ConstructionId order; only records with CP85 `ConstructionThermochromicMaster` metadata are selected, and that descriptor's parent MaterialId plus zero-based layer index address the final thermochromic parent and its slot in the already first-state-substituted effective stack",
  "each selected parent resolves to a typed `WindowGlazingThermochromicGroupMaterial` and its nonempty contiguous state-arena slice; every ordered state, including the first, supplies its retained optical-data temperature and resolved glazing MaterialId without sorting, deduplication, or duplicate rejection",
]
routine.create_tc_constructions.write_state = [
  "one immutable `ConstructionThermochromicSeries` per selected master retains the master ConstructionId, the first state's initial specification temperature, and a contiguous first-child/count descriptor",
  "one immutable `ConstructionThermochromicChild` per ordered state retains a dense projection-only child ID, master ConstructionId, zero-based state index, source-shaped `{:.0R}` normalized name with the pinned 40, -999.5, signed-zero, 0.05, and finite-limit boundaries, specification temperature, outside layer, and cloned effective layer stack with only the final master's retained layer slot replaced by that state's glazing MaterialId",
  "output order is master ConstructionId then input state; earlier thermochromic parents in a multiple-parent construction remain fixed at their first states, layer-zero replacement also updates the child outside layer, duplicate temperatures/materials/names are preserved, and a shared group produces a separate series for each master",
  "both projection arenas are built transactionally and published together only after every selected parent descriptor, state range, effective first-state layer, finite temperature, layer index, and dense child range validates; prior compiler errors suppress the pass rather than publishing projections from partial upstream arenas",
  "the main Construction arena and name map, ConstructionIds, object counts, material arena, model-graph edges, surfaces, and runtime state are unchanged",
]
routine.create_tc_constructions.history_state_ownership = "TypedModel owns only immutable thermochromic series and child projection arenas; no master mutation, active-state pointer, temperature history, surface state, optical/thermal cache, or mutable simulation history is allocated"
routine.create_tc_constructions.unsupported_state = [
  "appending deep-copied children to the global Construction arena, assigning source-global ConstructionIds after any WINDOW5-generated constructions, updating `TotConstructs`, and extending construction names, object counts, or construction/material graph edges",
  "full `ConstructionProps` deep copy, master `TCChildConstrs` allocation, child-to-master global construction links, and nominal R/U, pre-adjusted U, or coefficient-adjustment arrays",
  "source linear lookup and first-match behavior for generated-name collisions, including collisions between rounded temperatures, different masters, existing constructions, or deferred WINDOW5 children",
  "active surface-construction mutation, nearest-temperature state selection and history, fenestration binding, window optics and thermal calculations, daylighting, shading, ratings, EIO/SQLite or other reporting, and runtime behavior",
]
routine.create_tc_constructions.inactive_branches = [
  "with no retained thermochromic master construction, both projection arenas remain empty; an otherwise valid but unused thermochromic group produces no series or child",
  "reusing one thermochromic group from multiple masters produces an independent series and child range for each master without sharing projection identities",
  "the first state always produces a child even though its layers equal the master's already first-state-substituted effective stack; duplicate temperatures, glazing references, and formatted names remain distinct children in input order",
]
routine.create_tc_constructions.unsupported_active_branches = [
  "every typed thermochromic parent remains `UnsupportedSurfaceBoundary` and `RunBlocked`, including an unused parent and one used by one or more projected master constructions; projection materialization never admits partial runtime",
  "series and child identities are private derived state, not public ConstructionIds or object definitions, and do not change object, unsupported-object, capability, manifest, comparator, proof-variable, or conformance counts",
]
routine.create_tc_constructions.not_claimed_branches = [
  "complete source ordering relative to WINDOW5-synthesized constructions, source-global child identity and name lookup, exhaustive fmt 8.0.1 versus Rust generated-name equivalence for arbitrary finite `f64` values beyond the pinned boundaries, invalid-upstream partial allocation or assertion behavior, exact `ErrorsFound` side effects, and full deep-copy field parity",
  "representative-surface assignment output, construction/surface mutation, thermochromic state selection, optics, thermal calculations, daylighting, shading, ratings, EIO/SQLite or other reporting, runtime numerics, exact diagnostics/order/multiplicity, and conformance",
]

routine.check_valid_simulation_objects.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.check_valid_simulation_objects.source_routine = "CheckValidSimulationObjects"
routine.check_valid_simulation_objects.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_valid_simulation_objects.completion_status = "state_mapped"
routine.check_valid_simulation_objects.required_for_full_domain = false
routine.check_valid_simulation_objects.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_valid_simulation_objects.read_state = [
  "the inline `GetHeatBalanceInput` gate immediately after `CreateTCConstructions`: EnergyPlus calls `CheckValidSimulationObjects` only when `TotSurfaces > 0 && NumOfZones == 0`, then calls `CheckUsedConstructions`; the parent routine and exact gate remain source-mapped",
  "bounded Rust enters immediately after the CP103 projection, suppresses the check after any prior compile Error, requires an empty typed Zone arena, and accepts only raw `Shading:Site:Detailed` or `Shading:Building:Detailed` presence as a monotonic positive witness that at least one source Surface exists",
  "validity is raw presence only across EnergyPlus's ordered eight-family test: `SolarCollector:FlatPlate:Water`, `Generator:Photovoltaic`, `Generator:InternalCombustionEngine`, `Generator:CombustionTurbine`, `Generator:FuelCell`, `Generator:MicroCHP`, `Generator:MicroTurbine`, or `Generator:WindTurbine`; fields, names, references, typing, and runtime support are not inspected by this check",
]
routine.check_valid_simulation_objects.write_state = [
  "no model state is written; when the bounded surface witness exists, the typed Zone arena is empty, no prior Error exists, and all eight allowed raw families are absent, the compiler emits one blocking `InvalidSimulationWithoutZones` Error attached to object type `GetHeatBalanceInput` with no object name or field",
  "a prior Error, any typed Zone, absence of both bounded surface witnesses, or presence of any one allowed raw family suppresses this diagnostic without publishing a flag, identity, graph edge, capability, manifest, comparator, proof variable, or runtime state",
  "allowed-family presence affects only this bounded validity diagnostic and never promotes that family to typed input support or runtime execution",
]
routine.check_valid_simulation_objects.history_state_ownership = "the compiler owns no new persistent state for this diagnostic-only check; it reads immutable typed and raw input snapshots and may append one compile Error, with no surface, Zone, generator, collector, or runtime history"
routine.check_valid_simulation_objects.unsupported_state = [
  "the complete EnergyPlus `TotSurfaces` population and order, including legacy rectangular shading, Zone shading, heat-transfer and fenestration surfaces, generated opposite or multiplier surfaces, and every other surface family omitted from the two bounded positive witnesses",
  "input parsing, field/reference validation, simulation state, electrical or thermal behavior, and runtime support for every allowed collector/generator family; raw count presence is not a support claim",
  "the caller's mutable `ErrorsFound` flag, later fatal termination, full input-processor recovery behavior, and interaction with diagnostics or state outside the bounded compiler pass",
]
routine.check_valid_simulation_objects.inactive_branches = [
  "any prior compile Error suppresses the check so a diagnostic is not inferred from partially published typed arenas",
  "a nonempty typed Zone arena or no raw detailed Site/Building shading witness is silent; the latter is inconclusive rather than proof that EnergyPlus `TotSurfaces` is zero",
  "presence of any one of the eight raw allowed families is silent regardless of whether that family has typed or executable Rust support",
]
routine.check_valid_simulation_objects.unsupported_active_branches = [
  "an allowed raw collector/generator witness only suppresses `InvalidSimulationWithoutZones`; its own unsupported-object, malformed-input, or run-blocking treatment remains independent and unchanged",
  "a detected no-Zone invalid-simulation witness is compile-blocking and adds no partial runtime admission or simulation state",
]
routine.check_valid_simulation_objects.not_claimed_branches = [
  "exact parity for `TotSurfaces > 0 && NumOfZones == 0`, negative inference from absent detailed Site/Building shading, any other surface family, legacy `Shading:Site` or `Shading:Building`, generated surfaces, or invalid/raw Zone recovery",
  "exact EnergyPlus severe/fatal text, punctuation, severity mapping, order, multiplicity, `ErrorsFound` timing, or downstream termination; negative construction-use flags and unused warnings beyond CP105 positive metadata, collector/generator semantics, runtime, reporting, and conformance",
]

routine.check_used_constructions.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.check_used_constructions.source_routine = "CheckUsedConstructions"
routine.check_used_constructions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_used_constructions.completion_status = "state_mapped"
routine.check_used_constructions.required_for_full_domain = false
routine.check_used_constructions.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.check_used_constructions.read_state = [
  "the source call immediately after the inline no-Zone validity gate and before the caller's fatal `ErrorsFound` barrier; bounded Rust calls `Compiler::collect_known_construction_use_evidence` after `check_valid_simulation_objects_bounded` in the same relative order",
  "every retained typed `BuildingSurface:Detailed` contributes its already resolved ConstructionId as monotonic known-used evidence; this neither claims the complete EnergyPlus Surface arena nor interprets an absent typed surface as proof of non-use",
  "the six source-ordered raw reference scans and exact fields: `Pipe:Indoor.construction_name`, `Pipe:Outdoor.construction_name`, `Pipe:Underground.construction_name`, `GroundHeatExchanger:Surface.construction_name`, `DaylightingDevice:Tubular.construction_name` corresponding to legacy alpha field 4, then `EnergyManagementSystem:ConstructionIndexVariable.construction_object_name`",
  "raw references contribute only when the field is a nonblank string that case-insensitively resolves to an existing typed ConstructionId; missing fields, blanks, wrong JSON value types, and unresolved names are silent and publish no diagnostic or placeholder",
  "only resolved GroundHeatExchanger and EMS references are candidates for known-CTF-use evidence, and only when the typed construction kind is Opaque, including ordinary and F/C generated variants, or AirBoundary; Fenestration, ComplexFenestration, and WindowEquivalentLayer are the exact excluded window kinds",
  "known-CTF-use is only positive metadata mirroring the source non-window `IsUsedCTF` mark; inclusion of an AirBoundary ConstructionId does not assert that the construction owns or requires CTF coefficient state",
  "any compile Error already present when the collector begins, including one emitted by the preceding CP104 check, makes the collection transactionally inactive and leaves both evidence vectors empty",
]
routine.check_used_constructions.write_state = [
  "`TypedModel::known_used_constructions` is a sorted and deduplicated positive-only ConstructionId set containing the union of retained typed-surface references and all resolved references from the six raw families",
  "`TypedModel::known_ctf_used_constructions` is a separately sorted and deduplicated positive-only ConstructionId subset containing only qualifying non-window GroundHeatExchanger and EMS references; Pipe, tubular daylighting, and retained typed-surface references never imply CTF evidence in this checkpoint",
  "the two vectors add no public object identity, ConstructionId, construction name, object count, graph edge, support row, capability, manifest, comparator, proof variable, diagnostic, or runtime state",
]
routine.check_used_constructions.history_state_ownership = "TypedModel owns two immutable positive-evidence ConstructionId vectors; no mutable `IsUsed`/`IsUsedCTF` flag, surface history, construction history, CTF/CondFD history, or runtime selection state is allocated"
routine.check_used_constructions.unsupported_state = [
  "the complete EnergyPlus global Construction arena after WINDOW5 and thermochromic child append, generated identities absent from the typed ConstructionId arena, all Surface families beyond the retained typed detailed opaque subset, and any other use-marking path outside the six bounded raw scans",
  "negative `IsUsed` or `IsUsedCTF` state, the source count of constructions lacking use, summary and per-name unused-construction warnings, the `DisplayExtraWarnings` branch, and exact warning text/severity/order/multiplicity",
  "downstream CTF, CondFD, HAMT, window, daylighting, pipe, ground-heat-exchanger, EMS, and surface consumers; the raw six-family scan validates no referenced object's own fields, dependencies, or executability",
]
routine.check_used_constructions.inactive_branches = [
  "any prior compile Error leaves both evidence vectors empty so partially published typed arenas cannot yield positive metadata",
  "missing, blank, wrong-typed, or unresolved raw construction references remain silent and add no evidence; they are not reclassified as malformed typed definitions by this routine",
  "duplicate evidence from multiple surfaces, repeated raw records, or overlap between typed-surface and raw references collapses to one ConstructionId in each independently sorted vector",
  "a resolved Fenestration, ComplexFenestration, or WindowEquivalentLayer GroundHeatExchanger/EMS reference adds known-used evidence but no known-CTF-used evidence; the same reference from Pipe or tubular daylighting never adds CTF evidence regardless of construction kind",
]
routine.check_used_constructions.unsupported_active_branches = [
  "raw Pipe, GroundHeatExchanger, DaylightingDevice, and EMS object presence remains outside typed object coverage and runtime support; a resolved construction-reference string contributes metadata only and never promotes the referring family",
  "known-used or known-CTF-used evidence does not remove any existing all-definition run blocker, admit partial runtime, choose a heat-transfer algorithm, or prove that another construction is unused",
]
routine.check_used_constructions.not_claimed_branches = [
  "complete source `IsUsed`/`IsUsedCTF` mutation parity, false-state parity, complete Construction or Surface coverage/order, source invalid-input recovery, WINDOW5 or thermochromic-child identity, and negative inference from either evidence vector",
  "unused construction counts or names, `DisplayExtraWarnings`, exact diagnostics/order/multiplicity, CTF/CondFD selection or initialization, pipe/ground-heat-exchanger/daylighting/EMS semantics, runtime, reporting, numerical parity, capability support, and conformance",
]

routine.init_solar_view_factors.source_file = "src/EnergyPlus/HeatBalanceIntRadExchange.cc"
routine.init_solar_view_factors.source_routine = "InitSolarViewFactors"
routine.init_solar_view_factors.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_solar_view_factors.completion_status = "source_mapped"
routine.init_solar_view_factors.required_for_full_domain = true

routine.manage_internal_heat_gains.source_file = "src/EnergyPlus/InternalHeatGains.cc"
routine.manage_internal_heat_gains.source_routine = "ManageInternalHeatGains"
routine.manage_internal_heat_gains.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_internal_heat_gains.completion_status = "source_mapped"
routine.manage_internal_heat_gains.required_for_full_domain = true

routine.get_internal_heat_gains_input.source_file = "src/EnergyPlus/InternalHeatGains.cc"
routine.get_internal_heat_gains_input.source_routine = "GetInternalHeatGainsInput"
routine.get_internal_heat_gains_input.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_internal_heat_gains_input.completion_status = "state_mapped"
routine.get_internal_heat_gains_input.required_for_full_domain = false
routine.get_internal_heat_gains_input.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_internal_heat_gains_input.read_state = [
  "the line-320 `ManageInternalHeatGains(state, true)` parent call after `InitSolarViewFactors`; bounded Rust invokes `Compiler::parse_bounded_internal_heat_gains_input` after the CP105 construction-use collector, returns before either family when an Error already exists on entry, otherwise scans People before OtherEquipment, and continues into OtherEquipment even when People adds an Error",
  "every `People` definition whose required `zone_or_zonelist_or_space_or_spacelist_name` resolves directly to an existing typed Zone; an optional number-of-people schedule resolves through the existing typed schedule namespace, the calculation method defaults to People and accepts People, People/Area or PeoplePerArea, and Area/Person or AreaPerPerson, and the three finite sizing scalars default to zero and must be nonnegative",
  "every `OtherEquipment` definition whose same target field resolves directly to an existing typed Zone; an optional schedule resolves through the existing typed schedule namespace, fuel type defaults to normalized None, design-level method defaults to EquipmentLevel and accepts the bounded Watts/Area, Power/Area, WattsPerZoneFloorArea, Watts/Person, Power/Person, or WattsPerPerson aliases, and all existing finite numeric/default/range validation remains in force",
  "OtherEquipment retains finite design level and carbon-dioxide generation rate with zero defaults, nonnegative power-per-floor-area and power-per-person with zero defaults, and latent, radiant, and lost fractions each in [0,1] with zero defaults; a sum above one emits the existing blocking typed diagnostic without suppressing later definitions",
]
routine.get_internal_heat_gains_input.write_state = [
  "the existing deterministic People arena and normalized people name map, with dense InternalGainId, normalized name, direct ZoneId, optional ScheduleId, calculation-method enum, number of people, people per floor area, and floor area per person",
  "the existing deterministic OtherEquipment arena and normalized other-equipment name map, with dense InternalGainId, normalized name and fuel type, direct ZoneId, optional ScheduleId, design-level-method enum, design level, power-per-floor-area, power-per-person, latent/radiant/lost fractions, and carbon-dioxide generation rate",
  "family-level diagnostic projection ordered People before OtherEquipment for an error-free entry; validation remains definition-local, so a malformed People object does not prevent the bounded OtherEquipment family from being scanned in the same wrapper invocation",
]
routine.get_internal_heat_gains_input.history_state_ownership = "TypedModel owns immutable bounded People and OtherEquipment declaration state already used by downstream Rust paths; CP107 adds no mutable EnergyPlus internal-gain history, one-time input flag, runtime rate, schedule sample, meter, output, or EMS state"
routine.get_internal_heat_gains_input.unsupported_state = [
  "Lights, ElectricEquipment, GasEquipment, HotWaterEquipment, SteamEquipment, ElectricEquipment:ITE:AirCooled, ZoneBaseboard:OutdoorTemperatureControlled, and ZoneContaminantSourceAndSink:CarbonDioxide source-family input and all of their dependent state",
  "ZoneList, Space, and SpaceList target expansion; source-generated gain instances and declaration order; Space and Zone occupant totals; `setupIHGZonesAndSpaces`, `setDesignLevel`, floor-area/person-derived design-level materialization, schedule-minimum checks, nominal min/max gain arrays, and Space heat-balance topology",
  "ManageInternalHeatGains runtime branches and recurring summation/update behavior, daylighting order beyond the mapped parent call, reporting variables, EIO, meters, contaminant coupling, EMS actuators/internal variables, component-load reporting, and all numerical runtime state",
]
routine.get_internal_heat_gains_input.inactive_branches = [
  "any compile Error present on wrapper entry suppresses both bounded family scans and publishes no additional People or OtherEquipment state from this pass",
  "when neither bounded family is present, the wrapper is an empty no-op and adds no identity, count, capability, runtime state, or diagnostic",
  "a missing optional schedule reference retains no ScheduleId; absent optional/defaulted numeric, fuel, and method fields retain their existing typed defaults without creating schedule or derived design-level state",
]
routine.get_internal_heat_gains_input.unsupported_active_branches = [
  "a People Error created after entry does not suppress the OtherEquipment scan, but no claim follows for exact source recovery state, complete diagnostic ordering, or arbitrary compiler passes after this bounded wrapper",
  "a valid direct-Zone People or OtherEquipment definition retains its pre-existing typed support and downstream bounded uses only; CP107 does not promote full internal-gain runtime execution or any omitted family",
]
routine.get_internal_heat_gains_input.not_claimed_branches = [
  "complete `ManageInternalHeatGains` or `GetInternalHeatGainsInput` parity, source `GetInputFlag` persistence and re-entry behavior, `InitOnly` control beyond this compile-time projection, exact object/declaration ordering within either family, partial-allocation recovery, exact diagnostics/text/severity/order/multiplicity, or exact global compiler short-circuit behavior after Errors created inside the pass",
  "ZoneList/Space/SpaceList expansion, generated instances, Space/Zone occupant totals, every omitted internal-gain family, setup and design-level derivation, schedule minima, reporting, meters, EMS, runtime gains, numerical parity, capability expansion, object-count or graph expansion, and conformance",
]

routine.setup_kiva_instances.source_file = "src/EnergyPlus/HeatBalanceKivaManager.cc"
routine.setup_kiva_instances.source_routine = "setupKivaInstances"
routine.setup_kiva_instances.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.setup_kiva_instances.completion_status = "source_mapped"
routine.setup_kiva_instances.required_for_full_domain = false

routine.surface_octree_init.source_file = "src/EnergyPlus/SurfaceOctree.cc"
routine.surface_octree_init.source_routine = "init"
routine.surface_octree_init.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.surface_octree_init.completion_status = "source_mapped"
routine.surface_octree_init.required_for_full_domain = false

routine.set_computed_geometry.source_file = "src/EnergyPlus/DataSurfaces.cc"
routine.set_computed_geometry.source_routine = "set_computed_geometry"
routine.set_computed_geometry.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.set_computed_geometry.completion_status = "state_mapped"
routine.set_computed_geometry.required_for_full_domain = false
routine.set_computed_geometry.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.set_computed_geometry.read_state = [
  "the complete source loop at `ManageHeatBalance` lines 182-184, after sizing-mode and octree work but before the line-186 one-time flag clear; bounded Rust invokes `Compiler::set_bounded_surface_computed_geometry` after the CP107 executable slice while CP108-CP110 remain mapped or deferred, and returns without mutation when any compile Error already exists",
  "every retained typed `BuildingSurface:Detailed` Surface in dense typed order, reading only its already canonicalized world-coordinate `Point3` vertices; source `SurfaceGeometry.cc::GetVertices` world-coordinate assignment plus `ProcessSurfaceVertices` shape assignment and `isRectangle` are dependencies rather than promoted routines",
  "a positive-only candidate gate accepting exactly three or four vertices, all finite; a finite nonzero source-ordered Newell normal and finite plane; finite nonzero projected twice-signed area; and conservative coplanarity whose point-to-plane distance is at most `1.0e-9 * max(1, max(abs(world coordinate)))`",
  "for four-vertex candidates only, positive finite length for every wrap edge, source diagonal lengths 1-3 and 2-4 differing by strictly less than 0.020 m, and the absolute dot product of normalized source edges 3-2 and 2-1 no greater than `cos(89 degrees)`; three-vertex candidates map directly to `SurfaceShapeCategory::Triangular`, while admitted four-vertex candidates map to `Rectangular`",
  "source-ordered Newell accumulation without normal normalization; projection-axis selection from the largest absolute plane-normal component with X-over-all and then Y-over-Z tie precedence; X-to-(y,z), Y-to-(x,z), or Z-to-(x,y) projection; projected bounds; strict negative-area reversal of vertices 2 through N only; then wrap-edge construction and Rectangle-only projected edge-1/edge-4 squared lengths",
]
routine.set_computed_geometry.write_state = [
  "each admitted Surface receives `Some(SurfaceComputedGeometry)` containing its `SurfaceShapeCategory`, source-shaped four-element plane, `SurfaceProjectionAxis`, ordered `projected_vertices`, `projected_lower_bound`, `projected_upper_bound`, ordered `projected_edges`, and `rectangle_side_1_squared_m2` plus `rectangle_side_3_squared_m2`",
  "a negative projected signed area reverses only the copied projected tail before edges are written and never mutates the source 3D vertices or plane orientation; a Triangle stores exact zero for both rectangle side-square fields",
  "every excluded retained Surface receives `computed_geometry = None` without a diagnostic, identity, name-map entry, object-count increment, graph edge, support/capability change, manifest, comparator, proof variable, or runtime state; exclusion of one Surface does not suppress eligible later entries",
]
routine.set_computed_geometry.history_state_ownership = "`TypedModel::surfaces` owns immutable compile-time derived `Surface::computed_geometry` attachments only; CP111 allocates no mutable source input flag, octree pointer, intersection cache, Surface history, timestep state, reporting state, or runtime geometry consumer"
routine.set_computed_geometry.unsupported_state = [
  "the complete EnergyPlus Surface array and its source order, all generated/opposite surfaces, FenestrationSurface:Detailed, shading, InternalMass, doors/windows, overhangs/fins, and every other omitted Surface family or source shape",
  "source `Shape`, `IsConvex`, Convex and Nonconvex categories, nonrectangular quadrilaterals, polygons, `Surface2DSlab`, unique slab-Y state, inverse edge slopes, and the convex `n >= 20` slab switch",
  "complete `GetVertices` canonicalization and recovery, `CheckConvexity`, coincident/collinear deletion, planarity checking and diagnostics, `ProcessSurfaceVertices`, full `isRectangle` lifecycle, source partial writes/default objects, one-time flag ownership and clearing, octree/PierceSurface/intersection consumers, runtime, reporting, performance, and numerical conformance",
]
routine.set_computed_geometry.inactive_branches = [
  "any compile Error present on pass entry preserves the pre-pass computed-geometry state for every retained Surface and emits no CP111 diagnostic",
  "an empty Surface arena is a no-op; a retained Surface with a vertex count other than three or four, any nonfinite or overflow-derived value, a zero Newell normal or signed projected area, conservative noncoplanarity, or a rejected four-vertex rectangle remains `None` and does not affect another Surface",
  "strict source boundaries are retained for rectangle diagonal difference below 0.020 m, the inclusive cosine threshold, X-then-Y projection ties, and signed-area reversal only below zero; no tolerance is added to those branch comparisons beyond the separately declared conservative coplanarity gate",
]
routine.set_computed_geometry.unsupported_active_branches = [
  "valid computed geometry remains metadata only and grants no new object support, runtime admission, daylighting, shading, solar, octree, ray-intersection, or heat-balance behavior",
  "a four-vertex input that source local `isRectangle` could accept only through zero-vector normalization is deliberately rejected, as are finite source inputs whose derived state overflows or whose missing upstream validation cannot be conservatively established",
  "typed Surface order is preserved for the bounded pass, but this does not claim complete global Surface count/order, generated-family placement, source reordering, or exact loop side effects outside the retained arena",
]
routine.set_computed_geometry.not_claimed_branches = [
  "complete `SurfaceData::set_computed_geometry`, `computed_shapeCat`, `computed_plane`, `computed_surface2d`, `Surface2D` construction, `GetVertices`, `ProcessSurfaceVertices`, `CheckConvexity`, or `isRectangle` parity; source invalid-input recovery, partial mutation/default retention, exact diagnostic text/severity/order/multiplicity, or one-time flag clearing and re-entry",
  "Convex/Nonconvex and slab state, complete Surface families/order, octree/PierceSurface and every computed-geometry consumer, exact cross-language floating-point behavior, runtime numerical behavior or performance, output/report serialization, capability/support expansion, conformance cases, or numerical conformance",
]

routine.get_zone_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_zone_data.source_routine = "GetZoneData"
routine.get_zone_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_zone_data.completion_status = "source_mapped"
routine.get_zone_data.required_for_full_domain = true
routine.get_zone_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_zone_data.read_state = [
  "after all `ProcessZoneData` calls complete, EnergyPlus derives nominal control for every Zone, processes every ZoneList and ZoneGroup, calls `GetZoneLocalEnvData`, allocates ZonePreDefRep, then calls `GetSpaceData`; bounded Rust preserves those declaration phases while explicitly omitting the intervening reporting allocation",
  "nominal control is a case-insensitive field-value existence scan independent of full equipment-connection field parsing; every Zone is explicitly retained as true for a matching raw zone name or false otherwise",
  "each ZoneList has one required nonblank outer-key name and a nonempty `zones` array whose ordered entries each require one case-insensitively resolved Zone name; staged IDF lists use recovered declaration order while native epJSON lists use lexical outer-key order",
  "each ZoneGroup has one required nonblank outer-key name, one required case-insensitively resolved ZoneList name, and an integer multiplier defaulting to one in the source-compatible positive signed-integer range; staged IDF groups use recovered declaration order while native epJSON groups use lexical outer-key order",
  "each ZoneProperty:LocalEnvironment has one required nonblank outer-key name, one required case-insensitively resolved nonblank Zone name, and an optional outdoor-air node name; bounded Rust initializes NodeList declarations immediately before this phase because EnergyPlus `GetOnlySingleNode` lazily initializes them",
  "GetSpaceData reads authored Space then SpaceList outer keys in lexical order for both staged IDF and native epJSON, preserves each tag/member array order, and finally visits Zones in typed Zone order to create whole-zone defaults",
]
routine.get_zone_data.write_state = [
  "each typed Zone retains `is_nominal_controlled`, default false and set true solely by the bounded raw equipment-connection scan without claiming full connection validity",
  "a deterministic dense ZoneList arena and independent normalized name map retain each validated `ZoneListId`, normalized name, ordered ZoneId members, and maximum authored member-name length; empty, malformed, unresolved, or duplicate members fail before identity publication, while a Zone-name collision emits a nonblocking warning",
  "a deterministic dense ZoneGroup arena and independent normalized name map retain each validated `ZoneGroupId`, normalized name, ZoneListId, and multiplier; repeated list use or grouped-list Zone overlap fails before publication, while every valid member Zone receives the multiplier and the source-shaped ZoneList identity in `list_group`",
  "a deterministic dense ZoneLocalEnvironment arena and independent normalized name map retain each validated declaration, its ZoneId, and optional generic NodeId; each nonblank node resolves a one-member NodeList alias or registers/reuses a direct node, and ordered nonblank links overwrite the Zone link while a later blank node does not clear it",
  "deterministic Space, SpaceList, and first-seen SpaceType state retain validated authored declarations, ordered Zone/member links, then one whole-zone default for each Zone still without a Space",
  "every valid ZoneList or ZoneGroup is reported as `UnsupportedZoneGrouping`, every valid ZoneProperty:LocalEnvironment as `UnsupportedZoneLocalEnvironment`, and every authored Space or SpaceList as `UnsupportedSpacePartitioning`; the later CreateMissingSpaces contract applies that partition boundary to generated remainders while leaving sole whole-zone defaults inactive",
]
routine.get_zone_data.history_state_ownership = "TypedModel owns immutable nominal-control, ZoneList, ZoneGroup, ZoneLocalEnvironment, Space, SpaceList, SpaceType, and per-Zone list/local-node/space declaration state only; this checkpoint allocates no mutable local weather, geometry, sizing, reporting, equipment, surface, space-air, or zone-air history"
routine.get_zone_data.unsupported_state = [
  "the pre-Zone-loop ZoneDaylight and resilience allocations and the post-`GetZoneLocalEnvData` ZonePreDefRep allocation",
  "ZoneList expansion for People, gains, thermostat, sizing, and every other Zone-or-ZoneList consumer, plus comprehensive `Zone.Multiplier * Zone.ListMultiplier` consumption across geometry, loads, HVAC flows, sizing, and reports",
  "OutdoorAir:Node condition inputs and node-connection metadata, Space-or-SpaceList target expansion, SetupZoneGeometry behavior beyond the separately bounded CP97 GetHTSurfaceData/CreateMissingSpaces surface-membership composite, local weather and EMS state, space heat balance, reporting, runtime numerical behavior, and conformance evidence",
]
routine.get_zone_data.inactive_branches = [
  "when no raw equipment connection names a Zone, its nominal-control flag remains false without a diagnostic; unmatched connection names mark no Zone in this bounded phase",
  "when no ZoneList, ZoneGroup, or ZoneProperty:LocalEnvironment exists, every Zone retains list multiplier one, no list-group identity, and no linked outdoor-air node, adding no boundary from those absent families",
  "a ZoneGroup multiplier omitted from valid input retains one but still records group membership exactly like an authored multiplier and remains inside the fail-closed grouping boundary",
  "a blank or missing local-environment node remains an explicit no-node declaration and does not clear an earlier nonblank link for the same Zone",
  "when no authored Space exists, GetSpaceData still creates one General whole-zone default per Zone; that default adds no space-partition runtime boundary, including when every valid typed detailed surface explicitly references the sole default and no remainder is needed",
]
routine.get_zone_data.unsupported_active_branches = [
  "every valid ZoneList, including an otherwise unused definition, is typed but blocks arbitrary runtime execution until all Zone-or-ZoneList consumers expand it",
  "every valid ZoneGroup, including multiplier one, is typed but blocks arbitrary runtime execution until list multiplier semantics are comprehensively consumed",
  "every valid ZoneProperty:LocalEnvironment, including an otherwise unused definition or one with a blank node, is typed but blocks arbitrary runtime execution until local weather consumers are wired",
  "every authored Space and every SpaceList, including an otherwise unused or empty list, is typed but blocks arbitrary runtime execution until space partitioning and all downstream consumers are wired; generated remainder Spaces are blocked by the separate CreateMissingSpaces contract",
  "typed BuildingSurface:Detailed lookup and same-Zone validation belong to the bounded GetHTSurfaceData cross-section, while final fallback assignment and mixed-assignment remainder generation belong to the state-mapped CreateMissingSpaces contract; the composite does not promote full GetHTSurfaceData or this parent routine",
  "the nominal-control scan does not validate or execute ZoneHVAC equipment; the existing later typed equipment-connection and IdealLoads boundaries remain authoritative",
]
routine.get_zone_data.not_claimed_branches = [
  "complete GetZoneData parity, broad compiler pass-order parity, source partial-allocation and invalid-input recovery side effects, exact diagnostics/text/order/multiplicity, whitespace-preserving or case-colliding names, complete shared Zone/Space/ZoneList/SpaceList namespace behavior, full OutdoorAir:Node/NodeList and local-weather state, ZonePreDefRep, SetupZoneGeometry beyond the separate bounded surface-membership slice, sizing, reporting, numerical parity, and conformance",
]

routine.get_zone_local_env_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_zone_local_env_data.source_routine = "GetZoneLocalEnvData"
routine.get_zone_local_env_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_zone_local_env_data.completion_status = "state_mapped"
routine.get_zone_local_env_data.required_for_full_domain = false
routine.get_zone_local_env_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_zone_local_env_data.read_state = [
  "EnergyPlus calls `GetZoneLocalEnvData` after ZoneGroup processing and before ZonePreDefRep allocation and `GetSpaceData`; bounded Rust moves existing NodeList parsing immediately before this phase because `GetOnlySingleNode` lazily initializes NodeLists",
  "each ZoneProperty:LocalEnvironment has a required nonblank outer-key name and case-insensitively resolved nonblank `zone_name`; `outdoor_air_node_name` is optional, and blank or missing input retains no node; staged IDF instances use recovered declaration order while native epJSON instances use lexical outer-key order",
  "a nonblank node first resolves a typed NodeList alias, accepting exactly one member and rejecting multiple members; otherwise an existing generic NodeId is reused or a new generic NodeId is registered",
  "EnergyPlus 26.1 checks OutdoorAir:Node membership only inside a short-circuited `NodeNum == 0` condition, so bounded declaration state likewise does not require a nonblank direct node to have an OutdoorAir:Node definition",
  "ordered declarations for the same Zone are allowed; each nonblank resolved node overwrites that Zone's link while a later blank node does not clear the previous link, so the last nonblank node wins",
]
routine.get_zone_local_env_data.write_state = [
  "a deterministic dense ZoneLocalEnvironment arena and independent normalized name map retain each fully validated ZoneLocalEnvironmentId, normalized name, ZoneId, and optional generic NodeId; validation completes before identity, node, or Zone-link publication, so an invalid earlier record consumes none of those bounded identities or side effects",
  "each Zone retains an optional linked outdoor-air NodeId; a one-member NodeList stores its member node, a direct name registers or reuses that generic node, and blank input stores no node",
  "every valid ZoneProperty:LocalEnvironment definition is reported as `UnsupportedZoneLocalEnvironment` and `RunBlocked` before arbitrary runtime execution because mutable local-weather conditions and their consumers are not wired",
]
routine.get_zone_local_env_data.history_state_ownership = "TypedModel owns immutable local-environment declaration and per-Zone optional generic-node linkage only; this checkpoint allocates no mutable dry-bulb, wet-bulb, wind, psychrometric, EMS, surface, load, or zone-air history"
routine.get_zone_local_env_data.unsupported_state = [
  "complete OutdoorAir:Node condition input including height, dry-bulb/wet-bulb/wind schedules, wind-pressure-coefficient curve and symmetry/angle controls, plus NodeConnection metadata",
  "AnyLocalEnvironmentsInModel and mutable local dry-bulb, wet-bulb, humidity, pressure, wind, psychrometric, EMS, weather, surface, infiltration, load, and zone-air consumer state",
  "source-sized partial records and invalid-input side effects and the post-routine ZonePreDefRep allocation; GetSpaceData declaration/default state belongs to a separate bounded contract",
]
routine.get_zone_local_env_data.inactive_branches = [
  "when no ZoneProperty:LocalEnvironment exists, the arena stays empty, every Zone node link stays absent, and no local-environment runtime boundary is added",
  "a blank or missing node is a valid no-node declaration and does not clear an earlier nonblank link for the same Zone",
  "multiple declarations may reference one Zone; only ordered declarations with a nonblank resolved node overwrite its link",
]
routine.get_zone_local_env_data.unsupported_active_branches = [
  "every valid definition, including an otherwise unused record or a record with no node, blocks arbitrary runtime execution until local-weather consumers are wired",
  "a nonblank direct generic NodeId does not claim an OutdoorAir:Node declaration or any local environmental conditions",
]
routine.get_zone_local_env_data.not_claimed_branches = [
  "complete GetZoneLocalEnvData parity, source preallocation and partial invalid-record/node/link side effects, the source multi-member NodeList first-node side effect, exact diagnostics/text/order/multiplicity, whitespace-preserving or case-colliding names, OutdoorAir:Node condition state, local-weather consumers, ZonePreDefRep, geometry, reporting, numerical parity, and conformance",
]

routine.get_space_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_space_data.source_routine = "GetSpaceData"
routine.get_space_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_space_data.completion_status = "state_mapped"
routine.get_space_data.required_for_full_domain = false
routine.get_space_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_space_data.read_state = [
  "EnergyPlus calls `GetSpaceData` after `GetZoneLocalEnvData` and ZonePreDefRep allocation and before `SetupZoneGeometry`; bounded Rust calls its declaration/default phase immediately after local-environment parsing while leaving that intervening reporting allocation deferred",
  "authored Space outer keys are always lexical for both staged IDF and native epJSON because the source directly iterates the JSON object map; each Space requires a nonblank name and resolved nonblank Zone, while ceiling height, volume, and floor area preserve every finite number or default missing, blank, and Autocalculate input to AutoCalculate",
  "space type defaults to General for missing or blank input and joins a case-insensitive first-seen lexical-order registry; tag objects preserve array order, normalized text, duplicates, and empty or missing tag strings",
  "SpaceList outer keys are likewise always lexical while member arrays preserve authored order; missing or empty member arrays are valid, each present member resolves an authored Space, duplicate resolved members fail, and list names colliding with a Zone or authored Space fail",
  "after all lists are processed, Zones are visited in typed order and each Zone without an authored Space receives one appended whole-zone default named after the Zone with AutoCalculate geometry, no tags, and the General space type; its late position keeps it unavailable to the preceding SpaceList lookup while the later bounded GetHTSurfaceData cross-section can resolve it through the full pre-remainder Space arena",
]
routine.get_space_data.write_state = [
  "a deterministic dense Space arena retains each fully validated authored declaration followed by generated whole-zone defaults; authored names alone enter the reference map, validation completes before SpaceId, SpaceTypeId, or Zone-link publication, and each Space retains its ZoneId, three AutoOrNumber selectors, normalized type name/id, ordered tags, and origin",
  "each Zone retains ordered SpaceIds and exits the routine with at least one Space; generated defaults reuse or append General through the separately bounded `GetGeneralSpaceTypeNum` helper and remain outside the authored-name map used by the preceding SpaceList phase",
  "a deterministic dense SpaceList arena and normalized name map retain each validated SpaceListId, lexical name, ordered authored SpaceIds, and maximum authored member-name length, including valid zero-member lists",
  "every authored Space and SpaceList is reported as `UnsupportedSpacePartitioning` and `RunBlocked` before arbitrary runtime execution; the separate CreateMissingSpaces contract applies the same boundary to generated remainder Spaces, while sole generated whole-zone defaults remain inactive even when valid detailed surfaces explicitly reference them",
]
routine.get_space_data.history_state_ownership = "TypedModel owns immutable authored/default Space, SpaceList, SpaceType, and per-Zone SpaceId declaration topology only; this checkpoint allocates no mutable geometry, surface, space-air, zone-air, gain, HVAC, sizing, or reporting history"
routine.get_space_data.unsupported_state = [
  "the preceding ZonePreDefRep allocation and all predefined-report mutation",
  "SetupZoneGeometry behavior beyond the separately bounded CP97 composite of GetHTSurfaceData typed detailed-surface lookup/same-Zone validation and CreateMissingSpaces remainder/fallback finalization: other surface families, opposite-surface generation, reordering, Space surface lists, calculated height/volume/floor area, floor/volume fractions, enclosure and surface ranges, and geometry correction/warnings",
  "Space-or-SpaceList target expansion for internal gains, infiltration, mixing, ventilation, sizing, outdoor air, HVAC, outputs, and every space heat-balance/reporting/runtime consumer",
]
routine.get_space_data.inactive_branches = [
  "with no authored Space, every Zone receives one General whole-zone default and no UnsupportedSpacePartitioning boundary is added when detailed surfaces are all implicit or all validly explicit to that sole default",
  "a Zone with one or more authored Spaces receives no whole-zone default; if every Zone is covered and all authored types are non-General, the registry contains no General entry",
  "a SpaceList with a missing or empty member array is retained as a valid zero-member list but remains inside the all-definition runtime boundary",
]
routine.get_space_data.unsupported_active_branches = [
  "every authored Space, including one using only defaults and not referenced by a surface or load, blocks arbitrary runtime execution until space partition consumers are wired",
  "every SpaceList, including an empty or otherwise unused definition, blocks arbitrary runtime execution until all Space-or-SpaceList consumers expand it",
  "typed BuildingSurface:Detailed lookup and same-Zone validation belong to the bounded GetHTSurfaceData cross-section, while final fallback assignment and mixed-assignment remainder generation belong to the state-mapped CreateMissingSpaces contract; this declaration routine does not claim either part",
]
routine.get_space_data.not_claimed_branches = [
  "complete GetSpaceData parity, source allocation sizes and partial invalid Space/SpaceType/Zone/list side effects, exact diagnostics/text/order/multiplicity, source whitespace preservation or case-colliding same-family names, complete shared Zone/ZoneList/Space/SpaceList namespace behavior, SetupZoneGeometry beyond the separately bounded typed detailed-surface membership slice, loads, space heat balance, HVAC, reporting, numerical parity, and conformance",
]

routine.get_general_space_type_num.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.get_general_space_type_num.source_routine = "GetGeneralSpaceTypeNum"
routine.get_general_space_type_num.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_general_space_type_num.completion_status = "state_mapped"
routine.get_general_space_type_num.required_for_full_domain = false
routine.get_general_space_type_num.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_general_space_type_num.read_state = [
  "GetSpaceData calls `GetGeneralSpaceTypeNum` while generating a whole-zone default and CreateMissingSpaces calls it while generating a mixed-assignment remainder; the helper searches the ordered type registry built from validated authored Spaces and any earlier generated default using case-insensitive General matching",
]
routine.get_general_space_type_num.write_state = [
  "the helper reuses the first existing General SpaceTypeId or appends one General entry at the end and returns that dense identity; every generated whole-zone default and remainder in the same model reuses it",
]
routine.get_general_space_type_num.history_state_ownership = "TypedModel owns the immutable ordered normalized SpaceType name registry and dense IDs only; the helper allocates no mutable simulation history"
routine.get_general_space_type_num.unsupported_state = [
  "EnergyPlus allocation capacity and global integer numSpaceTypes storage beyond the equivalent dense typed registry",
  "all downstream type-based grouping, gains, reporting, geometry, HVAC, and space heat-balance consumers",
]
routine.get_general_space_type_num.inactive_branches = [
  "when every Zone already has an authored Space and no mixed explicit/blank surface assignment requires a remainder, the helper is not called, so a non-General-only authored registry remains unchanged",
  "when General already exists from an authored Space or earlier generated default, later default or remainder creation reuses that identity without appending another type",
]
routine.get_general_space_type_num.unsupported_active_branches = [
  "generated whole-zone default creation and General identity alone add no runtime boundary; authored Space and SpaceList definitions are blocked by the parent GetSpaceData contract, while generated remainders are blocked by the separate CreateMissingSpaces contract",
]
routine.get_general_space_type_num.not_claimed_branches = [
  "complete helper parity, source one-based numeric identity, allocation/counter side effects, whitespace-preserving labels, exact diagnostics, downstream type consumers, numerical parity, and conformance",
]

routine.process_zone_data.source_file = "src/EnergyPlus/HeatBalanceManager.cc"
routine.process_zone_data.source_routine = "ProcessZoneData"
routine.process_zone_data.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.process_zone_data.completion_status = "state_mapped"
routine.process_zone_data.required_for_full_domain = false
routine.process_zone_data.state_mapping_ref = "docs/src/porting-map/heat-balance-source-map.md"
routine.process_zone_data.read_state = [
  "EnergyPlus calls `GetBuildingData` after `GetConstructData` and orders `GetShadowingInput`, `GetZoneData`, then `SetupZoneGeometry`; bounded Rust state covers only each Zone declaration processed at the start of `GetZoneData`, while both wrappers remain source-mapped",
  "one required nonblank outer-key name; finite relative-north and x/y/z origins default to zero without numeric bounds, Type defaults to and must equal integer 1, and Multiplier defaults to integer 1 with source-compatible positive signed-integer range",
  "Ceiling Height, Volume, and Floor Area retain every finite authored number including zero or negative values, while missing, blank, or `Autocalculate` input retains an explicit `AutoCalculate` selector",
  "inside overrides accept Simple, TARP, CeilingDiffuser, TrombeWall, AdaptiveConvectionAlgorithm, or ASTMC1340; outside overrides accept SimpleCombined, TARP, MoWiTT, DOE-2, or AdaptiveConvectionAlgorithm; blank or missing values inherit already parsed global selections, defaulting to TARP and DOE-2 when no global object exists",
  "Part of Total Floor Area defaults to Yes and retains Yes as true or No as false; staged IDF Zone instances use recovered declaration order while native epJSON instances use lexical key order",
]
routine.process_zone_data.write_state = [
  "a deterministic dense `Zone` arena and normalized name map retain each validated `ZoneId`, normalized name, relative-north angle, origin, source-forced standard Type value 1, positive multiplier, three `AutoOrNumber` selectors, and total-floor-area membership flag",
  "each convection selection retains both its effective enum and whether it was inherited or authored locally; all fields and normalized duplicate checks complete before ID/name publication, so an invalid earlier record does not consume a dense identity",
  "positive authored Floor Area is preferred by shared runtime geometry, schedule/internal-gain, CLI IdealLoads, and typed IdealLoads outdoor-air consumers; nonpositive or autocalculated values fall through to the available geometry path rather than being normalized during declaration parsing",
  "any zone-local convection override is reported as `UnsupportedZoneConvectionOverride` and `RunBlocked` before arbitrary runtime execution because the current coefficient paths do not consume zone-local selectors",
]
routine.process_zone_data.history_state_ownership = "TypedModel owns immutable Zone declaration descriptors only; this checkpoint allocates no mutable zone-air, surface, weather, equipment, geometry, reporting, or predictor/corrector history"
routine.process_zone_data.unsupported_state = [
  "the remaining `GetZoneData` stages and allocations outside this routine: pre-loop ZoneDaylight and resilience arrays, the separately bounded post-Zone collection, local-environment, and Space declaration/default contracts, plus post-local-environment ZonePreDefRep allocation",
  "`SetupZoneGeometry` realization of ceiling height, volume, and floor area; five-percent comparison warnings, space-area adjustment, nonpositive-volume recovery, coordinate-system warnings, centroids, bounds, and other surface-derived state",
  "four zone outdoor-air output registrations, weather and EMS updates, actual inside/outside convection coefficient selection, CeilingDiffuser recovery, building floor-area reporting, EIO/SQLite output, and conformance evidence",
]
routine.process_zone_data.inactive_branches = [
  "missing or blank local convection fields inherit the effective project setting and do not add a runtime boundary; when the project object is absent the retained effective defaults are TARP inside and DOE-2 outside",
  "missing, blank, or `Autocalculate` geometry selectors remain explicit auto state; authored zero or negative values remain finite declaration values but are treated as geometry selectors requiring downstream realization",
  "Part of Total Floor Area equal to No is retained but has no current building-total reporting consumer; Type always materializes as the standard value 1",
]
routine.process_zone_data.unsupported_active_branches = [
  "every authored zone-local inside or outside convection override, including one equal to the inherited effective value, is typed but blocks arbitrary runtime execution until the zone-local coefficient consumer is wired",
  "Space declaration/default topology, nominal control, Zone collections, and local-environment node linkage belong to separate bounded GetSpaceData, GetZoneData, and GetZoneLocalEnvData contracts; typed detailed-surface lookup/same-Zone validation and remainder/fallback assignment belong to the bounded CP97 GetHTSurfaceData/CreateMissingSpaces composite, while full GetHTSurfaceData and broader geometry realization remain deferred",
]
routine.process_zone_data.not_claimed_branches = [
  "complete `GetBuildingData` or `GetZoneData` parity, broad Rust compiler pass-order parity, source control-character restoration, source/native canonical enum-case behavior, whitespace-preserving or case-colliding names, shared Zone/Space/ZoneList/SpaceList namespace uniqueness, invalid-input recovery, exact diagnostics/order/multiplicity, downstream geometry correction and warnings, output registration, runtime convection, reporting, numerical parity, and conformance",
]

[[algorithm]]
id = "heat_balance_surface_manager_source_order"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceSurfaceManager.cc",
  "src/EnergyPlus/HeatBalanceSurfaceManager.hh",
  "src/EnergyPlus/HeatBalFiniteDiffManager.hh",
  "src/EnergyPlus/ThermalComfort.cc",
  "src/EnergyPlus/ThermalComfort.hh",
  "src/EnergyPlus/OutputReportTabular.cc",
  "src/EnergyPlus/OutputReportTabular.hh",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::manage_surface_heat_balance_stage",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::calc_heat_balance_outside_surf_stage",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::calc_heat_balance_inside_surf_stage",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::update_final_surface_heat_balance_stage",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::update_thermal_histories_stage",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::report_surface_heat_balance_stage",
]
status = "scaffold"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Surface Inside Face Temperature",
  "Surface Outside Face Temperature",
]
claim_level = "none"
support_boundary = "Source-order scaffold only: CP116 expands the existing required `routine.manage_surface_heat_balance` source mapping for the unconditional `HeatBalanceManager.cc` line-209 call and the complete `HeatBalanceSurfaceManager.cc::ManageSurfaceHeatBalance` lines 145-230 parent body. In source order, the driver performs the first-time initialization display; `InitSurfaceHeatBalance`; first-time outside display; `CalcHeatBalanceOutsideSurf`; first-time inside display; `CalcHeatBalanceInsideSurf`; first-time air display; `ManageAirHeatBalance`; `UpdateFinalSurfaceHeatBalance`; `AnyCTF || AnyEMPD`-guarded `UpdateThermalHistories`; an `AnyCondFD`-guarded complete-Surface loop that skips construction numbers at most zero, windows, and non-CondFD algorithms before `SurfaceFD(SurfNum).UpdateMoistureBalance()`; `ManageThermalComfort(state, false)`; `ReportSurfaceHeatBalance`; `ZoneSizingCalc`-guarded `GatherComponentLoadsSurface`; `CalcThermalResilience`; the three independently guarded thermal, CO2, and visual resilience reports; then `ManageSurfaceHeatBalancefirstTime = false`. Rust exposes `manage_surface_heat_balance_stage` as metadata and `manage_surface_heat_balance_source_order_path` as an identity wrapper, but the current limited timestep wrapper encloses only the limited Init/Outside path, closes before the Air-manager shell, executes Air before the separately wrapped Inside pass, runs Final/History later, and wraps reporting from the run-period path; it therefore does not match or close the full source parent order. Existing narrow opaque-surface, CTF, face-temperature, zone-air, history, report, and promoted-variable implementations and claims remain unchanged. Complete first-time/display lifecycle, every child implementation and exact side effect, radiant/HVAC iteration, complete Surface/Construction/CTF/EMPD/CondFD/window state, finite-difference moisture updates, thermal comfort, reporting, sizing component loads, resilience, runtime numerics, performance, outputs, and broad conformance remain unclaimed. CP116 adds no row, source, project-contract, Rust/state, support, count, comparator, proof-variable, result, or conformance entry. CP117 maps the inline lines-158-160 `ManageSurfaceHeatBalancefirstTime` guard and exact `Initializing Surfaces` message without a synthetic routine. `HeatBalanceSurfaceManager.hh`, now listed as the declaration/reset source, defaults the member true at line 239 and resets it true in `clear_state()` at line 291. That shared flag remains true through all four parent progress guards and is written false only at successful parent-tail line 229; an abnormal exit can therefore preserve true and allow repeated progress output on re-entry. Rust has no persistent equivalent flag or progress-output implementation, and its identity-wrapper one-closure execution/nesting is not lifecycle parity. Exact output routing/format/callback behavior, the still-unmapped inside- and air-balance displays, implementation of the outside display mapped by CP119, abnormal/fatal behavior, complete child effects, runtime numerics, performance, outputs, and broad conformance remain unclaimed. CP117 adds no routine, project-contract, Rust/state, support, count, comparator, proof-variable, result, or conformance entry. CP118 adds canonical required `routine.init_surface_heat_balance` and the heat-balance project-contract entry for the unconditional parent line-161 call and `HeatBalanceSurfaceManager.cc` lines 272-621 implementation. Its flag-driven source order covers per-Surface outdoor weather, local nodes, and EMS overrides; BeginSim allocations/InterZoneWindow and active-construction properties; per-call variable absorptance; BeginEnvrn histories; movable insulation; ground reflectance; complex fenestration, anisotropic sky, shading, interior LW/SW exchange, daylighting, airflow windows, solar/internal gains and convection; BeginSim predefined reporting; CondFD init; CTF/EMPD plus source/sink history constants; radiant/PV/pool/HVAC-source resets; sizing gathering; completion display; and its own first-time clear. Existing `init_surface_heat_balance_stage` metadata and identity wrapper remain intentionally omitted from `rust_target`: the current wrapper only encloses a limited outside-balance closure and does not implement or promote this source driver or state. Complete flags, child routines, dependency state, mutations/resets, lifecycle/re-entry, outputs/diagnostics, numerics, performance, and conformance remain unclaimed; existing narrow implementations/claims remain unchanged. CP118 adds no Rust target/state, support, capability, manifest, comparator, proof variable, result, or conformance. CP119 maps the inline parent lines 165-167 shared-first-time guard and exact `Calculate Outside Surface Heat Balance` output after unconditional initialization returns and immediately before unconditional line-168 `CalcHeatBalanceOutsideSurf`. The CP117 flag default/reset/successful-tail clear applies unchanged: an abnormal exit before the display, including initialization failure, suppresses it; an exit after display but before the tail can repeat it on same-state re-entry. Rust has no persistent flag or progress output, and current stage metadata and identity wrappers are not parity. Exact output and failure behavior remain deferred. CP119 adds no synthetic routine, source, project-contract, Rust/state, support, capability, count, comparator, proof, result, or conformance entry. CP120 adds canonical required `routine.calc_heat_balance_outside_surf` and the heat-balance project-contract entry for the unconditional parent line-168 `CalcHeatBalanceOutsideSurf(state)` call and source lines 6951-7721. The caller omits optional `ZoneToResimulate`, selecting the complete normal path. In major source order the routine computes ground- and surrounding-surface average temperatures; optionally transfers radiant-system and integrated-PV flux; performs outside-face interior long-wave exchange without a zone filter; samples scheduled outside heat sources; then traverses Zone, Space, and heat-transfer-Surface order while skipping Windows, resolving active Constructions, and resetting exterior coefficients. Its exact boundary switch is `Ground`, `GroundFCfactorMethod`, `OtherSideCoefNoCalcExt`, `OtherSideCoefCalcExt`, `OtherSideCondModeledExt`, `ExternalEnvironment`, `KivaFoundation`, then default interior/other. Those branches own convection/radiation and moisture state, HAMT/CondFD, movable insulation, EcoRoof, exterior vented cavities, scheduled surroundings, CTF/EMPD/TDD outside solving, radiant-source coefficients, and fatal paths. Each branch that reaches the common tail stores outside convection per area through the lines 7723-7736 `GetQdotConvOutPerArea` dependency; the `ExternalEnvironment` EcoRoof line-7440 early `continue` skips that shared store. CP120 itself added no separate helper row; CP171 now adds the helper as a required routine entry. Existing Rust stage metadata and identity wrapping plus bounded retained opaque CTF/environmental balance and report terms do not implement or promote the complete topology/order, branch/child behavior, optional resimulation, state, errors, or numerics. CP120 adds no Rust target/state, support, capability, manifest, comparator, proof variable, result, or conformance promotion; complete branches, dependencies, lifecycle, side effects, runtime, outputs, performance, and broad conformance remain unclaimed. CP121 maps only the inline parent lines 169-171 shared-`ManageSurfaceHeatBalancefirstTime` guard and exact `Calculate Inside Surface Heat Balance` progress output. It is reached only after unconditional line-168 `CalcHeatBalanceOutsideSurf` returns and directly precedes unconditional line-172 `CalcHeatBalanceInsideSurf`. The CP117 true default/reset and successful-tail clear remain the only lifecycle state: a pre-display abnormal exit, including failure to return from the outside balance, suppresses this message, while a post-display/pre-tail abnormal exit can leave the flag true and repeat it on same-state re-entry. Rust has no persistent flag or progress output, and existing stage metadata and identity wrappers are not parity. Exact output routing and behavior, failure side effects, later guards, runtime, and conformance remain deferred. CP121 adds no synthetic routine, source, project-contract, Rust/state, support, capability, count, comparator, proof, result, output, or conformance entry. CP122 adds canonical required `routine.calc_heat_balance_inside_surf` and the heat-balance project-contract entry for the unconditional parent line-172 `CalcHeatBalanceInsideSurf(state)` call and canonical wrapper lines 7738-7813. The main caller omits optional `ZoneToResimulate`, selecting the complete-building path. In source order the wrapper performs first-call EMPD minimum-iteration/output/mixed-model setup; BeginEnvrn temperature, warmup-counter, and Kiva initialization with flag rearming; the lines 9277-9285 `sumSurfQdotRadHVAC` dependency now separately mapped by CP173; `AllCTF` dispatch to the existing lines 8658-9275 CTF-only child or otherwise the lines 7815-8656 general child; forced general-child dispatch for a partial Zone because its lists include adjacent interzone surfaces; then lines 5583-5699 `CalculateZoneMRT` and lines 4951-5020 `UpdateIntermediateSurfaceHeatBalanceResults`. The general child establishes reference-air and window state, samples inside heat-source schedules, gates Kiva calculation, and iterates long-wave exchange, 30-iteration convection reevaluation, moisture boundaries, representative opaque CTF/EMPD/CondFD/HAMT/Kiva/pool/source/movable-insulation branches, TDD and first-iteration regular windows, report/history commits, temperature checks, interzone synchronization, CondFD node convergence/relaxation, minimum iterations, and the greater-than-500 non-warmup warning and all-path break before post-loop HAMT/EMPD moisture sums. Optional resimulation has non-local side effects: first-call/environment work and the CP173-mapped radiant aggregation call are global; inside source and Kiva work can be global; moisture sums are globally zeroed before subset repopulation; MRT has global first-call state; and intermediate Kiva results use the global list. The general child directly fatals on movable insulation plus an embedded source/sink; the temperature helper owns separate extreme fatal paths. Its stored warmup counter is passed to both child call sites and received by the helper by value, so the local increment never persists and the coded greater-than-three and greater-than-ten warmup fatals are unreachable from those production calls; exact reachable diagnostics and extreme limits remain deferred. Existing `calc_heat_balance_inside_surf_stage`, its identity wrapper, bounded Rust surface passes, and the distinct required `routine.calc_heat_balance_inside_surf_2_ctf_only` row remain unchanged and do not implement or promote the canonical lifecycle, dispatch, complete topology, partial semantics, errors, or numerics. CP122 adds no EnergyPlus source, Rust target/state, capability, support, family gate, comparator, proof, result, or conformance promotion. CP123 maps only the inline parent lines 176-178 shared-`ManageSurfaceHeatBalancefirstTime` guard and exact `Calculate Air Heat Balance` progress output. It is reached only after unconditional line-172 `CalcHeatBalanceInsideSurf` returns and directly precedes unconditional line-179 `ManageAirHeatBalance`; lines 174-175 require Air balance before history because radiant systems can be present. The physical source order is outside, inside, Air, final, then history. Current Rust does not match it: the Surface-manager wrapper closes after limited Init/Outside work, Air executes before the separately wrapped Inside pass, and Final/History execute later. The CP117 true default/reset and successful line-229 tail clear remain the only lifecycle state: a pre-display inside-balance abnormal exit suppresses this message, while a post-display/pre-tail abnormal exit can leave the flag true and repeat it on same-state re-entry. Rust has no persistent flag or progress output. CP123 adds no synthetic routine, source, project-contract, Rust/state, support, capability, count, comparator, proof, result, output, or conformance entry; exact output/failure behavior, later stages, runtime, and conformance remain deferred. CP124 maps the unconditional Air-manager caller by reusing its existing canonical routine and project-contract entry. CP125 adds canonical required `routine.update_final_surface_heat_balance` and its project-contract entry for the unconditional parent line-184 call and `HeatBalanceSurfaceManager.cc` lines 5176-5219. In exact source order it unconditionally calls the Low-temperature radiant, High-temperature radiant, hot-water baseboard, steam baseboard, electric baseboard, cooling-panel, and swimming-pool averaged-source updaters. Each child initializes its caller-owned flag false and can still transfer, clear, reconcile, or redistribute averaged state when the final OR is false. If any of the seven flags is true, the routine reruns full-building `CalcHeatBalanceOutsideSurf(state)` and then `CalcHeatBalanceInsideSurf(state)` with omitted optional Zone arguments; it reruns neither initialization, Air balance, nor histories. Those updater children remain dependency context without routine rows or added source inventory. CP125 adds the existing `update_final_surface_heat_balance_stage` to `rust_target` because its execution-plan identity and bounded timestep wrapper already represent this final-stage barrier, but the wrapper only performs limited adiabatic synchronization/snapshot work and implements none of the seven equipment updaters or conditional complete two-pass replay. No Rust code/state, support, capability, output, numerical, or conformance promotion follows. CP126 adds canonical required `routine.update_thermal_histories` and its project-contract entry for the parent lines 186-189 `AnyCTF || AnyEMPD`-guarded call and canonical lines 5221-5581 body. A false parent guard skips every routine effect. On first entered call, the source dimensions five complete-Surface scratch arrays plus three more under `AnyInternalHeatSourceInInput`, then clears its separately defaulted/reset first-time flag. Its initial Zone/Space/opaque traversal accepts CTF/EMPD surfaces, reads the declared `surface.Construction`, skips `NumCTFTerms == 0`, and computes current inside/outside flux, area and per-area report terms with the outside report sign reversal, plus embedded source/user temperatures. `SimpleCTFOnly && !AnyConstrOverridesInModel` then recycles and shifts the four full history-array objects, copies term 1 into term 2, and returns. The normal capture, counter, and internal-source loops filter CTF/EMPD but deliberately do not repeat the zero-term skip: they save first samples at counter zero, increment `SurfCurrNumHist`, derive `SumTime`, roll master and working histories when `NumHistories` is reached or interpolate by `SumTime / CTFTimeStep`, then apply the analogous embedded source/user history path using the already-updated counter. The routine calls no named child and emits no diagnostics. Existing Rust `update_thermal_histories_stage`, identity wrapper, retained-surface vector-history pushes, and diagnostic outside-temperature overrides remain the unchanged bounded target; they do not implement the parent gate, complete topology, first-time/master state, exact counter/interpolation cadence, current report/source assignments, fast array recycling, or source/user histories. CP126 adds no EnergyPlus source, Rust target/code/state, support, capability, output, or conformance promotion; the inventory becomes 32 algorithms and 140 routines, split 58 state-mapped plus 82 source-mapped. CP127 maps the following independent parent lines 191-206 `AnyCondFD` block and adds non-required `routine.surface_data_fd_update_moisture_balance` for the inline `HeatBalFiniteDiffManager.hh` lines 175-182 helper. A true guard visits numeric `SurfNum` 1 through `TotSurfaces` in complete array order, reads declared `surface.Construction`, and skips exactly construction numbers at most zero, `TypeIsWindow` constructions, and non-CondFD algorithms before invoking `SurfaceFD(SurfNum).UpdateMoistureBalance()` with no arguments. It adds no representative, Zone/Space, heat-transfer, CTF-term, or other filter. The inline helper has no branch, diagnostic, or child call and performs exactly three whole-array snapshots: `TOld = T`, `RhovOld = Rhov`, and `TDOld = TDreport`. The later `InitHeatBalFiniteDiff` assignments at `HeatBalFiniteDiffManager.cc` lines 549-553 are dependency context showing restoration of `T`, `Rhov`, and `TD` plus separate `TDT`/`TDTLast` setup; they are not helper effects, a new routine row, or a reason to add the `.cc` file. CP127 adds only `HeatBalFiniteDiffManager.hh` to the algorithm source inventory and adds no project-contract routine, Rust target/code/state, support, capability, output, or conformance promotion. The inventory becomes 32 algorithms and 141 routines, split 58 state-mapped plus 83 source-mapped; required routines remain 47. CP128 maps the following unconditional parent line-208 `ThermalComfort::ManageThermalComfort(state, false)` call, before `ReportSurfaceHeatBalance`, and adds non-required `routine.manage_thermal_comfort` for canonical `ThermalComfort.cc` lines 105-164. Shared `FirstTimeFlag` defaults true at `ThermalComfort.hh` line 253 and `clear_state()` reconstructs the data object at lines 387-390; the manager calls `InitThermalComfort` lines 166-497 and clears the flag only after that child returns. Initialization allocates per-People comfort state and model-gated outputs, Zone and Facility ASHRAE-55/simple/setpoint-not-met state and outputs, angle-factor data, and occupancy hours. The manager maintains `TemporarySixAMTemperature` before its `InitializeOnly` test: on simulation day one before hour 7 it writes 1.868132, and at hour 7/timestep 1 on every day it samples outdoor dry bulb. This caller passes false, so it does not take the initialization-only return; it then zeros `ZoneOccHrs` under `BeginEnvrnFlag` and, only outside sizing and warmup, always calls Fanger, SimpleASH55, and setpoint-met calculations; conditionally calls Pierce, KSU, CoolingEffect, AnkleDraft, and both requested adaptive models; and passes `initiate=false` to both adaptive calls. The other production caller at `ZoneTempPredictorCorrector.cc` lines 5986-5988 passes true and can consume shared first-time initialization before this caller, but CP128 adds no ZoneTempPredictorCorrector source. The manager and initializer emit no direct diagnostic; deeper dependencies retain their warnings/fatals. This Fanger call omits optional `PNum`, making its `present(PNum)`-guarded air-velocity warning unreachable from this call. Children and diagnostics remain context without extra routine rows or source inventories. CP128 adds `ThermalComfort.cc` and `.hh` only to the Surface-manager source list and adds no project-contract routine, Rust target/code/state, support, capability, output, or conformance promotion. The inventory becomes 32 algorithms and 142 routines, split 58 state-mapped plus 84 source-mapped; required routines remain 47. CP129 maps the following unconditional parent line-210 `ReportSurfaceHeatBalance(state)` call and adds required `routine.report_surface_heat_balance` plus its heat-balance project-contract entry for canonical lines 6605-6891. The body has no routine-wide guard or early return. It first unconditionally calls the `SolarShading.cc` lines 11533-11619 `ReportSurfaceShading` dependency, which updates every Surface sunlit fraction/area and writes Window predefined-table values only on March 21, June 21, or December 21 at hours 9, 12, or 15 with `TimeStep == 4`. Under `UseRepresentativeSurfaceCalculations`, local lines 6893-6930 `ReportNonRepresentativeSurfaceResults` copies representative inside/outside convection classes and, under advanced reporting, three Window report values; its exact line-6918 scale is the self-area ratio `surface.Area / Surface(surfNum).Area`, ordinarily 1.0, not a representative-area ratio. The Zone/Space opaque-or-Window pass converts inside convection, net longwave, internal radiant, radiant-HVAC, outside convection, and outside radiation per-area values to rates and timestep energies, then computes `QAirExt = Area * HAirExt * (SurfTempOut - SurfOutDryBulbTemp)` and subtracts the opposite-sign outside convection rate for heat emission. Only `displayHeatEmissionsSummary` resets the emission sum and accumulates external-environment Surface emission energies; a false flag leaves the sum untouched. The Window pass chooses absorbed-shortwave layers from EQL `NL`, BSDF solid layers, unshaded/switchable active glass layers, or shaded-construction glass layers; zeroes and accumulates three area-weighted absorbed-shortwave reports; splits nonnegative gain from positive-magnitude loss while retaining signed transfer energy; and forwards diffuser gain/loss to TDD state. Under `AnyMovableInsulation`, local lines 6932-6940 copy the complete inside-temperature report array and replace only currently present interior movable-insulation entries. The opaque/internal-mass first pass reports outside absorbed shortwave, inside solar/lights, inside absorbed shortwave, and inside/outside conduction with sign-split gain/loss; its second pass derives average conduction as `(inside - outside) / 2` and storage as `-(inside + outside)`, with flux/rate/energy and sign splits. The exact component-load guard is `ZoneSizingCalc && CompLoadReportIsReq && !WarmupFlag`; it stores opaque lights/fenestration solar sequences and then repeats a redundant inner warmup test before pulse or normal convective loads and normal net longwave. Under advanced reporting, each Space adds opaque inside/outside conduction into Zone accumulators with `+=` and immediately updates sign-selected Zone reports. This body does not reset those accumulators or clear the opposite sign branch at each Space; it depends on the earlier unconditional `InitSolarHeatGains` lines 2558-2575 reset, so re-entry or a cumulative sign change can expose accumulated or stale opposite-branch values. The body and its three children emit no direct warning, severe, fatal, or display diagnostics; all topology, active construction, shading, solar, sizing, and initialization behavior is dependency context without child rows or added `SolarShading` sources. Existing Rust `report_surface_heat_balance_stage`, identity wrapper, bounded run-period retained-Surface report/trace path, and limited result-store output remain unchanged and do not implement the canonical nesting, complete Surface/window/opaque topology, dependencies, report flags, sizing arrays, or accumulator cadence. CP129 adds no EnergyPlus source, Rust target/code/state, support, capability, output, numerical, or conformance promotion. The inventory becomes 32 algorithms and 143 routines, split 58 state-mapped plus 85 source-mapped; required routines become 48. CP130 maps parent lines 211-213 `ZoneSizingCalc`-guarded `OutputReportTabular::GatherComponentLoadsSurface(state)` and adds non-required `routine.gather_component_loads_surface` for canonical `OutputReportTabular.cc` lines 15064-15132, declared at `OutputReportTabular.hh` line 869. A false parent guard skips the call; an entered call works only under `CompLoadReportIsReq && !isPulseZoneSizing`, so the effective condition has all three predicates, contains no warmup test, and makes pulse calls no-ops unlike the possible CP129 non-warmup `loadConvectedWithPulse` store. It derives `(HourOfDay - 1) * TimeStepsInHour + TimeStep`, indexes overall day and timestep minus one, resets only `feneCondInstantSeq` across the current Zone slice, and conditionally does the same for the Space slice under `doSpaceHeatBalanceSizing`. Its complete numeric Surface pass skips exactly `Zone == 0` and non-Window classes; the controlled-Zone test is commented out. Each survivor adds `SurfWinGainConvGlazToZoneRep + SurfWinConvHeatFlowNatural + SurfWinGainConvShadeToZoneRep + SurfWinGainFrameDividerToZoneRep` to its Zone and, when Space sizing is active, to `spaceNum - 1` without local Space validation. Instant solar remains comments only. The line-255 `DaylightingDeviceTubular` singleton then selects tubular convective gains through `InternalHeatGains.cc` lines 8170-8213 `SumInternalConvectionGainsByTypes`: all Zone Spaces for each Zone call and the explicit Space only for each optional Space call. `AllocateLoadComponentArrays` lines 14856-14919, sizing day/time indices, complete topology, Window report state, and internal-gain devices are dependencies. The routine and helper emit no diagnostics or bounds validation. The helper adds no row and `InternalHeatGains.cc/.hh` are not added to this algorithm. CP130 adds only `OutputReportTabular.cc` and `.hh` to the Surface-manager source inventory and adds no project-contract routine, Rust target/code/state, support, capability, output, numerical, or conformance promotion. The inventory becomes 32 algorithms and 144 routines, split 58 state-mapped plus 86 source-mapped; required routines remain 48. CP131 maps the unconditional parent line-215 `CalcThermalResilience(state)` call and adds non-required `routine.calc_thermal_resilience` for canonical `HeatBalanceSurfaceManager.cc` lines 5707-5799, declared at header line 155. It runs regardless of the CP130 branch and has no warmup, sizing, environment, occupancy, or routine-wide request guard. Under shared `ManageSurfaceHeatBalancefirstTime`, default true at header line 239 and reset true at 291, it registers `Zone Heat Index` in C and `Zone Humidity Index` in None for every Zone, then exact-name scans all requested variables to set the header lines 244-245 false-defaulted `reportVarHeatIndex` and `reportVarHumidex` flags, reset false at 296-297. It does not clear the shared flag; only successful parent-tail line 229 does, so an abnormal exit before that tail can repeat registration/scanning on re-entry. The Zone `Resilience` array is preallocated with zero-default values at `HeatBalanceManager.cc` lines 1853-1856; CP131 allocates or resets nothing and overwrites only active calculations. Heat Index runs under `reportVarHeatIndex || displayThermalResilienceSummary`, using ordinary Zone `ZTAV`, `airHumRatAvg`, barometric pressure, psychrometric RH times 100, and Fahrenheit temperature. `Simplified` below 80 F uses `0.5 * (T + 61 + 1.2 * (T - 68) + 0.094 * RH)`; at least 80 F uses the exact nine-coefficient Rothfusz polynomial `-42.379 + 2.04901523*T + 10.14333127*RH - 0.22475541*T*RH - 0.00683783*T^2 - 0.05481717*RH^2 + 0.00122874*T^2*RH + 0.00085282*T*RH^2 - 0.00000199*T^2*RH^2`, subtracts the low-humidity correction under `RH < 13 && T < 112` or adds the high-humidity correction under the `else if` `RH > 85 && T < 87`, then converts to C. Every non-`Simplified` enum uses `ExtendedHI::heatindex(state, ZoneT + Kelvin, ZoneRH / 100) - Kelvin`; its lines 538-557 dependency temporarily selects `ShortBisectionThenRegulaFalsi` and restores the root algorithm. Humidex independently runs under `reportVarHumidex || displayThermalResilienceSummary` and computes `TDewPointK = PsyTdpFnWPb(ZoneW, pressure) + Kelvin`, `e = 6.11 * exp(5417.7530 * (1 / 273.16 - 1 / TDewPointK))`, `h = 5 / 9 * (e - 10)`, and `ZoneHumidex = ZoneT + h`. Summary display enables both calculations; false gates retain existing/default values. The body emits no direct diagnostics; output registration, Psychrometrics range handling, ExtendedHI/root solving, preallocation, and Zone state remain dependencies without extra routine rows or source additions for ExtendedHI, Psychrometrics, or DataHeatBalance. CP131 adds no EnergyPlus source, project-contract routine, Rust target/code/state, support, capability, output, numerical, or conformance promotion. The inventory becomes 32 algorithms and 145 routines, split 58 state-mapped plus 87 source-mapped; required routines remain 48. CP132 maps parent lines 217-219 `displayThermalResilienceSummary` guard and adds non-required `routine.report_thermal_resilience` for `HeatBalanceSurfaceManager.cc` lines 5801-6388, declared at header line 157. Active thermal-period flags and each active period's `meterNumTotalsBEPS(1)` current-value addition to `totalElectricityUse` occur before both first-time work and the weather/warmup gate; the flag array is dimensioned under `TotReportPers > 0` but by `TotThermalReportPers`. Independent `reportThermalResilienceFirstTime`, true at header line 243/reset line 295, zeros per-Zone/period five-bin HI, Humidex, optional SET, and safety arrays, six-bin unmet arrays, four-bin PMV arrays, and period/annual SET-longest trackers, then clears itself. No People or any non-Pierce People disables SET for every Zone. There is no BeginEnvrn reset, so annual/crossing state spans RunPeriodWeather environments. Remaining aggregation requires `RunPeriodWeather && !WarmupFlag`, uses `TimeStepZone`, and has no hourly/daily reset. The People loop overwrites rather than sums Zone occupancy, leaves untouched Zones stale, updates SET-last-step per People object, duplicates raw safety/PMV Zone hours per People, and uses strict cold-safe `>`/heat-safe `<` thresholds plus first-crossing timestamps. Four independent PMV tests at `<-3`, `<-0.7`, `>0.7`, and `>3` duplicate extreme contributions into adjacent bins. Exact PierceSET/PMV inequality triggers the only two direct diagnostics, global-index recurring warnings. The Zone pass bins HI at `26.7/32.2/39.4/51.7` and Humidex at `29/40/45/50`, each as elapsed/person/occupied hours, and accumulates positive-setpoint cooling/heating unmet degree hours in raw/person/occupied variants. SET `<=12.2` and `>30` accumulates the same three variants plus longest occupied duration/start. Normal crossings use minute `(TimeStep - 1) * TimeStepZone * 60`; a continuing low-SET event at period start uses `TimeStepZone * TimeStep * 60` and source lines 6314-6315 erroneously reset the period high-SET tracker/start. Active periods duplicate all corresponding aggregation, so overlaps all accrue. Table writers, meter access, report-period helpers, People/ThermalComfort state, temperatures/setpoints, and CP131 HI/Humidex are dependencies without child rows or added sources. CP132 adds no project-contract routine, Rust target/code/state, support, capability, output, numerical, or conformance promotion. The inventory becomes 32 algorithms and 146 routines, split 58 state-mapped plus 88 source-mapped; required routines remain 48. CP133 maps the independently guarded CO2 summary as non-required `routine.report_co2_resilience`. Its first-time block initializes the three report-period bin families and can disable future calls after an explicit-only no-CO2 warning; remaining weather/non-warmup aggregation retains last-People-wins/stale occupancy, exact `<= 1000`/`(1000, 5000]`/else bins, active-period meter accumulation, cross-environment state, and the unreachable period-bin writer guard. It adds no project-contract/Rust/state/support/conformance promotion. The inventory becomes 32 algorithms and 147 routines, split 58 state-mapped plus 89 source-mapped; required routines remain 48. CP134 maps the independently guarded visual summary as non-required `routine.report_visual_resilience`. Its first-time block initializes the three four-bin report-period families and can disable future calls after an explicit-only no-daylight-controls warning; remaining weather/non-warmup aggregation retains last-People-wins/stale occupancy, per-call illuminance-sum reset, setpoint-versus-measured reference-point selection, zero-reference-point Zone skips, exact `<= 100`/`(100, 300]`/`(300, 500]`/else bins, active-period meter accumulation, cross-environment state, and annual/period writer quirks. It adds no project-contract/Rust/state/support/conformance promotion. The inventory becomes 32 algorithms and 148 routines, split 58 state-mapped plus 90 source-mapped; required routines remain 48. CP135 maps only the inline parent-tail `ManageSurfaceHeatBalancefirstTime = false` assignment without a synthetic routine row or count change. The source bool defaults and resets true, is read only by four progress guards and the `CalcThermalResilience` registration/request-scan block, and is cleared only after every entered parent child returns. CP133/CP134 child-local early returns still reach the tail, while a fatal, exception, or other non-return preserves true for repeated shared first-time effects on same-state re-entry even if child-local flags are already false. CP135 adds no project-contract/Rust/state/support/conformance promotion; the inventory remains 32 algorithms and 148 routines, split 58 state-mapped plus 90 source-mapped, with 48 required. CP136 maps the unconditional `HeatBalanceManager.cc` line-210 `EndZoneTimestepBeforeZoneReporting` call by reusing `routine.manage_ems`, after the Surface-manager return and before `RecKeepHeatBalance`. It passes the same caller-owned `anyRan` plus an absent manager index; generic `ManageEMS` overwrites the prior CP115 flag false, quick-returns without EMS, otherwise initializes the calling point, dispatches callbacks/plugins and exactly matching managers, and uses true as the gate for actuator commit plus `ReportEMS`. The absent index matters only on the user-defined-component path. The caller ignores `anyRan` and performs no Surface re-solve before record keeping. CP136 adds no duplicate row, project-contract/Rust/state/support/conformance promotion, or count change; the inventory remains 32 algorithms and 148 routines, split 58 state-mapped plus 90 source-mapped, with 48 required. CP137 adds required `routine.rec_keep_heat_balance` plus its heat-balance project-contract requirement for the unconditional line-211 call and source lines 2971-3057. The source records raw Zone temperature/heat/cool extrema, shifts two-sample temperature/load history, stores `max(heat, abs(cool))`, applies the exact first-day post-warmup report gate and state-wide EIO-header lifecycle, snapshots movable insulation, and unconditionally updates non-BSDF Window front/back face reports. BeginSim/BeginEnvrn resets, the intentionally omitted `LoadZoneSecPrevDay` environment reset, the helper, and optional EIO remain source-only. Existing Rust record-keep stage and composite-plan metadata are scaffolding, not implementation. CP137 adds no source file, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 149 routines, split 58 state-mapped plus 91 source-mapped, with 49 required. CP138 adds required `routine.report_heat_balance` and its heat-balance project-contract requirement for the unconditional line-217 call and `HeatBalanceManager.cc` lines 3321-3418. The source unconditionally runs schedule reporting, including its setup and `UpdateScheduleVals`, before selecting the normal `!WarmupFlag && DoOutputReporting` branch, the effective warmup-reporting branch, the external-interface fallback, or no further output. Node calculation remains non-sizing-only; Zone output and guarded HVAC sizing logs occur on the entered reporting/fallback paths; only normal reporting adds tabular and utility updates; only warmup reporting owns the dictionary and ESO/MTR environment-stamp lifecycle. Reporting subsystems remain dependencies without child rows or added sources. The body emits no direct diagnostics, while dependency non-return prevents CP139. Existing Rust report-stage, composite-plan, prebinding, and bounded result-store metadata are scaffolding, not implementation. CP138 adds no source file, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP139 maps the unconditional line-219 `EndZoneTimestepAfterZoneReporting` `ManageEMS` call by reusing `routine.manage_ems` without a duplicate row. Generic `ManageEMS` resets the shared caller-owned `anyRan` instead of accumulating the CP136 result, quick-returns when EMS is absent, otherwise initializes this calling point, dispatches callbacks/plugins and exactly matching managers, and applies actuators plus `ReportEMS` only if something ran. Because CP138 reporting has already completed, any commit cannot retroactively change those outputs; later numerical, trend, and plugin visibility is not inferred. CP139 adds no source, project-contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP140 adds non-required `routine.update_ems_trend_variables` for the unconditional line-221 call, `EMSManager.hh` line 122 declaration, and `EMSManager.cc` lines 1444-1479 body. After no-EMS and zero-trend quick returns, the source visits declarations in 1-based order, skips nonpositive Erl pointers or log depths, and pushes the current numeric value to history index 1 without local positive upper-bound validation. Input allocation, `TimeARR`, setup diagnostics, and environment resets remain dependencies; the body emits no direct diagnostics. Existing Rust plan scaffolding has no trend-update implementation or target. CP140 adds no source file, project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 151 routines, split 58 state-mapped plus 93 source-mapped, with 50 required. CP141 adds non-required `routine.update_plugin_values` for the unconditional line-222 call, `PluginManager.hh` line 198 declaration, and `PluginManager.cc` lines 1458-1467 body, adding those two PluginManager sources to the Manager inventory. Without `LINK_WITH_PYTHON` the call is a no-op; the linked body has no runtime manager guard and directly visits stored-order `state.dataPluginManager->trends`, naturally doing no work for an empty vector. It gets each current global, pushes it at index 0, and drops the oldest value while fixed `times` remain unchanged; the update validates neither history length nor deque size. Construction returns before input population without a retained `PythonPlugin:Instance`; retained-instance setup owns globals, trends, handles, positive history length, fixed offsets, diagnostics, and missing-handle fatals. BeginEnvironment zeros globals and trend values while retaining times. The getter fatals for an empty global vector, while a nonempty invalid handle uses unchecked `[]`; dependency failure can prevent CP142. Warmup updates do not test per-instance run-during-warmup. Rust plugin execution remains unsupported. CP141 adds no project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 152 routines, split 58 state-mapped plus 94 source-mapped, with 50 required. CP142 adds required `routine.check_warmup_convergence` and its project-contract entry for the outer `WarmupFlag && EndDayFlag` gate at lines 224-226 and `HeatBalanceManager.cc` lines 3059-3226 body, declared at header line 136. `NumOfZones <= 0` clears warmup immediately. Otherwise the Zone loop uses inclusive temperature-tolerance tests and strict current-load `> 1.0e-4` gates; active heat/cool tests mutate current and previous loads through `abs(max(value, 100.0))`, divide by the clamped current value, and pass inclusively at the load tolerance. No-load paths pass without refreshing their stored test values. Each Zone then copies current values to previous-day state and resets current extrema to `-9999.0`/`1000.0`. At the maximum day while `WarmupFlag` remains true, failures emit per-Zone severe/detail diagnostics with state-wide normal/sizing guidance latches, optionally diagnose a user maximum below the default 25, and finally force warmup false; ordinary convergence still honors the minimum day. Allocation, BeginEnvironment resets, CP137 extrema accumulation, controls, names, diagnostics, and failures remain dependencies. Existing Rust stage metadata and temperature-only diagnostic loop are not this routine. CP142 adds no EnergyPlus source, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP143 maps only the inner line-227 `!WarmupFlag` branch and ordered numeric `DayOfSim` then textual `DayOfSimChr` zero resets at lines 228-229, without a synthetic routine or count change. Despite the source convergence comment, the branch also follows CP142 zero-Zone and maximum-day forced exits, but not a below-maximum failed check. The following line-231 `BeginNewEnvironmentAfterWarmUp` EMS call observes both resets, which persist if that dependency does not return; on the same invocation the later line-235 report gate cannot satisfy `DayOfSim == 1`. Existing Rust warmup metadata and diagnostics implement none of these global mutations or nesting. CP143 adds no source, project contract, Rust/state/support/conformance promotion; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP144 maps the in-branch line-231 `BeginNewEnvironmentAfterWarmUp` call by reusing `routine.manage_ems`, passing the same `anyRan` and an absent manager index after CP143 resets. Generic dispatch overwrites the flag false, quick-returns without EMS, otherwise initializes this calling point, runs callbacks/plugins and exact managers, and uses any execution to gate actuator commit plus `ReportEMS`. The lines 268-271 runtime-language and plugin begin-environment hooks require the distinct `BeginNewEnvironment` point and do not run here. Observers see warmup false and both day values zero; the caller ignores the result. CP143 mutations persist on non-return, which prevents CP145; a successful return still leaves the same-invocation CP145 `DayOfSim == 1` test false. CP144 adds no duplicate routine, source, project contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP145 adds required `routine.report_warmup_convergence` and its project-contract entry for the lines 235-237 four-way first-nonwarmup-day guard and `HeatBalanceManager.cc` lines 3228-3301 body, declared at header line 138. Its internal non-warmup guard can no-op; a one-state-lifetime, positive-Zone EIO header precedes source-order Zone rows. It resets scratch, selects RunPeriod or SizingPeriod text, averages `CountWarmupDayPoints` temperature samples, and normalizes each stored load difference in place only when its maximum is strictly above `1.0e-4`, otherwise zero. Load means and population standard deviations use the mutated ratios although the header says W; rows include four CP142 `PassFail` values and `{:.10R}` reals. Repeated calls renormalize already changed loads. Allocation, BeginEnvironment reset, CP137 producer/count, EIO, formatting, and bounds are dependencies; the body validates no positive count or capacity and emits no direct diagnostics. Existing Rust warmup metadata/summary is not this report. CP145 adds no source file or Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 154 routines, split 58 state-mapped plus 96 source-mapped, with 52 required. CP146 adds required source-mapped `SetPreConstructionInputParameters`. The unconditional `SimulationManager.cc` line-216 call follows constant/state initialization, environment/report checks, and predefined-table setup and precedes time-pointer setup; the routine is declared at `HeatBalanceManager.hh` line 96 and implemented at `HeatBalanceManager.cc` lines 446-492. Each entry overwrites shared `MaxSolidWinLayers` with executable baseline 7 despite the stale comment saying 5; the member defaults to 0, module clear reconstructs 0, and re-entry recomputes from 7. Raw `Construction:ComplexFenestrationState` presence raises the bound to 10 without retrieving an item or erasing any complex-state record from InputProcessor `unusedInputs`. The raw `Construction:WindowEquivalentLayer` count drives 1-based `getObjectItem` calls into all shared alpha/numeric value, blank-marker, and field-name buffers; only the positional `NumAlpha - 1` span is folded into the maximum, including holes/blanks, while alpha contents, numerics, `NumNumber`, `IOStat`, markers, and names are ignored. For each found item, InputProcessor zeros both counts, sets `IOStat` to -1, clears the shared buffers, and erases the requested equivalent-layer record from `unusedInputs` before field traversal; only full completion sets `IOStat` to 1, and CP146 never checks it. Opaque F/C-factor constructions are ignored. Normal successful output is 7 through 11; successful order cannot change the maximum, but source-order retrieval leaves the last completed shared buffers and marks every requested equivalent-layer item used. If a dependency does not return during the current item, the maximum retains only baseline/CFS and previously returned equivalent-layer spans, while that current object may already be marked used and its shared buffers may be partial; later items and downstream work are skipped. The routine validates and diagnoses nothing locally. Construction array allocation plus Surface, SolarShading, SurfaceGeometry, and Window loops are downstream dependencies. Rust dynamic `Vec` storage and separate limits 8 and 11 do not implement the mutable shared bound, scan/call order, buffers, failure prefix, allocations, or reset/re-entry lifecycle. CP146 adds no source inventory, Rust target/state/code, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 155 routines, split 58 state-mapped plus 97 source-mapped, with 53 required. CP147 adds required source-mapped `GetSiteAtmosphereData`, declared at `HeatBalanceManager.hh` line 100, called at `GetHeatBalanceInput` line 264 between line-262 project controls and line-266 spectral input, and implemented at `HeatBalanceManager.cc` lines 1252-1317. It sets `CurrentModuleObject` to `Site:HeightVariation`, uses local alpha-1/numeric-3 arrays, and reads the raw count. The unique-object schema defaults N1/N2/N3 to 0.22, 370.0, and 0.0065 with respective inclusive-zero, strict-positive, and inclusive-zero minima and no maxima. Exactly one object uses `NumNums` to copy only the returned N1/N2/N3 prefix, so absent trailing fields retain Terrain-derived wind or the existing gradient while blank/missing fields inside the prefix propagate schema defaults. InputProcessor zeros counts, sets `IOStat` to -1, clears local values plus shared blank/name metadata, and erases the object from `unusedInputs` before traversal; full completion sets status 1, but CP147 ignores status, alphas, metadata, and all local validation. A non-return can leave used/partial dependency effects before any environment copy or EIO. More than one object reads and marks no item, changes no environment value, emits exact `Too many Site:HeightVariation objects, only 1 allowed.`, then monotonically sets caller `ErrorsFound`; zero objects retain predecessor Terrain wind and reset only gradient to 0.0065. Every normally returned branch unconditionally repeats the EIO header and formatted row, including retained values on duplicates; output failures preserve earlier mutations and can leave a header-only prefix. Environment/module clear restores default 0.22/370/0.0065 and empty current-object state, while re-entry repeats intake and output. Terrain setup, height-corrected dry/wet bulb and wind formulas, Surface bulk paths, Kiva, diagnostics, InputProcessor, and EIO are dependencies. Rust keeps this object raw-only; its Terrain wind mapping and fixed-gradient helper do not implement override/hybrid state, shared lifecycle, source formulas, diagnostics, usage/metadata effects, or output. CP147 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 156 routines, split 58 state-mapped plus 98 source-mapped, with 54 required. CP148 adds required source-mapped `AllocateZoneHeatBalArrays`, declared at `HeatBalanceManager.hh` line 130 and implemented at `HeatBalanceManager.cc` lines 2824-2854. `InitHeatBalance` calls `AllocateHeatBalArrays` under `BeginSimFlag` at lines 2617-2618, and that parent calls CP148 as its first action at line 2863. A false `ZoneIntGain.allocated()` sentinel invokes `AllocateIntGains`, which allocates Zone internal gain records, Space internal gain records, Space device records, then Space power-reduction factors defaulted 1.0; gain values/counts default zero. A true sole sentinel preserves and does not validate the whole companion bundle. On the ordinary path, `GetHeatBalanceInput` line 320 already calls `ManageInternalHeatGains(state, true)`, whose input path normally invokes `AllocateIntGains` at `InternalHeatGains.cc` lines 291-293; CP148 therefore preserves populated internal-gain state, while its fallback chiefly serves direct test and alternate paths. CP148 next unconditionally allocates Zone and Space heat-balance EPVectors even with Space mode false. EPVector sets allocated before resize and fills every element with `T{}`: exact inherited defaults are 23 C for MAT/MRT/ZTAV/ZT/ZTAVComf/XMPT/TMX/TM2/MixingMAT and all XMAT/DSXMAT slots, 0.01 for the five named humidity values, and zero for every remaining member including T1 and ZTM/W histories. Eight `NumOfSolarEnclosures` arrays allocate in QSDifSol, QD, QDforDaylight, DB, DBSSG, DBIntWin, QSWRad, QSWRadLights order; the 1-based loop zeros them in QSDifSol, QD, QDforDaylight, QSWRad, QSWRadLights, DB, DBSSG, DBIntWin order. Zero counts are accepted and set EPVector sentinels without elements. Negative `NumOfZones` or `numSpaces` converts to unsigned EPVector `size_type`, sets allocated true, then generally nonreturns through a huge resize; negative `NumOfSolarEnclosures` instead creates eight empty Objexx 1:0 arrays, skips the loop, and can return. Oversized, inconsistent, and topology-invalid counts receive no local validation or diagnostic. Sequential failures expose completed allocations and per-enclosure writes; allocated-before-resize can poison the Zone sentinel so an uncleared retry skips an incomplete fallback. Successful re-entry preserves internal gains but destructively rebuilds Zone/Space records and enclosure arrays. Owning clears are nonuniform: HeatBalance clear resets the Zone sentinel/Space companions/enclosure arrays, predictor clear resets Zone/Space records, and ViewFactor clear resets the enclosure count, but DaylightingManager clear retains `spacePowerReductionFactor`. The false reset Zone sentinel makes the next fallback re-dimension that retained factor and overwrite it with 1.0; not all participating state becomes uniformly fresh or unallocated. The parent remainder after line 2863, BeginSim lifecycle, predictor/corrector, convection, HVAC, internal-gain/daylighting, SolarShading, and Surface solar consumers are dependencies. Rust Zone vectors and initialization shells do not implement the Space/fallback/enclosure state, exact defaults/order, partial failure, destructive re-entry, or clear/retry lifecycle. CP148 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 157 routines, split 58 state-mapped plus 99 source-mapped, with 55 required. CP149 adds required source-mapped `routine.allocate_heat_bal_arrays` and its project-contract entry for the sole production `InitHeatBalance` BeginSim call, header line 132, and source lines 2855-2963. After the separate CP148 child, the source dimensions ten FanSystem Zone arrays to zero; no-value allocates `ZoneReOrder`, dimensions two flags false, then explicitly zeros the reorder; dimensions thermostat air to 23 C; independently samples each enabled CO2/generic schedule once and dimensions three arrays; resets the exact warmup extrema/history/report bundle; no-value allocates convergence records and two standard-deviation arrays; allocates cold then heat crossed-threshold matrices before explicitly setting cold then heat false; conditionally allocates eleven thermal, three CO2, and three visual vector-report matrices; unconditionally allocates six SET/longest/start matrices using `TotThermalReportPers`; and resets `CountWarmupDayPoints` last. Value-bearing `dimension` always refills. Ordinary release no-value allocation preserves linear cells whenever `resize` reports no reallocation, including same-product reshapes with reinterpreted indices; changed counts within reserved capacity remain a container dependency. Reported reallocation default-constructs vector cells but leaves arithmetic cells without CP149 initialization, while Objexx array-init/debug builds assign trait/debug values even on the no-reallocation path. False feature branches preserve old state on same-state re-entry. Zero Zone count still samples enabled contaminants and reaches the tail; nonpositive report counts skip only conditional families, while crossed and six SET allocations still run with empty negative extents. Effects are sequential with delayed fills, no validation, diagnostic, rollback, or status. The later BeginEnvironment block resets many warmup values but not `LoadZoneSecPrevDay`, convergence records, or standard deviations; owner clears make a fully cleared retry fresh. Rust has no parent analog. CP149 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 158 routines, split 58 state-mapped plus 100 source-mapped, with 56 required. CP150 adds required source-mapped `routine.update_window_face_temps_non_bsdf_win` and its project-contract entry immediately after `rec_keep_heat_balance` and before `report_heat_balance`. Declared at header line 140 and implemented at source lines 3303-3313, it is the unconditional last executable action of `RecKeepHeatBalance` at line 3056 and makes successful endpoint copies visible before same-timestep line-217 reporting. The helper traverses stored `AllHTWindowSurfaceList` order; normal clean-state production appends ascending indices satisfying `HeatTransSurf && Class == Window`, with no BSDF filter. Each entry first dereferences the current mutable `Surface.Construction`, skips only `WindowTypeBSDF`, copies outside history term 1 to `SurfWinFenLaySurfTempFront(SurfNum, 1)`, then copies inside history term 1 to `SurfWinFenLaySurfTempBack(SurfNum, TotLayers)`, where `TotLayers` is not the solid-layer count. Destination matrices are dependency-dimensioned `[TotSurfaces, MaxSolidWinLayers]` to zero; history term arrays are separately allocated/initialized and produced by Surface balance. Standard non-BSDF face-temperature outputs are registered only for exterior-solar front layer 1 and back layer `TotLayers`, but CP150 has no output, warmup, sizing, or kickoff guard. Empty lists no-op; duplicates repeat writes; valid BSDF entries and unlisted/intermediate/obsolete cells retain prior values. Invalid Surface/Construction/history/destination/layer state is unchecked. Effects are stored-order and front-before-back with no diagnostics, status, rollback, math, or finite check, so a back failure can preserve the current front plus earlier entries and prevent ReportHeatBalance. Re-entry overwrites current endpoints but does not clear cells skipped after Construction/list/layer changes. Owner clears reconstruct the list, destinations, histories, and Construction table; BeginEnvironment does not explicitly reset destination matrices. Rust blocks fenestration and has no list/report-matrix/history-handoff analog. CP150 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 159 routines, split 58 state-mapped plus 101 source-mapped, with 57 required. CP151 adds non-required source-mapped `routine.open_shading_file`, declared at header line 144 and implemented at source lines 3422-3438, for the sole production `InitHeatBalance` lines-2696-2698 call under exact `BeginSimFlag && DoWeathSim && ReportExtShadingSunlitFrac`. It first calls `shade.ensure_open` with caller label `OpenOutputFiles` and the independent `outputControl.extshd` selector. A non-good enabled stream opens text output with truncation or fatals on failure; disabled physical output creates a good dev-null sink. A good physical, string, or dev-null stream is retained with its current path/mode/position, so direct re-entry appends another header, while a closed/failed physical stream reopens with truncation. The body writes literal `Surface Name,`, every raw `Surface(1..TotSurfaces).Name` plus a comma in numeric order, then newline. It has no Surface filter, CSV quoting/escaping, status, flush, close, once flag, post-write check, or rollback; nonpositive counts still write the literal line, invalid positive Surface state or stream failure can leave a partial prefix, and only the open failure owns an exact fatal. Later daily shading rows use a separate BeginDay/non-warmup/RunPeriodWeather/report-request gate, which omits CP151 caller conditions `BeginSimFlag` and `DoWeathSim`; for a normal validated positive timestep count they emit `24 * TimeStepsInHour` rows and all `SurfSunlitFrac` values. They are dependency context, not CP151. Rust has no report flag, extshd selector, eplusshading CSV lifecycle, all-Surface export, or header/row writer; existing bounded incident-solar calculation/result state and Schedule:File:Shading input support are not parity. CP151 adds no project-contract requirement, source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 160 routines, split 58 state-mapped plus 102 source-mapped, with 57 required. CP152 adds non-required source-mapped `routine.set_storm_window_control` for the sole production `InitHeatBalance` line-2669 call under outer `TotStormWin > 0` and inner `BeginDayFlag`; only a successful return sets caller-owned `ChangeSet = false`. The body first clears `StormWinChangeThisDay`, then visits StormWindow records in numeric input order: it resolves `BaseWindowNum`, copies current to previous, computes local `DateOff - 1` with zero remapped to 366, uses inclusive/wrapping `BetweenDates(DayOfYear_Schedule, DateOn, adjusted DateOff)` to write current 1 or 0, makes `BeginSimFlag` overwrite previous with current, and latches any mismatch. Input dates and WeatherManager's test day use fixed leap-shaped `OrdinalDay(..., 1)` values, making `DateOn` inclusive and input `DateOff` exclusive; January 1 off wraps through day 366, and malformed equal dates that bypass input rejection are on for all 366 days. The first later non-BeginDay call while `ChangeSet` is false clears the event, snapshots each referenced current flag to previous, then sets `ChangeSet` true. Every positive-total caller pass then visits every unfiltered Zone/Space WindowSurface-span index, including normal Window, GlassDoor, and TDD_Diffuser entries rather than only StormWindow references, selecting the synthesized storm construction only for flag 1 plus Detailed and otherwise the current mutable `Surface.Construction`; this can reflect or overwrite the immediately preceding EMS active-construction decision. Duplicate BaseWindow records are ordered writes: last current wins, later previous sees the earlier new current, and when not BeginSim any intermediate mismatch latches; BeginSim suppresses each mismatch. Normal unique-base same-day re-entry clears an earlier transition. Effects are sequential with no validation, diagnostic, status, or rollback: helper failure preserves prefixes and prevents the `ChangeSet` clear and remainder; remainder failure preserves helper state, false `ChangeSet`, and its active-construction prefix while blocking CP151 and solar work; non-BeginDay snapshot failure leaves the event false, `ChangeSet` false, and a previous-day prefix for retry; and a nonpositive caller total skips the outer block and can leave stale state. Owner clears restore empty/zero Surface storm state, a false event, and true `ChangeSet`; BeginEnvironment does not. Active-construction selection, window starting-temperature guesses, daylighting recalculation, shaded-construction selection, divider behavior, and status output remain downstream dependencies. Rust has no storm-window input, state, controller, active-construction switch, or fenestration analog. CP152 adds no project-contract requirement, source inventory, Rust target/state/code, test, support, output, or conformance promotion; the inventory becomes 32 algorithms and 161 routines, split 58 state-mapped plus 103 source-mapped, with 57 required. CP153 adds required source-mapped `routine.init_conduction_transfer_functions` and its heat-balance project-contract requirement. The sole production `InitHeatBalance` path first enters `BeginSimFlag`, returns from `AllocateHeatBalArrays`, then under `AnyCTF || AnyEMPD` emits `Initializing Response Factors` and calls line 2621; EMPD uses the same thermal generator. The wrapper initializes local error/forced-report flags false, visits every Construction in array order through `calculateTransferFunction`, then after each returned child makes `SimpleCTFOnly` false for more than one history and raises `MaxCTFTerms`; any internal source independently makes the simple flag false. A second pass calls `reportLayers` only for `IsUsedCTF`; `ScanForReports` follows; request or forced error report writes four EIO headers and increments `cCounter` across all Constructions while reporting only used ones; accumulated errors fatal last with the exact wrapper message. The child first zeros all fixed CTF/QTF/source/user-temperature arrays plus timestep, counts, and U, then unused entries return. Used entries scan every material; source high-diffusivity diagnostics latch once without setting the shared bool, thickness over 3 m and resistance below `1.0e-3` set it, boundary/non-ROnly resistance layers receive equivalent-air properties, and interior ROnly layers stay massless. Any already-set shared error makes later used children reset and scan but return before generation. Adjacent massless merges alter local layers and persistently decrement source/user-temperature layer positions. All-resistive constructions use one history/term and `Outside0 = Cross0 = Inside0 = U = 1/R`, with a source/sink setting an error after coefficients are formed. A source-free exact reverse of an earlier used equal-layer Construction reuses its timestep/counts, swaps inside/outside, and retains cross/nonzero flux. New massive generation builds the bounded 1-D/optional-2-D node grid and source nodes, then each adaptive attempt calls exponential matrix, inverse matrix, gammas, and final coefficients in order. More than 18 terms or over-1% series-sum mismatch increments histories and timestep and retries; a zero largest sum fatals immediately, while timestep at least 7 hours emits convergence guidance, forces detailed reporting, sets the error, and breaks. Normal completion stores regular/QTF coefficients and U and deallocates work arrays; singular/nonfinite results have no distinct local validation. Nonfatal errors can leave earlier/full, failing/latest, and later/reset-zero Construction state, still run reachable layer/detail reporting, then fatal; immediate child/output failures bypass the remainder with no rollback. Re-entry recomputes child bundles and reverse reuse sees fresh earlier entries, but the wrapper never resets `SimpleCTFOnly` true or `MaxCTFTerms` zero, warning latches and appended reports persist, and repeated massless merging can decrement persistent source positions again. Owner clears reconstruct state; BeginEnvironment does not regenerate. Rust can consume `EnergyPlusEioSeeded` rows or `RustGeneratedSteady` no-history `1/R` coefficients and has bounded CTF consumers, but no native finite-difference/matrix/adaptive/QTF generator or this lifecycle. Construction helper files remain dependency context with no separate row or source-inventory addition. CP153 adds no Rust target/state/code, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 162 routines, split 58 state-mapped plus 104 source-mapped, with 58 required. CP154 adds non-required source-mapped `routine.gather_for_predefined_report`, declared at `HeatBalanceSurfaceManager.hh` line 99, implemented at `HeatBalanceSurfaceManager.cc` lines 623-1404, and reached only from the `InitSurfaceHeatBalance` lines-475-482 BeginSim branch; the first-time flag controls only the progress display, while the gather is unconditional within BeginSim and independent of whether predefined reports will be shown. The caller has already returned from `InitIntSolarDistribution` and `InitIntConvCoeff`; a successful gather next reaches `AnyCondFD` initialization and conduction-history work. Only `dataGeneral->Constructions` gates Construction EIO headers and rows. Each call zeros class counts and fenestration accumulators, allocates/zeros `computedNetArea`, optionally writes the assembly header, recreates all local dedup/header state, and first traverses stored `AllSurfaceListReportOrder` without an added use or report filter. Only `ExternalEnvironment`, `Ground`, `GroundFCfactorMethod`, and `KivaFoundation` are exterior. Exterior opaque classes are Wall/Floor/Roof; the interior branch also accepts IntMass. Opaque and Door area use Zone times List multipliers, while Window/TDD_Dome also use the Surface multiplier. Rows append construction/Zone/Space, no-film U, outside reflectance, geometry, and orientation; openings subtract from `computedNetArea(BaseSurf)` without validation or clamp. Azimuth is rounded to 0.01 before cardinal classification; `60 <= Tilt < 180` and azimuth at least 315 or below 45 alone marks north, while all other exterior fenestration contributes to non-north totals. Exterior Window/TDD reporting reuses nonzero SummerSHGC/visible cache or, for non-EQL zero cache, calls `CalcNominalWindowCond`, ignores `errFlag`, and stores returned optical values. EQL plus zero cache and dependency error can expose uninitialized locals; a non-EQL error can also poison the cache. Normal `CalcNominalWindowCond` completion also rewrites `SurfWinCoeffAdjRatio` for the complete exterior-Window list. Frames add the exact perimeter area, dividers use their grid formula, and the fully multiplied gross-plus-frame opening drives net subtraction and weighted U/SHGC/VT totals. Framed entries index the fixed NFRC product geometry/vision tables and call assembly helpers whose factory can substitute `SurfWinActiveShadedConstruction` from live `SurfWinShadingFlag`, independently of the supplied construction. An external-library model can overwrite NominalU and local SHGC through its NFRC rating helpers; locally unique construction/frame pairs alone write assembly EIO rows. Shaded-state count comes from the control list while shaded construction indices are read unchecked. Framed NFRC helpers run before pair dedup; unframed states use separate construction dedup plus NFRC width/height table index zero. The same shaded construction with different frames, or across framed/unframed domains, can append duplicate predefined cells keyed only by construction name. Two independent lazy same-tag EIO headers can expose different schemas. Active WindowShadingControl access is unchecked and the control row joins shaded plus storm construction names in stored order. Interior literal `iz-` mirrors skip all individual rows, net subtraction, and totals but still reach class counting. Interior AirBoundary openings contribute area and U to denominators while contributing zero SHGC/VT numerator, and interior legacy zero-cache results are not stored back. Every listed Surface increments its actual class; Window with original GlassDoor or TDD_Diffuser also increments the original class. Raw two-family Overhang and Fin object totals overwrite those buckets. A second report-order pass appends Wall/Floor/Roof net rows, followed by exterior overall/north/non-north and interior area-weighted totals with literal `-` on nonpositive denominators, then fourteen fixed labels in both total and exterior count columns; empty input still appends totals/counts. Every predefined entry appends. Real entries retain formatted text, original SI value, and digit metadata; report writing groups first-seen object order and takes the last duplicate cell, while `RetrievePreDefTableEntry` scans forward and takes the first; shaded construction/frame key collisions can trigger that disagreement within one invocation. Duplicate report-order indices/names therefore repeat counts, totals, subtraction, and entries and can drive net area farther negative. Unchecked topology/NFRC/list indices, ignored nominal errors, helper/output failures, and assertions leave partial predefined, cache, and EIO prefixes. CP154 itself emits no local diagnostic or status result and has no rollback; a non-return blocks the parent remainder even when a dependency diagnosed the cause. Re-entry recreates locals but appends every table row and repeats enabled EIO; nonzero optical cache skips legacy work, physical zero recomputes, and external-library/cache/scratch mutations persist. HeatBalanceSurfaceManager clear resets its own arrays/flags but owns no CP154 table/EIO state and does not remove duplicate predefined entries or emitted output. OutputReportPredefined clear removes definitions/ids/entries and requires `SetPredefinedTables`; owning topology/cache and General clears rebuild their respective state, while EIO stream rewind/truncation belongs to separate output-file lifecycle. Rust bounded opaque geometry, frame typing, and the static comparator are not this routine; Rust has no complete report-order/fenestration topology, predefined arena, NFRC/shading/report gatherer, EIO, duplicate, failure, or re-entry semantics. CP154 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 163 routines, split 58 state-mapped plus 105 source-mapped, with 58 required. CP155 adds required source-mapped `routine.allocate_surface_heat_bal_arrays` and its project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 101, implemented at `HeatBalanceSurfaceManager.cc` lines 1406-2206, and its sole production `src/` call is `InitSurfaceHeatBalance` line 350 inside the lines-349-355 BeginSim branch, after the caller's per-Surface outdoor/local-node weather and EMS overrides and before the `InterZoneWindow` reduction. The allocation phase defines 136 distinct fields across HeatBalSurf (100), FanSystem (15), MoistureBalance (12), HeatBalance (6), Surface (2), and HeatBalanceSurfaceManager (1): 119 ordinary per-Surface arrays, eight rectangular arrays, eight 19-element-outer jagged histories whose inners have extent `S`, and one EPVector. Of those fields, 121 are unconditional and 15 conditional. The source has 144 syntactic allocate/dimension sites, split 125 unconditional plus 19 conditional, because each of the eight jagged fields has separate outer allocation and inner dimensioning; 144 is therefore not the 121-plus-15 distinct-field count. When reached, exactly 133 distinct fields end at zero or zero-valued default construction; `SurfTempEffBulkAir` starts at 23 C, `TCondFDSourceNode` at 15 C, and `SurfRoughnessExt` at `Invalid` (-1). `AnyInternalHeatSourceInInput` gates ten fields and is evaluated separately at lines 1429 and 1555, while `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` gates five fields and is evaluated once at line 1466. False branches neither clear nor deallocate preserved prior state. After the reached allocation phase completes, line 1607 displays exact text `Setting up Surface Reporting Variables`; the body then visits numeric Surface indices `1..S` and skips only non-`HeatTransSurf` entries. It contains 78 syntactic `SetupOutputVariable` call sites: the conditional union of 77 per-Surface sites plus one unconditional site-total setup call, with 64 Average and 14 Sum sites. The exact Surface-side gates are non-Kiva, non-Window, external-environment-or-advanced with nested non-Window, opaque class with nested non-Kiva, `Construct(surface.Construction).SourceSinkPresent`, Window, exact external environment, and advanced-only construction index; the 21 retained-heat-transfer sites have no additional Surface gate. Per-Surface keys use `Surface.Name`; the site-total Sum uses key `Environment` and existing `SumSurfaceHeatEmission` without resetting it. There is no CP155 report-request filter before the dependency calls. OutputProcessor's first setup call initializes that subsystem when `OutputInitialized` is false, before request checking and dictionary insertion; the dictionary deduplicates name plus units, every call increments setup counters, and these no-meter calls append an active `OutVar` only for requested name/key matches. Allocation and setup are sequential and nontransactional, with no local validation, diagnostic, status, rollback, or cleanup. A failing dependency can retain a six-owner/output-setup prefix and blocks the caller's `InterZoneWindow` scan, remainder, and first-time clear. With `S == 0`, active jagged outers still allocate 19 elements, and `surfQRadFromHVAC.allocate(0)` leaves an empty EPVector with its allocated sentinel true; the display and site-total setup call still occur, and only a requested site-total match becomes active. Negative `S` can mutate many Objexx arrays before the EPVector argument converts to an enormous unsigned size; its allocation sets the allocated sentinel before resize fails. Direct re-entry refills reached value-bearing dimensions, may relocate backing storage, and preserves newly false conditional allocations; setup counters advance again, dictionary entries remain deduplicated, and requested matches can append duplicate active records whose earlier pointers reference pre-redimension storage. Five owner clears reconstruct their array owners; HeatBalance clear restores the two allocation flags, HeatBalanceSurfaceManager clear releases its own reference-air array and resets first-time flags, and OutputProcessor clear is separately required for registrations. BeginEnvironment alone is not a full CP155 reset or rerun. Seven unit-test files make 35 direct calls only as downstream setup and provide no dedicated zero-count or re-entry test. Rust has no complete six-owner allocation, history/default/conditional-preservation state, output-setup, or re-entry lifecycle analog. CP155 adds no source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 164 routines, split 58 state-mapped plus 106 source-mapped, with 59 required. CP156 adds required source-mapped `routine.init_thermal_and_flux_histories` and its project-contract entry immediately after `allocate_surface_heat_bal_arrays`. It is declared at `HeatBalanceSurfaceManager.hh` line 103, implemented at `HeatBalanceSurfaceManager.cc` lines 2208-2447, and its sole production `src/` call is `InitSurfaceHeatBalance` line 383 inside the lines-379-384 BeginEnvrn branch; no unit test calls it directly. The caller has already refreshed Surface weather/local-node/EMS state, conditionally run CP155, refreshed construction absorptances, and called `UpdateVariableAbsorptances`. Its first-time flag gates only exact display `Initializing Temperature and Flux Histories`; CP156 itself has no output or diagnostic, and a successful return next reaches movable insulation. CP156 first visits numeric Zones: placement-new reconstructs each ZoneHeatBalanceData with its named temperature/MAT histories and MixingMAT at 23 C, five humidity values initially 0.01, and remaining zero/default state; only `airHumRatAvg` and `airHumRat` are then overwritten by `OutHumRat`, and `TempTstatAir` becomes 23 C. Every enclosure MRT becomes 23 C, and every existing Space heat-balance record is independently placement-new reset regardless of `doSpaceHeatBalance`, with the same two humidity overwrites. Zone order, stored `spaceIndexes`, and inclusive heat-transfer ranges then receive exactly 34 assignments per reached Surface: `SurfTempEffBulkAir`, `SurfTempIn`, and `SurfTempInTmp` become 23 C, `SurfHConvInt` becomes 3.076 W/m2-K, and the other 30 coefficient/report/temperature fields become zero. Independently guarded nonnegative opaque/IntMass ranges zero four conduction/beam fields; guarded Window ranges set six frame/divider current/old/outside temperatures to 23 C and two shading flags to `NoShade`, with no class recheck. A Zone/Space/term-outer `1..19` pass sets both base temperatures to 23 C and both base fluxes to zero. The first line-2339 evaluation of `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` also resets `SurfCurrNumHist`, both master temperatures, and both master fluxes. A separate `AnyInternalHeatSourceInInput` pass, ordered Surface before term, sets four source/user temperature histories to 23 C and two source flux histories to zero. False feature branches preserve prior allocations and contents. Working `CondFDRelaxFactor` then copies its input. The next loop is term outer `1..MaxCTFTerms + 1` and stored AllHT Surface inner. ExternalEnvironment and OtherSideCondModeledExt outside temperatures use the already refreshed local dry bulb, Ground uses BuildingSurface ground temperature, GroundFCfactorMethod uses FCFactor ground temperature, and other boundaries retain 23 C under normal reachable topology. Each reached outside flux term becomes `q = Construct(surface.Construction).UValue * (OutsideTempHist(1) - InsideTempHist(1))`; each inside term copies base outside-flux slot 2. Because term 1 executes before slot 2 is written, normal preceding zero state leaves inside term 1 zero; term 2 first writes slot 2 to q, so inside terms 2 and higher receive q. Global MaxCTFTerms zero still executes term 1 and reads fixed slot 2; normal 18 reaches all 19 slots. An AllHT pass sets each referenced exterior cavity's `TbaffleLast` and `TairLast` to 20 C. Stored AllHTKiva order uses `surfaceConvMap[SurfNum]`, inserting a missing record, then assigns inside constant 3.076, default forced term, and outside constant 0 in `.in` then `.f` then `.out` order. The same SimpleCTF/EMS expression is evaluated again at line 2413. When true, special boundary families overwrite all 19 master outside-temperature terms; other boundaries retain 23 C, and terms `2..Construct.NumCTFTerms + 1` copy base outside-flux slot 2 to both master fluxes while term 1 and higher unused terms retain zero. Positive `TotOSCM` finally sets numeric OSCM TConv/HConv/TRad/HRad to 20/4/20/4 while preserving EMS fields and other members. CP156 is selective and nontransactional: direct re-entry destructively resets every reached record/range/history/factor/list/count target; newly false gates and removed ranges/list/map entries preserve prior state, so changed topology can mix fresh and stale cells. With zero Zones, existing enclosures and Space records, the CondFD factor, AllHT/cavity/Kiva lists, and OSCMs still reset; conventional `0..-1` ranges skip. It assumes CP155/prior allocations and validated counts, ranges, list membership, Construction/cavity indices, and term bounds. It has no return, local validation, warning, status, catch, rollback, or cleanup; nonfinite humidity, boundary temperatures, or U-value propagate. The concrete placement-new default construction is nonthrowing; unchecked index/container access can assert, terminate, or produce undefined behavior after earlier mutations. Kiva map allocation or `std::function` work can throw; a Kiva failure can leave an inserted key with only `.in` or `.in` plus `.f` committed, and any failure blocks the caller remainder and first-time tail. Clears are distributed across predictor/corrector, ViewFactor, HeatBalance, HeatBalSurf, FanSystem, Surfaces, and SurfaceGeometry/Kiva owners; no partial owner clear reproduces CP156, while a reached BeginEnvironment call is its ordinary reset boundary. Rust's bounded optional `HeatBalanceCtfInitialHistoryPolicy::EnergyPlusSurfInitial` helper accepts configurable initial temperature, selects a typed boundary temperature, computes steady `1/R` flux, and fills variable-length base histories with the same q on both sides. It does not implement EnergyPlus's fixed 19-slot current/prior shell and term-1 inside-flux asymmetry, Zone/Space and 34-plus-4-plus-8 Surface reset, master/source histories, cavity/Kiva/OSCM state, or selective failure/re-entry lifecycle. CP156 adds no source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 165 routines, split 58 state-mapped plus 107 source-mapped, with 60 required. CP157 adds non-required source-mapped `routine.eval_outside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 105 and implemented at `HeatBalanceSurfaceManager.cc` lines 2449-2481. Its sole production `src/` call is `InitSurfaceHeatBalance` line 388, the first child of the shared lines-387-390 `AnyMovableInsulation` block; CP158 follows at line 389, so CP157 failure blocks inside evaluation while a later CP158 failure preserves completed exterior effects. One unit test makes three direct calls with an always-on Schedule: Regular verifies solar absorptance, conductance, thermal absorptance, and roughness, while Glass and GlassEQL verify only solar absorptance; no inactive case is tested. CP157 visits stored `extMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `extMovInsuls(SurfNum)` and calls its Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads the current `SurfActiveConstruction` and its exterior `LayerPoint(1)` material, then copies raw `AbsorpSolar`, `AbsorpThermal`, and `Roughness` to the three exterior Surface fields in that order. This branch preserves stale `H` and exact `presentPrevTS` and does not use `movInsul.matNum`. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL then dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; the comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, preserves positive infinity, and has no upper clamp. Other groups copy raw `AbsorpSolar`. Both active material paths next copy raw thermal absorptance and roughness, and exact `presentPrevTS` remains untouched. Both successful branches overwrite the caller's just-refreshed construction and variable-absorptance exterior values. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-construction properties while preserving H, and removed entries receive no CP157 mutation. Under global `AnyMovableInsulation`, CondFD later reads `extMovInsuls(Surf).H` without checking `present` or exterior-list membership, so a stale positive active-to-inactive H can still select its movable-insulation branch. CP157 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/layer/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and a current inactive-present/property or active-present/H/property prefix. `SurfacesData::clear_state` reconstructs the records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied property arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no exterior movable-insulation input/state, traversal, Schedule controller, active-construction property restoration, glass optical path, conductance/CondFD handoff, or matching failure/re-entry analog. CP157 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 166 routines, split 58 state-mapped plus 108 source-mapped, with 60 required. CP158 adds non-required source-mapped `routine.eval_inside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 107 and implemented at `HeatBalanceSurfaceManager.cc` lines 2483-2513. Its sole production `src/` call is `InitSurfaceHeatBalance` line 389, the second child of the shared lines-387-390 `AnyMovableInsulation` block after CP157; a CP158 failure preserves completed exterior effects and blocks the caller remainder, after a successful CP158 return the caller resumes its intervening ground/shading/radiation work, and reaches unconditional CP159 at line 457 only if those dependencies also return. One unit test makes three positive always-on calls while reusing the same concrete `MaterialShade` record and mutating only its group tag Regular to Glass to GlassEQL. The first verifies solar absorptance, H, present, and thermal absorptance; the later two verify only solar absorptance, and no inactive case is tested. CP158 visits stored `intMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `intMovInsuls(SurfNum)` and calls Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads current `SurfActiveConstruction`, then copies that Construction's `InsideAbsorpSolar` followed by `InsideAbsorpThermal` to the two inside Surface fields. This branch preserves stale `H` and exact `presentPrevTS`, does not use `movInsul.matNum`, and writes no roughness. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; Objexx's comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, retains positive infinity, and applies no upper clamp. Other groups copy raw `AbsorpSolar`; both active paths then copy raw `AbsorpThermal`. Exact `presentPrevTS` and roughness remain untouched. Both successful branches overwrite the current inside absorptance values, including the caller's construction-derived refresh when its earlier gate ran; `UpdateVariableAbsorptances` writes only exterior arrays and contributes no inside value. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-Construction absorptances while preserving H, and removed entries receive no CP158 mutation. Unlike CP157's CondFD path, standard and optimized inside-balance H use is gated by the global flag plus `intMovInsuls(SurfNum).present`, though not by list membership: stale inactive H alone is suppressed, but a removed formerly-active record retaining `present = true` and H can still be consumed. After caller line-417 `CheckGlazingShadingStatusChange`, line-444 passes `SurfIterations = 0` into `CalcInteriorRadExchange`; outside BeginEnvironment, reached non-window enclosure Surfaces can invoke `UpdateMovableInsulationFlag`, which compares `present != presentPrevTS` then tests strict `abs(base Surface.Construction InsideAbsorpThermal - movable AbsorpThermal) > 0.01`. A NaN difference is false, while BeginEnvironment forces recalculation separately. End-zone-timestep `RecKeepHeatBalance` copies `present` to `presentPrevTS` at line 3051. CP158 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and an inactive present/solar or active present/H/solar prefix. `SurfacesData::clear_state` reconstructs records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied absorptance arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no interior movable-insulation input/state, traversal, Schedule controller, active-Construction absorptance restoration, glass optical branch, conductance/inside-balance handoff, long-wave change detection, or matching failure/re-entry analog. CP158 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 167 routines, split 58 state-mapped plus 109 source-mapped, with 60 required. CP159 adds required source-mapped `routine.init_solar_heat_gains` and its project-contract entry immediately after `init_thermal_and_flux_histories` and before `calc_heat_balance_outside_surf`. It is declared at `HeatBalanceSurfaceManager.hh` line 109, implemented at `HeatBalanceSurfaceManager.cc` lines 2515-3776, and called only as unconditional `InitSolarHeatGains(state)` inside `InitSurfaceHeatBalance` line 457; the caller first-time flag gates only its preceding progress text, a CP159 failure blocks daylighting/internal gains/CP160, and only the successful caller tail clears that flag. Exactly four direct calls occur in three unit-test contexts: the incident-multiplier fixture calls twice under positive solar and checks only exact half-versus-whole transmitted solar, WindowFrameTest exercises BeginEnvironment reset with zero solar and asserts only a downstream heat-loss relation, and the CFS fixture calls under positive solar before mutating back-surface state/rerunning distribution and asserts only downstream beam absorption. Every call unconditionally zeros Zone window/opaque conduction rate and energy reports, every enclosure initial-diffuse-reflection term, stored opaque-range conduction/absorbed/report fields, stored Window-range frame/divider/shade/convection/gain/energy fields, and `SurfWinQRadSWwinAbs` through `CFSMAXNL + 1`. While first-time remains true it seeds both ground-reflection factors from each Surface ground view factor. It defines positive solar by `SunIsUp && signed beam+ground+diffuse > 0`, derives sunset and sun-up/no-radiation plus BeginEnvironment reset, then commits `PreviousSolRadPositive` before any broad reset; NaN makes the comparison false, and a failed sunset-only reset loses its edge on retry while the other two reset causes persist. Positive-or-reset calls clear all-Surface inside-beam/incident/reflection and sky/ground arrays, enclosure transmitted reports/energies, stored-Window shade/transmission/reveal/BSDF/absorbed-layer fields; reset-only calls additionally clear only `EnclSolQD`/`EnclSolQDforDaylight`, TDD transmittances and selected dome rate/layer fields, optional three reflection factors, and selected all-Surface reports/profile/system optics. They do not clear `EnclSolQSDifSol`, either complex-window ground split, `SurfSkyDiffReflFacGnd`, positive-only `IncSolMultiplier`, custom-only `GndReflSolarRad`, `AbsDiffWin*`, scheduled-mutated `SurfWinA`, external-library total energy, or all TDD dome W/energy state. The positive branch asserts reflection-table dimensions even when reflection calculation is disabled, computes `max(beam*SOLCOS(3)+diffuse,0)` with the expression first so NaN persists, samples/stores one multiplier for every numeric Surface using same-index request alignment, computes sky and ground incidence, and, when enabled, writes the complex ground split before interpolating the beam ground factor, then interpolates obstruction/ground factors and rebuilds main sky/ground incidence. It calls profile angles, optional reveal reflection, then either the external-library simplified opaque plus interior distribution path or ordinary interior distribution. It forms daylight and normal enclosure diffuse power, copies daylight power into `EnclSolQSDifSol`, adds only flagged off-diagonal interzone contributions, then applies diagonal fraction and `solVMULT`. Shading-range reports omit obstruction additions; stored AllExt reports add them; TDD diffuser overrides use active diffuser `TransDiff` and dome angle/sunlit while retaining earlier ground-component reports; shelf ground overrides happen after incident totals and do not refresh them. Opaque absorption uses base Construction and the first exact scheduled Surface/base-Construction pair, with unscheduled beam scaling but scheduled bare `SurfOpaqAI`; duplicate opaque ranges overwrite outside/report assignments but repeat inside `+=`. The Window loop dereferences SurfaceWindow before its solar/TDD guard and uses active Construction except base EQL layer count; Detailed optics cover shade/screen/blind horizontal sky-ground splitting and switchable interpolation, BSDF uses the first exact scheduled Surface/active-Construction pair and mutates `SurfWinA`, EQL uses base-system count with active optics, and external-library layers never write total energy. Shaded-construction array/member ownership differs unchecked. Common frame/divider work preserves projection, reveal, switchable, suspended-divider, ExtBlind/ExtShade/ExtScreen formulas; between-glass enters alternate incidence but no final absorber case. A zeroed local beam array is populated by earlier lists/overrides and consumed later, so malformed list/range alignment can yield zero beam; repeated windows reset Detailed/EQL totals locally but can accumulate BSDF/external totals. Final TDD dome absorption uses base Construction and writes energy inside its layer loop. Representative averaging uses raw constituent-area divides with no zero/topology guard, can consume earlier representatives in traversal order, uses active representative `TotGlassLayers` regardless model, and overwrites only per-area opaque/window-layer/frame/divider fields without recomputing Window layer W, totals, energy, or reports. CP159 emits no direct diagnostic and has no validation, catch, rollback, or cleanup; dimension/material assertions, unchecked blind casts, indices, TDD/area/reflection denominators, trig/nonfinite inputs, child failures, or allocation can leave an ordered prefix. Six mutated owners require coordinated clear: HeatBalance, HeatBalSurf, Surfaces, Environment, DaylightingDevices placement-new reconstruction plus `HeatBalSurfMgr::clear_state`; the latter rearms first-time and recreates scratch arrays, Environment restores the previous-solar latch false, and dependency-owner clears alone do not replay CP159. Rust incident-solar forcing diagnostics and bounded opaque absorption, plus run-blocked multiplier/scheduled-inside declarations, are not this latch/distribution/window/report lifecycle. CP159 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 168 routines, split 58 state-mapped plus 110 source-mapped, with 61 required. CP160 adds required source-mapped `routine.init_int_solar_distribution` and its project-contract entry immediately after `init_solar_heat_gains` and before `compute_int_thermal_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 111, implemented at `HeatBalanceSurfaceManager.cc` lines 3778-4177, and called only as unconditional `InitIntSolarDistribution(state)` inside `InitSurfaceHeatBalance` line 468; the first-time flag gates only preceding progress text, and a CP160 failure blocks interior convection and the successful parent-tail flag clear. The only direct unit context calls twice: with InterZone disabled and beam report 10 W, the night call observes an already-zero report remain zero, while the sun-up call checks only 10/6 = 1.666667 W/m2. SunIsUp gates only that AllHT, non-Shading beam-report traversal; at night CP160 still distributes internal short wave and leaves prior beam intensity/amount/energy untouched. Every call assigns each Solar enclosure raw `EnclSolQD + sum(space QLTSW)` and lights-only `sum(QLTSW)`; an InterZoneWindow receiver/source double loop requires both receive flags, excludes the diagonal, recomputes source QLTSW, adds raw matrix-weighted total/lights/diffuse power, and overwrites diffuse-report energy after each qualifying source while a no-source receiver gets no energy write. The enclosure values are then multiplied by `solVMULT` and, only for InterZoneWindow, the diagonal fraction; there is no normalization or range/finite guard. Sun-up beam reporting divides Zone beam W by raw enclosure total area, then multiplies by Surface area plus divider area and timestep seconds. Zone/stored-Space traversal visits inclusive opaque then Window ranges without sorting, deduplication, class checks, or bound validation. Opaque total short-wave absorption uses current `SurfAbsSolarInt`, lights-only absorption uses base Construction `InsideAbsorpSolar`, and initial diffuse is added last. Under global movable insulation, every reached opaque record marked present first writes `SurfQRadSWOutMvIns = prior outside absorption * current exterior absorptance / base outside-material absorptance`, then overwrites outside absorption through an optional `MaterialFen.Trans` and a second division by current absorptance; a failed cast supplies zero transmittance and both denominators are unchecked. Direct retry feeds the overwritten outside value back, while a false global/present gate leaves that value and `SurfQRadSWOutMvIns` stale. Every Window dereferences current SurfaceWindow, radiant/Solar enclosures, and active Construction with no exterior-solar or class gate. Both EQL and non-EQL overwrite Window internal radiant gain from enclosure long wave, `radThermAbsMult`, and current thermal absorptance; a load-component pulse adds exactly 0.01 times radiant-enclosure floor area, but common frame/divider formulas keep the unpulsed value plus HVAC radiation. Non-EQL has no separate external-library branch and uses active conventional optics: unshaded layers add back diffuse absorption; shade/screen layers use shaded back optics; only IntBlind/ExtBlind interpolate pane absorption with unclamped `Interp`, so BGBlind skips pane addition while still receiving the later any-blind shade absorption. Only interior shade/blind writes shade long-wave absorption; exterior shade/blind/screen scales shade short wave by glazed fraction. Switchable layers use `InterpSw`, which clamps ordered factors including infinities to [0,1] but propagates NaN, and dereferences the shaded Construction without a zero-index guard. Zero-index or conditional/invalid shading can skip main layer work and later fail in the Detailed initial-diffuse tail. Positive frame/divider areas add Solar-enclosure short wave plus unpulsed internal/HVAC long wave with raw projection corrections; suspended dividers assert a last-layer MaterialGlass and use an unchecked multiple-reflection denominator, while IntShade asserts MaterialFen and IntBlind applies current solar/IR transmission. EQL uses active EQL NL and back diffuse optics and omits frame/divider work. For every positive adjacent link, a conventional target uses the current/source active layer count but adjacent base `AbsDiff`; an EQL target uses adjacent base EQL NL but current/source active `AbsDiffFrontEQL`. The final Detailed/BSDF/EQL tail adds prior initial-diffuse layer values, and Detailed shade/screen/blind also adds initial shade absorption. All CP160 Window additions mutate only per-area `SurfWinQRadSWwinAbs` and never refresh CP159 layer W, total W, or total energy; frame/divider additions likewise have no separate W/energy derivation here. The unconditional final TDD child is declared at `DaylightingDevices.hh` line 95, implemented at `DaylightingDevices.cc` lines 1506-1559, called only by CP160 line 4176, and has no direct unit-test call. It uses diffuser base `TransDiff`, stale CP159 diffuser total W, normally prior CP159/dependency dome layer one, and newly CP160-mutated diffuser layer one; its raw formula applies diffuser area even to the dome term and divides by `TransDiff`. Zero-first `max(0, rawGain)` maps negative, negative infinity, and NaN report values to zero while retaining positive infinity, but transition-zone gains use unclamped signed/nonfinite rawGain times length divided by unchecked total length. The current pipe report is committed before its transition loop, so failure can retain that report and a partial current `TZoneHeatGain` prefix after complete earlier pipes. Preceding `ManageInternalHeatGains`/`FigureTDDZoneGains` can zero current transition gains on BeginEnvironment but not the pipe report, and its current internal-gain update completes before CP160 writes new transition values. CP160 has no direct diagnostic, validation, return status, catch, rollback, or cleanup; only the suspended-divider and IntShade casts assert, while invalid topology, arrays, indices, layer mismatches, denominators, and nonfinite inputs can leave an ordered prefix. Re-entry mixes assigned enclosure/radiant/TDD state with additive Zone diffuse, opaque, layer, adjacency, frame/divider, and shade state; Sun-down, no-source, existing no-longer-reached range members, reduced transition-zone counts, and changed gates can preserve stale values, and normal parent order merely relies on preceding CP159 resets. Four directly mutated owners clear by placement-new: HeatBalance, HeatBalSurf, Surfaces, and DaylightingDevices; HeatBalSurfMgr clear only rearms the caller display flag. Rust separately consumes initialized/supplied opaque inside-short-wave state and has a bounded typed-Zone radiant-gain distributor whose ordered `<= 0` gates admit NaN, but no CP160 Solar-enclosure/interzone, Window/shade/frame/divider, adjacency, movable-insulation, TDD, report, failure, or re-entry state machine. CP160 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 169 routines, split 58 state-mapped plus 111 source-mapped, with 62 required. CP161 adds required source-mapped `routine.compute_int_thermal_absorp_factors` and its project-contract entry immediately after `init_int_solar_distribution` and before `compute_int_sw_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 113, implemented at `HeatBalanceSurfaceManager.cc` lines 4179-4295, and called only by unconditional `InitSurfaceHeatBalance` line 427; runtime execution is after shading-status production and before CP162, CP159, and CP160. The caller first-time flag gates only progress text plus the separate preceding `InitInteriorRadExchange`, not CP161, and a CP161 failure blocks every later child and the successful caller-tail flag clear. Its sole direct unit test uses one 1 m2 `IntBlind` Window with 0.1 shade plus 0.1 glass effective emissivity and checks only `SurfAbsThermalInt = 0.2` and `radThermAbsMult = 5`. Pass one scans the full allocated `EnclRadInfo` container, skips false `radReCalc`, then visits stored enclosure Space Window ranges: `IntShade`/`IntBlind` assign effective shade-plus-glass emissivity and every other flag assigns active-Construction inside thermal absorptance. Pass two, independently of `radReCalc`, scans `SurfMovSlatsIndexList` only when `AnyMovableSlat` and rewrites only currently interior shade/blind entries. Pass three again scans the full allocated enclosure container and each flagged `SurfacePtr`: nonswitchable area uses current `SurfAbsThermalInt`, while switchable area uses clamped-factor `InterpSw` between declared base-Construction and `activeShadedConstruction` absorptance. Positive frames add area times `(1 + 0.5*projection)` times emissivity. Positive dividers start with divider emissivity, substitute base-Construction absorptance when suspended, and either use the projected term or, for an interior device with a declared layer, assert the active shaded Construction last material as `MaterialFen` and add shade effective emissivity plus divider absorptance times shade thermal transmittance or current blind back-IR transmittance; the shaded Construction is dereferenced before the layer-present test. This preserves deliberate active/base/runtime-optics asymmetries. The first pass visits full Window ranges, but normally initialized `SurfacePtr` holds representative surfaces only; CP161 ignores constituent-aggregated `EnclRadInfo.Area` and uses raw representative Surface/frame/divider areas, creating a representative-extent mismatch. The producer bulk paths use separate Solar and radiant enclosure counts, while its shading-change loop is Solar-count-bounded but reads same-index radiant `SurfacePtr`; CP161 range-for can see a retained radiant-container tail and never clears `EnclRadInfo.radReCalc`. CP162 reads the corresponding `EnclSolInfo.radReCalc`, not the same flag object. Normal movable slats are observed by the producer through `SurfacePtr`, while direct or malformed topology can still let the independent list update an unflagged enclosure. More importantly, CP158 can change opaque inside thermal absorptance without that movable-insulation transition itself setting `radReCalc`, leaving a normal-path multiplier stale absent another producer cause. Stored orders have no local sort, deduplication, class/model, membership, or bounds validation. `radThermAbsMult = 1/SUM1` is committed only after a complete flagged enclosure with no zero, sign, minimum, or finite guard: zero becomes positive infinity, signed infinity becomes signed zero, negative stays negative, and NaN propagates; `InterpSw` clamps ordered factors including infinities but preserves NaN and raw nonfinite endpoint arithmetic. CP161 has no direct diagnostic, return status, allocation, catch, rollback, or cleanup and only the divider material assertion. Failure can leave a first-pass Window prefix, then a movable-list prefix, or complete earlier enclosure multipliers while the current/later multipliers remain old; false `radReCalc`, no-longer-reached range/list entries, malformed independent slat updates, and direct retry can preserve selective stale state, while a nonpositive frame/divider area simply drops from a recomputed flagged sum. Exactly two direct owners have mutated targets: HeatBalSurf owns the written `SurfAbsThermalInt` and separately supplies the read-only movable-slat list, while ViewFactor owns the written `radThermAbsMult` and separately supplies read-only enclosure flags/topology; Surfaces, HeatBalance topology, Construction, Material, Window, SolarShading, and interior-radiation state are dependencies only. Rust has only a bounded typed-Zone radiant-gain distributor that applies `.max(0.0)` to absorptance but whose ordered `<= 0` gain/sum gates admit NaN, not this recalculation/shading/representative/frame-divider/raw-reciprocal state machine. CP161 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 170 routines, split 58 state-mapped plus 112 source-mapped, with 63 required. CP162 adds required source-mapped `routine.compute_int_sw_absorp_factors` and its project-contract entry immediately after `compute_int_thermal_absorp_factors` and before `compute_dif_sol_exc_zones_wiz_windows` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 115, implemented at `HeatBalanceSurfaceManager.cc` lines 4297-4471, and called only by unconditional `InitSurfaceHeatBalance` line 433 immediately after CP161; the caller first-time flag gates only the preceding diffuse-solar progress text, and a CP162 failure blocks conditional interzone diffuse exchange, daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. There is no direct unit-test call. CP162 scans the full allocated `EnclSolInfo` container, skips false `radReCalc`, and accumulates each flagged enclosure over stored `SurfacePtr` order without local sort, deduplication, class, membership, index, or finite validation. Normal Solar topology contains every stored Space Surface except AirBoundary, including nonrepresentatives, whereas the shading-change producer is Solar-count-bounded but observes same-index representative-only radiant `SurfacePtr`; this records an observer/calculation-domain asymmetry without claiming a normal unflagged constituent. Only direct, malformed, representative-misaligned topology or count/container mismatch can turn that asymmetry into an unflagged Solar enclosure. Each Surface selects its active Construction and is opaque only when active `TransDiff <= 0`; opaque area uses current `SurfAbsSolarInt`, while NaN transmittance enters the Window branch. Conventional versus equivalent-layer selection then uses the declared base Construction `WindowTypeEQL`. Conventional Windows sum active-layer back absorptances and active diffuse transmission; only a nonzero storm-aware `SurfWinActiveShadedConstruction` enables shade/screen/blind optical substitution: shade/screen optics come from that Construction, blinds raw-linearly interpolate current slat endpoints, and a shade/blind flag with index zero silently retains bare active optics and zero shade absorption. Switchable glazing alone has two separate positive-shaded-Construction assertions and clamped-factor `InterpSw` for layer absorption and transmission. Their raw Surface-area term is `TransDiffWin + AbsDiffTotWin + DiffAbsShade`; positive frames add area times solar absorptance times `(1 + 0.5*projection)`. Positive dividers use current divider solar absorptance; suspended dividers assert the active Construction last layer as `MaterialGlass` and apply an unchecked glass/divider multiple-reflection denominator. Interior shade/blind dividers omit projection and add `DividerAbs + DiffAbsShade`; all others use `(1 + projection)*DividerAbs`. The base-EQL branch instead consumes the active Construction EQL pointer, layer count, diffuse transmission, and back absorptances and ignores shading, switching, frames, and dividers. After a complete enclosure, only strict `SUM1 > 0.01` writes `solVMULT = 1/SUM1`; positive infinity becomes positive zero, while exactly 0.01, smaller/negative values, negative infinity, and NaN enter the bad-sum branch, issue the exact misspelled `ComputeIntSWAbsorbFactors` warning only while `solAbsFirstCalc` is true, set that latch false after the warning returns, and write zero. A later good sum or BeginEnvironment does not rearm the latch. False recalculation preserves both targets; failures can leave earlier enclosure commits and the current/later multipliers and latch old, while warning-pipeline failure can leave partial diagnostic side effects before either target is committed. CP158 can change opaque `SurfAbsSolarInt` without itself producing a recalculation flag, giving the normal-path stale-multiplier case; direct or malformed Solar/radiant observer misalignment can also preserve stale state. ViewFactor is the sole direct mutation owner for `solVMULT` and `solAbsFirstCalc`; warning counters/streams/SQLite/callbacks are child diagnostic side effects, while Surface, HeatBalSurf, Construction, Material, equivalent-layer, Window, SolarShading, global, and topology state are dependencies. Rust has no `ComputeIntSWAbsorpFactors`, Solar multiplier, latch, or post-initialization production writer for `inside_shortwave_absorbed_w_per_m2`; its separate typed-Zone thermal-radiant helper has ordered `<= 0` gain/sum gates that admit NaN and is not this short-wave routine. CP162 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 171 routines, split 58 state-mapped plus 113 source-mapped, with 64 required. CP163 adds required source-mapped `routine.compute_dif_sol_exc_zones_wiz_windows` immediately after `compute_int_sw_absorp_factors` and before `init_ems_controlled_surface_properties` in full source-definition order; its project-contract entry remains after `compute_int_sw_absorp_factors` and before `calc_heat_balance_outside_surf` in required-routine order. It is declared at `HeatBalanceSurfaceManager.hh` line 117, implemented at `HeatBalanceSurfaceManager.cc` lines 4473-4644, and called only at `InitSurfaceHeatBalance` line 439 inside the lines-435-440 `InterZoneWindow` block; that parent flag gates both progress-and-call, while the nested first-time flag gates only the exact progress text. A guarded CP163 failure blocks daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. Exactly three direct calls occur in one unit fixture: with three enclosures and two reciprocal 1 m2, 0.1-transmittance Windows, the first call leaves receiver `HasInterZoneWindow` false and checks only identity diagonals plus all receive flags false; before the second call the fixture enables enclosures 1 and 2 and checks only flags true, true, false; before the third it enables `KickOffSimulation` and again checks only identity diagonals plus false flags. No test asserts a matrix coefficient or covers sizing kickoff, denominators, multi-enclosure paths, nonfinite values, allocation incoherence, or either assertion's failure. Allocation is gated solely by an unallocated `ZoneFractDifShortZtoZ`: that path allocates the target square matrix, receive-flag vector, then separate `DiffuseArray` scratch. Every call resets flags false, target identity, then scratch identity before either kickoff flag can return; already allocated dimensions are not repaired. Using the consumers' row-receiver/column-source convention, a stored-order `AllHTWindowSurfaceList` pass skips nonpositive adjacency, Surface-self adjacency, declared/base `TransDiff <= 0`, and a false receiver `HasInterZoneWindow`, then adds `A(receiver, source) += TransDiff * receiver.solVMULT * Area`; it does not use active/shaded/storm optics, validate the adjacent endpoint, sort, deduplicate, or validate indices, class, sign, range, or finiteness. The receiver flag is set when `solVMULT != 0`, independently of the coefficient. Same-enclosure paired Surfaces can modify raw diagonal state that the next transform discards. With raw matrix `A` frozen and identity scratch `D`, every distinct pair computes `D(R,S) = A(R,S)/(1-A(R,S)*A(S,R))`, and `D(N,N) = 1 + sum[M != N](A(N,M)*D(M,N))`; there is no denominator or finite guard. The copied target is followed by exactly two dimension assertions. A later scan only ORs flag `N` when some distinct receiver has `D(M,N) > 0`, making that scan a positive source-column witness rather than the direct pass's receiver witness. Frozen `D` then contributes every flagged, node-distinct simple path of exactly two, three, and four edges with products `D(J,K)*D(I,J)`, `D(K,L)*D(J,K)*D(I,J)`, and `D(L,M)*D(K,L)*D(J,K)*D(I,J)` into receiver/source cells `Z(I,K)`, `Z(I,L)`, and `Z(I,M)`; additions never feed later products, diagonals are not updated, node revisits and paths beyond four edges are omitted. Edge gates skip only exact signed zero, so negative, infinite, and NaN coefficients enter; ordered `> 0` flagging rejects negative and NaN but accepts positive infinity. CP163 emits no diagnostic and has no status, catch, rollback, cleanup, or local validation. Allocation, reset, direct-pass, pair-copy, flag-scan, or path failure can leave the corresponding ordered prefix; a coherent direct retry first erases all three objects, while kickoff commits false/identity state and returns. When production `InterZoneWindow` is false, CP163 is not entered and prior target/flag/scratch state remains dormant stale; CP159 and CP160 gate their normal interzone consumers on the same flag. Exactly two direct owners mutate: HeatBalSurf owns the target matrix and flags and clears them by placement-new and also supplies the read-only caller gate `InterZoneWindow`; CP163 never writes that gate and placement-new restores it false. HeatBalSurfMgr owns and explicitly clears scratch. Independent owner clears can desynchronize the single-guard allocation assumption in either direction. Rust has adjacent-zone opaque heat-transfer links and window-optical declarations but no Solar-enclosure exchange matrix/flags, bilateral transform, fixed simple-path expansion, kickoff reset, or matching failure/re-entry state. CP163 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 172 routines, split 58 state-mapped plus 114 source-mapped, with 65 required. CP164 adds non-required source-mapped `routine.init_ems_controlled_surface_properties` immediately after `compute_dif_sol_exc_zones_wiz_windows` and before `init_ems_controlled_constructions` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 119, implemented at `HeatBalanceSurfaceManager.cc` lines 4646-4720, and called only from `HeatBalanceManager.cc` line 2663 under `AnyEnergyManagementSystemInModel`; the same guard calls definition-next CP165 at line 2662 first, so CP165 is CP164's runtime predecessor, not successor. `ManageHeatBalance` calls the before-init EMS point before `InitHeatBalance`/CP164 and the after-init EMS point afterward, so CP164 observes actuator state as it stands after the before-init EMS point while after-init changes wait for a later entry. No unit test calls CP164 directly or references its three override-on fields. Each entered call first sets `dataGlobal->AnySurfPropOverridesInModel` false, then scans stored Material pointers until the first Regular material with any Solar, Thermal, or Visible override active; non-Regular flags cannot set the global, and no active Regular takes an immediate return. That last-off return does not restore prior working Material absorptances or Construction face copies, and the false global suppresses the normal non-BeginSim Surface refresh and override-caused enclosure recalculation absent another cause. With any active Regular override, a second full Material pass recomputes every Regular material in Solar, Thermal, Visible order: active channels use `max(min(value,0.9999),0.0001)` and inactive channels restore raw input, while non-Regular materials remain untouched. ObjexxFCL's ordered ternaries clamp negative infinity to 0.0001 and positive infinity or NaN to 0.9999; inactive input values are unguarded. The Construction pass skips Windows and exactly zero layers, then for every other layer count copies a nonzero last-layer endpoint into inside Visible, Solar, Thermal and a nonzero first-layer endpoint into outside Visible, Solar, Thermal. Window, zero-layer, and zero-endpoint faces retain old values; endpoint materials need not be Regular, and negative/oversized counts or indices, null pointers, and topology are unchecked. CP164 has no allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. A malformed detection pointer can fail after the initial global write but before any current-material target write; a second-pass pointer failure leaves only complete prior Regular-material triples, and invalid endpoint access fails before the current face's first Visible copy although an outside-face failure can follow a complete inside-face triple; active re-entry overwrites reached state, last-off or non-Regular-only re-entry preserves stale values, and a false outer EMS gate preserves everything without entry. Exactly three direct mutation owners are DataGlobal, MaterialData, and ConstructionData; their independent clears do not replay a coherent projection. Rust immutable material absorptances and run-blocked variable-absorptance declarations are not this EMS actuator/global-flag/mutable endpoint lifecycle. CP164 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 173 routines, split 58 state-mapped plus 115 source-mapped, with 65 required. CP165 adds non-required source-mapped `routine.init_ems_controlled_constructions` immediately after `init_ems_controlled_surface_properties` and before `update_intermediate_surface_heat_balance_results` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 121, implemented at `HeatBalanceSurfaceManager.cc` lines 4722-4940, and called only from `HeatBalanceManager.cc` line 2662 under `AnyEnergyManagementSystemInModel`, immediately before CP164 at line 2663. It observes actuator state as it stands after the before-init EMS point; after-init changes wait for a later `InitHeatBalance` entry. No unit test calls CP165 directly. Three `HeatBalanceManager` integration fixtures exercise it indirectly: the line-1373 Window fixture checks morning/afternoon Construction plus downstream transmittance/daylight after repeated manager calls, while the line-1974 Window and line-2181 CondFD fixtures check only final current Construction equal to the override value and the ON flag true. They do not cover no-ON last-off state, ON-nonpositive/OFF restoration, Window cache overwrite, cached-bad retention, first-versus-next-call asymmetry, CTF/EMPD warning or source-loss branches, CondFD node or source-loss branches, HAMT, Kiva, another algorithm, invalid targets, exact diagnostics, partial failure, or retry. Each entered call first sets `dataGlobal->AnyConstrOverridesInModel` false and scans only the Surface ON flags. Any ON flag, including one paired with a zero or negative integer value, sets it true and enters the full pass; zero ON flags take a stale quick return without restoring current/active Construction, `IsUsed`, or cache state. In the full pass, every OFF Surface and every ON Surface with a nonpositive integer value restores `Surface.Construction` and `SurfActiveConstruction` from `ConstructionStoredInputValue` but does not clear `IsUsed` or either compatibility cache. A positive value is used as an unchecked Construction/cache index. A Window target ignores Surface class and current heat-transfer algorithm, overwrites even a cached rejection to checked/okay before testing the pair, and fast-applies the target in the same call; an already checked/okay pair likewise writes current Construction, marks target `IsUsed`, and writes active Construction. A cached-bad pair does nothing and preserves whatever current/active Construction was already present. An unchecked CTF/EMPD pair premarks okay/checked, then orders a histories Warning plus three Continue calls, a terms Warning plus three Continue calls, and a one-way current-source/sink to target-without-source/sink Severe plus three Continue calls, for 12 possible diagnostics; only source loss rejects. CondFD similarly premarks, orders a node-mismatch Severe plus three Continue calls before an independent source-loss Severe plus three, and can emit eight; either mismatch rejects. HAMT and Kiva each emit Severe plus one Continue and cache rejection. Every other algorithm remains unchecked, silent, and unchanged, while Window, cached-good, cached-bad, and restore branches emit no CP165 diagnostic. A successful first unchecked CTF/EMPD or CondFD evaluation writes only `Surface.Construction`; only a later checked/okay call also marks `IsUsed` and updates `SurfActiveConstruction`. The historical cache is keyed only by target Construction and Surface, while compatibility reads the mutable current Construction, so target switching can validate against a prior override and later cache hits never revalidate changed current state. An invalid positive target fails at the initial `TypeIsWindow` dereference before premarking. CTF/EMPD warning interruption leaves a true cache and skips the remaining checks on retry, while interruption of its source-loss rejection can fast-accept a forbidden target. A failed CondFD node read or interrupted node diagnostic before the false write can likewise fast-accept, but after a complete node rejection has written false, a later source-loss diagnostic failure retries cached-bad; when node counts match, interruption of the sole source-loss rejection can fast-accept. Interrupted HAMT/Kiva diagnostics leave the pair available to diagnose again. CP165 performs no body allocation, status return, assertion, catch, rollback, cleanup, or index/topology validation, and cache/diagnostic/current/`IsUsed`/active failures can leave ordered prefixes. `AnyConstrOverridesInModel` independently disables the `SimpleCTFOnly` history fast path and forces Solar/radiant-enclosure recalculation even when the sole ON value is nonpositive. The four direct mutation owners are DataGlobal, SurfacesData, ConstructionData, and RuntimeLanguageData; diagnostic counters/streams/SQLite/callbacks are child side effects, and independent clears cannot replay a coherent current/active/cache/use projection. Rust typed Constructions, immutable surface references, positive construction-use evidence, and diagnostic CTF-coefficient overrides provide no EMS Construction actuator, mutable current/active selection, compatibility cache, `IsUsed` lifecycle, or matching diagnostics. CP165 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 174 routines, split 58 state-mapped plus 116 source-mapped, with 65 required. CP166 adds required source-mapped `routine.update_intermediate_surface_heat_balance_results` after `init_ems_controlled_constructions` and before CP167's `update_non_representative_surface_results` in the mapped Surface-manager ledger; its project-contract requirement is after `calculate_zone_mrt` and before `manage_air_heat_balance`, preserving the inside-parent/general-or-optimized-child/MRT/tail grouping. It is declared at `HeatBalanceSurfaceManager.hh` line 132, implemented at `HeatBalanceSurfaceManager.cc` lines 4951-5020, and called in production only from `CalcHeatBalanceInsideSurf` line 7812 immediately after `CalculateZoneMRT`; failure always blocks the wrapper return and, on the main Surface-manager path, downstream Air balance. Absent `ZoneToResimulate`, three Zone/Space passes use inclusive bounds 1 through `NumOfZones`; when present, both bounds become the unchecked supplied integer. The sole production partial caller normally dereferences that Zone's lists before CP166, while a direct present zero, negative, or greater-than-`NumOfZones` value reaches CP166's invalid Zone dereference; if an `INT_MAX` body completed, loop increment would overflow. With an absent nonpositive `NumOfZones`, CP166's three Zone loops and CP167's single Zone/Space traversal are empty but the global Kiva pass still runs. One direct unit fixture calls CP166 four times with the optional argument absent at lines 8694, 8702, 8710, and 8718 and asserts only `SurfQdotRadSolarInRepPerArea = SurfOpaqQRadSWInAbs - SurfQdotRadLightsInPerArea` for exact scenarios 0, 3, -0.1, and 0.00001 within tolerance; its Window and convective ranges are empty, representative mode is false, and the Kiva list is empty. The first pass visits each selected Zone's stored Space order and inclusive Window range, adding only `ExtSolar` `SurfWinHeatGain` values into the existing `ZoneWinHeatGain` without resetting it. After every Space, `>= 0` writes only gain rate then gain energy, while the negative branch writes only negated loss rate then loss energy; the opposite sign pair remains stale, zero and negative zero use gain, positive infinity uses gain, and negative infinity or NaN use loss. `InitSolarHeatGains` normally resets these five Zone arrays earlier, but direct or repeated CP166 entry accumulates Window gain again, and a multi-Space failure can leave reports lagging a newer partial sum. When `UseRepresentativeSurfaceCalculations` is true, CP166 then calls definition-next CP167 with the same optional argument; because this is after Window accumulation, the current call can sum pre-child nonrepresentative Window gain, while CP167 mutations feed later assignments and later calls. The second selected-Zone pass overwrites each inclusive opaque-or-Window range with `SurfQdotConvInPerArea = -SurfHConvInt * (SurfTempIn - RefAirTemp)`. The third overwrites each opaque-or-internal-mass range with the tested solar-minus-lights expression. Repeated Space/range membership overwrites the same targets again, while empty, inverted, or unreached ranges preserve prior per-Surface values. Finally, regardless of optional Zone bounds or `NumOfZones`, CP166 visits the global stored `AllHTKivaSurfaceList` and overwrites each `SurfOpaqInsFaceCondFlux` with the negated ordered sum of inside convection, net long-wave, HVAC radiation, internal-gain radiation, and absorbed short-wave terms; a nonselected Kiva Surface can therefore consume stale convection and still be mutated. All arithmetic is raw: there is no finite, sign, range, topology, duplicate, or overflow guard, so infinities and NaN follow IEEE operations and duplicate Window membership repeats additions. CP166 has no body allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. Failure can leave ordered prefixes of Window additions, per-Space sign-report rate/energy pairs, CP167 child effects, convective assignments, solar-minus-lights assignments, and global Kiva assignments; scalar expressions finish their reads before the current target write. Re-entry accumulates Window gains but overwrites the three per-Surface results from current inputs, preserving stale opposite-sign Zone reports. Exactly two direct owners mutate: HeatBalanceData owns the five Zone arrays and HeatBalSurfData owns convection, solar-minus-lights, and Kiva flux targets; both clear by placement-new. CP167 can indirectly mutate those plus placement-new-cleared SurfacesData and explicitly cleared HeatBalSurfMgr state. Independent clears cannot replay the projection. Rust has a separate bounded inside-convection report formula and typed surface state, but no CP166 Zone/Space/window-report orchestration, representative child, solar-minus-lights report state, global optional-zone-independent Kiva pass, or matching failure/re-entry lifecycle. CP166 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 175 routines, split 58 state-mapped plus 117 source-mapped, with 66 required. CP167 adds non-required source-mapped `routine.update_non_representative_surface_results` immediately after `update_intermediate_surface_heat_balance_results` and before `gather_for_predefined_report` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 134, implemented at `HeatBalanceSurfaceManager.cc` lines 5022-5174, and called only by CP166 line 4985 after CP166's additive Window pass and under `UseRepresentativeSurfaceCalculations`; CP167 itself does not test that flag, so a direct call bypasses it. No unit test calls CP167, and CP166's sole direct fixture explicitly disables representative calculations; a separate topology-only `DataSurfaces.unit.cc` fixture proves that a zero-area constituent can map to an area-20 representative but does not execute CP167. An absent optional Zone selects inclusive 1 through `NumOfZones` while a present raw integer selects one unchecked Zone; one outer traversal follows stored Zone/Space order and executes each reached Space's inclusive HT, opaque/internal-mass, then Window ranges before the next Space or Zone, without sorting, deduplication, class, membership, bounds, finite, or topology validation. Inverted ranges are empty, malformed 0-through-0 enters index zero, repeated ranges overwrite again, self representatives are skipped, and non-self chains copy one raw hop whose state may already have been projected earlier in traversal. The per-Space HT range performs at most 21 ordered writes: `SurfTempIn`, `SurfTempOut`, `SurfTempEffBulkAir`, `SurfHConvInt`, `SurfQdotRadNetLWInPerArea`, `SurfQdotRadIntGainsInPerArea`, `SurfQdotRadHVACInPerArea`, `SurfQdotConvOutPerArea`, `SurfHConvExt`, `SurfQdotRadOutRepPerArea`, `SurfHAirExt`, `SurfHSkyExt`, `SurfHGrdExt`, `SurfTAirRef`, conditional `SurfTAirRefRpt`, exterior-convection `hfModelEq` then `hnModelEq`, both additional heat-source values, `SurfTempInTmp`, and `RefAirTemp`. The report flag is recomputed from the copied enum only when it is not Invalid, leaving an Invalid target report stale and allowing another malformed enum to fail its unchecked table lookup; only the two named exterior-convection members are copied, and the `#if 0` diagnostic block is not built. The following per-Space opaque/internal-mass range copies `SurfQdotRadLightsInPerArea`, `SurfOpaqQRadSWOutAbs`, then `SurfOpaqQRadSWInAbs`. The final per-Space Window range computes target Area divided by the same target Area, so every finite nonzero value including a negative gives one while zero, infinity, or NaN gives NaN; glazing gains always use that ratio. Positive target frame/divider areas separately divide by unchecked representative areas and copy gain, loss, inside temperature, then outside-surface temperature; false branches preserve those four fields and contribute local zero. The combined frame/divider gain is always overwritten and whole gain becomes representative whole gain minus representative aggregate times the self-area ratio plus the new target aggregate, so two false branches preserve eight detailed fields but zero the aggregate. Raw nonfinite arithmetic, zero denominators, overflow, malformed chains, and repeat-call scaling are unguarded. CP167 has no body allocation, enabled diagnostic/assertion, status, catch, rollback, cleanup, or validation; failures can leave complete earlier-Space projections followed by ordered current-Space HT, opaque, glazing, frame/divider, aggregate, and whole-gain prefixes, while retry overwrites reached fields but preserves self, false-branch, empty, no-longer-reached, and Invalid-report stale state and can compound traversal-dependent chains. HeatBalSurfData, HeatBalanceData, and SurfacesData clear their direct mutations by placement-new, while HeatBalSurfMgr explicitly clears `RefAirTemp`; independent clears cannot replay the four-owner projection. Rust has bounded opaque `SurfaceHeatBalanceState` and typed `WindowFrameAndDivider` declarations but no representative flag/topology/ID projection, mutable matching Window gain/report lifecycle, or CP167 runtime/failure/re-entry behavior, and runtime Surface indexes leave fenestration empty. CP167 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 176 routines, split 58 state-mapped plus 118 source-mapped, with 66 required. CP168 adds required source-mapped `routine.calculate_zone_mrt` immediately after `update_thermal_histories` in the Surface-manager routine ledger; its project-contract requirement is after `calc_heat_balance_inside_surf_2_ctf_only` and before `update_intermediate_surface_heat_balance_results`. It is declared at `HeatBalanceSurfaceManager.hh` lines 140-141, implemented at `HeatBalanceSurfaceManager.cc` lines 5583-5699, and called in production only from `CalcHeatBalanceInsideSurf` line 7811 after the selected solve and immediately before CP166. One `SurfaceGeometry.unit.cc` fixture calls it three times with the optional Zone absent: all-five-Surface 10 C checks Zone/enclosure/Space MRT 10; Zone Surface temperatures 10/20/30 check Zone MRTs plus shared enclosure 16.667; and, after zeroing five Surface areas and manually rearming first-time state, Zone MAT fallbacks 20/25/28 and low-AE enclosure results 21.5/22 derived from Space MATs. The fixture does not assert Space Volume or which volume subbranch executes, and those expected outputs do not independently distinguish the strict volume-weighted branch from the arithmetic fallback; no test covers warnings, optional selection, cache staleness, exact thresholds, nonfinite/topology errors, failure, or retry. While `CalculateZoneMRTfirstTime` is true, CP168 allocates `SurfaceAE(TotSurfaces)` and `ZoneAESum(NumOfZones)`, zeros both plus every enclosure `sumAE`, then globally caches `Area * current Construction.InsideAbsorpThermal` only for numeric `HeatTransSurf` entries; positive-only Zone and radiant-enclosure index gates accumulate the same raw value without upper, finite, Construction, or topology validation. A partial first call still builds the global cache, which remains stale across later dimension/property/topology changes until HeatBalSurfMgr clear rearms the flag; only the successful routine tail clears it. A present raw Zone directly visits stored Spaces, zeroes each referenced enclosure `sumAET`, then sets `reCalcMRT` true; an absent argument sets every enclosure flag true without pre-zeroing. No source path writes those flags false, so a full call makes every later partial call recalculate all enclosures. Even a first partial call recomputes a marked shared enclosure from its complete `SurfacePtr` and writes every `spaceNums` MRT, including nonselected Zones. The selected-Zone loop uses strict cached `ZoneAESum > 0.01` and stored Space inclusive HT ranges: each `surfAET = SurfaceAE * SurfTempIn` enters the local numerator and unconditionally adds through the Surface's raw radiant-enclosure index, then `Zone.MRT = sumAET / ZoneAESum`. Exactly 0.01, negative, or NaN denominators take first-time Warning-plus-Continue then own-MAT fallback; later fallbacks are silent. Every true-flag enclosure uses strict `sumAE > 0.01`, resets `sumAET`, recomputes over complete `SurfacePtr`, and divides by `sumAE`, discarding prior Zone-loop additions. Low `sumAE` preserves `sumAET`, warns/continues only first-time, and computes stored-Space MAT from `sumMATVol/sumVol` when raw `sumVol > 0.01`, otherwise `sumMAT/(int)spaceNums.size()` with no empty or cast guard; either result is copied to every listed Space. Duplicate/raw lists and ranges repeat work, stale numerators can mismatch cached denominators, malformed 0-through-0 ranges enter index zero, and signs, exact thresholds, infinities, NaN, overflow, empty division, and invalid indices are unguarded. CP168 has no status, assertion, catch, rollback, cleanup, or local validation; allocations/cache writes, optional zero/flag pairs, Zone enclosure additions and MRTs, Warning/Continue effects, enclosure reset/add/MRT, and Space-copy prefixes can remain. A failed first-time retry rebuilds and rewarns but does not globally reset flags, `sumAET`, or prior MRT; later failures reuse stale caches and suppress fallback warnings. HeatBalSurfMgr explicitly owns/clears the two caches and flag, ViewFactorInfoData explicitly owns/clears enclosure sums/MRT/flags, and ZoneTempPredictorCorrectorData placement-new clears Zone/Space MRT; independent clears cannot replay the projection. Rust has bounded Surface area, inside thermal absorptance/temperature, and separate radiant helpers, but no Zone/Space/enclosure MRT state, cached weighting topology, recalculation flags, MAT fallback/diagnostic lifecycle, or CP168 analog. CP168 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 177 routines, split 58 state-mapped plus 119 source-mapped, with 67 required. CP169 adds non-required source-mapped `routine.report_non_representative_surface_results` immediately after `report_surface_heat_balance` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 151, implemented at `HeatBalanceSurfaceManager.cc` lines 6893-6930, and called in production only by `ReportSurfaceHeatBalance` line 6618 under `UseRepresentativeSurfaceCalculations`, immediately after `SolarShading::ReportSurfaceShading`; CP169 itself has no guard, so a direct call bypasses representative-mode selection. No unit test calls CP169 directly, and the seven direct unit calls to its parent leave representative mode false; the separate representative-topology fixture does not execute reporting. CP169 scans numeric Zones, stored Spaces, and each inclusive HT range, and every non-self target copies the representative's inside then outside `convClassRpt` integer without validation. After each Space's HT pass, `DisplayAdvancedReportVariables` independently gates its inclusive Window range. Every non-self Window computes `Surface(surfNum).Area / Surface(surfNum).Area`, so finite nonzero target areas including negative values yield one while zero, infinity, or NaN yields NaN; it then copies representative convective glazing gain, infrared glazing gain, and shortwave loss in that order through the raw ratio. It does not copy energies, frame/shade/gap terms, or whole Window heat gain and does not recompute any dependent report, so a false advanced gate and self, empty, inverted, or unreached ranges preserve prior values. This later report pass can overwrite CP167's earlier two glazing-component projections and adds the shortwave-loss projection only when advanced reporting is enabled. Raw Zone/Space/range/representative indices, class and membership, duplicates, chains, finiteness, and loop overflow are unchecked; 0-through-0 ranges enter index zero, duplicate or chained targets can consume earlier projections in traversal order, and all arithmetic follows IEEE behavior. CP169 has no allocation, child call, diagnostic, status, assertion, catch, rollback, cleanup, or local validation. Failure can leave complete earlier Spaces and earlier Surfaces within the current Space plus an inside-class-only HT prefix or one/two Window-component writes; coherent retry overwrites reached fields from current representative state but preserves skipped stale state and remains traversal-dependent for chains. SurfacesData alone owns all five targets and clears them by placement-new; Rust has no representative topology, convection-class report state, matching Window component lifecycle, advanced-report gate, or CP169 analog. CP169 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 178 routines, split 58 state-mapped plus 120 source-mapped, with 67 required. CP170 adds non-required source-mapped `routine.report_int_mov_ins_inside_surf_temp` immediately after `report_non_representative_surface_results` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 153, implemented at `HeatBalanceSurfaceManager.cc` lines 6932-6940, and called in production only by `ReportSurfaceHeatBalance` line 6741 under `AnyMovableInsulation`, after the parent Window reports and before opaque reporting. CP170 itself never reads that flag, so direct calls bypass the guard, while exterior-only movable insulation can make the parent call it even with an empty interior list. One direct unit fixture allocates two entries and calls CP170 once: a listed `present = false` Surface changes its prior report from 1.23 to the base `SurfTempIn` 23.0, and a listed `present = true` Surface changes 1.23 to `SurfTempInTmp` 12.3, both within 0.00001; it does not cover the parent gate, empty or malformed lists, duplicates, dimensions, nonfinite values, failure, or retry. Every entered call first assigns the complete `SurfTempIn` array to `SurfTempInMovInsRep`, then visits `intMovInsulSurfNums` in stored order and overwrites only raw indices whose `intMovInsuls(SurfNum).present` is true with `SurfTempInTmp(SurfNum)`. An empty list therefore leaves the full base copy; unlisted and listed-false entries keep the base value, and duplicates re-read current presence/input and repeat the same indexed overwrite. There is no arithmetic, clamp, finite filter, list repair, deduplication, bounds or dimension validation, child call, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. The first ObjexxFCL Array1D assignment is allocation-capable: a nonconformable target receives the source index range and is resized, which can allocate, deallocate, or reconstruct storage before element copy; a conformable target keeps its shape and overwrites all elements. Resize/allocation/range or copy failure reaches no list work and has no CP170 rollback guarantee; after a successful full copy, a bad raw index or input access can leave the complete base copy plus every earlier successful present override. A retry repeats the shape-conforming or shape-rebuilding assignment from current `SurfTempIn` and then reevaluates the current list, presence flags, and temporary temperatures, so a successful retry erases stale prior overrides for removed or now-false entries while remaining allocation- and raw-index-failure-prone. HeatBalSurfData alone owns the direct target and clears it by placement-new; normal setup dimensions it to `TotSurfaces` and BeginEnvironment zeroes reached entries, while SurfacesData separately owns/defaults/clears the gate, list, and insulation records. Rust has no movable-insulation topology/state, interior movable-insulation temperature report, conditional override pass, or matching failure/re-entry lifecycle. CP170 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 179 routines, split 58 state-mapped plus 121 source-mapped, with 67 required. CP171 remains immediately after `calc_heat_balance_outside_surf` in the Surface-manager definition ledger; CP175 now interposes `calc_outside_surf_temp` before CP171 in the heat-balance project execution list. It is declared at `HeatBalanceSurfaceManager.hh` line 173, implemented at `HeatBalanceSurfaceManager.cc` lines 7723-7736, and called in production only on the right-hand side of the `CalcHeatBalanceOutsideSurf` line-7717 common-tail assignment. The parent reaches that tail through Zone order, stored Space order, and inclusive HT ranges, but skips every Window, optional-zone-unrelated Surface, and line-7440 ExternalEnvironment EcoRoof; every other surviving boundary path reaches CP171. The helper binds raw `Surface(SurfNum)` and copies `OSCMPtr`; a strict positive pointer returns `-OSCM.HConv * (SurfOutsideTempHist(1) - OSCM.TConv)` and wins even when rain is true, ignoring `SurfHConvExt` and both weather temperatures. Zero or negative pointers fall through: rain uses `-SurfHConvExt * (SurfOutsideTempHist(1) - SurfOutWetBulbTemp)` and no rain uses the same expression with `SurfOutDryBulbTemp`. CP171 repeats no Area, class, boundary, wind, or parent-skip gate. One direct fixture makes exactly two default-zero-pointer calls: with surface history 6.71793958923051, wet bulb 6.66143784594778, coefficient 1000, and rain true it expects the wet formula within 0.01; after rain becomes false, coefficient becomes 5.65361106051348, and dry bulb is 7.2 it expects the dry formula within 0.01. The fixture's Area 58.197 is irrelevant. No test covers a positive OSCM pointer or its rain precedence, negative or invalid pointers, caller skips/store, boundary or wind independence, malformed indices/dimensions, nonfinite inputs, failure, or retry. The body is read-only and allocation-free, with no finite, sign, range, index, pointer, dimension, or topology validation, clamp, normalization, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. Raw negation, subtraction, multiplication, signed zero, infinities, NaN, overflow, and invalid-operation results follow underlying IEEE behavior, and a positive invalid pointer is indexed unchecked. Because the helper must return before the caller assignment, helper failure leaves the current `SurfQdotConvOutPerArea` old while prior parent effects for the current Surface and successful earlier-Surface writes persist. After a successful scalar return, an invalid or unallocated caller target access can still fail on the left-hand side and likewise leave the current target stale; a successful nonfinite result is stored only when that write succeeds. Direct retry rereads current inputs, while a full parent retry also repeats boundary work and overwrite-shaped storage without repairing skipped stale targets. HeatBalSurfData owns, dimensions, initializes, reports, and placement-new-clears the caller target; CP167 can later copy it to nonrepresentative Surfaces, WindowManager separately writes it for Windows, and reporting plus PlantPipingSystems consume the shared field. Rust's bounded `surface_exterior_report_terms_from_balance` has the same final `-coefficient * (outside_face_temperature - reference_temperature)` algebra and per-area report slot, with upstream typed wet-weather context, but no generic raw OSCM pointer and strict precedence, exact history-term lookup, full parent traversal and boundary topology, common-tail skips/store, or matching failure/re-entry lifecycle. CP171 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 180 routines, split 58 state-mapped plus 122 source-mapped, with 68 required. CP172 adds required source-mapped `routine.calc_heat_balance_inside_surf_2` between `calc_heat_balance_inside_surf` and the distinct optimized `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger; CP173 now follows the CTF-only child in that definition ledger and interposes `sum_surf_qdot_rad_hvac` between the wrapper and CP172 in the project execution list. It is declared at `HeatBalanceSurfaceManager.hh` lines 179-184, implemented at `HeatBalanceSurfaceManager.cc` lines 7815-8656, and called in production only by the canonical wrapper lines 7797 and 7809. An absent optional Zone reaches CP172 only when cached `AllCTF` is false and supplies the four global Surface lists; a present Zone always reaches CP172 with the four raw Zone lists regardless of `AllCTF`. Those vectors independently govern reference-air/history, interzone, non-window, and Window work without topology validation, while the optional Zone is forwarded only to interior-radiation and convection-coefficient children. The prologue skips TDD domes only in reference-air setup, zeros exactly 13 selected fields for every supplied Window, resets the global iteration count, samples the global scheduled inside-source list, and conditionally advances all Kiva instances. Each at-least-one convergence pass takes a whole-array inside-temperature snapshot, temporarily substitutes every Kiva map entry with `Trad - Kelvin` around interior radiation, restores from the snapshot, refreshes convection at pre-increment counts 30 through 480 on a maximum-length run, executes EMPD/HAMT setup and the representative-filtered non-window algorithm branches, runs TDD diffusers every pass but regular Windows only on pass one, commits histories/TDD/interzone updates, and increments the count. The post-increment `> 500` check therefore warns outside warmup and breaks after pass 501 even if that pass first met convergence; global `AnyCondFD` controls tolerance and relaxation. The EMPD/HAMT tail globally zeros all Zone moisture sums and repopulates only the supplied non-window subset. All five literal unit calls to the wrapper omit Zone and retain cached `AllCTF = true`, including the fixture that later changes one Surface algorithm to Kiva, so they dispatch to the CTF-only child; CP172 has no direct or known exercised unit path. Raw vectors, duplicates, indices, algorithms, dimensions, arithmetic, Kiva map insertion, child failures, and diagnostics are unchecked locally; there is no status, catch, rollback, or cleanup. Failure can leave selected-window resets, global snapshots and sources, Kiva substitutions or partial restores, solve/history/coefficient prefixes, diagnostics, relaxation, and globally zeroed or partly rebuilt moisture sums, while retry resets only the early selected state and iteration count and otherwise inherits the partial graph. HeatBalSurfMgr, HeatBalanceData, SurfacesData, HeatBalSurfData, HeatBalFanSysData, MoistureBalanceData, ZoneTempPredictorCorrectorData, SurfaceGeometryData, DataTimingsData, and the called algorithm managers separately own this state; independent clears cannot replay the routine. Rust has bounded inside-pass scaffolds and a distinct CTF-only mapping but no CP172 general solver, raw four-list/partial topology, non-CTF/window/Kiva/moisture lifecycle, 501-pass behavior, or matching failure/re-entry semantics. CP172 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 181 routines, split 58 state-mapped plus 123 source-mapped, with 69 required. CP173 adds required source-mapped `routine.sum_surf_qdot_rad_hvac` after `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger and after `calc_heat_balance_inside_surf` before `calc_heat_balance_inside_surf_2` in the heat-balance project contract. It is declared at `HeatBalanceSurfaceManager.hh` line 171, implemented at `HeatBalanceSurfaceManager.cc` lines 9277-9285, and called in production only by the canonical wrapper line 7788 before full, optimized, or partial dispatch. Every call, including optional-Zone resimulation, traverses the complete raw `allGetsRadiantHeatSurfaceList`. Five equipment input paths append nonzero Surface pointers without sorting or deduplication. For each stored index, CP173 binds `surfQRadFromHVAC` and overwrites `SurfQdotRadHVACInPerArea` with the exact left-associated `HTRadSys + HWBaseboard + SteamBaseboard + ElecBaseboard + CoolingPanel` sum. An empty list and unlisted targets preserve stale values; duplicates repeat the same overwrite rather than accumulating again. All five literal wrapper calls in `HeatBalanceSurfaceManager.unit.cc` leave the raw list default-empty and exercise only CP173's no-op. Four `SizingManager.unit.cc` `ManageSimulation` fixtures beginning at lines 2465, 2929, 3393, and 3818 include electric radiant baseboard input, populate the list, and indirectly execute CP173, but assert only downstream sizing; no test calls CP173 directly, observes the aggregate, or isolates arithmetic, order, duplicates, failure, or lifecycle. The routine has no validation, diagnostic, allocation, catch, rollback, cleanup, or latch; raw indices and IEEE addition are unchecked. Source binding precedes the assignment, whose complete right-hand side precedes target lookup and store, so failure can retain an earlier-list write prefix and leave the current target old; retry rereads the current list and component record without clearing skipped state. SurfacesData owns the raw list, HeatBalFanSysData owns the component records, and HeatBalSurfData owns the aggregate; their independent placement-new clears and initialization/distribution resets do not form a CP173 transaction. Rust has a bounded `inside_radiant_hvac_w_per_m2` destination consumed by its surface balance and reports, but no five-source producer, global list, or matching lifecycle. CP173 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 182 routines, split 58 `state_mapped` plus 124 `source_mapped`, with 70 required. CP175 adds required source-mapped `routine.calc_outside_surf_temp` after `test_surf_temp_calc_heat_balance_inside_surf` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, and after `calc_heat_balance_outside_surf` before `get_qdot_conv_out_per_area` in the heat-balance project contract. `CalcOutsideSurfTemp` is declared at `HeatBalanceSurfaceManager.hh` lines 195-202 and implemented at `HeatBalanceSurfaceManager.cc` lines 9470-9763. Its only direct production calls are the parent’s `OtherSideCoefCalcExt` CTF/EMPD path at line 7355, `OtherSideCondModeledExt` CTF/EMPD path at line 7419 after any exterior-vented-cavity update, and `ExternalEnvironment` CTF/EMPD-or-TDD path at line 7626; the first two retain zero movable insulation, while the exterior path alone samples exterior-insulation state. The reusable parent supplies complete or optional-Zone traversal, skips Windows, unrelated Surfaces, EcoRoof, and ineligible boundary/algorithm routes, then fatals immediately when CP175 sets its shared flag, before the CP171 common-tail convection store. CP175 uses strict `HMovInsul > 0.0` and `CTFCross[0] > 0.01` decisions and computes quick coupling as `CTFCross[0] / (CTFInside[0] + SurfHConvInt)`. Sky and ground start from global sky and outdoor dry-bulb temperatures; optional surrounding-property schedules override them, the Surface supplies its precomputed surrounding temperature, and the ground-surface property then wins over any scheduled ground value. A TDD dome resolves raw pipe, diffuser, and diffuser-Space topology, uses `Ueff = 1 / Reff`, and couples dome exterior forcing to diffuser shortwave, radiant, convection, and MAT terms without embedded-source terms. The remaining exhaustive paths separate no-movable slow/quick and movable slow/quick conduction. With no movable insulation, `OSCMPtr == 0` uses exterior shortwave, convection, air/sky/ground/surrounding radiation, additional source, and optional outside embedded-source forcing; quick conduction substitutes the coupled inside balance and denominator correction, but its no-OSCM source branch notably omits the inside embedded-source term. Any nonzero OSCM pointer, including a malformed negative value, indexes raw `TRad`/`HRad`, drops the separated solar/air/sky/ground/surrounding forcing, and its quick source branch includes both outside and coupled inside embedded-source terms. Movable paths ignore OSCM radiation, use `F2 = HMovInsul / (HMovInsul + HConvExt + HAirExt + HSkyExt + HGrdExt + HSrdSurfExt)`, couple exterior forcing through F2, and include no embedded-source terms because that combination is rejected later. After writing `SurfOutsideTempHist(1)`, CP175 overwrites `SurfQdotRadOutRepPerArea` with negative sky, air-radiation, ground, and surrounding exchanges while excluding exterior convection and the parent’s nonlinear surrounding flux. With an embedded source plus movable insulation, those two writes precede four diagnostics, raw insulation-material lookup, `ErrorFlag = true`, and return; radiant-system coefficients remain stale and a successful call never clears a preexisting flag. With an embedded source and no movable insulation, constant, inside-temperature, and source coefficients are written in order; the divisor preserves the source anomaly that `SurfHSrdSurfExt` appears twice, and the constant coefficient omits the additional outside source. A non-source call does not clear old coefficients. The one direct fixture covers only a non-OSCM Wall with positive insulation, zero-cross slow conduction, and invalid embedded source; it checks the flag and four diagnostics but not independent temperature or radiation numerics. Four direct CTF parent fixtures incidentally reach CP175 but assert only parent convection, surrounding/ground, or scheduled-source state, while a fifth CondFD fixture does not call it. No focused test covers valid slow/quick equations, exact 0/0.01 thresholds, OSCM, TDD, temperature precedence, coefficient anomalies, an input-true flag, malformed topology, nonfinite arithmetic, failure prefixes, or retry. There is no finite, denominator, index, dimension, topology, schedule, property, TDD, Space, or material validation, catch, rollback, cleanup, or local latch. Threshold equality and NaN select absent/slow, raw floating-point exceptional results propagate, and failure can preserve no write, only outside history, history plus radiation report, a diagnostic prefix with the flag still false, or a one/two-coefficient prefix. Direct retry rereads live inputs but does not clear a caller-supplied flag or untouched coefficients; a fresh parent call reinitializes its local flag but repeats earlier parent work. HeatBalSurfData owns the temperature history and radiant report, HeatBalFanSysData owns the three coefficients, the parent stack owns the shared flag, and the Surface, Construction, environment, schedule, daylighting, Space/MAT, material, and error owners reset independently rather than transactionally. Rust has bounded slow/quick typed Outdoors CTF and report helpers, but epsilon fallbacks, clamps, shortcuts, and a collapsed environment do not implement CP175’s TDD, OSCM, movable insulation, property precedence, embedded-source coefficient/error lifecycle, exact reports, raw topology, or failure/re-entry behavior, so no Rust target, support, numerical, or conformance promotion follows. The inventory becomes 32 algorithms and 184 routines, split 58 `state_mapped` plus 126 `source_mapped`, with 72 required. CP176 adds non-required source-mapped `routine.calc_exterior_vented_cavity` immediately after `calc_outside_surf_temp` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `CalcExteriorVentedCavity` is declared at `HeatBalanceSurfaceManager.hh` line 204 and implemented at `HeatBalanceSurfaceManager.cc` lines 9765-9850. Its only direct production calls are the `CalcHeatBalanceOutsideSurf` `OtherSideCondModeledExt` CTF/EMPD path at line 7416 and CondFD/HAMT path at line 7427, both guarded by the trigger Surface's `SurfExtCavityPresent`. Parent Window and optional-Zone skips, every other boundary, and every other heat-transfer algorithm cannot trigger CP176, but a reached cavity forwards its complete raw `SurfPtrs` group without Window or Zone filtering. Before either call, the parent applies EMS overrides to the trigger Surface's OSCM and snapshots its existing `TConv/HConv` into local `TempExt`, `SurfHConvExt`, and any CondFD/HAMT moisture boundary. CP176 writes the cavity's separate raw `OSCMPtr`, which neither the wrapper nor input loader checks against each Surface's `OSCMPtr`. The CTF/EMPD path then calls CP175 with the pre-CP176 convective snapshot but, when both pointers match, CP176's new live `TRad/HRad`; common-tail CP171 likewise reads the new `TConv/HConv` only when those pointers match. CP176 obtains `CavNum` from the trigger Surface, computes trigger humidity ratio and density from that Surface's dry/wet bulb and global pressure, forms `holeArea = ActualArea * Porosity` and `AspRat = 2 * HdeltaNPL / PlenGapThick`, and seeds temporary baffle and cavity-air temperatures from `TbaffleLast/TairLast`. It then calls deferred dependency `TranspiredCollector::CalcPassiveExteriorBaffleGap` exactly three times with no convergence check, carrying each pass's two updated temperatures into the next. The child area-weights weather, wind, current outside history, incident solar, and heat-transfer coefficients over the whole raw group, invokes exterior-convection setup for every member, applies rain and optional ICS branches, and returns the final gap coefficients, incident term, total mass flow, and wind/buoyancy volume flows. After all three passes return, CP176 stores `Isc`, `TAirCav`, `Tbaffle`, `HrPlen`, `HcPlen`, `PassiveACH = (MdotVent / rho_trigger) / (ProjArea * PlenGapThick) * 3600`, total/wind/buoyancy passive mass reports, both last-temperature seeds, then OSCM `TConv/HConv/TRad/HRad` in that order; it never writes the separate cavity `MdotVent` member. The child total mass flow uses group-weather density, while ACH and the two component mass reports use trigger-Surface density, so component sums and the total can diverge when member weather differs. There is no cavity-number deduplication: each eligible member trigger recomputes the complete group through three more passes and immediately advances shared last temperatures, making full and optional-Zone traversal order visible and a successful direct retry non-idempotent. Input accepts duplicate Surface references within and across cavity objects: repeated raw entries are weighted repeatedly, a later cavity overwrites a Surface's trigger cavity number while an earlier raw group retains it, and the three-times-N call count uses distinct traversed trigger Surfaces rather than duplicate list entries. No test calls CP176 or enables its parent gate. One `ICSCollector.unit.cc` fixture calls the shared child only once and checks baffle temperature plus Hc, Hr, and total mass flow, not the wrapper loop, air temperature, report/history/OSCM stores, density split, multi-member order, or retry. CP176 has no local index, dimension, topology, pointer-consistency, finite, sign, range, or denominator validation, diagnostic, status, convergence test, catch, rollback, cleanup, cache, or latch. Psychrometric and convection dependencies may diagnose or mutate their own state; a failure before or within the three child passes leaves wrapper-owned cavity results, both seeds, and OSCM fields old but can retain dependency effects. After all three child calls return, straight-line non-trapping scalar work commits every cavity/report field and both seeds before the first new raw-index risk at the initial OSCM access; an invalid pointer leaves all four OSCM fields old, while a valid stable index lets all four stores complete. Retry therefore starts from either both old or both newly committed seeds, never a CP176-created split pair. `HeatBalanceData` owns the cavity topology, inputs, results, reports, and seeds; its constructor starts the two seeds at 20 C and explicitly initializes the listed result/report/source scalars at zero, but omits the separate never-written `MdotVent` member and leaves it indeterminate. BeginEnvironment initialization resets only the two seeds to 20 C. `SurfacesData` separately owns membership, OSCM/EMS state, and member weather/geometry; BeginEnvironment resets each OSCM to 20 C and 4 W/m2-K but leaves cavity results and PV-fed `QdotSource` stale until recomputation. General-routines ICS state, solar collectors, PV, construction/material, convection, environment, outside histories, and incident solar have independent owners and clears. Rust collapses `OtherSideConditionsModel` to `OutsideBoundaryCondition::Other`, excludes it from the exterior list, and preserves the existing outside-face temperature; it has no cavity object/group, OSCM state, passive-gap/Nusselt/flow solve, three-pass seeds, ICS/PV coupling, six registered outputs, or matching failure/re-entry lifecycle. CP176 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 185 routines, split 58 `state_mapped` plus 127 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP177 adds non-required source-mapped `routine.gather_component_loads_surf_abs_fact` immediately after `calc_exterior_vented_cavity` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GatherComponentLoadsSurfAbsFact` is declared at `HeatBalanceSurfaceManager.hh` line 206 and implemented at `HeatBalanceSurfaceManager.cc` lines 9852-9880. Its only direct production call is `InitSurfaceHeatBalance` line 614 under the caller's `ZoneSizingCalc` gate; the body additionally requires `CompLoadReportIsReq && !isPulseZoneSizing` and checks neither `WarmupFlag`, `KickOffSizing`, `doLoadComponentPulseNow`, nor the parent first-time flag. That sole call can be reached through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; if the effective gates remain true, either path overwrites the same current slot and the last successful call wins. On an ordinary component-report sizing path, the preceding `ManageInternalHeatGains(state, false)` call lazily allocates zero-initialized `surfCompLoads` storage for `(TotDesDays + TotRunDesPersDays) * (24 * TimeStepsInHour) * TotSurfaces` records. CP177 computes `TimeStepInDay = (HourOfDay - 1) * TimeStepsInHour + TimeStep`, selects zero-based day and timestep slots from one-based `CurOverallSimDay` and `TimeStepInDay`, then traverses numeric Surfaces 1 through `TotSurfaces`. It skips exactly when `!HeatTransSurf` or `Zone == 0`, then skips `TDD_Dome`; a malformed negative Zone passes, and there is no other class, Space, representative, list, or deduplication filter, so eligible Windows and TDD diffusers are included. For every reached Surface it first overwrites `ITABSFseq` with `SurfAbsThermalInt`, then overwrites `TMULTseq` with `EnclRadInfo(RadEnclIndex).radThermAbsMult`. It copies the two live factors separately without multiplying, clamping, normalizing, recomputing, or clearing omitted records; a false gate or newly skipped Surface preserves the prior or default slot. Because there is no kickoff or warmup guard, ordinary `SetupZoneSizing` can write timestep 1 twice and the final timestep once per accepted environment before the sizing iterations, the pulse iteration writes neither field, and nonpulse warmup and normal calls overwrite whatever slots they reach. An unchanged successful retry is overwrite-idempotent. `OutputReportTabular::GetDelaySequences` is the only source reader of these fields and later forms `TMULTseq * ITABSFseq * Surface.Area * decayCurve`; the snapshot feeds Zone, Space, AirLoop, and Facility component-load tabular reporting but never feeds the heat-balance solver. No test calls CP177 directly. The upstream CP161 test checks only absorptance 0.2 and multiplier 5, the focused delay-sequence test manually seeds both fields, and full sizing/report fixtures exercise the chain without asserting the CP177 pair, gates, filters, or write order. CP177 performs no allocation, finite, sign, range, index, dimension, topology, enclosure-membership, or time-coordinate validation and has no diagnostic, status, assertion, catch, rollback, cleanup, or local latch. Invalid day, timestep, destination-Surface, or enclosure indexing is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build, a diagnosed day or timestep lookup failure precedes every Surface store; after traversal begins an earlier-Surface prefix may remain, and an invalid `RadEnclIndex` can be diagnosed after `ITABSFseq` commits but before `TMULTseq`, leaving a split new/old pair. A successful retry overwrites only currently reached eligible records and does not repair skipped stale cells. `OutputReportTabularData` owns the destination and clears it plus rearms its allocation latch on `clear_state`; normal post-sizing deallocation removes only pulse arrays and retains `surfCompLoads` for report consumption. `HeatBalSurfData`, `ViewFactorInfoData`, `SurfacesData`, `SizingData`, and global timing/gate state own the inputs independently, so their resets do not form a CP177 transaction. In a bounds-checked execution where an indexing failure propagates, the parent does not reach the following initialization-complete display or first-time-flag clear, and prior parent effects plus any committed CP177 prefix remain observable if control recovers; release-build undefined behavior provides no corresponding guarantee. Rust's retained inside thermal absorptance and transient typed-Zone radiant-gain normalization are adjacent primitives only. Rust has no sizing/design-day component-load lifecycle, radiant-enclosure multiplier state, day/timestep/Surface ITABSF/TMULT storage, delay-report consumer, or matching filtering, failure-prefix, reset, and re-entry behavior. CP177 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 186 routines, split 58 `state_mapped` plus 128 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP178 adds non-required source-mapped `routine.get_surf_incident_solar_multiplier` immediately after `gather_component_loads_surf_abs_fact` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurfIncidentSolarMultiplier` is declared at `HeatBalanceSurfaceManager.hh` line 208 and implemented at `HeatBalanceSurfaceManager.cc` lines 9882-9890. Its only direct production call is the numeric all-Surface loop at `InitSolarHeatGains` line 2820, reached only when `currSolRadPositive` is true: `SunIsUp` and the signed `BeamSolarRad + GndSolarRad + DifSolarRad` sum is greater than zero. The unconditional line-457 parent call can arrive through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; no first-time, warmup, kickoff, sizing, pulse, class, heat-transfer, exterior-solar, representative, or deduplication filter narrows CP178 after the positive-solar branch is entered. The `resetSolar` condition alone does not call it: sunset and sun-up/no-radiation paths skip CP178, and BeginEnvironment skips it only when the independent `currSolRadPositive` gate is false; a positive-solar BeginEnvironment call refreshes every Surface cache, while skipped calls preserve prior caches. For each numeric Surface, CP178 first reads same-index `Surface.hasIncSolMultiplier`. False returns exact 1.0 without touching the optional record vector. True with a nonnull same-index schedule returns `sched->getCurrentVal() * Scaler`, where the schedule getter selects `EMSVal` when actuated and `currentVal` otherwise; true with a null schedule returns `Scaler`. CP178 does not clamp, normalize, validate, cache, diagnose, or consult the record `Name`, `SurfaceIdx`, total-object count, prior cached value, Surface class, boundary, exposure, construction, shading, or any typed list. The caller stores a completed return into same-index `Surface.IncSolMultiplier` before downstream sky/ground, beam/diffuse, obstruction and reflected-solar contributions, TDD, shelf, opaque/window, frame/divider, daylighting, interior-distribution, solver, and report consumers. No test calls CP178 directly. The focused Surface-manager fixture manually sets `hasIncSolMultiplier`, a null schedule, and scalers 0.5 then 1.0, calls the parent twice under positive solar, and proves only an exact half-versus-whole transmitted-solar result; input tests inspect scaler and schedule state but never assert the scheduled product, default branch, EMS precedence, solar gate, failure prefix, or retry. CP178 has no finite, range, index, dimension, alignment, pointer, or lifecycle validation and no diagnostic, status, allocation, catch, rollback, cleanup, or latch. Invalid vector or pointer access is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build the helper itself has no writes; if a lookup failure propagates during the caller loop, only earlier Surface cache stores can remain while the current and later caches stay old. IEEE nonfinite results are returned and stored rather than diagnosed. A successful positive-solar retry re-reads live flags, scaler, and schedule or EMS state and overwrites every numeric Surface; a retry whose solar gate is false preserves any prior cache. `SurfacesData` owns the flag, same-index record vector, and cached multiplier; the Surface constructor defaults the flag and cache to false and 1.0, record defaults are scaler 1.0 and null schedule, placement-new `clear_state` replaces the visible owner with empty Surface and record vectors, and subsequently allocated entries receive those defaults, while BeginEnvironment does not reset an existing cache. `ScheduleManagerData` separately owns and deletes schedule objects, while `EnvironmentData` owns the solar gate and CP159 mutates preceding state across several owners, so independent clears and a failed parent are not a CP178 transaction. Rust retains only the CP99 immutable request snapshot with an unresolved fenestration target, bounded scalar, and optional `ScheduleId`; every definition remains run-blocked. Rust has no fenestration binding, per-Surface flag or cache, same-index record alignment, request-specific current or EMS schedule sampling, positive-solar traversal, or matching window, TDD, shelf, reflection, distribution, solver, and reporting consumers. Its request-free opaque incident-solar diagnostic and generic schedule infrastructure do not implement CP178. CP178 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 187 routines, split 58 `state_mapped` plus 129 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP179 adds non-required source-mapped `routine.init_surface_property_view_factors` immediately after `get_surf_incident_solar_multiplier` and before `update_final_surface_heat_balance`, with no project-contract entry. `InitSurfacePropertyViewFactors` is declared at `HeatBalanceSurfaceManager.hh` line 210, implemented at `HeatBalanceSurfaceManager.cc` lines 9893-9993, and called only by `InitHeatBalance` line 2623 inside its `BeginSimFlag` block after array allocation and optional CTF initialization but before Window, daylighting, and solar initialization. The body quick-returns unless `AnyLocalEnvironmentsInModel` and the separately owned `InitSurfaceHeatBalancefirstTime` latch are both true, then visits every numeric Surface whose surrounding or ground property flag is set. It sums a strictly positive stored sky value, every enumerated surrounding factor, and the selected ground value, with a separate GroundSurfaces object taking precedence over a SurroundingSurfaces ground field; a total above one emits severe/continue diagnostics but does not stop, clamp, or skip mutation. Both-set, sky-only, ground-only, and neither-set branches overwrite or scale Surface sky/ground IR factors, backfill property values and presence flags, and conditionally invoke the separately CP182-mapped `ReSetGroundSurfacesViewFactor` child, which replaces the ground-object sum and either disables zero-factor ground use or writes the whole inferred factor into its first entry. Shared property records make numeric Surface alias order observable, and a sky-only SurroundingSurfaces path without a separate ground object can alternate its inferred ground value across direct retries because the value is backfilled without setting its presence flag. Focused fixtures cover the four ordinary branches and selected long-wave consumers but not both quick returns, production latch order, shared aliases, sum-above-one continuation, malformed topology, failure prefixes, or retry. There is no local finite/index/topology validation, status, catch, rollback, cleanup, transaction, or latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface prefix and leave the later first-time clear unreached. Rust ordinary ground/sky complement helpers and run-blocked Zone local-environment records do not implement the Surface local-environment property arenas, mutable flags/factors, diagnostics, reset child, alias order, or lifecycle. CP179 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 188 routines, split 58 `state_mapped` plus 130 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP180 adds non-required source-mapped `routine.get_ground_surfaces_temperature_average` immediately after `init_surface_property_view_factors` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 212 and implemented at `HeatBalanceSurfaceManager.cc` lines 9995-10035. Its only direct production call is the unconditional first child of `CalcHeatBalanceOutsideSurf` at line 7016, before the separately CP183-mapped surrounding-temperature child, internal-source transfer, optional-Zone filter, and every outside-Surface calculation. All full-model and zone-limited parent invocations, including normal Surface management, final-radiant recalculation, SimulationManager resimulation, and radiant/HVAC retries, therefore enter CP180 before the optional-Zone filter; when `AnyLocalEnvironmentsInModel` passes, CP180 traverses the complete numeric Surface array. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state. Otherwise CP180 filters only `IsSurfPropertyGndSurfacesDefined`, follows the Surface property index, and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfTemp` while preserving the prior shared average. For a nonzero sum it skips exact-zero view factors and null temperature schedules; every other entry samples the live schedule with EMS precedence and contributes `view_factor * (temperature + Kelvin)^4`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsTempAvg` with zero and then disables only the current Surface use flag; otherwise CP180 stores `root_4(numerator / SurfsViewFactorSum) - Kelvin` and never enables a false use flag. Shared-property aliases on a nonzero-sum path repeat the schedule reads and shared average store in numeric Surface order while use flags remain per Surface. Stable successful inputs are numerically overwrite-idempotent, but either zero branch latches an affected use flag false until Surface-property input relinking restores it, and later nonzero schedules can refresh the report average without restoring solver consumption. One focused unit fixture makes four direct calls and checks a three-entry radiative average plus each one-entry selection; one outside-long-wave fixture indirectly consumes the ordinary average. They do not cover the global return, parent/optional-Zone order, zero-sum or zero-numerator stores, missing schedules, EMS values, aliases, use-flag latching, malformed topology, failure prefixes, or retry. Beyond its explicit global/property/equality/null checks, CP180 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust ordinary ground temperature, schedule, long-wave, and view-factor primitives do not implement the SurfaceProperty ground list, shared radiative-temperature average, per-Surface use flag, complete-Surface zone-resimulation order, output-variable binding, or matching failure/re-entry lifecycle. CP180 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 189 routines, split 58 `state_mapped` plus 131 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP181 adds non-required source-mapped `routine.get_ground_surfaces_reflectance_average` immediately after `get_ground_surfaces_temperature_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesReflectanceAverage` is declared at `HeatBalanceSurfaceManager.hh` line 214 and implemented at `HeatBalanceSurfaceManager.cc` lines 10037-10072. Its only direct production call is unconditional `InitSurfaceHeatBalance` line 395, after variable absorptance, optional environment-history and movable-insulation work but before complex-fenestration, anisotropic-sky, shading, radiation, daylighting, and solar-gain initialization. Both normal Surface management and `SimulationManager::Resimulate` under `ResimHB` reach it; no first-time, BeginEnvironment, day, hour, timestep, warmup, sizing, solar, Surface-class, or boundary gate narrows CP181 itself. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state; when that gate passes, CP181 traverses the complete numeric Surface array and filters only `IsSurfPropertyGndSurfacesDefined`. It follows the Surface property index and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfRefl` while preserving the prior shared average. For a nonzero sum it traverses every stored ground entry and skips only null reflectance schedules: even an exact-zero view factor still samples a nonnull schedule. Every sampled schedule uses EMS precedence and contributes `view_factor * current_reflectance`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsReflAvg` with zero and then disables only the current Surface use flag; otherwise CP181 stores `numerator / SurfsViewFactorSum` and never enables a false flag. Thus a valid zero-reflectance schedule selects the disable branch, and later solar consumers use their environment fallback rather than retaining a custom zero. Shared-property aliases on a nonzero-sum path repeat schedule reads and the shared store in numeric Surface order while use flags remain per Surface. Stable successful inputs are overwrite-idempotent, but either zero branch latches an affected flag false until Surface-property input relinking restores it; later nonzero schedules can refresh the output average without restoring custom solar consumption. One focused unit fixture makes four direct calls and checks a three-entry weighted average plus each one-entry result. One long-wave fixture incidentally executes the all-null-schedule disable path but asserts no reflectance state, and no test connects the average to its solar or output consumers. Missing coverage includes the global return, zero-sum stale average, partial-null denominator rule, exact-zero schedule meaning, EMS values, aliases, flag latching, malformed topology, failure prefixes, and retry. Beyond its explicit global/property/equality/null checks, CP181 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust's generic schedules, ordinary ground view factor, and fixed 0.2 ground-reflectance solar path do not implement the SurfaceProperty ground list, scheduled shared average, per-Surface use flag, EMS sampling, fallback transition, alias order, output binding, or matching failure/re-entry lifecycle. CP181 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 190 routines, split 58 `state_mapped` plus 132 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP182 adds non-required source-mapped `routine.re_set_ground_surfaces_view_factor` immediately after `get_ground_surfaces_reflectance_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `ReSetGroundSurfacesViewFactor` is declared at `HeatBalanceSurfaceManager.hh` line 216, implemented at `HeatBalanceSurfaceManager.cc` lines 10074-10092, and called in production only by `InitSurfacePropertyViewFactors` line 9989. CP182 itself has no global or first-time gate: its sole quick return tests the current Surface ground-property flag. The production parent supplies broader `AnyLocalEnvironmentsInModel` and `InitSurfaceHeatBalancefirstTime` gates and calls CP182 only for a numeric property-bearing Surface whose sky-only or neither-set branch inferred a ground factor while a separate GroundSurfaces object exists; before calling, the parent overwrites `Surface.ViewFactorGroundIR` and marks the shared property's `IsGroundViewFactorSet` true. CP182 follows the Surface property index and first overwrites shared `SurfsViewFactorSum` with that current Surface ground IR factor. An exact zero, including negative zero, then clears the current Surface reflectance-use flag before its temperature-use flag and returns without touching any entry. Every nonzero value, including negative, NaN, or infinity, is copied as the whole sum into only `GndSurfs(1).ViewFactor`. The helper does not validate or clamp the value, clear later entries, change either shared average, change `Surface.ViewFactorGroundIR`, alter entry metadata or count, write `IsGroundViewFactorSet`, or enable a false use flag. Consequently nonzero-to-zero direct retry leaves the old first entry behind, zero-to-nonzero retry cannot restore the two flags, and a pre-mutated multi-entry property can retain an entry total inconsistent with the stored sum. Shared properties make order observable: in the ordinary parent loop the first blank-property alias marks the object set and later aliases normally consume that state without re-entering CP182, while every standalone call overwrites the shared sum, only nonzero calls overwrite the first entry, and a zero call preserves that entry while disabling only its own Surface flags. CP181 reflectance and CP180 temperature averaging later consume the current shared denominator and entries; a zero CP182 branch selects their solver fallbacks through the Surface flags while leaving report averages stale until separately recomputed. No test calls CP182 directly. Four parent fixtures reach it for six Surfaces; two fixtures assert the positive first-entry backfill for four of them, while the other two assert only parent or downstream results. The property quick return, exact-zero path and flag-write order, sum assignment, stale-first-entry and sticky-flag retries, aliases, malformed values/topology, and failure prefix remain untested. CP182 has no finite, sign, range, index, allocation, count, topology, alias, or lifecycle validation and emits no diagnostic, status, catch, rollback, cleanup, transaction, or latch. In a bounds-checked failure, an invalid Surface or property index precedes the shared-sum write, whereas a missing first entry on the nonzero path can fail after that sum commits; the zero path never accesses the first entry and can still clear both flags. Release unchecked malformed access can be undefined, and a failure reached from the parent retains its earlier Surface-factor and set-flag writes. Rust has no SurfaceProperty ground arena, shared sum/first-entry mutation, per-Surface use flags, parent branch/latch behavior, alias semantics, or matching failure/re-entry lifecycle; ordinary ground/sky factors, fixed ground reflectance, schedules, and run-blocked Zone local-environment state are adjacent primitives only. CP182 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 191 routines, split 58 `state_mapped` plus 133 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP183 adds non-required source-mapped `routine.get_surrounding_surfaces_temperature_average` immediately after `re_set_ground_surfaces_view_factor` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurroundingSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 218, implemented at `HeatBalanceSurfaceManager.cc` lines 10094-10118, and is the final routine before those namespaces close. Its sole direct production call is unconditional `CalcHeatBalanceOutsideSurf` line 7019, after CP180 ground-temperature averaging and before internal-source transfer, optional-`ZoneToResimulate` handling, and every outside-Surface calculation. A CP180 failure prevents CP183, but every parent call that passes that sibling enters CP183 before any full-model/Zone filter; even a Zone-limited radiant or HVAC retry can refresh every eligible allocated Surface. CP183 first returns when `AnyLocalEnvironmentsInModel` is false, preserving every prior average. Otherwise it range-traverses the complete allocated Surface vector and skips only records whose `SurfHasSurroundingSurfProperty` flag is false. For each eligible Surface it follows `SurfSurroundingSurfacesNum`, zeros a local sum, visits entries 1 through `TotSurroundingSurface` in stored order, unconditionally dereferences every temperature schedule even for an exact-zero factor, samples current or EMS-actuated value, adds Kelvin, and accumulates `ViewFactor * temperature^4`. It then divides by the Surface-local cached `ViewFactorSrdSurfs` rather than the property's live `SurfsViewFactorSum`, takes the fourth root, subtracts Kelvin, and performs the routine's only persistent write to that Surface's `SrdSurfTemp`. CP183 does not clear, disable, clamp, renormalize, diagnose, or mutate the property, entry list, factors, denominator cache, presence flag, schedules, sky/ground fields, or any shared average. Normal input processing requires at least one complete entry and a resolved schedule, constrains each factor to [0,1], accumulates the property sum, copies it into the Surface cache, and clears the presence flag on an exact-zero sum, but does not constrain the aggregate to one; none of those dependency guarantees is revalidated here. A schedule of exactly -273.15 C yields -273.15 C rather than a disable branch, temperatures below absolute zero lose their Kelvin sign through the fourth power, and a zero-factor NaN or infinity still poisons the sum. Nonfinite inputs, overflow, zero or mismatched denominators, and a negative quotient can store NaN or infinity without a diagnostic. Shared-property aliases repeat every schedule read and write separate Surface averages; matching cached denominators produce the same result, while independently mutated caches can diverge. Stable inputs are overwrite-idempotent, schedule or EMS changes appear on the next call, and false global/property gates preserve stale values. Three direct fixtures make three calls, write four property-bearing Surfaces, and traverse eight entries: two directly prove mixed-temperature fourth-power means, while one proves a single-entry result only through Window long-wave output. Five direct-parent fixtures add a global return, a per-Surface skip, and nine eligible writes; only the CondFD fixture genuinely checks an average-derived coefficient, while other assertions can use the parent's separate raw-entry long-wave loop. No test covers optional-Zone all-Surface refresh, gate-preservation sentinels, aliases, retries after schedule change, cached-denominator mismatch, zero/negative/nonfinite arithmetic, malformed topology, or failure prefixes. CP183 has no finite, sign, range, index, allocation, count, pointer, topology, alias, or lifecycle validation and no status, diagnostic, catch, rollback, cleanup, transaction, or latch. In a bounds-checked or otherwise diagnosed malformed-access failure, an invalid property, entry, or schedule can leave the current Surface old because its sole store follows the complete inner loop, while earlier eligible Surfaces and preceding CP180 writes remain committed; unchecked release access can be undefined. Downstream CTF/CondFD exterior balances, surrounding-radiation coefficients, and ordinary, complex, and equivalent-layer Window paths consume `SrdSurfTemp`, while the separate outside-Surface raw surrounding-entry radiation sum does not. Rust has no typed Surface surrounding-property arena, entry list, denominator cache, per-Surface radiative average, current/EMS property binding, surrounding long-wave term, parent traversal, alias order, or matching failure/re-entry lifecycle; generic schedules, ordinary sky/air/ground long-wave terms, and run-blocked Zone local-environment state are adjacent primitives only. CP183 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 192 routines, split 58 `state_mapped` plus 134 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP184 adds required source-mapped `routine.get_air_heat_balance_input` immediately after `routine.manage_air_heat_balance` in the Air-manager definition ledger and adds `get_air_heat_balance_input` after `manage_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirHeatBalanceInput` is declared at `HeatBalanceAirManager.hh` line 67, implemented at `HeatBalanceAirManager.cc` lines 163-189, and called in production only by `ManageAirHeatBalance` line 150 while the caller-owned `ManageAirHeatBalanceGetInputFlag` is true. The manager writes that flag false only after CP184 returns, then calls initialization, Air calculation, and mean-air reporting; a CP184 fatal or earlier non-return therefore leaves the flag true and blocks those later children on that attempt, while a successful return makes later manager calls skip only CP184. The flag defaults true, `HeatBalanceAirMgrData::clear_state` rearms it and clears only that manager's uniqueness sets, and the supported clean retry boundary is the broader `EnergyPlusData::clear_state` that also resets child-owned heat-balance and room-air state. CP184 itself has no guard or persistent latch. Every direct entry creates local `ErrorsFound = false`, then unconditionally calls `GetAirFlowFlag(state, ErrorsFound)`, `SetZoneMassConservationFlag(state)`, and `GetRoomAirModelParameters(state, ErrorsFound)` in that order before testing the aggregate once. `GetRoomAirModelParameters` owns another local flag and only raises the shared flag on its own errors; it never clears an earlier airflow error, so the result is monotonic OR accumulation. A normally returned first-child error does not suppress either later child, whereas any child non-return prevents its successors and the final wrapper check. When the aggregate is true, CP184's only diagnostic is terminal `ShowFatalError` with exact text `GetAirHeatBalanceInput: Errors found in getting Air inputs`; it does not derive the predicate from the global severe count. CP184 writes no persistent domain state itself and provides no validation, catch, rollback, cleanup, transaction, or idempotence. Its child context is observably stateful: `GetAirFlowFlag` sets `AirFlowFlag`, parses and allocates simple airflow/report state, and can append an AirFlow EIO row even with the aggregate already true; `SetZoneMassConservationFlag` monotonically marks receiving then source Zone flags and can expose a partial prefix after malformed parsed topology; `GetRoomAirModelParameters` allocates and populates per-Zone room-air state and writes its EIO header/rows before propagating its local error. A final fatal preserves all returned-child mutations, registrations, diagnostics, EIO output, and uniqueness effects while leaving the parent latch true; same-state retry revisits them without rollback. A direct call never changes the parent latch, so a later manager call can repeat the complete input wrapper, whereas any successfully returned production call suppresses it for that state even if a later manager child fails. The only literal direct test call is PTAC integration setup after heat-balance input and array allocation; it asserts later PTAC node and flow results, not CP184 state, ordering, error, fatal, latch, output, or retry. No test directly calls `ManageAirHeatBalance`; eight fixtures contain explicit `ManageHeatBalance` paths but assert daylighting, EMS, construction, plant, or Window behavior. Full-harness simulations provide broader transitive smoke, including one fixture that checks parsed Mixing/CrossMixing records, but not the wrapper contract. Separate child tests prove one airflow error flag and one room-air error/severe stream, while no test proves aggregate fatal text, three-child continuation, parent latch retention, partial effects, direct-call duplication, or clean/reset retry. Rust's `get_air_heat_balance_input_compat` is only an identity closure alias whose sole use wraps a per-Zone air-temperature coefficient calculation; the timestep manager path does not nest it, Rust owns none of the three input children or caller latch, and its heat-balance state has no matching lifecycle. CP184 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 193 routines, split 58 `state_mapped` plus 135 `source_mapped`, with 73 required; the heat-balance project list becomes 42. CP185 adds required source-mapped `routine.get_air_flow_flag` immediately after `routine.get_air_heat_balance_input` in the Air-manager definition ledger and adds `get_air_flow_flag` after `get_air_heat_balance_input` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirFlowFlag` is declared at `HeatBalanceAirManager.hh` line 69, implemented at `HeatBalanceAirManager.cc` lines 191-214, and called in production only as CP184's unconditional first child at line 179. Every entry first overwrites the persistent `AirFlowFlag` true, then calls complete `GetSimpleAirModelInputs(state, ErrorsFound)`, then on normal child return sums `TotInfiltration`, `TotVentilation`, `TotMixing`, `TotCrossMixing`, and `TotRefDoorMixing`. A strictly positive sum appends exact two-line EIO text `! <AirFlow Model>, Simple` followed by ` AirFlow Model, Simple`; zero writes no CP185 summary even though the flag remains true. `TotZoneAirBalance` and auxiliary EarthTube, CoolTower, and ThermalChimney presence are not in that output predicate. CP185 neither initializes, reads, tests, clears, nor directly writes the caller's `ErrorsFound` reference. The parser only raises that shared flag on its diagnosed paths, so an incoming true remains true and a returned child error does not suppress the count test or positive-total summary. CP185 emits no diagnostic itself. The child remains separate source dependency context: it allocates Zone and optional Space air-report records, registers output/EMS state, parses the nine simple-airflow object families, populates counts/records/mass-conservation data, and writes detailed EIO before returning. `AirFlowFlag` defaults false in `HeatBalanceData`, CP185 is its only source writer, and `HeatBalanceData::clear_state` placement-new restores false and clears its counts and arenas. The flag gates `InitSimpleMixingConvectiveHeatGains` simple mixing/cross-mixing/refrigeration-door initialization and, after its own accumulator resets, `ZoneEquipmentManager::CalcAirFlowSimple` before the AirflowNetwork control gate and simple airflow/auxiliary calculations. Count and record state has further HVAC, hybrid-ventilation, SQLite, mass-conservation, nominal-report, and output consumers. CP185 has no guard, validation, catch, rollback, cleanup, transaction, or one-shot latch. A parser non-return preserves the already-true flag plus its child prefix and skips CP185's summary; a count-access or EIO failure preserves the flag and completed parser effects. On normal error return, CP184 still runs its mass-conservation and room-air children and can later fatal; because the manager clears its input latch only after CP184 returns, that fatal leaves the latch true and same-state retry re-enters CP185 with prior parser/output state. The flag write is value-idempotent after the first entry, but the parser, registrations, uniqueness checks, diagnostics, and EIO are not a repeat-safe transaction. Direct calls always re-enter the child, while a successful production input pass suppresses later CP185 calls through the parent latch. Air-manager-only clear rearms that latch and clears its uniqueness sets but does not reset `HeatBalanceData` or output state; the broader whole-state clear is the clean retry boundary. One direct unit fixture supplies two same-Zone `ZoneAirBalance:OutdoorAir` objects with missing schedules and asserts only that the shared error flag is true; it neither isolates the prior `GetZoneData` status nor checks CP185's flag, zero-total no-summary branch, diagnostics, or parsed state. One PTAC fixture indirectly executes a clean five-infiltration positive-total path through CP184 but asserts only later node and mass-flow behavior. Parser tests bypass CP185, and a consumer test manually toggles `AirFlowFlag`. No test isolates false/true overwrite, incoming-error preservation, exact summary text or absence, error-plus-positive-summary behavior, repeated calls, partial failure, parent latch, or reset/retry. Rust has no `GetAirFlowFlag` alias, selector, parser, five totals, simple-airflow arenas, or matching EIO path. Its non-system `sum_mcp*` fields stay zero without a simple-airflow producer; typed `Construction:AirBoundary` mixing metadata is explicitly run-blocked and generates no `ZoneCrossMixing`, AirflowNetwork is unsupported, and Ideal Loads outdoor air is a distinct subsystem. CP185 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 194 routines, split 58 `state_mapped` plus 136 `source_mapped`, with 74 required; the heat-balance project list becomes 43. CP186 adds required source-mapped `routine.set_zone_mass_conservation_flag` immediately after `routine.get_air_flow_flag` in the Air-manager definition ledger and adds `set_zone_mass_conservation_flag` after `get_air_flow_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `SetZoneMassConservationFlag` is declared at `HeatBalanceAirManager.hh` line 71, implemented at `HeatBalanceAirManager.cc` lines 216-233, and called in production only as CP184's unconditional second child at line 181, after CP185 returns and before the room-air child. It receives no error reference or status parameter. Every entry first reads `ZoneAirMassFlow.EnforceZoneMassBalance` and then tests `ZoneFlowAdjustment` against exact `AdjustmentType::NoAdjustReturnAndMixing`. A false first guard or exact no-adjust value performs no writes; otherwise it visits expanded `Mixing` records from 1 through `TotMixing` in stored order and monotonically writes each receiving `ZonePtr` flag true before its source `FromZone` flag true. Zero or negative totals naturally visit no records, duplicate or self links repeat true stores, and any enum value other than exact no-adjust passes the second guard. CP186 never writes false and does not include `CrossMixing`, AirBoundary-generated cross mixing, refrigeration-door mixing, infiltration, or ventilation. `GetProjectControlData` owns the two controls, CP185's parser owns `TotMixing` and `Mixing`, and ordinary `AllocateHeatBalArrays` dimensions `ZoneMassBalanceFlag` false before the production Air pass. CP186 is the only production true writer. `CalcZoneMassBalance` reads the flag on each controlled-Zone visit of the mass-balance iteration loop to include mixing/return flow in mass continuity, while `CalcAirFlowSimple` reads it in all three delta-temperature branches when adjusted mixing is requested; resulting effects can reach return, infiltration, heat, moisture, contaminant, resimulation, and report state. CP186 has no allocation, enum, count, index, topology, or result validation and emits no diagnostic or output. In direct or externally mutated state, a malformed receiving endpoint can fail before the current record changes, while a valid receiving endpoint followed by an invalid source can leave that first write plus every prior record committed; unchecked access can be undefined. Stable valid re-entry is value-idempotent, but false guards, zero totals, removed links, or changed topology preserve stale true flags. `HeatBalFanSysData::clear_state` discards the flag array for later false reallocation; HeatBalanceData or Air-manager-only clear does not reset this separate owner, so whole-state clear and reinitialization is the clean boundary. A CP185 error that returns normally does not suppress CP186, and CP186 cannot alter CP184's aggregate; a CP186 non-return prevents room-air input and the final fatal while leaving the parent latch true. Seven direct fixtures call CP186. One explicitly exercises the second-guard no-op with enforcement true and no-adjust, five active-adjustment fixtures transitively exercise downstream mass balance, and one active fixture asserts only parser-owned controls and `ZoneReOrder`; none directly asserts `ZoneMassBalanceFlag`. No test isolates the first guard, active zero count, exact endpoint write order, sticky/repeated topology, excluded families, malformed partial writes, parent retry, or reset. Rust has no `SetZoneMassConservationFlag` alias, `ZoneAirMassFlowConservation` controls, typed `ZoneMixing` topology, endpoint flag array, mass-balance solver, or adjusted simple-mixing consumer. Its zero-seeded `sum_mcp*` state, run-blocked AirBoundary metadata, and Ideal Loads airflow are adjacent but distinct. CP186 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 195 routines, split 58 `state_mapped` plus 137 `source_mapped`, with 75 required; the heat-balance project list becomes 44. CP187 adds required source-mapped `routine.get_simple_air_model_inputs` immediately after `routine.set_zone_mass_conservation_flag` in the Air-manager definition ledger and adds `get_simple_air_model_inputs` after `set_zone_mass_conservation_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `GetSimpleAirModelInputs(EnergyPlusData &state, bool &ErrorsFound)` is declared at `HeatBalanceAirManager.hh` line 73, implemented at `HeatBalanceAirManager.cc` lines 235-4244, and called in production only by CP185 `GetAirFlowFlag` line 207, unconditionally after that wrapper writes `AirFlowFlag = true`. It first dimensions local report-selection state, allocates Zone plus sizing-or-simulation Space air-report records, and registers baseline Zone, simulation-only Space, Site, and conditional advanced outputs; it then sizes shared input scratch arrays from exactly nine direct schemas and processes `ZoneAirBalance:OutdoorAir`, all three `ZoneInfiltration` families, both `ZoneVentilation` families, `ZoneMixing`, `ZoneCrossMixing`, and `ZoneRefrigerationDoorMixing` in that order. Count-driven arenas and expanded Space/Zone records feed output and EMS registrations; `ZoneMixing` additionally constructs per-Zone `MassConservation` topology and receiving/source `ZoneReOrder` state, while `TotCrossMixing` appends generated Construction:AirBoundary records after explicit CrossMixing input without making AirBoundary a tenth parser family. The tail emits nominal infiltration, ventilation, mixing, cross-mixing, and refrigeration-door EIO rows, stores per-Zone nominal totals, conditionally retains the last encountered infiltration pointer per Zone when mass-balance enforcement is enabled, registers mass-balance outputs, and deliberately excludes refrigeration-door flow from `TotMixingFlow`. CP187 never initializes, clears, tests, or terminally barriers the supplied `ErrorsFound` reference: incoming true does not suppress any phase, diagnosed paths only raise it, and normal return lets CP185 perform its count summary regardless of the value. A blank ZoneAirBalance induced-air schedule and an enforced-balance source-only Mixing Zone without Infiltration each emit severe diagnostics without raising the reference; malformed endpoints or schedules can instead fail before normal return, so diagnosed continuation is not a return guarantee. The routine has no guard, transaction, rollback, or cleanup. Same-state repeat normally encounters already allocated HeatBalance arrays and can repeat registrations, diagnostics, or EIO; retained `UniqueZoneNames` can turn a partially cleared ZoneAirBalance retry into duplicate errors, whereas `UniqueInfiltrationNames` is only reserved and has no semantic use. Air-manager-only and HeatBalance-only clears each leave other owners stale, so coordinated whole-state reset plus normal reinitialization is only the clean domain-state retry boundary; an output-clean replay additionally requires a fresh or reset EIO stream. Fifteen actual direct unit calls cover selected design-flow infiltration/ventilation expansion, all three infiltration models, explicit CrossMixing, several Mixing mass-balance paths, one `ZoneReOrder` assertion, and mass-balance output ordering; no direct parser fixture covers ZoneAirBalance, WindAndStack ventilation, refrigeration-door mixing, or AirBoundary-generated CrossMixing, and none isolates incoming-true behavior, either unflagged severe, exact nominal EIO, partial failure, repeat, or reset. Rust has no typed records, counts, topology, outputs, EMS, EIO, or runtime path for any of the nine families; generic Zone/schedule state, zero-seeded `sum_mcp*` fields, run-blocked AirBoundary metadata, and Ideal Loads outdoor air are adjacent dependencies only. CP187 adds no Rust target, code, state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 196 routines, split 58 `state_mapped` plus 138 `source_mapped`, with 76 required; the heat-balance project list becomes 45. CP188 adds required source-mapped `routine.get_room_air_model_parameters` immediately after `routine.get_simple_air_model_inputs` in the Air-manager definition ledger and adds `get_room_air_model_parameters` after `get_simple_air_model_inputs` and before `manage_zone_air_updates` in the heat-balance project contract. `GetRoomAirModelParameters(EnergyPlusData &state, bool &errFlag)` is declared at `HeatBalanceAirManager.hh` line 75, implemented at `HeatBalanceAirManager.cc` lines 4246-4492, and called in production only as CP184 `GetAirHeatBalanceInput` line 184's unconditional third child after CP185 and CP186 return. Every entry resizes and default-fills one `AirModelData` record per Zone as Mixing, Direct, and not simulated, initializes only a local error false, counts the sole direct schema `RoomAirModelType`, diagnoses but continues when its object count exceeds the Zone count, and false-dimensions three downstream per-Zone model arrays only when at least one object exists. The object loop resolves a Zone, maps eight model selectors and Direct/Indirect coupling, sets per-Zone selector/coupling/`SimAirModel` plus monotonic global use flags, validates only the matching Zone-name presence of five `RoomAirSettings:*` families, and for AirflowNetwork checks only whether `AirflowNetwork:SimulationControl` exists. UserDefined details, RoomAir nodes and patterns, AirflowNetwork intrazone topology, and all settings fields remain later `GetAirModelDatas` dependencies. Invalid model and coupling keys emit warnings and fall back to Mixing and Direct without raising the local error; too many objects, a missing Zone or companion, a missing AirflowNetwork control object, and any non-Mixing model combined with Space heat-balance sizing or simulation raise it. The nominal duplicate-Zone check reads `AirModel.Name` before the loop ever writes that field, so every clean entry sees empty names: repeated declarations for one Zone overwrite model and coupling in input order without that diagnostic, while a prior declaration's true `SimAirModel` or global used flag can remain sticky. CP188 never retains the authored object name and never sets `AirModelData::ZonePtr`. After the object loop it overwrites every Zone's record name with `MIXING AIR MODEL FOR <Zone>` regardless of selected model, refreshes its canonical Zone name, and monotonically sets `anyNonMixingRoomAirModel`. Even with zero objects, it materializes default Mixing records and, on every normally reached tail, writes the RoomAir EIO header plus one Zone-order row per Zone before any local error summary or outer-flag update. The outer `errFlag` is never read or cleared; a local error emits `Errors found in processing input for RoomAirModelType` and assigns only true after EIO, while an incoming true with locally clean input adds no CP188 summary and still executes every phase. An EIO or earlier non-return can preserve assigned state and local diagnostics before outer propagation. CP188 has no latch, transaction, rollback, or output cleanup. Direct repeat resets the per-Zone arena but repeats EIO and retains monotonic manager flags; `RoomAirModelData::clear_state` clears CP188 arenas and used flags but does not rearm `GetAirModelData`, `MyOneTimeFlag`, or `MyEnvrnFlag`, the parent Air-manager latch is separate, and `EnergyPlusData::clear_state` does not reset EIO. End-to-end clean retry therefore requires fresh or explicitly rearmed room-air and parent lifecycle state plus a fresh or reset EIO stream. Exactly two direct unit calls exist: one proves two missing-AirflowNetwork-control diagnostics and the outer true result, while one proves a valid AirflowNetwork false result only through later intrazone Zone indexing. No direct test covers zero-object defaults or EIO, count overflow, invalid Zone, duplicate overwrite, invalid keys, Indirect coupling, any non-AirflowNetwork active model, Space incompatibility, state fields, incoming true, partial failure, repeat, or reset. Rust has no `GetRoomAirModelParameters` alias, typed `RoomAirModelType` or `RoomAirSettings` records, per-Zone selector/coupling state, non-Mixing flags, RoomAir or intrazone-node topology, dependency validation, model dispatch, or RoomAir EIO. Those room-air objects remain RawOnly and run-blocking, AirflowNetwork remains explicitly unsupported, and typed Zone/Space identities, the single Zone mean-air state, numerical zone-air selectors, HVAC Zone-air-node edges, surface adjacent-air state, and oracle-side Mixing EIO are adjacent but non-equivalent. CP188 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 197 routines, split 58 `state_mapped` plus 139 `source_mapped`, with 77 required; the heat-balance project list becomes 46. CP189 adds required source-mapped `routine.init_air_heat_balance` immediately after `routine.get_room_air_model_parameters` in the Air-manager definition ledger and adds `init_air_heat_balance` after `get_room_air_model_parameters` and before `manage_zone_air_updates` in the heat-balance project contract. `InitAirHeatBalance(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 77 and implemented at `HeatBalanceAirManager.cc` lines 4494-4507. The body owns no condition, loop, local persistent state, status, diagnostic, output, catch, cleanup, or latch; every entry forwards the unchanged state exactly once to CP190 `InitSimpleMixingConvectiveHeatGains` line 4506 and returns only after that child returns. Two production call sites exist. The ordinary `ManageAirHeatBalance` path calls CP189 unconditionally at line 154 after its optional input block and before `CalcHeatBalanceAir` and `ReportZoneMeanAirTemp`; its input latch has already been cleared after successful input, so a CP189 non-return preserves the child's partial effects, suppresses both later children, and a same-state manager retry skips input and enters CP189 again. `SimulationManager::Resimulate` conditionally calls CP189 at line 2908 when `ResimHB` is true after Surface initialization and both Surface balances; a non-return prevents refrigeration-rack work, the heat-balance iteration increment, and the line-2912 assignment forcing `ResimHVAC` true, while prior Surface and child effects remain. This second path intentionally can execute multiple times without advancing simulation time and bypasses the Air-manager input latch and its `CalcHeatBalanceAir` and report siblings. CP189 itself neither makes child re-entry safe nor adds a wrapper-specific reset obligation; mutation, failure, retry, and owner-clear behavior are exactly those of CP190 and its dependencies. As bounded dependency context rather than parity, the child's enforced mass-balance fraction loop uses each record's receiving count against the global `Mixing(1..N)` prefix and raw `DesignLevel`, not that Zone's receiving pointers or schedule/EMS-scaled flow. Five direct unit calls all use CP189 as setup for ZoneEquipment mass-balance tests and assert downstream airflow or mass-balance results rather than the wrapper's one-child cardinality, either production caller order, resimulation, failure prefix, retry, or reset; four direct CP190 calls in two fixtures bypass the wrapper. Rust's `init_air_heat_balance_compat` is an identity closure invoked with an empty body in the timestep scaffold. It has no CP190 alias, typed mixing, cross-mixing, or refrigeration-door arenas, schedule or EMS refresh, mass-conservation fractions, demand-manager resimulation path, diagnostics, or outputs; zero-seeded `sum_mcp*` state and run-blocked AirBoundary SimpleMixing metadata are adjacent but non-equivalent. CP189 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 198 routines, split 58 `state_mapped` plus 140 `source_mapped`, with 78 required; the heat-balance project list becomes 47. CP190 adds required source-mapped `routine.init_simple_mixing_convective_heat_gains` immediately after `routine.init_air_heat_balance` in the Air-manager definition ledger and adds `init_simple_mixing_convective_heat_gains` after `init_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `InitSimpleMixingConvectiveHeatGains(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 79, implemented at `HeatBalanceAirManager.cc` lines 4509-4588, and called in production only as CP189 `InitAirHeatBalance` line 4506's unconditional child. Every entry first tests `AirFlowFlag`; false leaves every target untouched. True executes four ordered phases. The actual Mixing vector, not `TotMixing`, receives `DesignLevel * sched->getCurrentVal()`, then an optional object EMS override, then the final value in both `DesiredAirFlowRate` and `DesiredAirFlowRateSaved`. Under `EnforceZoneMassBalance` the actual MassConservation vector is traversed; for each record's receiving count `N`, the first pass adds global `Mixing(1..N).DesignLevel` before zeroing the corresponding fraction, and a strictly positive sum triggers normalization from that same raw global prefix. This ignores `ZoneMixingReceivingPtr` plus schedule- and EMS-scaled desired flows, so zero, negative, or NaN sums leave touched fractions zero and each Zone can receive weights derived from unrelated records. The actual CrossMixing vector, not `TotCrossMixing`, then gets the same schedule-before-object-EMS desired flow but no saved-flow write. Finally, `TotRefDoorMixing > 0` acts only as a gate over Zone indices 1 through `NumOfZones - 1`; a flagged record whose `ZonePtr` equals that index has every declared connection volume zeroed and then replaced only when its door EMS override is on. Open schedules and physical door flow remain later `CalcAirFlowSimple` work. CP190 does not initialize infiltration, ventilation, report flags, Mixing mass flow, CrossMixing saved flow, counts, or heat-gain accumulators, and it contains no AirflowNetwork gate, allocation, validation, diagnostic, EIO, status, latch, catch, rollback, or cleanup. Null schedules or mismatched vectors, counts, fractions, Zone-indexed records, or connection arrays can stop the routine after an ordered Mixing, fraction, CrossMixing, or door prefix. Normal repeat resamples current schedule and EMS state, restores Mixing baselines, rebuilds touched fractions, and resets reached door volumes, while false guards, shortened counts, or skipped records preserve untouched values. `HeatBalanceData::clear_state` reconstructs all CP190-owned records; Air-manager-local clearing only rearms the separate input lifecycle, so clean production replay needs coordinated dependency reinitialization, but CP190 itself has no output-stream reset requirement. Four direct calls in two fixtures cover only an `AirFlowFlag = false` fraction sentinel, zero and 100/300 raw-DesignLevel fractions, and downstream CrossMixing airflow effects. They do not directly assert Mixing desired/saved flow, non-unit schedules, schedule or object EMS, count-versus-vector behavior, the ignored receiving pointers, negative/nonfinite arithmetic, CrossMixing state, any refrigeration-door branch, partial failure, repeat, resimulation, or reset. Rust has no CP190 alias, typed ZoneMixing, ZoneCrossMixing, or ZoneRefrigerationDoorMixing runtime records, schedule/EMS flow refresh, mass-conservation fraction topology, door state, or consumers. The CP189 identity closure runs an empty body; generic schedule machinery, zero-seeded `sum_mcp*` fields, and typed but run-blocked `Construction:AirBoundary` SimpleMixing metadata are adjacent but non-equivalent. CP190 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 199 routines, split 58 `state_mapped` plus 141 `source_mapped`, with 79 required; the heat-balance project list becomes 48. CP191 adds required source-mapped `routine.calc_heat_balance_air` immediately after `routine.init_simple_mixing_convective_heat_gains` in the Air-manager definition ledger and adds `calc_heat_balance_air` after `init_simple_mixing_convective_heat_gains` and before `manage_zone_air_updates` in the heat-balance project contract. `CalcHeatBalanceAir(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 83, implemented at `HeatBalanceAirManager.cc` lines 4590-4604, and called in production only by `ManageAirHeatBalance` line 158 after CP189 returns and before `ReportZoneMeanAirTemp`. Each entry tests the current `state.dataGlobal->externalHVACManager`. A configured callback selects the external branch: when `externalHVACManagerInitialized` is false it first calls `initializeForExternalHVACManager`, then invokes the callback with `&state`; a true flag skips only that initializer. An absent callback ignores the flag and calls `HVACManager::ManageHVAC`, so the external branch bypasses the entire standard HVAC manager. The initializer presently calls only `InitZoneAirSetPoints`, while neither CP191, that child, nor the runtime registration APIs set `externalHVACManagerInitialized` true; source-wide references contain only the false default and CP191 read, so a normally registered callback repeats the initializer call on every entry unless external code mutates the flag. The child's own one-time flag can suppress only its internal one-time block; it is not CP191's initialized flag. CP191 owns no direct mutation, status, diagnostic, output, catch, cleanup, rollback, or transaction. An initializer non-return suppresses the callback, and any initializer, callback, or `ManageHVAC` effects before a non-return remain while the parent report and later Surface-manager work are suppressed. Normal repeat re-evaluates the callback and executes one branch every time; child work is stateful rather than idempotent. `DataGlobal::clear_state` placement-new removes the callback and resets the flag false; Air-manager-local clear touches neither, so external-mode replay needs callback re-registration plus dependency reset. There are no direct unit calls to CP191 or its initializer and no external-manager API test; ordinary simulation tests only transitively cover the default branch. Rust's `calc_heat_balance_air_compat` is an identity closure used once around a bounded `ManageZoneAirUpdates(PredictStep)` shell and zone-temperature work. It has no external callback, flag, initializer, `ManageHVAC`, complete predictor/corrector and system-timestep loop, HVAC equipment/air/plant dispatch, demand-manager work, or report topology; separate Ideal Loads and execution-plan scaffolds are not CP191 parity. CP191 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 200 routines, split 58 `state_mapped` plus 142 `source_mapped`, with 80 required; the heat-balance project list becomes 49. CP192 adds non-required source-mapped `routine.initialize_for_external_hvac_manager` immediately after `routine.calc_heat_balance_air` in the Air-manager definition ledger and adds no project-contract requirement. `initializeForExternalHVACManager(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 81, implemented at `HeatBalanceAirManager.cc` lines 4608-4613, and called in production only by CP191 line 4598 when an external callback exists and `externalHVACManagerInitialized` is false. The standard `ManageHVAC` branch and an external branch whose flag is true both bypass CP192. Its body has no local guard or write and forwards the unchanged state exactly once to `ZoneTempPredictorCorrector::InitZoneAirSetPoints` line 4612; it does not acquire Zone setpoint input, calculate setpoints, invoke the external callback, or set CP191's initialized flag. The child is independently called by `ManageZoneAirUpdates` line 220 and owns a one-time allocation/output-registration block, begin-environment and day latches, controlled-Zone and demand-limit work, sticky error state, a fatal tail, and several other owners' arrays. A child non-return preserves its ordered prefix and prevents the CP191 callback, Air report, and later Surface-manager work. Failure before the child's one-time flag clear can repeat partial allocation or registration on retry, while failure after that clear skips the one-time prefix; a retained environment flag or sticky error can likewise repeat reset work or fatal again. Because CP191 never sets its global initialized flag, normal external-mode entry calls CP192 before every callback, although the child's separate one-time flag suppresses its successful one-time block. Coordinated owner reset is required for a clean child replay, and `DataGlobal::clear_state` removes the external callback so whole-state replay also requires re-registration. CP192 has no direct test and no external-manager registration test; four direct `InitZoneAirSetPoints` calls in four HVAC/report fixtures bypass the wrapper and use the child only as setup. Rust has no CP192 or external-manager adapter. Its `init_zone_air_set_points_compat` is an identity closure used once around an empty setpoint-calculation shell and implements none of the source child's allocation, output, environment, demand, error, or reset lifecycle. CP192 adds no EnergyPlus source inventory, Rust target, code, state, test, project requirement, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 201 routines, split 58 `state_mapped` plus 143 `source_mapped`, with 80 required; the heat-balance project list remains 49. CP193 adds required source-mapped `routine.report_zone_mean_air_temp` immediately after `routine.initialize_for_external_hvac_manager` in the Air-manager definition ledger and adds `report_zone_mean_air_temp` after `calc_heat_balance_air` and before `manage_zone_air_updates` in the heat-balance project contract. `ReportZoneMeanAirTemp(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 85, implemented at `HeatBalanceAirManager.cc` lines 4615-4687, and called in production only by `ManageAirHeatBalance` line 160, unconditionally after CP191 returns. Its one-time phase scans the actual OutputProcessor request vector for exact Zone or Space Wetbulb Globe Temperature names, fans a stored empty key over the declared Zone or Space count, otherwise performs a literal name lookup, then scans EMS sensors `1..NumSensors`; Space handling is entirely guarded by `doSpaceHeatBalanceSimulation`, and only the Output:Variable keys are uppercased here. Reached matches monotonically set per-report `ReportWBGT` true, and only a completely returned scan clears `CalcExtraReportVarMyOneTimeFlag`. Every call then traverses Zones `1..NumOfZones`, calls CP194 `calcMeanAirTemps` once with each Zone's averaged temperature, humidity ratio, MRT, and report record, and, when Space simulation is active, visits that Zone's stored `spaceIndexes` in order and calls the same child with each Space's averaged state but the owning Zone number for operative-control metadata. CP194 owns the mean temperature and humidity copies, 50/50 operative temperature, dew-point psychrometrics, conditionally retained thermostat-operative value, and conditionally retained WBGT calculation; CP193 itself neither emits output nor allocates or validates these arenas. A request or sensor scan non-return preserves a true-flag prefix and the still-true latch, while a later non-return retains the false latch plus the completed Zone/Space and child-write prefix; there is no status, catch, cleanup, rollback, or transaction. Air-manager clear rearms CP193's scan but leaves its report state intact, while HeatBalance, predictor/corrector, OutputProcessor, EMS, Zone-control, and environment owners separately hold report flags and values, averaged inputs, requests, sensors, control metadata, and pressure, so clean replay requires coordinated reset and input reconstruction. No C++ unit test calls CP193 or CP194 directly or names the WBGT targets and report-only fields. Rust's `report_zone_mean_air_temp_compat` is an identity closure used only to choose one bounded Zone temperature sample during run-period aggregation; existing MAT/humidity result series and a CLI trace label provide neither the request/EMS scan, Space traversal, report bundle, operative/dew-point/WBGT calculations, failure lifecycle, nor CP193 call topology. CP193 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 202 routines, split 58 `state_mapped` plus 144 `source_mapped`, with 81 required; the heat-balance project list becomes 50. CP194 adds required source-mapped `routine.calc_mean_air_temps` immediately after `routine.report_zone_mean_air_temp`, closing the HeatBalanceAirManager definition ledger, and adds `calc_mean_air_temps` after `report_zone_mean_air_temp` and before `manage_zone_air_updates` in the heat-balance project contract. `calcMeanAirTemps(EnergyPlusData &state, Real64 ZTAV, Real64 airHumRatAvg, Real64 MRT, AirReportVars &thisAirRpt, int zoneNum)` is declared at `HeatBalanceAirManager.hh` lines 87-92, implemented at `HeatBalanceAirManager.cc` lines 4689-4728, and called only by CP193 for each Zone at line 4678 and each active stored Space membership at lines 4682-4683; Space inputs use that Space's averages but the owning Zone number for control metadata. Every entry first writes `MeanAirTemp = ZTAV`, the raw averaged humidity ratio, ordinary `0.5 * (ZTAV + MRT)` operative temperature, and dew point from current outdoor barometric pressure in that order. `PsyTdpFnWPb` floors only its local humidity input to `1e-5` before saturation work, so the stored report humidity remains raw. Thermostat-operative output updates only under `AnyOpTempControl`, controlled-Zone, and non-`None` guards. Only exact `Scheduled` reads the schedule's current value, including an EMS override; every other non-`None` value takes `FixedRadiativeFraction`. No index, pointer, finite-value, or `[0,1]` fraction check is local, the ordinary operative value is redundantly restored, and `ThermOperativeTemp = (1-f) * ZTAV + f * MRT` can extrapolate. A false guard retains its prior/default value. Only `ReportWBGT` updates `WetbulbGlobeTemp` as `0.7 * PsyTwbFnTdbWPb(ZTAV, airHumRatAvg, OutBaroPress) + 0.3 * OperativeTemp`, using raw humidity and the ordinary 50/50 operative value rather than the thermostat-weighted value; a false flag retains the old/default WBGT. The default psychrometric build uses quantized saturation and wet-bulb caches plus raw memo, warning, and recurring-error state, so normal numeric writes are not a claim of pure whole-routine idempotence or cold retry. CP194 owns no allocation, validation, diagnostic conversion, status, latch, catch, cleanup, rollback, or transaction: a dew-point non-return leaves the first three writes, schedule failure leaves the first four, and wet-bulb failure leaves all preceding reached writes while the final target remains retained. Clean replay spans separately owned HeatBalance report arenas, predictor averages, Zone controls, environment pressure, and psychrometric/cache state. No C++ unit test directly calls CP194 or CP193 or asserts any target report field; focused psychrometric tests do not cover the report composition, guards, Space topology, partial effects, repeat, or reset. Rust has no CP194 alias, integrated Air-report record, MRT/operative/control/WBGT state, Space report path, or W-input cached wet-bulb counterpart. Its isolated dew-point projection and RH-input outdoor wet-bulb helper are not wired to this source call topology. CP194 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list becomes 51. CP195 expands the existing required source-mapped `routine.manage_zone_air_updates` without adding a routine or project-contract entry. `ManageZoneAirUpdates(EnergyPlusData &state, PredictorCorrectorCtrl UpdateType, Real64 &ZoneTempChange, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 264-270 and implemented at `ZoneTempPredictorCorrector.cc` lines 195-244. Every entry first tests the shared `GetZoneAirStatsInputFlag`; true calls CP196 `GetZoneAirSetPoints` and clears the latch only after that child returns. It then calls `InitZoneAirSetPoints` unconditionally before dispatching. The six recognized selectors call `CalcZoneAirTempSetPoints`, `PredictSystemLoads` with all three timestep arguments, `correctZoneAirTemps` with only the history selector, or the three revert/Zone-push/system-push history children. `Invalid`, `Num`, and out-of-range casts reach a silent default only after the common input/init prefix. Only a normally returned `CorrectStep` assigns its child result into caller-owned `ZoneTempChange`; every other selector and a correct-child non-return preserve the caller's prior value. Seven `HVACManager::ManageHVAC` sites execute Get, initial Predict and Correct, optional shortened-step Predict/Correct/PushSystem repetitions, and final PushZone; two `SimulationManager::Resimulate` sites execute only Get then Predict when HVAC resimulation is requested. No production caller selects Revert. CP195 owns no selector validation, status, diagnostic conversion, catch, cleanup, rollback, or transaction. A Get failure retains its parser prefix and true latch while suppressing Init and dispatch; an Init failure retains the now-false latch and its prefix; branch failure retains both common phases and the reached branch prefix. Normal repeat skips successful one-time input but always reruns Init and the selected stateful child, while push/revert repetition is not idempotent. Clean replay requires coordinated ZoneControls, predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, RoomAir, node, schedule, environment, and output-owner reset. The C++ unit tree directly calls CP195 zero times; direct child calls exercise Get 9 times, Init 4, setpoint calculation 21, Predict 16, and Correct 5, but none of the three history children, and none proves wrapper order, latch timing, one-branch dispatch, default behavior, output-reference retention, partial failure, retry, or reset. Rust's stage object is metadata and `manage_zone_air_updates_compat` ignores its selector while passing one arbitrary closure through. Live Rust invokes only Predict and Correct selector shells, manually composes empty/common and history wrappers in a topology that is not the source dispatch, and has no shared input latch, unconditional Init contract, `ZoneTempChange` reference, invalid/default selector, or coordinated owner lifecycle. Existing bounded MAT, coefficient, correction, history, execution-plan, and promoted output evidence does not establish CP195 parity. CP195 adds no EnergyPlus source inventory, routine count, project entry, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory remains 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list remains 51. CP196 adds required source-mapped `routine.get_zone_air_set_points` immediately after `routine.manage_zone_air_updates` in the predictor/corrector definition ledger and adds `get_zone_air_set_points` after `manage_zone_air_updates` before `update_final_surface_heat_balance` in the heat-balance project contract. `GetZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 272 and implemented at `ZoneTempPredictorCorrector.cc` lines 246-2174 with no explicit return or internal latch. It executes ordinary thermostat and four setpoint families, humidistat, thermal-comfort and four Fanger families, unconditional HybridModel input, Zone capacitance assignment and EIO, operative/adaptive comfort, temperature-and-humidity overcool, and staged-dual input before one accumulated-error fatal. It expands ZoneLists, allocates and links control and schedule state, mutates Zone indices and multipliers, registers operative-temperature outputs, preserves warning and severe paths that do not set the local fatal flag, and can exit earlier through Hybrid or adaptive helper fatals. The staged precheck reads the routine-wide error flag, the capacitance ZoneList loop omits its final member, and no rollback or re-entry guard exists. Three production call sites share `GetZoneAirStatsInputFlag` and clear it only after return; a non-return retains partial state and the true latch, so same-state retry is unsafe. Nine direct C++ unit calls cover selected valid fields and two fatal diagnostics but not the complete family order, shared latch, valid staged, overcool, ZoneList, or capacitance-output paths, partial failure, retry, or reset. Rust types only direct-Zone DualSetpoint thermostat and humidistat subsets and exposes an identity `get_zone_air_set_points_compat` closure; it does not implement the complete 16-family transaction, ZoneList expansion, diagnostic and output order, latch, partial effects, or owner lifecycle. CP196 adds no new EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 204 routines, split 58 `state_mapped` plus 146 `source_mapped`, with 83 required; the heat-balance project list becomes 52. CP197 adds non-required source-mapped `routine.calculate_monthly_running_average_dry_bulb` immediately after `routine.get_zone_air_set_points`; the heat-balance project contract remains unchanged. `CalculateMonthlyRunningAverageDryBulb(EnergyPlusData &state, Array1D<Real64> &runningAverageASH, Array1D<Real64> &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` line 284 and implemented at `ZoneTempPredictorCorrector.cc` lines 2176-2275. Its only two production calls are CP196 operative-temperature branches at lines 1661 and 1731, each under the shared adaptive initialized guard and immediately before CP198; callers allocate fresh zeroed `NumDaysInYear` arrays, while CP197 neither reads nor sets the guard and only CP198 sets it true. CP197 checks and opens the weather path, discards nine lines, blindly reads `NumDaysInYear * 24` records, strips six comma fields, and averages field 7 into positional days before its ASH and CEN windows. The canonical eight-header mismatch shifts daily buckets by one hour; EOF and conversion status are ignored, `StrToReal` silently supplies -99999 on invalid text, and the corrupted final bucket is never consumed. The regular ASH path sums 31 inclusive days and divides by 30, the regular CEN path sums 8 and divides by 7, both wrap paths exclude `dailyDryTemp(N)`, and each caller cell self-accumulates as `x = x + avgDryBulb` without local clearing before `/=`. Missing or unreadable weather fatals before output writes, while malformed readable input can flow into CP198 and become latched. No C++ unit test executes CP197; the adaptive test calls CP198 with synthetic arrays. Rust has a typed EPW parser, while its legacy dry-bulb loader skips eight headers and reports numeric conversion errors; neither provides CP197 averaging, adaptive state, or an operative-control path. CP197 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 205 routines, split 58 `state_mapped` plus 147 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP198 adds non-required source-mapped `routine.calculate_adaptive_comfort_set_point_schl` immediately after `routine.calculate_monthly_running_average_dry_bulb`; the heat-balance project contract remains unchanged. `CalculateAdaptiveComfortSetPointSchl(EnergyPlusData &state, Array1D<Real64> const &runningAverageASH, Array1D<Real64> const &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` lines 286-287 and implemented at `ZoneTempPredictorCorrector.cc` lines 2277-2348. Its only two production calls are CP196 lines 1662 and 1732, immediately after CP197 and under the same external false-initialized guard; CP198 has no local guard. It scans all stored design days, uses only day type 9, derives `MaxDryBulb - DailyDBRange / 2`, and conditionally writes shared ASH slots 0-2 for strict `(10, 33.5)` and CEN slots 3-6 for strict `(10, 30)`. Later qualifying design days overwrite each family independently, while nonqualifying values retain earlier/default slots; the C++ declaration `std::array<Real64, 7> = {-1}` initializes the vector as `[-1, 0, 0, 0, 0, 0, 0]`. It then allocates seven `NumDaysInYear` arrays and overwrites every day with three `0.31 * T + offset` ASH values or `-1`, followed by four `0.33 * T + offset` CEN values or `-1`. Exact bounds and nonfinite daily inputs take the `-1` branches. Input shape is trusted, the const input arrays are untouched, and the shared `initialized` flag is set only after all design, allocation, and daily work returns. There is no status, diagnostic, catch, cleanup, or rollback; abnormal non-return can retain design writes, an allocation/day prefix, and the prior flag, while complete direct re-entry runs even when the flag is already true. A later CP196 fatal retains successfully committed schedules and causes same-state input retry to skip both adaptive helpers; only full predictor/corrector owner reconstruction resets them. One C++ fixture calls CP198 directly three times with uniform 0, 40, and 25 arrays, asserting day 1, all seven daily formulas for 25, the flag, and only two design slots; strict bounds, nonfinite and shaped inputs, remaining days, multiple/default design days, partial failure, guarded integration, retry, and reset remain uncovered. Rust has no operative-temperature control object, adaptive model/formulas, schedule state, or consumer; DualSetpoint thermostat parsing and execution-plan labels are not parity. CP198 adds no source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 206 routines, split 58 `state_mapped` plus 148 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP199 adds required source-mapped `routine.init_zone_air_set_points` immediately after `routine.calculate_adaptive_comfort_set_point_schl` and adds `init_zone_air_set_points` after `get_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `InitZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 274 and implemented at `ZoneTempPredictorCorrector.cc` lines 2350-2816. CP195 line 220 calls it on every selector after optional CP196 input and before dispatch; CP192 line 4612 also calls it directly for external-HVAC initialization without reproducing that input prefix. The default-true one-time block sizes thermostat setpoints, load correction, control/report, optional comfort/Fanger, setback/deadband, ZoneList/ZoneGroup load, six hybrid-history, Zone demand, and simulation-or-sizing Space demand state. It warning-checks each Zone's heat-transfer-surface reference-air consistency, registers Zone and simulation-Space heat-balance and sensible/moisture demand bundles, six direct thermostat/correction outputs, optional comfort outputs, and ZoneList/ZoneGroup outputs, and clears its one-time latch only after that complete prefix. A begin-environment gate then calls CP200 for every Zone and active Space, zeros four current thermostat setpoints, sets load correction to one and temperature control to Uncontrolled, resets Zone and active-Space demand helpers, `DeadBandOrSetback`, `NoHeatToReturnAir`, and hybrid histories, and clears `MyEnvrnFlag` only at its tail. It intentionally does not directly reset averaged setpoints, temperature-control report, comfort control/report/Fanger state, `Setback`, `CurDeadBandOrSetback`, or ZoneList/ZoneGroup totals; seeing `BeginEnvrnFlag` false rearms the gate. The begin-day branches only toggle `MyDayFlag`. Every call then scans ordinary temperature controls followed by comfort controls. Once Zone-equipment input is filled and before `ControlledZonesChecked`, missing Zone configuration emits Severe plus Continue diagnostics and sets the routine-owned sticky `ErrorsFound`. Independent demand-management branches clamp ordinary controls with strict `>`/`<` comparisons and comfort controls with inclusive `>=`/`<=` comparisons; selected single heat/cool and single-heat-cool comfort paths also rewrite the ordinary control type/report, and the later comfort loop can overwrite ordinary-loop state. The fatal check occurs after both loops and their reached clamp mutations; only a normally reached final tail sets `ControlledZonesChecked = true`. No entry clears `ErrorsFound` and no status, catch, cleanup, rollback, shape reconciliation, or transaction exists. A non-return can retain an allocation/output, environment-reset, diagnostic, or clamp prefix according to the already committed latches; same-state error retry can repeat validation diagnostics and fatal, while clean replay requires coordinated predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, ZoneControls, ZoneEquipment, Surface, environment, and OutputProcessor reset. Four direct C++ unit calls use CP199 only as setup with begin-environment, comfort, demand, and validation behavior inactive and assert none of its state; focused boundary, warning, output-bundle, partial-failure, retry, and reset coverage is absent. Rust's `init_zone_air_set_points_compat` is an identity closure used only in the Predict scaffold and omitted from its Correct shell. Its separate constant-DualSetpoint heating/cooling observed series and limited `ZoneSysEnergyDemand` snapshots implement neither CP199 allocation, output binding, lifecycle, validation, demand clamps, nor selector-independent topology. CP199 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 207 routines, split 58 `state_mapped` plus 149 `source_mapped`, with 84 required; the heat-balance project list becomes 53. CP200 adds required source-mapped `routine.zone_space_heat_balance_begin_environment_init` immediately after `routine.init_zone_air_set_points` and adds `zone_space_heat_balance_begin_environment_init` after `init_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::beginEnvironmentInit(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 213 and implemented at `ZoneTempPredictorCorrector.cc` lines 2818-2836. Its only two direct call expressions are CP199 lines 2624 and 2628 inside `MyEnvrnFlag && BeginEnvrnFlag`: every stored Zone record runs first, then every stored Space record only when current `doSpaceHeatBalance` is true. The helper owns no identity, count, gate, or latch. For each fixed index 0 through 3 it writes `ZTM[i] = 0`, `WPrevZoneTS[i] = OutHumRat`, `DSWPrevZoneTS[i] = OutHumRat`, and `WPrevZoneTSTemp[i] = 0` in that order; it then writes `WTimeMinusP`, `W1`, `WMX`, and `WM2` from `OutHumRat` before zeroing `airHumRatTemp`, `tempIndLoad`, `tempDepLoad`, `airRelHum`, `AirPowerCap`, and `T1`. These are 26 overwrites: 12 outdoor-humidity values and 14 zeros. CP200 reads no prior target value and leaves current/average air humidity, MAT/MRT/ZT and their other histories, coefficients outside the two load scalars, and every unlisted record field unchanged. All four indexed targets are fixed `std::array<Real64, 4>` values. Outdoor humidity is copied without finite, sign, range, or consistency validation, so negative, infinite, and quiet-NaN values propagate to all 12 humidity targets without a clamp or diagnostic. There is no allocation, checked access, status, diagnostic, child call, catch, cleanup, or rollback, and valid state has no ordinary catchable failure path. Complete repeat with unchanged outdoor humidity is overwrite-idempotent; a changed value replaces all 12 humidity targets. CP199 alone controls frequency and clears its environment latch after the entire parent reset, so a Space mode that becomes true later in the same uninterrupted BeginEnvironment interval does not replay skipped Space records. No C++ unit test calls CP200 directly; the four direct CP199 fixtures have `BeginEnvrnFlag` false. Of 56 active full-simulation tests that reach CP199, 55 execute at least one Zone CP200 call; `WeatherManager_SetRainFlag` has zero Zones and executes none. Seven `SizingManager` tests also reach sizing-Space and one `HeatBalanceAirManager` test reaches simulation-Space, but none asserts CP200 fields. Rust creates only Zone state at run initialization with configurable three-slot temperature histories and fixed-default three-slot humidity histories; when a weather series exists, it performs a one-time first-weather-sample seed of current and averaged humidity plus both three-slot histories. That seed touches a different field set and is not CP200's current-`OutHumRat` four-slot per-environment Zone/Space reset. Rust lacks the exact four-slot `ZTM`, `WPrevZoneTS`, `DSWPrevZoneTS`, and `WPrevZoneTSTemp` set, Space records, 26-field touched-versus-retained boundary, and environment gate. Its coefficient representation is initialized and computed through a different path; these adjacent histories and coefficients are not CP200 parity. CP200 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 208 routines, split 58 `state_mapped` plus 150 `source_mapped`, with 85 required; the heat-balance project list becomes 54. CP201 adds required source-mapped `routine.zone_space_heat_balance_set_up_output_vars` immediately after `routine.zone_space_heat_balance_begin_environment_init` and adds `zone_space_heat_balance_set_up_output_vars` after `zone_space_heat_balance_begin_environment_init` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::setUpOutputVars(EnergyPlusData &state, std::string_view prefix, std::string const &name)` is declared at `ZoneTempPredictorCorrector.hh` line 215 and implemented at `ZoneTempPredictorCorrector.cc` lines 2838-2868. Its only production call expressions are CP199 line 2443 for every Zone and lines 2446-2447 for each stored Space membership only when `doSpaceHeatBalanceSimulation` is true; Zone and nested Space calls follow parent Zone order, while sizing-only Space mode does not enter this child. Each call registers, in order, `{Zone|Space} Air Temperature` bound to `ZT` as C/System/Average, `Air Humidity Ratio` bound to `airHumRat` as Units::None/System/Average, `Air Relative Humidity` bound to `airRelHum` as percent/System/Average, and `Mean Radiant Temperature` bound to `MRT` as C/Zone/Average, keyed by the source Zone or Space name. The optional meter fields remain invalid or empty, multipliers remain one, SQL index remains -999, and the report-frequency argument defaults to Hour before any matching request overrides its frequency and schedule. CP201 writes none of the four members, but OutputProcessor initialization, request parsing, dictionary insertion, setup/total counters, requested keyed OutVar pointers, report identifiers, and report-dictionary sinks are persistent side effects. Rows with neither a report request nor a DataOutputs variable-list match still create or reuse dictionary rows and advance setup/total counters without an OutVar. A variable-list-only match appends a keyed dummy pointer, DD link, and report identifier without emitting a report dictionary row; a report request additionally marks the entry for reporting and writes its dictionary sinks. The dictionary deduplicates case-insensitive name plus units only, while direct re-entry repeats the applicable counter, dummy or requested entry, link, identifier, and sink effects. No identity, membership, prefix, key, lifecycle, guard, status, catch, cleanup, or rollback is local. A formatting, initialization, request-input, allocation, or report-write non-return can retain any completed prefix while CP199's one-time latch remains true until line 2618; safe replay requires coordinated predictor/corrector record and OutputProcessor ownership rather than either reset alone. There is no direct CP201 unit call or positive registration assertion. Four direct CP199 fixtures and 55 active full-simulation tests execute at least one Zone setup indirectly; one additional no-Zone `WeatherManager_SetRainFlag` simulation reaches CP199 but executes no CP201 call. Only `HeatBalanceAirManager_GetMixingAndCrossMixing` reaches simulation-Space setup, for three stored Spaces, while seven sizing-Space tests do not. Rust's registry registers adjacent `Zone Mean Air Temperature`, while its heat-balance ResultStore separately stores `Zone Mean Air Temperature` and `Zone Mean Air Humidity Ratio`; neither registers or stores CP201's exact eight-name Zone/Space output set, four member bindings, System-versus-Zone timestep metadata, Space records, or registration lifecycle. Exact `Zone Air Temperature` and `Zone Air Humidity Ratio` constants in the IdealLoads CLI are ESO oracle input identities; the humidity name is also reused for a diagnostic comparison label, but neither is a RuntimeOutputRegistry or ResultStore production definition. CP201 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 209 routines, split 58 `state_mapped` plus 151 `source_mapped`, with 86 required; the heat-balance project list becomes 55. CP202 adds required source-mapped `routine.predict_system_loads` immediately after `routine.zone_space_heat_balance_set_up_output_vars` and adds `predict_system_loads` after `zone_space_heat_balance_set_up_output_vars` before `update_final_surface_heat_balance` in the heat-balance project contract. `PredictSystemLoads(EnergyPlusData &state, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 276-280 and implemented at `ZoneTempPredictorCorrector.cc` lines 2870-3145. Its only production call expression is CP195 line 227; the initial and downstepped `HVACManager` PredictStep sites plus `SimulationManager::Resimulate` enter through that wrapper after CP196 and CP199 return. Four phases execute in order. The staged loop samples both base schedules, uses `MAT` or shortened `XMPT`, increments warning or recurring-warning state and clamps heat to cool minus 0.1 when heat is not below cool, chooses the last qualifying negative cooling or positive heating `StageNum`, and applies the source-literal throttling setpoint. The heating branch notably compares its offset with half `CoolThroRange` while shifting by half `HeatThroRange`. Active Space heat balance copies the Zone stage to every stored Space membership. The on/off prefix is gated by `NumOnOffCtrZone` but scans every temperature-controlled Zone having positive `DeltaTCutSet`, saves or restores last-mode flags according to `ShortenTimeStepSys`, clears both off flags, selects `MAT` or shortened `XMPT` only for ThirdOrder and `T1` otherwise, and revises SingleHeat, SingleCool, or both DualHeatCool setpoints with the literal strict comparisons around 0.02 C; `SingleHeatCool` and other switch values receive no setpoint write. An inclusive revised dual lower-greater-than-or-equal-upper collision emits severe and continuation diagnostics and then fatals before prediction dispatch. Every Zone then calls the still-source-mapped CP203 `ZoneSpaceHeatBalanceData::predictSystemLoad` with all three timestep arguments. Each stored Space calls that child when `doSpaceHeatBalance` is true; otherwise a shortened call copies only the returned Zone `MAT` and `airHumRat` to the Space, while a nonshortened call leaves it unchanged. `UseZoneTimeStepHistory` and `PriorTimeStep` are only forwarded locally. The final on/off pass derives `CoolModeLast` and `HeatModeLast` from the off flags and inclusive sign tests on child-produced `TotalOutputRequired`; zero can set both when both flags are true, while NaN clears both. There is no local latch, status, finite or topology validation, catch, cleanup, or rollback. Staged warnings and any completed setpoint, stage, save/restore, child, or Space-copy prefix persist across a fatal or child non-return; same-state retry samples schedules again, advances staged recurrence state, and repeats child effects. Two direct C++ fixtures make 16 calls and 24 setpoint assertions, covering selected Euler and ThirdOrder normal/shortened on/off cases only; global `NumOfZones` remains zero, so no Zone/Space child, forwarding, staged path, final-mode result, failure, retry, or reset contract is asserted. Of 56 active full simulations that reach CP202, 55 execute a Zone child and eight enable a Space child, but none is positive staged/on-off or direct CP202 state coverage. Rust retains the cutout delta only in typed thermostat input, never consumes it at runtime, has no staged control, `StageNum`, CP202 off/mode memory, sensible `ZoneSysEnergyDemand::TotalOutputRequired` field, or Space heat-balance record, and supplies only an identity `predict_system_loads_compat` wrapper around a different bounded Zone-temperature update. Oracle-derived IdealLoads demand snapshots and a separate `predict_step_source_order_path` wrapper-order test do not implement this transaction. CP202 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 210 routines, split 58 `state_mapped` plus 152 `source_mapped`, with 87 required; the heat-balance project list becomes 56. CP203 adds required source-mapped `routine.zone_space_heat_balance_predict_system_load` immediately after `routine.predict_system_loads` and adds `zone_space_heat_balance_predict_system_load` after `predict_system_loads` before `calc_zone_air_temp_set_points` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::predictSystemLoad(EnergyPlusData &state, bool shortenTimeStepSys, bool useZoneTimeStepHistory, Real64 priorTimeStep, int zoneNum, int spaceNum)` is declared at `ZoneTempPredictorCorrector.hh` lines 217-222 and implemented at `ZoneTempPredictorCorrector.cc` lines 3146-3257. Its only production call expressions are CP202 line 3116 for every Zone and lines 3119-3120 for every stored Space only while `doSpaceHeatBalance` is true. After a positive-`zoneNum` debug assertion, CP203 first calls the still-deferred `updateTemperatures` child with all timestep/history arguments and both identities. It then selects Space volume only for positive `spaceNum`, otherwise Zone volume, and writes `AirPowerCap` from that volume, the parent Zone's sensible capacitance multiplier, current pressure/MAT/humidity psychrometrics, and unguarded `TimeStepSysSec`. After `calcZoneOrSpaceSums(state, false, zoneNum, spaceNum)` returns, a Zone-only hybrid branch clears and refreshes `SumIntGainExceptPeople`. CP203 directly derives `TempDepCoef`, `TempIndCoef` including `SysDepZoneLoadsLagged`, the fixed three-history `TempHistoryTerm`, and ThirdOrder-shaped `tempDepLoad`/`tempIndLoad`. A used RoomAir AirflowNetwork control node under the state-wide nonmixing flag first runs `LoadPredictionRoomAirModelAFN`, then overwrites those coefficient, capacity, history, and load values and optionally obtains `HVAC(1).SupplyFraction` as local `RAFNFrac`; this Zone-keyed override is not suppressed for a Space record. CP203 next clears shared `ShortenTimeStepSysRoomAir`. ThirdOrder performs no further direct history rewrite. Every other solution algorithm uses `shortenTimeStepSys && TimeStepSys < TimeStepZone` to select this record's `TM2`/`WM2` when shared `PreviousTimeStep < TimeStepZone`, otherwise `TMX`/`WMX`, sets corresponding AirflowNetwork-node T1 histories, and sets the shared flag true; the other path copies current `ZT`/`airHumRat` and current AFN-node values while leaving it false. These later AFN history copies test only the Zone model type, without the earlier nonmixing or `IsUsed` guards. Non-ThirdOrder finally replaces both load scalars with the plain coefficients. The last ordered calls are `calcPredictedSystemLoad` and then `calcPredictedHumidityRatio` with `RAFNFrac` and both identities. Their thermostat, sensible-demand, humidity-control, moisture-demand, diagnostic, and reporting behavior, together with temperature updating, sum assembly, psychrometrics, hybrid gain collection, and AFN load prediction, remains child dependency behavior rather than CP203 implementation credit. Apart from the debug assertion, CP203 has no local identity, upper-bound, membership, finite, timestep, volume, multiplier, node, or history validation and no local diagnostic, latch, status, catch, cleanup, or rollback. An abnormal child return can retain any completed history, capacity, hybrid clear, coefficient, AFN, shared-flag, record-history, node-history, or sensible-demand prefix; a moisture-child failure follows the already committed sensible child. Same-state retry reruns every child and direct write, and clean replay requires coordinated Zone/Space records, HVAC timestep/global state, HeatBalFanSys and node histories, RoomAir/AFN, HybridModel/internal gains, sensible/moisture demand, diagnostics, and every child owner. No C++ test calls CP203 directly; CP202's 16 focused calls retain zero Zones and never enter it. Of 56 active full simulations reaching CP202, 55 execute at least one Zone CP203 and eight enable Space CP203, but their downstream assertions do not isolate this transaction and none covers its AFN override. Rust's identity `predict_system_loads_compat` encloses a different Zone-only temperature update. Existing coefficient, moist-air-capacitance, analytical/ThirdOrder, history-interpolation, and no-OA ThirdOrder moisture helpers cover adjacent formulas only; their guarded ownership omits CP203's Space path, lagged/hybrid/AFN overrides, shared room-air shortening flag, exact T1/W1 and AFN-node history transaction, and ordered sensible-then-moisture demand children. CP203 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 211 routines, split 58 `state_mapped` plus 153 `source_mapped`, with 88 required; the heat-balance project list becomes 57. CP204 adds required source-mapped `routine.calc_zone_air_temp_set_points` immediately after `routine.zone_space_heat_balance_predict_system_load` and adds `calc_zone_air_temp_set_points` after `zone_space_heat_balance_predict_system_load` before `zone_space_heat_balance_calc_predicted_humidity_ratio` in the heat-balance project contract. `CalcZoneAirTempSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 282 and implemented at `ZoneTempPredictorCorrector.cc` lines 3259-3460. Its only production call expression is CP195 line 224 for the `GetZoneSetPoints` selector; both ordinary HVAC timestep setup and HVAC resimulation enter through that wrapper after CP196 and CP199 return. Every call first assigns the complete `TempControlType` array to Uncontrolled, conditionally allocates but never resizes the two occupied-setpoint arrays, then fills every existing heating entry with zero and cooling entry with 100. The local `DeltaT` is assigned zero and never consumed, while `TempControlTypeRpt` and the thermostat setpoint fields receive no global reset. In stored temperature-control order, CP204 trusts `ActualZoneNum` and schedule pointers, samples and casts the control-type schedule, writes the enum plus integer report, and dispatches five nominal cases. Uncontrolled leaves all setpoint fields stale. SingleHeat alone guards its family with `isUsed`, samples the heating schedule into generic `setpt`, snapshots the raw low thermostat value, applies operative-temperature adjustment, and writes only `setptLo`. SingleCool samples its cooling schedule without an `isUsed` guard, snapshots the raw high value, optionally applies adaptive comfort and snapshots `setptAdapComfortCool`, then applies operative-temperature and temperature-and-humidity overcool adjustments while leaving `setptLo` stale. SingleHeatCool samples the family's heating schedule, optionally applies adaptive comfort, applies operative control, copies the result to both bounds, and under allocated optimum-start state may replace generic `setpt` from the SingleHeat family day array at `(ceil(OccStartTime) + 1) * TimeStepsInHour` before an independent `OptStartFlag` copies it to both bounds. DualHeatCool samples and snapshots cooling first, optionally applies adaptive comfort, applies operative control, then samples, snapshots, and operative-adjusts heating; a matching optimum-start identity fills the globally reset occupied arrays from the dual day arrays, an independent flag copies those values into the bounds, and overcool runs last. The invalid-control default emits one Severe diagnostic and continues without a local fatal. After every ordinary record branch, an enabled non-warmup, non-sizing, non-kickoff thermostat-fault scan stops at the first case-insensitive thermostat-name match even when its availability is nonpositive; an available match samples optional severity and subtracts the resulting offset from generic, low, and high setpoints without changing raw or adaptive snapshots. This can accumulate on fields the selected branch did not rewrite. After the ordinary loop, comfort setpoints run conditionally and can overwrite ordinary/fault results, then the EMS override runs unconditionally with final precedence. Operative, adaptive, overcool, comfort, EMS, schedule, and fault helpers remain dependency behavior rather than CP204 implementation credit. CP204 has no Space path, local latch, status, catch, cleanup, rollback, topology/shape/index/schedule/enum/time/finite validation, or allocation reconciliation. Any allocation, schedule, diagnostic, or child non-return retains the reached reset, allocation, record, fault, comfort, or EMS prefix; same-state retry resamples schedules, resets occupied arrays, repeats children and diagnostics, and can compound offsets in untouched fields. Clean replay requires coordinated ZoneControls, HeatBalFanSys setpoint/type/report, Availability optimum-start, schedule, FaultsManager, global/environment/weather, Zone heat-balance MRT/RH, comfort/ThermalComfort, EMS, diagnostic, and child-owned state. Four C++ fixtures make 21 direct calls and 33 thermostat-field post-call assertions: the optimum-start fixture contributes one call/two assertions, the reporting fixture four/seven, and two cutout/prediction fixtures eight/twelve each. The latter calls are composed with CP202, and helper-only tests cover adaptive adjustment and EMS override; no direct helper test covers operative adjustment, overcool, or comfort calculation. Of 57 active full simulations, one expected EMS fatal stops before CP204 and the other 56 reach it; 38 contain a thermostat declaration by static input evidence, but none directly asserts post-CP204 setpoint or control state and none contains comfort, operative-temperature, overcool, optimum-start, or thermostat-fault input. Rust's `calc_zone_air_temp_set_points_compat` is an identity closure invoked once around an empty closure inside the Predict scaffold, rather than the source `GetZoneSetPoints` branch, and has no direct test. Rust types only a direct-Zone DualSetpoint thermostat subset; its IdealLoads diagnostic helper selects the first control, ignores the control-type schedule, and repeats constant schedule values outside the heat-balance runtime. It implements none of CP204's control-family selection, adjustments, optimum-start, fault, comfort, EMS, mutation order, partial effects, or owner lifecycle. CP204 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 212 routines, split 58 `state_mapped` plus 154 `source_mapped`, with 89 required; the heat-balance project list becomes 58. CP205 adds required source-mapped `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` immediately after `routine.calc_zone_air_temp_set_points` and adds `zone_space_heat_balance_calc_predicted_humidity_ratio` after `calc_zone_air_temp_set_points` before `correct_zone_air_temps` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::calcPredictedHumidityRatio(EnergyPlusData &state, Real64 RAFNFrac, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 243 and implemented at `ZoneTempPredictorCorrector.cc` lines 3462-3815. Its only production call expression is CP203 line 3256, after the sensible-demand child; every Zone and every active Space therefore runs CP205 in parent order. CP205 first derives two local RH setpoints and a controlled flag from the parent Zone. A positive `humidityControlZoneIndex` samples humidifying then dehumidifying schedules, applies independent EMS overrides, then scans the first name-matching humidistat fault outside warmup, sizing, and kickoff. A thermostat-dependent fault searches thermostat faults by object name, fatals when none exists, and only for a nonzero available severity-scaled thermostat offset converts both RH values through humidity ratio at record `MAT + offset` and back at `MAT` before clamping to [0,100]. Every other humidistat-fault type samples its own availability/severity, subtracts the offset, and clamps only when available. The first matching humidistat or thermostat fault stops its scan even when unavailable. A humidifying value above dehumidifying emits first-occurrence plus recurring warnings through the control-owned `ErrorIndex` and is reduced to the dehumidifying value; exact equality marks a single setpoint. Without a humidistat, `DoingSizing && DoLatentSizing` scans Zone equipment configurations only to the first controlled entry, does not match it to `zoneNum`, uses its matching `Sizing:Zone` or falls back to the first sizing input, and, when latent sizing is enabled there, samples or reads its RH values, silently reduces humidifying to dehumidifying, and marks the record controlled. Thus the same first controlled sizing input can drive every otherwise uncontrolled Zone and Space invocation. Controlled work adds record `latentGain` to Zone-indexed radiant-system and pool latent gains, computes moist-air density and vapor enthalpy, and assembles `A` and `B` either from Zone-indexed AirflowNetwork multizone exchange plus record surface-moisture terms or from record outdoor, ventilation, exhaust, cross-mixing, mixing, outdoor-air, and surface-moisture fields. `C` uses Space volume only for positive `spaceNum`, otherwise Zone volume, together with the parent Zone moisture-capacitance multiplier and unguarded system-timestep seconds. A parent Zone RoomAir AirflowNetwork model then replaces `A`, `B`, `C`, and vapor enthalpy from its control node without the earlier CP203 nonmixing or `IsUsed` gates, including during a Space invocation. For each RH setpoint CP205 converts record `ZT` and pressure to humidity ratio, then computes ThirdOrder, Analytical, or Euler load. ThirdOrder uses the first three `WPrevZoneTSTemp` values. Analytical uses the exact `A == 0` branch or reuses `exp(min(700, -A/C))` from the humidifying calculation for dehumidifying. Euler uses `C * (Wsp - W1) + A * Wsp - B`. Any positive `RAFNFrac` divides each load; zero, negative, or NaN does not. An unknown solution enum leaves both initialized loads zero. Exact-equal setpoints select humidifying load. Otherwise two positive loads select humidifying, two negative select dehumidifying, and the inclusive humidifying-nonpositive/dehumidifying-nonnegative deadband selects zero; every other combination emits Severe/Continue diagnostics and fatals before demand commit. The final identity branch selects Space demand only for positive `spaceNum`. Controlled state calls `reportMoistLoadsZoneMultiplier` with the parent Zone identity; that dependency stores three unmultiplied predicted rates, three Zone-multiplied demand values, and conditionally initializes sequenced equipment demand. Uncontrolled state directly zeroes only `TotalOutputRequired`, `OutputRequiredToDehumidifyingSP`, and `OutputRequiredToHumidifyingSP`, leaving raw predicted and sequenced fields stale. Counts, indices, membership, pointers, schedule/EMS/fault/sizing values, setpoint ranges, pressure, temperature, humidity, histories, volume, multiplier, timestep, flows, AFN topology, `RAFNFrac`, and finite values are otherwise trusted. Apart from the humidity-control debug assertion, CP205 has no local validation, latch, status, catch, cleanup, or rollback. A missing dependent thermostat or impossible load combination fatals before final demand write; schedule, psychrometric, allocation, diagnostic, or report-child non-return retains any earlier diagnostic, recurrence-index, or partial demand prefix. Zone demand can commit before a later Space failure. Same-state retry resamples schedules and faults, repeats calculations, advances recurring warnings, and repeats child writes; clean replay requires coordinated Zone/Space heat-balance records, ZoneControls and schedules/EMS, FaultsManager, sizing and Zone equipment, HVAC timestep, environment and psychrometrics, AFN/RoomAir, Zone/Space moisture demand including sequences, multipliers, and diagnostics. No C++ test calls CP205 directly. One helper-only fixture calls `ZoneSystemMoistureDemand::reportMoistLoadsZoneMultiplier` four times with 21 assertions, covering only Zone raw and multiplied values, not Space or sequenced output. Of 57 active full simulations, one expected EMS fatal stops before prediction and one zero-Zone weather case reaches CP202 without CP205; the other 55 execute Zone CP205 and eight also execute Space CP205. Their enclosing inputs contain no humidistat, humidistat fault, EMS humidistat override, or RoomAir AFN. Only one ThirdOrder latent-sizing configuration makes the controlled calculation positive for one Zone and three Spaces, and its assertions are downstream; six Analytical configurations and every other reached record take the uncontrolled zero path, while no Euler configuration exists. Rust has no heat-balance CP205 wrapper, call, Zone/Space moisture-demand owner, or runtime mutation. The separate `calc_no_oa_third_order_moisture_demand_compat` implements a guarded Zone-only no-outdoor-air `A = 0` ThirdOrder subset with record density/capacity, internal latent gain, two RH conversions, three-history term, sign/deadband choice, and one combined Zone multiplier. It has two non-test callers, two direct predictor tests, and one adjacent history-term test; a transactional fixed-one-step IdealLoads Humidistat wrapper adds seven tests and eight call expressions around predictor, PurchasedAir, corrector, and history push. That subset rejects invalid inputs through `Option`, clamps both RH fractions, and returns only multiplied loads. It omits source schedule/EMS/fault/sizing ownership, radiant/pool gains, airflow and surface moisture, AFN/RoomAir/RAFN scaling, Space state, Analytical/Euler, diagnostics and partial effects, uncontrolled zero writes, raw predicted values, and sequenced demand. Its CLI path still consumes EnergyPlus-seeded histories, temperatures, sensible demand, and pressure; existing manifest-specific no-OA IdealLoads evidence does not state-map CP205. CP205 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 213 routines, split 58 `state_mapped` plus 155 `source_mapped`, with 90 required; the heat-balance project list becomes 59. CP206 adds required source-mapped `routine.correct_zone_air_temps` immediately after `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` and adds `correct_zone_air_temps` after `zone_space_heat_balance_calc_predicted_humidity_ratio` before `zone_space_heat_balance_correct_air_temp` in the heat-balance project contract. `correctZoneAirTemps(EnergyPlusData &state, bool useZoneTimeStepHistory)` is declared at `ZoneTempPredictorCorrector.hh` lines 289-291 and implemented at `ZoneTempPredictorCorrector.cc` lines 3817-3861. Its only production direct call is CP195 line 230, whose CorrectStep arm assigns the returned value to caller-owned `ZoneTempChange` only after CP206 returns. The ordinary HVAC parent runs initial Get, Predict, HVAC simulation, and then Correct. It selects adaptive downstepping only when the initial returned maximum exceeds `MaxZoneTempDiff` and `KickOffSimulation` is false; otherwise it uses one system step and Zone-timestep history. Each fine step repeats Predict, HVAC simulation, Correct, contaminant correction, and system-history push without recalculating the chosen count from later returns. Demand resimulation calls Get and Predict but not Correct. CP206 initializes its local maximum to zero and traverses Zones in ascending identity. For each Zone it first calls `ZoneSpaceHeatBalanceData::correctAirTemp` with the history selector and saves the result without folding it. It then visits every stored Space identity regardless of Space-HB flags. `doSpaceHeatBalanceSimulation && !DoingSizing` calls the Space correction child and immediately folds that result in Space order. Every other case may copy the controlled Zone system node Temp, HumRat, and Enthalpy to the Space node when `doSpaceHeatBalanceSizing && Zone.IsControlled`, a condition with no direct `DoingSizing` test, and then always copies Zone `ZT`, complete `ZTM`, `MAT`, `airHumRat`, and `airRelHum` into the Space record while deliberately omitting `ZTAVComf`. Only after all Spaces does CP206 fold the saved Zone result. It then always calls `CalcZoneComponentLoadSums` for the Zone, and when simulation Space HB is enabled it makes a second stored-Space traversal and calls that report child even during sizing when Space correction was bypassed. The history selector is only forwarded; shortening and prior-timestep inputs never enter CP206. The wrapper applies no absolute value, tolerance, or finite guard. Normal child returns are nonnegative absolute changes, but with the accumulator as the first `std::max` argument and every candidate second, negative and NaN candidates are ignored, positive infinity can be retained, and nonpositive Zone count returns zero. CP206 directly persists only the optional three Space-node values and five mirrored Space-HB values; correction, moisture, hybrid, and component-report mutations remain child behavior. It has no local assertion, count, identity, membership, arena, node, finite, or topology validation, diagnostic, stop flag, status, latch, catch, cleanup, transaction, or rollback. A Zone-child non-return suppresses that Zone remainder and later Zones. A Space-child failure retains the completed Zone child and earlier Spaces but prevents the Zone fold and reports. Node/mirror failure can retain a direct-write prefix, and report failure retains completed correction, copies, and earlier report effects. The local maximum is not externally visible on failure, and CP195 leaves the old caller value because its assignment has not completed. Same-state retry restarts at Zone one and repeats all children, copies, and reports; clean replay requires coordinated predictor/corrector, Zone/Space heat balance, node, topology, RoomAir, HVAC, report, diagnostic, and child-owner reset. One C++ HybridModel fixture makes five direct calls, all with Zone history true, one Zone, one stored Space, and false Space-HB flags. It therefore reaches Zone correction and five-field Space mirroring, but its five assertions check only hybrid child effects and never the returned maximum, mirror state, component reports, false-history path, failure, retry, or reset. Of 57 active full simulations, one expected EMS fatal stops before CP206, one zero-Zone weather case reaches the zero return, and 55 execute Zone correction. One Analytical configuration positively reaches active simulation-Space correction and Space reporting for two Zones and three Spaces, while seven sizing configurations reach the controlled node triplet and five-field mirror for one Zone and three Spaces each; their assertions are downstream topology or sizing evidence. One thermal-comfort simulation checks later Zone averages, but no test isolates CP206 maximum folding, adaptive step selection, component-report dispatch, failure, retry, or reset. Rust defines `correct_zone_air_temps_compat` only as a generic identity alias over `correct_step_source_order_path`. Its one non-test call and zero direct tests wrap a closure that returns unit, performs an all-Zone temperature pass followed by an all-Zone humidity pass, then optionally performs per-Zone adaptive correction or local history synchronization. It has no Space HB or node state, AirReportVars, component-report child, source history-selector input, or returned global maximum. The lower CorrectStep identity has additional calls during surface iteration and adaptive work that bypass the named CP206 wrapper. Rust chooses adaptive substeps independently per Zone from a hard-coded temperature threshold and repeats only local temperature/humidity work rather than the source global maximum and full Predict-HVAC-Correct retry. Existing coefficient, ThirdOrder, Analytical, history, storage-report, and bounded official 1ZoneUncontrolled evidence belongs to adjacent Zone-only child formulas and case-specific outputs, not CP206 topology or lifecycle parity. Authored Space execution, sizing, EMS, Python, and AirflowNetwork remain run-blocked. CP206 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 214 routines, split 58 `state_mapped` plus 156 `source_mapped`, with 91 required; the heat-balance project list becomes 60. CP207 adds required source-mapped `routine.zone_space_heat_balance_correct_air_temp` immediately after `routine.correct_zone_air_temps` and adds `zone_space_heat_balance_correct_air_temp` after `correct_zone_air_temps` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctAirTemp(EnergyPlusData &state, bool useZoneTimeStepHistory, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` lines 236-239 and implemented at `ZoneTempPredictorCorrector.cc` lines 3863-4165. Its only production direct call expressions are CP206 line 3825 for each Zone with default zero Space identity and line 3831 for positive active Spaces under `doSpaceHeatBalanceSimulation && !DoingSizing`; the Zone always precedes eligible Spaces. CP207 starts its local delta at zero, debug-asserts only positive `zoneNum`, reads the parent Zone multiplier product and system-timestep seconds, then copies complete `DSXMAT`/`DSWPrevZoneTS` histories when the selector is false or `XMAT`/`WPrevZoneTS` when true. Positive `spaceNum` alone selects Space volume; otherwise Zone volume. It overwrites `AirPowerCap` from that volume, parent sensible-capacitance multiplier, density at pre-correction `MAT` and `airHumRat`, specific heat, and unguarded timestep seconds. Exact zero alone calls `RoomAir::ManageAirModel`, then every identity calls `calcZoneOrSpaceSums` with the corrector flag true. `FlagHybridModel_PC` refreshes the current record's `SumIntGainExceptPeople` from the parent Zone without a Space guard. Positive Space alone selects its system node; a positive node, not `Zone.IsControlled`, selects the controlled branch. Controlled `TempDepCoef` is `SumHA + SumMCp + SumSysMCp`, while `TempIndCoef` adds internal and surface terms, non-system and system temperature terms, divided non-air response, lagged system load, and optional parent-Zone AFN sensible exchange and duct loss. The uncontrolled branch omits system flow, non-air response, and lagged system load but retains optional AFN and duct additions. Both branches dispatch unguarded ThirdOrder, Analytical, or Euler equations. Analytical tests exact zero dependence and caps its exponent with source-ordered `min(700, candidate)`; an unknown enum silently retains old `ZT` and continues. In the controlled node stage, a globally mixed or inactive model writes node temperature, Zone-only thermostat temperature, and correction factor one. Three-node displacement or UFAD has precedence and, like active UserDefined or one-node displacement, leaves node and thermostat untouched while conditionally computing the Zone-shared correction factor from supply temperature and existing node temperature; ordered `max(-3, raw)` then `min(3, value)` maps NaN raw to -3 and clamps infinities. AirflowNetwork replaces `ZT` from the parent control node and performs mixed writes; fallback also performs mixed writes. A Space uses its own node but parent RoomAir flags and can overwrite the Zone-shared correction factor after the Zone child, while never writing the thermostat. Controlled `SNLoad` uses `SumSysMCpT`, node mass flow and temperature divided by the Zone multiplier, record humidity specific heat, non-air response, and lagged load. The uncontrolled branch leaves zero load and shared node/control fields stale; only an exact uncontrolled Zone under global nonmixing and the AirflowNetwork enum replaces `ZT`, without testing `SimAirModel`. Exact Zone identity next conditionally calls `InverseModelTemperature` for enabled temperature inverse modes outside warmup and sizing. This occurs after node and load calculation, can replace `ZT` with measured temperature, and reads the global history flag rather than CP207's argument, so later `MAT`, humidity, RH, and delta can disagree with earlier node, thermostat, and load state. CP207 then writes `MAT = ZT`, reports sensible load to Space demand only for positive identity and otherwise Zone demand, calls the later-defined `correctHumRat` child, copies `airHumRatTemp` to `airHumRat`, and computes `airRelHum` through `PsyRhFnTdbWPb` without a caller-side clamp. Sensible demand commits before humidity. Returned change treats every Space as mixed. Exact Zone nonmixed classification recognizes only unmixed three-node displacement or UFAD flags. ThirdOrder compares `ZT` with `ZTM[0]` or the two room-air histories; Analytical and Euler compare with `T1` or the corresponding room-air one-step histories. Unknown enum returns zero. The outer `std::max` has zero first, so NaN is suppressed and positive infinity retained; in a nested nonmixed fold, NaN in the first child suppresses a finite second child while a finite first child survives a NaN second child. A malformed negative Space identity is accepted and combines Zone volume, node, and demand with skipped exact-zero RoomAir, thermostat, hybrid, AFN override, and nonmixed classification; production never supplies it. Direct record writes are the selected histories, capacitance, optional except-People sum, coefficients, `ZT`, `MAT`, humidity, and RH; direct shared writes are applicable node temperature, Zone thermostat, and Zone-shared correction factor. RoomAir, sum assembly, hybrid, sensible demand, humidity, and psychrometric diagnostics remain dependencies. Apart from the Zone assertion, CP207 has no local identity, bound, membership, arena, node, history, enum, topology, multiplier, volume, time, denominator, or finite validation, diagnostic, latch, status, catch, cleanup, transaction, or rollback. A failure can retain any ordered prefix through history, capacity, sums, coefficients, node/control, hybrid, `MAT`, sensible demand, humidity, or final RH; any non-return prevents CP206 from folding the delta. Same-state retry recomputes from already-mutated record, node, RoomAir, correction-factor, demand, and hybrid history state, and clean replay requires coordinated reset of all those owners plus AFN, duct, HVAC/environment, diagnostics, and children. No C++ test calls CP207 directly. One HybridModel fixture reaches its controlled, mixed, default-ThirdOrder Zone path five times with history true and asserts only hybrid child outputs. Helper-only tests call `correctHumRat` ten times and `calcZoneOrSpaceSums` five times but do not compose CP207. Of 57 active full simulations, one EMS fatal stops before it and one has zero Zones; the other 55 reach 81 statically declared Zone records, split 55 controlled and 26 uncontrolled. Forty-nine configurations select ThirdOrder for 74 records, six select Analytical for seven, and none selects Euler. One Analytical configuration reaches two uncontrolled Zones and three uncontrolled Spaces, but no controlled Space. One AFN-distribution and one duct-loss configuration reach their optional terms without asserting CP207 coefficients. No full simulation declares RoomAir or HybridModel input, and no focused assertion covers false history, solver output, node/control writes, sensible demand, RH, returned delta, failure, retry, or reset. Rust has no named `correct_air_temp` wrapper or direct CP207 test. Its all-Zone surface-driven temperature corrector has four non-test calls and no direct test; its adaptive single-Zone corrector has one non-test call and no direct test. The live Correct closure performs one all-Zone temperature pass, then a separate all-Zone humidity pass, then project-specific adaptive/history work instead of CP207's per-record transaction. The coefficient helper has five non-test calls and one direct fixture; the public ThirdOrder helper has no production call and one direct fixture, its coefficient solver has four non-test calls and no direct test, and the Analytical helper has three non-test calls and one direct fixture. There is no Euler helper. Rust preserves limited normal-finite Zone coefficient, ThirdOrder-history, and Analytical algebra, but combines system and non-system flow without a controlled-node decision; those runtime flow fields have no production writers and surface `SumHATref` is fixed zero. It omits exact sum assembly, non-air and lagged loads, AFN, duct, Space state, nodes, RoomAir, thermostat, correction factor, sensible demand, hybrid, distinct `ZT`/`MAT`/`T1`, RH, and returned delta. Rust capacity omits the sensible-capacitance multiplier and Space selection, can retain stale state on rejected weather/psychrometrics, zeroes AirPowerCap for nonpositive inputs, guards a near-zero ThirdOrder denominator, uses epsilon rather than exact Analytical zero, and has no source unknown-enum behavior. Its history selector is recorded but not consumed, and its separate saturation-clamped/defaulting humidity approximation is not `correctHumRat`. Existing formula tests and bounded official `1ZoneUncontrolled` MAT evidence remain adjacent case-specific evidence. Authored Space, sizing, EMS, Python, and AFN paths remain run-blocked. CP207 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 215 routines, split 58 `state_mapped` plus 157 `source_mapped`, with 92 required; the heat-balance project list becomes 61. CP208 adds required source-mapped `routine.push_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_correct_air_temp` and adds `push_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 293 and implemented at `ZoneTempPredictorCorrector.cc` lines 4167-4185. Its sole built-in direct call is CP195 line 236; the sole built-in selector request is `HVACManager::ManageHVAC` lines 579-584 after all system steps and Zone/Space comfort-average snapshots, and before the separate contaminant push, last-system-step-count commit, and demand-manager update. The dispatcher common input/init prefix runs first, but the CP208 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP208 aliases the predictor/corrector state, visits Zones 1 through `NumOfZones`, calls the Zone record child first, and only under the current aggregate `doSpaceHeatBalance` flag visits that Zone's stored `spaceIndexes` in container order and calls the Space child with both identities. It does not independently scan all Spaces, sort, deduplicate, or validate counts, arenas, membership, or identity consistency; a nonpositive Zone count is a no-op. Sizing can set the aggregate flag from `doSpaceHeatBalanceSizing` and normal simulation resets it from `doSpaceHeatBalanceSimulation`. CP208 itself reads traversal state but performs no direct history or numerical writes; all four-slot record, psychrometric, RoomAir, and AFN mutations belong to CP209. It has no local assertion, diagnostic, status, latch, catch, cleanup, transaction, or rollback. A child non-return retains every earlier child and the failing child's prefix, suppresses later traversal, and same-state retry shifts already-shifted histories again. No C++ unit calls CP208, its record child, `ManageZoneAirUpdates`, or `ManageHVAC` directly. Of 57 active `ManageSimulation` call expressions, one expected EMS fatal stops before CP208; the other 56 reach the wrapper, including one zero-Zone case, while the remaining 55 collectively span a static census of 81 Zone identities. Eight configurations collectively enable a static census of 24 Space identities, one in simulation and seven during sizing, but no assertion isolates the Zone/Space history shift, order, failure, retry, or reset. Rust has the selector name and identity source-order/compat wrappers, but production never passes the PushZone selector and the dispatcher ignores its selector. The one live compat call runs at the start of the Predict closure, shifts only three Zone temperature and humidity slots, chooses average versus current input by a project-specific adaptive flag, and encloses predictor work rather than running after the HVAC system loop. Rust has no Space heat-balance arena, membership traversal, fourth history slot, or CP209 RoomAir/AFN and auxiliary record state. One generic wrapper test checks only the wrapper call label and order in a larger vector; one uniform-temperature timestep assertion cannot distinguish slot movement, and there is no direct compat or nonuniform humidity-history test. Separate IdealLoads humidity history is not CP208 parity. CP208 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 216 routines, split 58 `state_mapped` plus 158 `source_mapped`, with 93 required; the heat-balance project list becomes 62. CP209 adds required source-mapped `routine.zone_space_heat_balance_push_zone_timestep_history` immediately after `routine.push_zone_timestep_histories` and adds `zone_space_heat_balance_push_zone_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushZoneTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 245 and implemented at `ZoneTempPredictorCorrector.cc` lines 4187-4275. Its only direct production expressions are CP208 lines 4178 and 4181, with every Zone first and positive eligible Spaces afterward. After a debug-only positive-Zone assertion, it aliases the parent AirModel before any mutation. It interleaves descending four-slot `XMAT` and `WPrevZoneTS` shifts, inserts `ZTAV` and `airHumRatAvg` at slot zero, writes `XMPT = ZT`, commits `airHumRat` and `WTimeMinusP` from `airHumRatTemp`, then writes percent `airRelHum` from `PsyRhFnTdbWPb` using `ZT`, the newly committed humidity, barometric pressure, and diagnostic label `pushTimestepHistories`. The helper floors only its local working humidity to 1e-5, can clamp a comparison-detected out-of-range RH, and can propagate NaN; CP209 stores the original humidity and applies no caller-side finite or range guard. Exact zero Space identity alone enables RoomAir. Three-node displacement and UFAD shift four-slot Floor, occupied, and mixed temperature histories and copy current temperatures to their MAT fields. AirflowNetwork shifts four-slot temperature and humidity histories for every stored node. These branches test the enum without an activity guard. Every solution enum except exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` and `WM2/WMX` from averages; exact-Zone stratified RoomAir advances its three M2/MX pairs and AFN advances per-node T2/TX temperature and humidity pairs. ThirdOrder still performs the common record and Zone RoomAir/AFN four-slot work. Any nonzero Space identity, including a malformed negative value, skips shared RoomAir while retaining common and non-ThirdOrder record writes. CP209 has no upper-bound, record-kind, membership, allocation, enum, topology, pressure, or finite validation and no local diagnostic beyond the psychrometric dependency, status, catch, cleanup, transaction, or rollback. An abnormal non-return after record mutation, for example from malformed psychrometric dependency or RoomAir/AFN indexing state, can leave the common prefix through `WTimeMinusP` with stale RH or retain later record and RoomAir prefixes. Retry shifts already shifted histories again, and the later revert routine is not a full inverse. No C++ test directly calls CP209 or asserts any destination. Fifty-five completing nonzero-Zone configurations collectively span 105 static record identities: 95 ThirdOrder and 10 Analytical, with no Euler. All 81 Zone identities use Mixing RoomAir; 24 Space identities skip RoomAir, so every special RoomAir/AFN branch has zero active full-simulation reach. Tracked official one-Zone outputs are downstream-only and do not isolate this commit. Rust has no singular record helper, call, or test. Its one live plural-wrapper closure shifts only three Zone temperature and humidity slots before predictor work, conditionally chooses average versus current input, and lacks Space, the fourth slot, `XMPT`, temporary/previous-system humidity scalars, RH, non-ThirdOrder record state, and all RoomAir/AFN histories. One uniform history assertion cannot identify the shift; separate system-step and IdealLoads histories are not CP209 parity. CP209 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 217 routines, split 58 `state_mapped` plus 159 `source_mapped`, with 94 required; the heat-balance project list becomes 63. CP210 adds required source-mapped `routine.push_system_timestep_histories` immediately after `routine.zone_space_heat_balance_push_zone_timestep_history` and adds `push_system_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushSystemTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 295 and implemented at `ZoneTempPredictorCorrector.cc` lines 4277-4295. Its only built-in direct call is CP195 lines 238-240; its only built-in selector request is `HVACManager::ManageHVAC` lines 388-393 inside each system iteration's strict `TimeStepSys < TimeStepZone` branch, after Predict/HVAC/Correct and before the separate contaminant push, `PreviousTimeStep` commit, and current-step average accumulation. Equal-step and kickoff paths do not request it. The dispatcher common prefix runs first, but the CP210 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP210 visits Zones in ascending order, calls each Zone child before stored eligible Spaces under the current aggregate `doSpaceHeatBalance` flag, and does not independently scan, sort, deduplicate, or validate topology. It performs no direct history write; CP211 owns downstepped record, RoomAir/AFN, and non-ThirdOrder mutations. It has no local assertion, diagnostic, status, catch, cleanup, transaction, or rollback. An abnormal child non-return retains completed and failing prefixes, suppresses later traversal and the parent's following contaminant/time/average work, and retry shifts completed system histories again; Zone-timestep revert does not restore them. No C++ test directly calls CP210 or its child, and no destination assertion or tracked oracle proves the numerical adaptive gate; 55 completing nonzero-Zone configurations provide only potential topology of 81 Zone and 24 eligible Space identities, not execution evidence. Rust has the selector name and generic wrappers, but production never dispatches the PushSystem selector. Production has exactly three lexical compat sites. The feature-disabled site is exclusive with the adaptive helper; when adaptive correction is enabled, each Zone independently takes either the count-one or count-greater-than-one site, so both adaptive sites can execute in one multi-Zone timestep. Exactly one site executes per Zone, rebuilding three slots even at a full Zone step or committing after all locally chosen adaptive substeps rather than performing the source per-fine-step global Zone/Space pass. Rust lacks Space topology, a fourth slot, and CP211 RoomAir/AFN and auxiliary state; the source-order wrapper has one direct label/order test, while the compat wrapper and PushSystem selector dispatch have none. One indirect count-one three-slot sync test does not prove CP210. CP210 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 218 routines, split 58 `state_mapped` plus 160 `source_mapped`, with 95 required; the heat-balance project list becomes 64. CP211 adds required source-mapped `routine.zone_space_heat_balance_push_system_timestep_history` immediately after `routine.push_system_timestep_histories` and adds `zone_space_heat_balance_push_system_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushSystemTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 247 and implemented at `ZoneTempPredictorCorrector.cc` lines 4297-4370. Its only production calls are CP210 lines 4288 and 4291 for each selected Zone and eligible stored Space. After a debug-only positive-Zone assertion, CP211 interleaves descending four-slot `DSXMAT` and `DSWPrevZoneTS` shifts and inserts current `MAT` and `airHumRat`. Exact Zone identity under the global nonmixing flag also advances applicable Floor/occupied/mixed four-slot temperature histories or each stored AirflowNetwork node's four-slot temperature and humidity histories. Every solution value other than exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` from `MAT` and `WM2/WMX` from distinct `airHumRatTemp`; exact Zones additionally advance applicable stratified or AirflowNetwork two-slot state. ThirdOrder skips only this final stage. CP211 has no upper-bound, Space-sign, membership, record-kind, topology, node-count, enum, or finite validation and no diagnostic, status, catch, cleanup, transaction, or rollback. A late abnormal non-return retains an ordered record or RoomAir/AFN prefix, blocks the rest of CP210 and its parent's following contaminant/time/average work, and same-state retry shifts completed histories again; the following Zone-timestep revert does not restore this downstepped state. No C++ test directly calls CP211 or asserts a destination. Fifty-five nonzero-Zone configurations provide only conditional potential topology if one CP210 pass is selected: 105 records split 95 ThirdOrder and 10 Analytical, with no Euler; all 81 Zones are Mixing and 24 Spaces skip shared RoomAir, so special RoomAir/AFN branches have zero corpus potential. Rust has no singular record helper, Space arena, fourth slot, `airHumRatTemp`, non-ThirdOrder scalar state, RoomAir, or AFN histories. Its count-one and feature-disabled helpers rebuild three Zone slots from current and Zone-timestep histories even when source CP211 is absent, while adaptive count-greater-than-one shifts local three-slot arrays after each Zone-local correction outside the named wrapper and commits them only once after all local substeps. One source-order label test and one indirect count-one rebuild test do not establish CP211; there is no adaptive-count-greater-than-one commit test. CP211 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 219 routines, split 58 `state_mapped` plus 161 `source_mapped`, with 96 required; the heat-balance project list becomes 65. CP212 adds required source-mapped `routine.revert_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_push_system_timestep_history` and adds `revert_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `RevertZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 297 and implemented at `ZoneTempPredictorCorrector.cc` lines 4372-4389. Its only direct thermal source call is the CP195 dispatcher arm at lines 232-234, while all nine built-in `ManageZoneAirUpdates` call sites and the test tree request other selectors; EnergyPlus 26.1 therefore has no built-in CP212 timing or runtime gate. An external dispatcher request would still run the common input/init prefix, ignore shortening, history-selector, and prior-step arguments, and preserve `ZoneTempChange`. CP212 visits Zones ascending, calls each Zone child first, then under the current aggregate `doSpaceHeatBalance` flag visits stored Spaces in container order. It does not scan, sort, deduplicate, validate topology, or write history directly; CP213 owns the `XMAT`/`WPrevZoneTS` and optional RoomAir/AFN mutations. It never restores CP211 `DSXMAT`/`DSWPrevZoneTS`, non-ThirdOrder scalars, or downstepped RoomAir/AFN state. CP212 has no assertion, diagnostic, status, completion count, catch, cleanup, transaction, or rollback. A child abnormal non-return retains completed and failing prefixes and suppresses later traversal; same-state retry reapplies CP213's forward copies and is destructive. No C++ test directly calls the wrapper or child or asserts a destination, and all 57 active `ManageSimulation` expressions execute CP212 zero times. The 81 Zone plus 24 eligible Space records are only counterfactual once-requested topology, not full-simulation reach evidence; special RoomAir/AFN topology remains absent. Rust has the selector label and one source-order/compat identity pair, but its dispatcher ignores the selector. Its sole live compat site runs per Zone only on the adaptive count-greater-than-one path and conditionally overwrites current mean temperature and humidity when the new count differs; it shifts no Zone history, has no Space traversal, and can run despite the source branch being dormant. The one source-order label test has no state assertion, and the only focused adaptive test takes count one and never reaches the site. CP212 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 220 routines, split 58 `state_mapped` plus 162 `source_mapped`, with 97 required; the heat-balance project list becomes 66. CP214 adds required source-mapped `routine.zone_space_heat_balance_correct_hum_rat` immediately after `routine.zone_space_heat_balance_revert_zone_timestep_history` and adds `zone_space_heat_balance_correct_hum_rat` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctHumRat(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 241 and implemented at `ZoneTempPredictorCorrector.cc` lines 4433-4619. Its sole production expression is CP207 line 4128, after temperature, `MAT`, and sensible reporting but before record humidity/RH commit and returned delta. Each correction pass visits every Zone and only active non-sizing simulation Spaces; initial and adaptive fine-step HVAC corrections can repeat it. CP214 selects controlled, return-plenum, supply-plenum, or zero primary flow, then independently adds positive parallel-PIU leakage. The literal PIU loop uses global ordinals one through the stored list size rather than the identities in `leakageParallelPIUNums`. It builds latent gain, density, vapor enthalpy, default moisture `A/B`, optionally replaces them with AFN multizone exchange, uses parent-Zone volume for `C` even on Space calls, then independently adds AFN distribution and duct latent terms. ThirdOrder, Analytical, and Euler equations write `airHumRatTemp`; an unmatched enum retains its prior value. Strict negative and saturation clamps run before a parent-Zone RoomAir AFN control-node override that also affects Spaces and is not reclamped. Exact-Zone hybrid inference can mutate measured humidity state without changing the temporary solution; a positive selected node receives humidity before enthalpy, and latent sizing reports raw latent gain with earlier sensible demand. Positive Spaces retain parent Zone equipment, multiplier, capacity, AFN, duct, radiant/pool, and RoomAir context; malformed negative Space identities use Zone node/demand state while skipping only the exact-Zone hybrid gate. CP214 has only a debug positive-Zone assertion and no bounds, membership, topology, allocation, multiplier, timestep, pressure, denominator, enum, or finite validation, status, catch, transaction, or rollback. Failure can retain solved humidity, hybrid, node, or demand prefixes after the parent `MAT` and sensible writes while blocking final record humidity/RH and later traversal; retry can resample schedules and repeat histories, reports, and diagnostics. Ten direct C++ calls plus five indirect focused calls cover only Euler node humidity and hybrid inference slices. Fifty-five nonzero-Zone corpus configurations provide a static one-pass census of 81 Zone plus three active Space records, split 74 ThirdOrder and ten Analytical with no Euler. Fifty-five records are controlled and 29 have no controlled primary flow; plenum/PIU topology is absent, while AFN replacement can reach five Zones, AFN distribution three, and duct latent addition three without an isolating assertion. Runtime cadence is larger and no assertion isolates the full transaction. Rust has no singular Zone/Space helper. Its main and adaptive heat-balance humidity paths use three-slot history only, directly write Zone humidity, enforce a `1e-5` floor and `0.008` invalid fallback, and omit source moisture coefficients and lifecycle effects. The separate `correct_no_oa_third_order_humidity_ratio_compat` is a guarded single-Zone IdealLoads subset for one purchased-air supply with exact ThirdOrder `A/B/C` and clamps, but omits all other branches, Space/node/RH/hybrid/sizing ownership, and source failure semantics. Existing case-bounded humidity claims remain unchanged. CP214 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 222 routines, split 58 `state_mapped` plus 164 `source_mapped`, with 99 required; the heat-balance project list becomes 68. CP215 adds required source-mapped `routine.down_interpolate_4_history_values` immediately after `routine.zone_space_heat_balance_correct_hum_rat` and adds `down_interpolate_4_history_values` before `update_final_surface_heat_balance` in the heat-balance project contract. The first scalar-output `DownInterpolate4HistoryValues` overload is declared at `ZoneTempPredictorCorrector.hh` lines 299-308 and implemented at `ZoneTempPredictorCorrector.cc` lines 4621-4702. It snapshots three inputs by value, writes five referenced outputs in order, tests strict ratio-two then ratio-three tolerance bands, and uses a sequential recurrence for every other ratio without validating timesteps, shortening direction, integer ratio, finite values, or aliasing. Its only production expressions are the CO2 then generic-contaminant calls in `PredictZoneContaminants`; each Zone reaches them only on the first shortened adaptive predictor when the system-step count changed and the matching species is enabled. One direct C++ ratio-two call asserts five outputs. Three contaminant predictor fixtures keep shortening false, and all 57 active full-simulation expressions have contaminant simulation disabled, so focused indirect and corpus reach are zero. Rust's nearest helper returns only the first three thermal Zone history values, rejects nonpositive timesteps, has two adaptive temperature/humidity call sites and ratio-two/three/four tests, and owns no contaminant state or five-output reference transaction. CP215 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required; the heat-balance project list becomes 69. CP216 expands that same logical routine mapping to the independent array-return `DownInterpolate4HistoryValues` overload at `ZoneTempPredictorCorrector.cc` lines 4704-4736. It computes the raw old/new timestep ratio before output, writes four array elements in strict ratio-two, then ratio-three, then fallback order, and returns `oldVals[0]` only after the writes. The fourth input is never read, the third is used only for the ratio-two final output, and no timestep, finite-value, shortening, integer-ratio, or input/output alias validation is performed. A same-array call exposes live-reference aliasing and makes retry generally non-idempotent, while stable distinct-array replay overwrites deterministically. Seven production expressions in `ZoneSpaceHeatBalanceData::updateTemperatures` provide two base temperature/humidity calls per eligible Zone or active Space, three exact-Zone displacement/UFAD calls, and two calls per exact-Zone AFN node. They require shortening and a system-step count different from the prior Zone timestep, so normal HVAC timing permits only the first shortened fine-step candidate; a matching count reuses prior downstepped arrays. One direct C++ ratio-two call asserts the return, all four outputs, and four unchanged inputs. Focused wrapper calls have zero Zones, while the 55 completing nonzero-Zone corpus configurations leave adaptive entry unobserved; their conditional one-pass topology is 81 Zones plus 24 eligible Spaces, or 210 base calls, with zero stratified/AFN potential because all Zones are Mixing. Rust's nearest helper returns only three values by value, rejects nonpositive timesteps, and has Zone-only adaptive temperature/humidity ownership without the fourth output, separate scalar return, Space/RoomAir/AFN topology, node rollback, or alias behavior. CP216 adds no second routine, project-contract item, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion; counts remain 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required, and the heat-balance project list remains 69. CP217 adds required source-mapped `routine.inverse_model_temperature` and project-contract item after `down_interpolate_4_history_values`. `InverseModelTemperature`, declared at `ZoneTempPredictorCorrector.hh` lines 313-325 and implemented at `ZoneTempPredictorCorrector.cc` lines 4737-4951, samples measured temperature and resets the current mass multiplier, conditionally overwrites `ZT`, runs independent infiltration, internal-mass, and people inverse branches under global Zone-history selection, then shifts three measured histories even outside the date window or during adaptive fine-step calls. Its sole exact-Zone production expression is inside `correctAirTemp`; five indirect focused calls assert only one multiplier, two infiltration rates, and two zero people counts, while all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed `HybridModel:Zone`, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP217 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 224 routines, split 58 `state_mapped` plus 166 `source_mapped`, with 101 required, and the heat-balance project list becomes 70. CP218 adds required source-mapped `routine.process_inverse_model_multp_hm` and project-contract item after `inverse_model_temperature`. `processInverseModelMultpHM`, declared at `ZoneTempPredictorCorrector.hh` lines 327-333 and implemented at `ZoneTempPredictorCorrector.cc` lines 4953-4991, clamps only values below one, warns but does not cap values above 30, includes every value above one in persistent sum/count/average state, and owns a per-Zone first/recurring diagnostic index. Its sole production call is CP217 internal-mass inference; one direct five-call fixture has 26 assertions and proves an uncapped 50 is aggregated, one indirect call asserts only current multiplier 15.13, and all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed HybridModel object, inferred multiplier, aggregates, recurring index, exact output/table, runtime path, or focused test, and the raw object remains run-blocking. CP218 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 225 routines, split 58 `state_mapped` plus 167 `source_mapped`, with 102 required, and the heat-balance project list becomes 71. CP219 adds required source-mapped `routine.inverse_model_humidity` and project-contract item `inverse_model_humidity` immediately after `process_inverse_model_multp_hm`. `InverseModelHumidity`, declared at `ZoneTempPredictorCorrector.hh` lines 335-343 and implemented at `ZoneTempPredictorCorrector.cc` lines 4993-5131, unconditionally samples measured humidity, conditionally runs infiltration and people inverse balances, and always shifts measured histories. Its sole exact-Zone production call is inside `correctHumRat`; four indirect fixture calls assert only two ACH and two people results, while all 57 active full simulations configure no HybridModel. Rust has no typed `HybridModel:Zone`, measured humidity history, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP219 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 226 routines, split 58 `state_mapped` plus 168 `source_mapped`, with 103 required, and the heat-balance project list becomes 72. CP220 adds required source-mapped `routine.zone_space_heat_balance_calc_zone_or_space_sums` and project-contract item `zone_space_heat_balance_calc_zone_or_space_sums` after `inverse_model_humidity`. `ZoneSpaceHeatBalanceData::calcZoneOrSpaceSums`, declared at `ZoneTempPredictorCorrector.hh` lines 226-230 and implemented at `ZoneTempPredictorCorrector.cc` lines 5133-5281, reconstructs internal, non-system, system, and virtual surface sums for predictor and correction passes. It uses parent-Zone AFN/equipment/plenum/PIU context for Space records, includes the literal PIU-ordinal anomaly, volume-scales only uncontrolled-Space system sums, and can repeat child Window side effects. One five-call C++ Zone fixture has 12 bounded assertions; the 55 nonzero-Zone corpus configurations provide static one-pass censuses of 105 prediction and 84 correction records but no controlled-Space, plenum, or PIU topology. Rust has only adjacent Zone opaque-surface and OtherEquipment subsets with zero-initialized flow fields, no matching routine or Space/airflow topology, and no promotion follows. The inventory becomes 32 algorithms and 227 routines, split 58 `state_mapped` plus 169 `source_mapped`, with 104 required; the heat-balance project list becomes 73. CP221 adds required source-mapped `routine.zone_heat_balance_calc_sum_hat` and project-contract item `zone_heat_balance_calc_sum_hat` after `zone_space_heat_balance_calc_zone_or_space_sums`. `ZoneHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 254 and implemented at `ZoneTempPredictorCorrector.cc` lines 5283-5298, debug-asserts Zone identity, zero-initializes four return fields, and folds every stored Space child in container order without gating, sorting, filtering, deduplication, or membership validation. It owns no persistent write, but child Window effects can survive failure and repeat on retry. One CP220 fixture reaches it five times and asserts six aggregate surface values. The 55 nonzero-Zone corpus configurations give static prediction and correction censuses of 81 Zone aggregates dispatching 99 stored children per pass. Rust directly folds Zone opaque-Surface indexes into only HA/HATsurf/HATref=0, with no four-field result, Space topology, child dispatch, or exact test; existing one-Zone output evidence does not promote this routine. The inventory becomes 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list becomes 74. CP222 expands the existing required source-mapped `routine.zone_heat_balance_calc_sum_hat` to the independent Space override without a second routine or project-contract item because both C++ definitions share the unqualified source identifier `calcSumHAT`. `SpaceHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 259 and implemented at `ZoneTempPredictorCorrector.cc` lines 5300-5413, debug-asserts positive identities, zero-initializes four fields, visits the selected Space inclusive Surface range without filtering, applies ordered Window gains/report mutations and frame/divider/base convection, and dispatches ZoneMean, Adjacent, ZoneSupply, or silent-default reference-air logic using parent-Zone supply state. Its uncontrolled ZoneSupply branch is fatal, and its airflow/no-return `SurfWinHeatGain +=` side effect makes replay and nested-plus-explicit duplicate visits generally non-idempotent. No direct test exists; one CP220 fixture reaches a one-Space, three-non-Window child five times and asserts six surface aggregates, while the static corpus census is 123 prediction and 102 correction calls, or 225 combined. Rust's nearest Zone-only opaque Surface fold returns only HA/HATsurf/HATref=0 and has no Space result, Window runtime, reference switch, or side effects; existing one-Zone evidence does not promote this routine. Counts remain 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list remains 74. CP223 adds required source-mapped `routine.calc_zone_component_load_sums` and project-contract item `calc_zone_component_load_sums` after `routine.zone_heat_balance_calc_sum_hat`. `CalcZoneComponentLoadSums`, declared at `ZoneTempPredictorCorrector.hh` lines 345-348 and implemented at `ZoneTempPredictorCorrector.cc` lines 5414-5677, is the correction-only component-report update sequence. It zeroes ten target report fields, combines Zone-wide internal gains and parent-Zone airflow/equipment/plenum/PIU/non-air topology with receiver-HB temperatures, coefficients, and storage state, re-walks every parent-Zone Space Surface range for every Zone or active simulation-Space report, accumulates Window convection and CondFD enthalpy, computes ThirdOrder, Analytical, or Euler air storage, and conditionally mutates a parent-Zone shared imbalance-warning index. Controlled inlet work can overwrite four ADU report fields, repeated Space reports can leave the last Space calculation on shared ADUs, and the literal PIU loop again uses global ordinals rather than stored identities. No direct C++ test exists; five HybridModel wrapper calls reach only a Zone report and assert unrelated hybrid state. The 55 nonzero-Zone corpus configurations give a static one-correction census of 81 Zone plus three Space reports, or 84 calls, with 104 stored-Space range walks; no full-simulation assertion isolates the transaction. Rust has separate Zone-only internal-gain, surface-convection, and air-storage report helpers plus a hard-coded zero outdoor-transfer series, but no ten-field record, Space report arena, complete airflow/equipment/Window/PCM/diagnostic transaction, or failure lifecycle. Existing four-row one-Zone conformance remains bounded output evidence and does not promote this routine. CP223 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 229 routines, split 58 `state_mapped` plus 171 `source_mapped`, with 106 required; the heat-balance project list becomes 75. CP224 adds required source-mapped `routine.verify_thermostat_in_zone` and project-contract item `verify_thermostat_in_zone` after `routine.calc_zone_component_load_sums`. `VerifyThermostatInZone`, declared at `ZoneTempPredictorCorrector.hh` line 350 and implemented at `ZoneTempPredictorCorrector.cc` lines 5679-5700, conditionally executes CP196 under the shared `GetZoneAirStatsInputFlag`, clears the latch only after normal return, gates lookup on positive `NumTempControlledZones`, then exact-string scans the full allocated `TempControlledZone.ZoneName` arena and returns only membership. Its sole production call is `SetUpZoneSizingArrays` for each record whose cooling or heating method is exactly `FromDDCalc` once any equipment configuration is controlled; a false result only makes the caller issue a non-pulse warning. CP224 itself owns no diagnostic or mutation after input acquisition. A CP196 non-return retains its partial state and the true latch, so same-state retry can re-enter the non-idempotent loader. No direct C++ test exists; one focused setup fixture reaches two false results without an isolating assertion. Thirty-four of 57 active full simulations provide 48 direct-Zone DesignDay sizing records with matching thermostats and equipment connections, hence a static first-setup census of 48 true calls, but no assertion isolates the predicate, lazy latch, exact-name mismatch, warning, failure, retry, or reset. Rust has bounded normalized direct-Zone DualSetpoint records, graph edges, an IdealLoads diagnostic, and plan metadata, but no exact helper, full CP196 arena or lazy input, executable `Sizing:Zone` caller, or source lookup and failure lifecycle. CP224 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 230 routines, split 58 `state_mapped` plus 172 `source_mapped`, with 107 required; the heat-balance project list becomes 76. CP225 adds required source-mapped `routine.verify_controlled_zone_for_thermostat` and project-contract item `verify_controlled_zone_for_thermostat` after `routine.verify_thermostat_in_zone`. `VerifyControlledZoneForThermostat`, declared at `ZoneTempPredictorCorrector.hh` line 352 and implemented at `ZoneTempPredictorCorrector.cc` lines 5702-5713, directly exact-string scans the full allocated `ZoneEquipConfig.ZoneName` arena and returns only positive membership. It performs no input load, normalization, `IsControlled`, equipment-list, Zone-identity, count, or Space check and owns no write or diagnostic; an exact default-sentinel or manually corrupted uncontrolled-slot match can therefore return true. Its only call expressions are CP199 ordinary then comfort control loops, each deferred until Zone-equipment input is filled and gated by a state-lifetime checked latch. False results produce caller-owned Severe/Continue diagnostics and sticky `ErrorsFound`; both loops finish before fatal, and only a normal input-filled return commits the latch. CP225 itself is read-only and deterministic, but caught parent retry repeats missing diagnostics and fatal because the error remains true, the checked latch remains false, and earlier CP199 effects persist. No direct C++ test exists; four direct CP199 fixture calls are gated off. Thirty-eight of 57 active full simulations provide 52 matching direct-Zone ordinary thermostat/equipment records, yielding 52 true calls, zero false calls, and zero comfort calls without an isolating assertion. Rust independently normalizes a raw nominal-controlled marker, direct-Zone DualSetpoint thermostats, and typed ZoneId equipment connections, plus an IdealLoads-only dispatch validator, but has no preserved-name helper, cross-family ordinary/comfort check, deferred latch, or source diagnostic/failure lifecycle. CP225 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 231 routines, split 58 `state_mapped` plus 173 `source_mapped`, with 108 required; the heat-balance project list becomes 77. CP226 adds required source-mapped `routine.detect_oscillating_zone_temp` and project item `detect_oscillating_zone_temp` after `routine.verify_controlled_zone_for_thermostat`. `DetectOscillatingZoneTemp`, declared at `ZoneTempPredictorCorrector.hh` line 354 and implemented at `ZoneTempPredictorCorrector.cc` lines 5715-5861, performs clear-on-success one-time allocation and registration, latches calculation from six output names or `PerformancePrecisionTradeoffs`, then on every enabled accepted system timestep shifts an independent zero-seeded four-slot Zone history and detects only strict alternating three-difference swings beyond `0.15 C`. It publishes Zone and Facility-any durations, separately classifies allocated ASH55 occupancy and unguarded deadband state, and accumulates three state-lifetime performance-log scalars without an environment reset. Setup and mid-loop failure are non-transactional, and duplicate enabled calls shift and can accumulate again. No C++ test references CP226 state or output. Fifty-six of 57 active full simulations reach setup and statically register 411 values across 81 nonzero-Zone records plus one zero-Zone configuration, but none requests the variables, monthly report, or performance tradeoff, so the calculation body has zero corpus reach. Rust has adjacent three-slot MAT history, adaptive `0.3 C` step selection, IdealLoads-only occupancy/deadband concepts, and hourly MAT/debug output, but no exact helper, four-slot detector, activation latch, six output-name families, annual/perflog state, HVAC-manager caller, or focused test. CP226 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 232 routines, split 58 `state_mapped` plus 174 `source_mapped`, with 109 required; the heat-balance project list becomes 78. CP227 adds required source-mapped `routine.adjust_air_set_points_for_op_temp_cntrl` and project item `adjust_air_set_points_for_op_temp_cntrl` after `routine.detect_oscillating_zone_temp`. `AdjustAirSetPointsforOpTempCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 356 and implemented at `ZoneTempPredictorCorrector.cc` lines 5863-5897, returns first on a false global operative-control flag and then only on exact per-record None; otherwise it selects a scheduled or fixed MRT fraction, copies the supplied Zone MRT, and overwrites the referenced target with `(setpoint - f * MRT) / (1 - f)`. Normal input validates `f` in `[0.0, 0.9)`, but the routine has no identity, pointer, denominator, finite, or range checks; untargeted records default to Invalid/fixed zero rather than None once another Zone raises the global flag. Five parent call expressions cover one-call single modes and cooling-then-heating Dual mode before later optimum, humidity, fault, comfort, and EMS effects. Built-in execution occurs once per Zone timestep before system substeps, while demand resimulation can add passes; full-parent replay reloads raw schedules, but a direct duplicate routine call generally double-transforms. No direct C++ test exists. Twenty-one direct parent calls produce 46 global-off entries, and the 57-expression full corpus contains no operative object: its static 101 active-sweep entry opportunities all return before record, MRT, or formula work. Rust has bounded DualSetpoint graph and raw-schedule IdealLoads evidence but no typed operative object, global/per-record control state, fraction binding, Zone MRT, exact helper, live caller, or focused test; the raw operative object run-blocks. CP227 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 233 routines, split 58 `state_mapped` plus 175 `source_mapped`, with 110 required; the heat-balance project list becomes 79. CP228 adds required source-mapped `routine.adjust_operative_set_points_for_adap_comfort` and project item `adjust_operative_set_points_for_adap_comfort` after `routine.adjust_air_set_points_for_op_temp_cntrl`. `AdjustOperativeSetPointsforAdapComfort`, declared at `ZoneTempPredictorCorrector.hh` line 358 and implemented at `ZoneTempPredictorCorrector.cc` lines 5899-5964, consumes one of seven adaptive daily arrays outside DesignDay/HVACSizeDesignDay or a shared summer-design-day slot for those two environment kinds, then applies the source integer-truncated baseline and exact `-1` fallback before CP227 converts the operative target to an air setpoint. Three guarded parent expressions cover SingleCool, SingleHeatCool, and Dual high only and snapshot the pre-CP227 operative result. Four direct C++ calls prove two central-model values and two lower-than-baseline sentinel restorations without isolating the later exact-sentinel branch, while 21 parent calls and all 57 active full simulations yield zero production entries. Rust has no typed operative object, adaptive state, selector, environment branch, output, live caller, or test, and the raw object run-blocks. CP228 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 234 routines, split 58 `state_mapped` plus 176 `source_mapped`, with 111 required, and the heat-balance project list becomes 80. CP229 adds required source-mapped `routine.calc_zone_air_comfort_set_points` and project-contract item `calc_zone_air_comfort_set_points` after `adjust_operative_set_points_for_adap_comfort`. `CalcZoneAirComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` line 360 and implemented at `ZoneTempPredictorCorrector.cc` lines 5966-6329, performs first-use ThermalComfort initialization, resets and samples comfort control types, selects PMV schedules, executes NO/SPE/OBJ/PEO People dispatch through the next child, clamps and overwrites ordinary dry-bulb setpoints, and leaves EMS with final precedence. Function-scope object and occupant counts bias later Zones, OBJ/PEO SingleCool use the low `-999` sentinel, child endpoint equality can retain stale outputs, and warnings plus partial Fanger/People/setpoint writes survive retry. No direct C++ call or active full-simulation expression reaches it; two raw setup fixtures do not call the parent, and the one installed comfort ExampleFile is not adopted by the repository. Rust has no typed comfort thermostat/setpoint, PMV/Fanger state, inverse child, exact outputs, live caller, or test, and active raw comfort input run-blocks. CP229 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 235 routines, split 58 `state_mapped` plus 177 `source_mapped`, with 112 required, and the heat-balance project list becomes 81. CP230 adds required source-mapped `routine.get_comfort_set_points` and project-contract item `get_comfort_set_points` after `calc_zone_air_comfort_set_points`. `GetComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` lines 362-367 and implemented at `ZoneTempPredictorCorrector.cc` lines 6331-6415, always evaluates the impure forward Fanger model at the stored minimum then maximum dry-bulb bounds. Only a strict interior PMV target enters configurable global `SolveRoot` with residual tolerance 0.001 and limit 500; below/above targets select a bound; with ordered or equal endpoint PMVs exact equality preserves the referenced output, while reversed equality can select the opposite temperature bound, and a NaN target or any case making all three comparisons false also preserves the output. The solver repeats both endpoints, can evaluate 501 candidates because convergence precedes the strict limit check, returns the last evaluated candidate on an iterated `-1` but Tmin on a zero-candidate width `-1` or `-2`, and yields two, four, or five-through-505 total Fanger calls by path. Each trial can resample People schedules and room-air state, overwrite ThermalComfort scratch plus People/Fanger report state, and repeat child diagnostics; lower clamp and `-2` can return Tmin while the final report state reflects Tmax. SurfaceWeighted MRT first-use can mutate Surface `AE`/`enclAESum` and its latches, and a bad sum returns a MAT/surface average on the first trial but zero later, providing a concrete stateful path from the initial bracket to `-2`. The two solver warning count/index pairs are shared across all comfort Zones and People, persist across environments, and are suppressed during warmup even though calculation and child effects continue. All 12 production expressions are inside CP229, with one/two NO-SPE calls, N/2N OBJ or positive-PEO calls, and 2N/4N nonpositive-PEO-plus-fallback calls. There is no status, transaction, rollback, or latch, so partial child/output/diagnostic state survives failure and retries repeat effects. No unit test reaches CP230, all 21 direct parent calls skip comfort, and the 57 active full simulations contain no comfort input. Separate forward-Fanger and generic-root tests do not compose this inverse. A manual stock-26.1 ExampleFile run supplies only uninstrumented diagnostic evidence and is not adopted as a repository case. Rust has no comfort type, PMV/Fanger state, generic configurable root solver, diagnostics, live caller, or composed test; RawOnly comfort input still run-blocks, and an unrelated private IdealLoads bisection does not promote the boundary. CP230 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 236 routines, split 58 `state_mapped` plus 178 `source_mapped`, with 113 required, and the heat-balance project list becomes 82. CP231 adds required source-mapped `routine.adjust_cooling_set_point_for_temp_and_humidity_control` and project item `adjust_cooling_set_point_for_temp_and_humidity_control` after `get_comfort_set_points`. `AdjustCoolingSetPointforTempAndHumidityControl`, declared at `ZoneTempPredictorCorrector.hh` lines 369-372 and implemented at `ZoneTempPredictorCorrector.cc` lines 6417-6458, aliases both indexed records before its global and exact-None guards, chooses a scheduled or constant maximum range, caps only for a strictly positive high-low gap, samples Zone RH and an independent dehumidifying schedule, and under strict positive excess/ratio gates subtracts only from `setptHi`. Default Invalid records do not return; the object-count global flag plus null untargeted schedules creates a mixed-record failure dependency, while zero/reversed gaps, ObjexxFCL second-argument-on-unordered `min`, malformed values, and direct duplicate calls retain literal nonfinite, deadband, and non-idempotent behavior. The two SingleCool/Dual parent expressions run after adaptive/operative and optional optimum-start work and before fault, comfort, and final EMS overrides; SingleCool load still consumes the untouched scalar, and a later positive cutout path can rebuild high from the earlier raw snapshot. No direct C++ test exists; 20 indirect unit entries and all active full-corpus entries return at the false global guard because no exact modifier input is present. The sole installed exact ExampleFile is an upstream Constant-path simulation test, but this repository does not adopt it and the case neither requests the adjusted cooling-setpoint output nor directly asserts CP231. Rust has only adjacent DualSetpoint, Humidistat, psychrometric, and IdealLoads moisture paths, with no typed modifier, percent-RH/range/ratio state, mutable setpoint triple, exact helper, live caller, or test; the raw object run-blocks. CP231 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 237 routines, split 58 `state_mapped` plus 179 `source_mapped`, with 114 required; the heat-balance project list becomes 83. CP232 adds required source-mapped `routine.override_air_set_points_for_ems_cntrl` and project item `override_air_set_points_for_ems_cntrl` after `adjust_cooling_set_point_for_temp_and_humidity_control`. `OverrideAirSetPointsforEMSCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 374 and implemented at `ZoneTempPredictorCorrector.cc` lines 6460-6555, traverses every ordinary record then every comfort record, with heating before cooling. Single modes write scalar plus one bound, SingleHeatCool leaves scalar at cooling when both flags are active, and Dual writes only low/high; dispatch uses live per-Zone types, so record collisions and the later comfort loop resolve field-by-field without validation. Ordinary actuators bind under `Zone Temperature Control` with `[C]`, while comfort actuators bind under `Zone Comfort Control` with `[]` yet are copied directly into Celsius-backed fields. The unconditional parent-tail call follows ordinary modifiers, fault, and comfort processing, while later staged or positive-cutout prediction can replace fields from other/raw state. One direct unit test proves only ordinary and comfort Dual low/high writes; all indirect unit and active full-corpus visits retain false flags, and installed testfiles provide no exact actuator-active ExampleFile. Rust has no EMS engine, actuator state, comfort type, mutable setpoint triple, helper, or live caller, and all EMS input run-blocks. CP232 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 238 routines, split 58 `state_mapped` plus 180 `source_mapped`, with 115 required; the heat-balance project list becomes 84. CP233 adds required source-mapped `routine.fill_predefined_table_on_thermostat_setpoints` and project item `fill_predefined_table_on_thermostat_setpoints` after `override_air_set_points_for_ems_cntrl`. `FillPredefinedTableOnThermostatSetpoints`, declared at `ZoneTempPredictorCorrector.hh` line 376 and implemented at `ZoneTempPredictorCorrector.cc` lines 6558-6672, visits all authored SingleHeating, SingleCooling, SingleHeatingOrCooling, then DualSetpoint definitions and lets the first occurrence of each numeric Schedule ID own its winter, summer, or split synthetic rows in the LEED temperature-schedule table. Detailed schedules sample the first Wednesday at hours 11 and 23 with one selected-date DST shift and count exact matching Wednesday-profile values across 365/366 calendar-day rules; constants return final `currentVal` and every day. Every cell appends without a transaction or once latch; valid parsed names avoid case-sensitive base/synthetic collisions, while manual mixed-case state can still collide, and retry can diverge between last-wins rendering and first-wins retrieval. The sole end-simulation tabular caller runs even when table files are disabled. No C++ test calls CP233; 56 successful full simulations give unasserted static reach, while a helper-only test makes nine calls and 21 assertions. Rust has only adjacent DualSetpoint graph state, calendar-aware schedule series, and constant-schedule IdealLoads diagnostics; it has no four-family arena, source ID deduplication, seasonal query, predefined table store, exact helper, caller, or composed test, and summary-report input stays ignored. CP233 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 239 routines, split 58 `state_mapped` plus 181 `source_mapped`, with 116 required; the heat-balance project list becomes 85. CP234 adds required source-mapped `routine.fill_predefined_table_on_thermostat_schedules` and project item `fill_predefined_table_on_thermostat_schedules` after CP233. `FillPredefinedTableOnThermostatSchedules`, declared at `ZoneTempPredictorCorrector.hh` line 378 and implemented at `ZoneTempPredictorCorrector.cc` lines 6674-6766, visits materialized ordinary TempControlledZone records in stored order, always appends thermostat/control-schedule plus independently joined control-type/name cells, and conditionally appends heating/cooling schedule cells. Nonempty control-object names alone select fixed type slots; tuple-lexical sorting and per-column empty filtering can break positional alignment. The five-slot resize followed by move-append relies on valid-unspecified moved-from strings being empty in observed libraries. Append-only predefined entries have partial-failure, duplicate-retry, last-wins-render versus first-wins-retrieval, and separate reset behavior. One direct call has 24 assertions only for one type per Zone; 56 successful full simulations provide unasserted static reach across 52 records and 312 cells. Rust has only adjacent direct-Zone DualSetpoint graph state and no complete control arena, exact sort/joins, predefined System Summary store, caller, serializer, or comparator; summary-report input remains ignored. CP234 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 240 routines, split 58 `state_mapped` plus 182 `source_mapped`, with 117 required; the heat-balance project list becomes 86. CP235 next maps `ZoneSpaceHeatBalanceData::updateTemperatures`, declared at `ZoneTempPredictorCorrector.hh` lines 233-234 and implemented at `ZoneTempPredictorCorrector.cc` lines 6768-6833."
routine.manage_surface_heat_balance.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.manage_surface_heat_balance.source_routine = "ManageSurfaceHeatBalance"
routine.manage_surface_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_surface_heat_balance.completion_status = "source_mapped"
routine.manage_surface_heat_balance.required_for_full_domain = true
routine.init_surface_heat_balance.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_surface_heat_balance.source_routine = "InitSurfaceHeatBalance"
routine.init_surface_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_surface_heat_balance.completion_status = "source_mapped"
routine.init_surface_heat_balance.required_for_full_domain = true

routine.allocate_surface_heat_bal_arrays.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.allocate_surface_heat_bal_arrays.source_routine = "AllocateSurfaceHeatBalArrays"
routine.allocate_surface_heat_bal_arrays.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.allocate_surface_heat_bal_arrays.completion_status = "source_mapped"
routine.allocate_surface_heat_bal_arrays.required_for_full_domain = true

routine.init_thermal_and_flux_histories.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_thermal_and_flux_histories.source_routine = "InitThermalAndFluxHistories"
routine.init_thermal_and_flux_histories.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_thermal_and_flux_histories.completion_status = "source_mapped"
routine.init_thermal_and_flux_histories.required_for_full_domain = true

routine.eval_outside_movable_insulation.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.eval_outside_movable_insulation.source_routine = "EvalOutsideMovableInsulation"
routine.eval_outside_movable_insulation.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.eval_outside_movable_insulation.completion_status = "source_mapped"
routine.eval_outside_movable_insulation.required_for_full_domain = false

routine.eval_inside_movable_insulation.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.eval_inside_movable_insulation.source_routine = "EvalInsideMovableInsulation"
routine.eval_inside_movable_insulation.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.eval_inside_movable_insulation.completion_status = "source_mapped"
routine.eval_inside_movable_insulation.required_for_full_domain = false

routine.init_solar_heat_gains.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_solar_heat_gains.source_routine = "InitSolarHeatGains"
routine.init_solar_heat_gains.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_solar_heat_gains.completion_status = "source_mapped"
routine.init_solar_heat_gains.required_for_full_domain = true

routine.init_int_solar_distribution.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_int_solar_distribution.source_routine = "InitIntSolarDistribution"
routine.init_int_solar_distribution.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_int_solar_distribution.completion_status = "source_mapped"
routine.init_int_solar_distribution.required_for_full_domain = true

routine.compute_int_thermal_absorp_factors.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.compute_int_thermal_absorp_factors.source_routine = "ComputeIntThermalAbsorpFactors"
routine.compute_int_thermal_absorp_factors.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.compute_int_thermal_absorp_factors.completion_status = "source_mapped"
routine.compute_int_thermal_absorp_factors.required_for_full_domain = true

routine.compute_int_sw_absorp_factors.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.compute_int_sw_absorp_factors.source_routine = "ComputeIntSWAbsorpFactors"
routine.compute_int_sw_absorp_factors.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.compute_int_sw_absorp_factors.completion_status = "source_mapped"
routine.compute_int_sw_absorp_factors.required_for_full_domain = true

routine.compute_dif_sol_exc_zones_wiz_windows.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.compute_dif_sol_exc_zones_wiz_windows.source_routine = "ComputeDifSolExcZonesWIZWindows"
routine.compute_dif_sol_exc_zones_wiz_windows.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.compute_dif_sol_exc_zones_wiz_windows.completion_status = "source_mapped"
routine.compute_dif_sol_exc_zones_wiz_windows.required_for_full_domain = true

routine.init_ems_controlled_surface_properties.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_ems_controlled_surface_properties.source_routine = "InitEMSControlledSurfaceProperties"
routine.init_ems_controlled_surface_properties.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_ems_controlled_surface_properties.completion_status = "source_mapped"
routine.init_ems_controlled_surface_properties.required_for_full_domain = false

routine.init_ems_controlled_constructions.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_ems_controlled_constructions.source_routine = "InitEMSControlledConstructions"
routine.init_ems_controlled_constructions.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_ems_controlled_constructions.completion_status = "source_mapped"
routine.init_ems_controlled_constructions.required_for_full_domain = false

routine.update_intermediate_surface_heat_balance_results.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.update_intermediate_surface_heat_balance_results.source_routine = "UpdateIntermediateSurfaceHeatBalanceResults"
routine.update_intermediate_surface_heat_balance_results.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_intermediate_surface_heat_balance_results.completion_status = "source_mapped"
routine.update_intermediate_surface_heat_balance_results.required_for_full_domain = true

routine.update_non_representative_surface_results.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.update_non_representative_surface_results.source_routine = "UpdateNonRepresentativeSurfaceResults"
routine.update_non_representative_surface_results.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_non_representative_surface_results.completion_status = "source_mapped"
routine.update_non_representative_surface_results.required_for_full_domain = false

routine.gather_for_predefined_report.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.gather_for_predefined_report.source_routine = "GatherForPredefinedReport"
routine.gather_for_predefined_report.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.gather_for_predefined_report.completion_status = "source_mapped"
routine.gather_for_predefined_report.required_for_full_domain = false

routine.calc_heat_balance_outside_surf.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_heat_balance_outside_surf.source_routine = "CalcHeatBalanceOutsideSurf"
routine.calc_heat_balance_outside_surf.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_heat_balance_outside_surf.completion_status = "source_mapped"
routine.calc_heat_balance_outside_surf.required_for_full_domain = true

routine.get_qdot_conv_out_per_area.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.get_qdot_conv_out_per_area.source_routine = "GetQdotConvOutPerArea"
routine.get_qdot_conv_out_per_area.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_qdot_conv_out_per_area.completion_status = "source_mapped"
routine.get_qdot_conv_out_per_area.required_for_full_domain = true

routine.calc_heat_balance_inside_surf.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_heat_balance_inside_surf.source_routine = "CalcHeatBalanceInsideSurf"
routine.calc_heat_balance_inside_surf.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_heat_balance_inside_surf.completion_status = "source_mapped"
routine.calc_heat_balance_inside_surf.required_for_full_domain = true

routine.calc_heat_balance_inside_surf_2.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_heat_balance_inside_surf_2.source_routine = "CalcHeatBalanceInsideSurf2"
routine.calc_heat_balance_inside_surf_2.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_heat_balance_inside_surf_2.completion_status = "source_mapped"
routine.calc_heat_balance_inside_surf_2.required_for_full_domain = true

routine.calc_heat_balance_inside_surf_2_ctf_only.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_heat_balance_inside_surf_2_ctf_only.source_routine = "CalcHeatBalanceInsideSurf2CTFOnly"
routine.calc_heat_balance_inside_surf_2_ctf_only.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_heat_balance_inside_surf_2_ctf_only.completion_status = "source_mapped"
routine.calc_heat_balance_inside_surf_2_ctf_only.required_for_full_domain = true

routine.sum_surf_qdot_rad_hvac.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.sum_surf_qdot_rad_hvac.source_routine = "sumSurfQdotRadHVAC"
routine.sum_surf_qdot_rad_hvac.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.sum_surf_qdot_rad_hvac.completion_status = "source_mapped"
routine.sum_surf_qdot_rad_hvac.required_for_full_domain = true

routine.test_surf_temp_calc_heat_balance_inside_surf.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.test_surf_temp_calc_heat_balance_inside_surf.source_routine = "TestSurfTempCalcHeatBalanceInsideSurf"
routine.test_surf_temp_calc_heat_balance_inside_surf.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.test_surf_temp_calc_heat_balance_inside_surf.completion_status = "source_mapped"
routine.test_surf_temp_calc_heat_balance_inside_surf.required_for_full_domain = true

routine.calc_outside_surf_temp.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_outside_surf_temp.source_routine = "CalcOutsideSurfTemp"
routine.calc_outside_surf_temp.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_outside_surf_temp.completion_status = "source_mapped"
routine.calc_outside_surf_temp.required_for_full_domain = true

routine.calc_exterior_vented_cavity.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_exterior_vented_cavity.source_routine = "CalcExteriorVentedCavity"
routine.calc_exterior_vented_cavity.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_exterior_vented_cavity.completion_status = "source_mapped"
routine.calc_exterior_vented_cavity.required_for_full_domain = false

routine.gather_component_loads_surf_abs_fact.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.gather_component_loads_surf_abs_fact.source_routine = "GatherComponentLoadsSurfAbsFact"
routine.gather_component_loads_surf_abs_fact.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.gather_component_loads_surf_abs_fact.completion_status = "source_mapped"
routine.gather_component_loads_surf_abs_fact.required_for_full_domain = false

routine.get_surf_incident_solar_multiplier.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.get_surf_incident_solar_multiplier.source_routine = "GetSurfIncidentSolarMultiplier"
routine.get_surf_incident_solar_multiplier.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_surf_incident_solar_multiplier.completion_status = "source_mapped"
routine.get_surf_incident_solar_multiplier.required_for_full_domain = false

routine.init_surface_property_view_factors.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.init_surface_property_view_factors.source_routine = "InitSurfacePropertyViewFactors"
routine.init_surface_property_view_factors.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_surface_property_view_factors.completion_status = "source_mapped"
routine.init_surface_property_view_factors.required_for_full_domain = false
routine.get_ground_surfaces_temperature_average.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.get_ground_surfaces_temperature_average.source_routine = "GetGroundSurfacesTemperatureAverage"
routine.get_ground_surfaces_temperature_average.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_ground_surfaces_temperature_average.completion_status = "source_mapped"
routine.get_ground_surfaces_temperature_average.required_for_full_domain = false
routine.get_ground_surfaces_reflectance_average.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.get_ground_surfaces_reflectance_average.source_routine = "GetGroundSurfacesReflectanceAverage"
routine.get_ground_surfaces_reflectance_average.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_ground_surfaces_reflectance_average.completion_status = "source_mapped"
routine.get_ground_surfaces_reflectance_average.required_for_full_domain = false
routine.re_set_ground_surfaces_view_factor.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.re_set_ground_surfaces_view_factor.source_routine = "ReSetGroundSurfacesViewFactor"
routine.re_set_ground_surfaces_view_factor.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.re_set_ground_surfaces_view_factor.completion_status = "source_mapped"
routine.re_set_ground_surfaces_view_factor.required_for_full_domain = false
routine.get_surrounding_surfaces_temperature_average.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.get_surrounding_surfaces_temperature_average.source_routine = "GetSurroundingSurfacesTemperatureAverage"
routine.get_surrounding_surfaces_temperature_average.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_surrounding_surfaces_temperature_average.completion_status = "source_mapped"
routine.get_surrounding_surfaces_temperature_average.required_for_full_domain = false

routine.update_final_surface_heat_balance.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.update_final_surface_heat_balance.source_routine = "UpdateFinalSurfaceHeatBalance"
routine.update_final_surface_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_final_surface_heat_balance.completion_status = "source_mapped"
routine.update_final_surface_heat_balance.required_for_full_domain = true
routine.update_thermal_histories.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.update_thermal_histories.source_routine = "UpdateThermalHistories"
routine.update_thermal_histories.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.update_thermal_histories.completion_status = "source_mapped"
routine.update_thermal_histories.required_for_full_domain = true
routine.calculate_zone_mrt.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calculate_zone_mrt.source_routine = "CalculateZoneMRT"
routine.calculate_zone_mrt.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calculate_zone_mrt.completion_status = "source_mapped"
routine.calculate_zone_mrt.required_for_full_domain = true
routine.surface_data_fd_update_moisture_balance.source_file = "src/EnergyPlus/HeatBalFiniteDiffManager.hh"
routine.surface_data_fd_update_moisture_balance.source_routine = "UpdateMoistureBalance"
routine.surface_data_fd_update_moisture_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.surface_data_fd_update_moisture_balance.completion_status = "source_mapped"
routine.surface_data_fd_update_moisture_balance.required_for_full_domain = false
routine.manage_thermal_comfort.source_file = "src/EnergyPlus/ThermalComfort.cc"
routine.manage_thermal_comfort.source_routine = "ManageThermalComfort"
routine.manage_thermal_comfort.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_thermal_comfort.completion_status = "source_mapped"
routine.manage_thermal_comfort.required_for_full_domain = false
routine.report_surface_heat_balance.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_surface_heat_balance.source_routine = "ReportSurfaceHeatBalance"
routine.report_surface_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_surface_heat_balance.completion_status = "source_mapped"
routine.report_surface_heat_balance.required_for_full_domain = true
routine.report_non_representative_surface_results.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_non_representative_surface_results.source_routine = "ReportNonRepresentativeSurfaceResults"
routine.report_non_representative_surface_results.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_non_representative_surface_results.completion_status = "source_mapped"
routine.report_non_representative_surface_results.required_for_full_domain = false
routine.report_int_mov_ins_inside_surf_temp.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_int_mov_ins_inside_surf_temp.source_routine = "ReportIntMovInsInsideSurfTemp"
routine.report_int_mov_ins_inside_surf_temp.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_int_mov_ins_inside_surf_temp.completion_status = "source_mapped"
routine.report_int_mov_ins_inside_surf_temp.required_for_full_domain = false
routine.gather_component_loads_surface.source_file = "src/EnergyPlus/OutputReportTabular.cc"
routine.gather_component_loads_surface.source_routine = "GatherComponentLoadsSurface"
routine.gather_component_loads_surface.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.gather_component_loads_surface.completion_status = "source_mapped"
routine.gather_component_loads_surface.required_for_full_domain = false
routine.calc_thermal_resilience.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.calc_thermal_resilience.source_routine = "CalcThermalResilience"
routine.calc_thermal_resilience.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_thermal_resilience.completion_status = "source_mapped"
routine.calc_thermal_resilience.required_for_full_domain = false
routine.report_thermal_resilience.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_thermal_resilience.source_routine = "ReportThermalResilience"
routine.report_thermal_resilience.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_thermal_resilience.completion_status = "source_mapped"
routine.report_thermal_resilience.required_for_full_domain = false
routine.report_co2_resilience.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_co2_resilience.source_routine = "ReportCO2Resilience"
routine.report_co2_resilience.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_co2_resilience.completion_status = "source_mapped"
routine.report_co2_resilience.required_for_full_domain = false
routine.report_visual_resilience.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.report_visual_resilience.source_routine = "ReportVisualResilience"
routine.report_visual_resilience.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_visual_resilience.completion_status = "source_mapped"
routine.report_visual_resilience.required_for_full_domain = false

[[algorithm]]
id = "heat_balance_air_manager_source_order"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceAirManager.cc",
  "src/EnergyPlus/HeatBalanceAirManager.hh",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/air_manager.rs::manage_air_heat_balance_stage",
]
status = "scaffold"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Zone Air Heat Balance Surface Convection Rate",
]
claim_level = "none"
support_boundary = "Source-order scaffold only: CP124 maps the unconditional `HeatBalanceSurfaceManager.cc` line-179 caller and reuses the existing required `routine.manage_air_heat_balance` row for canonical `HeatBalanceAirManager.cc::ManageAirHeatBalance` lines 135-161. The source driver conditionally calls `GetAirHeatBalanceInput` under `ManageAirHeatBalanceGetInputFlag` and clears that flag only after the child returns, then unconditionally calls `InitAirHeatBalance`, `CalcHeatBalanceAir`, and `ReportZoneMeanAirTemp` in that order. `HeatBalanceAirManager.hh` defaults the input flag true and resets it true in `clear_state()`; this lifecycle is distinct from both the Surface-manager progress-display flag and `ReportZoneMeanAirTemp`'s one-time extra-report-variable flag. An input fatal or other non-return preserves the input flag for same-state re-entry. The mapped children retain their source boundaries: input lines 163-189, initialization lines 4494-4507, Air calculation lines 4590-4604, and reporting lines 4615-4687. Existing Rust `manage_air_heat_balance_stage` metadata, identity/compatibility wrappers, bounded zone-air calculations, and later run-period report wrapper do not implement the complete source driver: the current timestep closes the Surface-manager wrapper after limited Init/Outside work, runs and closes the Air-manager/Air-calculation shell before the separately wrapped Inside pass, and reports later. No source routine, flag lifecycle, child topology, external-or-standard HVAC dispatch, report-request scan, complete state, failure behavior, exact parent order, numerics, output, support, or conformance is promoted. CP124 adds no routine/project-contract/Rust/state/count/capability/comparator/proof/result entry; after CP122 the inventory remains 32 algorithms and 138 routines, split 58 state-mapped plus 80 source-mapped. CP125 maps the following `UpdateFinalSurfaceHeatBalance` as a required source routine; CP126 maps the guarded thermal-history update as a required source routine; CP127 maps the inline CondFD moisture-history block and non-required helper; CP128 maps the unconditional false-initialize-only ThermalComfort call as a non-required source routine; CP129 maps the unconditional Surface reporting call as a required source routine; CP130 maps the sizing-only component-load gathering call as a non-required source routine; CP131 maps the unconditional thermal-resilience calculation as a non-required source routine; CP132 maps its guarded thermal-resilience summary as a non-required source routine; CP133 maps the independently guarded CO2 summary as a non-required source routine; CP134 maps the independently guarded visual summary as a non-required source routine; CP135 maps the inline parent-tail first-time-flag clear without a synthetic routine or count change; CP136 maps the unconditional `EndZoneTimestepBeforeZoneReporting` `ManageEMS` call by reusing the existing generic routine without a duplicate row or count change; CP137 adds required `routine.rec_keep_heat_balance` and its project-contract requirement for line 211 and source lines 2971-3057 without Rust/state/support/conformance promotion; CP138 adds required `routine.report_heat_balance` and its project-contract requirement for the unconditional line-217 call and source lines 3321-3418. The source always calls `Sched::ReportScheduleVals`, then selects the normal non-warmup output, effective warmup-reporting, external-interface fallback, or no-further-output path; exact node, Zone output, sizing-log, dictionary/stamp, tabular, and utility ordering remains source-only dependency context, with no child rows or added sources. Existing Rust report-stage, composite-plan, prebinding, and bounded result-store metadata are scaffolding, not implementation. CP138 adds no source file, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP139 maps the unconditional line-219 `EndZoneTimestepAfterZoneReporting` `ManageEMS` call by reusing `routine.manage_ems` without a duplicate row. Generic `ManageEMS` resets the shared caller-owned `anyRan` instead of accumulating the CP136 result, quick-returns when EMS is absent, otherwise initializes this calling point, dispatches callbacks/plugins and exactly matching managers, and applies actuators plus `ReportEMS` only if something ran. Because CP138 reporting has already completed, any commit cannot retroactively change those outputs; later numerical, trend, and plugin visibility is not inferred. CP139 adds no source, project-contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 150 routines, split 58 state-mapped plus 92 source-mapped, with 50 required. CP140 adds non-required `routine.update_ems_trend_variables` for the unconditional line-221 call, `EMSManager.hh` line 122 declaration, and `EMSManager.cc` lines 1444-1479 body. After no-EMS and zero-trend quick returns, the source visits declarations in 1-based order, skips nonpositive Erl pointers or log depths, and pushes the current numeric value to history index 1 without local positive upper-bound validation. Input allocation, `TimeARR`, setup diagnostics, and environment resets remain dependencies; the body emits no direct diagnostics. Existing Rust plan scaffolding has no trend-update implementation or target. CP140 adds no source file, project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 151 routines, split 58 state-mapped plus 93 source-mapped, with 50 required. CP141 adds non-required `routine.update_plugin_values` for the unconditional line-222 call, `PluginManager.hh` line 198 declaration, and `PluginManager.cc` lines 1458-1467 body, adding those two PluginManager sources to the Manager inventory. Without `LINK_WITH_PYTHON` the call is a no-op; the linked body has no runtime manager guard and directly visits stored-order `state.dataPluginManager->trends`, naturally doing no work for an empty vector. It gets each current global, pushes it at index 0, and drops the oldest value while fixed `times` remain unchanged; the update validates neither history length nor deque size. Construction returns before input population without a retained `PythonPlugin:Instance`; retained-instance setup owns globals, trends, handles, positive history length, fixed offsets, diagnostics, and missing-handle fatals. BeginEnvironment zeros globals and trend values while retaining times. The getter fatals for an empty global vector, while a nonempty invalid handle uses unchecked `[]`; dependency failure can prevent CP142. Warmup updates do not test per-instance run-during-warmup. Rust plugin execution remains unsupported. CP141 adds no project contract, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 152 routines, split 58 state-mapped plus 94 source-mapped, with 50 required. CP142 adds required `routine.check_warmup_convergence` and its project-contract entry for the outer `WarmupFlag && EndDayFlag` gate at lines 224-226 and `HeatBalanceManager.cc` lines 3059-3226 body, declared at header line 136. `NumOfZones <= 0` clears warmup immediately. Otherwise the Zone loop uses inclusive temperature-tolerance tests and strict current-load `> 1.0e-4` gates; active heat/cool tests mutate current and previous loads through `abs(max(value, 100.0))`, divide by the clamped current value, and pass inclusively at the load tolerance. No-load paths pass without refreshing their stored test values. Each Zone then copies current values to previous-day state and resets current extrema to `-9999.0`/`1000.0`. At the maximum day while `WarmupFlag` remains true, failures emit per-Zone severe/detail diagnostics with state-wide normal/sizing guidance latches, optionally diagnose a user maximum below the default 25, and finally force warmup false; ordinary convergence still honors the minimum day. Allocation, BeginEnvironment resets, CP137 extrema accumulation, controls, names, diagnostics, and failures remain dependencies. Existing Rust stage metadata and temperature-only diagnostic loop are not this routine. CP142 adds no EnergyPlus source, Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP143 maps only the inner line-227 `!WarmupFlag` branch and ordered numeric `DayOfSim` then textual `DayOfSimChr` zero resets at lines 228-229, without a synthetic routine or count change. Despite the source convergence comment, the branch also follows CP142 zero-Zone and maximum-day forced exits, but not a below-maximum failed check. The following line-231 `BeginNewEnvironmentAfterWarmUp` EMS call observes both resets, which persist if that dependency does not return; on the same invocation the later line-235 report gate cannot satisfy `DayOfSim == 1`. Existing Rust warmup metadata and diagnostics implement none of these global mutations or nesting. CP143 adds no source, project contract, Rust/state/support/conformance promotion; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP144 maps the in-branch line-231 `BeginNewEnvironmentAfterWarmUp` call by reusing `routine.manage_ems`, passing the same `anyRan` and an absent manager index after CP143 resets. Generic dispatch overwrites the flag false, quick-returns without EMS, otherwise initializes this calling point, runs callbacks/plugins and exact managers, and uses any execution to gate actuator commit plus `ReportEMS`. The lines 268-271 runtime-language and plugin begin-environment hooks require the distinct `BeginNewEnvironment` point and do not run here. Observers see warmup false and both day values zero; the caller ignores the result. CP143 mutations persist on non-return, which prevents CP145; a successful return still leaves the same-invocation CP145 `DayOfSim == 1` test false. CP144 adds no duplicate routine, source, project contract, Rust/state/support/conformance promotion or count change; the inventory remains 32 algorithms and 153 routines, split 58 state-mapped plus 95 source-mapped, with 51 required. CP145 adds required `routine.report_warmup_convergence` and its project-contract entry for the lines 235-237 four-way first-nonwarmup-day guard and `HeatBalanceManager.cc` lines 3228-3301 body, declared at header line 138. Its internal non-warmup guard can no-op; a one-state-lifetime, positive-Zone EIO header precedes source-order Zone rows. It resets scratch, selects RunPeriod or SizingPeriod text, averages `CountWarmupDayPoints` temperature samples, and normalizes each stored load difference in place only when its maximum is strictly above `1.0e-4`, otherwise zero. Load means and population standard deviations use the mutated ratios although the header says W; rows include four CP142 `PassFail` values and `{:.10R}` reals. Repeated calls renormalize already changed loads. Allocation, BeginEnvironment reset, CP137 producer/count, EIO, formatting, and bounds are dependencies; the body validates no positive count or capacity and emits no direct diagnostics. Existing Rust warmup metadata/summary is not this report. CP145 adds no source file or Rust/state/support/conformance promotion; the inventory becomes 32 algorithms and 154 routines, split 58 state-mapped plus 96 source-mapped, with 52 required. CP146 adds required source-mapped `SetPreConstructionInputParameters`. The unconditional `SimulationManager.cc` line-216 call follows constant/state initialization, environment/report checks, and predefined-table setup and precedes time-pointer setup; the routine is declared at `HeatBalanceManager.hh` line 96 and implemented at `HeatBalanceManager.cc` lines 446-492. Each entry overwrites shared `MaxSolidWinLayers` with executable baseline 7 despite the stale comment saying 5; the member defaults to 0, module clear reconstructs 0, and re-entry recomputes from 7. Raw `Construction:ComplexFenestrationState` presence raises the bound to 10 without retrieving an item or erasing any complex-state record from InputProcessor `unusedInputs`. The raw `Construction:WindowEquivalentLayer` count drives 1-based `getObjectItem` calls into all shared alpha/numeric value, blank-marker, and field-name buffers; only the positional `NumAlpha - 1` span is folded into the maximum, including holes/blanks, while alpha contents, numerics, `NumNumber`, `IOStat`, markers, and names are ignored. For each found item, InputProcessor zeros both counts, sets `IOStat` to -1, clears the shared buffers, and erases the requested equivalent-layer record from `unusedInputs` before field traversal; only full completion sets `IOStat` to 1, and CP146 never checks it. Opaque F/C-factor constructions are ignored. Normal successful output is 7 through 11; successful order cannot change the maximum, but source-order retrieval leaves the last completed shared buffers and marks every requested equivalent-layer item used. If a dependency does not return during the current item, the maximum retains only baseline/CFS and previously returned equivalent-layer spans, while that current object may already be marked used and its shared buffers may be partial; later items and downstream work are skipped. The routine validates and diagnoses nothing locally. Construction array allocation plus Surface, SolarShading, SurfaceGeometry, and Window loops are downstream dependencies. Rust dynamic `Vec` storage and separate limits 8 and 11 do not implement the mutable shared bound, scan/call order, buffers, failure prefix, allocations, or reset/re-entry lifecycle. CP146 adds no source inventory, Rust target/state/code, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 155 routines, split 58 state-mapped plus 97 source-mapped, with 53 required. CP147 adds required source-mapped `GetSiteAtmosphereData`, declared at `HeatBalanceManager.hh` line 100, called at `GetHeatBalanceInput` line 264 between line-262 project controls and line-266 spectral input, and implemented at `HeatBalanceManager.cc` lines 1252-1317. It sets `CurrentModuleObject` to `Site:HeightVariation`, uses local alpha-1/numeric-3 arrays, and reads the raw count. The unique-object schema defaults N1/N2/N3 to 0.22, 370.0, and 0.0065 with respective inclusive-zero, strict-positive, and inclusive-zero minima and no maxima. Exactly one object uses `NumNums` to copy only the returned N1/N2/N3 prefix, so absent trailing fields retain Terrain-derived wind or the existing gradient while blank/missing fields inside the prefix propagate schema defaults. InputProcessor zeros counts, sets `IOStat` to -1, clears local values plus shared blank/name metadata, and erases the object from `unusedInputs` before traversal; full completion sets status 1, but CP147 ignores status, alphas, metadata, and all local validation. A non-return can leave used/partial dependency effects before any environment copy or EIO. More than one object reads and marks no item, changes no environment value, emits exact `Too many Site:HeightVariation objects, only 1 allowed.`, then monotonically sets caller `ErrorsFound`; zero objects retain predecessor Terrain wind and reset only gradient to 0.0065. Every normally returned branch unconditionally repeats the EIO header and formatted row, including retained values on duplicates; output failures preserve earlier mutations and can leave a header-only prefix. Environment/module clear restores default 0.22/370/0.0065 and empty current-object state, while re-entry repeats intake and output. Terrain setup, height-corrected dry/wet bulb and wind formulas, Surface bulk paths, Kiva, diagnostics, InputProcessor, and EIO are dependencies. Rust keeps this object raw-only; its Terrain wind mapping and fixed-gradient helper do not implement override/hybrid state, shared lifecycle, source formulas, diagnostics, usage/metadata effects, or output. CP147 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 156 routines, split 58 state-mapped plus 98 source-mapped, with 54 required. CP148 adds required source-mapped `AllocateZoneHeatBalArrays`, declared at `HeatBalanceManager.hh` line 130 and implemented at `HeatBalanceManager.cc` lines 2824-2854. `InitHeatBalance` calls `AllocateHeatBalArrays` under `BeginSimFlag` at lines 2617-2618, and that parent calls CP148 as its first action at line 2863. A false `ZoneIntGain.allocated()` sentinel invokes `AllocateIntGains`, which allocates Zone internal gain records, Space internal gain records, Space device records, then Space power-reduction factors defaulted 1.0; gain values/counts default zero. A true sole sentinel preserves and does not validate the whole companion bundle. On the ordinary path, `GetHeatBalanceInput` line 320 already calls `ManageInternalHeatGains(state, true)`, whose input path normally invokes `AllocateIntGains` at `InternalHeatGains.cc` lines 291-293; CP148 therefore preserves populated internal-gain state, while its fallback chiefly serves direct test and alternate paths. CP148 next unconditionally allocates Zone and Space heat-balance EPVectors even with Space mode false. EPVector sets allocated before resize and fills every element with `T{}`: exact inherited defaults are 23 C for MAT/MRT/ZTAV/ZT/ZTAVComf/XMPT/TMX/TM2/MixingMAT and all XMAT/DSXMAT slots, 0.01 for the five named humidity values, and zero for every remaining member including T1 and ZTM/W histories. Eight `NumOfSolarEnclosures` arrays allocate in QSDifSol, QD, QDforDaylight, DB, DBSSG, DBIntWin, QSWRad, QSWRadLights order; the 1-based loop zeros them in QSDifSol, QD, QDforDaylight, QSWRad, QSWRadLights, DB, DBSSG, DBIntWin order. Zero counts are accepted and set EPVector sentinels without elements. Negative `NumOfZones` or `numSpaces` converts to unsigned EPVector `size_type`, sets allocated true, then generally nonreturns through a huge resize; negative `NumOfSolarEnclosures` instead creates eight empty Objexx 1:0 arrays, skips the loop, and can return. Oversized, inconsistent, and topology-invalid counts receive no local validation or diagnostic. Sequential failures expose completed allocations and per-enclosure writes; allocated-before-resize can poison the Zone sentinel so an uncleared retry skips an incomplete fallback. Successful re-entry preserves internal gains but destructively rebuilds Zone/Space records and enclosure arrays. Owning clears are nonuniform: HeatBalance clear resets the Zone sentinel/Space companions/enclosure arrays, predictor clear resets Zone/Space records, and ViewFactor clear resets the enclosure count, but DaylightingManager clear retains `spacePowerReductionFactor`. The false reset Zone sentinel makes the next fallback re-dimension that retained factor and overwrite it with 1.0; not all participating state becomes uniformly fresh or unallocated. The parent remainder after line 2863, BeginSim lifecycle, predictor/corrector, convection, HVAC, internal-gain/daylighting, SolarShading, and Surface solar consumers are dependencies. Rust Zone vectors and initialization shells do not implement the Space/fallback/enclosure state, exact defaults/order, partial failure, destructive re-entry, or clear/retry lifecycle. CP148 adds no source inventory, Rust target/state/code, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 157 routines, split 58 state-mapped plus 99 source-mapped, with 55 required. CP149 adds required source-mapped `routine.allocate_heat_bal_arrays` and its project-contract entry for the sole production `InitHeatBalance` BeginSim call, header line 132, and source lines 2855-2963. After the separate CP148 child, the source dimensions ten FanSystem Zone arrays to zero; no-value allocates `ZoneReOrder`, dimensions two flags false, then explicitly zeros the reorder; dimensions thermostat air to 23 C; independently samples each enabled CO2/generic schedule once and dimensions three arrays; resets the exact warmup extrema/history/report bundle; no-value allocates convergence records and two standard-deviation arrays; allocates cold then heat crossed-threshold matrices before explicitly setting cold then heat false; conditionally allocates eleven thermal, three CO2, and three visual vector-report matrices; unconditionally allocates six SET/longest/start matrices using `TotThermalReportPers`; and resets `CountWarmupDayPoints` last. Value-bearing `dimension` always refills. Ordinary release no-value allocation preserves linear cells whenever `resize` reports no reallocation, including same-product reshapes with reinterpreted indices; changed counts within reserved capacity remain a container dependency. Reported reallocation default-constructs vector cells but leaves arithmetic cells without CP149 initialization, while Objexx array-init/debug builds assign trait/debug values even on the no-reallocation path. False feature branches preserve old state on same-state re-entry. Zero Zone count still samples enabled contaminants and reaches the tail; nonpositive report counts skip only conditional families, while crossed and six SET allocations still run with empty negative extents. Effects are sequential with delayed fills, no validation, diagnostic, rollback, or status. The later BeginEnvironment block resets many warmup values but not `LoadZoneSecPrevDay`, convergence records, or standard deviations; owner clears make a fully cleared retry fresh. Rust has no parent analog. CP149 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 158 routines, split 58 state-mapped plus 100 source-mapped, with 56 required. CP150 adds required source-mapped `routine.update_window_face_temps_non_bsdf_win` and its project-contract entry immediately after `rec_keep_heat_balance` and before `report_heat_balance`. Declared at header line 140 and implemented at source lines 3303-3313, it is the unconditional last executable action of `RecKeepHeatBalance` at line 3056 and makes successful endpoint copies visible before same-timestep line-217 reporting. The helper traverses stored `AllHTWindowSurfaceList` order; normal clean-state production appends ascending indices satisfying `HeatTransSurf && Class == Window`, with no BSDF filter. Each entry first dereferences the current mutable `Surface.Construction`, skips only `WindowTypeBSDF`, copies outside history term 1 to `SurfWinFenLaySurfTempFront(SurfNum, 1)`, then copies inside history term 1 to `SurfWinFenLaySurfTempBack(SurfNum, TotLayers)`, where `TotLayers` is not the solid-layer count. Destination matrices are dependency-dimensioned `[TotSurfaces, MaxSolidWinLayers]` to zero; history term arrays are separately allocated/initialized and produced by Surface balance. Standard non-BSDF face-temperature outputs are registered only for exterior-solar front layer 1 and back layer `TotLayers`, but CP150 has no output, warmup, sizing, or kickoff guard. Empty lists no-op; duplicates repeat writes; valid BSDF entries and unlisted/intermediate/obsolete cells retain prior values. Invalid Surface/Construction/history/destination/layer state is unchecked. Effects are stored-order and front-before-back with no diagnostics, status, rollback, math, or finite check, so a back failure can preserve the current front plus earlier entries and prevent ReportHeatBalance. Re-entry overwrites current endpoints but does not clear cells skipped after Construction/list/layer changes. Owner clears reconstruct the list, destinations, histories, and Construction table; BeginEnvironment does not explicitly reset destination matrices. Rust blocks fenestration and has no list/report-matrix/history-handoff analog. CP150 adds no source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 159 routines, split 58 state-mapped plus 101 source-mapped, with 57 required. CP151 adds non-required source-mapped `routine.open_shading_file`, declared at header line 144 and implemented at source lines 3422-3438, for the sole production `InitHeatBalance` lines-2696-2698 call under exact `BeginSimFlag && DoWeathSim && ReportExtShadingSunlitFrac`. It first calls `shade.ensure_open` with caller label `OpenOutputFiles` and the independent `outputControl.extshd` selector. A non-good enabled stream opens text output with truncation or fatals on failure; disabled physical output creates a good dev-null sink. A good physical, string, or dev-null stream is retained with its current path/mode/position, so direct re-entry appends another header, while a closed/failed physical stream reopens with truncation. The body writes literal `Surface Name,`, every raw `Surface(1..TotSurfaces).Name` plus a comma in numeric order, then newline. It has no Surface filter, CSV quoting/escaping, status, flush, close, once flag, post-write check, or rollback; nonpositive counts still write the literal line, invalid positive Surface state or stream failure can leave a partial prefix, and only the open failure owns an exact fatal. Later daily shading rows use a separate BeginDay/non-warmup/RunPeriodWeather/report-request gate, which omits CP151 caller conditions `BeginSimFlag` and `DoWeathSim`; for a normal validated positive timestep count they emit `24 * TimeStepsInHour` rows and all `SurfSunlitFrac` values. They are dependency context, not CP151. Rust has no report flag, extshd selector, eplusshading CSV lifecycle, all-Surface export, or header/row writer; existing bounded incident-solar calculation/result state and Schedule:File:Shading input support are not parity. CP151 adds no project-contract requirement, source inventory, Rust target/state/code, support, output, or conformance promotion; the inventory becomes 32 algorithms and 160 routines, split 58 state-mapped plus 102 source-mapped, with 57 required. CP152 adds non-required source-mapped `routine.set_storm_window_control` for the sole production `InitHeatBalance` line-2669 call under outer `TotStormWin > 0` and inner `BeginDayFlag`; only a successful return sets caller-owned `ChangeSet = false`. The body first clears `StormWinChangeThisDay`, then visits StormWindow records in numeric input order: it resolves `BaseWindowNum`, copies current to previous, computes local `DateOff - 1` with zero remapped to 366, uses inclusive/wrapping `BetweenDates(DayOfYear_Schedule, DateOn, adjusted DateOff)` to write current 1 or 0, makes `BeginSimFlag` overwrite previous with current, and latches any mismatch. Input dates and WeatherManager's test day use fixed leap-shaped `OrdinalDay(..., 1)` values, making `DateOn` inclusive and input `DateOff` exclusive; January 1 off wraps through day 366, and malformed equal dates that bypass input rejection are on for all 366 days. The first later non-BeginDay call while `ChangeSet` is false clears the event, snapshots each referenced current flag to previous, then sets `ChangeSet` true. Every positive-total caller pass then visits every unfiltered Zone/Space WindowSurface-span index, including normal Window, GlassDoor, and TDD_Diffuser entries rather than only StormWindow references, selecting the synthesized storm construction only for flag 1 plus Detailed and otherwise the current mutable `Surface.Construction`; this can reflect or overwrite the immediately preceding EMS active-construction decision. Duplicate BaseWindow records are ordered writes: last current wins, later previous sees the earlier new current, and when not BeginSim any intermediate mismatch latches; BeginSim suppresses each mismatch. Normal unique-base same-day re-entry clears an earlier transition. Effects are sequential with no validation, diagnostic, status, or rollback: helper failure preserves prefixes and prevents the `ChangeSet` clear and remainder; remainder failure preserves helper state, false `ChangeSet`, and its active-construction prefix while blocking CP151 and solar work; non-BeginDay snapshot failure leaves the event false, `ChangeSet` false, and a previous-day prefix for retry; and a nonpositive caller total skips the outer block and can leave stale state. Owner clears restore empty/zero Surface storm state, a false event, and true `ChangeSet`; BeginEnvironment does not. Active-construction selection, window starting-temperature guesses, daylighting recalculation, shaded-construction selection, divider behavior, and status output remain downstream dependencies. Rust has no storm-window input, state, controller, active-construction switch, or fenestration analog. CP152 adds no project-contract requirement, source inventory, Rust target/state/code, test, support, output, or conformance promotion; the inventory becomes 32 algorithms and 161 routines, split 58 state-mapped plus 103 source-mapped, with 57 required. CP153 adds required source-mapped `routine.init_conduction_transfer_functions` and its heat-balance project-contract requirement. The sole production `InitHeatBalance` path first enters `BeginSimFlag`, returns from `AllocateHeatBalArrays`, then under `AnyCTF || AnyEMPD` emits `Initializing Response Factors` and calls line 2621; EMPD uses the same thermal generator. The wrapper initializes local error/forced-report flags false, visits every Construction in array order through `calculateTransferFunction`, then after each returned child makes `SimpleCTFOnly` false for more than one history and raises `MaxCTFTerms`; any internal source independently makes the simple flag false. A second pass calls `reportLayers` only for `IsUsedCTF`; `ScanForReports` follows; request or forced error report writes four EIO headers and increments `cCounter` across all Constructions while reporting only used ones; accumulated errors fatal last with the exact wrapper message. The child first zeros all fixed CTF/QTF/source/user-temperature arrays plus timestep, counts, and U, then unused entries return. Used entries scan every material; source high-diffusivity diagnostics latch once without setting the shared bool, thickness over 3 m and resistance below `1.0e-3` set it, boundary/non-ROnly resistance layers receive equivalent-air properties, and interior ROnly layers stay massless. Any already-set shared error makes later used children reset and scan but return before generation. Adjacent massless merges alter local layers and persistently decrement source/user-temperature layer positions. All-resistive constructions use one history/term and `Outside0 = Cross0 = Inside0 = U = 1/R`, with a source/sink setting an error after coefficients are formed. A source-free exact reverse of an earlier used equal-layer Construction reuses its timestep/counts, swaps inside/outside, and retains cross/nonzero flux. New massive generation builds the bounded 1-D/optional-2-D node grid and source nodes, then each adaptive attempt calls exponential matrix, inverse matrix, gammas, and final coefficients in order. More than 18 terms or over-1% series-sum mismatch increments histories and timestep and retries; a zero largest sum fatals immediately, while timestep at least 7 hours emits convergence guidance, forces detailed reporting, sets the error, and breaks. Normal completion stores regular/QTF coefficients and U and deallocates work arrays; singular/nonfinite results have no distinct local validation. Nonfatal errors can leave earlier/full, failing/latest, and later/reset-zero Construction state, still run reachable layer/detail reporting, then fatal; immediate child/output failures bypass the remainder with no rollback. Re-entry recomputes child bundles and reverse reuse sees fresh earlier entries, but the wrapper never resets `SimpleCTFOnly` true or `MaxCTFTerms` zero, warning latches and appended reports persist, and repeated massless merging can decrement persistent source positions again. Owner clears reconstruct state; BeginEnvironment does not regenerate. Rust can consume `EnergyPlusEioSeeded` rows or `RustGeneratedSteady` no-history `1/R` coefficients and has bounded CTF consumers, but no native finite-difference/matrix/adaptive/QTF generator or this lifecycle. Construction helper files remain dependency context with no separate row or source-inventory addition. CP153 adds no Rust target/state/code, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 162 routines, split 58 state-mapped plus 104 source-mapped, with 58 required. CP154 adds non-required source-mapped `routine.gather_for_predefined_report`, declared at `HeatBalanceSurfaceManager.hh` line 99, implemented at `HeatBalanceSurfaceManager.cc` lines 623-1404, and reached only from the `InitSurfaceHeatBalance` lines-475-482 BeginSim branch; the first-time flag controls only the progress display, while the gather is unconditional within BeginSim and independent of whether predefined reports will be shown. The caller has already returned from `InitIntSolarDistribution` and `InitIntConvCoeff`; a successful gather next reaches `AnyCondFD` initialization and conduction-history work. Only `dataGeneral->Constructions` gates Construction EIO headers and rows. Each call zeros class counts and fenestration accumulators, allocates/zeros `computedNetArea`, optionally writes the assembly header, recreates all local dedup/header state, and first traverses stored `AllSurfaceListReportOrder` without an added use or report filter. Only `ExternalEnvironment`, `Ground`, `GroundFCfactorMethod`, and `KivaFoundation` are exterior. Exterior opaque classes are Wall/Floor/Roof; the interior branch also accepts IntMass. Opaque and Door area use Zone times List multipliers, while Window/TDD_Dome also use the Surface multiplier. Rows append construction/Zone/Space, no-film U, outside reflectance, geometry, and orientation; openings subtract from `computedNetArea(BaseSurf)` without validation or clamp. Azimuth is rounded to 0.01 before cardinal classification; `60 <= Tilt < 180` and azimuth at least 315 or below 45 alone marks north, while all other exterior fenestration contributes to non-north totals. Exterior Window/TDD reporting reuses nonzero SummerSHGC/visible cache or, for non-EQL zero cache, calls `CalcNominalWindowCond`, ignores `errFlag`, and stores returned optical values. EQL plus zero cache and dependency error can expose uninitialized locals; a non-EQL error can also poison the cache. Normal `CalcNominalWindowCond` completion also rewrites `SurfWinCoeffAdjRatio` for the complete exterior-Window list. Frames add the exact perimeter area, dividers use their grid formula, and the fully multiplied gross-plus-frame opening drives net subtraction and weighted U/SHGC/VT totals. Framed entries index the fixed NFRC product geometry/vision tables and call assembly helpers whose factory can substitute `SurfWinActiveShadedConstruction` from live `SurfWinShadingFlag`, independently of the supplied construction. An external-library model can overwrite NominalU and local SHGC through its NFRC rating helpers; locally unique construction/frame pairs alone write assembly EIO rows. Shaded-state count comes from the control list while shaded construction indices are read unchecked. Framed NFRC helpers run before pair dedup; unframed states use separate construction dedup plus NFRC width/height table index zero. The same shaded construction with different frames, or across framed/unframed domains, can append duplicate predefined cells keyed only by construction name. Two independent lazy same-tag EIO headers can expose different schemas. Active WindowShadingControl access is unchecked and the control row joins shaded plus storm construction names in stored order. Interior literal `iz-` mirrors skip all individual rows, net subtraction, and totals but still reach class counting. Interior AirBoundary openings contribute area and U to denominators while contributing zero SHGC/VT numerator, and interior legacy zero-cache results are not stored back. Every listed Surface increments its actual class; Window with original GlassDoor or TDD_Diffuser also increments the original class. Raw two-family Overhang and Fin object totals overwrite those buckets. A second report-order pass appends Wall/Floor/Roof net rows, followed by exterior overall/north/non-north and interior area-weighted totals with literal `-` on nonpositive denominators, then fourteen fixed labels in both total and exterior count columns; empty input still appends totals/counts. Every predefined entry appends. Real entries retain formatted text, original SI value, and digit metadata; report writing groups first-seen object order and takes the last duplicate cell, while `RetrievePreDefTableEntry` scans forward and takes the first; shaded construction/frame key collisions can trigger that disagreement within one invocation. Duplicate report-order indices/names therefore repeat counts, totals, subtraction, and entries and can drive net area farther negative. Unchecked topology/NFRC/list indices, ignored nominal errors, helper/output failures, and assertions leave partial predefined, cache, and EIO prefixes. CP154 itself emits no local diagnostic or status result and has no rollback; a non-return blocks the parent remainder even when a dependency diagnosed the cause. Re-entry recreates locals but appends every table row and repeats enabled EIO; nonzero optical cache skips legacy work, physical zero recomputes, and external-library/cache/scratch mutations persist. HeatBalanceSurfaceManager clear resets its own arrays/flags but owns no CP154 table/EIO state and does not remove duplicate predefined entries or emitted output. OutputReportPredefined clear removes definitions/ids/entries and requires `SetPredefinedTables`; owning topology/cache and General clears rebuild their respective state, while EIO stream rewind/truncation belongs to separate output-file lifecycle. Rust bounded opaque geometry, frame typing, and the static comparator are not this routine; Rust has no complete report-order/fenestration topology, predefined arena, NFRC/shading/report gatherer, EIO, duplicate, failure, or re-entry semantics. CP154 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 163 routines, split 58 state-mapped plus 105 source-mapped, with 58 required. CP155 adds required source-mapped `routine.allocate_surface_heat_bal_arrays` and its project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 101, implemented at `HeatBalanceSurfaceManager.cc` lines 1406-2206, and its sole production `src/` call is `InitSurfaceHeatBalance` line 350 inside the lines-349-355 BeginSim branch, after the caller's per-Surface outdoor/local-node weather and EMS overrides and before the `InterZoneWindow` reduction. The allocation phase defines 136 distinct fields across HeatBalSurf (100), FanSystem (15), MoistureBalance (12), HeatBalance (6), Surface (2), and HeatBalanceSurfaceManager (1): 119 ordinary per-Surface arrays, eight rectangular arrays, eight 19-element-outer jagged histories whose inners have extent `S`, and one EPVector. Of those fields, 121 are unconditional and 15 conditional. The source has 144 syntactic allocate/dimension sites, split 125 unconditional plus 19 conditional, because each of the eight jagged fields has separate outer allocation and inner dimensioning; 144 is therefore not the 121-plus-15 distinct-field count. When reached, exactly 133 distinct fields end at zero or zero-valued default construction; `SurfTempEffBulkAir` starts at 23 C, `TCondFDSourceNode` at 15 C, and `SurfRoughnessExt` at `Invalid` (-1). `AnyInternalHeatSourceInInput` gates ten fields and is evaluated separately at lines 1429 and 1555, while `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` gates five fields and is evaluated once at line 1466. False branches neither clear nor deallocate preserved prior state. After the reached allocation phase completes, line 1607 displays exact text `Setting up Surface Reporting Variables`; the body then visits numeric Surface indices `1..S` and skips only non-`HeatTransSurf` entries. It contains 78 syntactic `SetupOutputVariable` call sites: the conditional union of 77 per-Surface sites plus one unconditional site-total setup call, with 64 Average and 14 Sum sites. The exact Surface-side gates are non-Kiva, non-Window, external-environment-or-advanced with nested non-Window, opaque class with nested non-Kiva, `Construct(surface.Construction).SourceSinkPresent`, Window, exact external environment, and advanced-only construction index; the 21 retained-heat-transfer sites have no additional Surface gate. Per-Surface keys use `Surface.Name`; the site-total Sum uses key `Environment` and existing `SumSurfaceHeatEmission` without resetting it. There is no CP155 report-request filter before the dependency calls. OutputProcessor's first setup call initializes that subsystem when `OutputInitialized` is false, before request checking and dictionary insertion; the dictionary deduplicates name plus units, every call increments setup counters, and these no-meter calls append an active `OutVar` only for requested name/key matches. Allocation and setup are sequential and nontransactional, with no local validation, diagnostic, status, rollback, or cleanup. A failing dependency can retain a six-owner/output-setup prefix and blocks the caller's `InterZoneWindow` scan, remainder, and first-time clear. With `S == 0`, active jagged outers still allocate 19 elements, and `surfQRadFromHVAC.allocate(0)` leaves an empty EPVector with its allocated sentinel true; the display and site-total setup call still occur, and only a requested site-total match becomes active. Negative `S` can mutate many Objexx arrays before the EPVector argument converts to an enormous unsigned size; its allocation sets the allocated sentinel before resize fails. Direct re-entry refills reached value-bearing dimensions, may relocate backing storage, and preserves newly false conditional allocations; setup counters advance again, dictionary entries remain deduplicated, and requested matches can append duplicate active records whose earlier pointers reference pre-redimension storage. Five owner clears reconstruct their array owners; HeatBalance clear restores the two allocation flags, HeatBalanceSurfaceManager clear releases its own reference-air array and resets first-time flags, and OutputProcessor clear is separately required for registrations. BeginEnvironment alone is not a full CP155 reset or rerun. Seven unit-test files make 35 direct calls only as downstream setup and provide no dedicated zero-count or re-entry test. Rust has no complete six-owner allocation, history/default/conditional-preservation state, output-setup, or re-entry lifecycle analog. CP155 adds no source inventory, Rust target/code/state, test, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 164 routines, split 58 state-mapped plus 106 source-mapped, with 59 required. CP156 adds required source-mapped `routine.init_thermal_and_flux_histories` and its project-contract entry immediately after `allocate_surface_heat_bal_arrays`. It is declared at `HeatBalanceSurfaceManager.hh` line 103, implemented at `HeatBalanceSurfaceManager.cc` lines 2208-2447, and its sole production `src/` call is `InitSurfaceHeatBalance` line 383 inside the lines-379-384 BeginEnvrn branch; no unit test calls it directly. The caller has already refreshed Surface weather/local-node/EMS state, conditionally run CP155, refreshed construction absorptances, and called `UpdateVariableAbsorptances`. Its first-time flag gates only exact display `Initializing Temperature and Flux Histories`; CP156 itself has no output or diagnostic, and a successful return next reaches movable insulation. CP156 first visits numeric Zones: placement-new reconstructs each ZoneHeatBalanceData with its named temperature/MAT histories and MixingMAT at 23 C, five humidity values initially 0.01, and remaining zero/default state; only `airHumRatAvg` and `airHumRat` are then overwritten by `OutHumRat`, and `TempTstatAir` becomes 23 C. Every enclosure MRT becomes 23 C, and every existing Space heat-balance record is independently placement-new reset regardless of `doSpaceHeatBalance`, with the same two humidity overwrites. Zone order, stored `spaceIndexes`, and inclusive heat-transfer ranges then receive exactly 34 assignments per reached Surface: `SurfTempEffBulkAir`, `SurfTempIn`, and `SurfTempInTmp` become 23 C, `SurfHConvInt` becomes 3.076 W/m2-K, and the other 30 coefficient/report/temperature fields become zero. Independently guarded nonnegative opaque/IntMass ranges zero four conduction/beam fields; guarded Window ranges set six frame/divider current/old/outside temperatures to 23 C and two shading flags to `NoShade`, with no class recheck. A Zone/Space/term-outer `1..19` pass sets both base temperatures to 23 C and both base fluxes to zero. The first line-2339 evaluation of `!SimpleCTFOnly || AnyEnergyManagementSystemInModel` also resets `SurfCurrNumHist`, both master temperatures, and both master fluxes. A separate `AnyInternalHeatSourceInInput` pass, ordered Surface before term, sets four source/user temperature histories to 23 C and two source flux histories to zero. False feature branches preserve prior allocations and contents. Working `CondFDRelaxFactor` then copies its input. The next loop is term outer `1..MaxCTFTerms + 1` and stored AllHT Surface inner. ExternalEnvironment and OtherSideCondModeledExt outside temperatures use the already refreshed local dry bulb, Ground uses BuildingSurface ground temperature, GroundFCfactorMethod uses FCFactor ground temperature, and other boundaries retain 23 C under normal reachable topology. Each reached outside flux term becomes `q = Construct(surface.Construction).UValue * (OutsideTempHist(1) - InsideTempHist(1))`; each inside term copies base outside-flux slot 2. Because term 1 executes before slot 2 is written, normal preceding zero state leaves inside term 1 zero; term 2 first writes slot 2 to q, so inside terms 2 and higher receive q. Global MaxCTFTerms zero still executes term 1 and reads fixed slot 2; normal 18 reaches all 19 slots. An AllHT pass sets each referenced exterior cavity's `TbaffleLast` and `TairLast` to 20 C. Stored AllHTKiva order uses `surfaceConvMap[SurfNum]`, inserting a missing record, then assigns inside constant 3.076, default forced term, and outside constant 0 in `.in` then `.f` then `.out` order. The same SimpleCTF/EMS expression is evaluated again at line 2413. When true, special boundary families overwrite all 19 master outside-temperature terms; other boundaries retain 23 C, and terms `2..Construct.NumCTFTerms + 1` copy base outside-flux slot 2 to both master fluxes while term 1 and higher unused terms retain zero. Positive `TotOSCM` finally sets numeric OSCM TConv/HConv/TRad/HRad to 20/4/20/4 while preserving EMS fields and other members. CP156 is selective and nontransactional: direct re-entry destructively resets every reached record/range/history/factor/list/count target; newly false gates and removed ranges/list/map entries preserve prior state, so changed topology can mix fresh and stale cells. With zero Zones, existing enclosures and Space records, the CondFD factor, AllHT/cavity/Kiva lists, and OSCMs still reset; conventional `0..-1` ranges skip. It assumes CP155/prior allocations and validated counts, ranges, list membership, Construction/cavity indices, and term bounds. It has no return, local validation, warning, status, catch, rollback, or cleanup; nonfinite humidity, boundary temperatures, or U-value propagate. The concrete placement-new default construction is nonthrowing; unchecked index/container access can assert, terminate, or produce undefined behavior after earlier mutations. Kiva map allocation or `std::function` work can throw; a Kiva failure can leave an inserted key with only `.in` or `.in` plus `.f` committed, and any failure blocks the caller remainder and first-time tail. Clears are distributed across predictor/corrector, ViewFactor, HeatBalance, HeatBalSurf, FanSystem, Surfaces, and SurfaceGeometry/Kiva owners; no partial owner clear reproduces CP156, while a reached BeginEnvironment call is its ordinary reset boundary. Rust's bounded optional `HeatBalanceCtfInitialHistoryPolicy::EnergyPlusSurfInitial` helper accepts configurable initial temperature, selects a typed boundary temperature, computes steady `1/R` flux, and fills variable-length base histories with the same q on both sides. It does not implement EnergyPlus's fixed 19-slot current/prior shell and term-1 inside-flux asymmetry, Zone/Space and 34-plus-4-plus-8 Surface reset, master/source histories, cavity/Kiva/OSCM state, or selective failure/re-entry lifecycle. CP156 adds no source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 165 routines, split 58 state-mapped plus 107 source-mapped, with 60 required. CP157 adds non-required source-mapped `routine.eval_outside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 105 and implemented at `HeatBalanceSurfaceManager.cc` lines 2449-2481. Its sole production `src/` call is `InitSurfaceHeatBalance` line 388, the first child of the shared lines-387-390 `AnyMovableInsulation` block; CP158 follows at line 389, so CP157 failure blocks inside evaluation while a later CP158 failure preserves completed exterior effects. One unit test makes three direct calls with an always-on Schedule: Regular verifies solar absorptance, conductance, thermal absorptance, and roughness, while Glass and GlassEQL verify only solar absorptance; no inactive case is tested. CP157 visits stored `extMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `extMovInsuls(SurfNum)` and calls its Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads the current `SurfActiveConstruction` and its exterior `LayerPoint(1)` material, then copies raw `AbsorpSolar`, `AbsorpThermal`, and `Roughness` to the three exterior Surface fields in that order. This branch preserves stale `H` and exact `presentPrevTS` and does not use `movInsul.matNum`. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL then dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; the comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, preserves positive infinity, and has no upper clamp. Other groups copy raw `AbsorpSolar`. Both active material paths next copy raw thermal absorptance and roughness, and exact `presentPrevTS` remains untouched. Both successful branches overwrite the caller's just-refreshed construction and variable-absorptance exterior values. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-construction properties while preserving H, and removed entries receive no CP157 mutation. Under global `AnyMovableInsulation`, CondFD later reads `extMovInsuls(Surf).H` without checking `present` or exterior-list membership, so a stale positive active-to-inactive H can still select its movable-insulation branch. CP157 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/layer/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and a current inactive-present/property or active-present/H/property prefix. `SurfacesData::clear_state` reconstructs the records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied property arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no exterior movable-insulation input/state, traversal, Schedule controller, active-construction property restoration, glass optical path, conductance/CondFD handoff, or matching failure/re-entry analog. CP157 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 166 routines, split 58 state-mapped plus 108 source-mapped, with 60 required. CP158 adds non-required source-mapped `routine.eval_inside_movable_insulation`, declared at `HeatBalanceSurfaceManager.hh` line 107 and implemented at `HeatBalanceSurfaceManager.cc` lines 2483-2513. Its sole production `src/` call is `InitSurfaceHeatBalance` line 389, the second child of the shared lines-387-390 `AnyMovableInsulation` block after CP157; a CP158 failure preserves completed exterior effects and blocks the caller remainder, after a successful CP158 return the caller resumes its intervening ground/shading/radiation work, and reaches unconditional CP159 at line 457 only if those dependencies also return. One unit test makes three positive always-on calls while reusing the same concrete `MaterialShade` record and mutating only its group tag Regular to Glass to GlassEQL. The first verifies solar absorptance, H, present, and thermal absorptance; the later two verify only solar absorptance, and no inactive case is tested. CP158 visits stored `intMovInsulSurfNums` order without sorting, deduplication, or another Surface filter. Each entry resolves `intMovInsuls(SurfNum)` and calls Schedule `getCurrentVal` once, selecting `EMSVal` when actuated and cached `currentVal` otherwise. For schedule value `<= 0`, including signed zero, negative finite values, and negative infinity, it first sets `present = false`, reads current `SurfActiveConstruction`, then copies that Construction's `InsideAbsorpSolar` followed by `InsideAbsorpThermal` to the two inside Surface fields. This branch preserves stale `H` and exact `presentPrevTS`, does not use `movInsul.matNum`, and writes no roughness. Every other value, including NaN, resolves `materials(movInsul.matNum)`, sets `present = true`, and writes `H = 1 / (schedule * Resistance)` with raw unchecked floating-point behavior. Glass and GlassEQL dynamic-cast to `MaterialFen`, assert non-null, and set solar absorptance to `max(0, 1 - Trans - ReflectSolBeamFront)`; Objexx's comparison-style max with zero first maps finite negative, negative infinity, and a NaN optical expression to zero, retains positive infinity, and applies no upper clamp. Other groups copy raw `AbsorpSolar`; both active paths then copy raw `AbsorpThermal`. Exact `presentPrevTS` and roughness remain untouched. Both successful branches overwrite the current inside absorptance values, including the caller's construction-derived refresh when its earlier gate ran; `UpdateVariableAbsorptances` writes only exterior arrays and contributes no inside value. Direct re-entry repeats Schedule reads and ordered writes; duplicates repeat, active values recompute H, active-to-inactive clears only `present` and restores active-Construction absorptances while preserving H, and removed entries receive no CP158 mutation. Unlike CP157's CondFD path, standard and optimized inside-balance H use is gated by the global flag plus `intMovInsuls(SurfNum).present`, though not by list membership: stale inactive H alone is suppressed, but a removed formerly-active record retaining `present = true` and H can still be consumed. After caller line-417 `CheckGlazingShadingStatusChange`, line-444 passes `SurfIterations = 0` into `CalcInteriorRadExchange`; outside BeginEnvironment, reached non-window enclosure Surfaces can invoke `UpdateMovableInsulationFlag`, which compares `present != presentPrevTS` then tests strict `abs(base Surface.Construction InsideAbsorpThermal - movable AbsorpThermal) > 0.01`. A NaN difference is false, while BeginEnvironment forces recalculation separately. End-zone-timestep `RecKeepHeatBalance` copies `present` to `presentPrevTS` at line 3051. CP158 has no return, local validation, diagnostic, warning, catch, rollback, or cleanup. Invalid list/array/Construction/material state, null Schedule, or mismatched glass dynamic type can assert, terminate, or produce undefined behavior; a non-return can retain earlier entries and an inactive present/solar or active present/H/solar prefix. `SurfacesData::clear_state` reconstructs records/list and restores `present = false`, exact `presentPrevTS = false`, and `H = 0`; `HeatBalSurfData::clear_state` releases the copied absorptance arrays, while Material, Construction, and Schedule clears separately rebuild dependencies. Rust has no interior movable-insulation input/state, traversal, Schedule controller, active-Construction absorptance restoration, glass optical branch, conductance/inside-balance handoff, long-wave change detection, or matching failure/re-entry analog. CP158 adds no project-contract requirement, source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 167 routines, split 58 state-mapped plus 109 source-mapped, with 60 required. CP159 adds required source-mapped `routine.init_solar_heat_gains` and its project-contract entry immediately after `init_thermal_and_flux_histories` and before `calc_heat_balance_outside_surf`. It is declared at `HeatBalanceSurfaceManager.hh` line 109, implemented at `HeatBalanceSurfaceManager.cc` lines 2515-3776, and called only as unconditional `InitSolarHeatGains(state)` inside `InitSurfaceHeatBalance` line 457; the caller first-time flag gates only its preceding progress text, a CP159 failure blocks daylighting/internal gains/CP160, and only the successful caller tail clears that flag. Exactly four direct calls occur in three unit-test contexts: the incident-multiplier fixture calls twice under positive solar and checks only exact half-versus-whole transmitted solar, WindowFrameTest exercises BeginEnvironment reset with zero solar and asserts only a downstream heat-loss relation, and the CFS fixture calls under positive solar before mutating back-surface state/rerunning distribution and asserts only downstream beam absorption. Every call unconditionally zeros Zone window/opaque conduction rate and energy reports, every enclosure initial-diffuse-reflection term, stored opaque-range conduction/absorbed/report fields, stored Window-range frame/divider/shade/convection/gain/energy fields, and `SurfWinQRadSWwinAbs` through `CFSMAXNL + 1`. While first-time remains true it seeds both ground-reflection factors from each Surface ground view factor. It defines positive solar by `SunIsUp && signed beam+ground+diffuse > 0`, derives sunset and sun-up/no-radiation plus BeginEnvironment reset, then commits `PreviousSolRadPositive` before any broad reset; NaN makes the comparison false, and a failed sunset-only reset loses its edge on retry while the other two reset causes persist. Positive-or-reset calls clear all-Surface inside-beam/incident/reflection and sky/ground arrays, enclosure transmitted reports/energies, stored-Window shade/transmission/reveal/BSDF/absorbed-layer fields; reset-only calls additionally clear only `EnclSolQD`/`EnclSolQDforDaylight`, TDD transmittances and selected dome rate/layer fields, optional three reflection factors, and selected all-Surface reports/profile/system optics. They do not clear `EnclSolQSDifSol`, either complex-window ground split, `SurfSkyDiffReflFacGnd`, positive-only `IncSolMultiplier`, custom-only `GndReflSolarRad`, `AbsDiffWin*`, scheduled-mutated `SurfWinA`, external-library total energy, or all TDD dome W/energy state. The positive branch asserts reflection-table dimensions even when reflection calculation is disabled, computes `max(beam*SOLCOS(3)+diffuse,0)` with the expression first so NaN persists, samples/stores one multiplier for every numeric Surface using same-index request alignment, computes sky and ground incidence, and, when enabled, writes the complex ground split before interpolating the beam ground factor, then interpolates obstruction/ground factors and rebuilds main sky/ground incidence. It calls profile angles, optional reveal reflection, then either the external-library simplified opaque plus interior distribution path or ordinary interior distribution. It forms daylight and normal enclosure diffuse power, copies daylight power into `EnclSolQSDifSol`, adds only flagged off-diagonal interzone contributions, then applies diagonal fraction and `solVMULT`. Shading-range reports omit obstruction additions; stored AllExt reports add them; TDD diffuser overrides use active diffuser `TransDiff` and dome angle/sunlit while retaining earlier ground-component reports; shelf ground overrides happen after incident totals and do not refresh them. Opaque absorption uses base Construction and the first exact scheduled Surface/base-Construction pair, with unscheduled beam scaling but scheduled bare `SurfOpaqAI`; duplicate opaque ranges overwrite outside/report assignments but repeat inside `+=`. The Window loop dereferences SurfaceWindow before its solar/TDD guard and uses active Construction except base EQL layer count; Detailed optics cover shade/screen/blind horizontal sky-ground splitting and switchable interpolation, BSDF uses the first exact scheduled Surface/active-Construction pair and mutates `SurfWinA`, EQL uses base-system count with active optics, and external-library layers never write total energy. Shaded-construction array/member ownership differs unchecked. Common frame/divider work preserves projection, reveal, switchable, suspended-divider, ExtBlind/ExtShade/ExtScreen formulas; between-glass enters alternate incidence but no final absorber case. A zeroed local beam array is populated by earlier lists/overrides and consumed later, so malformed list/range alignment can yield zero beam; repeated windows reset Detailed/EQL totals locally but can accumulate BSDF/external totals. Final TDD dome absorption uses base Construction and writes energy inside its layer loop. Representative averaging uses raw constituent-area divides with no zero/topology guard, can consume earlier representatives in traversal order, uses active representative `TotGlassLayers` regardless model, and overwrites only per-area opaque/window-layer/frame/divider fields without recomputing Window layer W, totals, energy, or reports. CP159 emits no direct diagnostic and has no validation, catch, rollback, or cleanup; dimension/material assertions, unchecked blind casts, indices, TDD/area/reflection denominators, trig/nonfinite inputs, child failures, or allocation can leave an ordered prefix. Six mutated owners require coordinated clear: HeatBalance, HeatBalSurf, Surfaces, Environment, DaylightingDevices placement-new reconstruction plus `HeatBalSurfMgr::clear_state`; the latter rearms first-time and recreates scratch arrays, Environment restores the previous-solar latch false, and dependency-owner clears alone do not replay CP159. Rust incident-solar forcing diagnostics and bounded opaque absorption, plus run-blocked multiplier/scheduled-inside declarations, are not this latch/distribution/window/report lifecycle. CP159 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 168 routines, split 58 state-mapped plus 110 source-mapped, with 61 required. CP160 adds required source-mapped `routine.init_int_solar_distribution` and its project-contract entry immediately after `init_solar_heat_gains` and before `compute_int_thermal_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 111, implemented at `HeatBalanceSurfaceManager.cc` lines 3778-4177, and called only as unconditional `InitIntSolarDistribution(state)` inside `InitSurfaceHeatBalance` line 468; the first-time flag gates only preceding progress text, and a CP160 failure blocks interior convection and the successful parent-tail flag clear. The only direct unit context calls twice: with InterZone disabled and beam report 10 W, the night call observes an already-zero report remain zero, while the sun-up call checks only 10/6 = 1.666667 W/m2. SunIsUp gates only that AllHT, non-Shading beam-report traversal; at night CP160 still distributes internal short wave and leaves prior beam intensity/amount/energy untouched. Every call assigns each Solar enclosure raw `EnclSolQD + sum(space QLTSW)` and lights-only `sum(QLTSW)`; an InterZoneWindow receiver/source double loop requires both receive flags, excludes the diagonal, recomputes source QLTSW, adds raw matrix-weighted total/lights/diffuse power, and overwrites diffuse-report energy after each qualifying source while a no-source receiver gets no energy write. The enclosure values are then multiplied by `solVMULT` and, only for InterZoneWindow, the diagonal fraction; there is no normalization or range/finite guard. Sun-up beam reporting divides Zone beam W by raw enclosure total area, then multiplies by Surface area plus divider area and timestep seconds. Zone/stored-Space traversal visits inclusive opaque then Window ranges without sorting, deduplication, class checks, or bound validation. Opaque total short-wave absorption uses current `SurfAbsSolarInt`, lights-only absorption uses base Construction `InsideAbsorpSolar`, and initial diffuse is added last. Under global movable insulation, every reached opaque record marked present first writes `SurfQRadSWOutMvIns = prior outside absorption * current exterior absorptance / base outside-material absorptance`, then overwrites outside absorption through an optional `MaterialFen.Trans` and a second division by current absorptance; a failed cast supplies zero transmittance and both denominators are unchecked. Direct retry feeds the overwritten outside value back, while a false global/present gate leaves that value and `SurfQRadSWOutMvIns` stale. Every Window dereferences current SurfaceWindow, radiant/Solar enclosures, and active Construction with no exterior-solar or class gate. Both EQL and non-EQL overwrite Window internal radiant gain from enclosure long wave, `radThermAbsMult`, and current thermal absorptance; a load-component pulse adds exactly 0.01 times radiant-enclosure floor area, but common frame/divider formulas keep the unpulsed value plus HVAC radiation. Non-EQL has no separate external-library branch and uses active conventional optics: unshaded layers add back diffuse absorption; shade/screen layers use shaded back optics; only IntBlind/ExtBlind interpolate pane absorption with unclamped `Interp`, so BGBlind skips pane addition while still receiving the later any-blind shade absorption. Only interior shade/blind writes shade long-wave absorption; exterior shade/blind/screen scales shade short wave by glazed fraction. Switchable layers use `InterpSw`, which clamps ordered factors including infinities to [0,1] but propagates NaN, and dereferences the shaded Construction without a zero-index guard. Zero-index or conditional/invalid shading can skip main layer work and later fail in the Detailed initial-diffuse tail. Positive frame/divider areas add Solar-enclosure short wave plus unpulsed internal/HVAC long wave with raw projection corrections; suspended dividers assert a last-layer MaterialGlass and use an unchecked multiple-reflection denominator, while IntShade asserts MaterialFen and IntBlind applies current solar/IR transmission. EQL uses active EQL NL and back diffuse optics and omits frame/divider work. For every positive adjacent link, a conventional target uses the current/source active layer count but adjacent base `AbsDiff`; an EQL target uses adjacent base EQL NL but current/source active `AbsDiffFrontEQL`. The final Detailed/BSDF/EQL tail adds prior initial-diffuse layer values, and Detailed shade/screen/blind also adds initial shade absorption. All CP160 Window additions mutate only per-area `SurfWinQRadSWwinAbs` and never refresh CP159 layer W, total W, or total energy; frame/divider additions likewise have no separate W/energy derivation here. The unconditional final TDD child is declared at `DaylightingDevices.hh` line 95, implemented at `DaylightingDevices.cc` lines 1506-1559, called only by CP160 line 4176, and has no direct unit-test call. It uses diffuser base `TransDiff`, stale CP159 diffuser total W, normally prior CP159/dependency dome layer one, and newly CP160-mutated diffuser layer one; its raw formula applies diffuser area even to the dome term and divides by `TransDiff`. Zero-first `max(0, rawGain)` maps negative, negative infinity, and NaN report values to zero while retaining positive infinity, but transition-zone gains use unclamped signed/nonfinite rawGain times length divided by unchecked total length. The current pipe report is committed before its transition loop, so failure can retain that report and a partial current `TZoneHeatGain` prefix after complete earlier pipes. Preceding `ManageInternalHeatGains`/`FigureTDDZoneGains` can zero current transition gains on BeginEnvironment but not the pipe report, and its current internal-gain update completes before CP160 writes new transition values. CP160 has no direct diagnostic, validation, return status, catch, rollback, or cleanup; only the suspended-divider and IntShade casts assert, while invalid topology, arrays, indices, layer mismatches, denominators, and nonfinite inputs can leave an ordered prefix. Re-entry mixes assigned enclosure/radiant/TDD state with additive Zone diffuse, opaque, layer, adjacency, frame/divider, and shade state; Sun-down, no-source, existing no-longer-reached range members, reduced transition-zone counts, and changed gates can preserve stale values, and normal parent order merely relies on preceding CP159 resets. Four directly mutated owners clear by placement-new: HeatBalance, HeatBalSurf, Surfaces, and DaylightingDevices; HeatBalSurfMgr clear only rearms the caller display flag. Rust separately consumes initialized/supplied opaque inside-short-wave state and has a bounded typed-Zone radiant-gain distributor whose ordered `<= 0` gates admit NaN, but no CP160 Solar-enclosure/interzone, Window/shade/frame/divider, adjacency, movable-insulation, TDD, report, failure, or re-entry state machine. CP160 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 169 routines, split 58 state-mapped plus 111 source-mapped, with 62 required. CP161 adds required source-mapped `routine.compute_int_thermal_absorp_factors` and its project-contract entry immediately after `init_int_solar_distribution` and before `compute_int_sw_absorp_factors` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 113, implemented at `HeatBalanceSurfaceManager.cc` lines 4179-4295, and called only by unconditional `InitSurfaceHeatBalance` line 427; runtime execution is after shading-status production and before CP162, CP159, and CP160. The caller first-time flag gates only progress text plus the separate preceding `InitInteriorRadExchange`, not CP161, and a CP161 failure blocks every later child and the successful caller-tail flag clear. Its sole direct unit test uses one 1 m2 `IntBlind` Window with 0.1 shade plus 0.1 glass effective emissivity and checks only `SurfAbsThermalInt = 0.2` and `radThermAbsMult = 5`. Pass one scans the full allocated `EnclRadInfo` container, skips false `radReCalc`, then visits stored enclosure Space Window ranges: `IntShade`/`IntBlind` assign effective shade-plus-glass emissivity and every other flag assigns active-Construction inside thermal absorptance. Pass two, independently of `radReCalc`, scans `SurfMovSlatsIndexList` only when `AnyMovableSlat` and rewrites only currently interior shade/blind entries. Pass three again scans the full allocated enclosure container and each flagged `SurfacePtr`: nonswitchable area uses current `SurfAbsThermalInt`, while switchable area uses clamped-factor `InterpSw` between declared base-Construction and `activeShadedConstruction` absorptance. Positive frames add area times `(1 + 0.5*projection)` times emissivity. Positive dividers start with divider emissivity, substitute base-Construction absorptance when suspended, and either use the projected term or, for an interior device with a declared layer, assert the active shaded Construction last material as `MaterialFen` and add shade effective emissivity plus divider absorptance times shade thermal transmittance or current blind back-IR transmittance; the shaded Construction is dereferenced before the layer-present test. This preserves deliberate active/base/runtime-optics asymmetries. The first pass visits full Window ranges, but normally initialized `SurfacePtr` holds representative surfaces only; CP161 ignores constituent-aggregated `EnclRadInfo.Area` and uses raw representative Surface/frame/divider areas, creating a representative-extent mismatch. The producer bulk paths use separate Solar and radiant enclosure counts, while its shading-change loop is Solar-count-bounded but reads same-index radiant `SurfacePtr`; CP161 range-for can see a retained radiant-container tail and never clears `EnclRadInfo.radReCalc`. CP162 reads the corresponding `EnclSolInfo.radReCalc`, not the same flag object. Normal movable slats are observed by the producer through `SurfacePtr`, while direct or malformed topology can still let the independent list update an unflagged enclosure. More importantly, CP158 can change opaque inside thermal absorptance without that movable-insulation transition itself setting `radReCalc`, leaving a normal-path multiplier stale absent another producer cause. Stored orders have no local sort, deduplication, class/model, membership, or bounds validation. `radThermAbsMult = 1/SUM1` is committed only after a complete flagged enclosure with no zero, sign, minimum, or finite guard: zero becomes positive infinity, signed infinity becomes signed zero, negative stays negative, and NaN propagates; `InterpSw` clamps ordered factors including infinities but preserves NaN and raw nonfinite endpoint arithmetic. CP161 has no direct diagnostic, return status, allocation, catch, rollback, or cleanup and only the divider material assertion. Failure can leave a first-pass Window prefix, then a movable-list prefix, or complete earlier enclosure multipliers while the current/later multipliers remain old; false `radReCalc`, no-longer-reached range/list entries, malformed independent slat updates, and direct retry can preserve selective stale state, while a nonpositive frame/divider area simply drops from a recomputed flagged sum. Exactly two direct owners have mutated targets: HeatBalSurf owns the written `SurfAbsThermalInt` and separately supplies the read-only movable-slat list, while ViewFactor owns the written `radThermAbsMult` and separately supplies read-only enclosure flags/topology; Surfaces, HeatBalance topology, Construction, Material, Window, SolarShading, and interior-radiation state are dependencies only. Rust has only a bounded typed-Zone radiant-gain distributor that applies `.max(0.0)` to absorptance but whose ordered `<= 0` gain/sum gates admit NaN, not this recalculation/shading/representative/frame-divider/raw-reciprocal state machine. CP161 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 170 routines, split 58 state-mapped plus 112 source-mapped, with 63 required. CP162 adds required source-mapped `routine.compute_int_sw_absorp_factors` and its project-contract entry immediately after `compute_int_thermal_absorp_factors` and before `compute_dif_sol_exc_zones_wiz_windows` in source-definition order. It is declared at `HeatBalanceSurfaceManager.hh` line 115, implemented at `HeatBalanceSurfaceManager.cc` lines 4297-4471, and called only by unconditional `InitSurfaceHeatBalance` line 433 immediately after CP161; the caller first-time flag gates only the preceding diffuse-solar progress text, and a CP162 failure blocks conditional interzone diffuse exchange, daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. There is no direct unit-test call. CP162 scans the full allocated `EnclSolInfo` container, skips false `radReCalc`, and accumulates each flagged enclosure over stored `SurfacePtr` order without local sort, deduplication, class, membership, index, or finite validation. Normal Solar topology contains every stored Space Surface except AirBoundary, including nonrepresentatives, whereas the shading-change producer is Solar-count-bounded but observes same-index representative-only radiant `SurfacePtr`; this records an observer/calculation-domain asymmetry without claiming a normal unflagged constituent. Only direct, malformed, representative-misaligned topology or count/container mismatch can turn that asymmetry into an unflagged Solar enclosure. Each Surface selects its active Construction and is opaque only when active `TransDiff <= 0`; opaque area uses current `SurfAbsSolarInt`, while NaN transmittance enters the Window branch. Conventional versus equivalent-layer selection then uses the declared base Construction `WindowTypeEQL`. Conventional Windows sum active-layer back absorptances and active diffuse transmission; only a nonzero storm-aware `SurfWinActiveShadedConstruction` enables shade/screen/blind optical substitution: shade/screen optics come from that Construction, blinds raw-linearly interpolate current slat endpoints, and a shade/blind flag with index zero silently retains bare active optics and zero shade absorption. Switchable glazing alone has two separate positive-shaded-Construction assertions and clamped-factor `InterpSw` for layer absorption and transmission. Their raw Surface-area term is `TransDiffWin + AbsDiffTotWin + DiffAbsShade`; positive frames add area times solar absorptance times `(1 + 0.5*projection)`. Positive dividers use current divider solar absorptance; suspended dividers assert the active Construction last layer as `MaterialGlass` and apply an unchecked glass/divider multiple-reflection denominator. Interior shade/blind dividers omit projection and add `DividerAbs + DiffAbsShade`; all others use `(1 + projection)*DividerAbs`. The base-EQL branch instead consumes the active Construction EQL pointer, layer count, diffuse transmission, and back absorptances and ignores shading, switching, frames, and dividers. After a complete enclosure, only strict `SUM1 > 0.01` writes `solVMULT = 1/SUM1`; positive infinity becomes positive zero, while exactly 0.01, smaller/negative values, negative infinity, and NaN enter the bad-sum branch, issue the exact misspelled `ComputeIntSWAbsorbFactors` warning only while `solAbsFirstCalc` is true, set that latch false after the warning returns, and write zero. A later good sum or BeginEnvironment does not rearm the latch. False recalculation preserves both targets; failures can leave earlier enclosure commits and the current/later multipliers and latch old, while warning-pipeline failure can leave partial diagnostic side effects before either target is committed. CP158 can change opaque `SurfAbsSolarInt` without itself producing a recalculation flag, giving the normal-path stale-multiplier case; direct or malformed Solar/radiant observer misalignment can also preserve stale state. ViewFactor is the sole direct mutation owner for `solVMULT` and `solAbsFirstCalc`; warning counters/streams/SQLite/callbacks are child diagnostic side effects, while Surface, HeatBalSurf, Construction, Material, equivalent-layer, Window, SolarShading, global, and topology state are dependencies. Rust has no `ComputeIntSWAbsorpFactors`, Solar multiplier, latch, or post-initialization production writer for `inside_shortwave_absorbed_w_per_m2`; its separate typed-Zone thermal-radiant helper has ordered `<= 0` gain/sum gates that admit NaN and is not this short-wave routine. CP162 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 171 routines, split 58 state-mapped plus 113 source-mapped, with 64 required. CP163 adds required source-mapped `routine.compute_dif_sol_exc_zones_wiz_windows` immediately after `compute_int_sw_absorp_factors` and before `init_ems_controlled_surface_properties` in full source-definition order; its project-contract entry remains after `compute_int_sw_absorp_factors` and before `calc_heat_balance_outside_surf` in required-routine order. It is declared at `HeatBalanceSurfaceManager.hh` line 117, implemented at `HeatBalanceSurfaceManager.cc` lines 4473-4644, and called only at `InitSurfaceHeatBalance` line 439 inside the lines-435-440 `InterZoneWindow` block; that parent flag gates both progress-and-call, while the nested first-time flag gates only the exact progress text. A guarded CP163 failure blocks daylighting, interior long-wave exchange, CP159, CP160, and the successful caller-tail flag clear. Exactly three direct calls occur in one unit fixture: with three enclosures and two reciprocal 1 m2, 0.1-transmittance Windows, the first call leaves receiver `HasInterZoneWindow` false and checks only identity diagonals plus all receive flags false; before the second call the fixture enables enclosures 1 and 2 and checks only flags true, true, false; before the third it enables `KickOffSimulation` and again checks only identity diagonals plus false flags. No test asserts a matrix coefficient or covers sizing kickoff, denominators, multi-enclosure paths, nonfinite values, allocation incoherence, or either assertion's failure. Allocation is gated solely by an unallocated `ZoneFractDifShortZtoZ`: that path allocates the target square matrix, receive-flag vector, then separate `DiffuseArray` scratch. Every call resets flags false, target identity, then scratch identity before either kickoff flag can return; already allocated dimensions are not repaired. Using the consumers' row-receiver/column-source convention, a stored-order `AllHTWindowSurfaceList` pass skips nonpositive adjacency, Surface-self adjacency, declared/base `TransDiff <= 0`, and a false receiver `HasInterZoneWindow`, then adds `A(receiver, source) += TransDiff * receiver.solVMULT * Area`; it does not use active/shaded/storm optics, validate the adjacent endpoint, sort, deduplicate, or validate indices, class, sign, range, or finiteness. The receiver flag is set when `solVMULT != 0`, independently of the coefficient. Same-enclosure paired Surfaces can modify raw diagonal state that the next transform discards. With raw matrix `A` frozen and identity scratch `D`, every distinct pair computes `D(R,S) = A(R,S)/(1-A(R,S)*A(S,R))`, and `D(N,N) = 1 + sum[M != N](A(N,M)*D(M,N))`; there is no denominator or finite guard. The copied target is followed by exactly two dimension assertions. A later scan only ORs flag `N` when some distinct receiver has `D(M,N) > 0`, making that scan a positive source-column witness rather than the direct pass's receiver witness. Frozen `D` then contributes every flagged, node-distinct simple path of exactly two, three, and four edges with products `D(J,K)*D(I,J)`, `D(K,L)*D(J,K)*D(I,J)`, and `D(L,M)*D(K,L)*D(J,K)*D(I,J)` into receiver/source cells `Z(I,K)`, `Z(I,L)`, and `Z(I,M)`; additions never feed later products, diagonals are not updated, node revisits and paths beyond four edges are omitted. Edge gates skip only exact signed zero, so negative, infinite, and NaN coefficients enter; ordered `> 0` flagging rejects negative and NaN but accepts positive infinity. CP163 emits no diagnostic and has no status, catch, rollback, cleanup, or local validation. Allocation, reset, direct-pass, pair-copy, flag-scan, or path failure can leave the corresponding ordered prefix; a coherent direct retry first erases all three objects, while kickoff commits false/identity state and returns. When production `InterZoneWindow` is false, CP163 is not entered and prior target/flag/scratch state remains dormant stale; CP159 and CP160 gate their normal interzone consumers on the same flag. Exactly two direct owners mutate: HeatBalSurf owns the target matrix and flags and clears them by placement-new and also supplies the read-only caller gate `InterZoneWindow`; CP163 never writes that gate and placement-new restores it false. HeatBalSurfMgr owns and explicitly clears scratch. Independent owner clears can desynchronize the single-guard allocation assumption in either direction. Rust has adjacent-zone opaque heat-transfer links and window-optical declarations but no Solar-enclosure exchange matrix/flags, bilateral transform, fixed simple-path expansion, kickoff reset, or matching failure/re-entry state. CP163 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 172 routines, split 58 state-mapped plus 114 source-mapped, with 65 required. CP164 adds non-required source-mapped `routine.init_ems_controlled_surface_properties` immediately after `compute_dif_sol_exc_zones_wiz_windows` and before `init_ems_controlled_constructions` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 119, implemented at `HeatBalanceSurfaceManager.cc` lines 4646-4720, and called only from `HeatBalanceManager.cc` line 2663 under `AnyEnergyManagementSystemInModel`; the same guard calls definition-next CP165 at line 2662 first, so CP165 is CP164's runtime predecessor, not successor. `ManageHeatBalance` calls the before-init EMS point before `InitHeatBalance`/CP164 and the after-init EMS point afterward, so CP164 observes actuator state as it stands after the before-init EMS point while after-init changes wait for a later entry. No unit test calls CP164 directly or references its three override-on fields. Each entered call first sets `dataGlobal->AnySurfPropOverridesInModel` false, then scans stored Material pointers until the first Regular material with any Solar, Thermal, or Visible override active; non-Regular flags cannot set the global, and no active Regular takes an immediate return. That last-off return does not restore prior working Material absorptances or Construction face copies, and the false global suppresses the normal non-BeginSim Surface refresh and override-caused enclosure recalculation absent another cause. With any active Regular override, a second full Material pass recomputes every Regular material in Solar, Thermal, Visible order: active channels use `max(min(value,0.9999),0.0001)` and inactive channels restore raw input, while non-Regular materials remain untouched. ObjexxFCL's ordered ternaries clamp negative infinity to 0.0001 and positive infinity or NaN to 0.9999; inactive input values are unguarded. The Construction pass skips Windows and exactly zero layers, then for every other layer count copies a nonzero last-layer endpoint into inside Visible, Solar, Thermal and a nonzero first-layer endpoint into outside Visible, Solar, Thermal. Window, zero-layer, and zero-endpoint faces retain old values; endpoint materials need not be Regular, and negative/oversized counts or indices, null pointers, and topology are unchecked. CP164 has no allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. A malformed detection pointer can fail after the initial global write but before any current-material target write; a second-pass pointer failure leaves only complete prior Regular-material triples, and invalid endpoint access fails before the current face's first Visible copy although an outside-face failure can follow a complete inside-face triple; active re-entry overwrites reached state, last-off or non-Regular-only re-entry preserves stale values, and a false outer EMS gate preserves everything without entry. Exactly three direct mutation owners are DataGlobal, MaterialData, and ConstructionData; their independent clears do not replay a coherent projection. Rust immutable material absorptances and run-blocked variable-absorptance declarations are not this EMS actuator/global-flag/mutable endpoint lifecycle. CP164 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 173 routines, split 58 state-mapped plus 115 source-mapped, with 65 required. CP165 adds non-required source-mapped `routine.init_ems_controlled_constructions` immediately after `init_ems_controlled_surface_properties` and before `update_intermediate_surface_heat_balance_results` in source-definition order, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 121, implemented at `HeatBalanceSurfaceManager.cc` lines 4722-4940, and called only from `HeatBalanceManager.cc` line 2662 under `AnyEnergyManagementSystemInModel`, immediately before CP164 at line 2663. It observes actuator state as it stands after the before-init EMS point; after-init changes wait for a later `InitHeatBalance` entry. No unit test calls CP165 directly. Three `HeatBalanceManager` integration fixtures exercise it indirectly: the line-1373 Window fixture checks morning/afternoon Construction plus downstream transmittance/daylight after repeated manager calls, while the line-1974 Window and line-2181 CondFD fixtures check only final current Construction equal to the override value and the ON flag true. They do not cover no-ON last-off state, ON-nonpositive/OFF restoration, Window cache overwrite, cached-bad retention, first-versus-next-call asymmetry, CTF/EMPD warning or source-loss branches, CondFD node or source-loss branches, HAMT, Kiva, another algorithm, invalid targets, exact diagnostics, partial failure, or retry. Each entered call first sets `dataGlobal->AnyConstrOverridesInModel` false and scans only the Surface ON flags. Any ON flag, including one paired with a zero or negative integer value, sets it true and enters the full pass; zero ON flags take a stale quick return without restoring current/active Construction, `IsUsed`, or cache state. In the full pass, every OFF Surface and every ON Surface with a nonpositive integer value restores `Surface.Construction` and `SurfActiveConstruction` from `ConstructionStoredInputValue` but does not clear `IsUsed` or either compatibility cache. A positive value is used as an unchecked Construction/cache index. A Window target ignores Surface class and current heat-transfer algorithm, overwrites even a cached rejection to checked/okay before testing the pair, and fast-applies the target in the same call; an already checked/okay pair likewise writes current Construction, marks target `IsUsed`, and writes active Construction. A cached-bad pair does nothing and preserves whatever current/active Construction was already present. An unchecked CTF/EMPD pair premarks okay/checked, then orders a histories Warning plus three Continue calls, a terms Warning plus three Continue calls, and a one-way current-source/sink to target-without-source/sink Severe plus three Continue calls, for 12 possible diagnostics; only source loss rejects. CondFD similarly premarks, orders a node-mismatch Severe plus three Continue calls before an independent source-loss Severe plus three, and can emit eight; either mismatch rejects. HAMT and Kiva each emit Severe plus one Continue and cache rejection. Every other algorithm remains unchecked, silent, and unchanged, while Window, cached-good, cached-bad, and restore branches emit no CP165 diagnostic. A successful first unchecked CTF/EMPD or CondFD evaluation writes only `Surface.Construction`; only a later checked/okay call also marks `IsUsed` and updates `SurfActiveConstruction`. The historical cache is keyed only by target Construction and Surface, while compatibility reads the mutable current Construction, so target switching can validate against a prior override and later cache hits never revalidate changed current state. An invalid positive target fails at the initial `TypeIsWindow` dereference before premarking. CTF/EMPD warning interruption leaves a true cache and skips the remaining checks on retry, while interruption of its source-loss rejection can fast-accept a forbidden target. A failed CondFD node read or interrupted node diagnostic before the false write can likewise fast-accept, but after a complete node rejection has written false, a later source-loss diagnostic failure retries cached-bad; when node counts match, interruption of the sole source-loss rejection can fast-accept. Interrupted HAMT/Kiva diagnostics leave the pair available to diagnose again. CP165 performs no body allocation, status return, assertion, catch, rollback, cleanup, or index/topology validation, and cache/diagnostic/current/`IsUsed`/active failures can leave ordered prefixes. `AnyConstrOverridesInModel` independently disables the `SimpleCTFOnly` history fast path and forces Solar/radiant-enclosure recalculation even when the sole ON value is nonpositive. The four direct mutation owners are DataGlobal, SurfacesData, ConstructionData, and RuntimeLanguageData; diagnostic counters/streams/SQLite/callbacks are child side effects, and independent clears cannot replay a coherent current/active/cache/use projection. Rust typed Constructions, immutable surface references, positive construction-use evidence, and diagnostic CTF-coefficient overrides provide no EMS Construction actuator, mutable current/active selection, compatibility cache, `IsUsed` lifecycle, or matching diagnostics. CP165 adds no EnergyPlus source inventory, project contract, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 174 routines, split 58 state-mapped plus 116 source-mapped, with 65 required. CP166 adds required source-mapped `routine.update_intermediate_surface_heat_balance_results` after `init_ems_controlled_constructions` and before CP167's `update_non_representative_surface_results` in the mapped Surface-manager ledger; its project-contract requirement is after `calculate_zone_mrt` and before `manage_air_heat_balance`, preserving the inside-parent/general-or-optimized-child/MRT/tail grouping. It is declared at `HeatBalanceSurfaceManager.hh` line 132, implemented at `HeatBalanceSurfaceManager.cc` lines 4951-5020, and called in production only from `CalcHeatBalanceInsideSurf` line 7812 immediately after `CalculateZoneMRT`; failure always blocks the wrapper return and, on the main Surface-manager path, downstream Air balance. Absent `ZoneToResimulate`, three Zone/Space passes use inclusive bounds 1 through `NumOfZones`; when present, both bounds become the unchecked supplied integer. The sole production partial caller normally dereferences that Zone's lists before CP166, while a direct present zero, negative, or greater-than-`NumOfZones` value reaches CP166's invalid Zone dereference; if an `INT_MAX` body completed, loop increment would overflow. With an absent nonpositive `NumOfZones`, CP166's three Zone loops and CP167's single Zone/Space traversal are empty but the global Kiva pass still runs. One direct unit fixture calls CP166 four times with the optional argument absent at lines 8694, 8702, 8710, and 8718 and asserts only `SurfQdotRadSolarInRepPerArea = SurfOpaqQRadSWInAbs - SurfQdotRadLightsInPerArea` for exact scenarios 0, 3, -0.1, and 0.00001 within tolerance; its Window and convective ranges are empty, representative mode is false, and the Kiva list is empty. The first pass visits each selected Zone's stored Space order and inclusive Window range, adding only `ExtSolar` `SurfWinHeatGain` values into the existing `ZoneWinHeatGain` without resetting it. After every Space, `>= 0` writes only gain rate then gain energy, while the negative branch writes only negated loss rate then loss energy; the opposite sign pair remains stale, zero and negative zero use gain, positive infinity uses gain, and negative infinity or NaN use loss. `InitSolarHeatGains` normally resets these five Zone arrays earlier, but direct or repeated CP166 entry accumulates Window gain again, and a multi-Space failure can leave reports lagging a newer partial sum. When `UseRepresentativeSurfaceCalculations` is true, CP166 then calls definition-next CP167 with the same optional argument; because this is after Window accumulation, the current call can sum pre-child nonrepresentative Window gain, while CP167 mutations feed later assignments and later calls. The second selected-Zone pass overwrites each inclusive opaque-or-Window range with `SurfQdotConvInPerArea = -SurfHConvInt * (SurfTempIn - RefAirTemp)`. The third overwrites each opaque-or-internal-mass range with the tested solar-minus-lights expression. Repeated Space/range membership overwrites the same targets again, while empty, inverted, or unreached ranges preserve prior per-Surface values. Finally, regardless of optional Zone bounds or `NumOfZones`, CP166 visits the global stored `AllHTKivaSurfaceList` and overwrites each `SurfOpaqInsFaceCondFlux` with the negated ordered sum of inside convection, net long-wave, HVAC radiation, internal-gain radiation, and absorbed short-wave terms; a nonselected Kiva Surface can therefore consume stale convection and still be mutated. All arithmetic is raw: there is no finite, sign, range, topology, duplicate, or overflow guard, so infinities and NaN follow IEEE operations and duplicate Window membership repeats additions. CP166 has no body allocation, diagnostic, status, assertion, catch, rollback, cleanup, or validation. Failure can leave ordered prefixes of Window additions, per-Space sign-report rate/energy pairs, CP167 child effects, convective assignments, solar-minus-lights assignments, and global Kiva assignments; scalar expressions finish their reads before the current target write. Re-entry accumulates Window gains but overwrites the three per-Surface results from current inputs, preserving stale opposite-sign Zone reports. Exactly two direct owners mutate: HeatBalanceData owns the five Zone arrays and HeatBalSurfData owns convection, solar-minus-lights, and Kiva flux targets; both clear by placement-new. CP167 can indirectly mutate those plus placement-new-cleared SurfacesData and explicitly cleared HeatBalSurfMgr state. Independent clears cannot replay the projection. Rust has a separate bounded inside-convection report formula and typed surface state, but no CP166 Zone/Space/window-report orchestration, representative child, solar-minus-lights report state, global optional-zone-independent Kiva pass, or matching failure/re-entry lifecycle. CP166 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 175 routines, split 58 state-mapped plus 117 source-mapped, with 66 required. CP167 adds non-required source-mapped `routine.update_non_representative_surface_results` immediately after `update_intermediate_surface_heat_balance_results` and before `gather_for_predefined_report` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 134, implemented at `HeatBalanceSurfaceManager.cc` lines 5022-5174, and called only by CP166 line 4985 after CP166's additive Window pass and under `UseRepresentativeSurfaceCalculations`; CP167 itself does not test that flag, so a direct call bypasses it. No unit test calls CP167, and CP166's sole direct fixture explicitly disables representative calculations; a separate topology-only `DataSurfaces.unit.cc` fixture proves that a zero-area constituent can map to an area-20 representative but does not execute CP167. An absent optional Zone selects inclusive 1 through `NumOfZones` while a present raw integer selects one unchecked Zone; one outer traversal follows stored Zone/Space order and executes each reached Space's inclusive HT, opaque/internal-mass, then Window ranges before the next Space or Zone, without sorting, deduplication, class, membership, bounds, finite, or topology validation. Inverted ranges are empty, malformed 0-through-0 enters index zero, repeated ranges overwrite again, self representatives are skipped, and non-self chains copy one raw hop whose state may already have been projected earlier in traversal. The per-Space HT range performs at most 21 ordered writes: `SurfTempIn`, `SurfTempOut`, `SurfTempEffBulkAir`, `SurfHConvInt`, `SurfQdotRadNetLWInPerArea`, `SurfQdotRadIntGainsInPerArea`, `SurfQdotRadHVACInPerArea`, `SurfQdotConvOutPerArea`, `SurfHConvExt`, `SurfQdotRadOutRepPerArea`, `SurfHAirExt`, `SurfHSkyExt`, `SurfHGrdExt`, `SurfTAirRef`, conditional `SurfTAirRefRpt`, exterior-convection `hfModelEq` then `hnModelEq`, both additional heat-source values, `SurfTempInTmp`, and `RefAirTemp`. The report flag is recomputed from the copied enum only when it is not Invalid, leaving an Invalid target report stale and allowing another malformed enum to fail its unchecked table lookup; only the two named exterior-convection members are copied, and the `#if 0` diagnostic block is not built. The following per-Space opaque/internal-mass range copies `SurfQdotRadLightsInPerArea`, `SurfOpaqQRadSWOutAbs`, then `SurfOpaqQRadSWInAbs`. The final per-Space Window range computes target Area divided by the same target Area, so every finite nonzero value including a negative gives one while zero, infinity, or NaN gives NaN; glazing gains always use that ratio. Positive target frame/divider areas separately divide by unchecked representative areas and copy gain, loss, inside temperature, then outside-surface temperature; false branches preserve those four fields and contribute local zero. The combined frame/divider gain is always overwritten and whole gain becomes representative whole gain minus representative aggregate times the self-area ratio plus the new target aggregate, so two false branches preserve eight detailed fields but zero the aggregate. Raw nonfinite arithmetic, zero denominators, overflow, malformed chains, and repeat-call scaling are unguarded. CP167 has no body allocation, enabled diagnostic/assertion, status, catch, rollback, cleanup, or validation; failures can leave complete earlier-Space projections followed by ordered current-Space HT, opaque, glazing, frame/divider, aggregate, and whole-gain prefixes, while retry overwrites reached fields but preserves self, false-branch, empty, no-longer-reached, and Invalid-report stale state and can compound traversal-dependent chains. HeatBalSurfData, HeatBalanceData, and SurfacesData clear their direct mutations by placement-new, while HeatBalSurfMgr explicitly clears `RefAirTemp`; independent clears cannot replay the four-owner projection. Rust has bounded opaque `SurfaceHeatBalanceState` and typed `WindowFrameAndDivider` declarations but no representative flag/topology/ID projection, mutable matching Window gain/report lifecycle, or CP167 runtime/failure/re-entry behavior, and runtime Surface indexes leave fenestration empty. CP167 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 176 routines, split 58 state-mapped plus 118 source-mapped, with 66 required. CP168 adds required source-mapped `routine.calculate_zone_mrt` immediately after `update_thermal_histories` in the Surface-manager routine ledger; its project-contract requirement is after `calc_heat_balance_inside_surf_2_ctf_only` and before `update_intermediate_surface_heat_balance_results`. It is declared at `HeatBalanceSurfaceManager.hh` lines 140-141, implemented at `HeatBalanceSurfaceManager.cc` lines 5583-5699, and called in production only from `CalcHeatBalanceInsideSurf` line 7811 after the selected solve and immediately before CP166. One `SurfaceGeometry.unit.cc` fixture calls it three times with the optional Zone absent: all-five-Surface 10 C checks Zone/enclosure/Space MRT 10; Zone Surface temperatures 10/20/30 check Zone MRTs plus shared enclosure 16.667; and, after zeroing five Surface areas and manually rearming first-time state, Zone MAT fallbacks 20/25/28 and low-AE enclosure results 21.5/22 derived from Space MATs. The fixture does not assert Space Volume or which volume subbranch executes, and those expected outputs do not independently distinguish the strict volume-weighted branch from the arithmetic fallback; no test covers warnings, optional selection, cache staleness, exact thresholds, nonfinite/topology errors, failure, or retry. While `CalculateZoneMRTfirstTime` is true, CP168 allocates `SurfaceAE(TotSurfaces)` and `ZoneAESum(NumOfZones)`, zeros both plus every enclosure `sumAE`, then globally caches `Area * current Construction.InsideAbsorpThermal` only for numeric `HeatTransSurf` entries; positive-only Zone and radiant-enclosure index gates accumulate the same raw value without upper, finite, Construction, or topology validation. A partial first call still builds the global cache, which remains stale across later dimension/property/topology changes until HeatBalSurfMgr clear rearms the flag; only the successful routine tail clears it. A present raw Zone directly visits stored Spaces, zeroes each referenced enclosure `sumAET`, then sets `reCalcMRT` true; an absent argument sets every enclosure flag true without pre-zeroing. No source path writes those flags false, so a full call makes every later partial call recalculate all enclosures. Even a first partial call recomputes a marked shared enclosure from its complete `SurfacePtr` and writes every `spaceNums` MRT, including nonselected Zones. The selected-Zone loop uses strict cached `ZoneAESum > 0.01` and stored Space inclusive HT ranges: each `surfAET = SurfaceAE * SurfTempIn` enters the local numerator and unconditionally adds through the Surface's raw radiant-enclosure index, then `Zone.MRT = sumAET / ZoneAESum`. Exactly 0.01, negative, or NaN denominators take first-time Warning-plus-Continue then own-MAT fallback; later fallbacks are silent. Every true-flag enclosure uses strict `sumAE > 0.01`, resets `sumAET`, recomputes over complete `SurfacePtr`, and divides by `sumAE`, discarding prior Zone-loop additions. Low `sumAE` preserves `sumAET`, warns/continues only first-time, and computes stored-Space MAT from `sumMATVol/sumVol` when raw `sumVol > 0.01`, otherwise `sumMAT/(int)spaceNums.size()` with no empty or cast guard; either result is copied to every listed Space. Duplicate/raw lists and ranges repeat work, stale numerators can mismatch cached denominators, malformed 0-through-0 ranges enter index zero, and signs, exact thresholds, infinities, NaN, overflow, empty division, and invalid indices are unguarded. CP168 has no status, assertion, catch, rollback, cleanup, or local validation; allocations/cache writes, optional zero/flag pairs, Zone enclosure additions and MRTs, Warning/Continue effects, enclosure reset/add/MRT, and Space-copy prefixes can remain. A failed first-time retry rebuilds and rewarns but does not globally reset flags, `sumAET`, or prior MRT; later failures reuse stale caches and suppress fallback warnings. HeatBalSurfMgr explicitly owns/clears the two caches and flag, ViewFactorInfoData explicitly owns/clears enclosure sums/MRT/flags, and ZoneTempPredictorCorrectorData placement-new clears Zone/Space MRT; independent clears cannot replay the projection. Rust has bounded Surface area, inside thermal absorptance/temperature, and separate radiant helpers, but no Zone/Space/enclosure MRT state, cached weighting topology, recalculation flags, MAT fallback/diagnostic lifecycle, or CP168 analog. CP168 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 177 routines, split 58 state-mapped plus 119 source-mapped, with 67 required. CP169 adds non-required source-mapped `routine.report_non_representative_surface_results` immediately after `report_surface_heat_balance` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 151, implemented at `HeatBalanceSurfaceManager.cc` lines 6893-6930, and called in production only by `ReportSurfaceHeatBalance` line 6618 under `UseRepresentativeSurfaceCalculations`, immediately after `SolarShading::ReportSurfaceShading`; CP169 itself has no guard, so a direct call bypasses representative-mode selection. No unit test calls CP169 directly, and the seven direct unit calls to its parent leave representative mode false; the separate representative-topology fixture does not execute reporting. CP169 scans numeric Zones, stored Spaces, and each inclusive HT range, and every non-self target copies the representative's inside then outside `convClassRpt` integer without validation. After each Space's HT pass, `DisplayAdvancedReportVariables` independently gates its inclusive Window range. Every non-self Window computes `Surface(surfNum).Area / Surface(surfNum).Area`, so finite nonzero target areas including negative values yield one while zero, infinity, or NaN yields NaN; it then copies representative convective glazing gain, infrared glazing gain, and shortwave loss in that order through the raw ratio. It does not copy energies, frame/shade/gap terms, or whole Window heat gain and does not recompute any dependent report, so a false advanced gate and self, empty, inverted, or unreached ranges preserve prior values. This later report pass can overwrite CP167's earlier two glazing-component projections and adds the shortwave-loss projection only when advanced reporting is enabled. Raw Zone/Space/range/representative indices, class and membership, duplicates, chains, finiteness, and loop overflow are unchecked; 0-through-0 ranges enter index zero, duplicate or chained targets can consume earlier projections in traversal order, and all arithmetic follows IEEE behavior. CP169 has no allocation, child call, diagnostic, status, assertion, catch, rollback, cleanup, or local validation. Failure can leave complete earlier Spaces and earlier Surfaces within the current Space plus an inside-class-only HT prefix or one/two Window-component writes; coherent retry overwrites reached fields from current representative state but preserves skipped stale state and remains traversal-dependent for chains. SurfacesData alone owns all five targets and clears them by placement-new; Rust has no representative topology, convection-class report state, matching Window component lifecycle, advanced-report gate, or CP169 analog. CP169 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 178 routines, split 58 state-mapped plus 120 source-mapped, with 67 required. CP170 adds non-required source-mapped `routine.report_int_mov_ins_inside_surf_temp` immediately after `report_non_representative_surface_results` and before `gather_component_loads_surface` in the Surface-manager ledger, with no project-contract entry. It is declared at `HeatBalanceSurfaceManager.hh` line 153, implemented at `HeatBalanceSurfaceManager.cc` lines 6932-6940, and called in production only by `ReportSurfaceHeatBalance` line 6741 under `AnyMovableInsulation`, after the parent Window reports and before opaque reporting. CP170 itself never reads that flag, so direct calls bypass the guard, while exterior-only movable insulation can make the parent call it even with an empty interior list. One direct unit fixture allocates two entries and calls CP170 once: a listed `present = false` Surface changes its prior report from 1.23 to the base `SurfTempIn` 23.0, and a listed `present = true` Surface changes 1.23 to `SurfTempInTmp` 12.3, both within 0.00001; it does not cover the parent gate, empty or malformed lists, duplicates, dimensions, nonfinite values, failure, or retry. Every entered call first assigns the complete `SurfTempIn` array to `SurfTempInMovInsRep`, then visits `intMovInsulSurfNums` in stored order and overwrites only raw indices whose `intMovInsuls(SurfNum).present` is true with `SurfTempInTmp(SurfNum)`. An empty list therefore leaves the full base copy; unlisted and listed-false entries keep the base value, and duplicates re-read current presence/input and repeat the same indexed overwrite. There is no arithmetic, clamp, finite filter, list repair, deduplication, bounds or dimension validation, child call, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. The first ObjexxFCL Array1D assignment is allocation-capable: a nonconformable target receives the source index range and is resized, which can allocate, deallocate, or reconstruct storage before element copy; a conformable target keeps its shape and overwrites all elements. Resize/allocation/range or copy failure reaches no list work and has no CP170 rollback guarantee; after a successful full copy, a bad raw index or input access can leave the complete base copy plus every earlier successful present override. A retry repeats the shape-conforming or shape-rebuilding assignment from current `SurfTempIn` and then reevaluates the current list, presence flags, and temporary temperatures, so a successful retry erases stale prior overrides for removed or now-false entries while remaining allocation- and raw-index-failure-prone. HeatBalSurfData alone owns the direct target and clears it by placement-new; normal setup dimensions it to `TotSurfaces` and BeginEnvironment zeroes reached entries, while SurfacesData separately owns/defaults/clears the gate, list, and insulation records. Rust has no movable-insulation topology/state, interior movable-insulation temperature report, conditional override pass, or matching failure/re-entry lifecycle. CP170 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 179 routines, split 58 state-mapped plus 121 source-mapped, with 67 required. CP171 remains immediately after `calc_heat_balance_outside_surf` in the Surface-manager definition ledger; CP175 now interposes `calc_outside_surf_temp` before CP171 in the heat-balance project execution list. It is declared at `HeatBalanceSurfaceManager.hh` line 173, implemented at `HeatBalanceSurfaceManager.cc` lines 7723-7736, and called in production only on the right-hand side of the `CalcHeatBalanceOutsideSurf` line-7717 common-tail assignment. The parent reaches that tail through Zone order, stored Space order, and inclusive HT ranges, but skips every Window, optional-zone-unrelated Surface, and line-7440 ExternalEnvironment EcoRoof; every other surviving boundary path reaches CP171. The helper binds raw `Surface(SurfNum)` and copies `OSCMPtr`; a strict positive pointer returns `-OSCM.HConv * (SurfOutsideTempHist(1) - OSCM.TConv)` and wins even when rain is true, ignoring `SurfHConvExt` and both weather temperatures. Zero or negative pointers fall through: rain uses `-SurfHConvExt * (SurfOutsideTempHist(1) - SurfOutWetBulbTemp)` and no rain uses the same expression with `SurfOutDryBulbTemp`. CP171 repeats no Area, class, boundary, wind, or parent-skip gate. One direct fixture makes exactly two default-zero-pointer calls: with surface history 6.71793958923051, wet bulb 6.66143784594778, coefficient 1000, and rain true it expects the wet formula within 0.01; after rain becomes false, coefficient becomes 5.65361106051348, and dry bulb is 7.2 it expects the dry formula within 0.01. The fixture's Area 58.197 is irrelevant. No test covers a positive OSCM pointer or its rain precedence, negative or invalid pointers, caller skips/store, boundary or wind independence, malformed indices/dimensions, nonfinite inputs, failure, or retry. The body is read-only and allocation-free, with no finite, sign, range, index, pointer, dimension, or topology validation, clamp, normalization, diagnostic, status, assertion, catch, rollback, cleanup, cache, or latch. Raw negation, subtraction, multiplication, signed zero, infinities, NaN, overflow, and invalid-operation results follow underlying IEEE behavior, and a positive invalid pointer is indexed unchecked. Because the helper must return before the caller assignment, helper failure leaves the current `SurfQdotConvOutPerArea` old while prior parent effects for the current Surface and successful earlier-Surface writes persist. After a successful scalar return, an invalid or unallocated caller target access can still fail on the left-hand side and likewise leave the current target stale; a successful nonfinite result is stored only when that write succeeds. Direct retry rereads current inputs, while a full parent retry also repeats boundary work and overwrite-shaped storage without repairing skipped stale targets. HeatBalSurfData owns, dimensions, initializes, reports, and placement-new-clears the caller target; CP167 can later copy it to nonrepresentative Surfaces, WindowManager separately writes it for Windows, and reporting plus PlantPipingSystems consume the shared field. Rust's bounded `surface_exterior_report_terms_from_balance` has the same final `-coefficient * (outside_face_temperature - reference_temperature)` algebra and per-area report slot, with upstream typed wet-weather context, but no generic raw OSCM pointer and strict precedence, exact history-term lookup, full parent traversal and boundary topology, common-tail skips/store, or matching failure/re-entry lifecycle. CP171 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 180 routines, split 58 state-mapped plus 122 source-mapped, with 68 required. CP172 adds required source-mapped `routine.calc_heat_balance_inside_surf_2` between `calc_heat_balance_inside_surf` and the distinct optimized `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger; CP173 now follows the CTF-only child in that definition ledger and interposes `sum_surf_qdot_rad_hvac` between the wrapper and CP172 in the project execution list. It is declared at `HeatBalanceSurfaceManager.hh` lines 179-184, implemented at `HeatBalanceSurfaceManager.cc` lines 7815-8656, and called in production only by the canonical wrapper lines 7797 and 7809. An absent optional Zone reaches CP172 only when cached `AllCTF` is false and supplies the four global Surface lists; a present Zone always reaches CP172 with the four raw Zone lists regardless of `AllCTF`. Those vectors independently govern reference-air/history, interzone, non-window, and Window work without topology validation, while the optional Zone is forwarded only to interior-radiation and convection-coefficient children. The prologue skips TDD domes only in reference-air setup, zeros exactly 13 selected fields for every supplied Window, resets the global iteration count, samples the global scheduled inside-source list, and conditionally advances all Kiva instances. Each at-least-one convergence pass takes a whole-array inside-temperature snapshot, temporarily substitutes every Kiva map entry with `Trad - Kelvin` around interior radiation, restores from the snapshot, refreshes convection at pre-increment counts 30 through 480 on a maximum-length run, executes EMPD/HAMT setup and the representative-filtered non-window algorithm branches, runs TDD diffusers every pass but regular Windows only on pass one, commits histories/TDD/interzone updates, and increments the count. The post-increment `> 500` check therefore warns outside warmup and breaks after pass 501 even if that pass first met convergence; global `AnyCondFD` controls tolerance and relaxation. The EMPD/HAMT tail globally zeros all Zone moisture sums and repopulates only the supplied non-window subset. All five literal unit calls to the wrapper omit Zone and retain cached `AllCTF = true`, including the fixture that later changes one Surface algorithm to Kiva, so they dispatch to the CTF-only child; CP172 has no direct or known exercised unit path. Raw vectors, duplicates, indices, algorithms, dimensions, arithmetic, Kiva map insertion, child failures, and diagnostics are unchecked locally; there is no status, catch, rollback, or cleanup. Failure can leave selected-window resets, global snapshots and sources, Kiva substitutions or partial restores, solve/history/coefficient prefixes, diagnostics, relaxation, and globally zeroed or partly rebuilt moisture sums, while retry resets only the early selected state and iteration count and otherwise inherits the partial graph. HeatBalSurfMgr, HeatBalanceData, SurfacesData, HeatBalSurfData, HeatBalFanSysData, MoistureBalanceData, ZoneTempPredictorCorrectorData, SurfaceGeometryData, DataTimingsData, and the called algorithm managers separately own this state; independent clears cannot replay the routine. Rust has bounded inside-pass scaffolds and a distinct CTF-only mapping but no CP172 general solver, raw four-list/partial topology, non-CTF/window/Kiva/moisture lifecycle, 501-pass behavior, or matching failure/re-entry semantics. CP172 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 181 routines, split 58 state-mapped plus 123 source-mapped, with 69 required. CP173 adds required source-mapped `routine.sum_surf_qdot_rad_hvac` after `calc_heat_balance_inside_surf_2_ctf_only` in the Surface-manager definition ledger and after `calc_heat_balance_inside_surf` before `calc_heat_balance_inside_surf_2` in the heat-balance project contract. It is declared at `HeatBalanceSurfaceManager.hh` line 171, implemented at `HeatBalanceSurfaceManager.cc` lines 9277-9285, and called in production only by the canonical wrapper line 7788 before full, optimized, or partial dispatch. Every call, including optional-Zone resimulation, traverses the complete raw `allGetsRadiantHeatSurfaceList`. Five equipment input paths append nonzero Surface pointers without sorting or deduplication. For each stored index, CP173 binds `surfQRadFromHVAC` and overwrites `SurfQdotRadHVACInPerArea` with the exact left-associated `HTRadSys + HWBaseboard + SteamBaseboard + ElecBaseboard + CoolingPanel` sum. An empty list and unlisted targets preserve stale values; duplicates repeat the same overwrite rather than accumulating again. All five literal wrapper calls in `HeatBalanceSurfaceManager.unit.cc` leave the raw list default-empty and exercise only CP173's no-op. Four `SizingManager.unit.cc` `ManageSimulation` fixtures beginning at lines 2465, 2929, 3393, and 3818 include electric radiant baseboard input, populate the list, and indirectly execute CP173, but assert only downstream sizing; no test calls CP173 directly, observes the aggregate, or isolates arithmetic, order, duplicates, failure, or lifecycle. The routine has no validation, diagnostic, allocation, catch, rollback, cleanup, or latch; raw indices and IEEE addition are unchecked. Source binding precedes the assignment, whose complete right-hand side precedes target lookup and store, so failure can retain an earlier-list write prefix and leave the current target old; retry rereads the current list and component record without clearing skipped state. SurfacesData owns the raw list, HeatBalFanSysData owns the component records, and HeatBalSurfData owns the aggregate; their independent placement-new clears and initialization/distribution resets do not form a CP173 transaction. Rust has a bounded `inside_radiant_hvac_w_per_m2` destination consumed by its surface balance and reports, but no five-source producer, global list, or matching lifecycle. CP173 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 182 routines, split 58 `state_mapped` plus 124 `source_mapped`, with 70 required. CP175 adds required source-mapped `routine.calc_outside_surf_temp` after `test_surf_temp_calc_heat_balance_inside_surf` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, and after `calc_heat_balance_outside_surf` before `get_qdot_conv_out_per_area` in the heat-balance project contract. `CalcOutsideSurfTemp` is declared at `HeatBalanceSurfaceManager.hh` lines 195-202 and implemented at `HeatBalanceSurfaceManager.cc` lines 9470-9763. Its only direct production calls are the parent’s `OtherSideCoefCalcExt` CTF/EMPD path at line 7355, `OtherSideCondModeledExt` CTF/EMPD path at line 7419 after any exterior-vented-cavity update, and `ExternalEnvironment` CTF/EMPD-or-TDD path at line 7626; the first two retain zero movable insulation, while the exterior path alone samples exterior-insulation state. The reusable parent supplies complete or optional-Zone traversal, skips Windows, unrelated Surfaces, EcoRoof, and ineligible boundary/algorithm routes, then fatals immediately when CP175 sets its shared flag, before the CP171 common-tail convection store. CP175 uses strict `HMovInsul > 0.0` and `CTFCross[0] > 0.01` decisions and computes quick coupling as `CTFCross[0] / (CTFInside[0] + SurfHConvInt)`. Sky and ground start from global sky and outdoor dry-bulb temperatures; optional surrounding-property schedules override them, the Surface supplies its precomputed surrounding temperature, and the ground-surface property then wins over any scheduled ground value. A TDD dome resolves raw pipe, diffuser, and diffuser-Space topology, uses `Ueff = 1 / Reff`, and couples dome exterior forcing to diffuser shortwave, radiant, convection, and MAT terms without embedded-source terms. The remaining exhaustive paths separate no-movable slow/quick and movable slow/quick conduction. With no movable insulation, `OSCMPtr == 0` uses exterior shortwave, convection, air/sky/ground/surrounding radiation, additional source, and optional outside embedded-source forcing; quick conduction substitutes the coupled inside balance and denominator correction, but its no-OSCM source branch notably omits the inside embedded-source term. Any nonzero OSCM pointer, including a malformed negative value, indexes raw `TRad`/`HRad`, drops the separated solar/air/sky/ground/surrounding forcing, and its quick source branch includes both outside and coupled inside embedded-source terms. Movable paths ignore OSCM radiation, use `F2 = HMovInsul / (HMovInsul + HConvExt + HAirExt + HSkyExt + HGrdExt + HSrdSurfExt)`, couple exterior forcing through F2, and include no embedded-source terms because that combination is rejected later. After writing `SurfOutsideTempHist(1)`, CP175 overwrites `SurfQdotRadOutRepPerArea` with negative sky, air-radiation, ground, and surrounding exchanges while excluding exterior convection and the parent’s nonlinear surrounding flux. With an embedded source plus movable insulation, those two writes precede four diagnostics, raw insulation-material lookup, `ErrorFlag = true`, and return; radiant-system coefficients remain stale and a successful call never clears a preexisting flag. With an embedded source and no movable insulation, constant, inside-temperature, and source coefficients are written in order; the divisor preserves the source anomaly that `SurfHSrdSurfExt` appears twice, and the constant coefficient omits the additional outside source. A non-source call does not clear old coefficients. The one direct fixture covers only a non-OSCM Wall with positive insulation, zero-cross slow conduction, and invalid embedded source; it checks the flag and four diagnostics but not independent temperature or radiation numerics. Four direct CTF parent fixtures incidentally reach CP175 but assert only parent convection, surrounding/ground, or scheduled-source state, while a fifth CondFD fixture does not call it. No focused test covers valid slow/quick equations, exact 0/0.01 thresholds, OSCM, TDD, temperature precedence, coefficient anomalies, an input-true flag, malformed topology, nonfinite arithmetic, failure prefixes, or retry. There is no finite, denominator, index, dimension, topology, schedule, property, TDD, Space, or material validation, catch, rollback, cleanup, or local latch. Threshold equality and NaN select absent/slow, raw floating-point exceptional results propagate, and failure can preserve no write, only outside history, history plus radiation report, a diagnostic prefix with the flag still false, or a one/two-coefficient prefix. Direct retry rereads live inputs but does not clear a caller-supplied flag or untouched coefficients; a fresh parent call reinitializes its local flag but repeats earlier parent work. HeatBalSurfData owns the temperature history and radiant report, HeatBalFanSysData owns the three coefficients, the parent stack owns the shared flag, and the Surface, Construction, environment, schedule, daylighting, Space/MAT, material, and error owners reset independently rather than transactionally. Rust has bounded slow/quick typed Outdoors CTF and report helpers, but epsilon fallbacks, clamps, shortcuts, and a collapsed environment do not implement CP175’s TDD, OSCM, movable insulation, property precedence, embedded-source coefficient/error lifecycle, exact reports, raw topology, or failure/re-entry behavior, so no Rust target, support, numerical, or conformance promotion follows. The inventory becomes 32 algorithms and 184 routines, split 58 `state_mapped` plus 126 `source_mapped`, with 72 required. CP176 adds non-required source-mapped `routine.calc_exterior_vented_cavity` immediately after `calc_outside_surf_temp` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `CalcExteriorVentedCavity` is declared at `HeatBalanceSurfaceManager.hh` line 204 and implemented at `HeatBalanceSurfaceManager.cc` lines 9765-9850. Its only direct production calls are the `CalcHeatBalanceOutsideSurf` `OtherSideCondModeledExt` CTF/EMPD path at line 7416 and CondFD/HAMT path at line 7427, both guarded by the trigger Surface's `SurfExtCavityPresent`. Parent Window and optional-Zone skips, every other boundary, and every other heat-transfer algorithm cannot trigger CP176, but a reached cavity forwards its complete raw `SurfPtrs` group without Window or Zone filtering. Before either call, the parent applies EMS overrides to the trigger Surface's OSCM and snapshots its existing `TConv/HConv` into local `TempExt`, `SurfHConvExt`, and any CondFD/HAMT moisture boundary. CP176 writes the cavity's separate raw `OSCMPtr`, which neither the wrapper nor input loader checks against each Surface's `OSCMPtr`. The CTF/EMPD path then calls CP175 with the pre-CP176 convective snapshot but, when both pointers match, CP176's new live `TRad/HRad`; common-tail CP171 likewise reads the new `TConv/HConv` only when those pointers match. CP176 obtains `CavNum` from the trigger Surface, computes trigger humidity ratio and density from that Surface's dry/wet bulb and global pressure, forms `holeArea = ActualArea * Porosity` and `AspRat = 2 * HdeltaNPL / PlenGapThick`, and seeds temporary baffle and cavity-air temperatures from `TbaffleLast/TairLast`. It then calls deferred dependency `TranspiredCollector::CalcPassiveExteriorBaffleGap` exactly three times with no convergence check, carrying each pass's two updated temperatures into the next. The child area-weights weather, wind, current outside history, incident solar, and heat-transfer coefficients over the whole raw group, invokes exterior-convection setup for every member, applies rain and optional ICS branches, and returns the final gap coefficients, incident term, total mass flow, and wind/buoyancy volume flows. After all three passes return, CP176 stores `Isc`, `TAirCav`, `Tbaffle`, `HrPlen`, `HcPlen`, `PassiveACH = (MdotVent / rho_trigger) / (ProjArea * PlenGapThick) * 3600`, total/wind/buoyancy passive mass reports, both last-temperature seeds, then OSCM `TConv/HConv/TRad/HRad` in that order; it never writes the separate cavity `MdotVent` member. The child total mass flow uses group-weather density, while ACH and the two component mass reports use trigger-Surface density, so component sums and the total can diverge when member weather differs. There is no cavity-number deduplication: each eligible member trigger recomputes the complete group through three more passes and immediately advances shared last temperatures, making full and optional-Zone traversal order visible and a successful direct retry non-idempotent. Input accepts duplicate Surface references within and across cavity objects: repeated raw entries are weighted repeatedly, a later cavity overwrites a Surface's trigger cavity number while an earlier raw group retains it, and the three-times-N call count uses distinct traversed trigger Surfaces rather than duplicate list entries. No test calls CP176 or enables its parent gate. One `ICSCollector.unit.cc` fixture calls the shared child only once and checks baffle temperature plus Hc, Hr, and total mass flow, not the wrapper loop, air temperature, report/history/OSCM stores, density split, multi-member order, or retry. CP176 has no local index, dimension, topology, pointer-consistency, finite, sign, range, or denominator validation, diagnostic, status, convergence test, catch, rollback, cleanup, cache, or latch. Psychrometric and convection dependencies may diagnose or mutate their own state; a failure before or within the three child passes leaves wrapper-owned cavity results, both seeds, and OSCM fields old but can retain dependency effects. After all three child calls return, straight-line non-trapping scalar work commits every cavity/report field and both seeds before the first new raw-index risk at the initial OSCM access; an invalid pointer leaves all four OSCM fields old, while a valid stable index lets all four stores complete. Retry therefore starts from either both old or both newly committed seeds, never a CP176-created split pair. `HeatBalanceData` owns the cavity topology, inputs, results, reports, and seeds; its constructor starts the two seeds at 20 C and explicitly initializes the listed result/report/source scalars at zero, but omits the separate never-written `MdotVent` member and leaves it indeterminate. BeginEnvironment initialization resets only the two seeds to 20 C. `SurfacesData` separately owns membership, OSCM/EMS state, and member weather/geometry; BeginEnvironment resets each OSCM to 20 C and 4 W/m2-K but leaves cavity results and PV-fed `QdotSource` stale until recomputation. General-routines ICS state, solar collectors, PV, construction/material, convection, environment, outside histories, and incident solar have independent owners and clears. Rust collapses `OtherSideConditionsModel` to `OutsideBoundaryCondition::Other`, excludes it from the exterior list, and preserves the existing outside-face temperature; it has no cavity object/group, OSCM state, passive-gap/Nusselt/flow solve, three-pass seeds, ICS/PV coupling, six registered outputs, or matching failure/re-entry lifecycle. CP176 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 185 routines, split 58 `state_mapped` plus 127 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP177 adds non-required source-mapped `routine.gather_component_loads_surf_abs_fact` immediately after `calc_exterior_vented_cavity` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GatherComponentLoadsSurfAbsFact` is declared at `HeatBalanceSurfaceManager.hh` line 206 and implemented at `HeatBalanceSurfaceManager.cc` lines 9852-9880. Its only direct production call is `InitSurfaceHeatBalance` line 614 under the caller's `ZoneSizingCalc` gate; the body additionally requires `CompLoadReportIsReq && !isPulseZoneSizing` and checks neither `WarmupFlag`, `KickOffSizing`, `doLoadComponentPulseNow`, nor the parent first-time flag. That sole call can be reached through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; if the effective gates remain true, either path overwrites the same current slot and the last successful call wins. On an ordinary component-report sizing path, the preceding `ManageInternalHeatGains(state, false)` call lazily allocates zero-initialized `surfCompLoads` storage for `(TotDesDays + TotRunDesPersDays) * (24 * TimeStepsInHour) * TotSurfaces` records. CP177 computes `TimeStepInDay = (HourOfDay - 1) * TimeStepsInHour + TimeStep`, selects zero-based day and timestep slots from one-based `CurOverallSimDay` and `TimeStepInDay`, then traverses numeric Surfaces 1 through `TotSurfaces`. It skips exactly when `!HeatTransSurf` or `Zone == 0`, then skips `TDD_Dome`; a malformed negative Zone passes, and there is no other class, Space, representative, list, or deduplication filter, so eligible Windows and TDD diffusers are included. For every reached Surface it first overwrites `ITABSFseq` with `SurfAbsThermalInt`, then overwrites `TMULTseq` with `EnclRadInfo(RadEnclIndex).radThermAbsMult`. It copies the two live factors separately without multiplying, clamping, normalizing, recomputing, or clearing omitted records; a false gate or newly skipped Surface preserves the prior or default slot. Because there is no kickoff or warmup guard, ordinary `SetupZoneSizing` can write timestep 1 twice and the final timestep once per accepted environment before the sizing iterations, the pulse iteration writes neither field, and nonpulse warmup and normal calls overwrite whatever slots they reach. An unchanged successful retry is overwrite-idempotent. `OutputReportTabular::GetDelaySequences` is the only source reader of these fields and later forms `TMULTseq * ITABSFseq * Surface.Area * decayCurve`; the snapshot feeds Zone, Space, AirLoop, and Facility component-load tabular reporting but never feeds the heat-balance solver. No test calls CP177 directly. The upstream CP161 test checks only absorptance 0.2 and multiplier 5, the focused delay-sequence test manually seeds both fields, and full sizing/report fixtures exercise the chain without asserting the CP177 pair, gates, filters, or write order. CP177 performs no allocation, finite, sign, range, index, dimension, topology, enclosure-membership, or time-coordinate validation and has no diagnostic, status, assertion, catch, rollback, cleanup, or local latch. Invalid day, timestep, destination-Surface, or enclosure indexing is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build, a diagnosed day or timestep lookup failure precedes every Surface store; after traversal begins an earlier-Surface prefix may remain, and an invalid `RadEnclIndex` can be diagnosed after `ITABSFseq` commits but before `TMULTseq`, leaving a split new/old pair. A successful retry overwrites only currently reached eligible records and does not repair skipped stale cells. `OutputReportTabularData` owns the destination and clears it plus rearms its allocation latch on `clear_state`; normal post-sizing deallocation removes only pulse arrays and retains `surfCompLoads` for report consumption. `HeatBalSurfData`, `ViewFactorInfoData`, `SurfacesData`, `SizingData`, and global timing/gate state own the inputs independently, so their resets do not form a CP177 transaction. In a bounds-checked execution where an indexing failure propagates, the parent does not reach the following initialization-complete display or first-time-flag clear, and prior parent effects plus any committed CP177 prefix remain observable if control recovers; release-build undefined behavior provides no corresponding guarantee. Rust's retained inside thermal absorptance and transient typed-Zone radiant-gain normalization are adjacent primitives only. Rust has no sizing/design-day component-load lifecycle, radiant-enclosure multiplier state, day/timestep/Surface ITABSF/TMULT storage, delay-report consumer, or matching filtering, failure-prefix, reset, and re-entry behavior. CP177 adds no EnergyPlus source inventory, Rust target/code/state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 186 routines, split 58 `state_mapped` plus 128 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP178 adds non-required source-mapped `routine.get_surf_incident_solar_multiplier` immediately after `gather_component_loads_surf_abs_fact` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurfIncidentSolarMultiplier` is declared at `HeatBalanceSurfaceManager.hh` line 208 and implemented at `HeatBalanceSurfaceManager.cc` lines 9882-9890. Its only direct production call is the numeric all-Surface loop at `InitSolarHeatGains` line 2820, reached only when `currSolRadPositive` is true: `SunIsUp` and the signed `BeamSolarRad + GndSolarRad + DifSolarRad` sum is greater than zero. The unconditional line-457 parent call can arrive through the normal manager chain or `SimulationManager::Resimulate` line 2903 under `ResimHB`; no first-time, warmup, kickoff, sizing, pulse, class, heat-transfer, exterior-solar, representative, or deduplication filter narrows CP178 after the positive-solar branch is entered. The `resetSolar` condition alone does not call it: sunset and sun-up/no-radiation paths skip CP178, and BeginEnvironment skips it only when the independent `currSolRadPositive` gate is false; a positive-solar BeginEnvironment call refreshes every Surface cache, while skipped calls preserve prior caches. For each numeric Surface, CP178 first reads same-index `Surface.hasIncSolMultiplier`. False returns exact 1.0 without touching the optional record vector. True with a nonnull same-index schedule returns `sched->getCurrentVal() * Scaler`, where the schedule getter selects `EMSVal` when actuated and `currentVal` otherwise; true with a null schedule returns `Scaler`. CP178 does not clamp, normalize, validate, cache, diagnose, or consult the record `Name`, `SurfaceIdx`, total-object count, prior cached value, Surface class, boundary, exposure, construction, shading, or any typed list. The caller stores a completed return into same-index `Surface.IncSolMultiplier` before downstream sky/ground, beam/diffuse, obstruction and reflected-solar contributions, TDD, shelf, opaque/window, frame/divider, daylighting, interior-distribution, solver, and report consumers. No test calls CP178 directly. The focused Surface-manager fixture manually sets `hasIncSolMultiplier`, a null schedule, and scalers 0.5 then 1.0, calls the parent twice under positive solar, and proves only an exact half-versus-whole transmitted-solar result; input tests inspect scaler and schedule state but never assert the scheduled product, default branch, EMS precedence, solar gate, failure prefix, or retry. CP178 has no finite, range, index, dimension, alignment, pointer, or lifecycle validation and no diagnostic, status, allocation, catch, rollback, cleanup, or latch. Invalid vector or pointer access is unchecked undefined behavior in release builds, so no deterministic write prefix is guaranteed. In a bounds-checked build the helper itself has no writes; if a lookup failure propagates during the caller loop, only earlier Surface cache stores can remain while the current and later caches stay old. IEEE nonfinite results are returned and stored rather than diagnosed. A successful positive-solar retry re-reads live flags, scaler, and schedule or EMS state and overwrites every numeric Surface; a retry whose solar gate is false preserves any prior cache. `SurfacesData` owns the flag, same-index record vector, and cached multiplier; the Surface constructor defaults the flag and cache to false and 1.0, record defaults are scaler 1.0 and null schedule, placement-new `clear_state` replaces the visible owner with empty Surface and record vectors, and subsequently allocated entries receive those defaults, while BeginEnvironment does not reset an existing cache. `ScheduleManagerData` separately owns and deletes schedule objects, while `EnvironmentData` owns the solar gate and CP159 mutates preceding state across several owners, so independent clears and a failed parent are not a CP178 transaction. Rust retains only the CP99 immutable request snapshot with an unresolved fenestration target, bounded scalar, and optional `ScheduleId`; every definition remains run-blocked. Rust has no fenestration binding, per-Surface flag or cache, same-index record alignment, request-specific current or EMS schedule sampling, positive-solar traversal, or matching window, TDD, shelf, reflection, distribution, solver, and reporting consumers. Its request-free opaque incident-solar diagnostic and generic schedule infrastructure do not implement CP178. CP178 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 187 routines, split 58 `state_mapped` plus 129 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP179 adds non-required source-mapped `routine.init_surface_property_view_factors` immediately after `get_surf_incident_solar_multiplier` and before `update_final_surface_heat_balance`, with no project-contract entry. `InitSurfacePropertyViewFactors` is declared at `HeatBalanceSurfaceManager.hh` line 210, implemented at `HeatBalanceSurfaceManager.cc` lines 9893-9993, and called only by `InitHeatBalance` line 2623 inside its `BeginSimFlag` block after array allocation and optional CTF initialization but before Window, daylighting, and solar initialization. The body quick-returns unless `AnyLocalEnvironmentsInModel` and the separately owned `InitSurfaceHeatBalancefirstTime` latch are both true, then visits every numeric Surface whose surrounding or ground property flag is set. It sums a strictly positive stored sky value, every enumerated surrounding factor, and the selected ground value, with a separate GroundSurfaces object taking precedence over a SurroundingSurfaces ground field; a total above one emits severe/continue diagnostics but does not stop, clamp, or skip mutation. Both-set, sky-only, ground-only, and neither-set branches overwrite or scale Surface sky/ground IR factors, backfill property values and presence flags, and conditionally invoke the separately CP182-mapped `ReSetGroundSurfacesViewFactor` child, which replaces the ground-object sum and either disables zero-factor ground use or writes the whole inferred factor into its first entry. Shared property records make numeric Surface alias order observable, and a sky-only SurroundingSurfaces path without a separate ground object can alternate its inferred ground value across direct retries because the value is backfilled without setting its presence flag. Focused fixtures cover the four ordinary branches and selected long-wave consumers but not both quick returns, production latch order, shared aliases, sum-above-one continuation, malformed topology, failure prefixes, or retry. There is no local finite/index/topology validation, status, catch, rollback, cleanup, transaction, or latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface prefix and leave the later first-time clear unreached. Rust ordinary ground/sky complement helpers and run-blocked Zone local-environment records do not implement the Surface local-environment property arenas, mutable flags/factors, diagnostics, reset child, alias order, or lifecycle. CP179 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 188 routines, split 58 `state_mapped` plus 130 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP180 adds non-required source-mapped `routine.get_ground_surfaces_temperature_average` immediately after `init_surface_property_view_factors` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 212 and implemented at `HeatBalanceSurfaceManager.cc` lines 9995-10035. Its only direct production call is the unconditional first child of `CalcHeatBalanceOutsideSurf` at line 7016, before the separately CP183-mapped surrounding-temperature child, internal-source transfer, optional-Zone filter, and every outside-Surface calculation. All full-model and zone-limited parent invocations, including normal Surface management, final-radiant recalculation, SimulationManager resimulation, and radiant/HVAC retries, therefore enter CP180 before the optional-Zone filter; when `AnyLocalEnvironmentsInModel` passes, CP180 traverses the complete numeric Surface array. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state. Otherwise CP180 filters only `IsSurfPropertyGndSurfacesDefined`, follows the Surface property index, and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfTemp` while preserving the prior shared average. For a nonzero sum it skips exact-zero view factors and null temperature schedules; every other entry samples the live schedule with EMS precedence and contributes `view_factor * (temperature + Kelvin)^4`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsTempAvg` with zero and then disables only the current Surface use flag; otherwise CP180 stores `root_4(numerator / SurfsViewFactorSum) - Kelvin` and never enables a false use flag. Shared-property aliases on a nonzero-sum path repeat the schedule reads and shared average store in numeric Surface order while use flags remain per Surface. Stable successful inputs are numerically overwrite-idempotent, but either zero branch latches an affected use flag false until Surface-property input relinking restores it, and later nonzero schedules can refresh the report average without restoring solver consumption. One focused unit fixture makes four direct calls and checks a three-entry radiative average plus each one-entry selection; one outside-long-wave fixture indirectly consumes the ordinary average. They do not cover the global return, parent/optional-Zone order, zero-sum or zero-numerator stores, missing schedules, EMS values, aliases, use-flag latching, malformed topology, failure prefixes, or retry. Beyond its explicit global/property/equality/null checks, CP180 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust ordinary ground temperature, schedule, long-wave, and view-factor primitives do not implement the SurfaceProperty ground list, shared radiative-temperature average, per-Surface use flag, complete-Surface zone-resimulation order, output-variable binding, or matching failure/re-entry lifecycle. CP180 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 189 routines, split 58 `state_mapped` plus 131 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP181 adds non-required source-mapped `routine.get_ground_surfaces_reflectance_average` immediately after `get_ground_surfaces_temperature_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetGroundSurfacesReflectanceAverage` is declared at `HeatBalanceSurfaceManager.hh` line 214 and implemented at `HeatBalanceSurfaceManager.cc` lines 10037-10072. Its only direct production call is unconditional `InitSurfaceHeatBalance` line 395, after variable absorptance, optional environment-history and movable-insulation work but before complex-fenestration, anisotropic-sky, shading, radiation, daylighting, and solar-gain initialization. Both normal Surface management and `SimulationManager::Resimulate` under `ResimHB` reach it; no first-time, BeginEnvironment, day, hour, timestep, warmup, sizing, solar, Surface-class, or boundary gate narrows CP181 itself. A false `AnyLocalEnvironmentsInModel` returns without clearing prior state; when that gate passes, CP181 traverses the complete numeric Surface array and filters only `IsSurfPropertyGndSurfacesDefined`. It follows the Surface property index and, when the property-wide view-factor sum is exactly zero, disables that Surface's `UseSurfPropertyGndSurfRefl` while preserving the prior shared average. For a nonzero sum it traverses every stored ground entry and skips only null reflectance schedules: even an exact-zero view factor still samples a nonnull schedule. Every sampled schedule uses EMS precedence and contributes `view_factor * current_reflectance`. The denominator remains the complete stored property sum, including factors whose null schedules were skipped. An exactly zero numerator first overwrites shared `SurfsReflAvg` with zero and then disables only the current Surface use flag; otherwise CP181 stores `numerator / SurfsViewFactorSum` and never enables a false flag. Thus a valid zero-reflectance schedule selects the disable branch, and later solar consumers use their environment fallback rather than retaining a custom zero. Shared-property aliases on a nonzero-sum path repeat schedule reads and the shared store in numeric Surface order while use flags remain per Surface. Stable successful inputs are overwrite-idempotent, but either zero branch latches an affected flag false until Surface-property input relinking restores it; later nonzero schedules can refresh the output average without restoring custom solar consumption. One focused unit fixture makes four direct calls and checks a three-entry weighted average plus each one-entry result. One long-wave fixture incidentally executes the all-null-schedule disable path but asserts no reflectance state, and no test connects the average to its solar or output consumers. Missing coverage includes the global return, zero-sum stale average, partial-null denominator rule, exact-zero schedule meaning, EMS values, aliases, flag latching, malformed topology, failure prefixes, and retry. Beyond its explicit global/property/equality/null checks, CP181 has no local finite, sign, range, index, dimension, alignment, count, topology, schedule-domain, result, or lifecycle validation and no diagnostic, status, catch, rollback, cleanup, transaction, or one-shot latch; unchecked release access can be undefined, while a bounds-checked failure can retain an earlier Surface/property prefix. Rust's generic schedules, ordinary ground view factor, and fixed 0.2 ground-reflectance solar path do not implement the SurfaceProperty ground list, scheduled shared average, per-Surface use flag, EMS sampling, fallback transition, alias order, output binding, or matching failure/re-entry lifecycle. CP181 adds no EnergyPlus source inventory, Rust target/code/state/test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 190 routines, split 58 `state_mapped` plus 132 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP182 adds non-required source-mapped `routine.re_set_ground_surfaces_view_factor` immediately after `get_ground_surfaces_reflectance_average` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `ReSetGroundSurfacesViewFactor` is declared at `HeatBalanceSurfaceManager.hh` line 216, implemented at `HeatBalanceSurfaceManager.cc` lines 10074-10092, and called in production only by `InitSurfacePropertyViewFactors` line 9989. CP182 itself has no global or first-time gate: its sole quick return tests the current Surface ground-property flag. The production parent supplies broader `AnyLocalEnvironmentsInModel` and `InitSurfaceHeatBalancefirstTime` gates and calls CP182 only for a numeric property-bearing Surface whose sky-only or neither-set branch inferred a ground factor while a separate GroundSurfaces object exists; before calling, the parent overwrites `Surface.ViewFactorGroundIR` and marks the shared property's `IsGroundViewFactorSet` true. CP182 follows the Surface property index and first overwrites shared `SurfsViewFactorSum` with that current Surface ground IR factor. An exact zero, including negative zero, then clears the current Surface reflectance-use flag before its temperature-use flag and returns without touching any entry. Every nonzero value, including negative, NaN, or infinity, is copied as the whole sum into only `GndSurfs(1).ViewFactor`. The helper does not validate or clamp the value, clear later entries, change either shared average, change `Surface.ViewFactorGroundIR`, alter entry metadata or count, write `IsGroundViewFactorSet`, or enable a false use flag. Consequently nonzero-to-zero direct retry leaves the old first entry behind, zero-to-nonzero retry cannot restore the two flags, and a pre-mutated multi-entry property can retain an entry total inconsistent with the stored sum. Shared properties make order observable: in the ordinary parent loop the first blank-property alias marks the object set and later aliases normally consume that state without re-entering CP182, while every standalone call overwrites the shared sum, only nonzero calls overwrite the first entry, and a zero call preserves that entry while disabling only its own Surface flags. CP181 reflectance and CP180 temperature averaging later consume the current shared denominator and entries; a zero CP182 branch selects their solver fallbacks through the Surface flags while leaving report averages stale until separately recomputed. No test calls CP182 directly. Four parent fixtures reach it for six Surfaces; two fixtures assert the positive first-entry backfill for four of them, while the other two assert only parent or downstream results. The property quick return, exact-zero path and flag-write order, sum assignment, stale-first-entry and sticky-flag retries, aliases, malformed values/topology, and failure prefix remain untested. CP182 has no finite, sign, range, index, allocation, count, topology, alias, or lifecycle validation and emits no diagnostic, status, catch, rollback, cleanup, transaction, or latch. In a bounds-checked failure, an invalid Surface or property index precedes the shared-sum write, whereas a missing first entry on the nonzero path can fail after that sum commits; the zero path never accesses the first entry and can still clear both flags. Release unchecked malformed access can be undefined, and a failure reached from the parent retains its earlier Surface-factor and set-flag writes. Rust has no SurfaceProperty ground arena, shared sum/first-entry mutation, per-Surface use flags, parent branch/latch behavior, alias semantics, or matching failure/re-entry lifecycle; ordinary ground/sky factors, fixed ground reflectance, schedules, and run-blocked Zone local-environment state are adjacent primitives only. CP182 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 191 routines, split 58 `state_mapped` plus 133 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP183 adds non-required source-mapped `routine.get_surrounding_surfaces_temperature_average` immediately after `re_set_ground_surfaces_view_factor` and before `update_final_surface_heat_balance` in the Surface-manager definition ledger, with no project-contract entry. `GetSurroundingSurfacesTemperatureAverage` is declared at `HeatBalanceSurfaceManager.hh` line 218, implemented at `HeatBalanceSurfaceManager.cc` lines 10094-10118, and is the final routine before those namespaces close. Its sole direct production call is unconditional `CalcHeatBalanceOutsideSurf` line 7019, after CP180 ground-temperature averaging and before internal-source transfer, optional-`ZoneToResimulate` handling, and every outside-Surface calculation. A CP180 failure prevents CP183, but every parent call that passes that sibling enters CP183 before any full-model/Zone filter; even a Zone-limited radiant or HVAC retry can refresh every eligible allocated Surface. CP183 first returns when `AnyLocalEnvironmentsInModel` is false, preserving every prior average. Otherwise it range-traverses the complete allocated Surface vector and skips only records whose `SurfHasSurroundingSurfProperty` flag is false. For each eligible Surface it follows `SurfSurroundingSurfacesNum`, zeros a local sum, visits entries 1 through `TotSurroundingSurface` in stored order, unconditionally dereferences every temperature schedule even for an exact-zero factor, samples current or EMS-actuated value, adds Kelvin, and accumulates `ViewFactor * temperature^4`. It then divides by the Surface-local cached `ViewFactorSrdSurfs` rather than the property's live `SurfsViewFactorSum`, takes the fourth root, subtracts Kelvin, and performs the routine's only persistent write to that Surface's `SrdSurfTemp`. CP183 does not clear, disable, clamp, renormalize, diagnose, or mutate the property, entry list, factors, denominator cache, presence flag, schedules, sky/ground fields, or any shared average. Normal input processing requires at least one complete entry and a resolved schedule, constrains each factor to [0,1], accumulates the property sum, copies it into the Surface cache, and clears the presence flag on an exact-zero sum, but does not constrain the aggregate to one; none of those dependency guarantees is revalidated here. A schedule of exactly -273.15 C yields -273.15 C rather than a disable branch, temperatures below absolute zero lose their Kelvin sign through the fourth power, and a zero-factor NaN or infinity still poisons the sum. Nonfinite inputs, overflow, zero or mismatched denominators, and a negative quotient can store NaN or infinity without a diagnostic. Shared-property aliases repeat every schedule read and write separate Surface averages; matching cached denominators produce the same result, while independently mutated caches can diverge. Stable inputs are overwrite-idempotent, schedule or EMS changes appear on the next call, and false global/property gates preserve stale values. Three direct fixtures make three calls, write four property-bearing Surfaces, and traverse eight entries: two directly prove mixed-temperature fourth-power means, while one proves a single-entry result only through Window long-wave output. Five direct-parent fixtures add a global return, a per-Surface skip, and nine eligible writes; only the CondFD fixture genuinely checks an average-derived coefficient, while other assertions can use the parent's separate raw-entry long-wave loop. No test covers optional-Zone all-Surface refresh, gate-preservation sentinels, aliases, retries after schedule change, cached-denominator mismatch, zero/negative/nonfinite arithmetic, malformed topology, or failure prefixes. CP183 has no finite, sign, range, index, allocation, count, pointer, topology, alias, or lifecycle validation and no status, diagnostic, catch, rollback, cleanup, transaction, or latch. In a bounds-checked or otherwise diagnosed malformed-access failure, an invalid property, entry, or schedule can leave the current Surface old because its sole store follows the complete inner loop, while earlier eligible Surfaces and preceding CP180 writes remain committed; unchecked release access can be undefined. Downstream CTF/CondFD exterior balances, surrounding-radiation coefficients, and ordinary, complex, and equivalent-layer Window paths consume `SrdSurfTemp`, while the separate outside-Surface raw surrounding-entry radiation sum does not. Rust has no typed Surface surrounding-property arena, entry list, denominator cache, per-Surface radiative average, current/EMS property binding, surrounding long-wave term, parent traversal, alias order, or matching failure/re-entry lifecycle; generic schedules, ordinary sky/air/ground long-wave terms, and run-blocked Zone local-environment state are adjacent primitives only. CP183 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 192 routines, split 58 `state_mapped` plus 134 `source_mapped`, while required routines remain 72 and the heat-balance project list remains 41. CP184 adds required source-mapped `routine.get_air_heat_balance_input` immediately after `routine.manage_air_heat_balance` in the Air-manager definition ledger and adds `get_air_heat_balance_input` after `manage_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirHeatBalanceInput` is declared at `HeatBalanceAirManager.hh` line 67, implemented at `HeatBalanceAirManager.cc` lines 163-189, and called in production only by `ManageAirHeatBalance` line 150 while the caller-owned `ManageAirHeatBalanceGetInputFlag` is true. The manager writes that flag false only after CP184 returns, then calls initialization, Air calculation, and mean-air reporting; a CP184 fatal or earlier non-return therefore leaves the flag true and blocks those later children on that attempt, while a successful return makes later manager calls skip only CP184. The flag defaults true, `HeatBalanceAirMgrData::clear_state` rearms it and clears only that manager's uniqueness sets, and the supported clean retry boundary is the broader `EnergyPlusData::clear_state` that also resets child-owned heat-balance and room-air state. CP184 itself has no guard or persistent latch. Every direct entry creates local `ErrorsFound = false`, then unconditionally calls `GetAirFlowFlag(state, ErrorsFound)`, `SetZoneMassConservationFlag(state)`, and `GetRoomAirModelParameters(state, ErrorsFound)` in that order before testing the aggregate once. `GetRoomAirModelParameters` owns another local flag and only raises the shared flag on its own errors; it never clears an earlier airflow error, so the result is monotonic OR accumulation. A normally returned first-child error does not suppress either later child, whereas any child non-return prevents its successors and the final wrapper check. When the aggregate is true, CP184's only diagnostic is terminal `ShowFatalError` with exact text `GetAirHeatBalanceInput: Errors found in getting Air inputs`; it does not derive the predicate from the global severe count. CP184 writes no persistent domain state itself and provides no validation, catch, rollback, cleanup, transaction, or idempotence. Its child context is observably stateful: `GetAirFlowFlag` sets `AirFlowFlag`, parses and allocates simple airflow/report state, and can append an AirFlow EIO row even with the aggregate already true; `SetZoneMassConservationFlag` monotonically marks receiving then source Zone flags and can expose a partial prefix after malformed parsed topology; `GetRoomAirModelParameters` allocates and populates per-Zone room-air state and writes its EIO header/rows before propagating its local error. A final fatal preserves all returned-child mutations, registrations, diagnostics, EIO output, and uniqueness effects while leaving the parent latch true; same-state retry revisits them without rollback. A direct call never changes the parent latch, so a later manager call can repeat the complete input wrapper, whereas any successfully returned production call suppresses it for that state even if a later manager child fails. The only literal direct test call is PTAC integration setup after heat-balance input and array allocation; it asserts later PTAC node and flow results, not CP184 state, ordering, error, fatal, latch, output, or retry. No test directly calls `ManageAirHeatBalance`; eight fixtures contain explicit `ManageHeatBalance` paths but assert daylighting, EMS, construction, plant, or Window behavior. Full-harness simulations provide broader transitive smoke, including one fixture that checks parsed Mixing/CrossMixing records, but not the wrapper contract. Separate child tests prove one airflow error flag and one room-air error/severe stream, while no test proves aggregate fatal text, three-child continuation, parent latch retention, partial effects, direct-call duplication, or clean/reset retry. Rust's `get_air_heat_balance_input_compat` is only an identity closure alias whose sole use wraps a per-Zone air-temperature coefficient calculation; the timestep manager path does not nest it, Rust owns none of the three input children or caller latch, and its heat-balance state has no matching lifecycle. CP184 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 193 routines, split 58 `state_mapped` plus 135 `source_mapped`, with 73 required; the heat-balance project list becomes 42. CP185 adds required source-mapped `routine.get_air_flow_flag` immediately after `routine.get_air_heat_balance_input` in the Air-manager definition ledger and adds `get_air_flow_flag` after `get_air_heat_balance_input` and before `manage_zone_air_updates` in the heat-balance project contract. `GetAirFlowFlag` is declared at `HeatBalanceAirManager.hh` line 69, implemented at `HeatBalanceAirManager.cc` lines 191-214, and called in production only as CP184's unconditional first child at line 179. Every entry first overwrites the persistent `AirFlowFlag` true, then calls complete `GetSimpleAirModelInputs(state, ErrorsFound)`, then on normal child return sums `TotInfiltration`, `TotVentilation`, `TotMixing`, `TotCrossMixing`, and `TotRefDoorMixing`. A strictly positive sum appends exact two-line EIO text `! <AirFlow Model>, Simple` followed by ` AirFlow Model, Simple`; zero writes no CP185 summary even though the flag remains true. `TotZoneAirBalance` and auxiliary EarthTube, CoolTower, and ThermalChimney presence are not in that output predicate. CP185 neither initializes, reads, tests, clears, nor directly writes the caller's `ErrorsFound` reference. The parser only raises that shared flag on its diagnosed paths, so an incoming true remains true and a returned child error does not suppress the count test or positive-total summary. CP185 emits no diagnostic itself. The child remains separate source dependency context: it allocates Zone and optional Space air-report records, registers output/EMS state, parses the nine simple-airflow object families, populates counts/records/mass-conservation data, and writes detailed EIO before returning. `AirFlowFlag` defaults false in `HeatBalanceData`, CP185 is its only source writer, and `HeatBalanceData::clear_state` placement-new restores false and clears its counts and arenas. The flag gates `InitSimpleMixingConvectiveHeatGains` simple mixing/cross-mixing/refrigeration-door initialization and, after its own accumulator resets, `ZoneEquipmentManager::CalcAirFlowSimple` before the AirflowNetwork control gate and simple airflow/auxiliary calculations. Count and record state has further HVAC, hybrid-ventilation, SQLite, mass-conservation, nominal-report, and output consumers. CP185 has no guard, validation, catch, rollback, cleanup, transaction, or one-shot latch. A parser non-return preserves the already-true flag plus its child prefix and skips CP185's summary; a count-access or EIO failure preserves the flag and completed parser effects. On normal error return, CP184 still runs its mass-conservation and room-air children and can later fatal; because the manager clears its input latch only after CP184 returns, that fatal leaves the latch true and same-state retry re-enters CP185 with prior parser/output state. The flag write is value-idempotent after the first entry, but the parser, registrations, uniqueness checks, diagnostics, and EIO are not a repeat-safe transaction. Direct calls always re-enter the child, while a successful production input pass suppresses later CP185 calls through the parent latch. Air-manager-only clear rearms that latch and clears its uniqueness sets but does not reset `HeatBalanceData` or output state; the broader whole-state clear is the clean retry boundary. One direct unit fixture supplies two same-Zone `ZoneAirBalance:OutdoorAir` objects with missing schedules and asserts only that the shared error flag is true; it neither isolates the prior `GetZoneData` status nor checks CP185's flag, zero-total no-summary branch, diagnostics, or parsed state. One PTAC fixture indirectly executes a clean five-infiltration positive-total path through CP184 but asserts only later node and mass-flow behavior. Parser tests bypass CP185, and a consumer test manually toggles `AirFlowFlag`. No test isolates false/true overwrite, incoming-error preservation, exact summary text or absence, error-plus-positive-summary behavior, repeated calls, partial failure, parent latch, or reset/retry. Rust has no `GetAirFlowFlag` alias, selector, parser, five totals, simple-airflow arenas, or matching EIO path. Its non-system `sum_mcp*` fields stay zero without a simple-airflow producer; typed `Construction:AirBoundary` mixing metadata is explicitly run-blocked and generates no `ZoneCrossMixing`, AirflowNetwork is unsupported, and Ideal Loads outdoor air is a distinct subsystem. CP185 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 194 routines, split 58 `state_mapped` plus 136 `source_mapped`, with 74 required; the heat-balance project list becomes 43. CP186 adds required source-mapped `routine.set_zone_mass_conservation_flag` immediately after `routine.get_air_flow_flag` in the Air-manager definition ledger and adds `set_zone_mass_conservation_flag` after `get_air_flow_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `SetZoneMassConservationFlag` is declared at `HeatBalanceAirManager.hh` line 71, implemented at `HeatBalanceAirManager.cc` lines 216-233, and called in production only as CP184's unconditional second child at line 181, after CP185 returns and before the room-air child. It receives no error reference or status parameter. Every entry first reads `ZoneAirMassFlow.EnforceZoneMassBalance` and then tests `ZoneFlowAdjustment` against exact `AdjustmentType::NoAdjustReturnAndMixing`. A false first guard or exact no-adjust value performs no writes; otherwise it visits expanded `Mixing` records from 1 through `TotMixing` in stored order and monotonically writes each receiving `ZonePtr` flag true before its source `FromZone` flag true. Zero or negative totals naturally visit no records, duplicate or self links repeat true stores, and any enum value other than exact no-adjust passes the second guard. CP186 never writes false and does not include `CrossMixing`, AirBoundary-generated cross mixing, refrigeration-door mixing, infiltration, or ventilation. `GetProjectControlData` owns the two controls, CP185's parser owns `TotMixing` and `Mixing`, and ordinary `AllocateHeatBalArrays` dimensions `ZoneMassBalanceFlag` false before the production Air pass. CP186 is the only production true writer. `CalcZoneMassBalance` reads the flag on each controlled-Zone visit of the mass-balance iteration loop to include mixing/return flow in mass continuity, while `CalcAirFlowSimple` reads it in all three delta-temperature branches when adjusted mixing is requested; resulting effects can reach return, infiltration, heat, moisture, contaminant, resimulation, and report state. CP186 has no allocation, enum, count, index, topology, or result validation and emits no diagnostic or output. In direct or externally mutated state, a malformed receiving endpoint can fail before the current record changes, while a valid receiving endpoint followed by an invalid source can leave that first write plus every prior record committed; unchecked access can be undefined. Stable valid re-entry is value-idempotent, but false guards, zero totals, removed links, or changed topology preserve stale true flags. `HeatBalFanSysData::clear_state` discards the flag array for later false reallocation; HeatBalanceData or Air-manager-only clear does not reset this separate owner, so whole-state clear and reinitialization is the clean boundary. A CP185 error that returns normally does not suppress CP186, and CP186 cannot alter CP184's aggregate; a CP186 non-return prevents room-air input and the final fatal while leaving the parent latch true. Seven direct fixtures call CP186. One explicitly exercises the second-guard no-op with enforcement true and no-adjust, five active-adjustment fixtures transitively exercise downstream mass balance, and one active fixture asserts only parser-owned controls and `ZoneReOrder`; none directly asserts `ZoneMassBalanceFlag`. No test isolates the first guard, active zero count, exact endpoint write order, sticky/repeated topology, excluded families, malformed partial writes, parent retry, or reset. Rust has no `SetZoneMassConservationFlag` alias, `ZoneAirMassFlowConservation` controls, typed `ZoneMixing` topology, endpoint flag array, mass-balance solver, or adjusted simple-mixing consumer. Its zero-seeded `sum_mcp*` state, run-blocked AirBoundary metadata, and Ideal Loads airflow are adjacent but distinct. CP186 adds no EnergyPlus source inventory, Rust target/code/state/test, capability, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 195 routines, split 58 `state_mapped` plus 137 `source_mapped`, with 75 required; the heat-balance project list becomes 44. CP187 adds required source-mapped `routine.get_simple_air_model_inputs` immediately after `routine.set_zone_mass_conservation_flag` in the Air-manager definition ledger and adds `get_simple_air_model_inputs` after `set_zone_mass_conservation_flag` and before `manage_zone_air_updates` in the heat-balance project contract. `GetSimpleAirModelInputs(EnergyPlusData &state, bool &ErrorsFound)` is declared at `HeatBalanceAirManager.hh` line 73, implemented at `HeatBalanceAirManager.cc` lines 235-4244, and called in production only by CP185 `GetAirFlowFlag` line 207, unconditionally after that wrapper writes `AirFlowFlag = true`. It first dimensions local report-selection state, allocates Zone plus sizing-or-simulation Space air-report records, and registers baseline Zone, simulation-only Space, Site, and conditional advanced outputs; it then sizes shared input scratch arrays from exactly nine direct schemas and processes `ZoneAirBalance:OutdoorAir`, all three `ZoneInfiltration` families, both `ZoneVentilation` families, `ZoneMixing`, `ZoneCrossMixing`, and `ZoneRefrigerationDoorMixing` in that order. Count-driven arenas and expanded Space/Zone records feed output and EMS registrations; `ZoneMixing` additionally constructs per-Zone `MassConservation` topology and receiving/source `ZoneReOrder` state, while `TotCrossMixing` appends generated Construction:AirBoundary records after explicit CrossMixing input without making AirBoundary a tenth parser family. The tail emits nominal infiltration, ventilation, mixing, cross-mixing, and refrigeration-door EIO rows, stores per-Zone nominal totals, conditionally retains the last encountered infiltration pointer per Zone when mass-balance enforcement is enabled, registers mass-balance outputs, and deliberately excludes refrigeration-door flow from `TotMixingFlow`. CP187 never initializes, clears, tests, or terminally barriers the supplied `ErrorsFound` reference: incoming true does not suppress any phase, diagnosed paths only raise it, and normal return lets CP185 perform its count summary regardless of the value. A blank ZoneAirBalance induced-air schedule and an enforced-balance source-only Mixing Zone without Infiltration each emit severe diagnostics without raising the reference; malformed endpoints or schedules can instead fail before normal return, so diagnosed continuation is not a return guarantee. The routine has no guard, transaction, rollback, or cleanup. Same-state repeat normally encounters already allocated HeatBalance arrays and can repeat registrations, diagnostics, or EIO; retained `UniqueZoneNames` can turn a partially cleared ZoneAirBalance retry into duplicate errors, whereas `UniqueInfiltrationNames` is only reserved and has no semantic use. Air-manager-only and HeatBalance-only clears each leave other owners stale, so coordinated whole-state reset plus normal reinitialization is only the clean domain-state retry boundary; an output-clean replay additionally requires a fresh or reset EIO stream. Fifteen actual direct unit calls cover selected design-flow infiltration/ventilation expansion, all three infiltration models, explicit CrossMixing, several Mixing mass-balance paths, one `ZoneReOrder` assertion, and mass-balance output ordering; no direct parser fixture covers ZoneAirBalance, WindAndStack ventilation, refrigeration-door mixing, or AirBoundary-generated CrossMixing, and none isolates incoming-true behavior, either unflagged severe, exact nominal EIO, partial failure, repeat, or reset. Rust has no typed records, counts, topology, outputs, EMS, EIO, or runtime path for any of the nine families; generic Zone/schedule state, zero-seeded `sum_mcp*` fields, run-blocked AirBoundary metadata, and Ideal Loads outdoor air are adjacent dependencies only. CP187 adds no Rust target, code, state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 196 routines, split 58 `state_mapped` plus 138 `source_mapped`, with 76 required; the heat-balance project list becomes 45. CP188 adds required source-mapped `routine.get_room_air_model_parameters` immediately after `routine.get_simple_air_model_inputs` in the Air-manager definition ledger and adds `get_room_air_model_parameters` after `get_simple_air_model_inputs` and before `manage_zone_air_updates` in the heat-balance project contract. `GetRoomAirModelParameters(EnergyPlusData &state, bool &errFlag)` is declared at `HeatBalanceAirManager.hh` line 75, implemented at `HeatBalanceAirManager.cc` lines 4246-4492, and called in production only as CP184 `GetAirHeatBalanceInput` line 184's unconditional third child after CP185 and CP186 return. Every entry resizes and default-fills one `AirModelData` record per Zone as Mixing, Direct, and not simulated, initializes only a local error false, counts the sole direct schema `RoomAirModelType`, diagnoses but continues when its object count exceeds the Zone count, and false-dimensions three downstream per-Zone model arrays only when at least one object exists. The object loop resolves a Zone, maps eight model selectors and Direct/Indirect coupling, sets per-Zone selector/coupling/`SimAirModel` plus monotonic global use flags, validates only the matching Zone-name presence of five `RoomAirSettings:*` families, and for AirflowNetwork checks only whether `AirflowNetwork:SimulationControl` exists. UserDefined details, RoomAir nodes and patterns, AirflowNetwork intrazone topology, and all settings fields remain later `GetAirModelDatas` dependencies. Invalid model and coupling keys emit warnings and fall back to Mixing and Direct without raising the local error; too many objects, a missing Zone or companion, a missing AirflowNetwork control object, and any non-Mixing model combined with Space heat-balance sizing or simulation raise it. The nominal duplicate-Zone check reads `AirModel.Name` before the loop ever writes that field, so every clean entry sees empty names: repeated declarations for one Zone overwrite model and coupling in input order without that diagnostic, while a prior declaration's true `SimAirModel` or global used flag can remain sticky. CP188 never retains the authored object name and never sets `AirModelData::ZonePtr`. After the object loop it overwrites every Zone's record name with `MIXING AIR MODEL FOR <Zone>` regardless of selected model, refreshes its canonical Zone name, and monotonically sets `anyNonMixingRoomAirModel`. Even with zero objects, it materializes default Mixing records and, on every normally reached tail, writes the RoomAir EIO header plus one Zone-order row per Zone before any local error summary or outer-flag update. The outer `errFlag` is never read or cleared; a local error emits `Errors found in processing input for RoomAirModelType` and assigns only true after EIO, while an incoming true with locally clean input adds no CP188 summary and still executes every phase. An EIO or earlier non-return can preserve assigned state and local diagnostics before outer propagation. CP188 has no latch, transaction, rollback, or output cleanup. Direct repeat resets the per-Zone arena but repeats EIO and retains monotonic manager flags; `RoomAirModelData::clear_state` clears CP188 arenas and used flags but does not rearm `GetAirModelData`, `MyOneTimeFlag`, or `MyEnvrnFlag`, the parent Air-manager latch is separate, and `EnergyPlusData::clear_state` does not reset EIO. End-to-end clean retry therefore requires fresh or explicitly rearmed room-air and parent lifecycle state plus a fresh or reset EIO stream. Exactly two direct unit calls exist: one proves two missing-AirflowNetwork-control diagnostics and the outer true result, while one proves a valid AirflowNetwork false result only through later intrazone Zone indexing. No direct test covers zero-object defaults or EIO, count overflow, invalid Zone, duplicate overwrite, invalid keys, Indirect coupling, any non-AirflowNetwork active model, Space incompatibility, state fields, incoming true, partial failure, repeat, or reset. Rust has no `GetRoomAirModelParameters` alias, typed `RoomAirModelType` or `RoomAirSettings` records, per-Zone selector/coupling state, non-Mixing flags, RoomAir or intrazone-node topology, dependency validation, model dispatch, or RoomAir EIO. Those room-air objects remain RawOnly and run-blocking, AirflowNetwork remains explicitly unsupported, and typed Zone/Space identities, the single Zone mean-air state, numerical zone-air selectors, HVAC Zone-air-node edges, surface adjacent-air state, and oracle-side Mixing EIO are adjacent but non-equivalent. CP188 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 197 routines, split 58 `state_mapped` plus 139 `source_mapped`, with 77 required; the heat-balance project list becomes 46. CP189 adds required source-mapped `routine.init_air_heat_balance` immediately after `routine.get_room_air_model_parameters` in the Air-manager definition ledger and adds `init_air_heat_balance` after `get_room_air_model_parameters` and before `manage_zone_air_updates` in the heat-balance project contract. `InitAirHeatBalance(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 77 and implemented at `HeatBalanceAirManager.cc` lines 4494-4507. The body owns no condition, loop, local persistent state, status, diagnostic, output, catch, cleanup, or latch; every entry forwards the unchanged state exactly once to CP190 `InitSimpleMixingConvectiveHeatGains` line 4506 and returns only after that child returns. Two production call sites exist. The ordinary `ManageAirHeatBalance` path calls CP189 unconditionally at line 154 after its optional input block and before `CalcHeatBalanceAir` and `ReportZoneMeanAirTemp`; its input latch has already been cleared after successful input, so a CP189 non-return preserves the child's partial effects, suppresses both later children, and a same-state manager retry skips input and enters CP189 again. `SimulationManager::Resimulate` conditionally calls CP189 at line 2908 when `ResimHB` is true after Surface initialization and both Surface balances; a non-return prevents refrigeration-rack work, the heat-balance iteration increment, and the line-2912 assignment forcing `ResimHVAC` true, while prior Surface and child effects remain. This second path intentionally can execute multiple times without advancing simulation time and bypasses the Air-manager input latch and its `CalcHeatBalanceAir` and report siblings. CP189 itself neither makes child re-entry safe nor adds a wrapper-specific reset obligation; mutation, failure, retry, and owner-clear behavior are exactly those of CP190 and its dependencies. As bounded dependency context rather than parity, the child's enforced mass-balance fraction loop uses each record's receiving count against the global `Mixing(1..N)` prefix and raw `DesignLevel`, not that Zone's receiving pointers or schedule/EMS-scaled flow. Five direct unit calls all use CP189 as setup for ZoneEquipment mass-balance tests and assert downstream airflow or mass-balance results rather than the wrapper's one-child cardinality, either production caller order, resimulation, failure prefix, retry, or reset; four direct CP190 calls in two fixtures bypass the wrapper. Rust's `init_air_heat_balance_compat` is an identity closure invoked with an empty body in the timestep scaffold. It has no CP190 alias, typed mixing, cross-mixing, or refrigeration-door arenas, schedule or EMS refresh, mass-conservation fractions, demand-manager resimulation path, diagnostics, or outputs; zero-seeded `sum_mcp*` state and run-blocked AirBoundary SimpleMixing metadata are adjacent but non-equivalent. CP189 adds no Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 198 routines, split 58 `state_mapped` plus 140 `source_mapped`, with 78 required; the heat-balance project list becomes 47. CP190 adds required source-mapped `routine.init_simple_mixing_convective_heat_gains` immediately after `routine.init_air_heat_balance` in the Air-manager definition ledger and adds `init_simple_mixing_convective_heat_gains` after `init_air_heat_balance` and before `manage_zone_air_updates` in the heat-balance project contract. `InitSimpleMixingConvectiveHeatGains(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 79, implemented at `HeatBalanceAirManager.cc` lines 4509-4588, and called in production only as CP189 `InitAirHeatBalance` line 4506's unconditional child. Every entry first tests `AirFlowFlag`; false leaves every target untouched. True executes four ordered phases. The actual Mixing vector, not `TotMixing`, receives `DesignLevel * sched->getCurrentVal()`, then an optional object EMS override, then the final value in both `DesiredAirFlowRate` and `DesiredAirFlowRateSaved`. Under `EnforceZoneMassBalance` the actual MassConservation vector is traversed; for each record's receiving count `N`, the first pass adds global `Mixing(1..N).DesignLevel` before zeroing the corresponding fraction, and a strictly positive sum triggers normalization from that same raw global prefix. This ignores `ZoneMixingReceivingPtr` plus schedule- and EMS-scaled desired flows, so zero, negative, or NaN sums leave touched fractions zero and each Zone can receive weights derived from unrelated records. The actual CrossMixing vector, not `TotCrossMixing`, then gets the same schedule-before-object-EMS desired flow but no saved-flow write. Finally, `TotRefDoorMixing > 0` acts only as a gate over Zone indices 1 through `NumOfZones - 1`; a flagged record whose `ZonePtr` equals that index has every declared connection volume zeroed and then replaced only when its door EMS override is on. Open schedules and physical door flow remain later `CalcAirFlowSimple` work. CP190 does not initialize infiltration, ventilation, report flags, Mixing mass flow, CrossMixing saved flow, counts, or heat-gain accumulators, and it contains no AirflowNetwork gate, allocation, validation, diagnostic, EIO, status, latch, catch, rollback, or cleanup. Null schedules or mismatched vectors, counts, fractions, Zone-indexed records, or connection arrays can stop the routine after an ordered Mixing, fraction, CrossMixing, or door prefix. Normal repeat resamples current schedule and EMS state, restores Mixing baselines, rebuilds touched fractions, and resets reached door volumes, while false guards, shortened counts, or skipped records preserve untouched values. `HeatBalanceData::clear_state` reconstructs all CP190-owned records; Air-manager-local clearing only rearms the separate input lifecycle, so clean production replay needs coordinated dependency reinitialization, but CP190 itself has no output-stream reset requirement. Four direct calls in two fixtures cover only an `AirFlowFlag = false` fraction sentinel, zero and 100/300 raw-DesignLevel fractions, and downstream CrossMixing airflow effects. They do not directly assert Mixing desired/saved flow, non-unit schedules, schedule or object EMS, count-versus-vector behavior, the ignored receiving pointers, negative/nonfinite arithmetic, CrossMixing state, any refrigeration-door branch, partial failure, repeat, resimulation, or reset. Rust has no CP190 alias, typed ZoneMixing, ZoneCrossMixing, or ZoneRefrigerationDoorMixing runtime records, schedule/EMS flow refresh, mass-conservation fraction topology, door state, or consumers. The CP189 identity closure runs an empty body; generic schedule machinery, zero-seeded `sum_mcp*` fields, and typed but run-blocked `Construction:AirBoundary` SimpleMixing metadata are adjacent but non-equivalent. CP190 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 199 routines, split 58 `state_mapped` plus 141 `source_mapped`, with 79 required; the heat-balance project list becomes 48. CP191 adds required source-mapped `routine.calc_heat_balance_air` immediately after `routine.init_simple_mixing_convective_heat_gains` in the Air-manager definition ledger and adds `calc_heat_balance_air` after `init_simple_mixing_convective_heat_gains` and before `manage_zone_air_updates` in the heat-balance project contract. `CalcHeatBalanceAir(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 83, implemented at `HeatBalanceAirManager.cc` lines 4590-4604, and called in production only by `ManageAirHeatBalance` line 158 after CP189 returns and before `ReportZoneMeanAirTemp`. Each entry tests the current `state.dataGlobal->externalHVACManager`. A configured callback selects the external branch: when `externalHVACManagerInitialized` is false it first calls `initializeForExternalHVACManager`, then invokes the callback with `&state`; a true flag skips only that initializer. An absent callback ignores the flag and calls `HVACManager::ManageHVAC`, so the external branch bypasses the entire standard HVAC manager. The initializer presently calls only `InitZoneAirSetPoints`, while neither CP191, that child, nor the runtime registration APIs set `externalHVACManagerInitialized` true; source-wide references contain only the false default and CP191 read, so a normally registered callback repeats the initializer call on every entry unless external code mutates the flag. The child's own one-time flag can suppress only its internal one-time block; it is not CP191's initialized flag. CP191 owns no direct mutation, status, diagnostic, output, catch, cleanup, rollback, or transaction. An initializer non-return suppresses the callback, and any initializer, callback, or `ManageHVAC` effects before a non-return remain while the parent report and later Surface-manager work are suppressed. Normal repeat re-evaluates the callback and executes one branch every time; child work is stateful rather than idempotent. `DataGlobal::clear_state` placement-new removes the callback and resets the flag false; Air-manager-local clear touches neither, so external-mode replay needs callback re-registration plus dependency reset. There are no direct unit calls to CP191 or its initializer and no external-manager API test; ordinary simulation tests only transitively cover the default branch. Rust's `calc_heat_balance_air_compat` is an identity closure used once around a bounded `ManageZoneAirUpdates(PredictStep)` shell and zone-temperature work. It has no external callback, flag, initializer, `ManageHVAC`, complete predictor/corrector and system-timestep loop, HVAC equipment/air/plant dispatch, demand-manager work, or report topology; separate Ideal Loads and execution-plan scaffolds are not CP191 parity. CP191 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 200 routines, split 58 `state_mapped` plus 142 `source_mapped`, with 80 required; the heat-balance project list becomes 49. CP192 adds non-required source-mapped `routine.initialize_for_external_hvac_manager` immediately after `routine.calc_heat_balance_air` in the Air-manager definition ledger and adds no project-contract requirement. `initializeForExternalHVACManager(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 81, implemented at `HeatBalanceAirManager.cc` lines 4608-4613, and called in production only by CP191 line 4598 when an external callback exists and `externalHVACManagerInitialized` is false. The standard `ManageHVAC` branch and an external branch whose flag is true both bypass CP192. Its body has no local guard or write and forwards the unchanged state exactly once to `ZoneTempPredictorCorrector::InitZoneAirSetPoints` line 4612; it does not acquire Zone setpoint input, calculate setpoints, invoke the external callback, or set CP191's initialized flag. The child is independently called by `ManageZoneAirUpdates` line 220 and owns a one-time allocation/output-registration block, begin-environment and day latches, controlled-Zone and demand-limit work, sticky error state, a fatal tail, and several other owners' arrays. A child non-return preserves its ordered prefix and prevents the CP191 callback, Air report, and later Surface-manager work. Failure before the child's one-time flag clear can repeat partial allocation or registration on retry, while failure after that clear skips the one-time prefix; a retained environment flag or sticky error can likewise repeat reset work or fatal again. Because CP191 never sets its global initialized flag, normal external-mode entry calls CP192 before every callback, although the child's separate one-time flag suppresses its successful one-time block. Coordinated owner reset is required for a clean child replay, and `DataGlobal::clear_state` removes the external callback so whole-state replay also requires re-registration. CP192 has no direct test and no external-manager registration test; four direct `InitZoneAirSetPoints` calls in four HVAC/report fixtures bypass the wrapper and use the child only as setup. Rust has no CP192 or external-manager adapter. Its `init_zone_air_set_points_compat` is an identity closure used once around an empty setpoint-calculation shell and implements none of the source child's allocation, output, environment, demand, error, or reset lifecycle. CP192 adds no EnergyPlus source inventory, Rust target, code, state, test, project requirement, support, capability, output, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 201 routines, split 58 `state_mapped` plus 143 `source_mapped`, with 80 required; the heat-balance project list remains 49. CP193 adds required source-mapped `routine.report_zone_mean_air_temp` immediately after `routine.initialize_for_external_hvac_manager` in the Air-manager definition ledger and adds `report_zone_mean_air_temp` after `calc_heat_balance_air` and before `manage_zone_air_updates` in the heat-balance project contract. `ReportZoneMeanAirTemp(EnergyPlusData &state)` is declared at `HeatBalanceAirManager.hh` line 85, implemented at `HeatBalanceAirManager.cc` lines 4615-4687, and called in production only by `ManageAirHeatBalance` line 160, unconditionally after CP191 returns. Its one-time phase scans the actual OutputProcessor request vector for exact Zone or Space Wetbulb Globe Temperature names, fans a stored empty key over the declared Zone or Space count, otherwise performs a literal name lookup, then scans EMS sensors `1..NumSensors`; Space handling is entirely guarded by `doSpaceHeatBalanceSimulation`, and only the Output:Variable keys are uppercased here. Reached matches monotonically set per-report `ReportWBGT` true, and only a completely returned scan clears `CalcExtraReportVarMyOneTimeFlag`. Every call then traverses Zones `1..NumOfZones`, calls CP194 `calcMeanAirTemps` once with each Zone's averaged temperature, humidity ratio, MRT, and report record, and, when Space simulation is active, visits that Zone's stored `spaceIndexes` in order and calls the same child with each Space's averaged state but the owning Zone number for operative-control metadata. CP194 owns the mean temperature and humidity copies, 50/50 operative temperature, dew-point psychrometrics, conditionally retained thermostat-operative value, and conditionally retained WBGT calculation; CP193 itself neither emits output nor allocates or validates these arenas. A request or sensor scan non-return preserves a true-flag prefix and the still-true latch, while a later non-return retains the false latch plus the completed Zone/Space and child-write prefix; there is no status, catch, cleanup, rollback, or transaction. Air-manager clear rearms CP193's scan but leaves its report state intact, while HeatBalance, predictor/corrector, OutputProcessor, EMS, Zone-control, and environment owners separately hold report flags and values, averaged inputs, requests, sensors, control metadata, and pressure, so clean replay requires coordinated reset and input reconstruction. No C++ unit test calls CP193 or CP194 directly or names the WBGT targets and report-only fields. Rust's `report_zone_mean_air_temp_compat` is an identity closure used only to choose one bounded Zone temperature sample during run-period aggregation; existing MAT/humidity result series and a CLI trace label provide neither the request/EMS scan, Space traversal, report bundle, operative/dew-point/WBGT calculations, failure lifecycle, nor CP193 call topology. CP193 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 202 routines, split 58 `state_mapped` plus 144 `source_mapped`, with 81 required; the heat-balance project list becomes 50. CP194 adds required source-mapped `routine.calc_mean_air_temps` immediately after `routine.report_zone_mean_air_temp`, closing the HeatBalanceAirManager definition ledger, and adds `calc_mean_air_temps` after `report_zone_mean_air_temp` and before `manage_zone_air_updates` in the heat-balance project contract. `calcMeanAirTemps(EnergyPlusData &state, Real64 ZTAV, Real64 airHumRatAvg, Real64 MRT, AirReportVars &thisAirRpt, int zoneNum)` is declared at `HeatBalanceAirManager.hh` lines 87-92, implemented at `HeatBalanceAirManager.cc` lines 4689-4728, and called only by CP193 for each Zone at line 4678 and each active stored Space membership at lines 4682-4683; Space inputs use that Space's averages but the owning Zone number for control metadata. Every entry first writes `MeanAirTemp = ZTAV`, the raw averaged humidity ratio, ordinary `0.5 * (ZTAV + MRT)` operative temperature, and dew point from current outdoor barometric pressure in that order. `PsyTdpFnWPb` floors only its local humidity input to `1e-5` before saturation work, so the stored report humidity remains raw. Thermostat-operative output updates only under `AnyOpTempControl`, controlled-Zone, and non-`None` guards. Only exact `Scheduled` reads the schedule's current value, including an EMS override; every other non-`None` value takes `FixedRadiativeFraction`. No index, pointer, finite-value, or `[0,1]` fraction check is local, the ordinary operative value is redundantly restored, and `ThermOperativeTemp = (1-f) * ZTAV + f * MRT` can extrapolate. A false guard retains its prior/default value. Only `ReportWBGT` updates `WetbulbGlobeTemp` as `0.7 * PsyTwbFnTdbWPb(ZTAV, airHumRatAvg, OutBaroPress) + 0.3 * OperativeTemp`, using raw humidity and the ordinary 50/50 operative value rather than the thermostat-weighted value; a false flag retains the old/default WBGT. The default psychrometric build uses quantized saturation and wet-bulb caches plus raw memo, warning, and recurring-error state, so normal numeric writes are not a claim of pure whole-routine idempotence or cold retry. CP194 owns no allocation, validation, diagnostic conversion, status, latch, catch, cleanup, rollback, or transaction: a dew-point non-return leaves the first three writes, schedule failure leaves the first four, and wet-bulb failure leaves all preceding reached writes while the final target remains retained. Clean replay spans separately owned HeatBalance report arenas, predictor averages, Zone controls, environment pressure, and psychrometric/cache state. No C++ unit test directly calls CP194 or CP193 or asserts any target report field; focused psychrometric tests do not cover the report composition, guards, Space topology, partial effects, repeat, or reset. Rust has no CP194 alias, integrated Air-report record, MRT/operative/control/WBGT state, Space report path, or W-input cached wet-bulb counterpart. Its isolated dew-point projection and RH-input outdoor wet-bulb helper are not wired to this source call topology. CP194 adds no EnergyPlus source inventory, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list becomes 51. CP195 expands the existing required source-mapped `routine.manage_zone_air_updates` without adding a routine or project-contract entry. `ManageZoneAirUpdates(EnergyPlusData &state, PredictorCorrectorCtrl UpdateType, Real64 &ZoneTempChange, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 264-270 and implemented at `ZoneTempPredictorCorrector.cc` lines 195-244. Every entry first tests the shared `GetZoneAirStatsInputFlag`; true calls CP196 `GetZoneAirSetPoints` and clears the latch only after that child returns. It then calls `InitZoneAirSetPoints` unconditionally before dispatching. The six recognized selectors call `CalcZoneAirTempSetPoints`, `PredictSystemLoads` with all three timestep arguments, `correctZoneAirTemps` with only the history selector, or the three revert/Zone-push/system-push history children. `Invalid`, `Num`, and out-of-range casts reach a silent default only after the common input/init prefix. Only a normally returned `CorrectStep` assigns its child result into caller-owned `ZoneTempChange`; every other selector and a correct-child non-return preserve the caller's prior value. Seven `HVACManager::ManageHVAC` sites execute Get, initial Predict and Correct, optional shortened-step Predict/Correct/PushSystem repetitions, and final PushZone; two `SimulationManager::Resimulate` sites execute only Get then Predict when HVAC resimulation is requested. No production caller selects Revert. CP195 owns no selector validation, status, diagnostic conversion, catch, cleanup, rollback, or transaction. A Get failure retains its parser prefix and true latch while suppressing Init and dispatch; an Init failure retains the now-false latch and its prefix; branch failure retains both common phases and the reached branch prefix. Normal repeat skips successful one-time input but always reruns Init and the selected stateful child, while push/revert repetition is not idempotent. Clean replay requires coordinated ZoneControls, predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, RoomAir, node, schedule, environment, and output-owner reset. The C++ unit tree directly calls CP195 zero times; direct child calls exercise Get 9 times, Init 4, setpoint calculation 21, Predict 16, and Correct 5, but none of the three history children, and none proves wrapper order, latch timing, one-branch dispatch, default behavior, output-reference retention, partial failure, retry, or reset. Rust's stage object is metadata and `manage_zone_air_updates_compat` ignores its selector while passing one arbitrary closure through. Live Rust invokes only Predict and Correct selector shells, manually composes empty/common and history wrappers in a topology that is not the source dispatch, and has no shared input latch, unconditional Init contract, `ZoneTempChange` reference, invalid/default selector, or coordinated owner lifecycle. Existing bounded MAT, coefficient, correction, history, execution-plan, and promoted output evidence does not establish CP195 parity. CP195 adds no EnergyPlus source inventory, routine count, project entry, Rust target, code, state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory remains 32 algorithms and 203 routines, split 58 `state_mapped` plus 145 `source_mapped`, with 82 required; the heat-balance project list remains 51. CP196 adds required source-mapped `routine.get_zone_air_set_points` immediately after `routine.manage_zone_air_updates` in the predictor/corrector definition ledger and adds `get_zone_air_set_points` after `manage_zone_air_updates` before `update_final_surface_heat_balance` in the heat-balance project contract. `GetZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 272 and implemented at `ZoneTempPredictorCorrector.cc` lines 246-2174 with no explicit return or internal latch. It executes ordinary thermostat and four setpoint families, humidistat, thermal-comfort and four Fanger families, unconditional HybridModel input, Zone capacitance assignment and EIO, operative/adaptive comfort, temperature-and-humidity overcool, and staged-dual input before one accumulated-error fatal. It expands ZoneLists, allocates and links control and schedule state, mutates Zone indices and multipliers, registers operative-temperature outputs, preserves warning and severe paths that do not set the local fatal flag, and can exit earlier through Hybrid or adaptive helper fatals. The staged precheck reads the routine-wide error flag, the capacitance ZoneList loop omits its final member, and no rollback or re-entry guard exists. Three production call sites share `GetZoneAirStatsInputFlag` and clear it only after return; a non-return retains partial state and the true latch, so same-state retry is unsafe. Nine direct C++ unit calls cover selected valid fields and two fatal diagnostics but not the complete family order, shared latch, valid staged, overcool, ZoneList, or capacitance-output paths, partial failure, retry, or reset. Rust types only direct-Zone DualSetpoint thermostat and humidistat subsets and exposes an identity `get_zone_air_set_points_compat` closure; it does not implement the complete 16-family transaction, ZoneList expansion, diagnostic and output order, latch, partial effects, or owner lifecycle. CP196 adds no new EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 204 routines, split 58 `state_mapped` plus 146 `source_mapped`, with 83 required; the heat-balance project list becomes 52. CP197 adds non-required source-mapped `routine.calculate_monthly_running_average_dry_bulb` immediately after `routine.get_zone_air_set_points`; the heat-balance project contract remains unchanged. `CalculateMonthlyRunningAverageDryBulb(EnergyPlusData &state, Array1D<Real64> &runningAverageASH, Array1D<Real64> &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` line 284 and implemented at `ZoneTempPredictorCorrector.cc` lines 2176-2275. Its only two production calls are CP196 operative-temperature branches at lines 1661 and 1731, each under the shared adaptive initialized guard and immediately before CP198; callers allocate fresh zeroed `NumDaysInYear` arrays, while CP197 neither reads nor sets the guard and only CP198 sets it true. CP197 checks and opens the weather path, discards nine lines, blindly reads `NumDaysInYear * 24` records, strips six comma fields, and averages field 7 into positional days before its ASH and CEN windows. The canonical eight-header mismatch shifts daily buckets by one hour; EOF and conversion status are ignored, `StrToReal` silently supplies -99999 on invalid text, and the corrupted final bucket is never consumed. The regular ASH path sums 31 inclusive days and divides by 30, the regular CEN path sums 8 and divides by 7, both wrap paths exclude `dailyDryTemp(N)`, and each caller cell self-accumulates as `x = x + avgDryBulb` without local clearing before `/=`. Missing or unreadable weather fatals before output writes, while malformed readable input can flow into CP198 and become latched. No C++ unit test executes CP197; the adaptive test calls CP198 with synthetic arrays. Rust has a typed EPW parser, while its legacy dry-bulb loader skips eight headers and reports numeric conversion errors; neither provides CP197 averaging, adaptive state, or an operative-control path. CP197 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 205 routines, split 58 `state_mapped` plus 147 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP198 adds non-required source-mapped `routine.calculate_adaptive_comfort_set_point_schl` immediately after `routine.calculate_monthly_running_average_dry_bulb`; the heat-balance project contract remains unchanged. `CalculateAdaptiveComfortSetPointSchl(EnergyPlusData &state, Array1D<Real64> const &runningAverageASH, Array1D<Real64> const &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` lines 286-287 and implemented at `ZoneTempPredictorCorrector.cc` lines 2277-2348. Its only two production calls are CP196 lines 1662 and 1732, immediately after CP197 and under the same external false-initialized guard; CP198 has no local guard. It scans all stored design days, uses only day type 9, derives `MaxDryBulb - DailyDBRange / 2`, and conditionally writes shared ASH slots 0-2 for strict `(10, 33.5)` and CEN slots 3-6 for strict `(10, 30)`. Later qualifying design days overwrite each family independently, while nonqualifying values retain earlier/default slots; the C++ declaration `std::array<Real64, 7> = {-1}` initializes the vector as `[-1, 0, 0, 0, 0, 0, 0]`. It then allocates seven `NumDaysInYear` arrays and overwrites every day with three `0.31 * T + offset` ASH values or `-1`, followed by four `0.33 * T + offset` CEN values or `-1`. Exact bounds and nonfinite daily inputs take the `-1` branches. Input shape is trusted, the const input arrays are untouched, and the shared `initialized` flag is set only after all design, allocation, and daily work returns. There is no status, diagnostic, catch, cleanup, or rollback; abnormal non-return can retain design writes, an allocation/day prefix, and the prior flag, while complete direct re-entry runs even when the flag is already true. A later CP196 fatal retains successfully committed schedules and causes same-state input retry to skip both adaptive helpers; only full predictor/corrector owner reconstruction resets them. One C++ fixture calls CP198 directly three times with uniform 0, 40, and 25 arrays, asserting day 1, all seven daily formulas for 25, the flag, and only two design slots; strict bounds, nonfinite and shaped inputs, remaining days, multiple/default design days, partial failure, guarded integration, retry, and reset remain uncovered. Rust has no operative-temperature control object, adaptive model/formulas, schedule state, or consumer; DualSetpoint thermostat parsing and execution-plan labels are not parity. CP198 adds no source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 206 routines, split 58 `state_mapped` plus 148 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP199 adds required source-mapped `routine.init_zone_air_set_points` immediately after `routine.calculate_adaptive_comfort_set_point_schl` and adds `init_zone_air_set_points` after `get_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `InitZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 274 and implemented at `ZoneTempPredictorCorrector.cc` lines 2350-2816. CP195 line 220 calls it on every selector after optional CP196 input and before dispatch; CP192 line 4612 also calls it directly for external-HVAC initialization without reproducing that input prefix. The default-true one-time block sizes thermostat setpoints, load correction, control/report, optional comfort/Fanger, setback/deadband, ZoneList/ZoneGroup load, six hybrid-history, Zone demand, and simulation-or-sizing Space demand state. It warning-checks each Zone's heat-transfer-surface reference-air consistency, registers Zone and simulation-Space heat-balance and sensible/moisture demand bundles, six direct thermostat/correction outputs, optional comfort outputs, and ZoneList/ZoneGroup outputs, and clears its one-time latch only after that complete prefix. A begin-environment gate then calls CP200 for every Zone and active Space, zeros four current thermostat setpoints, sets load correction to one and temperature control to Uncontrolled, resets Zone and active-Space demand helpers, `DeadBandOrSetback`, `NoHeatToReturnAir`, and hybrid histories, and clears `MyEnvrnFlag` only at its tail. It intentionally does not directly reset averaged setpoints, temperature-control report, comfort control/report/Fanger state, `Setback`, `CurDeadBandOrSetback`, or ZoneList/ZoneGroup totals; seeing `BeginEnvrnFlag` false rearms the gate. The begin-day branches only toggle `MyDayFlag`. Every call then scans ordinary temperature controls followed by comfort controls. Once Zone-equipment input is filled and before `ControlledZonesChecked`, missing Zone configuration emits Severe plus Continue diagnostics and sets the routine-owned sticky `ErrorsFound`. Independent demand-management branches clamp ordinary controls with strict `>`/`<` comparisons and comfort controls with inclusive `>=`/`<=` comparisons; selected single heat/cool and single-heat-cool comfort paths also rewrite the ordinary control type/report, and the later comfort loop can overwrite ordinary-loop state. The fatal check occurs after both loops and their reached clamp mutations; only a normally reached final tail sets `ControlledZonesChecked = true`. No entry clears `ErrorsFound` and no status, catch, cleanup, rollback, shape reconciliation, or transaction exists. A non-return can retain an allocation/output, environment-reset, diagnostic, or clamp prefix according to the already committed latches; same-state error retry can repeat validation diagnostics and fatal, while clean replay requires coordinated predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, ZoneControls, ZoneEquipment, Surface, environment, and OutputProcessor reset. Four direct C++ unit calls use CP199 only as setup with begin-environment, comfort, demand, and validation behavior inactive and assert none of its state; focused boundary, warning, output-bundle, partial-failure, retry, and reset coverage is absent. Rust's `init_zone_air_set_points_compat` is an identity closure used only in the Predict scaffold and omitted from its Correct shell. Its separate constant-DualSetpoint heating/cooling observed series and limited `ZoneSysEnergyDemand` snapshots implement neither CP199 allocation, output binding, lifecycle, validation, demand clamps, nor selector-independent topology. CP199 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 207 routines, split 58 `state_mapped` plus 149 `source_mapped`, with 84 required; the heat-balance project list becomes 53. CP200 adds required source-mapped `routine.zone_space_heat_balance_begin_environment_init` immediately after `routine.init_zone_air_set_points` and adds `zone_space_heat_balance_begin_environment_init` after `init_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::beginEnvironmentInit(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 213 and implemented at `ZoneTempPredictorCorrector.cc` lines 2818-2836. Its only two direct call expressions are CP199 lines 2624 and 2628 inside `MyEnvrnFlag && BeginEnvrnFlag`: every stored Zone record runs first, then every stored Space record only when current `doSpaceHeatBalance` is true. The helper owns no identity, count, gate, or latch. For each fixed index 0 through 3 it writes `ZTM[i] = 0`, `WPrevZoneTS[i] = OutHumRat`, `DSWPrevZoneTS[i] = OutHumRat`, and `WPrevZoneTSTemp[i] = 0` in that order; it then writes `WTimeMinusP`, `W1`, `WMX`, and `WM2` from `OutHumRat` before zeroing `airHumRatTemp`, `tempIndLoad`, `tempDepLoad`, `airRelHum`, `AirPowerCap`, and `T1`. These are 26 overwrites: 12 outdoor-humidity values and 14 zeros. CP200 reads no prior target value and leaves current/average air humidity, MAT/MRT/ZT and their other histories, coefficients outside the two load scalars, and every unlisted record field unchanged. All four indexed targets are fixed `std::array<Real64, 4>` values. Outdoor humidity is copied without finite, sign, range, or consistency validation, so negative, infinite, and quiet-NaN values propagate to all 12 humidity targets without a clamp or diagnostic. There is no allocation, checked access, status, diagnostic, child call, catch, cleanup, or rollback, and valid state has no ordinary catchable failure path. Complete repeat with unchanged outdoor humidity is overwrite-idempotent; a changed value replaces all 12 humidity targets. CP199 alone controls frequency and clears its environment latch after the entire parent reset, so a Space mode that becomes true later in the same uninterrupted BeginEnvironment interval does not replay skipped Space records. No C++ unit test calls CP200 directly; the four direct CP199 fixtures have `BeginEnvrnFlag` false. Of 56 active full-simulation tests that reach CP199, 55 execute at least one Zone CP200 call; `WeatherManager_SetRainFlag` has zero Zones and executes none. Seven `SizingManager` tests also reach sizing-Space and one `HeatBalanceAirManager` test reaches simulation-Space, but none asserts CP200 fields. Rust creates only Zone state at run initialization with configurable three-slot temperature histories and fixed-default three-slot humidity histories; when a weather series exists, it performs a one-time first-weather-sample seed of current and averaged humidity plus both three-slot histories. That seed touches a different field set and is not CP200's current-`OutHumRat` four-slot per-environment Zone/Space reset. Rust lacks the exact four-slot `ZTM`, `WPrevZoneTS`, `DSWPrevZoneTS`, and `WPrevZoneTSTemp` set, Space records, 26-field touched-versus-retained boundary, and environment gate. Its coefficient representation is initialized and computed through a different path; these adjacent histories and coefficients are not CP200 parity. CP200 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 208 routines, split 58 `state_mapped` plus 150 `source_mapped`, with 85 required; the heat-balance project list becomes 54. CP201 adds required source-mapped `routine.zone_space_heat_balance_set_up_output_vars` immediately after `routine.zone_space_heat_balance_begin_environment_init` and adds `zone_space_heat_balance_set_up_output_vars` after `zone_space_heat_balance_begin_environment_init` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::setUpOutputVars(EnergyPlusData &state, std::string_view prefix, std::string const &name)` is declared at `ZoneTempPredictorCorrector.hh` line 215 and implemented at `ZoneTempPredictorCorrector.cc` lines 2838-2868. Its only production call expressions are CP199 line 2443 for every Zone and lines 2446-2447 for each stored Space membership only when `doSpaceHeatBalanceSimulation` is true; Zone and nested Space calls follow parent Zone order, while sizing-only Space mode does not enter this child. Each call registers, in order, `{Zone|Space} Air Temperature` bound to `ZT` as C/System/Average, `Air Humidity Ratio` bound to `airHumRat` as Units::None/System/Average, `Air Relative Humidity` bound to `airRelHum` as percent/System/Average, and `Mean Radiant Temperature` bound to `MRT` as C/Zone/Average, keyed by the source Zone or Space name. The optional meter fields remain invalid or empty, multipliers remain one, SQL index remains -999, and the report-frequency argument defaults to Hour before any matching request overrides its frequency and schedule. CP201 writes none of the four members, but OutputProcessor initialization, request parsing, dictionary insertion, setup/total counters, requested keyed OutVar pointers, report identifiers, and report-dictionary sinks are persistent side effects. Rows with neither a report request nor a DataOutputs variable-list match still create or reuse dictionary rows and advance setup/total counters without an OutVar. A variable-list-only match appends a keyed dummy pointer, DD link, and report identifier without emitting a report dictionary row; a report request additionally marks the entry for reporting and writes its dictionary sinks. The dictionary deduplicates case-insensitive name plus units only, while direct re-entry repeats the applicable counter, dummy or requested entry, link, identifier, and sink effects. No identity, membership, prefix, key, lifecycle, guard, status, catch, cleanup, or rollback is local. A formatting, initialization, request-input, allocation, or report-write non-return can retain any completed prefix while CP199's one-time latch remains true until line 2618; safe replay requires coordinated predictor/corrector record and OutputProcessor ownership rather than either reset alone. There is no direct CP201 unit call or positive registration assertion. Four direct CP199 fixtures and 55 active full-simulation tests execute at least one Zone setup indirectly; one additional no-Zone `WeatherManager_SetRainFlag` simulation reaches CP199 but executes no CP201 call. Only `HeatBalanceAirManager_GetMixingAndCrossMixing` reaches simulation-Space setup, for three stored Spaces, while seven sizing-Space tests do not. Rust's registry registers adjacent `Zone Mean Air Temperature`, while its heat-balance ResultStore separately stores `Zone Mean Air Temperature` and `Zone Mean Air Humidity Ratio`; neither registers or stores CP201's exact eight-name Zone/Space output set, four member bindings, System-versus-Zone timestep metadata, Space records, or registration lifecycle. Exact `Zone Air Temperature` and `Zone Air Humidity Ratio` constants in the IdealLoads CLI are ESO oracle input identities; the humidity name is also reused for a diagnostic comparison label, but neither is a RuntimeOutputRegistry or ResultStore production definition. CP201 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 209 routines, split 58 `state_mapped` plus 151 `source_mapped`, with 86 required; the heat-balance project list becomes 55. CP202 adds required source-mapped `routine.predict_system_loads` immediately after `routine.zone_space_heat_balance_set_up_output_vars` and adds `predict_system_loads` after `zone_space_heat_balance_set_up_output_vars` before `update_final_surface_heat_balance` in the heat-balance project contract. `PredictSystemLoads(EnergyPlusData &state, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 276-280 and implemented at `ZoneTempPredictorCorrector.cc` lines 2870-3145. Its only production call expression is CP195 line 227; the initial and downstepped `HVACManager` PredictStep sites plus `SimulationManager::Resimulate` enter through that wrapper after CP196 and CP199 return. Four phases execute in order. The staged loop samples both base schedules, uses `MAT` or shortened `XMPT`, increments warning or recurring-warning state and clamps heat to cool minus 0.1 when heat is not below cool, chooses the last qualifying negative cooling or positive heating `StageNum`, and applies the source-literal throttling setpoint. The heating branch notably compares its offset with half `CoolThroRange` while shifting by half `HeatThroRange`. Active Space heat balance copies the Zone stage to every stored Space membership. The on/off prefix is gated by `NumOnOffCtrZone` but scans every temperature-controlled Zone having positive `DeltaTCutSet`, saves or restores last-mode flags according to `ShortenTimeStepSys`, clears both off flags, selects `MAT` or shortened `XMPT` only for ThirdOrder and `T1` otherwise, and revises SingleHeat, SingleCool, or both DualHeatCool setpoints with the literal strict comparisons around 0.02 C; `SingleHeatCool` and other switch values receive no setpoint write. An inclusive revised dual lower-greater-than-or-equal-upper collision emits severe and continuation diagnostics and then fatals before prediction dispatch. Every Zone then calls the still-source-mapped CP203 `ZoneSpaceHeatBalanceData::predictSystemLoad` with all three timestep arguments. Each stored Space calls that child when `doSpaceHeatBalance` is true; otherwise a shortened call copies only the returned Zone `MAT` and `airHumRat` to the Space, while a nonshortened call leaves it unchanged. `UseZoneTimeStepHistory` and `PriorTimeStep` are only forwarded locally. The final on/off pass derives `CoolModeLast` and `HeatModeLast` from the off flags and inclusive sign tests on child-produced `TotalOutputRequired`; zero can set both when both flags are true, while NaN clears both. There is no local latch, status, finite or topology validation, catch, cleanup, or rollback. Staged warnings and any completed setpoint, stage, save/restore, child, or Space-copy prefix persist across a fatal or child non-return; same-state retry samples schedules again, advances staged recurrence state, and repeats child effects. Two direct C++ fixtures make 16 calls and 24 setpoint assertions, covering selected Euler and ThirdOrder normal/shortened on/off cases only; global `NumOfZones` remains zero, so no Zone/Space child, forwarding, staged path, final-mode result, failure, retry, or reset contract is asserted. Of 56 active full simulations that reach CP202, 55 execute a Zone child and eight enable a Space child, but none is positive staged/on-off or direct CP202 state coverage. Rust retains the cutout delta only in typed thermostat input, never consumes it at runtime, has no staged control, `StageNum`, CP202 off/mode memory, sensible `ZoneSysEnergyDemand::TotalOutputRequired` field, or Space heat-balance record, and supplies only an identity `predict_system_loads_compat` wrapper around a different bounded Zone-temperature update. Oracle-derived IdealLoads demand snapshots and a separate `predict_step_source_order_path` wrapper-order test do not implement this transaction. CP202 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 210 routines, split 58 `state_mapped` plus 152 `source_mapped`, with 87 required; the heat-balance project list becomes 56. CP203 adds required source-mapped `routine.zone_space_heat_balance_predict_system_load` immediately after `routine.predict_system_loads` and adds `zone_space_heat_balance_predict_system_load` after `predict_system_loads` before `calc_zone_air_temp_set_points` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::predictSystemLoad(EnergyPlusData &state, bool shortenTimeStepSys, bool useZoneTimeStepHistory, Real64 priorTimeStep, int zoneNum, int spaceNum)` is declared at `ZoneTempPredictorCorrector.hh` lines 217-222 and implemented at `ZoneTempPredictorCorrector.cc` lines 3146-3257. Its only production call expressions are CP202 line 3116 for every Zone and lines 3119-3120 for every stored Space only while `doSpaceHeatBalance` is true. After a positive-`zoneNum` debug assertion, CP203 first calls the still-deferred `updateTemperatures` child with all timestep/history arguments and both identities. It then selects Space volume only for positive `spaceNum`, otherwise Zone volume, and writes `AirPowerCap` from that volume, the parent Zone's sensible capacitance multiplier, current pressure/MAT/humidity psychrometrics, and unguarded `TimeStepSysSec`. After `calcZoneOrSpaceSums(state, false, zoneNum, spaceNum)` returns, a Zone-only hybrid branch clears and refreshes `SumIntGainExceptPeople`. CP203 directly derives `TempDepCoef`, `TempIndCoef` including `SysDepZoneLoadsLagged`, the fixed three-history `TempHistoryTerm`, and ThirdOrder-shaped `tempDepLoad`/`tempIndLoad`. A used RoomAir AirflowNetwork control node under the state-wide nonmixing flag first runs `LoadPredictionRoomAirModelAFN`, then overwrites those coefficient, capacity, history, and load values and optionally obtains `HVAC(1).SupplyFraction` as local `RAFNFrac`; this Zone-keyed override is not suppressed for a Space record. CP203 next clears shared `ShortenTimeStepSysRoomAir`. ThirdOrder performs no further direct history rewrite. Every other solution algorithm uses `shortenTimeStepSys && TimeStepSys < TimeStepZone` to select this record's `TM2`/`WM2` when shared `PreviousTimeStep < TimeStepZone`, otherwise `TMX`/`WMX`, sets corresponding AirflowNetwork-node T1 histories, and sets the shared flag true; the other path copies current `ZT`/`airHumRat` and current AFN-node values while leaving it false. These later AFN history copies test only the Zone model type, without the earlier nonmixing or `IsUsed` guards. Non-ThirdOrder finally replaces both load scalars with the plain coefficients. The last ordered calls are `calcPredictedSystemLoad` and then `calcPredictedHumidityRatio` with `RAFNFrac` and both identities. Their thermostat, sensible-demand, humidity-control, moisture-demand, diagnostic, and reporting behavior, together with temperature updating, sum assembly, psychrometrics, hybrid gain collection, and AFN load prediction, remains child dependency behavior rather than CP203 implementation credit. Apart from the debug assertion, CP203 has no local identity, upper-bound, membership, finite, timestep, volume, multiplier, node, or history validation and no local diagnostic, latch, status, catch, cleanup, or rollback. An abnormal child return can retain any completed history, capacity, hybrid clear, coefficient, AFN, shared-flag, record-history, node-history, or sensible-demand prefix; a moisture-child failure follows the already committed sensible child. Same-state retry reruns every child and direct write, and clean replay requires coordinated Zone/Space records, HVAC timestep/global state, HeatBalFanSys and node histories, RoomAir/AFN, HybridModel/internal gains, sensible/moisture demand, diagnostics, and every child owner. No C++ test calls CP203 directly; CP202's 16 focused calls retain zero Zones and never enter it. Of 56 active full simulations reaching CP202, 55 execute at least one Zone CP203 and eight enable Space CP203, but their downstream assertions do not isolate this transaction and none covers its AFN override. Rust's identity `predict_system_loads_compat` encloses a different Zone-only temperature update. Existing coefficient, moist-air-capacitance, analytical/ThirdOrder, history-interpolation, and no-OA ThirdOrder moisture helpers cover adjacent formulas only; their guarded ownership omits CP203's Space path, lagged/hybrid/AFN overrides, shared room-air shortening flag, exact T1/W1 and AFN-node history transaction, and ordered sensible-then-moisture demand children. CP203 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 211 routines, split 58 `state_mapped` plus 153 `source_mapped`, with 88 required; the heat-balance project list becomes 57. CP204 adds required source-mapped `routine.calc_zone_air_temp_set_points` immediately after `routine.zone_space_heat_balance_predict_system_load` and adds `calc_zone_air_temp_set_points` after `zone_space_heat_balance_predict_system_load` before `zone_space_heat_balance_calc_predicted_humidity_ratio` in the heat-balance project contract. `CalcZoneAirTempSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 282 and implemented at `ZoneTempPredictorCorrector.cc` lines 3259-3460. Its only production call expression is CP195 line 224 for the `GetZoneSetPoints` selector; both ordinary HVAC timestep setup and HVAC resimulation enter through that wrapper after CP196 and CP199 return. Every call first assigns the complete `TempControlType` array to Uncontrolled, conditionally allocates but never resizes the two occupied-setpoint arrays, then fills every existing heating entry with zero and cooling entry with 100. The local `DeltaT` is assigned zero and never consumed, while `TempControlTypeRpt` and the thermostat setpoint fields receive no global reset. In stored temperature-control order, CP204 trusts `ActualZoneNum` and schedule pointers, samples and casts the control-type schedule, writes the enum plus integer report, and dispatches five nominal cases. Uncontrolled leaves all setpoint fields stale. SingleHeat alone guards its family with `isUsed`, samples the heating schedule into generic `setpt`, snapshots the raw low thermostat value, applies operative-temperature adjustment, and writes only `setptLo`. SingleCool samples its cooling schedule without an `isUsed` guard, snapshots the raw high value, optionally applies adaptive comfort and snapshots `setptAdapComfortCool`, then applies operative-temperature and temperature-and-humidity overcool adjustments while leaving `setptLo` stale. SingleHeatCool samples the family's heating schedule, optionally applies adaptive comfort, applies operative control, copies the result to both bounds, and under allocated optimum-start state may replace generic `setpt` from the SingleHeat family day array at `(ceil(OccStartTime) + 1) * TimeStepsInHour` before an independent `OptStartFlag` copies it to both bounds. DualHeatCool samples and snapshots cooling first, optionally applies adaptive comfort, applies operative control, then samples, snapshots, and operative-adjusts heating; a matching optimum-start identity fills the globally reset occupied arrays from the dual day arrays, an independent flag copies those values into the bounds, and overcool runs last. The invalid-control default emits one Severe diagnostic and continues without a local fatal. After every ordinary record branch, an enabled non-warmup, non-sizing, non-kickoff thermostat-fault scan stops at the first case-insensitive thermostat-name match even when its availability is nonpositive; an available match samples optional severity and subtracts the resulting offset from generic, low, and high setpoints without changing raw or adaptive snapshots. This can accumulate on fields the selected branch did not rewrite. After the ordinary loop, comfort setpoints run conditionally and can overwrite ordinary/fault results, then the EMS override runs unconditionally with final precedence. Operative, adaptive, overcool, comfort, EMS, schedule, and fault helpers remain dependency behavior rather than CP204 implementation credit. CP204 has no Space path, local latch, status, catch, cleanup, rollback, topology/shape/index/schedule/enum/time/finite validation, or allocation reconciliation. Any allocation, schedule, diagnostic, or child non-return retains the reached reset, allocation, record, fault, comfort, or EMS prefix; same-state retry resamples schedules, resets occupied arrays, repeats children and diagnostics, and can compound offsets in untouched fields. Clean replay requires coordinated ZoneControls, HeatBalFanSys setpoint/type/report, Availability optimum-start, schedule, FaultsManager, global/environment/weather, Zone heat-balance MRT/RH, comfort/ThermalComfort, EMS, diagnostic, and child-owned state. Four C++ fixtures make 21 direct calls and 33 thermostat-field post-call assertions: the optimum-start fixture contributes one call/two assertions, the reporting fixture four/seven, and two cutout/prediction fixtures eight/twelve each. The latter calls are composed with CP202, and helper-only tests cover adaptive adjustment and EMS override; no direct helper test covers operative adjustment, overcool, or comfort calculation. Of 57 active full simulations, one expected EMS fatal stops before CP204 and the other 56 reach it; 38 contain a thermostat declaration by static input evidence, but none directly asserts post-CP204 setpoint or control state and none contains comfort, operative-temperature, overcool, optimum-start, or thermostat-fault input. Rust's `calc_zone_air_temp_set_points_compat` is an identity closure invoked once around an empty closure inside the Predict scaffold, rather than the source `GetZoneSetPoints` branch, and has no direct test. Rust types only a direct-Zone DualSetpoint thermostat subset; its IdealLoads diagnostic helper selects the first control, ignores the control-type schedule, and repeats constant schedule values outside the heat-balance runtime. It implements none of CP204's control-family selection, adjustments, optimum-start, fault, comfort, EMS, mutation order, partial effects, or owner lifecycle. CP204 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 212 routines, split 58 `state_mapped` plus 154 `source_mapped`, with 89 required; the heat-balance project list becomes 58. CP205 adds required source-mapped `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` immediately after `routine.calc_zone_air_temp_set_points` and adds `zone_space_heat_balance_calc_predicted_humidity_ratio` after `calc_zone_air_temp_set_points` before `correct_zone_air_temps` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::calcPredictedHumidityRatio(EnergyPlusData &state, Real64 RAFNFrac, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 243 and implemented at `ZoneTempPredictorCorrector.cc` lines 3462-3815. Its only production call expression is CP203 line 3256, after the sensible-demand child; every Zone and every active Space therefore runs CP205 in parent order. CP205 first derives two local RH setpoints and a controlled flag from the parent Zone. A positive `humidityControlZoneIndex` samples humidifying then dehumidifying schedules, applies independent EMS overrides, then scans the first name-matching humidistat fault outside warmup, sizing, and kickoff. A thermostat-dependent fault searches thermostat faults by object name, fatals when none exists, and only for a nonzero available severity-scaled thermostat offset converts both RH values through humidity ratio at record `MAT + offset` and back at `MAT` before clamping to [0,100]. Every other humidistat-fault type samples its own availability/severity, subtracts the offset, and clamps only when available. The first matching humidistat or thermostat fault stops its scan even when unavailable. A humidifying value above dehumidifying emits first-occurrence plus recurring warnings through the control-owned `ErrorIndex` and is reduced to the dehumidifying value; exact equality marks a single setpoint. Without a humidistat, `DoingSizing && DoLatentSizing` scans Zone equipment configurations only to the first controlled entry, does not match it to `zoneNum`, uses its matching `Sizing:Zone` or falls back to the first sizing input, and, when latent sizing is enabled there, samples or reads its RH values, silently reduces humidifying to dehumidifying, and marks the record controlled. Thus the same first controlled sizing input can drive every otherwise uncontrolled Zone and Space invocation. Controlled work adds record `latentGain` to Zone-indexed radiant-system and pool latent gains, computes moist-air density and vapor enthalpy, and assembles `A` and `B` either from Zone-indexed AirflowNetwork multizone exchange plus record surface-moisture terms or from record outdoor, ventilation, exhaust, cross-mixing, mixing, outdoor-air, and surface-moisture fields. `C` uses Space volume only for positive `spaceNum`, otherwise Zone volume, together with the parent Zone moisture-capacitance multiplier and unguarded system-timestep seconds. A parent Zone RoomAir AirflowNetwork model then replaces `A`, `B`, `C`, and vapor enthalpy from its control node without the earlier CP203 nonmixing or `IsUsed` gates, including during a Space invocation. For each RH setpoint CP205 converts record `ZT` and pressure to humidity ratio, then computes ThirdOrder, Analytical, or Euler load. ThirdOrder uses the first three `WPrevZoneTSTemp` values. Analytical uses the exact `A == 0` branch or reuses `exp(min(700, -A/C))` from the humidifying calculation for dehumidifying. Euler uses `C * (Wsp - W1) + A * Wsp - B`. Any positive `RAFNFrac` divides each load; zero, negative, or NaN does not. An unknown solution enum leaves both initialized loads zero. Exact-equal setpoints select humidifying load. Otherwise two positive loads select humidifying, two negative select dehumidifying, and the inclusive humidifying-nonpositive/dehumidifying-nonnegative deadband selects zero; every other combination emits Severe/Continue diagnostics and fatals before demand commit. The final identity branch selects Space demand only for positive `spaceNum`. Controlled state calls `reportMoistLoadsZoneMultiplier` with the parent Zone identity; that dependency stores three unmultiplied predicted rates, three Zone-multiplied demand values, and conditionally initializes sequenced equipment demand. Uncontrolled state directly zeroes only `TotalOutputRequired`, `OutputRequiredToDehumidifyingSP`, and `OutputRequiredToHumidifyingSP`, leaving raw predicted and sequenced fields stale. Counts, indices, membership, pointers, schedule/EMS/fault/sizing values, setpoint ranges, pressure, temperature, humidity, histories, volume, multiplier, timestep, flows, AFN topology, `RAFNFrac`, and finite values are otherwise trusted. Apart from the humidity-control debug assertion, CP205 has no local validation, latch, status, catch, cleanup, or rollback. A missing dependent thermostat or impossible load combination fatals before final demand write; schedule, psychrometric, allocation, diagnostic, or report-child non-return retains any earlier diagnostic, recurrence-index, or partial demand prefix. Zone demand can commit before a later Space failure. Same-state retry resamples schedules and faults, repeats calculations, advances recurring warnings, and repeats child writes; clean replay requires coordinated Zone/Space heat-balance records, ZoneControls and schedules/EMS, FaultsManager, sizing and Zone equipment, HVAC timestep, environment and psychrometrics, AFN/RoomAir, Zone/Space moisture demand including sequences, multipliers, and diagnostics. No C++ test calls CP205 directly. One helper-only fixture calls `ZoneSystemMoistureDemand::reportMoistLoadsZoneMultiplier` four times with 21 assertions, covering only Zone raw and multiplied values, not Space or sequenced output. Of 57 active full simulations, one expected EMS fatal stops before prediction and one zero-Zone weather case reaches CP202 without CP205; the other 55 execute Zone CP205 and eight also execute Space CP205. Their enclosing inputs contain no humidistat, humidistat fault, EMS humidistat override, or RoomAir AFN. Only one ThirdOrder latent-sizing configuration makes the controlled calculation positive for one Zone and three Spaces, and its assertions are downstream; six Analytical configurations and every other reached record take the uncontrolled zero path, while no Euler configuration exists. Rust has no heat-balance CP205 wrapper, call, Zone/Space moisture-demand owner, or runtime mutation. The separate `calc_no_oa_third_order_moisture_demand_compat` implements a guarded Zone-only no-outdoor-air `A = 0` ThirdOrder subset with record density/capacity, internal latent gain, two RH conversions, three-history term, sign/deadband choice, and one combined Zone multiplier. It has two non-test callers, two direct predictor tests, and one adjacent history-term test; a transactional fixed-one-step IdealLoads Humidistat wrapper adds seven tests and eight call expressions around predictor, PurchasedAir, corrector, and history push. That subset rejects invalid inputs through `Option`, clamps both RH fractions, and returns only multiplied loads. It omits source schedule/EMS/fault/sizing ownership, radiant/pool gains, airflow and surface moisture, AFN/RoomAir/RAFN scaling, Space state, Analytical/Euler, diagnostics and partial effects, uncontrolled zero writes, raw predicted values, and sequenced demand. Its CLI path still consumes EnergyPlus-seeded histories, temperatures, sensible demand, and pressure; existing manifest-specific no-OA IdealLoads evidence does not state-map CP205. CP205 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 213 routines, split 58 `state_mapped` plus 155 `source_mapped`, with 90 required; the heat-balance project list becomes 59. CP206 adds required source-mapped `routine.correct_zone_air_temps` immediately after `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` and adds `correct_zone_air_temps` after `zone_space_heat_balance_calc_predicted_humidity_ratio` before `zone_space_heat_balance_correct_air_temp` in the heat-balance project contract. `correctZoneAirTemps(EnergyPlusData &state, bool useZoneTimeStepHistory)` is declared at `ZoneTempPredictorCorrector.hh` lines 289-291 and implemented at `ZoneTempPredictorCorrector.cc` lines 3817-3861. Its only production direct call is CP195 line 230, whose CorrectStep arm assigns the returned value to caller-owned `ZoneTempChange` only after CP206 returns. The ordinary HVAC parent runs initial Get, Predict, HVAC simulation, and then Correct. It selects adaptive downstepping only when the initial returned maximum exceeds `MaxZoneTempDiff` and `KickOffSimulation` is false; otherwise it uses one system step and Zone-timestep history. Each fine step repeats Predict, HVAC simulation, Correct, contaminant correction, and system-history push without recalculating the chosen count from later returns. Demand resimulation calls Get and Predict but not Correct. CP206 initializes its local maximum to zero and traverses Zones in ascending identity. For each Zone it first calls `ZoneSpaceHeatBalanceData::correctAirTemp` with the history selector and saves the result without folding it. It then visits every stored Space identity regardless of Space-HB flags. `doSpaceHeatBalanceSimulation && !DoingSizing` calls the Space correction child and immediately folds that result in Space order. Every other case may copy the controlled Zone system node Temp, HumRat, and Enthalpy to the Space node when `doSpaceHeatBalanceSizing && Zone.IsControlled`, a condition with no direct `DoingSizing` test, and then always copies Zone `ZT`, complete `ZTM`, `MAT`, `airHumRat`, and `airRelHum` into the Space record while deliberately omitting `ZTAVComf`. Only after all Spaces does CP206 fold the saved Zone result. It then always calls `CalcZoneComponentLoadSums` for the Zone, and when simulation Space HB is enabled it makes a second stored-Space traversal and calls that report child even during sizing when Space correction was bypassed. The history selector is only forwarded; shortening and prior-timestep inputs never enter CP206. The wrapper applies no absolute value, tolerance, or finite guard. Normal child returns are nonnegative absolute changes, but with the accumulator as the first `std::max` argument and every candidate second, negative and NaN candidates are ignored, positive infinity can be retained, and nonpositive Zone count returns zero. CP206 directly persists only the optional three Space-node values and five mirrored Space-HB values; correction, moisture, hybrid, and component-report mutations remain child behavior. It has no local assertion, count, identity, membership, arena, node, finite, or topology validation, diagnostic, stop flag, status, latch, catch, cleanup, transaction, or rollback. A Zone-child non-return suppresses that Zone remainder and later Zones. A Space-child failure retains the completed Zone child and earlier Spaces but prevents the Zone fold and reports. Node/mirror failure can retain a direct-write prefix, and report failure retains completed correction, copies, and earlier report effects. The local maximum is not externally visible on failure, and CP195 leaves the old caller value because its assignment has not completed. Same-state retry restarts at Zone one and repeats all children, copies, and reports; clean replay requires coordinated predictor/corrector, Zone/Space heat balance, node, topology, RoomAir, HVAC, report, diagnostic, and child-owner reset. One C++ HybridModel fixture makes five direct calls, all with Zone history true, one Zone, one stored Space, and false Space-HB flags. It therefore reaches Zone correction and five-field Space mirroring, but its five assertions check only hybrid child effects and never the returned maximum, mirror state, component reports, false-history path, failure, retry, or reset. Of 57 active full simulations, one expected EMS fatal stops before CP206, one zero-Zone weather case reaches the zero return, and 55 execute Zone correction. One Analytical configuration positively reaches active simulation-Space correction and Space reporting for two Zones and three Spaces, while seven sizing configurations reach the controlled node triplet and five-field mirror for one Zone and three Spaces each; their assertions are downstream topology or sizing evidence. One thermal-comfort simulation checks later Zone averages, but no test isolates CP206 maximum folding, adaptive step selection, component-report dispatch, failure, retry, or reset. Rust defines `correct_zone_air_temps_compat` only as a generic identity alias over `correct_step_source_order_path`. Its one non-test call and zero direct tests wrap a closure that returns unit, performs an all-Zone temperature pass followed by an all-Zone humidity pass, then optionally performs per-Zone adaptive correction or local history synchronization. It has no Space HB or node state, AirReportVars, component-report child, source history-selector input, or returned global maximum. The lower CorrectStep identity has additional calls during surface iteration and adaptive work that bypass the named CP206 wrapper. Rust chooses adaptive substeps independently per Zone from a hard-coded temperature threshold and repeats only local temperature/humidity work rather than the source global maximum and full Predict-HVAC-Correct retry. Existing coefficient, ThirdOrder, Analytical, history, storage-report, and bounded official 1ZoneUncontrolled evidence belongs to adjacent Zone-only child formulas and case-specific outputs, not CP206 topology or lifecycle parity. Authored Space execution, sizing, EMS, Python, and AirflowNetwork remain run-blocked. CP206 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 214 routines, split 58 `state_mapped` plus 156 `source_mapped`, with 91 required; the heat-balance project list becomes 60. CP207 adds required source-mapped `routine.zone_space_heat_balance_correct_air_temp` immediately after `routine.correct_zone_air_temps` and adds `zone_space_heat_balance_correct_air_temp` after `correct_zone_air_temps` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctAirTemp(EnergyPlusData &state, bool useZoneTimeStepHistory, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` lines 236-239 and implemented at `ZoneTempPredictorCorrector.cc` lines 3863-4165. Its only production direct call expressions are CP206 line 3825 for each Zone with default zero Space identity and line 3831 for positive active Spaces under `doSpaceHeatBalanceSimulation && !DoingSizing`; the Zone always precedes eligible Spaces. CP207 starts its local delta at zero, debug-asserts only positive `zoneNum`, reads the parent Zone multiplier product and system-timestep seconds, then copies complete `DSXMAT`/`DSWPrevZoneTS` histories when the selector is false or `XMAT`/`WPrevZoneTS` when true. Positive `spaceNum` alone selects Space volume; otherwise Zone volume. It overwrites `AirPowerCap` from that volume, parent sensible-capacitance multiplier, density at pre-correction `MAT` and `airHumRat`, specific heat, and unguarded timestep seconds. Exact zero alone calls `RoomAir::ManageAirModel`, then every identity calls `calcZoneOrSpaceSums` with the corrector flag true. `FlagHybridModel_PC` refreshes the current record's `SumIntGainExceptPeople` from the parent Zone without a Space guard. Positive Space alone selects its system node; a positive node, not `Zone.IsControlled`, selects the controlled branch. Controlled `TempDepCoef` is `SumHA + SumMCp + SumSysMCp`, while `TempIndCoef` adds internal and surface terms, non-system and system temperature terms, divided non-air response, lagged system load, and optional parent-Zone AFN sensible exchange and duct loss. The uncontrolled branch omits system flow, non-air response, and lagged system load but retains optional AFN and duct additions. Both branches dispatch unguarded ThirdOrder, Analytical, or Euler equations. Analytical tests exact zero dependence and caps its exponent with source-ordered `min(700, candidate)`; an unknown enum silently retains old `ZT` and continues. In the controlled node stage, a globally mixed or inactive model writes node temperature, Zone-only thermostat temperature, and correction factor one. Three-node displacement or UFAD has precedence and, like active UserDefined or one-node displacement, leaves node and thermostat untouched while conditionally computing the Zone-shared correction factor from supply temperature and existing node temperature; ordered `max(-3, raw)` then `min(3, value)` maps NaN raw to -3 and clamps infinities. AirflowNetwork replaces `ZT` from the parent control node and performs mixed writes; fallback also performs mixed writes. A Space uses its own node but parent RoomAir flags and can overwrite the Zone-shared correction factor after the Zone child, while never writing the thermostat. Controlled `SNLoad` uses `SumSysMCpT`, node mass flow and temperature divided by the Zone multiplier, record humidity specific heat, non-air response, and lagged load. The uncontrolled branch leaves zero load and shared node/control fields stale; only an exact uncontrolled Zone under global nonmixing and the AirflowNetwork enum replaces `ZT`, without testing `SimAirModel`. Exact Zone identity next conditionally calls `InverseModelTemperature` for enabled temperature inverse modes outside warmup and sizing. This occurs after node and load calculation, can replace `ZT` with measured temperature, and reads the global history flag rather than CP207's argument, so later `MAT`, humidity, RH, and delta can disagree with earlier node, thermostat, and load state. CP207 then writes `MAT = ZT`, reports sensible load to Space demand only for positive identity and otherwise Zone demand, calls the later-defined `correctHumRat` child, copies `airHumRatTemp` to `airHumRat`, and computes `airRelHum` through `PsyRhFnTdbWPb` without a caller-side clamp. Sensible demand commits before humidity. Returned change treats every Space as mixed. Exact Zone nonmixed classification recognizes only unmixed three-node displacement or UFAD flags. ThirdOrder compares `ZT` with `ZTM[0]` or the two room-air histories; Analytical and Euler compare with `T1` or the corresponding room-air one-step histories. Unknown enum returns zero. The outer `std::max` has zero first, so NaN is suppressed and positive infinity retained; in a nested nonmixed fold, NaN in the first child suppresses a finite second child while a finite first child survives a NaN second child. A malformed negative Space identity is accepted and combines Zone volume, node, and demand with skipped exact-zero RoomAir, thermostat, hybrid, AFN override, and nonmixed classification; production never supplies it. Direct record writes are the selected histories, capacitance, optional except-People sum, coefficients, `ZT`, `MAT`, humidity, and RH; direct shared writes are applicable node temperature, Zone thermostat, and Zone-shared correction factor. RoomAir, sum assembly, hybrid, sensible demand, humidity, and psychrometric diagnostics remain dependencies. Apart from the Zone assertion, CP207 has no local identity, bound, membership, arena, node, history, enum, topology, multiplier, volume, time, denominator, or finite validation, diagnostic, latch, status, catch, cleanup, transaction, or rollback. A failure can retain any ordered prefix through history, capacity, sums, coefficients, node/control, hybrid, `MAT`, sensible demand, humidity, or final RH; any non-return prevents CP206 from folding the delta. Same-state retry recomputes from already-mutated record, node, RoomAir, correction-factor, demand, and hybrid history state, and clean replay requires coordinated reset of all those owners plus AFN, duct, HVAC/environment, diagnostics, and children. No C++ test calls CP207 directly. One HybridModel fixture reaches its controlled, mixed, default-ThirdOrder Zone path five times with history true and asserts only hybrid child outputs. Helper-only tests call `correctHumRat` ten times and `calcZoneOrSpaceSums` five times but do not compose CP207. Of 57 active full simulations, one EMS fatal stops before it and one has zero Zones; the other 55 reach 81 statically declared Zone records, split 55 controlled and 26 uncontrolled. Forty-nine configurations select ThirdOrder for 74 records, six select Analytical for seven, and none selects Euler. One Analytical configuration reaches two uncontrolled Zones and three uncontrolled Spaces, but no controlled Space. One AFN-distribution and one duct-loss configuration reach their optional terms without asserting CP207 coefficients. No full simulation declares RoomAir or HybridModel input, and no focused assertion covers false history, solver output, node/control writes, sensible demand, RH, returned delta, failure, retry, or reset. Rust has no named `correct_air_temp` wrapper or direct CP207 test. Its all-Zone surface-driven temperature corrector has four non-test calls and no direct test; its adaptive single-Zone corrector has one non-test call and no direct test. The live Correct closure performs one all-Zone temperature pass, then a separate all-Zone humidity pass, then project-specific adaptive/history work instead of CP207's per-record transaction. The coefficient helper has five non-test calls and one direct fixture; the public ThirdOrder helper has no production call and one direct fixture, its coefficient solver has four non-test calls and no direct test, and the Analytical helper has three non-test calls and one direct fixture. There is no Euler helper. Rust preserves limited normal-finite Zone coefficient, ThirdOrder-history, and Analytical algebra, but combines system and non-system flow without a controlled-node decision; those runtime flow fields have no production writers and surface `SumHATref` is fixed zero. It omits exact sum assembly, non-air and lagged loads, AFN, duct, Space state, nodes, RoomAir, thermostat, correction factor, sensible demand, hybrid, distinct `ZT`/`MAT`/`T1`, RH, and returned delta. Rust capacity omits the sensible-capacitance multiplier and Space selection, can retain stale state on rejected weather/psychrometrics, zeroes AirPowerCap for nonpositive inputs, guards a near-zero ThirdOrder denominator, uses epsilon rather than exact Analytical zero, and has no source unknown-enum behavior. Its history selector is recorded but not consumed, and its separate saturation-clamped/defaulting humidity approximation is not `correctHumRat`. Existing formula tests and bounded official `1ZoneUncontrolled` MAT evidence remain adjacent case-specific evidence. Authored Space, sizing, EMS, Python, and AFN paths remain run-blocked. CP207 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 215 routines, split 58 `state_mapped` plus 157 `source_mapped`, with 92 required; the heat-balance project list becomes 61. CP208 adds required source-mapped `routine.push_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_correct_air_temp` and adds `push_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 293 and implemented at `ZoneTempPredictorCorrector.cc` lines 4167-4185. Its sole built-in direct call is CP195 line 236; the sole built-in selector request is `HVACManager::ManageHVAC` lines 579-584 after all system steps and Zone/Space comfort-average snapshots, and before the separate contaminant push, last-system-step-count commit, and demand-manager update. The dispatcher common input/init prefix runs first, but the CP208 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP208 aliases the predictor/corrector state, visits Zones 1 through `NumOfZones`, calls the Zone record child first, and only under the current aggregate `doSpaceHeatBalance` flag visits that Zone's stored `spaceIndexes` in container order and calls the Space child with both identities. It does not independently scan all Spaces, sort, deduplicate, or validate counts, arenas, membership, or identity consistency; a nonpositive Zone count is a no-op. Sizing can set the aggregate flag from `doSpaceHeatBalanceSizing` and normal simulation resets it from `doSpaceHeatBalanceSimulation`. CP208 itself reads traversal state but performs no direct history or numerical writes; all four-slot record, psychrometric, RoomAir, and AFN mutations belong to CP209. It has no local assertion, diagnostic, status, latch, catch, cleanup, transaction, or rollback. A child non-return retains every earlier child and the failing child's prefix, suppresses later traversal, and same-state retry shifts already-shifted histories again. No C++ unit calls CP208, its record child, `ManageZoneAirUpdates`, or `ManageHVAC` directly. Of 57 active `ManageSimulation` call expressions, one expected EMS fatal stops before CP208; the other 56 reach the wrapper, including one zero-Zone case, while the remaining 55 collectively span a static census of 81 Zone identities. Eight configurations collectively enable a static census of 24 Space identities, one in simulation and seven during sizing, but no assertion isolates the Zone/Space history shift, order, failure, retry, or reset. Rust has the selector name and identity source-order/compat wrappers, but production never passes the PushZone selector and the dispatcher ignores its selector. The one live compat call runs at the start of the Predict closure, shifts only three Zone temperature and humidity slots, chooses average versus current input by a project-specific adaptive flag, and encloses predictor work rather than running after the HVAC system loop. Rust has no Space heat-balance arena, membership traversal, fourth history slot, or CP209 RoomAir/AFN and auxiliary record state. One generic wrapper test checks only the wrapper call label and order in a larger vector; one uniform-temperature timestep assertion cannot distinguish slot movement, and there is no direct compat or nonuniform humidity-history test. Separate IdealLoads humidity history is not CP208 parity. CP208 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 216 routines, split 58 `state_mapped` plus 158 `source_mapped`, with 93 required; the heat-balance project list becomes 62. CP209 adds required source-mapped `routine.zone_space_heat_balance_push_zone_timestep_history` immediately after `routine.push_zone_timestep_histories` and adds `zone_space_heat_balance_push_zone_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushZoneTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 245 and implemented at `ZoneTempPredictorCorrector.cc` lines 4187-4275. Its only direct production expressions are CP208 lines 4178 and 4181, with every Zone first and positive eligible Spaces afterward. After a debug-only positive-Zone assertion, it aliases the parent AirModel before any mutation. It interleaves descending four-slot `XMAT` and `WPrevZoneTS` shifts, inserts `ZTAV` and `airHumRatAvg` at slot zero, writes `XMPT = ZT`, commits `airHumRat` and `WTimeMinusP` from `airHumRatTemp`, then writes percent `airRelHum` from `PsyRhFnTdbWPb` using `ZT`, the newly committed humidity, barometric pressure, and diagnostic label `pushTimestepHistories`. The helper floors only its local working humidity to 1e-5, can clamp a comparison-detected out-of-range RH, and can propagate NaN; CP209 stores the original humidity and applies no caller-side finite or range guard. Exact zero Space identity alone enables RoomAir. Three-node displacement and UFAD shift four-slot Floor, occupied, and mixed temperature histories and copy current temperatures to their MAT fields. AirflowNetwork shifts four-slot temperature and humidity histories for every stored node. These branches test the enum without an activity guard. Every solution enum except exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` and `WM2/WMX` from averages; exact-Zone stratified RoomAir advances its three M2/MX pairs and AFN advances per-node T2/TX temperature and humidity pairs. ThirdOrder still performs the common record and Zone RoomAir/AFN four-slot work. Any nonzero Space identity, including a malformed negative value, skips shared RoomAir while retaining common and non-ThirdOrder record writes. CP209 has no upper-bound, record-kind, membership, allocation, enum, topology, pressure, or finite validation and no local diagnostic beyond the psychrometric dependency, status, catch, cleanup, transaction, or rollback. An abnormal non-return after record mutation, for example from malformed psychrometric dependency or RoomAir/AFN indexing state, can leave the common prefix through `WTimeMinusP` with stale RH or retain later record and RoomAir prefixes. Retry shifts already shifted histories again, and the later revert routine is not a full inverse. No C++ test directly calls CP209 or asserts any destination. Fifty-five completing nonzero-Zone configurations collectively span 105 static record identities: 95 ThirdOrder and 10 Analytical, with no Euler. All 81 Zone identities use Mixing RoomAir; 24 Space identities skip RoomAir, so every special RoomAir/AFN branch has zero active full-simulation reach. Tracked official one-Zone outputs are downstream-only and do not isolate this commit. Rust has no singular record helper, call, or test. Its one live plural-wrapper closure shifts only three Zone temperature and humidity slots before predictor work, conditionally chooses average versus current input, and lacks Space, the fourth slot, `XMPT`, temporary/previous-system humidity scalars, RH, non-ThirdOrder record state, and all RoomAir/AFN histories. One uniform history assertion cannot identify the shift; separate system-step and IdealLoads histories are not CP209 parity. CP209 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 217 routines, split 58 `state_mapped` plus 159 `source_mapped`, with 94 required; the heat-balance project list becomes 63. CP210 adds required source-mapped `routine.push_system_timestep_histories` immediately after `routine.zone_space_heat_balance_push_zone_timestep_history` and adds `push_system_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushSystemTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 295 and implemented at `ZoneTempPredictorCorrector.cc` lines 4277-4295. Its only built-in direct call is CP195 lines 238-240; its only built-in selector request is `HVACManager::ManageHVAC` lines 388-393 inside each system iteration's strict `TimeStepSys < TimeStepZone` branch, after Predict/HVAC/Correct and before the separate contaminant push, `PreviousTimeStep` commit, and current-step average accumulation. Equal-step and kickoff paths do not request it. The dispatcher common prefix runs first, but the CP210 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP210 visits Zones in ascending order, calls each Zone child before stored eligible Spaces under the current aggregate `doSpaceHeatBalance` flag, and does not independently scan, sort, deduplicate, or validate topology. It performs no direct history write; CP211 owns downstepped record, RoomAir/AFN, and non-ThirdOrder mutations. It has no local assertion, diagnostic, status, catch, cleanup, transaction, or rollback. An abnormal child non-return retains completed and failing prefixes, suppresses later traversal and the parent's following contaminant/time/average work, and retry shifts completed system histories again; Zone-timestep revert does not restore them. No C++ test directly calls CP210 or its child, and no destination assertion or tracked oracle proves the numerical adaptive gate; 55 completing nonzero-Zone configurations provide only potential topology of 81 Zone and 24 eligible Space identities, not execution evidence. Rust has the selector name and generic wrappers, but production never dispatches the PushSystem selector. Production has exactly three lexical compat sites. The feature-disabled site is exclusive with the adaptive helper; when adaptive correction is enabled, each Zone independently takes either the count-one or count-greater-than-one site, so both adaptive sites can execute in one multi-Zone timestep. Exactly one site executes per Zone, rebuilding three slots even at a full Zone step or committing after all locally chosen adaptive substeps rather than performing the source per-fine-step global Zone/Space pass. Rust lacks Space topology, a fourth slot, and CP211 RoomAir/AFN and auxiliary state; the source-order wrapper has one direct label/order test, while the compat wrapper and PushSystem selector dispatch have none. One indirect count-one three-slot sync test does not prove CP210. CP210 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 218 routines, split 58 `state_mapped` plus 160 `source_mapped`, with 95 required; the heat-balance project list becomes 64. CP211 adds required source-mapped `routine.zone_space_heat_balance_push_system_timestep_history` immediately after `routine.push_system_timestep_histories` and adds `zone_space_heat_balance_push_system_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushSystemTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 247 and implemented at `ZoneTempPredictorCorrector.cc` lines 4297-4370. Its only production calls are CP210 lines 4288 and 4291 for each selected Zone and eligible stored Space. After a debug-only positive-Zone assertion, CP211 interleaves descending four-slot `DSXMAT` and `DSWPrevZoneTS` shifts and inserts current `MAT` and `airHumRat`. Exact Zone identity under the global nonmixing flag also advances applicable Floor/occupied/mixed four-slot temperature histories or each stored AirflowNetwork node's four-slot temperature and humidity histories. Every solution value other than exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` from `MAT` and `WM2/WMX` from distinct `airHumRatTemp`; exact Zones additionally advance applicable stratified or AirflowNetwork two-slot state. ThirdOrder skips only this final stage. CP211 has no upper-bound, Space-sign, membership, record-kind, topology, node-count, enum, or finite validation and no diagnostic, status, catch, cleanup, transaction, or rollback. A late abnormal non-return retains an ordered record or RoomAir/AFN prefix, blocks the rest of CP210 and its parent's following contaminant/time/average work, and same-state retry shifts completed histories again; the following Zone-timestep revert does not restore this downstepped state. No C++ test directly calls CP211 or asserts a destination. Fifty-five nonzero-Zone configurations provide only conditional potential topology if one CP210 pass is selected: 105 records split 95 ThirdOrder and 10 Analytical, with no Euler; all 81 Zones are Mixing and 24 Spaces skip shared RoomAir, so special RoomAir/AFN branches have zero corpus potential. Rust has no singular record helper, Space arena, fourth slot, `airHumRatTemp`, non-ThirdOrder scalar state, RoomAir, or AFN histories. Its count-one and feature-disabled helpers rebuild three Zone slots from current and Zone-timestep histories even when source CP211 is absent, while adaptive count-greater-than-one shifts local three-slot arrays after each Zone-local correction outside the named wrapper and commits them only once after all local substeps. One source-order label test and one indirect count-one rebuild test do not establish CP211; there is no adaptive-count-greater-than-one commit test. CP211 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 219 routines, split 58 `state_mapped` plus 161 `source_mapped`, with 96 required; the heat-balance project list becomes 65. CP212 adds required source-mapped `routine.revert_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_push_system_timestep_history` and adds `revert_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `RevertZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 297 and implemented at `ZoneTempPredictorCorrector.cc` lines 4372-4389. Its only direct thermal source call is the CP195 dispatcher arm at lines 232-234, while all nine built-in `ManageZoneAirUpdates` call sites and the test tree request other selectors; EnergyPlus 26.1 therefore has no built-in CP212 timing or runtime gate. An external dispatcher request would still run the common input/init prefix, ignore shortening, history-selector, and prior-step arguments, and preserve `ZoneTempChange`. CP212 visits Zones ascending, calls each Zone child first, then under the current aggregate `doSpaceHeatBalance` flag visits stored Spaces in container order. It does not scan, sort, deduplicate, validate topology, or write history directly; CP213 owns the `XMAT`/`WPrevZoneTS` and optional RoomAir/AFN mutations. It never restores CP211 `DSXMAT`/`DSWPrevZoneTS`, non-ThirdOrder scalars, or downstepped RoomAir/AFN state. CP212 has no assertion, diagnostic, status, completion count, catch, cleanup, transaction, or rollback. A child abnormal non-return retains completed and failing prefixes and suppresses later traversal; same-state retry reapplies CP213's forward copies and is destructive. No C++ test directly calls the wrapper or child or asserts a destination, and all 57 active `ManageSimulation` expressions execute CP212 zero times. The 81 Zone plus 24 eligible Space records are only counterfactual once-requested topology, not full-simulation reach evidence; special RoomAir/AFN topology remains absent. Rust has the selector label and one source-order/compat identity pair, but its dispatcher ignores the selector. Its sole live compat site runs per Zone only on the adaptive count-greater-than-one path and conditionally overwrites current mean temperature and humidity when the new count differs; it shifts no Zone history, has no Space traversal, and can run despite the source branch being dormant. The one source-order label test has no state assertion, and the only focused adaptive test takes count one and never reaches the site. CP212 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 220 routines, split 58 `state_mapped` plus 162 `source_mapped`, with 97 required; the heat-balance project list becomes 66. CP214 adds required source-mapped `routine.zone_space_heat_balance_correct_hum_rat` immediately after `routine.zone_space_heat_balance_revert_zone_timestep_history` and adds `zone_space_heat_balance_correct_hum_rat` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctHumRat(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 241 and implemented at `ZoneTempPredictorCorrector.cc` lines 4433-4619. Its sole production expression is CP207 line 4128, after temperature, `MAT`, and sensible reporting but before record humidity/RH commit and returned delta. Each correction pass visits every Zone and only active non-sizing simulation Spaces; initial and adaptive fine-step HVAC corrections can repeat it. CP214 selects controlled, return-plenum, supply-plenum, or zero primary flow, then independently adds positive parallel-PIU leakage. The literal PIU loop uses global ordinals one through the stored list size rather than the identities in `leakageParallelPIUNums`. It builds latent gain, density, vapor enthalpy, default moisture `A/B`, optionally replaces them with AFN multizone exchange, uses parent-Zone volume for `C` even on Space calls, then independently adds AFN distribution and duct latent terms. ThirdOrder, Analytical, and Euler equations write `airHumRatTemp`; an unmatched enum retains its prior value. Strict negative and saturation clamps run before a parent-Zone RoomAir AFN control-node override that also affects Spaces and is not reclamped. Exact-Zone hybrid inference can mutate measured humidity state without changing the temporary solution; a positive selected node receives humidity before enthalpy, and latent sizing reports raw latent gain with earlier sensible demand. Positive Spaces retain parent Zone equipment, multiplier, capacity, AFN, duct, radiant/pool, and RoomAir context; malformed negative Space identities use Zone node/demand state while skipping only the exact-Zone hybrid gate. CP214 has only a debug positive-Zone assertion and no bounds, membership, topology, allocation, multiplier, timestep, pressure, denominator, enum, or finite validation, status, catch, transaction, or rollback. Failure can retain solved humidity, hybrid, node, or demand prefixes after the parent `MAT` and sensible writes while blocking final record humidity/RH and later traversal; retry can resample schedules and repeat histories, reports, and diagnostics. Ten direct C++ calls plus five indirect focused calls cover only Euler node humidity and hybrid inference slices. Fifty-five nonzero-Zone corpus configurations provide a static one-pass census of 81 Zone plus three active Space records, split 74 ThirdOrder and ten Analytical with no Euler. Fifty-five records are controlled and 29 have no controlled primary flow; plenum/PIU topology is absent, while AFN replacement can reach five Zones, AFN distribution three, and duct latent addition three without an isolating assertion. Runtime cadence is larger and no assertion isolates the full transaction. Rust has no singular Zone/Space helper. Its main and adaptive heat-balance humidity paths use three-slot history only, directly write Zone humidity, enforce a `1e-5` floor and `0.008` invalid fallback, and omit source moisture coefficients and lifecycle effects. The separate `correct_no_oa_third_order_humidity_ratio_compat` is a guarded single-Zone IdealLoads subset for one purchased-air supply with exact ThirdOrder `A/B/C` and clamps, but omits all other branches, Space/node/RH/hybrid/sizing ownership, and source failure semantics. Existing case-bounded humidity claims remain unchanged. CP214 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 222 routines, split 58 `state_mapped` plus 164 `source_mapped`, with 99 required; the heat-balance project list becomes 68. CP215 adds required source-mapped `routine.down_interpolate_4_history_values` immediately after `routine.zone_space_heat_balance_correct_hum_rat` and adds `down_interpolate_4_history_values` before `update_final_surface_heat_balance` in the heat-balance project contract. The first scalar-output `DownInterpolate4HistoryValues` overload is declared at `ZoneTempPredictorCorrector.hh` lines 299-308 and implemented at `ZoneTempPredictorCorrector.cc` lines 4621-4702. It snapshots three inputs by value, writes five referenced outputs in order, tests strict ratio-two then ratio-three tolerance bands, and uses a sequential recurrence for every other ratio without validating timesteps, shortening direction, integer ratio, finite values, or aliasing. Its only production expressions are the CO2 then generic-contaminant calls in `PredictZoneContaminants`; each Zone reaches them only on the first shortened adaptive predictor when the system-step count changed and the matching species is enabled. One direct C++ ratio-two call asserts five outputs. Three contaminant predictor fixtures keep shortening false, and all 57 active full-simulation expressions have contaminant simulation disabled, so focused indirect and corpus reach are zero. Rust's nearest helper returns only the first three thermal Zone history values, rejects nonpositive timesteps, has two adaptive temperature/humidity call sites and ratio-two/three/four tests, and owns no contaminant state or five-output reference transaction. CP215 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required; the heat-balance project list becomes 69. CP216 expands that same logical routine mapping to the independent array-return `DownInterpolate4HistoryValues` overload at `ZoneTempPredictorCorrector.cc` lines 4704-4736. It computes the raw old/new timestep ratio before output, writes four array elements in strict ratio-two, then ratio-three, then fallback order, and returns `oldVals[0]` only after the writes. The fourth input is never read, the third is used only for the ratio-two final output, and no timestep, finite-value, shortening, integer-ratio, or input/output alias validation is performed. A same-array call exposes live-reference aliasing and makes retry generally non-idempotent, while stable distinct-array replay overwrites deterministically. Seven production expressions in `ZoneSpaceHeatBalanceData::updateTemperatures` provide two base temperature/humidity calls per eligible Zone or active Space, three exact-Zone displacement/UFAD calls, and two calls per exact-Zone AFN node. They require shortening and a system-step count different from the prior Zone timestep, so normal HVAC timing permits only the first shortened fine-step candidate; a matching count reuses prior downstepped arrays. One direct C++ ratio-two call asserts the return, all four outputs, and four unchanged inputs. Focused wrapper calls have zero Zones, while the 55 completing nonzero-Zone corpus configurations leave adaptive entry unobserved; their conditional one-pass topology is 81 Zones plus 24 eligible Spaces, or 210 base calls, with zero stratified/AFN potential because all Zones are Mixing. Rust's nearest helper returns only three values by value, rejects nonpositive timesteps, and has Zone-only adaptive temperature/humidity ownership without the fourth output, separate scalar return, Space/RoomAir/AFN topology, node rollback, or alias behavior. CP216 adds no second routine, project-contract item, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion; counts remain 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required, and the heat-balance project list remains 69. CP217 adds required source-mapped `routine.inverse_model_temperature` and project-contract item after `down_interpolate_4_history_values`. `InverseModelTemperature`, declared at `ZoneTempPredictorCorrector.hh` lines 313-325 and implemented at `ZoneTempPredictorCorrector.cc` lines 4737-4951, samples measured temperature and resets the current mass multiplier, conditionally overwrites `ZT`, runs independent infiltration, internal-mass, and people inverse branches under global Zone-history selection, then shifts three measured histories even outside the date window or during adaptive fine-step calls. Its sole exact-Zone production expression is inside `correctAirTemp`; five indirect focused calls assert only one multiplier, two infiltration rates, and two zero people counts, while all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed `HybridModel:Zone`, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP217 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 224 routines, split 58 `state_mapped` plus 166 `source_mapped`, with 101 required, and the heat-balance project list becomes 70. CP218 adds required source-mapped `routine.process_inverse_model_multp_hm` and project-contract item after `inverse_model_temperature`. `processInverseModelMultpHM`, declared at `ZoneTempPredictorCorrector.hh` lines 327-333 and implemented at `ZoneTempPredictorCorrector.cc` lines 4953-4991, clamps only values below one, warns but does not cap values above 30, includes every value above one in persistent sum/count/average state, and owns a per-Zone first/recurring diagnostic index. Its sole production call is CP217 internal-mass inference; one direct five-call fixture has 26 assertions and proves an uncapped 50 is aggregated, one indirect call asserts only current multiplier 15.13, and all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed HybridModel object, inferred multiplier, aggregates, recurring index, exact output/table, runtime path, or focused test, and the raw object remains run-blocking. CP218 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 225 routines, split 58 `state_mapped` plus 167 `source_mapped`, with 102 required, and the heat-balance project list becomes 71. CP219 adds required source-mapped `routine.inverse_model_humidity` and project-contract item `inverse_model_humidity` immediately after `process_inverse_model_multp_hm`. `InverseModelHumidity`, declared at `ZoneTempPredictorCorrector.hh` lines 335-343 and implemented at `ZoneTempPredictorCorrector.cc` lines 4993-5131, unconditionally samples measured humidity, conditionally runs infiltration and people inverse balances, and always shifts measured histories. Its sole exact-Zone production call is inside `correctHumRat`; four indirect fixture calls assert only two ACH and two people results, while all 57 active full simulations configure no HybridModel. Rust has no typed `HybridModel:Zone`, measured humidity history, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP219 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 226 routines, split 58 `state_mapped` plus 168 `source_mapped`, with 103 required, and the heat-balance project list becomes 72. CP220 adds required source-mapped `routine.zone_space_heat_balance_calc_zone_or_space_sums` and project-contract item `zone_space_heat_balance_calc_zone_or_space_sums` after `inverse_model_humidity`. `ZoneSpaceHeatBalanceData::calcZoneOrSpaceSums`, declared at `ZoneTempPredictorCorrector.hh` lines 226-230 and implemented at `ZoneTempPredictorCorrector.cc` lines 5133-5281, reconstructs internal, non-system, system, and virtual surface sums for predictor and correction passes. It uses parent-Zone AFN/equipment/plenum/PIU context for Space records, includes the literal PIU-ordinal anomaly, volume-scales only uncontrolled-Space system sums, and can repeat child Window side effects. One five-call C++ Zone fixture has 12 bounded assertions; the 55 nonzero-Zone corpus configurations provide static one-pass censuses of 105 prediction and 84 correction records but no controlled-Space, plenum, or PIU topology. Rust has only adjacent Zone opaque-surface and OtherEquipment subsets with zero-initialized flow fields, no matching routine or Space/airflow topology, and no promotion follows. The inventory becomes 32 algorithms and 227 routines, split 58 `state_mapped` plus 169 `source_mapped`, with 104 required; the heat-balance project list becomes 73. CP221 adds required source-mapped `routine.zone_heat_balance_calc_sum_hat` and project-contract item `zone_heat_balance_calc_sum_hat` after `zone_space_heat_balance_calc_zone_or_space_sums`. `ZoneHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 254 and implemented at `ZoneTempPredictorCorrector.cc` lines 5283-5298, debug-asserts Zone identity, zero-initializes four return fields, and folds every stored Space child in container order without gating, sorting, filtering, deduplication, or membership validation. It owns no persistent write, but child Window effects can survive failure and repeat on retry. One CP220 fixture reaches it five times and asserts six aggregate surface values. The 55 nonzero-Zone corpus configurations give static prediction and correction censuses of 81 Zone aggregates dispatching 99 stored children per pass. Rust directly folds Zone opaque-Surface indexes into only HA/HATsurf/HATref=0, with no four-field result, Space topology, child dispatch, or exact test; existing one-Zone output evidence does not promote this routine. The inventory becomes 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list becomes 74. CP222 expands the existing required source-mapped `routine.zone_heat_balance_calc_sum_hat` to the independent Space override without a second routine or project-contract item because both C++ definitions share the unqualified source identifier `calcSumHAT`. `SpaceHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 259 and implemented at `ZoneTempPredictorCorrector.cc` lines 5300-5413, debug-asserts positive identities, zero-initializes four fields, visits the selected Space inclusive Surface range without filtering, applies ordered Window gains/report mutations and frame/divider/base convection, and dispatches ZoneMean, Adjacent, ZoneSupply, or silent-default reference-air logic using parent-Zone supply state. Its uncontrolled ZoneSupply branch is fatal, and its airflow/no-return `SurfWinHeatGain +=` side effect makes replay and nested-plus-explicit duplicate visits generally non-idempotent. No direct test exists; one CP220 fixture reaches a one-Space, three-non-Window child five times and asserts six surface aggregates, while the static corpus census is 123 prediction and 102 correction calls, or 225 combined. Rust's nearest Zone-only opaque Surface fold returns only HA/HATsurf/HATref=0 and has no Space result, Window runtime, reference switch, or side effects; existing one-Zone evidence does not promote this routine. Counts remain 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list remains 74. CP223 adds required source-mapped `routine.calc_zone_component_load_sums` and project-contract item `calc_zone_component_load_sums` after `routine.zone_heat_balance_calc_sum_hat`. `CalcZoneComponentLoadSums`, declared at `ZoneTempPredictorCorrector.hh` lines 345-348 and implemented at `ZoneTempPredictorCorrector.cc` lines 5414-5677, is the correction-only component-report update sequence. It zeroes ten target report fields, combines Zone-wide internal gains and parent-Zone airflow/equipment/plenum/PIU/non-air topology with receiver-HB temperatures, coefficients, and storage state, re-walks every parent-Zone Space Surface range for every Zone or active simulation-Space report, accumulates Window convection and CondFD enthalpy, computes ThirdOrder, Analytical, or Euler air storage, and conditionally mutates a parent-Zone shared imbalance-warning index. Controlled inlet work can overwrite four ADU report fields, repeated Space reports can leave the last Space calculation on shared ADUs, and the literal PIU loop again uses global ordinals rather than stored identities. No direct C++ test exists; five HybridModel wrapper calls reach only a Zone report and assert unrelated hybrid state. The 55 nonzero-Zone corpus configurations give a static one-correction census of 81 Zone plus three Space reports, or 84 calls, with 104 stored-Space range walks; no full-simulation assertion isolates the transaction. Rust has separate Zone-only internal-gain, surface-convection, and air-storage report helpers plus a hard-coded zero outdoor-transfer series, but no ten-field record, Space report arena, complete airflow/equipment/Window/PCM/diagnostic transaction, or failure lifecycle. Existing four-row one-Zone conformance remains bounded output evidence and does not promote this routine. CP223 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 229 routines, split 58 `state_mapped` plus 171 `source_mapped`, with 106 required; the heat-balance project list becomes 75. CP224 adds required source-mapped `routine.verify_thermostat_in_zone` and project-contract item `verify_thermostat_in_zone` after `routine.calc_zone_component_load_sums`. `VerifyThermostatInZone`, declared at `ZoneTempPredictorCorrector.hh` line 350 and implemented at `ZoneTempPredictorCorrector.cc` lines 5679-5700, conditionally executes CP196 under the shared `GetZoneAirStatsInputFlag`, clears the latch only after normal return, gates lookup on positive `NumTempControlledZones`, then exact-string scans the full allocated `TempControlledZone.ZoneName` arena and returns only membership. Its sole production call is `SetUpZoneSizingArrays` for each record whose cooling or heating method is exactly `FromDDCalc` once any equipment configuration is controlled; a false result only makes the caller issue a non-pulse warning. CP224 itself owns no diagnostic or mutation after input acquisition. A CP196 non-return retains its partial state and the true latch, so same-state retry can re-enter the non-idempotent loader. No direct C++ test exists; one focused setup fixture reaches two false results without an isolating assertion. Thirty-four of 57 active full simulations provide 48 direct-Zone DesignDay sizing records with matching thermostats and equipment connections, hence a static first-setup census of 48 true calls, but no assertion isolates the predicate, lazy latch, exact-name mismatch, warning, failure, retry, or reset. Rust has bounded normalized direct-Zone DualSetpoint records, graph edges, an IdealLoads diagnostic, and plan metadata, but no exact helper, full CP196 arena or lazy input, executable `Sizing:Zone` caller, or source lookup and failure lifecycle. CP224 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 230 routines, split 58 `state_mapped` plus 172 `source_mapped`, with 107 required; the heat-balance project list becomes 76. CP225 adds required source-mapped `routine.verify_controlled_zone_for_thermostat` and project-contract item `verify_controlled_zone_for_thermostat` after `routine.verify_thermostat_in_zone`. `VerifyControlledZoneForThermostat`, declared at `ZoneTempPredictorCorrector.hh` line 352 and implemented at `ZoneTempPredictorCorrector.cc` lines 5702-5713, directly exact-string scans the full allocated `ZoneEquipConfig.ZoneName` arena and returns only positive membership. It performs no input load, normalization, `IsControlled`, equipment-list, Zone-identity, count, or Space check and owns no write or diagnostic; an exact default-sentinel or manually corrupted uncontrolled-slot match can therefore return true. Its only call expressions are CP199 ordinary then comfort control loops, each deferred until Zone-equipment input is filled and gated by a state-lifetime checked latch. False results produce caller-owned Severe/Continue diagnostics and sticky `ErrorsFound`; both loops finish before fatal, and only a normal input-filled return commits the latch. CP225 itself is read-only and deterministic, but caught parent retry repeats missing diagnostics and fatal because the error remains true, the checked latch remains false, and earlier CP199 effects persist. No direct C++ test exists; four direct CP199 fixture calls are gated off. Thirty-eight of 57 active full simulations provide 52 matching direct-Zone ordinary thermostat/equipment records, yielding 52 true calls, zero false calls, and zero comfort calls without an isolating assertion. Rust independently normalizes a raw nominal-controlled marker, direct-Zone DualSetpoint thermostats, and typed ZoneId equipment connections, plus an IdealLoads-only dispatch validator, but has no preserved-name helper, cross-family ordinary/comfort check, deferred latch, or source diagnostic/failure lifecycle. CP225 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 231 routines, split 58 `state_mapped` plus 173 `source_mapped`, with 108 required; the heat-balance project list becomes 77. CP226 adds required source-mapped `routine.detect_oscillating_zone_temp` and project item `detect_oscillating_zone_temp` after `routine.verify_controlled_zone_for_thermostat`. `DetectOscillatingZoneTemp`, declared at `ZoneTempPredictorCorrector.hh` line 354 and implemented at `ZoneTempPredictorCorrector.cc` lines 5715-5861, performs clear-on-success one-time allocation and registration, latches calculation from six output names or `PerformancePrecisionTradeoffs`, then on every enabled accepted system timestep shifts an independent zero-seeded four-slot Zone history and detects only strict alternating three-difference swings beyond `0.15 C`. It publishes Zone and Facility-any durations, separately classifies allocated ASH55 occupancy and unguarded deadband state, and accumulates three state-lifetime performance-log scalars without an environment reset. Setup and mid-loop failure are non-transactional, and duplicate enabled calls shift and can accumulate again. No C++ test references CP226 state or output. Fifty-six of 57 active full simulations reach setup and statically register 411 values across 81 nonzero-Zone records plus one zero-Zone configuration, but none requests the variables, monthly report, or performance tradeoff, so the calculation body has zero corpus reach. Rust has adjacent three-slot MAT history, adaptive `0.3 C` step selection, IdealLoads-only occupancy/deadband concepts, and hourly MAT/debug output, but no exact helper, four-slot detector, activation latch, six output-name families, annual/perflog state, HVAC-manager caller, or focused test. CP226 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 232 routines, split 58 `state_mapped` plus 174 `source_mapped`, with 109 required; the heat-balance project list becomes 78. CP227 adds required source-mapped `routine.adjust_air_set_points_for_op_temp_cntrl` and project item `adjust_air_set_points_for_op_temp_cntrl` after `routine.detect_oscillating_zone_temp`. `AdjustAirSetPointsforOpTempCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 356 and implemented at `ZoneTempPredictorCorrector.cc` lines 5863-5897, returns first on a false global operative-control flag and then only on exact per-record None; otherwise it selects a scheduled or fixed MRT fraction, copies the supplied Zone MRT, and overwrites the referenced target with `(setpoint - f * MRT) / (1 - f)`. Normal input validates `f` in `[0.0, 0.9)`, but the routine has no identity, pointer, denominator, finite, or range checks; untargeted records default to Invalid/fixed zero rather than None once another Zone raises the global flag. Five parent call expressions cover one-call single modes and cooling-then-heating Dual mode before later optimum, humidity, fault, comfort, and EMS effects. Built-in execution occurs once per Zone timestep before system substeps, while demand resimulation can add passes; full-parent replay reloads raw schedules, but a direct duplicate routine call generally double-transforms. No direct C++ test exists. Twenty-one direct parent calls produce 46 global-off entries, and the 57-expression full corpus contains no operative object: its static 101 active-sweep entry opportunities all return before record, MRT, or formula work. Rust has bounded DualSetpoint graph and raw-schedule IdealLoads evidence but no typed operative object, global/per-record control state, fraction binding, Zone MRT, exact helper, live caller, or focused test; the raw operative object run-blocks. CP227 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 233 routines, split 58 `state_mapped` plus 175 `source_mapped`, with 110 required; the heat-balance project list becomes 79. CP228 adds required source-mapped `routine.adjust_operative_set_points_for_adap_comfort` and project item `adjust_operative_set_points_for_adap_comfort` after `routine.adjust_air_set_points_for_op_temp_cntrl`. `AdjustOperativeSetPointsforAdapComfort`, declared at `ZoneTempPredictorCorrector.hh` line 358 and implemented at `ZoneTempPredictorCorrector.cc` lines 5899-5964, consumes one of seven adaptive daily arrays outside DesignDay/HVACSizeDesignDay or a shared summer-design-day slot for those two environment kinds, then applies the source integer-truncated baseline and exact `-1` fallback before CP227 converts the operative target to an air setpoint. Three guarded parent expressions cover SingleCool, SingleHeatCool, and Dual high only and snapshot the pre-CP227 operative result. Four direct C++ calls prove two central-model values and two lower-than-baseline sentinel restorations without isolating the later exact-sentinel branch, while 21 parent calls and all 57 active full simulations yield zero production entries. Rust has no typed operative object, adaptive state, selector, environment branch, output, live caller, or test, and the raw object run-blocks. CP228 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 234 routines, split 58 `state_mapped` plus 176 `source_mapped`, with 111 required, and the heat-balance project list becomes 80. CP229 adds required source-mapped `routine.calc_zone_air_comfort_set_points` and project-contract item `calc_zone_air_comfort_set_points` after `adjust_operative_set_points_for_adap_comfort`. `CalcZoneAirComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` line 360 and implemented at `ZoneTempPredictorCorrector.cc` lines 5966-6329, performs first-use ThermalComfort initialization, resets and samples comfort control types, selects PMV schedules, executes NO/SPE/OBJ/PEO People dispatch through the next child, clamps and overwrites ordinary dry-bulb setpoints, and leaves EMS with final precedence. Function-scope object and occupant counts bias later Zones, OBJ/PEO SingleCool use the low `-999` sentinel, child endpoint equality can retain stale outputs, and warnings plus partial Fanger/People/setpoint writes survive retry. No direct C++ call or active full-simulation expression reaches it; two raw setup fixtures do not call the parent, and the one installed comfort ExampleFile is not adopted by the repository. Rust has no typed comfort thermostat/setpoint, PMV/Fanger state, inverse child, exact outputs, live caller, or test, and active raw comfort input run-blocks. CP229 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 235 routines, split 58 `state_mapped` plus 177 `source_mapped`, with 112 required, and the heat-balance project list becomes 81. CP230 adds required source-mapped `routine.get_comfort_set_points` and project-contract item `get_comfort_set_points` after `calc_zone_air_comfort_set_points`. `GetComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` lines 362-367 and implemented at `ZoneTempPredictorCorrector.cc` lines 6331-6415, always evaluates the impure forward Fanger model at the stored minimum then maximum dry-bulb bounds. Only a strict interior PMV target enters configurable global `SolveRoot` with residual tolerance 0.001 and limit 500; below/above targets select a bound; with ordered or equal endpoint PMVs exact equality preserves the referenced output, while reversed equality can select the opposite temperature bound, and a NaN target or any case making all three comparisons false also preserves the output. The solver repeats both endpoints, can evaluate 501 candidates because convergence precedes the strict limit check, returns the last evaluated candidate on an iterated `-1` but Tmin on a zero-candidate width `-1` or `-2`, and yields two, four, or five-through-505 total Fanger calls by path. Each trial can resample People schedules and room-air state, overwrite ThermalComfort scratch plus People/Fanger report state, and repeat child diagnostics; lower clamp and `-2` can return Tmin while the final report state reflects Tmax. SurfaceWeighted MRT first-use can mutate Surface `AE`/`enclAESum` and its latches, and a bad sum returns a MAT/surface average on the first trial but zero later, providing a concrete stateful path from the initial bracket to `-2`. The two solver warning count/index pairs are shared across all comfort Zones and People, persist across environments, and are suppressed during warmup even though calculation and child effects continue. All 12 production expressions are inside CP229, with one/two NO-SPE calls, N/2N OBJ or positive-PEO calls, and 2N/4N nonpositive-PEO-plus-fallback calls. There is no status, transaction, rollback, or latch, so partial child/output/diagnostic state survives failure and retries repeat effects. No unit test reaches CP230, all 21 direct parent calls skip comfort, and the 57 active full simulations contain no comfort input. Separate forward-Fanger and generic-root tests do not compose this inverse. A manual stock-26.1 ExampleFile run supplies only uninstrumented diagnostic evidence and is not adopted as a repository case. Rust has no comfort type, PMV/Fanger state, generic configurable root solver, diagnostics, live caller, or composed test; RawOnly comfort input still run-blocks, and an unrelated private IdealLoads bisection does not promote the boundary. CP230 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 236 routines, split 58 `state_mapped` plus 178 `source_mapped`, with 113 required, and the heat-balance project list becomes 82. CP231 adds required source-mapped `routine.adjust_cooling_set_point_for_temp_and_humidity_control` and project item `adjust_cooling_set_point_for_temp_and_humidity_control` after `get_comfort_set_points`. `AdjustCoolingSetPointforTempAndHumidityControl`, declared at `ZoneTempPredictorCorrector.hh` lines 369-372 and implemented at `ZoneTempPredictorCorrector.cc` lines 6417-6458, aliases both indexed records before its global and exact-None guards, chooses a scheduled or constant maximum range, caps only for a strictly positive high-low gap, samples Zone RH and an independent dehumidifying schedule, and under strict positive excess/ratio gates subtracts only from `setptHi`. Default Invalid records do not return; the object-count global flag plus null untargeted schedules creates a mixed-record failure dependency, while zero/reversed gaps, ObjexxFCL second-argument-on-unordered `min`, malformed values, and direct duplicate calls retain literal nonfinite, deadband, and non-idempotent behavior. The two SingleCool/Dual parent expressions run after adaptive/operative and optional optimum-start work and before fault, comfort, and final EMS overrides; SingleCool load still consumes the untouched scalar, and a later positive cutout path can rebuild high from the earlier raw snapshot. No direct C++ test exists; 20 indirect unit entries and all active full-corpus entries return at the false global guard because no exact modifier input is present. The sole installed exact ExampleFile is an upstream Constant-path simulation test, but this repository does not adopt it and the case neither requests the adjusted cooling-setpoint output nor directly asserts CP231. Rust has only adjacent DualSetpoint, Humidistat, psychrometric, and IdealLoads moisture paths, with no typed modifier, percent-RH/range/ratio state, mutable setpoint triple, exact helper, live caller, or test; the raw object run-blocks. CP231 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 237 routines, split 58 `state_mapped` plus 179 `source_mapped`, with 114 required; the heat-balance project list becomes 83. CP232 adds required source-mapped `routine.override_air_set_points_for_ems_cntrl` and project item `override_air_set_points_for_ems_cntrl` after `adjust_cooling_set_point_for_temp_and_humidity_control`. `OverrideAirSetPointsforEMSCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 374 and implemented at `ZoneTempPredictorCorrector.cc` lines 6460-6555, traverses every ordinary record then every comfort record, with heating before cooling. Single modes write scalar plus one bound, SingleHeatCool leaves scalar at cooling when both flags are active, and Dual writes only low/high; dispatch uses live per-Zone types, so record collisions and the later comfort loop resolve field-by-field without validation. Ordinary actuators bind under `Zone Temperature Control` with `[C]`, while comfort actuators bind under `Zone Comfort Control` with `[]` yet are copied directly into Celsius-backed fields. The unconditional parent-tail call follows ordinary modifiers, fault, and comfort processing, while later staged or positive-cutout prediction can replace fields from other/raw state. One direct unit test proves only ordinary and comfort Dual low/high writes; all indirect unit and active full-corpus visits retain false flags, and installed testfiles provide no exact actuator-active ExampleFile. Rust has no EMS engine, actuator state, comfort type, mutable setpoint triple, helper, or live caller, and all EMS input run-blocks. CP232 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 238 routines, split 58 `state_mapped` plus 180 `source_mapped`, with 115 required; the heat-balance project list becomes 84. CP233 adds required source-mapped `routine.fill_predefined_table_on_thermostat_setpoints` and project item `fill_predefined_table_on_thermostat_setpoints` after `override_air_set_points_for_ems_cntrl`. `FillPredefinedTableOnThermostatSetpoints`, declared at `ZoneTempPredictorCorrector.hh` line 376 and implemented at `ZoneTempPredictorCorrector.cc` lines 6558-6672, visits all authored SingleHeating, SingleCooling, SingleHeatingOrCooling, then DualSetpoint definitions and lets the first occurrence of each numeric Schedule ID own its winter, summer, or split synthetic rows in the LEED temperature-schedule table. Detailed schedules sample the first Wednesday at hours 11 and 23 with one selected-date DST shift and count exact matching Wednesday-profile values across 365/366 calendar-day rules; constants return final `currentVal` and every day. Every cell appends without a transaction or once latch; valid parsed names avoid case-sensitive base/synthetic collisions, while manual mixed-case state can still collide, and retry can diverge between last-wins rendering and first-wins retrieval. The sole end-simulation tabular caller runs even when table files are disabled. No C++ test calls CP233; 56 successful full simulations give unasserted static reach, while a helper-only test makes nine calls and 21 assertions. Rust has only adjacent DualSetpoint graph state, calendar-aware schedule series, and constant-schedule IdealLoads diagnostics; it has no four-family arena, source ID deduplication, seasonal query, predefined table store, exact helper, caller, or composed test, and summary-report input stays ignored. CP233 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 239 routines, split 58 `state_mapped` plus 181 `source_mapped`, with 116 required; the heat-balance project list becomes 85. CP234 adds required source-mapped `routine.fill_predefined_table_on_thermostat_schedules` and project item `fill_predefined_table_on_thermostat_schedules` after CP233. `FillPredefinedTableOnThermostatSchedules`, declared at `ZoneTempPredictorCorrector.hh` line 378 and implemented at `ZoneTempPredictorCorrector.cc` lines 6674-6766, visits materialized ordinary TempControlledZone records in stored order, always appends thermostat/control-schedule plus independently joined control-type/name cells, and conditionally appends heating/cooling schedule cells. Nonempty control-object names alone select fixed type slots; tuple-lexical sorting and per-column empty filtering can break positional alignment. The five-slot resize followed by move-append relies on valid-unspecified moved-from strings being empty in observed libraries. Append-only predefined entries have partial-failure, duplicate-retry, last-wins-render versus first-wins-retrieval, and separate reset behavior. One direct call has 24 assertions only for one type per Zone; 56 successful full simulations provide unasserted static reach across 52 records and 312 cells. Rust has only adjacent direct-Zone DualSetpoint graph state and no complete control arena, exact sort/joins, predefined System Summary store, caller, serializer, or comparator; summary-report input remains ignored. CP234 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 240 routines, split 58 `state_mapped` plus 182 `source_mapped`, with 117 required; the heat-balance project list becomes 86. CP235 next maps `ZoneSpaceHeatBalanceData::updateTemperatures`, declared at `ZoneTempPredictorCorrector.hh` lines 233-234 and implemented at `ZoneTempPredictorCorrector.cc` lines 6768-6833."
routine.manage_air_heat_balance.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.manage_air_heat_balance.source_routine = "ManageAirHeatBalance"
routine.manage_air_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.manage_air_heat_balance.completion_status = "source_mapped"
routine.manage_air_heat_balance.required_for_full_domain = true
routine.get_air_heat_balance_input.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.get_air_heat_balance_input.source_routine = "GetAirHeatBalanceInput"
routine.get_air_heat_balance_input.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_air_heat_balance_input.completion_status = "source_mapped"
routine.get_air_heat_balance_input.required_for_full_domain = true
routine.get_air_flow_flag.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.get_air_flow_flag.source_routine = "GetAirFlowFlag"
routine.get_air_flow_flag.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_air_flow_flag.completion_status = "source_mapped"
routine.get_air_flow_flag.required_for_full_domain = true
routine.set_zone_mass_conservation_flag.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.set_zone_mass_conservation_flag.source_routine = "SetZoneMassConservationFlag"
routine.set_zone_mass_conservation_flag.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.set_zone_mass_conservation_flag.completion_status = "source_mapped"
routine.set_zone_mass_conservation_flag.required_for_full_domain = true
routine.get_simple_air_model_inputs.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.get_simple_air_model_inputs.source_routine = "GetSimpleAirModelInputs"
routine.get_simple_air_model_inputs.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_simple_air_model_inputs.completion_status = "source_mapped"
routine.get_simple_air_model_inputs.required_for_full_domain = true
routine.get_room_air_model_parameters.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.get_room_air_model_parameters.source_routine = "GetRoomAirModelParameters"
routine.get_room_air_model_parameters.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.get_room_air_model_parameters.completion_status = "source_mapped"
routine.get_room_air_model_parameters.required_for_full_domain = true
routine.init_air_heat_balance.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.init_air_heat_balance.source_routine = "InitAirHeatBalance"
routine.init_air_heat_balance.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_air_heat_balance.completion_status = "source_mapped"
routine.init_air_heat_balance.required_for_full_domain = true
routine.init_simple_mixing_convective_heat_gains.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.init_simple_mixing_convective_heat_gains.source_routine = "InitSimpleMixingConvectiveHeatGains"
routine.init_simple_mixing_convective_heat_gains.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.init_simple_mixing_convective_heat_gains.completion_status = "source_mapped"
routine.init_simple_mixing_convective_heat_gains.required_for_full_domain = true
routine.calc_heat_balance_air.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.calc_heat_balance_air.source_routine = "CalcHeatBalanceAir"
routine.calc_heat_balance_air.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_heat_balance_air.completion_status = "source_mapped"
routine.calc_heat_balance_air.required_for_full_domain = true
routine.initialize_for_external_hvac_manager.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.initialize_for_external_hvac_manager.source_routine = "initializeForExternalHVACManager"
routine.initialize_for_external_hvac_manager.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.initialize_for_external_hvac_manager.completion_status = "source_mapped"
routine.initialize_for_external_hvac_manager.required_for_full_domain = false
routine.report_zone_mean_air_temp.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.report_zone_mean_air_temp.source_routine = "ReportZoneMeanAirTemp"
routine.report_zone_mean_air_temp.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.report_zone_mean_air_temp.completion_status = "source_mapped"
routine.report_zone_mean_air_temp.required_for_full_domain = true
routine.calc_mean_air_temps.source_file = "src/EnergyPlus/HeatBalanceAirManager.cc"
routine.calc_mean_air_temps.source_routine = "calcMeanAirTemps"
routine.calc_mean_air_temps.source_map = "docs/src/porting-map/heat-balance-source-map.md"
routine.calc_mean_air_temps.completion_status = "source_mapped"
routine.calc_mean_air_temps.required_for_full_domain = true

[[algorithm]]
id = "zone_temp_predictor_corrector_source_order"
domain = "heat_balance"
source_map = "docs/src/porting-map/zone-air-update-map.md"
energyplus_source = [
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/zone_predictor_corrector.rs::manage_zone_air_updates_stage",
]
status = "scaffold"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Zone Mean Air Temperature",
]
claim_level = "none"
support_boundary = "CP195 keeps the algorithm a scaffold and the existing required ManageZoneAirUpdates routine source-mapped only: its conditional input latch, unconditional InitZoneAirSetPoints call, six-way dispatch, caller-owned ZoneTempChange semantics, production caller orders, partial effects, retry, and coordinated reset remain source-only. Rust's selector-ignoring identity closure plus bounded MAT, coefficient, correction, history, and execution-plan evidence do not implement the dispatcher or widen conformance. CP196 adds required source-mapped `routine.get_zone_air_set_points` immediately after `routine.manage_zone_air_updates` in the predictor/corrector definition ledger and adds `get_zone_air_set_points` after `manage_zone_air_updates` before `update_final_surface_heat_balance` in the heat-balance project contract. `GetZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 272 and implemented at `ZoneTempPredictorCorrector.cc` lines 246-2174 with no explicit return or internal latch. It executes ordinary thermostat and four setpoint families, humidistat, thermal-comfort and four Fanger families, unconditional HybridModel input, Zone capacitance assignment and EIO, operative/adaptive comfort, temperature-and-humidity overcool, and staged-dual input before one accumulated-error fatal. It expands ZoneLists, allocates and links control and schedule state, mutates Zone indices and multipliers, registers operative-temperature outputs, preserves warning and severe paths that do not set the local fatal flag, and can exit earlier through Hybrid or adaptive helper fatals. The staged precheck reads the routine-wide error flag, the capacitance ZoneList loop omits its final member, and no rollback or re-entry guard exists. Three production call sites share `GetZoneAirStatsInputFlag` and clear it only after return; a non-return retains partial state and the true latch, so same-state retry is unsafe. Nine direct C++ unit calls cover selected valid fields and two fatal diagnostics but not the complete family order, shared latch, valid staged, overcool, ZoneList, or capacitance-output paths, partial failure, retry, or reset. Rust types only direct-Zone DualSetpoint thermostat and humidistat subsets and exposes an identity `get_zone_air_set_points_compat` closure; it does not implement the complete 16-family transaction, ZoneList expansion, diagnostic and output order, latch, partial effects, or owner lifecycle. CP196 adds no new EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 204 routines, split 58 `state_mapped` plus 146 `source_mapped`, with 83 required; the heat-balance project list becomes 52. CP197 adds non-required source-mapped `routine.calculate_monthly_running_average_dry_bulb` immediately after `routine.get_zone_air_set_points`; the heat-balance project contract remains unchanged. `CalculateMonthlyRunningAverageDryBulb(EnergyPlusData &state, Array1D<Real64> &runningAverageASH, Array1D<Real64> &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` line 284 and implemented at `ZoneTempPredictorCorrector.cc` lines 2176-2275. Its only two production calls are CP196 operative-temperature branches at lines 1661 and 1731, each under the shared adaptive initialized guard and immediately before CP198; callers allocate fresh zeroed `NumDaysInYear` arrays, while CP197 neither reads nor sets the guard and only CP198 sets it true. CP197 checks and opens the weather path, discards nine lines, blindly reads `NumDaysInYear * 24` records, strips six comma fields, and averages field 7 into positional days before its ASH and CEN windows. The canonical eight-header mismatch shifts daily buckets by one hour; EOF and conversion status are ignored, `StrToReal` silently supplies -99999 on invalid text, and the corrupted final bucket is never consumed. The regular ASH path sums 31 inclusive days and divides by 30, the regular CEN path sums 8 and divides by 7, both wrap paths exclude `dailyDryTemp(N)`, and each caller cell self-accumulates as `x = x + avgDryBulb` without local clearing before `/=`. Missing or unreadable weather fatals before output writes, while malformed readable input can flow into CP198 and become latched. No C++ unit test executes CP197; the adaptive test calls CP198 with synthetic arrays. Rust has a typed EPW parser, while its legacy dry-bulb loader skips eight headers and reports numeric conversion errors; neither provides CP197 averaging, adaptive state, or an operative-control path. CP197 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 205 routines, split 58 `state_mapped` plus 147 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP198 adds non-required source-mapped `routine.calculate_adaptive_comfort_set_point_schl` immediately after `routine.calculate_monthly_running_average_dry_bulb`; the heat-balance project contract remains unchanged. `CalculateAdaptiveComfortSetPointSchl(EnergyPlusData &state, Array1D<Real64> const &runningAverageASH, Array1D<Real64> const &runningAverageCEN)` is declared at `ZoneTempPredictorCorrector.hh` lines 286-287 and implemented at `ZoneTempPredictorCorrector.cc` lines 2277-2348. Its only two production calls are CP196 lines 1662 and 1732, immediately after CP197 and under the same external false-initialized guard; CP198 has no local guard. It scans all stored design days, uses only day type 9, derives `MaxDryBulb - DailyDBRange / 2`, and conditionally writes shared ASH slots 0-2 for strict `(10, 33.5)` and CEN slots 3-6 for strict `(10, 30)`. Later qualifying design days overwrite each family independently, while nonqualifying values retain earlier/default slots; the C++ declaration `std::array<Real64, 7> = {-1}` initializes the vector as `[-1, 0, 0, 0, 0, 0, 0]`. It then allocates seven `NumDaysInYear` arrays and overwrites every day with three `0.31 * T + offset` ASH values or `-1`, followed by four `0.33 * T + offset` CEN values or `-1`. Exact bounds and nonfinite daily inputs take the `-1` branches. Input shape is trusted, the const input arrays are untouched, and the shared `initialized` flag is set only after all design, allocation, and daily work returns. There is no status, diagnostic, catch, cleanup, or rollback; abnormal non-return can retain design writes, an allocation/day prefix, and the prior flag, while complete direct re-entry runs even when the flag is already true. A later CP196 fatal retains successfully committed schedules and causes same-state input retry to skip both adaptive helpers; only full predictor/corrector owner reconstruction resets them. One C++ fixture calls CP198 directly three times with uniform 0, 40, and 25 arrays, asserting day 1, all seven daily formulas for 25, the flag, and only two design slots; strict bounds, nonfinite and shaped inputs, remaining days, multiple/default design days, partial failure, guarded integration, retry, and reset remain uncovered. Rust has no operative-temperature control object, adaptive model/formulas, schedule state, or consumer; DualSetpoint thermostat parsing and execution-plan labels are not parity. CP198 adds no source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 206 routines, split 58 `state_mapped` plus 148 `source_mapped`, with 83 required; the heat-balance project list remains 52. CP199 adds required source-mapped `routine.init_zone_air_set_points` immediately after `routine.calculate_adaptive_comfort_set_point_schl` and adds `init_zone_air_set_points` after `get_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `InitZoneAirSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 274 and implemented at `ZoneTempPredictorCorrector.cc` lines 2350-2816. CP195 line 220 calls it on every selector after optional CP196 input and before dispatch; CP192 line 4612 also calls it directly for external-HVAC initialization without reproducing that input prefix. The default-true one-time block sizes thermostat setpoints, load correction, control/report, optional comfort/Fanger, setback/deadband, ZoneList/ZoneGroup load, six hybrid-history, Zone demand, and simulation-or-sizing Space demand state. It warning-checks each Zone's heat-transfer-surface reference-air consistency, registers Zone and simulation-Space heat-balance and sensible/moisture demand bundles, six direct thermostat/correction outputs, optional comfort outputs, and ZoneList/ZoneGroup outputs, and clears its one-time latch only after that complete prefix. A begin-environment gate then calls CP200 for every Zone and active Space, zeros four current thermostat setpoints, sets load correction to one and temperature control to Uncontrolled, resets Zone and active-Space demand helpers, `DeadBandOrSetback`, `NoHeatToReturnAir`, and hybrid histories, and clears `MyEnvrnFlag` only at its tail. It intentionally does not directly reset averaged setpoints, temperature-control report, comfort control/report/Fanger state, `Setback`, `CurDeadBandOrSetback`, or ZoneList/ZoneGroup totals; seeing `BeginEnvrnFlag` false rearms the gate. The begin-day branches only toggle `MyDayFlag`. Every call then scans ordinary temperature controls followed by comfort controls. Once Zone-equipment input is filled and before `ControlledZonesChecked`, missing Zone configuration emits Severe plus Continue diagnostics and sets the routine-owned sticky `ErrorsFound`. Independent demand-management branches clamp ordinary controls with strict `>`/`<` comparisons and comfort controls with inclusive `>=`/`<=` comparisons; selected single heat/cool and single-heat-cool comfort paths also rewrite the ordinary control type/report, and the later comfort loop can overwrite ordinary-loop state. The fatal check occurs after both loops and their reached clamp mutations; only a normally reached final tail sets `ControlledZonesChecked = true`. No entry clears `ErrorsFound` and no status, catch, cleanup, rollback, shape reconciliation, or transaction exists. A non-return can retain an allocation/output, environment-reset, diagnostic, or clamp prefix according to the already committed latches; same-state error retry can repeat validation diagnostics and fatal, while clean replay requires coordinated predictor/corrector, HeatBalFanSys, ZoneEnergyDemand, HeatBalance, ZoneControls, ZoneEquipment, Surface, environment, and OutputProcessor reset. Four direct C++ unit calls use CP199 only as setup with begin-environment, comfort, demand, and validation behavior inactive and assert none of its state; focused boundary, warning, output-bundle, partial-failure, retry, and reset coverage is absent. Rust's `init_zone_air_set_points_compat` is an identity closure used only in the Predict scaffold and omitted from its Correct shell. Its separate constant-DualSetpoint heating/cooling observed series and limited `ZoneSysEnergyDemand` snapshots implement neither CP199 allocation, output binding, lifecycle, validation, demand clamps, nor selector-independent topology. CP199 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 207 routines, split 58 `state_mapped` plus 149 `source_mapped`, with 84 required; the heat-balance project list becomes 53. CP200 adds required source-mapped `routine.zone_space_heat_balance_begin_environment_init` immediately after `routine.init_zone_air_set_points` and adds `zone_space_heat_balance_begin_environment_init` after `init_zone_air_set_points` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::beginEnvironmentInit(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 213 and implemented at `ZoneTempPredictorCorrector.cc` lines 2818-2836. Its only two direct call expressions are CP199 lines 2624 and 2628 inside `MyEnvrnFlag && BeginEnvrnFlag`: every stored Zone record runs first, then every stored Space record only when current `doSpaceHeatBalance` is true. The helper owns no identity, count, gate, or latch. For each fixed index 0 through 3 it writes `ZTM[i] = 0`, `WPrevZoneTS[i] = OutHumRat`, `DSWPrevZoneTS[i] = OutHumRat`, and `WPrevZoneTSTemp[i] = 0` in that order; it then writes `WTimeMinusP`, `W1`, `WMX`, and `WM2` from `OutHumRat` before zeroing `airHumRatTemp`, `tempIndLoad`, `tempDepLoad`, `airRelHum`, `AirPowerCap`, and `T1`. These are 26 overwrites: 12 outdoor-humidity values and 14 zeros. CP200 reads no prior target value and leaves current/average air humidity, MAT/MRT/ZT and their other histories, coefficients outside the two load scalars, and every unlisted record field unchanged. All four indexed targets are fixed `std::array<Real64, 4>` values. Outdoor humidity is copied without finite, sign, range, or consistency validation, so negative, infinite, and quiet-NaN values propagate to all 12 humidity targets without a clamp or diagnostic. There is no allocation, checked access, status, diagnostic, child call, catch, cleanup, or rollback, and valid state has no ordinary catchable failure path. Complete repeat with unchanged outdoor humidity is overwrite-idempotent; a changed value replaces all 12 humidity targets. CP199 alone controls frequency and clears its environment latch after the entire parent reset, so a Space mode that becomes true later in the same uninterrupted BeginEnvironment interval does not replay skipped Space records. No C++ unit test calls CP200 directly; the four direct CP199 fixtures have `BeginEnvrnFlag` false. Of 56 active full-simulation tests that reach CP199, 55 execute at least one Zone CP200 call; `WeatherManager_SetRainFlag` has zero Zones and executes none. Seven `SizingManager` tests also reach sizing-Space and one `HeatBalanceAirManager` test reaches simulation-Space, but none asserts CP200 fields. Rust creates only Zone state at run initialization with configurable three-slot temperature histories and fixed-default three-slot humidity histories; when a weather series exists, it performs a one-time first-weather-sample seed of current and averaged humidity plus both three-slot histories. That seed touches a different field set and is not CP200's current-`OutHumRat` four-slot per-environment Zone/Space reset. Rust lacks the exact four-slot `ZTM`, `WPrevZoneTS`, `DSWPrevZoneTS`, and `WPrevZoneTSTemp` set, Space records, 26-field touched-versus-retained boundary, and environment gate. Its coefficient representation is initialized and computed through a different path; these adjacent histories and coefficients are not CP200 parity. CP200 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 208 routines, split 58 `state_mapped` plus 150 `source_mapped`, with 85 required; the heat-balance project list becomes 54. CP201 adds required source-mapped `routine.zone_space_heat_balance_set_up_output_vars` immediately after `routine.zone_space_heat_balance_begin_environment_init` and adds `zone_space_heat_balance_set_up_output_vars` after `zone_space_heat_balance_begin_environment_init` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::setUpOutputVars(EnergyPlusData &state, std::string_view prefix, std::string const &name)` is declared at `ZoneTempPredictorCorrector.hh` line 215 and implemented at `ZoneTempPredictorCorrector.cc` lines 2838-2868. Its only production call expressions are CP199 line 2443 for every Zone and lines 2446-2447 for each stored Space membership only when `doSpaceHeatBalanceSimulation` is true; Zone and nested Space calls follow parent Zone order, while sizing-only Space mode does not enter this child. Each call registers, in order, `{Zone|Space} Air Temperature` bound to `ZT` as C/System/Average, `Air Humidity Ratio` bound to `airHumRat` as Units::None/System/Average, `Air Relative Humidity` bound to `airRelHum` as percent/System/Average, and `Mean Radiant Temperature` bound to `MRT` as C/Zone/Average, keyed by the source Zone or Space name. The optional meter fields remain invalid or empty, multipliers remain one, SQL index remains -999, and the report-frequency argument defaults to Hour before any matching request overrides its frequency and schedule. CP201 writes none of the four members, but OutputProcessor initialization, request parsing, dictionary insertion, setup/total counters, requested keyed OutVar pointers, report identifiers, and report-dictionary sinks are persistent side effects. Rows with neither a report request nor a DataOutputs variable-list match still create or reuse dictionary rows and advance setup/total counters without an OutVar. A variable-list-only match appends a keyed dummy pointer, DD link, and report identifier without emitting a report dictionary row; a report request additionally marks the entry for reporting and writes its dictionary sinks. The dictionary deduplicates case-insensitive name plus units only, while direct re-entry repeats the applicable counter, dummy or requested entry, link, identifier, and sink effects. No identity, membership, prefix, key, lifecycle, guard, status, catch, cleanup, or rollback is local. A formatting, initialization, request-input, allocation, or report-write non-return can retain any completed prefix while CP199's one-time latch remains true until line 2618; safe replay requires coordinated predictor/corrector record and OutputProcessor ownership rather than either reset alone. There is no direct CP201 unit call or positive registration assertion. Four direct CP199 fixtures and 55 active full-simulation tests execute at least one Zone setup indirectly; one additional no-Zone `WeatherManager_SetRainFlag` simulation reaches CP199 but executes no CP201 call. Only `HeatBalanceAirManager_GetMixingAndCrossMixing` reaches simulation-Space setup, for three stored Spaces, while seven sizing-Space tests do not. Rust's registry registers adjacent `Zone Mean Air Temperature`, while its heat-balance ResultStore separately stores `Zone Mean Air Temperature` and `Zone Mean Air Humidity Ratio`; neither registers or stores CP201's exact eight-name Zone/Space output set, four member bindings, System-versus-Zone timestep metadata, Space records, or registration lifecycle. Exact `Zone Air Temperature` and `Zone Air Humidity Ratio` constants in the IdealLoads CLI are ESO oracle input identities; the humidity name is also reused for a diagnostic comparison label, but neither is a RuntimeOutputRegistry or ResultStore production definition. CP201 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 209 routines, split 58 `state_mapped` plus 151 `source_mapped`, with 86 required; the heat-balance project list becomes 55. CP202 adds required source-mapped `routine.predict_system_loads` immediately after `routine.zone_space_heat_balance_set_up_output_vars` and adds `predict_system_loads` after `zone_space_heat_balance_set_up_output_vars` before `update_final_surface_heat_balance` in the heat-balance project contract. `PredictSystemLoads(EnergyPlusData &state, bool ShortenTimeStepSys, bool UseZoneTimeStepHistory, Real64 PriorTimeStep)` is declared at `ZoneTempPredictorCorrector.hh` lines 276-280 and implemented at `ZoneTempPredictorCorrector.cc` lines 2870-3145. Its only production call expression is CP195 line 227; the initial and downstepped `HVACManager` PredictStep sites plus `SimulationManager::Resimulate` enter through that wrapper after CP196 and CP199 return. Four phases execute in order. The staged loop samples both base schedules, uses `MAT` or shortened `XMPT`, increments warning or recurring-warning state and clamps heat to cool minus 0.1 when heat is not below cool, chooses the last qualifying negative cooling or positive heating `StageNum`, and applies the source-literal throttling setpoint. The heating branch notably compares its offset with half `CoolThroRange` while shifting by half `HeatThroRange`. Active Space heat balance copies the Zone stage to every stored Space membership. The on/off prefix is gated by `NumOnOffCtrZone` but scans every temperature-controlled Zone having positive `DeltaTCutSet`, saves or restores last-mode flags according to `ShortenTimeStepSys`, clears both off flags, selects `MAT` or shortened `XMPT` only for ThirdOrder and `T1` otherwise, and revises SingleHeat, SingleCool, or both DualHeatCool setpoints with the literal strict comparisons around 0.02 C; `SingleHeatCool` and other switch values receive no setpoint write. An inclusive revised dual lower-greater-than-or-equal-upper collision emits severe and continuation diagnostics and then fatals before prediction dispatch. Every Zone then calls the still-source-mapped CP203 `ZoneSpaceHeatBalanceData::predictSystemLoad` with all three timestep arguments. Each stored Space calls that child when `doSpaceHeatBalance` is true; otherwise a shortened call copies only the returned Zone `MAT` and `airHumRat` to the Space, while a nonshortened call leaves it unchanged. `UseZoneTimeStepHistory` and `PriorTimeStep` are only forwarded locally. The final on/off pass derives `CoolModeLast` and `HeatModeLast` from the off flags and inclusive sign tests on child-produced `TotalOutputRequired`; zero can set both when both flags are true, while NaN clears both. There is no local latch, status, finite or topology validation, catch, cleanup, or rollback. Staged warnings and any completed setpoint, stage, save/restore, child, or Space-copy prefix persist across a fatal or child non-return; same-state retry samples schedules again, advances staged recurrence state, and repeats child effects. Two direct C++ fixtures make 16 calls and 24 setpoint assertions, covering selected Euler and ThirdOrder normal/shortened on/off cases only; global `NumOfZones` remains zero, so no Zone/Space child, forwarding, staged path, final-mode result, failure, retry, or reset contract is asserted. Of 56 active full simulations that reach CP202, 55 execute a Zone child and eight enable a Space child, but none is positive staged/on-off or direct CP202 state coverage. Rust retains the cutout delta only in typed thermostat input, never consumes it at runtime, has no staged control, `StageNum`, CP202 off/mode memory, sensible `ZoneSysEnergyDemand::TotalOutputRequired` field, or Space heat-balance record, and supplies only an identity `predict_system_loads_compat` wrapper around a different bounded Zone-temperature update. Oracle-derived IdealLoads demand snapshots and a separate `predict_step_source_order_path` wrapper-order test do not implement this transaction. CP202 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 210 routines, split 58 `state_mapped` plus 152 `source_mapped`, with 87 required; the heat-balance project list becomes 56. CP203 adds required source-mapped `routine.zone_space_heat_balance_predict_system_load` immediately after `routine.predict_system_loads` and adds `zone_space_heat_balance_predict_system_load` after `predict_system_loads` before `calc_zone_air_temp_set_points` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::predictSystemLoad(EnergyPlusData &state, bool shortenTimeStepSys, bool useZoneTimeStepHistory, Real64 priorTimeStep, int zoneNum, int spaceNum)` is declared at `ZoneTempPredictorCorrector.hh` lines 217-222 and implemented at `ZoneTempPredictorCorrector.cc` lines 3146-3257. Its only production call expressions are CP202 line 3116 for every Zone and lines 3119-3120 for every stored Space only while `doSpaceHeatBalance` is true. After a positive-`zoneNum` debug assertion, CP203 first calls the still-deferred `updateTemperatures` child with all timestep/history arguments and both identities. It then selects Space volume only for positive `spaceNum`, otherwise Zone volume, and writes `AirPowerCap` from that volume, the parent Zone's sensible capacitance multiplier, current pressure/MAT/humidity psychrometrics, and unguarded `TimeStepSysSec`. After `calcZoneOrSpaceSums(state, false, zoneNum, spaceNum)` returns, a Zone-only hybrid branch clears and refreshes `SumIntGainExceptPeople`. CP203 directly derives `TempDepCoef`, `TempIndCoef` including `SysDepZoneLoadsLagged`, the fixed three-history `TempHistoryTerm`, and ThirdOrder-shaped `tempDepLoad`/`tempIndLoad`. A used RoomAir AirflowNetwork control node under the state-wide nonmixing flag first runs `LoadPredictionRoomAirModelAFN`, then overwrites those coefficient, capacity, history, and load values and optionally obtains `HVAC(1).SupplyFraction` as local `RAFNFrac`; this Zone-keyed override is not suppressed for a Space record. CP203 next clears shared `ShortenTimeStepSysRoomAir`. ThirdOrder performs no further direct history rewrite. Every other solution algorithm uses `shortenTimeStepSys && TimeStepSys < TimeStepZone` to select this record's `TM2`/`WM2` when shared `PreviousTimeStep < TimeStepZone`, otherwise `TMX`/`WMX`, sets corresponding AirflowNetwork-node T1 histories, and sets the shared flag true; the other path copies current `ZT`/`airHumRat` and current AFN-node values while leaving it false. These later AFN history copies test only the Zone model type, without the earlier nonmixing or `IsUsed` guards. Non-ThirdOrder finally replaces both load scalars with the plain coefficients. The last ordered calls are `calcPredictedSystemLoad` and then `calcPredictedHumidityRatio` with `RAFNFrac` and both identities. Their thermostat, sensible-demand, humidity-control, moisture-demand, diagnostic, and reporting behavior, together with temperature updating, sum assembly, psychrometrics, hybrid gain collection, and AFN load prediction, remains child dependency behavior rather than CP203 implementation credit. Apart from the debug assertion, CP203 has no local identity, upper-bound, membership, finite, timestep, volume, multiplier, node, or history validation and no local diagnostic, latch, status, catch, cleanup, or rollback. An abnormal child return can retain any completed history, capacity, hybrid clear, coefficient, AFN, shared-flag, record-history, node-history, or sensible-demand prefix; a moisture-child failure follows the already committed sensible child. Same-state retry reruns every child and direct write, and clean replay requires coordinated Zone/Space records, HVAC timestep/global state, HeatBalFanSys and node histories, RoomAir/AFN, HybridModel/internal gains, sensible/moisture demand, diagnostics, and every child owner. No C++ test calls CP203 directly; CP202's 16 focused calls retain zero Zones and never enter it. Of 56 active full simulations reaching CP202, 55 execute at least one Zone CP203 and eight enable Space CP203, but their downstream assertions do not isolate this transaction and none covers its AFN override. Rust's identity `predict_system_loads_compat` encloses a different Zone-only temperature update. Existing coefficient, moist-air-capacitance, analytical/ThirdOrder, history-interpolation, and no-OA ThirdOrder moisture helpers cover adjacent formulas only; their guarded ownership omits CP203's Space path, lagged/hybrid/AFN overrides, shared room-air shortening flag, exact T1/W1 and AFN-node history transaction, and ordered sensible-then-moisture demand children. CP203 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 211 routines, split 58 `state_mapped` plus 153 `source_mapped`, with 88 required; the heat-balance project list becomes 57. CP204 adds required source-mapped `routine.calc_zone_air_temp_set_points` immediately after `routine.zone_space_heat_balance_predict_system_load` and adds `calc_zone_air_temp_set_points` after `zone_space_heat_balance_predict_system_load` before `zone_space_heat_balance_calc_predicted_humidity_ratio` in the heat-balance project contract. `CalcZoneAirTempSetPoints(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 282 and implemented at `ZoneTempPredictorCorrector.cc` lines 3259-3460. Its only production call expression is CP195 line 224 for the `GetZoneSetPoints` selector; both ordinary HVAC timestep setup and HVAC resimulation enter through that wrapper after CP196 and CP199 return. Every call first assigns the complete `TempControlType` array to Uncontrolled, conditionally allocates but never resizes the two occupied-setpoint arrays, then fills every existing heating entry with zero and cooling entry with 100. The local `DeltaT` is assigned zero and never consumed, while `TempControlTypeRpt` and the thermostat setpoint fields receive no global reset. In stored temperature-control order, CP204 trusts `ActualZoneNum` and schedule pointers, samples and casts the control-type schedule, writes the enum plus integer report, and dispatches five nominal cases. Uncontrolled leaves all setpoint fields stale. SingleHeat alone guards its family with `isUsed`, samples the heating schedule into generic `setpt`, snapshots the raw low thermostat value, applies operative-temperature adjustment, and writes only `setptLo`. SingleCool samples its cooling schedule without an `isUsed` guard, snapshots the raw high value, optionally applies adaptive comfort and snapshots `setptAdapComfortCool`, then applies operative-temperature and temperature-and-humidity overcool adjustments while leaving `setptLo` stale. SingleHeatCool samples the family's heating schedule, optionally applies adaptive comfort, applies operative control, copies the result to both bounds, and under allocated optimum-start state may replace generic `setpt` from the SingleHeat family day array at `(ceil(OccStartTime) + 1) * TimeStepsInHour` before an independent `OptStartFlag` copies it to both bounds. DualHeatCool samples and snapshots cooling first, optionally applies adaptive comfort, applies operative control, then samples, snapshots, and operative-adjusts heating; a matching optimum-start identity fills the globally reset occupied arrays from the dual day arrays, an independent flag copies those values into the bounds, and overcool runs last. The invalid-control default emits one Severe diagnostic and continues without a local fatal. After every ordinary record branch, an enabled non-warmup, non-sizing, non-kickoff thermostat-fault scan stops at the first case-insensitive thermostat-name match even when its availability is nonpositive; an available match samples optional severity and subtracts the resulting offset from generic, low, and high setpoints without changing raw or adaptive snapshots. This can accumulate on fields the selected branch did not rewrite. After the ordinary loop, comfort setpoints run conditionally and can overwrite ordinary/fault results, then the EMS override runs unconditionally with final precedence. Operative, adaptive, overcool, comfort, EMS, schedule, and fault helpers remain dependency behavior rather than CP204 implementation credit. CP204 has no Space path, local latch, status, catch, cleanup, rollback, topology/shape/index/schedule/enum/time/finite validation, or allocation reconciliation. Any allocation, schedule, diagnostic, or child non-return retains the reached reset, allocation, record, fault, comfort, or EMS prefix; same-state retry resamples schedules, resets occupied arrays, repeats children and diagnostics, and can compound offsets in untouched fields. Clean replay requires coordinated ZoneControls, HeatBalFanSys setpoint/type/report, Availability optimum-start, schedule, FaultsManager, global/environment/weather, Zone heat-balance MRT/RH, comfort/ThermalComfort, EMS, diagnostic, and child-owned state. Four C++ fixtures make 21 direct calls and 33 thermostat-field post-call assertions: the optimum-start fixture contributes one call/two assertions, the reporting fixture four/seven, and two cutout/prediction fixtures eight/twelve each. The latter calls are composed with CP202, and helper-only tests cover adaptive adjustment and EMS override; no direct helper test covers operative adjustment, overcool, or comfort calculation. Of 57 active full simulations, one expected EMS fatal stops before CP204 and the other 56 reach it; 38 contain a thermostat declaration by static input evidence, but none directly asserts post-CP204 setpoint or control state and none contains comfort, operative-temperature, overcool, optimum-start, or thermostat-fault input. Rust's `calc_zone_air_temp_set_points_compat` is an identity closure invoked once around an empty closure inside the Predict scaffold, rather than the source `GetZoneSetPoints` branch, and has no direct test. Rust types only a direct-Zone DualSetpoint thermostat subset; its IdealLoads diagnostic helper selects the first control, ignores the control-type schedule, and repeats constant schedule values outside the heat-balance runtime. It implements none of CP204's control-family selection, adjustments, optimum-start, fault, comfort, EMS, mutation order, partial effects, or owner lifecycle. CP204 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 212 routines, split 58 `state_mapped` plus 154 `source_mapped`, with 89 required; the heat-balance project list becomes 58. CP205 adds required source-mapped `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` immediately after `routine.calc_zone_air_temp_set_points` and adds `zone_space_heat_balance_calc_predicted_humidity_ratio` after `calc_zone_air_temp_set_points` before `correct_zone_air_temps` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::calcPredictedHumidityRatio(EnergyPlusData &state, Real64 RAFNFrac, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 243 and implemented at `ZoneTempPredictorCorrector.cc` lines 3462-3815. Its only production call expression is CP203 line 3256, after the sensible-demand child; every Zone and every active Space therefore runs CP205 in parent order. CP205 first derives two local RH setpoints and a controlled flag from the parent Zone. A positive `humidityControlZoneIndex` samples humidifying then dehumidifying schedules, applies independent EMS overrides, then scans the first name-matching humidistat fault outside warmup, sizing, and kickoff. A thermostat-dependent fault searches thermostat faults by object name, fatals when none exists, and only for a nonzero available severity-scaled thermostat offset converts both RH values through humidity ratio at record `MAT + offset` and back at `MAT` before clamping to [0,100]. Every other humidistat-fault type samples its own availability/severity, subtracts the offset, and clamps only when available. The first matching humidistat or thermostat fault stops its scan even when unavailable. A humidifying value above dehumidifying emits first-occurrence plus recurring warnings through the control-owned `ErrorIndex` and is reduced to the dehumidifying value; exact equality marks a single setpoint. Without a humidistat, `DoingSizing && DoLatentSizing` scans Zone equipment configurations only to the first controlled entry, does not match it to `zoneNum`, uses its matching `Sizing:Zone` or falls back to the first sizing input, and, when latent sizing is enabled there, samples or reads its RH values, silently reduces humidifying to dehumidifying, and marks the record controlled. Thus the same first controlled sizing input can drive every otherwise uncontrolled Zone and Space invocation. Controlled work adds record `latentGain` to Zone-indexed radiant-system and pool latent gains, computes moist-air density and vapor enthalpy, and assembles `A` and `B` either from Zone-indexed AirflowNetwork multizone exchange plus record surface-moisture terms or from record outdoor, ventilation, exhaust, cross-mixing, mixing, outdoor-air, and surface-moisture fields. `C` uses Space volume only for positive `spaceNum`, otherwise Zone volume, together with the parent Zone moisture-capacitance multiplier and unguarded system-timestep seconds. A parent Zone RoomAir AirflowNetwork model then replaces `A`, `B`, `C`, and vapor enthalpy from its control node without the earlier CP203 nonmixing or `IsUsed` gates, including during a Space invocation. For each RH setpoint CP205 converts record `ZT` and pressure to humidity ratio, then computes ThirdOrder, Analytical, or Euler load. ThirdOrder uses the first three `WPrevZoneTSTemp` values. Analytical uses the exact `A == 0` branch or reuses `exp(min(700, -A/C))` from the humidifying calculation for dehumidifying. Euler uses `C * (Wsp - W1) + A * Wsp - B`. Any positive `RAFNFrac` divides each load; zero, negative, or NaN does not. An unknown solution enum leaves both initialized loads zero. Exact-equal setpoints select humidifying load. Otherwise two positive loads select humidifying, two negative select dehumidifying, and the inclusive humidifying-nonpositive/dehumidifying-nonnegative deadband selects zero; every other combination emits Severe/Continue diagnostics and fatals before demand commit. The final identity branch selects Space demand only for positive `spaceNum`. Controlled state calls `reportMoistLoadsZoneMultiplier` with the parent Zone identity; that dependency stores three unmultiplied predicted rates, three Zone-multiplied demand values, and conditionally initializes sequenced equipment demand. Uncontrolled state directly zeroes only `TotalOutputRequired`, `OutputRequiredToDehumidifyingSP`, and `OutputRequiredToHumidifyingSP`, leaving raw predicted and sequenced fields stale. Counts, indices, membership, pointers, schedule/EMS/fault/sizing values, setpoint ranges, pressure, temperature, humidity, histories, volume, multiplier, timestep, flows, AFN topology, `RAFNFrac`, and finite values are otherwise trusted. Apart from the humidity-control debug assertion, CP205 has no local validation, latch, status, catch, cleanup, or rollback. A missing dependent thermostat or impossible load combination fatals before final demand write; schedule, psychrometric, allocation, diagnostic, or report-child non-return retains any earlier diagnostic, recurrence-index, or partial demand prefix. Zone demand can commit before a later Space failure. Same-state retry resamples schedules and faults, repeats calculations, advances recurring warnings, and repeats child writes; clean replay requires coordinated Zone/Space heat-balance records, ZoneControls and schedules/EMS, FaultsManager, sizing and Zone equipment, HVAC timestep, environment and psychrometrics, AFN/RoomAir, Zone/Space moisture demand including sequences, multipliers, and diagnostics. No C++ test calls CP205 directly. One helper-only fixture calls `ZoneSystemMoistureDemand::reportMoistLoadsZoneMultiplier` four times with 21 assertions, covering only Zone raw and multiplied values, not Space or sequenced output. Of 57 active full simulations, one expected EMS fatal stops before prediction and one zero-Zone weather case reaches CP202 without CP205; the other 55 execute Zone CP205 and eight also execute Space CP205. Their enclosing inputs contain no humidistat, humidistat fault, EMS humidistat override, or RoomAir AFN. Only one ThirdOrder latent-sizing configuration makes the controlled calculation positive for one Zone and three Spaces, and its assertions are downstream; six Analytical configurations and every other reached record take the uncontrolled zero path, while no Euler configuration exists. Rust has no heat-balance CP205 wrapper, call, Zone/Space moisture-demand owner, or runtime mutation. The separate `calc_no_oa_third_order_moisture_demand_compat` implements a guarded Zone-only no-outdoor-air `A = 0` ThirdOrder subset with record density/capacity, internal latent gain, two RH conversions, three-history term, sign/deadband choice, and one combined Zone multiplier. It has two non-test callers, two direct predictor tests, and one adjacent history-term test; a transactional fixed-one-step IdealLoads Humidistat wrapper adds seven tests and eight call expressions around predictor, PurchasedAir, corrector, and history push. That subset rejects invalid inputs through `Option`, clamps both RH fractions, and returns only multiplied loads. It omits source schedule/EMS/fault/sizing ownership, radiant/pool gains, airflow and surface moisture, AFN/RoomAir/RAFN scaling, Space state, Analytical/Euler, diagnostics and partial effects, uncontrolled zero writes, raw predicted values, and sequenced demand. Its CLI path still consumes EnergyPlus-seeded histories, temperatures, sensible demand, and pressure; existing manifest-specific no-OA IdealLoads evidence does not state-map CP205. CP205 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 213 routines, split 58 `state_mapped` plus 155 `source_mapped`, with 90 required; the heat-balance project list becomes 59. CP206 adds required source-mapped `routine.correct_zone_air_temps` immediately after `routine.zone_space_heat_balance_calc_predicted_humidity_ratio` and adds `correct_zone_air_temps` after `zone_space_heat_balance_calc_predicted_humidity_ratio` before `zone_space_heat_balance_correct_air_temp` in the heat-balance project contract. `correctZoneAirTemps(EnergyPlusData &state, bool useZoneTimeStepHistory)` is declared at `ZoneTempPredictorCorrector.hh` lines 289-291 and implemented at `ZoneTempPredictorCorrector.cc` lines 3817-3861. Its only production direct call is CP195 line 230, whose CorrectStep arm assigns the returned value to caller-owned `ZoneTempChange` only after CP206 returns. The ordinary HVAC parent runs initial Get, Predict, HVAC simulation, and then Correct. It selects adaptive downstepping only when the initial returned maximum exceeds `MaxZoneTempDiff` and `KickOffSimulation` is false; otherwise it uses one system step and Zone-timestep history. Each fine step repeats Predict, HVAC simulation, Correct, contaminant correction, and system-history push without recalculating the chosen count from later returns. Demand resimulation calls Get and Predict but not Correct. CP206 initializes its local maximum to zero and traverses Zones in ascending identity. For each Zone it first calls `ZoneSpaceHeatBalanceData::correctAirTemp` with the history selector and saves the result without folding it. It then visits every stored Space identity regardless of Space-HB flags. `doSpaceHeatBalanceSimulation && !DoingSizing` calls the Space correction child and immediately folds that result in Space order. Every other case may copy the controlled Zone system node Temp, HumRat, and Enthalpy to the Space node when `doSpaceHeatBalanceSizing && Zone.IsControlled`, a condition with no direct `DoingSizing` test, and then always copies Zone `ZT`, complete `ZTM`, `MAT`, `airHumRat`, and `airRelHum` into the Space record while deliberately omitting `ZTAVComf`. Only after all Spaces does CP206 fold the saved Zone result. It then always calls `CalcZoneComponentLoadSums` for the Zone, and when simulation Space HB is enabled it makes a second stored-Space traversal and calls that report child even during sizing when Space correction was bypassed. The history selector is only forwarded; shortening and prior-timestep inputs never enter CP206. The wrapper applies no absolute value, tolerance, or finite guard. Normal child returns are nonnegative absolute changes, but with the accumulator as the first `std::max` argument and every candidate second, negative and NaN candidates are ignored, positive infinity can be retained, and nonpositive Zone count returns zero. CP206 directly persists only the optional three Space-node values and five mirrored Space-HB values; correction, moisture, hybrid, and component-report mutations remain child behavior. It has no local assertion, count, identity, membership, arena, node, finite, or topology validation, diagnostic, stop flag, status, latch, catch, cleanup, transaction, or rollback. A Zone-child non-return suppresses that Zone remainder and later Zones. A Space-child failure retains the completed Zone child and earlier Spaces but prevents the Zone fold and reports. Node/mirror failure can retain a direct-write prefix, and report failure retains completed correction, copies, and earlier report effects. The local maximum is not externally visible on failure, and CP195 leaves the old caller value because its assignment has not completed. Same-state retry restarts at Zone one and repeats all children, copies, and reports; clean replay requires coordinated predictor/corrector, Zone/Space heat balance, node, topology, RoomAir, HVAC, report, diagnostic, and child-owner reset. One C++ HybridModel fixture makes five direct calls, all with Zone history true, one Zone, one stored Space, and false Space-HB flags. It therefore reaches Zone correction and five-field Space mirroring, but its five assertions check only hybrid child effects and never the returned maximum, mirror state, component reports, false-history path, failure, retry, or reset. Of 57 active full simulations, one expected EMS fatal stops before CP206, one zero-Zone weather case reaches the zero return, and 55 execute Zone correction. One Analytical configuration positively reaches active simulation-Space correction and Space reporting for two Zones and three Spaces, while seven sizing configurations reach the controlled node triplet and five-field mirror for one Zone and three Spaces each; their assertions are downstream topology or sizing evidence. One thermal-comfort simulation checks later Zone averages, but no test isolates CP206 maximum folding, adaptive step selection, component-report dispatch, failure, retry, or reset. Rust defines `correct_zone_air_temps_compat` only as a generic identity alias over `correct_step_source_order_path`. Its one non-test call and zero direct tests wrap a closure that returns unit, performs an all-Zone temperature pass followed by an all-Zone humidity pass, then optionally performs per-Zone adaptive correction or local history synchronization. It has no Space HB or node state, AirReportVars, component-report child, source history-selector input, or returned global maximum. The lower CorrectStep identity has additional calls during surface iteration and adaptive work that bypass the named CP206 wrapper. Rust chooses adaptive substeps independently per Zone from a hard-coded temperature threshold and repeats only local temperature/humidity work rather than the source global maximum and full Predict-HVAC-Correct retry. Existing coefficient, ThirdOrder, Analytical, history, storage-report, and bounded official 1ZoneUncontrolled evidence belongs to adjacent Zone-only child formulas and case-specific outputs, not CP206 topology or lifecycle parity. Authored Space execution, sizing, EMS, Python, and AirflowNetwork remain run-blocked. CP206 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 214 routines, split 58 `state_mapped` plus 156 `source_mapped`, with 91 required; the heat-balance project list becomes 60. CP207 adds required source-mapped `routine.zone_space_heat_balance_correct_air_temp` immediately after `routine.correct_zone_air_temps` and adds `zone_space_heat_balance_correct_air_temp` after `correct_zone_air_temps` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctAirTemp(EnergyPlusData &state, bool useZoneTimeStepHistory, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` lines 236-239 and implemented at `ZoneTempPredictorCorrector.cc` lines 3863-4165. Its only production direct call expressions are CP206 line 3825 for each Zone with default zero Space identity and line 3831 for positive active Spaces under `doSpaceHeatBalanceSimulation && !DoingSizing`; the Zone always precedes eligible Spaces. CP207 starts its local delta at zero, debug-asserts only positive `zoneNum`, reads the parent Zone multiplier product and system-timestep seconds, then copies complete `DSXMAT`/`DSWPrevZoneTS` histories when the selector is false or `XMAT`/`WPrevZoneTS` when true. Positive `spaceNum` alone selects Space volume; otherwise Zone volume. It overwrites `AirPowerCap` from that volume, parent sensible-capacitance multiplier, density at pre-correction `MAT` and `airHumRat`, specific heat, and unguarded timestep seconds. Exact zero alone calls `RoomAir::ManageAirModel`, then every identity calls `calcZoneOrSpaceSums` with the corrector flag true. `FlagHybridModel_PC` refreshes the current record's `SumIntGainExceptPeople` from the parent Zone without a Space guard. Positive Space alone selects its system node; a positive node, not `Zone.IsControlled`, selects the controlled branch. Controlled `TempDepCoef` is `SumHA + SumMCp + SumSysMCp`, while `TempIndCoef` adds internal and surface terms, non-system and system temperature terms, divided non-air response, lagged system load, and optional parent-Zone AFN sensible exchange and duct loss. The uncontrolled branch omits system flow, non-air response, and lagged system load but retains optional AFN and duct additions. Both branches dispatch unguarded ThirdOrder, Analytical, or Euler equations. Analytical tests exact zero dependence and caps its exponent with source-ordered `min(700, candidate)`; an unknown enum silently retains old `ZT` and continues. In the controlled node stage, a globally mixed or inactive model writes node temperature, Zone-only thermostat temperature, and correction factor one. Three-node displacement or UFAD has precedence and, like active UserDefined or one-node displacement, leaves node and thermostat untouched while conditionally computing the Zone-shared correction factor from supply temperature and existing node temperature; ordered `max(-3, raw)` then `min(3, value)` maps NaN raw to -3 and clamps infinities. AirflowNetwork replaces `ZT` from the parent control node and performs mixed writes; fallback also performs mixed writes. A Space uses its own node but parent RoomAir flags and can overwrite the Zone-shared correction factor after the Zone child, while never writing the thermostat. Controlled `SNLoad` uses `SumSysMCpT`, node mass flow and temperature divided by the Zone multiplier, record humidity specific heat, non-air response, and lagged load. The uncontrolled branch leaves zero load and shared node/control fields stale; only an exact uncontrolled Zone under global nonmixing and the AirflowNetwork enum replaces `ZT`, without testing `SimAirModel`. Exact Zone identity next conditionally calls `InverseModelTemperature` for enabled temperature inverse modes outside warmup and sizing. This occurs after node and load calculation, can replace `ZT` with measured temperature, and reads the global history flag rather than CP207's argument, so later `MAT`, humidity, RH, and delta can disagree with earlier node, thermostat, and load state. CP207 then writes `MAT = ZT`, reports sensible load to Space demand only for positive identity and otherwise Zone demand, calls the later-defined `correctHumRat` child, copies `airHumRatTemp` to `airHumRat`, and computes `airRelHum` through `PsyRhFnTdbWPb` without a caller-side clamp. Sensible demand commits before humidity. Returned change treats every Space as mixed. Exact Zone nonmixed classification recognizes only unmixed three-node displacement or UFAD flags. ThirdOrder compares `ZT` with `ZTM[0]` or the two room-air histories; Analytical and Euler compare with `T1` or the corresponding room-air one-step histories. Unknown enum returns zero. The outer `std::max` has zero first, so NaN is suppressed and positive infinity retained; in a nested nonmixed fold, NaN in the first child suppresses a finite second child while a finite first child survives a NaN second child. A malformed negative Space identity is accepted and combines Zone volume, node, and demand with skipped exact-zero RoomAir, thermostat, hybrid, AFN override, and nonmixed classification; production never supplies it. Direct record writes are the selected histories, capacitance, optional except-People sum, coefficients, `ZT`, `MAT`, humidity, and RH; direct shared writes are applicable node temperature, Zone thermostat, and Zone-shared correction factor. RoomAir, sum assembly, hybrid, sensible demand, humidity, and psychrometric diagnostics remain dependencies. Apart from the Zone assertion, CP207 has no local identity, bound, membership, arena, node, history, enum, topology, multiplier, volume, time, denominator, or finite validation, diagnostic, latch, status, catch, cleanup, transaction, or rollback. A failure can retain any ordered prefix through history, capacity, sums, coefficients, node/control, hybrid, `MAT`, sensible demand, humidity, or final RH; any non-return prevents CP206 from folding the delta. Same-state retry recomputes from already-mutated record, node, RoomAir, correction-factor, demand, and hybrid history state, and clean replay requires coordinated reset of all those owners plus AFN, duct, HVAC/environment, diagnostics, and children. No C++ test calls CP207 directly. One HybridModel fixture reaches its controlled, mixed, default-ThirdOrder Zone path five times with history true and asserts only hybrid child outputs. Helper-only tests call `correctHumRat` ten times and `calcZoneOrSpaceSums` five times but do not compose CP207. Of 57 active full simulations, one EMS fatal stops before it and one has zero Zones; the other 55 reach 81 statically declared Zone records, split 55 controlled and 26 uncontrolled. Forty-nine configurations select ThirdOrder for 74 records, six select Analytical for seven, and none selects Euler. One Analytical configuration reaches two uncontrolled Zones and three uncontrolled Spaces, but no controlled Space. One AFN-distribution and one duct-loss configuration reach their optional terms without asserting CP207 coefficients. No full simulation declares RoomAir or HybridModel input, and no focused assertion covers false history, solver output, node/control writes, sensible demand, RH, returned delta, failure, retry, or reset. Rust has no named `correct_air_temp` wrapper or direct CP207 test. Its all-Zone surface-driven temperature corrector has four non-test calls and no direct test; its adaptive single-Zone corrector has one non-test call and no direct test. The live Correct closure performs one all-Zone temperature pass, then a separate all-Zone humidity pass, then project-specific adaptive/history work instead of CP207's per-record transaction. The coefficient helper has five non-test calls and one direct fixture; the public ThirdOrder helper has no production call and one direct fixture, its coefficient solver has four non-test calls and no direct test, and the Analytical helper has three non-test calls and one direct fixture. There is no Euler helper. Rust preserves limited normal-finite Zone coefficient, ThirdOrder-history, and Analytical algebra, but combines system and non-system flow without a controlled-node decision; those runtime flow fields have no production writers and surface `SumHATref` is fixed zero. It omits exact sum assembly, non-air and lagged loads, AFN, duct, Space state, nodes, RoomAir, thermostat, correction factor, sensible demand, hybrid, distinct `ZT`/`MAT`/`T1`, RH, and returned delta. Rust capacity omits the sensible-capacitance multiplier and Space selection, can retain stale state on rejected weather/psychrometrics, zeroes AirPowerCap for nonpositive inputs, guards a near-zero ThirdOrder denominator, uses epsilon rather than exact Analytical zero, and has no source unknown-enum behavior. Its history selector is recorded but not consumed, and its separate saturation-clamped/defaulting humidity approximation is not `correctHumRat`. Existing formula tests and bounded official `1ZoneUncontrolled` MAT evidence remain adjacent case-specific evidence. Authored Space, sizing, EMS, Python, and AFN paths remain run-blocked. CP207 adds no EnergyPlus source inventory, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 215 routines, split 58 `state_mapped` plus 157 `source_mapped`, with 92 required; the heat-balance project list becomes 61. CP208 adds required source-mapped `routine.push_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_correct_air_temp` and adds `push_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 293 and implemented at `ZoneTempPredictorCorrector.cc` lines 4167-4185. Its sole built-in direct call is CP195 line 236; the sole built-in selector request is `HVACManager::ManageHVAC` lines 579-584 after all system steps and Zone/Space comfort-average snapshots, and before the separate contaminant push, last-system-step-count commit, and demand-manager update. The dispatcher common input/init prefix runs first, but the CP208 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP208 aliases the predictor/corrector state, visits Zones 1 through `NumOfZones`, calls the Zone record child first, and only under the current aggregate `doSpaceHeatBalance` flag visits that Zone's stored `spaceIndexes` in container order and calls the Space child with both identities. It does not independently scan all Spaces, sort, deduplicate, or validate counts, arenas, membership, or identity consistency; a nonpositive Zone count is a no-op. Sizing can set the aggregate flag from `doSpaceHeatBalanceSizing` and normal simulation resets it from `doSpaceHeatBalanceSimulation`. CP208 itself reads traversal state but performs no direct history or numerical writes; all four-slot record, psychrometric, RoomAir, and AFN mutations belong to CP209. It has no local assertion, diagnostic, status, latch, catch, cleanup, transaction, or rollback. A child non-return retains every earlier child and the failing child's prefix, suppresses later traversal, and same-state retry shifts already-shifted histories again. No C++ unit calls CP208, its record child, `ManageZoneAirUpdates`, or `ManageHVAC` directly. Of 57 active `ManageSimulation` call expressions, one expected EMS fatal stops before CP208; the other 56 reach the wrapper, including one zero-Zone case, while the remaining 55 collectively span a static census of 81 Zone identities. Eight configurations collectively enable a static census of 24 Space identities, one in simulation and seven during sizing, but no assertion isolates the Zone/Space history shift, order, failure, retry, or reset. Rust has the selector name and identity source-order/compat wrappers, but production never passes the PushZone selector and the dispatcher ignores its selector. The one live compat call runs at the start of the Predict closure, shifts only three Zone temperature and humidity slots, chooses average versus current input by a project-specific adaptive flag, and encloses predictor work rather than running after the HVAC system loop. Rust has no Space heat-balance arena, membership traversal, fourth history slot, or CP209 RoomAir/AFN and auxiliary record state. One generic wrapper test checks only the wrapper call label and order in a larger vector; one uniform-temperature timestep assertion cannot distinguish slot movement, and there is no direct compat or nonuniform humidity-history test. Separate IdealLoads humidity history is not CP208 parity. CP208 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 216 routines, split 58 `state_mapped` plus 158 `source_mapped`, with 93 required; the heat-balance project list becomes 62. CP209 adds required source-mapped `routine.zone_space_heat_balance_push_zone_timestep_history` immediately after `routine.push_zone_timestep_histories` and adds `zone_space_heat_balance_push_zone_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushZoneTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 245 and implemented at `ZoneTempPredictorCorrector.cc` lines 4187-4275. Its only direct production expressions are CP208 lines 4178 and 4181, with every Zone first and positive eligible Spaces afterward. After a debug-only positive-Zone assertion, it aliases the parent AirModel before any mutation. It interleaves descending four-slot `XMAT` and `WPrevZoneTS` shifts, inserts `ZTAV` and `airHumRatAvg` at slot zero, writes `XMPT = ZT`, commits `airHumRat` and `WTimeMinusP` from `airHumRatTemp`, then writes percent `airRelHum` from `PsyRhFnTdbWPb` using `ZT`, the newly committed humidity, barometric pressure, and diagnostic label `pushTimestepHistories`. The helper floors only its local working humidity to 1e-5, can clamp a comparison-detected out-of-range RH, and can propagate NaN; CP209 stores the original humidity and applies no caller-side finite or range guard. Exact zero Space identity alone enables RoomAir. Three-node displacement and UFAD shift four-slot Floor, occupied, and mixed temperature histories and copy current temperatures to their MAT fields. AirflowNetwork shifts four-slot temperature and humidity histories for every stored node. These branches test the enum without an activity guard. Every solution enum except exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` and `WM2/WMX` from averages; exact-Zone stratified RoomAir advances its three M2/MX pairs and AFN advances per-node T2/TX temperature and humidity pairs. ThirdOrder still performs the common record and Zone RoomAir/AFN four-slot work. Any nonzero Space identity, including a malformed negative value, skips shared RoomAir while retaining common and non-ThirdOrder record writes. CP209 has no upper-bound, record-kind, membership, allocation, enum, topology, pressure, or finite validation and no local diagnostic beyond the psychrometric dependency, status, catch, cleanup, transaction, or rollback. An abnormal non-return after record mutation, for example from malformed psychrometric dependency or RoomAir/AFN indexing state, can leave the common prefix through `WTimeMinusP` with stale RH or retain later record and RoomAir prefixes. Retry shifts already shifted histories again, and the later revert routine is not a full inverse. No C++ test directly calls CP209 or asserts any destination. Fifty-five completing nonzero-Zone configurations collectively span 105 static record identities: 95 ThirdOrder and 10 Analytical, with no Euler. All 81 Zone identities use Mixing RoomAir; 24 Space identities skip RoomAir, so every special RoomAir/AFN branch has zero active full-simulation reach. Tracked official one-Zone outputs are downstream-only and do not isolate this commit. Rust has no singular record helper, call, or test. Its one live plural-wrapper closure shifts only three Zone temperature and humidity slots before predictor work, conditionally chooses average versus current input, and lacks Space, the fourth slot, `XMPT`, temporary/previous-system humidity scalars, RH, non-ThirdOrder record state, and all RoomAir/AFN histories. One uniform history assertion cannot identify the shift; separate system-step and IdealLoads histories are not CP209 parity. CP209 adds no Rust target, mapped state, support, capability, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 217 routines, split 58 `state_mapped` plus 159 `source_mapped`, with 94 required; the heat-balance project list becomes 63. CP210 adds required source-mapped `routine.push_system_timestep_histories` immediately after `routine.zone_space_heat_balance_push_zone_timestep_history` and adds `push_system_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `PushSystemTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 295 and implemented at `ZoneTempPredictorCorrector.cc` lines 4277-4295. Its only built-in direct call is CP195 lines 238-240; its only built-in selector request is `HVACManager::ManageHVAC` lines 388-393 inside each system iteration's strict `TimeStepSys < TimeStepZone` branch, after Predict/HVAC/Correct and before the separate contaminant push, `PreviousTimeStep` commit, and current-step average accumulation. Equal-step and kickoff paths do not request it. The dispatcher common prefix runs first, but the CP210 arm ignores shortening, history-selector, and prior-timestep arguments and preserves `ZoneTempChange`. CP210 visits Zones in ascending order, calls each Zone child before stored eligible Spaces under the current aggregate `doSpaceHeatBalance` flag, and does not independently scan, sort, deduplicate, or validate topology. It performs no direct history write; CP211 owns downstepped record, RoomAir/AFN, and non-ThirdOrder mutations. It has no local assertion, diagnostic, status, catch, cleanup, transaction, or rollback. An abnormal child non-return retains completed and failing prefixes, suppresses later traversal and the parent's following contaminant/time/average work, and retry shifts completed system histories again; Zone-timestep revert does not restore them. No C++ test directly calls CP210 or its child, and no destination assertion or tracked oracle proves the numerical adaptive gate; 55 completing nonzero-Zone configurations provide only potential topology of 81 Zone and 24 eligible Space identities, not execution evidence. Rust has the selector name and generic wrappers, but production never dispatches the PushSystem selector. Production has exactly three lexical compat sites. The feature-disabled site is exclusive with the adaptive helper; when adaptive correction is enabled, each Zone independently takes either the count-one or count-greater-than-one site, so both adaptive sites can execute in one multi-Zone timestep. Exactly one site executes per Zone, rebuilding three slots even at a full Zone step or committing after all locally chosen adaptive substeps rather than performing the source per-fine-step global Zone/Space pass. Rust lacks Space topology, a fourth slot, and CP211 RoomAir/AFN and auxiliary state; the source-order wrapper has one direct label/order test, while the compat wrapper and PushSystem selector dispatch have none. One indirect count-one three-slot sync test does not prove CP210. CP210 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 218 routines, split 58 `state_mapped` plus 160 `source_mapped`, with 95 required; the heat-balance project list becomes 64. CP211 adds required source-mapped `routine.zone_space_heat_balance_push_system_timestep_history` immediately after `routine.push_system_timestep_histories` and adds `zone_space_heat_balance_push_system_timestep_history` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::pushSystemTimestepHistory(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 247 and implemented at `ZoneTempPredictorCorrector.cc` lines 4297-4370. Its only production calls are CP210 lines 4288 and 4291 for each selected Zone and eligible stored Space. After a debug-only positive-Zone assertion, CP211 interleaves descending four-slot `DSXMAT` and `DSWPrevZoneTS` shifts and inserts current `MAT` and `airHumRat`. Exact Zone identity under the global nonmixing flag also advances applicable Floor/occupied/mixed four-slot temperature histories or each stored AirflowNetwork node's four-slot temperature and humidity histories. Every solution value other than exact ThirdOrder, including invalid or unmatched values, then advances record `TM2/TMX` from `MAT` and `WM2/WMX` from distinct `airHumRatTemp`; exact Zones additionally advance applicable stratified or AirflowNetwork two-slot state. ThirdOrder skips only this final stage. CP211 has no upper-bound, Space-sign, membership, record-kind, topology, node-count, enum, or finite validation and no diagnostic, status, catch, cleanup, transaction, or rollback. A late abnormal non-return retains an ordered record or RoomAir/AFN prefix, blocks the rest of CP210 and its parent's following contaminant/time/average work, and same-state retry shifts completed histories again; the following Zone-timestep revert does not restore this downstepped state. No C++ test directly calls CP211 or asserts a destination. Fifty-five nonzero-Zone configurations provide only conditional potential topology if one CP210 pass is selected: 105 records split 95 ThirdOrder and 10 Analytical, with no Euler; all 81 Zones are Mixing and 24 Spaces skip shared RoomAir, so special RoomAir/AFN branches have zero corpus potential. Rust has no singular record helper, Space arena, fourth slot, `airHumRatTemp`, non-ThirdOrder scalar state, RoomAir, or AFN histories. Its count-one and feature-disabled helpers rebuild three Zone slots from current and Zone-timestep histories even when source CP211 is absent, while adaptive count-greater-than-one shifts local three-slot arrays after each Zone-local correction outside the named wrapper and commits them only once after all local substeps. One source-order label test and one indirect count-one rebuild test do not establish CP211; there is no adaptive-count-greater-than-one commit test. CP211 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 219 routines, split 58 `state_mapped` plus 161 `source_mapped`, with 96 required; the heat-balance project list becomes 65. CP212 adds required source-mapped `routine.revert_zone_timestep_histories` immediately after `routine.zone_space_heat_balance_push_system_timestep_history` and adds `revert_zone_timestep_histories` before `update_final_surface_heat_balance` in the heat-balance project contract. `RevertZoneTimestepHistories(EnergyPlusData &state)` is declared at `ZoneTempPredictorCorrector.hh` line 297 and implemented at `ZoneTempPredictorCorrector.cc` lines 4372-4389. Its only direct thermal source call is the CP195 dispatcher arm at lines 232-234, while all nine built-in `ManageZoneAirUpdates` call sites and the test tree request other selectors; EnergyPlus 26.1 therefore has no built-in CP212 timing or runtime gate. An external dispatcher request would still run the common input/init prefix, ignore shortening, history-selector, and prior-step arguments, and preserve `ZoneTempChange`. CP212 visits Zones ascending, calls each Zone child first, then under the current aggregate `doSpaceHeatBalance` flag visits stored Spaces in container order. It does not scan, sort, deduplicate, validate topology, or write history directly; CP213 owns the `XMAT`/`WPrevZoneTS` and optional RoomAir/AFN mutations. It never restores CP211 `DSXMAT`/`DSWPrevZoneTS`, non-ThirdOrder scalars, or downstepped RoomAir/AFN state. CP212 has no assertion, diagnostic, status, completion count, catch, cleanup, transaction, or rollback. A child abnormal non-return retains completed and failing prefixes and suppresses later traversal; same-state retry reapplies CP213's forward copies and is destructive. No C++ test directly calls the wrapper or child or asserts a destination, and all 57 active `ManageSimulation` expressions execute CP212 zero times. The 81 Zone plus 24 eligible Space records are only counterfactual once-requested topology, not full-simulation reach evidence; special RoomAir/AFN topology remains absent. Rust has the selector label and one source-order/compat identity pair, but its dispatcher ignores the selector. Its sole live compat site runs per Zone only on the adaptive count-greater-than-one path and conditionally overwrites current mean temperature and humidity when the new count differs; it shifts no Zone history, has no Space traversal, and can run despite the source branch being dormant. The one source-order label test has no state assertion, and the only focused adaptive test takes count one and never reaches the site. CP212 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 220 routines, split 58 `state_mapped` plus 162 `source_mapped`, with 97 required; the heat-balance project list becomes 66. CP214 adds required source-mapped `routine.zone_space_heat_balance_correct_hum_rat` immediately after `routine.zone_space_heat_balance_revert_zone_timestep_history` and adds `zone_space_heat_balance_correct_hum_rat` before `update_final_surface_heat_balance` in the heat-balance project contract. `ZoneSpaceHeatBalanceData::correctHumRat(EnergyPlusData &state, int zoneNum, int spaceNum = 0)` is declared at `ZoneTempPredictorCorrector.hh` line 241 and implemented at `ZoneTempPredictorCorrector.cc` lines 4433-4619. Its sole production expression is CP207 line 4128, after temperature, `MAT`, and sensible reporting but before record humidity/RH commit and returned delta. Each correction pass visits every Zone and only active non-sizing simulation Spaces; initial and adaptive fine-step HVAC corrections can repeat it. CP214 selects controlled, return-plenum, supply-plenum, or zero primary flow, then independently adds positive parallel-PIU leakage. The literal PIU loop uses global ordinals one through the stored list size rather than the identities in `leakageParallelPIUNums`. It builds latent gain, density, vapor enthalpy, default moisture `A/B`, optionally replaces them with AFN multizone exchange, uses parent-Zone volume for `C` even on Space calls, then independently adds AFN distribution and duct latent terms. ThirdOrder, Analytical, and Euler equations write `airHumRatTemp`; an unmatched enum retains its prior value. Strict negative and saturation clamps run before a parent-Zone RoomAir AFN control-node override that also affects Spaces and is not reclamped. Exact-Zone hybrid inference can mutate measured humidity state without changing the temporary solution; a positive selected node receives humidity before enthalpy, and latent sizing reports raw latent gain with earlier sensible demand. Positive Spaces retain parent Zone equipment, multiplier, capacity, AFN, duct, radiant/pool, and RoomAir context; malformed negative Space identities use Zone node/demand state while skipping only the exact-Zone hybrid gate. CP214 has only a debug positive-Zone assertion and no bounds, membership, topology, allocation, multiplier, timestep, pressure, denominator, enum, or finite validation, status, catch, transaction, or rollback. Failure can retain solved humidity, hybrid, node, or demand prefixes after the parent `MAT` and sensible writes while blocking final record humidity/RH and later traversal; retry can resample schedules and repeat histories, reports, and diagnostics. Ten direct C++ calls plus five indirect focused calls cover only Euler node humidity and hybrid inference slices. Fifty-five nonzero-Zone corpus configurations provide a static one-pass census of 81 Zone plus three active Space records, split 74 ThirdOrder and ten Analytical with no Euler. Fifty-five records are controlled and 29 have no controlled primary flow; plenum/PIU topology is absent, while AFN replacement can reach five Zones, AFN distribution three, and duct latent addition three without an isolating assertion. Runtime cadence is larger and no assertion isolates the full transaction. Rust has no singular Zone/Space helper. Its main and adaptive heat-balance humidity paths use three-slot history only, directly write Zone humidity, enforce a `1e-5` floor and `0.008` invalid fallback, and omit source moisture coefficients and lifecycle effects. The separate `correct_no_oa_third_order_humidity_ratio_compat` is a guarded single-Zone IdealLoads subset for one purchased-air supply with exact ThirdOrder `A/B/C` and clamps, but omits all other branches, Space/node/RH/hybrid/sizing ownership, and source failure semantics. Existing case-bounded humidity claims remain unchanged. CP214 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 222 routines, split 58 `state_mapped` plus 164 `source_mapped`, with 99 required; the heat-balance project list becomes 68. CP215 adds required source-mapped `routine.down_interpolate_4_history_values` immediately after `routine.zone_space_heat_balance_correct_hum_rat` and adds `down_interpolate_4_history_values` before `update_final_surface_heat_balance` in the heat-balance project contract. The first scalar-output `DownInterpolate4HistoryValues` overload is declared at `ZoneTempPredictorCorrector.hh` lines 299-308 and implemented at `ZoneTempPredictorCorrector.cc` lines 4621-4702. It snapshots three inputs by value, writes five referenced outputs in order, tests strict ratio-two then ratio-three tolerance bands, and uses a sequential recurrence for every other ratio without validating timesteps, shortening direction, integer ratio, finite values, or aliasing. Its only production expressions are the CO2 then generic-contaminant calls in `PredictZoneContaminants`; each Zone reaches them only on the first shortened adaptive predictor when the system-step count changed and the matching species is enabled. One direct C++ ratio-two call asserts five outputs. Three contaminant predictor fixtures keep shortening false, and all 57 active full-simulation expressions have contaminant simulation disabled, so focused indirect and corpus reach are zero. Rust's nearest helper returns only the first three thermal Zone history values, rejects nonpositive timesteps, has two adaptive temperature/humidity call sites and ratio-two/three/four tests, and owns no contaminant state or five-output reference transaction. CP215 adds no EnergyPlus source inventory, Rust target, code, mapped state, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required; the heat-balance project list becomes 69. CP216 expands that same logical routine mapping to the independent array-return `DownInterpolate4HistoryValues` overload at `ZoneTempPredictorCorrector.cc` lines 4704-4736. It computes the raw old/new timestep ratio before output, writes four array elements in strict ratio-two, then ratio-three, then fallback order, and returns `oldVals[0]` only after the writes. The fourth input is never read, the third is used only for the ratio-two final output, and no timestep, finite-value, shortening, integer-ratio, or input/output alias validation is performed. A same-array call exposes live-reference aliasing and makes retry generally non-idempotent, while stable distinct-array replay overwrites deterministically. Seven production expressions in `ZoneSpaceHeatBalanceData::updateTemperatures` provide two base temperature/humidity calls per eligible Zone or active Space, three exact-Zone displacement/UFAD calls, and two calls per exact-Zone AFN node. They require shortening and a system-step count different from the prior Zone timestep, so normal HVAC timing permits only the first shortened fine-step candidate; a matching count reuses prior downstepped arrays. One direct C++ ratio-two call asserts the return, all four outputs, and four unchanged inputs. Focused wrapper calls have zero Zones, while the 55 completing nonzero-Zone corpus configurations leave adaptive entry unobserved; their conditional one-pass topology is 81 Zones plus 24 eligible Spaces, or 210 base calls, with zero stratified/AFN potential because all Zones are Mixing. Rust's nearest helper returns only three values by value, rejects nonpositive timesteps, and has Zone-only adaptive temperature/humidity ownership without the fourth output, separate scalar return, Space/RoomAir/AFN topology, node rollback, or alias behavior. CP216 adds no second routine, project-contract item, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion; counts remain 32 algorithms and 223 routines, split 58 `state_mapped` plus 165 `source_mapped`, with 100 required, and the heat-balance project list remains 69. CP217 adds required source-mapped `routine.inverse_model_temperature` and project-contract item after `down_interpolate_4_history_values`. `InverseModelTemperature`, declared at `ZoneTempPredictorCorrector.hh` lines 313-325 and implemented at `ZoneTempPredictorCorrector.cc` lines 4737-4951, samples measured temperature and resets the current mass multiplier, conditionally overwrites `ZT`, runs independent infiltration, internal-mass, and people inverse branches under global Zone-history selection, then shifts three measured histories even outside the date window or during adaptive fine-step calls. Its sole exact-Zone production expression is inside `correctAirTemp`; five indirect focused calls assert only one multiplier, two infiltration rates, and two zero people counts, while all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed `HybridModel:Zone`, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP217 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 224 routines, split 58 `state_mapped` plus 166 `source_mapped`, with 101 required, and the heat-balance project list becomes 70. CP218 adds required source-mapped `routine.process_inverse_model_multp_hm` and project-contract item after `inverse_model_temperature`. `processInverseModelMultpHM`, declared at `ZoneTempPredictorCorrector.hh` lines 327-333 and implemented at `ZoneTempPredictorCorrector.cc` lines 4953-4991, clamps only values below one, warns but does not cap values above 30, includes every value above one in persistent sum/count/average state, and owns a per-Zone first/recurring diagnostic index. Its sole production call is CP217 internal-mass inference; one direct five-call fixture has 26 assertions and proves an uncapped 50 is aggregated, one indirect call asserts only current multiplier 15.13, and all 57 active full simulations configure no HybridModel and reach it zero times. Rust has no typed HybridModel object, inferred multiplier, aggregates, recurring index, exact output/table, runtime path, or focused test, and the raw object remains run-blocking. CP218 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 225 routines, split 58 `state_mapped` plus 167 `source_mapped`, with 102 required, and the heat-balance project list becomes 71. CP219 adds required source-mapped `routine.inverse_model_humidity` and project-contract item `inverse_model_humidity` immediately after `process_inverse_model_multp_hm`. `InverseModelHumidity`, declared at `ZoneTempPredictorCorrector.hh` lines 335-343 and implemented at `ZoneTempPredictorCorrector.cc` lines 4993-5131, unconditionally samples measured humidity, conditionally runs infiltration and people inverse balances, and always shifts measured histories. Its sole exact-Zone production call is inside `correctHumRat`; four indirect fixture calls assert only two ACH and two people results, while all 57 active full simulations configure no HybridModel. Rust has no typed `HybridModel:Zone`, measured humidity history, inverse state, exact outputs, runtime path, or focused test, and the raw object remains run-blocking. CP219 adds no Rust/state/support/output/conformance promotion; the inventory becomes 32 algorithms and 226 routines, split 58 `state_mapped` plus 168 `source_mapped`, with 103 required, and the heat-balance project list becomes 72. CP220 adds required source-mapped `routine.zone_space_heat_balance_calc_zone_or_space_sums` and project-contract item `zone_space_heat_balance_calc_zone_or_space_sums` after `inverse_model_humidity`. `ZoneSpaceHeatBalanceData::calcZoneOrSpaceSums`, declared at `ZoneTempPredictorCorrector.hh` lines 226-230 and implemented at `ZoneTempPredictorCorrector.cc` lines 5133-5281, reconstructs internal, non-system, system, and virtual surface sums for predictor and correction passes. It uses parent-Zone AFN/equipment/plenum/PIU context for Space records, includes the literal PIU-ordinal anomaly, volume-scales only uncontrolled-Space system sums, and can repeat child Window side effects. One five-call C++ Zone fixture has 12 bounded assertions; the 55 nonzero-Zone corpus configurations provide static one-pass censuses of 105 prediction and 84 correction records but no controlled-Space, plenum, or PIU topology. Rust has only adjacent Zone opaque-surface and OtherEquipment subsets with zero-initialized flow fields, no matching routine or Space/airflow topology, and no promotion follows. The inventory becomes 32 algorithms and 227 routines, split 58 `state_mapped` plus 169 `source_mapped`, with 104 required; the heat-balance project list becomes 73. CP221 adds required source-mapped `routine.zone_heat_balance_calc_sum_hat` and project-contract item `zone_heat_balance_calc_sum_hat` after `zone_space_heat_balance_calc_zone_or_space_sums`. `ZoneHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 254 and implemented at `ZoneTempPredictorCorrector.cc` lines 5283-5298, debug-asserts Zone identity, zero-initializes four return fields, and folds every stored Space child in container order without gating, sorting, filtering, deduplication, or membership validation. It owns no persistent write, but child Window effects can survive failure and repeat on retry. One CP220 fixture reaches it five times and asserts six aggregate surface values. The 55 nonzero-Zone corpus configurations give static prediction and correction censuses of 81 Zone aggregates dispatching 99 stored children per pass. Rust directly folds Zone opaque-Surface indexes into only HA/HATsurf/HATref=0, with no four-field result, Space topology, child dispatch, or exact test; existing one-Zone output evidence does not promote this routine. The inventory becomes 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list becomes 74. CP222 expands the existing required source-mapped `routine.zone_heat_balance_calc_sum_hat` to the independent Space override without a second routine or project-contract item because both C++ definitions share the unqualified source identifier `calcSumHAT`. `SpaceHeatBalanceData::calcSumHAT`, declared at `ZoneTempPredictorCorrector.hh` line 259 and implemented at `ZoneTempPredictorCorrector.cc` lines 5300-5413, debug-asserts positive identities, zero-initializes four fields, visits the selected Space inclusive Surface range without filtering, applies ordered Window gains/report mutations and frame/divider/base convection, and dispatches ZoneMean, Adjacent, ZoneSupply, or silent-default reference-air logic using parent-Zone supply state. Its uncontrolled ZoneSupply branch is fatal, and its airflow/no-return `SurfWinHeatGain +=` side effect makes replay and nested-plus-explicit duplicate visits generally non-idempotent. No direct test exists; one CP220 fixture reaches a one-Space, three-non-Window child five times and asserts six surface aggregates, while the static corpus census is 123 prediction and 102 correction calls, or 225 combined. Rust's nearest Zone-only opaque Surface fold returns only HA/HATsurf/HATref=0 and has no Space result, Window runtime, reference switch, or side effects; existing one-Zone evidence does not promote this routine. Counts remain 32 algorithms and 228 routines, split 58 `state_mapped` plus 170 `source_mapped`, with 105 required; the heat-balance project list remains 74. CP223 adds required source-mapped `routine.calc_zone_component_load_sums` and project-contract item `calc_zone_component_load_sums` after `routine.zone_heat_balance_calc_sum_hat`. `CalcZoneComponentLoadSums`, declared at `ZoneTempPredictorCorrector.hh` lines 345-348 and implemented at `ZoneTempPredictorCorrector.cc` lines 5414-5677, is the correction-only component-report update sequence. It zeroes ten target report fields, combines Zone-wide internal gains and parent-Zone airflow/equipment/plenum/PIU/non-air topology with receiver-HB temperatures, coefficients, and storage state, re-walks every parent-Zone Space Surface range for every Zone or active simulation-Space report, accumulates Window convection and CondFD enthalpy, computes ThirdOrder, Analytical, or Euler air storage, and conditionally mutates a parent-Zone shared imbalance-warning index. Controlled inlet work can overwrite four ADU report fields, repeated Space reports can leave the last Space calculation on shared ADUs, and the literal PIU loop again uses global ordinals rather than stored identities. No direct C++ test exists; five HybridModel wrapper calls reach only a Zone report and assert unrelated hybrid state. The 55 nonzero-Zone corpus configurations give a static one-correction census of 81 Zone plus three Space reports, or 84 calls, with 104 stored-Space range walks; no full-simulation assertion isolates the transaction. Rust has separate Zone-only internal-gain, surface-convection, and air-storage report helpers plus a hard-coded zero outdoor-transfer series, but no ten-field record, Space report arena, complete airflow/equipment/Window/PCM/diagnostic transaction, or failure lifecycle. Existing four-row one-Zone conformance remains bounded output evidence and does not promote this routine. CP223 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 229 routines, split 58 `state_mapped` plus 171 `source_mapped`, with 106 required; the heat-balance project list becomes 75. CP224 adds required source-mapped `routine.verify_thermostat_in_zone` and project-contract item `verify_thermostat_in_zone` after `routine.calc_zone_component_load_sums`. `VerifyThermostatInZone`, declared at `ZoneTempPredictorCorrector.hh` line 350 and implemented at `ZoneTempPredictorCorrector.cc` lines 5679-5700, conditionally executes CP196 under the shared `GetZoneAirStatsInputFlag`, clears the latch only after normal return, gates lookup on positive `NumTempControlledZones`, then exact-string scans the full allocated `TempControlledZone.ZoneName` arena and returns only membership. Its sole production call is `SetUpZoneSizingArrays` for each record whose cooling or heating method is exactly `FromDDCalc` once any equipment configuration is controlled; a false result only makes the caller issue a non-pulse warning. CP224 itself owns no diagnostic or mutation after input acquisition. A CP196 non-return retains its partial state and the true latch, so same-state retry can re-enter the non-idempotent loader. No direct C++ test exists; one focused setup fixture reaches two false results without an isolating assertion. Thirty-four of 57 active full simulations provide 48 direct-Zone DesignDay sizing records with matching thermostats and equipment connections, hence a static first-setup census of 48 true calls, but no assertion isolates the predicate, lazy latch, exact-name mismatch, warning, failure, retry, or reset. Rust has bounded normalized direct-Zone DualSetpoint records, graph edges, an IdealLoads diagnostic, and plan metadata, but no exact helper, full CP196 arena or lazy input, executable `Sizing:Zone` caller, or source lookup and failure lifecycle. CP224 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 230 routines, split 58 `state_mapped` plus 172 `source_mapped`, with 107 required; the heat-balance project list becomes 76. CP225 adds required source-mapped `routine.verify_controlled_zone_for_thermostat` and project-contract item `verify_controlled_zone_for_thermostat` after `routine.verify_thermostat_in_zone`. `VerifyControlledZoneForThermostat`, declared at `ZoneTempPredictorCorrector.hh` line 352 and implemented at `ZoneTempPredictorCorrector.cc` lines 5702-5713, directly exact-string scans the full allocated `ZoneEquipConfig.ZoneName` arena and returns only positive membership. It performs no input load, normalization, `IsControlled`, equipment-list, Zone-identity, count, or Space check and owns no write or diagnostic; an exact default-sentinel or manually corrupted uncontrolled-slot match can therefore return true. Its only call expressions are CP199 ordinary then comfort control loops, each deferred until Zone-equipment input is filled and gated by a state-lifetime checked latch. False results produce caller-owned Severe/Continue diagnostics and sticky `ErrorsFound`; both loops finish before fatal, and only a normal input-filled return commits the latch. CP225 itself is read-only and deterministic, but caught parent retry repeats missing diagnostics and fatal because the error remains true, the checked latch remains false, and earlier CP199 effects persist. No direct C++ test exists; four direct CP199 fixture calls are gated off. Thirty-eight of 57 active full simulations provide 52 matching direct-Zone ordinary thermostat/equipment records, yielding 52 true calls, zero false calls, and zero comfort calls without an isolating assertion. Rust independently normalizes a raw nominal-controlled marker, direct-Zone DualSetpoint thermostats, and typed ZoneId equipment connections, plus an IdealLoads-only dispatch validator, but has no preserved-name helper, cross-family ordinary/comfort check, deferred latch, or source diagnostic/failure lifecycle. CP225 adds no algorithm-level `energyplus_source` entry, Rust target, code, mapped state, test, support, capability, output implementation, comparator, manifest, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 231 routines, split 58 `state_mapped` plus 173 `source_mapped`, with 108 required; the heat-balance project list becomes 77. CP226 adds required source-mapped `routine.detect_oscillating_zone_temp` and project item `detect_oscillating_zone_temp` after `routine.verify_controlled_zone_for_thermostat`. `DetectOscillatingZoneTemp`, declared at `ZoneTempPredictorCorrector.hh` line 354 and implemented at `ZoneTempPredictorCorrector.cc` lines 5715-5861, performs clear-on-success one-time allocation and registration, latches calculation from six output names or `PerformancePrecisionTradeoffs`, then on every enabled accepted system timestep shifts an independent zero-seeded four-slot Zone history and detects only strict alternating three-difference swings beyond `0.15 C`. It publishes Zone and Facility-any durations, separately classifies allocated ASH55 occupancy and unguarded deadband state, and accumulates three state-lifetime performance-log scalars without an environment reset. Setup and mid-loop failure are non-transactional, and duplicate enabled calls shift and can accumulate again. No C++ test references CP226 state or output. Fifty-six of 57 active full simulations reach setup and statically register 411 values across 81 nonzero-Zone records plus one zero-Zone configuration, but none requests the variables, monthly report, or performance tradeoff, so the calculation body has zero corpus reach. Rust has adjacent three-slot MAT history, adaptive `0.3 C` step selection, IdealLoads-only occupancy/deadband concepts, and hourly MAT/debug output, but no exact helper, four-slot detector, activation latch, six output-name families, annual/perflog state, HVAC-manager caller, or focused test. CP226 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 232 routines, split 58 `state_mapped` plus 174 `source_mapped`, with 109 required; the heat-balance project list becomes 78. CP227 adds required source-mapped `routine.adjust_air_set_points_for_op_temp_cntrl` and project item `adjust_air_set_points_for_op_temp_cntrl` after `routine.detect_oscillating_zone_temp`. `AdjustAirSetPointsforOpTempCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 356 and implemented at `ZoneTempPredictorCorrector.cc` lines 5863-5897, returns first on a false global operative-control flag and then only on exact per-record None; otherwise it selects a scheduled or fixed MRT fraction, copies the supplied Zone MRT, and overwrites the referenced target with `(setpoint - f * MRT) / (1 - f)`. Normal input validates `f` in `[0.0, 0.9)`, but the routine has no identity, pointer, denominator, finite, or range checks; untargeted records default to Invalid/fixed zero rather than None once another Zone raises the global flag. Five parent call expressions cover one-call single modes and cooling-then-heating Dual mode before later optimum, humidity, fault, comfort, and EMS effects. Built-in execution occurs once per Zone timestep before system substeps, while demand resimulation can add passes; full-parent replay reloads raw schedules, but a direct duplicate routine call generally double-transforms. No direct C++ test exists. Twenty-one direct parent calls produce 46 global-off entries, and the 57-expression full corpus contains no operative object: its static 101 active-sweep entry opportunities all return before record, MRT, or formula work. Rust has bounded DualSetpoint graph and raw-schedule IdealLoads evidence but no typed operative object, global/per-record control state, fraction binding, Zone MRT, exact helper, live caller, or focused test; the raw operative object run-blocks. CP227 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion. The inventory becomes 32 algorithms and 233 routines, split 58 `state_mapped` plus 175 `source_mapped`, with 110 required; the heat-balance project list becomes 79. CP228 adds required source-mapped `routine.adjust_operative_set_points_for_adap_comfort` and project item `adjust_operative_set_points_for_adap_comfort` after `routine.adjust_air_set_points_for_op_temp_cntrl`. `AdjustOperativeSetPointsforAdapComfort`, declared at `ZoneTempPredictorCorrector.hh` line 358 and implemented at `ZoneTempPredictorCorrector.cc` lines 5899-5964, consumes one of seven adaptive daily arrays outside DesignDay/HVACSizeDesignDay or a shared summer-design-day slot for those two environment kinds, then applies the source integer-truncated baseline and exact `-1` fallback before CP227 converts the operative target to an air setpoint. Three guarded parent expressions cover SingleCool, SingleHeatCool, and Dual high only and snapshot the pre-CP227 operative result. Four direct C++ calls prove two central-model values and two lower-than-baseline sentinel restorations without isolating the later exact-sentinel branch, while 21 parent calls and all 57 active full simulations yield zero production entries. Rust has no typed operative object, adaptive state, selector, environment branch, output, live caller, or test, and the raw object run-blocks. CP228 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 234 routines, split 58 `state_mapped` plus 176 `source_mapped`, with 111 required, and the heat-balance project list becomes 80. CP229 adds required source-mapped `routine.calc_zone_air_comfort_set_points` and project-contract item `calc_zone_air_comfort_set_points` after `adjust_operative_set_points_for_adap_comfort`. `CalcZoneAirComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` line 360 and implemented at `ZoneTempPredictorCorrector.cc` lines 5966-6329, performs first-use ThermalComfort initialization, resets and samples comfort control types, selects PMV schedules, executes NO/SPE/OBJ/PEO People dispatch through the next child, clamps and overwrites ordinary dry-bulb setpoints, and leaves EMS with final precedence. Function-scope object and occupant counts bias later Zones, OBJ/PEO SingleCool use the low `-999` sentinel, child endpoint equality can retain stale outputs, and warnings plus partial Fanger/People/setpoint writes survive retry. No direct C++ call or active full-simulation expression reaches it; two raw setup fixtures do not call the parent, and the one installed comfort ExampleFile is not adopted by the repository. Rust has no typed comfort thermostat/setpoint, PMV/Fanger state, inverse child, exact outputs, live caller, or test, and active raw comfort input run-blocks. CP229 adds no Rust/state/support/output/numerical/performance/conformance promotion; the inventory becomes 32 algorithms and 235 routines, split 58 `state_mapped` plus 177 `source_mapped`, with 112 required, and the heat-balance project list becomes 81. CP230 adds required source-mapped `routine.get_comfort_set_points` and project-contract item `get_comfort_set_points` after `calc_zone_air_comfort_set_points`. `GetComfortSetPoints`, declared at `ZoneTempPredictorCorrector.hh` lines 362-367 and implemented at `ZoneTempPredictorCorrector.cc` lines 6331-6415, always evaluates the impure forward Fanger model at the stored minimum then maximum dry-bulb bounds. Only a strict interior PMV target enters configurable global `SolveRoot` with residual tolerance 0.001 and limit 500; below/above targets select a bound; with ordered or equal endpoint PMVs exact equality preserves the referenced output, while reversed equality can select the opposite temperature bound, and a NaN target or any case making all three comparisons false also preserves the output. The solver repeats both endpoints, can evaluate 501 candidates because convergence precedes the strict limit check, returns the last evaluated candidate on an iterated `-1` but Tmin on a zero-candidate width `-1` or `-2`, and yields two, four, or five-through-505 total Fanger calls by path. Each trial can resample People schedules and room-air state, overwrite ThermalComfort scratch plus People/Fanger report state, and repeat child diagnostics; lower clamp and `-2` can return Tmin while the final report state reflects Tmax. SurfaceWeighted MRT first-use can mutate Surface `AE`/`enclAESum` and its latches, and a bad sum returns a MAT/surface average on the first trial but zero later, providing a concrete stateful path from the initial bracket to `-2`. The two solver warning count/index pairs are shared across all comfort Zones and People, persist across environments, and are suppressed during warmup even though calculation and child effects continue. All 12 production expressions are inside CP229, with one/two NO-SPE calls, N/2N OBJ or positive-PEO calls, and 2N/4N nonpositive-PEO-plus-fallback calls. There is no status, transaction, rollback, or latch, so partial child/output/diagnostic state survives failure and retries repeat effects. No unit test reaches CP230, all 21 direct parent calls skip comfort, and the 57 active full simulations contain no comfort input. Separate forward-Fanger and generic-root tests do not compose this inverse. A manual stock-26.1 ExampleFile run supplies only uninstrumented diagnostic evidence and is not adopted as a repository case. Rust has no comfort type, PMV/Fanger state, generic configurable root solver, diagnostics, live caller, or composed test; RawOnly comfort input still run-blocks, and an unrelated private IdealLoads bisection does not promote the boundary. CP230 adds no algorithm-level source, Rust, state, support, output, numerical, performance, or conformance promotion; the inventory becomes 32 algorithms and 236 routines, split 58 `state_mapped` plus 178 `source_mapped`, with 113 required, and the heat-balance project list becomes 82. CP231 adds required source-mapped `routine.adjust_cooling_set_point_for_temp_and_humidity_control` and project item `adjust_cooling_set_point_for_temp_and_humidity_control` after `get_comfort_set_points`. `AdjustCoolingSetPointforTempAndHumidityControl`, declared at `ZoneTempPredictorCorrector.hh` lines 369-372 and implemented at `ZoneTempPredictorCorrector.cc` lines 6417-6458, aliases both indexed records before its global and exact-None guards, chooses a scheduled or constant maximum range, caps only for a strictly positive high-low gap, samples Zone RH and an independent dehumidifying schedule, and under strict positive excess/ratio gates subtracts only from `setptHi`. Default Invalid records do not return; the object-count global flag plus null untargeted schedules creates a mixed-record failure dependency, while zero/reversed gaps, ObjexxFCL second-argument-on-unordered `min`, malformed values, and direct duplicate calls retain literal nonfinite, deadband, and non-idempotent behavior. The two SingleCool/Dual parent expressions run after adaptive/operative and optional optimum-start work and before fault, comfort, and final EMS overrides; SingleCool load still consumes the untouched scalar, and a later positive cutout path can rebuild high from the earlier raw snapshot. No direct C++ test exists; 20 indirect unit entries and all active full-corpus entries return at the false global guard because no exact modifier input is present. The sole installed exact ExampleFile is an upstream Constant-path simulation test, but this repository does not adopt it and the case neither requests the adjusted cooling-setpoint output nor directly asserts CP231. Rust has only adjacent DualSetpoint, Humidistat, psychrometric, and IdealLoads moisture paths, with no typed modifier, percent-RH/range/ratio state, mutable setpoint triple, exact helper, live caller, or test; the raw object run-blocks. CP231 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 237 routines, split 58 `state_mapped` plus 179 `source_mapped`, with 114 required; the heat-balance project list becomes 83. CP232 adds required source-mapped `routine.override_air_set_points_for_ems_cntrl` and project item `override_air_set_points_for_ems_cntrl` after `adjust_cooling_set_point_for_temp_and_humidity_control`. `OverrideAirSetPointsforEMSCntrl`, declared at `ZoneTempPredictorCorrector.hh` line 374 and implemented at `ZoneTempPredictorCorrector.cc` lines 6460-6555, traverses every ordinary record then every comfort record, with heating before cooling. Single modes write scalar plus one bound, SingleHeatCool leaves scalar at cooling when both flags are active, and Dual writes only low/high; dispatch uses live per-Zone types, so record collisions and the later comfort loop resolve field-by-field without validation. Ordinary actuators bind under `Zone Temperature Control` with `[C]`, while comfort actuators bind under `Zone Comfort Control` with `[]` yet are copied directly into Celsius-backed fields. The unconditional parent-tail call follows ordinary modifiers, fault, and comfort processing, while later staged or positive-cutout prediction can replace fields from other/raw state. One direct unit test proves only ordinary and comfort Dual low/high writes; all indirect unit and active full-corpus visits retain false flags, and installed testfiles provide no exact actuator-active ExampleFile. Rust has no EMS engine, actuator state, comfort type, mutable setpoint triple, helper, or live caller, and all EMS input run-blocks. CP232 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 238 routines, split 58 `state_mapped` plus 180 `source_mapped`, with 115 required; the heat-balance project list becomes 84. CP233 adds required source-mapped `routine.fill_predefined_table_on_thermostat_setpoints` and project item `fill_predefined_table_on_thermostat_setpoints` after `override_air_set_points_for_ems_cntrl`. `FillPredefinedTableOnThermostatSetpoints`, declared at `ZoneTempPredictorCorrector.hh` line 376 and implemented at `ZoneTempPredictorCorrector.cc` lines 6558-6672, visits all authored SingleHeating, SingleCooling, SingleHeatingOrCooling, then DualSetpoint definitions and lets the first occurrence of each numeric Schedule ID own its winter, summer, or split synthetic rows in the LEED temperature-schedule table. Detailed schedules sample the first Wednesday at hours 11 and 23 with one selected-date DST shift and count exact matching Wednesday-profile values across 365/366 calendar-day rules; constants return final `currentVal` and every day. Every cell appends without a transaction or once latch; valid parsed names avoid case-sensitive base/synthetic collisions, while manual mixed-case state can still collide, and retry can diverge between last-wins rendering and first-wins retrieval. The sole end-simulation tabular caller runs even when table files are disabled. No C++ test calls CP233; 56 successful full simulations give unasserted static reach, while a helper-only test makes nine calls and 21 assertions. Rust has only adjacent DualSetpoint graph state, calendar-aware schedule series, and constant-schedule IdealLoads diagnostics; it has no four-family arena, source ID deduplication, seasonal query, predefined table store, exact helper, caller, or composed test, and summary-report input stays ignored. CP233 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 239 routines, split 58 `state_mapped` plus 181 `source_mapped`, with 116 required; the heat-balance project list becomes 85. CP234 adds required source-mapped `routine.fill_predefined_table_on_thermostat_schedules` and project item `fill_predefined_table_on_thermostat_schedules` after CP233. `FillPredefinedTableOnThermostatSchedules`, declared at `ZoneTempPredictorCorrector.hh` line 378 and implemented at `ZoneTempPredictorCorrector.cc` lines 6674-6766, visits materialized ordinary TempControlledZone records in stored order, always appends thermostat/control-schedule plus independently joined control-type/name cells, and conditionally appends heating/cooling schedule cells. Nonempty control-object names alone select fixed type slots; tuple-lexical sorting and per-column empty filtering can break positional alignment. The five-slot resize followed by move-append relies on valid-unspecified moved-from strings being empty in observed libraries. Append-only predefined entries have partial-failure, duplicate-retry, last-wins-render versus first-wins-retrieval, and separate reset behavior. One direct call has 24 assertions only for one type per Zone; 56 successful full simulations provide unasserted static reach across 52 records and 312 cells. Rust has only adjacent direct-Zone DualSetpoint graph state and no complete control arena, exact sort/joins, predefined System Summary store, caller, serializer, or comparator; summary-report input remains ignored. CP234 adds no algorithm-level source, Rust/state/support/output/numerical/performance/conformance promotion. The inventory becomes 32 algorithms and 240 routines, split 58 `state_mapped` plus 182 `source_mapped`, with 117 required; the heat-balance project list becomes 86. CP235 next maps `ZoneSpaceHeatBalanceData::updateTemperatures`, declared at `ZoneTempPredictorCorrector.hh` lines 233-234 and implemented at `ZoneTempPredictorCorrector.cc` lines 6768-6833."
routine.manage_zone_air_updates.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.manage_zone_air_updates.source_routine = "ManageZoneAirUpdates"
routine.manage_zone_air_updates.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.manage_zone_air_updates.completion_status = "source_mapped"
routine.manage_zone_air_updates.required_for_full_domain = true
routine.get_zone_air_set_points.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.get_zone_air_set_points.source_routine = "GetZoneAirSetPoints"
routine.get_zone_air_set_points.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.get_zone_air_set_points.completion_status = "source_mapped"
routine.get_zone_air_set_points.required_for_full_domain = true
routine.calculate_monthly_running_average_dry_bulb.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.calculate_monthly_running_average_dry_bulb.source_routine = "CalculateMonthlyRunningAverageDryBulb"
routine.calculate_monthly_running_average_dry_bulb.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.calculate_monthly_running_average_dry_bulb.completion_status = "source_mapped"
routine.calculate_monthly_running_average_dry_bulb.required_for_full_domain = false
routine.calculate_adaptive_comfort_set_point_schl.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.calculate_adaptive_comfort_set_point_schl.source_routine = "CalculateAdaptiveComfortSetPointSchl"
routine.calculate_adaptive_comfort_set_point_schl.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.calculate_adaptive_comfort_set_point_schl.completion_status = "source_mapped"
routine.calculate_adaptive_comfort_set_point_schl.required_for_full_domain = false
routine.init_zone_air_set_points.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.init_zone_air_set_points.source_routine = "InitZoneAirSetPoints"
routine.init_zone_air_set_points.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.init_zone_air_set_points.completion_status = "source_mapped"
routine.init_zone_air_set_points.required_for_full_domain = true
routine.zone_space_heat_balance_begin_environment_init.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_begin_environment_init.source_routine = "beginEnvironmentInit"
routine.zone_space_heat_balance_begin_environment_init.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_begin_environment_init.completion_status = "source_mapped"
routine.zone_space_heat_balance_begin_environment_init.required_for_full_domain = true
routine.zone_space_heat_balance_set_up_output_vars.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_set_up_output_vars.source_routine = "setUpOutputVars"
routine.zone_space_heat_balance_set_up_output_vars.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_set_up_output_vars.completion_status = "source_mapped"
routine.zone_space_heat_balance_set_up_output_vars.required_for_full_domain = true
routine.predict_system_loads.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.predict_system_loads.source_routine = "PredictSystemLoads"
routine.predict_system_loads.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.predict_system_loads.completion_status = "source_mapped"
routine.predict_system_loads.required_for_full_domain = true
routine.zone_space_heat_balance_predict_system_load.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_predict_system_load.source_routine = "predictSystemLoad"
routine.zone_space_heat_balance_predict_system_load.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_predict_system_load.completion_status = "source_mapped"
routine.zone_space_heat_balance_predict_system_load.required_for_full_domain = true
routine.calc_zone_air_temp_set_points.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.calc_zone_air_temp_set_points.source_routine = "CalcZoneAirTempSetPoints"
routine.calc_zone_air_temp_set_points.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.calc_zone_air_temp_set_points.completion_status = "source_mapped"
routine.calc_zone_air_temp_set_points.required_for_full_domain = true
routine.zone_space_heat_balance_calc_predicted_humidity_ratio.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_calc_predicted_humidity_ratio.source_routine = "calcPredictedHumidityRatio"
routine.zone_space_heat_balance_calc_predicted_humidity_ratio.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_calc_predicted_humidity_ratio.completion_status = "source_mapped"
routine.zone_space_heat_balance_calc_predicted_humidity_ratio.required_for_full_domain = true
routine.correct_zone_air_temps.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.correct_zone_air_temps.source_routine = "correctZoneAirTemps"
routine.correct_zone_air_temps.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.correct_zone_air_temps.completion_status = "source_mapped"
routine.correct_zone_air_temps.required_for_full_domain = true
routine.zone_space_heat_balance_correct_air_temp.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_correct_air_temp.source_routine = "correctAirTemp"
routine.zone_space_heat_balance_correct_air_temp.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_correct_air_temp.completion_status = "source_mapped"
routine.zone_space_heat_balance_correct_air_temp.required_for_full_domain = true
routine.push_zone_timestep_histories.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.push_zone_timestep_histories.source_routine = "PushZoneTimestepHistories"
routine.push_zone_timestep_histories.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.push_zone_timestep_histories.completion_status = "source_mapped"
routine.push_zone_timestep_histories.required_for_full_domain = true
routine.zone_space_heat_balance_push_zone_timestep_history.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_push_zone_timestep_history.source_routine = "pushZoneTimestepHistory"
routine.zone_space_heat_balance_push_zone_timestep_history.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_push_zone_timestep_history.completion_status = "source_mapped"
routine.zone_space_heat_balance_push_zone_timestep_history.required_for_full_domain = true
routine.push_system_timestep_histories.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.push_system_timestep_histories.source_routine = "PushSystemTimestepHistories"
routine.push_system_timestep_histories.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.push_system_timestep_histories.completion_status = "source_mapped"
routine.push_system_timestep_histories.required_for_full_domain = true

routine.zone_space_heat_balance_push_system_timestep_history.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_push_system_timestep_history.source_routine = "pushSystemTimestepHistory"
routine.zone_space_heat_balance_push_system_timestep_history.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_push_system_timestep_history.completion_status = "source_mapped"
routine.zone_space_heat_balance_push_system_timestep_history.required_for_full_domain = true

routine.revert_zone_timestep_histories.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.revert_zone_timestep_histories.source_routine = "RevertZoneTimestepHistories"
routine.revert_zone_timestep_histories.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.revert_zone_timestep_histories.completion_status = "source_mapped"
routine.revert_zone_timestep_histories.required_for_full_domain = true

routine.zone_space_heat_balance_revert_zone_timestep_history.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_revert_zone_timestep_history.source_routine = "revertZoneTimestepHistory"
routine.zone_space_heat_balance_revert_zone_timestep_history.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_revert_zone_timestep_history.completion_status = "source_mapped"
routine.zone_space_heat_balance_revert_zone_timestep_history.required_for_full_domain = true

routine.zone_space_heat_balance_correct_hum_rat.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_correct_hum_rat.source_routine = "correctHumRat"
routine.zone_space_heat_balance_correct_hum_rat.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_correct_hum_rat.completion_status = "source_mapped"
routine.zone_space_heat_balance_correct_hum_rat.required_for_full_domain = true

routine.down_interpolate_4_history_values.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.down_interpolate_4_history_values.source_routine = "DownInterpolate4HistoryValues"
routine.down_interpolate_4_history_values.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.down_interpolate_4_history_values.completion_status = "source_mapped"
routine.down_interpolate_4_history_values.required_for_full_domain = true

routine.inverse_model_temperature.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.inverse_model_temperature.source_routine = "InverseModelTemperature"
routine.inverse_model_temperature.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.inverse_model_temperature.completion_status = "source_mapped"
routine.inverse_model_temperature.required_for_full_domain = true

routine.process_inverse_model_multp_hm.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.process_inverse_model_multp_hm.source_routine = "processInverseModelMultpHM"
routine.process_inverse_model_multp_hm.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.process_inverse_model_multp_hm.completion_status = "source_mapped"
routine.process_inverse_model_multp_hm.required_for_full_domain = true

routine.inverse_model_humidity.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.inverse_model_humidity.source_routine = "InverseModelHumidity"
routine.inverse_model_humidity.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.inverse_model_humidity.completion_status = "source_mapped"
routine.inverse_model_humidity.required_for_full_domain = true

routine.zone_space_heat_balance_calc_zone_or_space_sums.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_space_heat_balance_calc_zone_or_space_sums.source_routine = "calcZoneOrSpaceSums"
routine.zone_space_heat_balance_calc_zone_or_space_sums.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_space_heat_balance_calc_zone_or_space_sums.completion_status = "source_mapped"
routine.zone_space_heat_balance_calc_zone_or_space_sums.required_for_full_domain = true

routine.zone_heat_balance_calc_sum_hat.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.zone_heat_balance_calc_sum_hat.source_routine = "calcSumHAT"
routine.zone_heat_balance_calc_sum_hat.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.zone_heat_balance_calc_sum_hat.completion_status = "source_mapped"
routine.zone_heat_balance_calc_sum_hat.required_for_full_domain = true

routine.calc_zone_component_load_sums.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.calc_zone_component_load_sums.source_routine = "CalcZoneComponentLoadSums"
routine.calc_zone_component_load_sums.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.calc_zone_component_load_sums.completion_status = "source_mapped"
routine.calc_zone_component_load_sums.required_for_full_domain = true

routine.verify_thermostat_in_zone.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.verify_thermostat_in_zone.source_routine = "VerifyThermostatInZone"
routine.verify_thermostat_in_zone.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.verify_thermostat_in_zone.completion_status = "source_mapped"
routine.verify_thermostat_in_zone.required_for_full_domain = true

routine.verify_controlled_zone_for_thermostat.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.verify_controlled_zone_for_thermostat.source_routine = "VerifyControlledZoneForThermostat"
routine.verify_controlled_zone_for_thermostat.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.verify_controlled_zone_for_thermostat.completion_status = "source_mapped"
routine.verify_controlled_zone_for_thermostat.required_for_full_domain = true

routine.detect_oscillating_zone_temp.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.detect_oscillating_zone_temp.source_routine = "DetectOscillatingZoneTemp"
routine.detect_oscillating_zone_temp.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.detect_oscillating_zone_temp.completion_status = "source_mapped"
routine.detect_oscillating_zone_temp.required_for_full_domain = true

routine.adjust_air_set_points_for_op_temp_cntrl.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.adjust_air_set_points_for_op_temp_cntrl.source_routine = "AdjustAirSetPointsforOpTempCntrl"
routine.adjust_air_set_points_for_op_temp_cntrl.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.adjust_air_set_points_for_op_temp_cntrl.completion_status = "source_mapped"
routine.adjust_air_set_points_for_op_temp_cntrl.required_for_full_domain = true

routine.adjust_operative_set_points_for_adap_comfort.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.adjust_operative_set_points_for_adap_comfort.source_routine = "AdjustOperativeSetPointsforAdapComfort"
routine.adjust_operative_set_points_for_adap_comfort.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.adjust_operative_set_points_for_adap_comfort.completion_status = "source_mapped"
routine.adjust_operative_set_points_for_adap_comfort.required_for_full_domain = true
routine.calc_zone_air_comfort_set_points.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.calc_zone_air_comfort_set_points.source_routine = "CalcZoneAirComfortSetPoints"
routine.calc_zone_air_comfort_set_points.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.calc_zone_air_comfort_set_points.completion_status = "source_mapped"
routine.calc_zone_air_comfort_set_points.required_for_full_domain = true

routine.get_comfort_set_points.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.get_comfort_set_points.source_routine = "GetComfortSetPoints"
routine.get_comfort_set_points.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.get_comfort_set_points.completion_status = "source_mapped"
routine.get_comfort_set_points.required_for_full_domain = true

routine.adjust_cooling_set_point_for_temp_and_humidity_control.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.adjust_cooling_set_point_for_temp_and_humidity_control.source_routine = "AdjustCoolingSetPointforTempAndHumidityControl"
routine.adjust_cooling_set_point_for_temp_and_humidity_control.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.adjust_cooling_set_point_for_temp_and_humidity_control.completion_status = "source_mapped"
routine.adjust_cooling_set_point_for_temp_and_humidity_control.required_for_full_domain = true

routine.override_air_set_points_for_ems_cntrl.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.override_air_set_points_for_ems_cntrl.source_routine = "OverrideAirSetPointsforEMSCntrl"
routine.override_air_set_points_for_ems_cntrl.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.override_air_set_points_for_ems_cntrl.completion_status = "source_mapped"
routine.override_air_set_points_for_ems_cntrl.required_for_full_domain = true

routine.fill_predefined_table_on_thermostat_setpoints.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.fill_predefined_table_on_thermostat_setpoints.source_routine = "FillPredefinedTableOnThermostatSetpoints"
routine.fill_predefined_table_on_thermostat_setpoints.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.fill_predefined_table_on_thermostat_setpoints.completion_status = "source_mapped"
routine.fill_predefined_table_on_thermostat_setpoints.required_for_full_domain = true

routine.fill_predefined_table_on_thermostat_schedules.source_file = "src/EnergyPlus/ZoneTempPredictorCorrector.cc"
routine.fill_predefined_table_on_thermostat_schedules.source_routine = "FillPredefinedTableOnThermostatSchedules"
routine.fill_predefined_table_on_thermostat_schedules.source_map = "docs/src/porting-map/zone-air-update-map.md"
routine.fill_predefined_table_on_thermostat_schedules.completion_status = "source_mapped"
routine.fill_predefined_table_on_thermostat_schedules.required_for_full_domain = true

[[algorithm]]
id = "surface_inside_temperature"
domain = "heat_balance"
source_map = "docs/src/porting-map/heat-balance-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceSurfaceManager.cc",
]
rust_target = [
  "crates/ep_runtime/src/heat_balance/state.rs::SurfaceHeatBalanceState",
  "crates/ep_runtime/src/runtime.rs::simulate_heat_balance",
]
status = "conformance"
first_case = "surface_temperature_nomass_001"
first_evidence = "surface_temperature_nomass_001"
proof_variables = [
  "Surface Inside Face Temperature",
]
claim_level = "limited-conformance"
support_boundary = "Limited conformance only for declared opaque no-mass surface face-temperature proof variables; no solar, fenestration, or general exterior boundary claim."

[[algorithm]]
id = "calendar_time_state"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/SimulationManager.cc",
  "src/EnergyPlus/WeatherManager.cc",
  "src/EnergyPlus/ScheduleManager.cc",
  "src/EnergyPlus/OutputProcessor.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/SimulationManager.cc|ManageSimulation|ManageSimulation|Environment",
  "src/EnergyPlus/SimulationManager.cc|GetProjectData|GetProjectData|Environment",
  "src/EnergyPlus/WeatherManager.cc|GetRunPeriodData|GetRunPeriodData|Environment",
  "src/EnergyPlus/WeatherManager.cc|SetupEnvironmentTypes|SetupEnvironmentTypes|Environment",
  "src/EnergyPlus/WeatherManager.cc|GetNextEnvironment|GetNextEnvironment|Environment",
  "src/EnergyPlus/WeatherManager.cc|GetSpecialDayPeriodData|GetSpecialDayPeriodData|Environment",
  "src/EnergyPlus/WeatherManager.cc|GetDSTData|GetDSTData|Environment",
  "src/EnergyPlus/WeatherManager.cc|SetDSTDateRanges|SetDSTDateRanges|Environment",
  "src/EnergyPlus/WeatherManager.cc|SetSpecialDayDates|SetSpecialDayDates|Environment",
  "src/EnergyPlus/WeatherManager.cc|ProcessEPWHeader|ProcessEPWHeader|Environment",
  "src/EnergyPlus/WeatherManager.cc|ManageWeather|ManageWeather|Environment",
  "src/EnergyPlus/WeatherManager.cc|InitializeWeather|InitializeWeather|Environment",
  "src/EnergyPlus/WeatherManager.cc|ReadWeatherForDay|ReadWeatherForDay|Environment",
  "src/EnergyPlus/WeatherManager.cc|ReadEPlusWeatherForDay|ReadEPlusWeatherForDay|Environment",
  "src/EnergyPlus/WeatherManager.cc|UpdateWeatherData|UpdateWeatherData|Environment",
  "src/EnergyPlus/WeatherManager.cc|SetCurrentWeather|SetCurrentWeather|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessIntervalFields|ProcessIntervalFields|Environment",
  "src/EnergyPlus/ScheduleManager.cc|populateFromMinuteVals|populateFromMinuteVals|Environment",
  "src/EnergyPlus/ScheduleManager.cc|UpdateScheduleVals|UpdateScheduleVals|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
  "src/EnergyPlus/OutputProcessor.cc|WriteTimeStampFormatData|WriteTimeStampFormatData|Output",
]
rust_target = [
  "crates/ep_raw_model/src/idf_order.rs::apply_idf_declaration_order",
  "crates/ep_raw_model/src/lib.rs::RawModel::ordered_instances",
  "crates/ep_compiler/src/compiler.rs::Compiler::objects",
  "crates/ep_model/src/objects/calendar.rs::CalendarDateRule",
  "crates/ep_model/src/objects/calendar.rs::RunPeriodDaylightSavingTime",
  "crates/ep_model/src/objects/calendar.rs::SpecialDayType",
  "crates/ep_model/src/objects/calendar.rs::RunPeriodSpecialDay",
  "crates/ep_model/src/objects/schedules.rs::ScheduleDayType",
  "crates/ep_model/src/objects/schedules.rs::ScheduleInterpolation",
  "crates/ep_model/src/objects/schedules.rs::ScheduleCompactDayProfile",
  "crates/ep_model/src/objects/schedules.rs::ScheduleCompactPeriod",
  "crates/ep_model/src/objects/schedules.rs::ScheduleCompact",
  "crates/ep_model/src/model.rs::TimestepConfig",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_timestep",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_compact_schedules",
  "crates/ep_compiler/src/compiler.rs::Compiler::compact_schedule_periods",
  "crates/ep_runtime/src/time_axis.rs::ResolvedRunPeriodCalendar",
  "crates/ep_runtime/src/time_axis.rs::ResolvedWeatherEnvironmentCalendar",
  "crates/ep_runtime/src/time_axis/daylight_saving.rs::ResolvedDaylightSavingDate",
  "crates/ep_runtime/src/time_axis/daylight_saving.rs::ResolvedDaylightSavingPeriod",
  "crates/ep_runtime/src/time_axis/daylight_saving.rs::DaylightSavingPeriodSource",
  "crates/ep_runtime/src/time_axis/daylight_saving.rs::DaylightSavingAxisState",
  "crates/ep_runtime/src/time_axis/daylight_saving.rs::resolve_daylight_saving_axis_state",
  "crates/ep_runtime/src/time_axis/day_type.rs::DayType",
  "crates/ep_runtime/src/time_axis/special_days.rs::ResolvedSpecialDayDate",
  "crates/ep_runtime/src/time_axis/special_days.rs::ResolvedSpecialDay",
  "crates/ep_runtime/src/time_axis/special_days.rs::SpecialDayAxisState",
  "crates/ep_runtime/src/time_axis.rs::EnvironmentTimePoint",
  "crates/ep_runtime/src/time_axis.rs::EnvironmentTimeAxis",
  "crates/ep_runtime/src/time_axis/weather_calendar.rs::resolve_weather_environment_calendar",
  "crates/ep_runtime/src/time_axis.rs::build_environment_time_axes",
  "crates/ep_runtime/src/time_axis.rs::build_hourly_time_axis",
  "crates/ep_runtime/src/time_axis.rs::build_hourly_time_axis_with_weather_metadata",
  "crates/ep_runtime/src/schedules/cache.rs::ScheduleSampleStorage",
  "crates/ep_runtime/src/schedules/cache.rs::CachedScheduleSeries",
  "crates/ep_runtime/src/schedules/cache.rs::ScheduleCacheProfile",
  "crates/ep_runtime/src/schedules/cache.rs::ScheduleSeriesCache",
  "crates/ep_runtime/src/schedules/cache.rs::precompute_schedule_cache",
  "crates/ep_runtime/src/schedules/cache.rs::precompute_schedule_cache_for_time_axis",
  "crates/ep_runtime/src/schedules/cache.rs::precompute_schedule_cache_for_environment_time_axis",
  "crates/ep_runtime/src/schedules/constant.rs::precompute_constant_schedule_cache",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_time_axis",
  "crates/ep_runtime/src/schedules.rs::detailed_schedule_lookup_state",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
  "crates/ep_runtime/src/schedules.rs::detailed_schedule_environment_lookup_state",
  "crates/ep_runtime/src/schedules.rs::precompile_compact_schedule_periods",
  "crates/ep_runtime/src/schedules.rs::precompile_compact_schedule_intervals",
  "crates/ep_runtime/src/weather_calendar.rs::EpwCalendarDateRule",
  "crates/ep_runtime/src/weather_calendar.rs::EpwDaylightSavingPeriod",
  "crates/ep_runtime/src/weather_calendar.rs::EpwHoliday",
  "crates/ep_runtime/src/weather_calendar.rs::EpwCalendarMetadata",
  "crates/ep_runtime/src/weather.rs::EpwDataPeriod",
  "crates/ep_runtime/src/weather.rs::EpwDataPeriods",
  "crates/ep_runtime/src/weather.rs::EpwWeatherFile",
  "crates/ep_runtime/src/weather_calendar.rs::parse_epw_calendar_metadata",
  "crates/ep_runtime/src/weather_data_periods.rs::parse_epw_data_periods",
  "crates/ep_runtime/src/weather_environment.rs::EpwEnvironmentWeather",
  "crates/ep_runtime/src/weather_environment.rs::select_epw_environment_weather",
  "crates/ep_runtime/src/weather.rs::next_solar_weather_record_within_day",
  "crates/ep_runtime/src/heat_balance/solar.rs::solar_weather_interpolation_weights",
  "crates/ep_runtime/src/output/timestamp.rs::normalized_hourly_timestamp_label",
  "crates/ep_runtime/src/output/timestamp.rs::normalized_environment_timestep_timestamp_label",
  "crates/ep_cli/src/time_weather_schedule.rs::schedule_samples",
  "crates/ep_cli/src/time_weather_schedule.rs::schedule_timestep_samples",
  "crates/ep_cli/src/main.rs::run_compare_schedule_value",
  "crates/ep_run/src/pipeline.rs::PreparedRuntimeInputs",
  "crates/ep_run/src/pipeline.rs::prepare_runtime_inputs",
  "crates/ep_run/src/pipeline.rs::schedule_cache_json",
]
status = "scaffold"
first_case = "schedule_constant_001"
family_cases = [
  "calendar_schedule_hourly_exact_001",
  "calendar_schedule_compact_through_for_day_type_hourly_exact_001",
  "calendar_schedule_compact_interpolation_modes_exact_001",
  "calendar_schedule_file_8760_leap_exact_001",
  "calendar_schedule_day_hourly_week_daily_year_leap_exact_001",
  "calendar_schedule_day_interval_modes_exact_001",
  "calendar_schedule_day_list_modes_exact_001",
  "calendar_schedule_week_compact_day_types_exact_001",
  "calendar_schedule_compact_zone_timestep_exact_001",
  "calendar_schedule_dst_hour24_tomorrow_day_type_exact_001",
  "calendar_schedule_weather_leap_policy_no_001",
  "calendar_dst_fixed_date_hourly_exact_001",
  "calendar_dst_fixed_date_disabled_hourly_exact_001",
  "calendar_dst_epw_idf_precedence_hourly_exact_001",
  "calendar_epw_dst_weekday_rules_hourly_exact_001",
  "calendar_epw_dst_southern_wrap_hourly_exact_001",
  "calendar_epw_dst_southern_wrap_start_hourly_exact_001",
  "calendar_epw_dst_cross_year_start_year_projection_hourly_exact_001",
  "calendar_special_day_fixed_date_hourly_exact_001",
  "calendar_special_day_cross_year_start_year_projection_hourly_exact_001",
  "calendar_special_day_duration_wrap_common_year_hourly_exact_001",
  "calendar_special_day_duration_wrap_leap_year_hourly_exact_001",
  "calendar_special_day_overlap_zulu_then_alpha_hourly_exact_001",
  "calendar_special_day_overlap_alpha_then_zulu_hourly_exact_001",
  "calendar_special_day_epw_idf_precedence_hourly_exact_001",
  "calendar_special_day_nth_weekday_hourly_exact_001",
  "calendar_special_day_last_weekday_hourly_exact_001",
  "calendar_special_day_nonexistent_fifth_weekday_failure_001",
  "calendar_special_day_weekend_rule_enabled_hourly_exact_001",
  "calendar_special_day_weekend_rule_disabled_hourly_exact_001",
  "calendar_special_day_weekend_rule_blank_hourly_exact_001",
  "calendar_special_day_weekend_saturday_enabled_hourly_exact_001",
  "calendar_special_day_weekend_saturday_disabled_hourly_exact_001",
  "calendar_epw_holiday_fixed_date_enabled_hourly_exact_001",
  "calendar_epw_holiday_fixed_date_disabled_hourly_exact_001",
  "calendar_epw_holiday_weekday_rules_hourly_exact_001",
  "weather_record_start_offset_nonactual_001",
  "weather_fields_001",
]
proof_variables = [
  "Schedule Value",
  "Site Daylight Saving Time Status",
  "Site Day Type Index",
  "Site Outdoor Air Drybulb Temperature",
]
claim_level = "none"
support_boundary = "Source-order scaffold for EnergyPlus environment, shared calendar/day-type/special-day state, EPW header/day-read, weather-update, schedule-update, and timestamp ownership. The paired calendar_schedule_hourly_exact_001 and calendar_schedule_weather_leap_policy_no_001 cases use the same IDF and the same 72 raw EPW rows to lock the narrow weather-effective leap-policy branch: Leap Year Observed=Yes retains 72 ordered samples through Tuesday March 1, while No skips February 29 and retains 48 through simulation Monday. Rust unit tests separately lock Gregorian/weather/schedule day-of-year state as 61/60/61 on no-leap March 1; the external pair does not prove those ordinal fields because its AllDays schedule is hour-only and its normalized timestamp omits them. The paired calendar_dst_fixed_date_hourly_exact_001 and calendar_dst_fixed_date_disabled_hourly_exact_001 cases share the exact EPW 2/29 through 3/1 period and differ only in the RunPeriod weather-file DST flag Yes/No. Their pair gate proves 72 ordered-exact-unique zero-tolerance samples per case: enabled 0/1/1 with 48 active hours, disabled 0/0/0 with zero, plus exact EnergyPlus 26.1 Environment and Environment:Daylight Saving rows including `Environment:Daylight Saving,No,RunPeriod Object`. Rust declaration/use/active/resolution/sample fields are summary diagnostics, not extra EIO. Other periods, years, rules, or policy combinations beyond the separate precedence case, schedule/civil-clock effects, actual weather, Rust raw ESO, and broad WeatherManager behavior remain unclaimed. calendar_dst_epw_idf_precedence_hourly_exact_001 uses that exact RunPeriod and EPW with use No plus one IDF `RunPeriodControl:DaylightSavingTime` 2/28 through 2/29 object. The input-file source wins, proving 72 ordered-exact-unique zero-delta ESO values/timestamps in daily status 1/1/0 with 48 active hours, exact rows `Environment,DST FIXED DATE RUN PERIOD,WeatherFileRunPeriod,02/28/2016,03/01/2016,Sunday,3,Use RunPeriod Specified Day,No,No,No,No,No,Clark and Allen` and `Environment:Daylight Saving,Yes,InputFile,02/28,02/29`, and clean 0/0 completion. Rust `RunPeriodDaylightSavingTime` and JSON/Markdown input-file declaration/effective-source fields are summary diagnostics, not EIO. Other fixed/Nth/last/wrap rules, typed-diagnostic error parity, years, RunPeriods, policies, clock effects, actual/cross-year/DesignDay execution, Rust raw ESO, and broad WeatherManager behavior remain unclaimed. calendar_epw_dst_weekday_rules_hourly_exact_001 adds only the literal EPW start/end pair `4th Monday in February` through `Last Wednesday in February` on an explicit Sunday-start 2032-02-22 through 2032-02-26 RunPeriod whose six calendar/weather policies are No/Yes/No/No/No/No. The boundaries resolve to February 23 day of year 54 through February 25 day of year 56 without wrap; its zero-tolerance gate proves 120 ordered-exact-unique samples in daily order 0/1/1/1/0 with exactly 72 active hours, the conformance/weather/hourly/ESO/timestamp series metadata, every raw EnergyPlus oracle ESO timestamp field and value, the exact WeatherFile EIO row, and 0 Warning/0 Severe completion. calendar_epw_dst_southern_wrap_hourly_exact_001 locks only the literal `Last Sunday in October` through `Last Sunday in March` pair, resolved EIO dates 10/31 and 03/28, and the same-year March 27-29 end-side window: 72 exact ESO values/timestamps in daily status 1/1/0 with 48 active and 24 inactive hours. Rust day-of-year 305/88 and wraps_year=true are source-mapped diagnostics, not EIO. calendar_epw_dst_southern_wrap_start_hourly_exact_001 uses the same rules and EIO dates but only the same-year October 30-November 1 start-side window: 72 exact values/timestamps in status 0/1/1 with 48 active and 24 inactive hours. Neither proves full-year/cross-year traversal, the other window, broader years/months/rules, generic southern/wrap behavior, disabling beyond the fixed pair, precedence beyond the exact fixed-date case, clock effects, actual weather, or broad WeatherManager behavior. IDF DST behavior beyond that case, schedule hour-24/tomorrow rollover, holidays, Rust raw ESO, and cross-year traversal beyond the separate exact case below remain outside this evidence. calendar_epw_dst_cross_year_start_year_projection_hourly_exact_001 separately locks only one non-actual, single-DATA-PERIOD, one-record-per-hour 2031-12-30 through 2032-01-02 traversal. Its EPW Nth-weekday range 1st Thursday in January through 1st Friday in January resolves against the 2031 environment-start annual table as January 2 through January 3. The zero-tolerance gate proves 96 ordered-exact-unique Site Daylight Saving Time Status samples in daily order 0/0/0/1 with exactly 24 active and 72 inactive hours, all 96 raw EnergyPlus ESO values and timestamp rows, exact rows Environment,CROSS YEAR DST START YEAR RUN PERIOD,WeatherFileRunPeriod,12/30/2031,01/02/2032,Tuesday,4,Use RunPeriod Specified Day,Yes,No,No,No,No,Clark and Allen and Environment:Daylight Saving,Yes,WeatherFile,01/02,01/03, and clean 0 Warning/0 Severe completion. EnergyPlus 26.1 source separately maps the January 2 result to prefetch before the January 1 annual-table reset; that internal buffer order is not an ESO/EIO-emitted field. January 3 and later annual reset/reprojection, multiple year boundaries or DATA PERIODS, actual weather, DST-shifted schedule-clock lookup and hour-24/tomorrow semantics beyond the separate exact CP42 rollover case remain unclaimed. calendar_special_day_cross_year_start_year_projection_hourly_exact_001 separately locks only one non-actual, single-DATA-PERIOD, one-record-per-hour 2031-12-30 through 2032-01-02 traversal. Its explicit Tuesday-start RunPeriod and one input-file 1st Thursday in January Holiday are resolved against the 2031 environment-start annual table and retained across that single boundary, producing 96 ordered-exact-unique zero-tolerance Site Day Type Index samples in daily order 3/4/5/8 with January 2 Holiday. The gate locks 96 raw EnergyPlus ESO values and hourly timestamp rows, exact rows Environment,CROSS YEAR SPECIAL DAY RUN PERIOD,WeatherFileRunPeriod,12/30/2031,01/02/2032,Tuesday,4,Use RunPeriod Specified Day,No,No,No,No,No,Clark and Allen and Environment:Special Days,CROSS YEAR NEW YEAR HOLIDAY,Holiday,InputFile,01/02,  1, and clean 0 Warning/0 Severe completion. A Gregorian 2032 January 1 reprojection is deliberately not claimed. Actual weather, multiple year boundaries or DATA PERIODS, later annual resets/reprojection, DST cross-year semantics, February 29 coupling, EPW holidays, weekend shifting, duration wrap, overlaps, warmup lifecycle parity, records per hour above one, broad WeatherManager behavior, and broad schedule behavior remain unclaimed. calendar_special_day_fixed_date_hourly_exact_001 is the baseline external input-file special-day evidence: over explicit 2016-02-28 through 2016-03-01 it declares one fixed IDF February 29 duration-one Holiday, explicitly disables weather-file holidays, weekend observation, and DST, and proves exactly 72 ordered, unique Site Day Type Index samples and normalized timestamps at zero tolerance as 24 Sunday=1, 24 Holiday=8, and 24 Tuesday=3. The paired calendar_special_day_duration_wrap_common_year_hourly_exact_001 and calendar_special_day_duration_wrap_leap_year_hourly_exact_001 cases each declare one fixed December 31 duration-three input-file Holiday over an explicit January 1 through January 3 same-year RunPeriod, with every RunPeriod weather/calendar policy explicitly No and no EPW holidays or daylight-saving rule. The common-year 2017 case uses Leap Year Observed=No, resolves the start to day of year 365, and proves 72 ordered-exact-unique zero-tolerance samples in daily-index order 8/8/3; the leap-year 2016 case uses Leap Year Observed=Yes, resolves day of year 366, and proves 72 samples in order 8/8/1. Each case contains exactly 48 Holiday=8 samples. This locks only the EnergyPlus SetSpecialDayDates common-year `JDay1 == 366 && LeapYearAdd == 0` and leap-year `JDay1 == 367` source branches for a same-year cyclic annual table. Actual-weather cross-year execution, non-actual cross-year execution beyond the exact single-boundary fixture, or per-year reprojection; overlap, precedence, declaration order, or warning parity; and other durations, dates, special-day types, policy values, or EPW calendar rules remain outside this evidence. The paired calendar_special_day_nth_weekday_hourly_exact_001 and calendar_special_day_last_weekday_hourly_exact_001 cases narrow IDF weekday-rule evidence to two explicit forms over the same 2032-02-22 through 2032-03-01 calendar, each with Apply Weekend Holiday Rule explicitly Yes and zero weekend shift. `4th Sunday in February` resolves to February 22, day of year 53, and proves 216 ordered, exact, unique zero-tolerance hourly samples with daily indices 8/2/3/4/5/6/7/1/2. `Last Sunday in February` resolves to leap day February 29, day of year 60, and proves 216 corresponding samples with daily indices 1/2/3/4/5/6/7/8/2. Only those two IDF strings and that explicit 2032 calendar are externally claimed. The calendar_special_day_weekend_rule_enabled_hourly_exact_001, calendar_special_day_weekend_rule_disabled_hourly_exact_001, and calendar_special_day_weekend_rule_blank_hourly_exact_001 cases use the same explicit 2016 three-day calendar, the same fixed MonthDay Sunday 2/28 duration-one input-file Holiday, and an EPW without holidays; their IDFs differ only in RunPeriod A5 as explicit Yes, explicit No, or genuinely blank. Yes and blank both report apply_weekend_rule=true, resolve the Holiday to Monday 2/29, day of year 60, shift 1, and prove 72 ordered-exact-unique zero-tolerance samples as 24 Sunday=1, 24 Holiday=8, and 24 Tuesday=3; their oracle values and timestamp rows are identical. No reports false, resolves Sunday 2/28, day of year 59, shift 0, and proves 24 Holiday=8, 24 Monday=2, and 24 Tuesday=3. EnergyPlus 26.1's IDD and epJSON schema declare A5 default No, while this exact blank IDF follows the executable's enabled branch; all three oracle runs complete with exactly 0 Warning and 0 Severe errors. This locks only that fixed-Sunday executable branch and narrow blank-A5 mismatch. Omitted A5, native epJSON defaults, other blank/default fields, EIO Special Days date semantics, warning text or repetition parity beyond exact clean completion counts, and other dates/durations/types/rules remain unclaimed. The paired calendar_special_day_weekend_saturday_enabled_hourly_exact_001 and calendar_special_day_weekend_saturday_disabled_hourly_exact_001 cases use the same explicit 2016-02-27 through 2016-02-29 calendar, the same fixed MonthDay Saturday 2/27 duration-one input-file Holiday, and an EPW without holidays; their IDFs differ only in the explicit Apply Weekend Holiday Rule Yes/No value. Their zero-tolerance ordered-exact-unique gate proves exactly 72 Site Day Type Index samples and normalized timestamps per case: enabled observes the Holiday on Monday 2/29 as 24 Saturday=7, 24 Sunday=1, and 24 Holiday=8; disabled keeps it on Saturday as 24 Holiday=8, 24 Sunday=1, and 24 Monday=2. This pair proves only the fixed-Saturday plus-two-day observation branch; the fixed-Sunday plus-one-day behavior remains the separate three-case evidence above. The paired calendar_epw_holiday_fixed_date_enabled_hourly_exact_001 and calendar_epw_holiday_fixed_date_disabled_hourly_exact_001 cases share one leap-observed EPW with a fixed February 29 holiday and differ only in the RunPeriod weather-file-holiday use flag. Their zero-tolerance ordered-exact-unique gate proves exactly 72 Site Day Type Index samples and normalized timestamps per case: enabled is 24 Sunday=1, 24 source-exact EPW-holiday Sunday=1, and 24 Tuesday=3; disabled is 24 Sunday=1, 24 Monday=2, and 24 Tuesday=3. This pair proves only fixed-date EPW holiday intake plus RunPeriod enable/disable filtering. calendar_epw_holiday_weekday_rules_hourly_exact_001 separately uses the explicit non-actual 2032-02-23 through 2032-02-25 calendar with weather-file holidays enabled and the EPW header rules `4th Monday in February` then `Last Wednesday in February`. They resolve in header order to days of year 54 and 56, each with source weather-file and Sunday/index 1, and prove 72 ordered-exact-unique zero-tolerance samples in daily order 1/3/1. The EnergyPlus run completes with exactly 0 Warning and 0 Severe errors. This claims only those two literal EPW rules, stated resolutions/order/source, exact values/timestamps, and clean counts. Corresponding IDF forms, fixed EPW dates, other ordinals/weekdays/months/years/header orderings, overlaps, multiple policy combinations, warning text/count/repetition parity beyond exact clean counts, EIO date semantics, schedule/tomorrow state, actual weather, and cross-year behavior remain unclaimed. The paired calendar_special_day_overlap_zulu_then_alpha_hourly_exact_001 and calendar_special_day_overlap_alpha_then_zulu_hourly_exact_001 cases share the explicit 2016-02-28 through 2016-03-01 RunPeriod, one 72-row leap-observed EPW, and two duration-one February 29 definitions named `Zulu Holiday Definition` and `Alpha Custom Day Definition`; their IDFs differ only in those SpecialDays declarations' source order. Zulu then Alpha makes the later CustomDay1 definition win and proves 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3; Alpha then Zulu makes the later Holiday definition win and proves 72 samples in order 1/8/3. This locks only original IDF declaration-order recovery for `RunPeriodControl:SpecialDays` and later-definition overwrite for exactly two definitions on one fixed date. That pair does not itself claim native epJSON ordering, declaration order for other object types, other overlap shapes/counts/durations/types, EPW-versus-IDF precedence, or EnergyPlus warning text or repetition parity. calendar_special_day_epw_idf_precedence_hourly_exact_001 separately uses the explicit 2016-02-28 through 2016-03-01 RunPeriod with weather-file holidays enabled, one fixed February 29 EPW Holiday resolved first as the source-exact Sunday type, and one later duration-one input-file CustomDay1 definition on the same date. Its 72 ordered-exact-unique zero-tolerance samples in daily order 1/11/3 lock only weather-file-then-input-file resolution and the later IDF CustomDay1 overwrite for that one mixed collision. Reversed or multiple input definitions, other EPW or IDF rules/types/durations/overlap shapes, warning text/count/repetition parity, schedule lookup, tomorrow state, raw ESO serialization, actual-weather behavior, and cross-year execution or reprojection remain unclaimed. `calendar_special_day_nonexistent_fifth_weekday_failure_001` adds blocking smoke/nonclaim negative evidence only for the explicit 2016-02-28 through 2016-03-01 calendar and one duration-one IDF `5th Sunday in February` Holiday with Apply Weekend Holiday Rule explicitly Yes; that policy does not rescue the nonexistent Nth rule. EnergyPlus exits 1 through the `SetSpecialDayDates` not-enough-Nths Severe-to-Fatal path, with 0 counted warnings, 1 severe error, and zero data rows. The Rust arbitrary-run exits 6 with phase `runtime`, code `RuntimeConvergenceFailure`, and the exact special-day rejection identifying the run period, special day, occurrence 5, Sunday, and month 2. This locks semantic rejection and zero produced hourly samples, not numeric exit-code equality or numerical Site Day Type Index conformance. Other input-file ordinals, weekdays, months, years, or date strings; multiple errors or definitions; partial-artifact parity; weekend behavior beyond this explicit non-rescue and the declared fixed-Sunday Yes/No/blank and fixed-Saturday Yes/No cases; duration/date/type combinations beyond the paired exact wrap cases; directly ordered typed-vector overwrite; overlap arrangements beyond the exact pair; declaration order beyond SpecialDays and the exact EPW header; other policy values or EPW calendar rules; schedule day-type lookup; tomorrow special-day state; raw successful-run ESO serialization; and actual-weather cross-year execution, non-actual cross-year execution beyond the exact single-boundary fixture, or per-year reprojection remain unit/source evidence or unclaimed. weather_record_start_offset_nonactual_001 separately proves that the supported same-year non-actual, single-data-period, one-record-per-hour selector skips 24 leading decoy rows and exactly matches 48 ordered hourly dry-bulb timestamps and values at zero tolerance; differing EPW row years prove only month/day matching. Today/Tomorrow source-index transitions, interpolation seeds, the day-local hour-24 solar NextHr selection, and the one-timestep-per-hour current-only solar branch remain unit evidence, not external internal-state claims. Weather-required heat-balance ep_run setup consumes the same metadata-aware axis and selector before execution, but that arbitrary-run wiring adds no independent oracle evidence and does not complete any mapped EnergyPlus routine. RunPeriodControl:DaylightSavingTime behavior beyond the exact fixed-date precedence case, DST-shifted schedule lookup and hour-24 rollover beyond the separate exact CP42 rollover case, actual-weather year matching, cross-year traversal beyond the exact single-boundary fixture, multiple-data-period execution, records-per-hour greater than one, missing/range repair, warmup, design-day, subhourly solar interpolation, complete SetCurrentWeather/solar/WeatherManager conformance, and broad schedule/weather conformance remain outside the claim. calendar_schedule_compact_through_for_day_type_hourly_exact_001 separately locks only one non-actual 2031-12-30 through 2032-01-03 RunPeriod with one DATA PERIOD and one record per hour. Its fixed Month/Day Through 1/1 and 12/31 periods, declared source-ordered For selectors, period-local AllOtherDays complement, and one hourly Until endpoint per profile produce exactly 120 ordered-exact-unique zero-tolerance Schedule Value samples and timestamps in daily order 103/104/105/108/199, each repeated 24 times. The gate separately locks all raw EnergyPlus schedule and day-type ESO values and timestamp fields, exact Environment, disabled Environment:Daylight Saving, and input-file Environment:Special Days EIO rows, and clean 0 Warning/0 Severe completion. Rust schedule-day ordinals 365/366/1/2/3 are source-mapped unit-test and summary diagnostics, not ESO/EIO-emitted fields; Rust EPW record selection is null and unclaimed for this schedule-only case. DST schedule-clock and hour-24/tomorrow behavior beyond the separate exact CP42 rollover case, subhourly or interpolation behavior, other schedule families, additional periods, RunPeriods, or year boundaries, actual/design-day/warmup execution, internal-gain/HVAC/IdealLoads calendar consumption, Rust raw ESO parity, and broad warning/error parity remain unclaimed. calendar_schedule_dst_hour24_tomorrow_day_type_exact_001 separately locks only the non-actual 2032-10-30 through 2032-11-01 schedule-clock boundary. Its EPW Last Sunday in October through Last Sunday in March range gives daily DST 0/1/1, and its fixed November 1 input-file Holiday gives raw day types 7/1/8. Exactly 72 ordered-exact-unique zero-tolerance Schedule Value samples and timestamps are 100 repeated 23 times then 124, 200 repeated 23 times then 801, and 800 repeated 23 times then 901. This source-exact sequence proves current-day DST +1-hour detailed lookup, DST hour 24 advancing to the next schedule ordinal and tomorrow Holiday profile, and the final-run hour retaining the stale final Holiday TomorrowVariables day type while advancing the schedule ordinal. The gate separately locks all raw EnergyPlus schedule, DST, and day-type ESO values and timestamp fields, exact Environment, Environment:Daylight Saving, and Environment:Special Days EIO rows, and clean 0 Warning/0 Severe completion. Rust EPW record selection is null and unclaimed for this schedule-only case. Schedule-specific DST opt-out, subhourly values or interpolation beyond the separate exact CP43 aligned default-No case, other schedule families, additional RunPeriods, DATA PERIODS, DST or holiday boundaries, year-end schedule-ordinal wrap beyond unit evidence, actual/design-day/warmup execution, downstream internal-gain/HVAC/IdealLoads calendar consumption, EMS and current-value update semantics, Rust raw ESO parity, and broad warning/error parity remain unclaimed. calendar_schedule_compact_zone_timestep_exact_001 separately locks only one non-actual 2032-01-01 RunPeriod with Timestep,4, all calendar policy flags No, and one Schedule:Compact profile whose omitted Interpolate directive selects EnergyPlus's default Interpolate:No meaning. Exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and normalized timestamps are 11/12/13/14 at hour 1's aligned 15-minute endpoints, followed by 90 for the remaining 92 zone timesteps. The gate separately locks all 96 raw EnergyPlus schedule values and timestep timestamp fields, exact Environment and disabled Environment:Daylight Saving EIO rows, and clean 0 Warning/0 Severe completion. This source-exact boundary covers only ProcessScheduleInput omitted-directive default ownership, ProcessIntervalFields flat minute filling, DaySchedule::populateFromMinuteVals default-No end-minute sampling, and ScheduleDetailed::getHrTsVal explicit timestep indexing for this aligned case. Rust EPW record selection is null and unclaimed. Explicit Interpolate directives, Average/Linear, non-aligned Until warnings and resulting sampling behavior, invalid/default timestep-count correction, DST interaction with subhourly lookup, other public/input schedule object families including Schedule:File, Schedule:Day:*, Schedule:Week:*, and Schedule:Year, actual/design-day/warmup execution, UpdateScheduleVals, EMS/currentVal policy, downstream consumption, Rust raw ESO serialization, and broad warning/error parity remain unclaimed. CP55 Rust-only infrastructure compiles the supported time-axis schedule families into an immutable ScheduleSeriesCache, preserves legacy Schedule Value bits, lengths, family/source order, and duplicate-ID first-wins behavior through adapters, stores Schedule:Constant plus inactive ExternalInterface, FMU import, and FMU export schedule values once as scalar storage without dense per-sample arrays, and exposes deterministic allocation/index profile counters. The referenced-only 24-sample cache is now consumed by the Rust heat-balance initializer, warmup, and run-period timestep path for validated hour-only OtherEquipment references to Schedule:Constant, inactive ExternalInterface/FMU schedule families, and calendar-invariant Schedule:Compact profiles. Unit and structure tests lock fallback bit equivalence, duplicate priority, fail-closed NaN, hour clamping, substep reuse, and daily reuse. This adds no new oracle case or conformance: EnergyPlus numerical heat-balance parity, Schedule:File/Schedule:File:Shading/Schedule:Year or calendar-aware downstream consumption, IdealLoads consumption, wall-clock speedup, weather-cache redesign, repeated-run cache sharing, and live EMS or external-interface updates remain unclaimed."
support_boundary_addendum = "CP55 deterministic Rust operation evidence is limited to the simulation-owned profile for one immutable referenced-only hour-only OtherEquipment cache; it excludes ep_cli full-axis precomputation and the public one-step fallback. The profile separates one successful build, entry/logical-sample counts, and build-time Compact value evaluations from initialization, warmup, and reported run-period indexed reads. One referenced calendar-invariant Compact profile incurs exactly 24 Compact value evaluations while its 24-sample entry is built; hot cached phases record zero live fallback lookups, zero legacy schedule-object-family-chain entries, zero Compact profile resolutions, and zero Compact value evaluations. A 24-hour Constant-plus-Compact cached/live timestep A/B test preserves every scoped convective, zone-temperature, radiant-source, and surface-temperature f64 bit while making both paths nonvacuous. Both lanes share one cached initialization, so the A/B does not directly establish cache-on/off initialization bit equality. The counters do not mutate the cache, and OtherEquipment, zone, surface, and people iteration remains. This is operation-count evidence, not wall-clock timing, cache-specific speedup attribution, EnergyPlus numerical heat-balance or Schedule Value promotion, unsupported schedule-family/calendar-aware downstream consumption, IdealLoads/HVAC/thermostat consumption, weather redesign, cross-run sharing, live EMS/external-interface mutation, warning parity, broad ScheduleManager completion, or a claim about other runtime hot paths."
routine.manage_simulation.source_file = "src/EnergyPlus/SimulationManager.cc"
routine.manage_simulation.source_routine = "ManageSimulation"
routine.manage_simulation.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.manage_simulation.completion_status = "source_mapped"
routine.manage_simulation.required_for_full_domain = true
routine.get_project_data.source_file = "src/EnergyPlus/SimulationManager.cc"
routine.get_project_data.source_routine = "GetProjectData"
routine.get_project_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.get_project_data.completion_status = "source_mapped"
routine.get_project_data.required_for_full_domain = true
routine.get_run_period_data.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.get_run_period_data.source_routine = "GetRunPeriodData"
routine.get_run_period_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.get_run_period_data.completion_status = "source_mapped"
routine.get_run_period_data.required_for_full_domain = true
routine.setup_environment_types.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.setup_environment_types.source_routine = "SetupEnvironmentTypes"
routine.setup_environment_types.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.setup_environment_types.completion_status = "source_mapped"
routine.setup_environment_types.required_for_full_domain = true
routine.get_next_environment.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.get_next_environment.source_routine = "GetNextEnvironment"
routine.get_next_environment.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.get_next_environment.completion_status = "source_mapped"
routine.get_next_environment.required_for_full_domain = true
routine.get_special_day_period_data.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.get_special_day_period_data.source_routine = "GetSpecialDayPeriodData"
routine.get_special_day_period_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.get_special_day_period_data.completion_status = "source_mapped"
routine.get_special_day_period_data.required_for_full_domain = true
routine.get_dst_data.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.get_dst_data.source_routine = "GetDSTData"
routine.get_dst_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.get_dst_data.completion_status = "source_mapped"
routine.get_dst_data.required_for_full_domain = true
routine.set_dst_date_ranges.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.set_dst_date_ranges.source_routine = "SetDSTDateRanges"
routine.set_dst_date_ranges.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.set_dst_date_ranges.completion_status = "source_mapped"
routine.set_dst_date_ranges.required_for_full_domain = true
routine.set_special_day_dates.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.set_special_day_dates.source_routine = "SetSpecialDayDates"
routine.set_special_day_dates.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.set_special_day_dates.completion_status = "source_mapped"
routine.set_special_day_dates.required_for_full_domain = true
routine.manage_weather.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.manage_weather.source_routine = "ManageWeather"
routine.manage_weather.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.manage_weather.completion_status = "source_mapped"
routine.manage_weather.required_for_full_domain = true
routine.initialize_weather.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.initialize_weather.source_routine = "InitializeWeather"
routine.initialize_weather.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.initialize_weather.completion_status = "source_mapped"
routine.initialize_weather.required_for_full_domain = true
routine.process_epw_header.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.process_epw_header.source_routine = "ProcessEPWHeader"
routine.process_epw_header.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.process_epw_header.completion_status = "source_mapped"
routine.process_epw_header.required_for_full_domain = true
routine.read_weather_for_day.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.read_weather_for_day.source_routine = "ReadWeatherForDay"
routine.read_weather_for_day.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.read_weather_for_day.completion_status = "source_mapped"
routine.read_weather_for_day.required_for_full_domain = true
routine.read_eplus_weather_for_day.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.read_eplus_weather_for_day.source_routine = "ReadEPlusWeatherForDay"
routine.read_eplus_weather_for_day.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.read_eplus_weather_for_day.completion_status = "source_mapped"
routine.read_eplus_weather_for_day.required_for_full_domain = true
routine.update_weather_data.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.update_weather_data.source_routine = "UpdateWeatherData"
routine.update_weather_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.update_weather_data.completion_status = "source_mapped"
routine.update_weather_data.required_for_full_domain = true
routine.set_current_weather.source_file = "src/EnergyPlus/WeatherManager.cc"
routine.set_current_weather.source_routine = "SetCurrentWeather"
routine.set_current_weather.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.set_current_weather.completion_status = "source_mapped"
routine.set_current_weather.required_for_full_domain = true
routine.process_schedule_input.source_file = "src/EnergyPlus/ScheduleManager.cc"
routine.process_schedule_input.source_routine = "ProcessScheduleInput"
routine.process_schedule_input.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.process_schedule_input.completion_status = "source_mapped"
routine.process_schedule_input.required_for_full_domain = true
routine.process_interval_fields.source_file = "src/EnergyPlus/ScheduleManager.cc"
routine.process_interval_fields.source_routine = "ProcessIntervalFields"
routine.process_interval_fields.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.process_interval_fields.completion_status = "source_mapped"
routine.process_interval_fields.required_for_full_domain = true
routine.day_schedule_populate_from_minute_vals.source_file = "src/EnergyPlus/ScheduleManager.cc"
routine.day_schedule_populate_from_minute_vals.source_routine = "populateFromMinuteVals"
routine.day_schedule_populate_from_minute_vals.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.day_schedule_populate_from_minute_vals.completion_status = "source_mapped"
routine.day_schedule_populate_from_minute_vals.required_for_full_domain = true
routine.update_schedule_vals.source_file = "src/EnergyPlus/ScheduleManager.cc"
routine.update_schedule_vals.source_routine = "UpdateScheduleVals"
routine.update_schedule_vals.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.update_schedule_vals.completion_status = "source_mapped"
routine.update_schedule_vals.required_for_full_domain = true
routine.schedule_detailed_get_hr_ts_val.source_file = "src/EnergyPlus/ScheduleManager.cc"
routine.schedule_detailed_get_hr_ts_val.source_routine = "getHrTsVal"
routine.schedule_detailed_get_hr_ts_val.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.schedule_detailed_get_hr_ts_val.completion_status = "source_mapped"
routine.schedule_detailed_get_hr_ts_val.required_for_full_domain = true
routine.write_time_stamp_format_data.source_file = "src/EnergyPlus/OutputProcessor.cc"
routine.write_time_stamp_format_data.source_routine = "WriteTimeStampFormatData"
routine.write_time_stamp_format_data.source_map = "docs/src/porting-map/time-weather-schedule.md"
routine.write_time_stamp_format_data.completion_status = "source_mapped"
routine.write_time_stamp_format_data.required_for_full_domain = true

[[algorithm]]
id = "schedule_compact_interpolation_modes"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/SimulationManager.cc",
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/SimulationManager.cc|GetProjectData|GetProjectData|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessIntervalFields|ProcessIntervalFields|Environment",
  "src/EnergyPlus/ScheduleManager.cc|populateFromMinuteVals|populateFromMinuteVals|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleInterpolation",
  "crates/ep_model/src/objects/schedules.rs::ScheduleCompactDayProfile",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_compact_schedules",
  "crates/ep_compiler/src/compiler.rs::Compiler::compact_schedule_periods",
  "crates/ep_runtime/src/schedules.rs::precompile_compact_schedule_periods",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_compact_interpolation_modes_exact_001"
first_evidence = "calendar_schedule_compact_interpolation_modes_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_compact_interpolation_modes_exact_001: one non-actual 2032-01-01 Thursday, Timestep,4, all calendar policies No, and three source-ordered all-days Schedule:Compact objects using explicit Interpolate:No, Average, and Linear with Until 00:20=10, 01:15=175, and 24:00=175. The blocking gate proves exactly three 96-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series. Their first five values are No 10/175/175/175/175, Average 10/120/175/175/175, and Linear 10/40/85/130/175; samples 6 through 96 are 175. It locks the first Linear interval flat, the later cross-hour day-minute ramp, the Average 15-minute window mean, explicit-No endpoint selection, all 288 raw EnergyPlus values, 96 shared timestamp rows, exact Environment and disabled daylight-saving EIO rows, and the fixture's exact 1 Warning/0 Severe completion with the one non-multiple Until warning attached only to explicit No. Rust expands through an intermediate 1,440-minute lattice and stores an immutable 96-value zone-timestep cache; Rust EPW record selection is null and unclaimed. Other timestep counts, Until 24:MM correction, malformed/overlap/zero/incomplete profiles, DST plus subhourly interpolation, mixed multi-profile modes, hourly aggregation, downstream consumption, Schedule:File, public/input Schedule:Day:*, Schedule:Week:*, and Schedule:Year, UpdateScheduleVals, EMS/currentVal, Rust raw ESO serialization, and broad diagnostic or schedule parity remain unclaimed. Typed Rust invalid/duplicate/misplaced directive errors and the compiler warning code are source/unit evidence only. The exact EnergyPlus oracle warning text/count is locked only for this one fixture; Rust warning text/count parity remains unclaimed. The calendar_time_state algorithm and its ProcessScheduleInput, ProcessIntervalFields, populateFromMinuteVals, UpdateScheduleVals, and getHrTsVal routine records remain scaffold/source_mapped and are not promoted by this bounded entry."

[[algorithm]]
id = "schedule_file_hourly_8760_leap"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/DataSystemVariables.cc",
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/DataSystemVariables.cc|CheckForActualFilePath|CheckForActualFilePath|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleFile",
  "crates/ep_model/src/objects/schedules.rs::ScheduleFileColumnSeparator",
  "crates/ep_compiler/src/compiler.rs::compile_raw_model_with_auxiliary_root",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_file_schedules",
  "crates/ep_compiler/src/compiler.rs::Compiler::schedule_file_values",
  "crates/ep_runtime/src/schedules.rs::file_schedule_hourly_8760_value",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_file_8760_leap_exact_001"
first_evidence = "calendar_schedule_file_8760_leap_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_file_8760_leap_exact_001: one staged flat comma CSV basename with one header plus exactly 8760 three-column numeric rows, selected one-based column 2, Rows to Skip=1, Number of Hours=8760, explicit Interpolate=No, Minutes per Item=60, Adjust Schedule for Daylight Savings=No, Timestep=4, and the non-actual 2016-02-28 through 2016-03-01 RunPeriod. The blocking gate proves 72 ordered-exact-unique zero-tolerance hourly Schedule Value samples and timestamps: February 28 is 1393 through 1416, February 29 repeats 1393 through 1416 exactly, and March 1 is 1417 through 1440. It also locks byte-identical auxiliary staging beside input.idf, raw EnergyPlus ESO values/timestamp fields, and exact 0 Warning/0 Severe completion. Rust loads the selected column once into immutable typed values and maps leap-shaped day 60 to source day 59, shifting later days back one source day. The claim excludes 8784 rows, declared-hour/actual-row mismatch, short/extra/missing/non-numeric diagnostics, subhourly input, Interpolate=Yes or blank/default, active/default DST adjustment, other separators/skip/column shapes, JSON/unknown extensions, same-path cache metadata, Schedule:File:Shading, actual/design-day/warmup/multiyear execution, EMS/currentVal, downstream consumption, arbitrary-run staging, Rust raw ESO, and broad warning/error/file-search parity. Typed Rust diagnostics for those rejected branches are source/unit evidence only. The calendar_time_state algorithm and ProcessScheduleInput routine remain scaffold/source_mapped and are not promoted by this bounded entry."

[[algorithm]]
id = "schedule_file_shading_zone_timestep"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/DataSystemVariables.cc",
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/DataSystemVariables.cc|CheckForActualFilePath|CheckForActualFilePath|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleFileShading",
  "crates/ep_model/src/objects/schedules.rs::ScheduleFileShadingColumn",
  "crates/ep_compiler/src/compiler.rs::compile_raw_model_with_auxiliary_root",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_file_shading_schedule",
  "crates/ep_runtime/src/schedules/file_shading.rs::file_shading_series_for_environment_time_axis",
  "crates/ep_runtime/src/schedules/file_shading.rs::file_shading_zone_timestep_value",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_file_shading_exact_001"
first_evidence = "calendar_schedule_file_shading_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_file_shading_exact_001: one byte-staged flat comma CSV basename with one header plus exactly 35,040 rows for a common-year Timestep,4 source. The timestamp column is excluded and the two unique surface headers generate ALPHA SURFACE_shading and BETA SURFACE_shading schedules. The blocking gate proves two 96-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series on 2013-01-01: 0.01..0.96 and 0.96..0.01. It also locks the converted epJSON file_name, raw EnergyPlus ESO values/timestamps, exact EIO environment and default PolygonClipping/non-Imported row, and 0 Warning/0 Severe completion. Rust loads the two columns once into immutable typed values and maps the calendar day, hour-ending, and zone timestep to a source row. The case has no zones, surfaces, or ShadowCalculation object and therefore does not promote Imported solar consumption, surface matching, sunlit fraction, incident solar, or heat balance. JSON/unknown extensions, multiple objects, duplicate/missing/extra/trailing headers, nonnumeric/short/extra data, timestamp validation, other timestep counts, 366-day input and February 29 behavior, actual/design-day/warmup/multienvironment execution, EMS/currentVal, downstream consumers, Rust raw ESO, and broad file-search or diagnostic parity remain unclaimed. Typed rejection branches and wider common-year/leap lookup branches are source/unit evidence only. The calendar_time_state algorithm and ProcessScheduleInput/getHrTsVal routine records remain scaffold/source_mapped and are not promoted by this bounded entry."

[[algorithm]]
id = "external_interface_schedule_initial_value"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
  "src/EnergyPlus/ExternalInterface.cc",
  "src/EnergyPlus/SimulationManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ExternalInterfaceSetSchedule|ExternalInterfaceSetSchedule|Environment",
  "src/EnergyPlus/ExternalInterface.cc|GetExternalInterfaceInput|GetExternalInterfaceInput|Environment",
  "src/EnergyPlus/ExternalInterface.cc|WarnIfExternalInterfaceObjectsAreUsed|WarnIfExternalInterfaceObjectsAreUsed|Environment",
  "src/EnergyPlus/SimulationManager.cc|ManageSimulation|ManageSimulation|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ExternalInterfaceSchedule",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_external_interface_schedules",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_schedule_value",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_cached_schedule_series",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_external_interface_initial_value_exact_001"
first_evidence = "calendar_schedule_external_interface_initial_value_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_external_interface_initial_value_exact_001: one ExternalInterface:Schedule named EXTERNAL INITIAL VALUE, one unlimited Any Number type reference, Initial Value 0.375, no ExternalInterface activation object, one non-actual 2032-01-01 Thursday RunPeriod, and Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all 0.375. It also locks converted epJSON retention of schedule_type_limits_name and initial_value with activation absence, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact EnergyPlus missing-activation warning once, and 1 Warning/0 Severe completion in ERR and END. Rust compiles the inactive object into one immutable scalar and precomputes the same value at every time-axis sample. The EnergyPlus warning lock is oracle evidence only; Rust warning text/count parity is unclaimed. BCVTB/FMU live exchange, sockets/configuration, mutable updates/currentVal, warmup/sizing exchange lifecycle, EMS priority, generated internal day/week EIO parity, downstream consumers, multiple-object and diagnostic parity, bounded type-limit validation, actual/design-day/multienvironment execution, Rust raw ESO serialization, and the following FunctionalMockupUnitImport/Export schedule families remain unclaimed. The mapped routines remain source-order ownership context and are not promoted wholesale by this bounded entry."

[[algorithm]]
id = "external_interface_fmu_import_schedule_initial_value"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
  "src/EnergyPlus/ExternalInterface.cc",
  "src/EnergyPlus/SimulationManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ExternalInterfaceSetSchedule|ExternalInterfaceSetSchedule|Environment",
  "src/EnergyPlus/ExternalInterface.cc|GetExternalInterfaceInput|GetExternalInterfaceInput|Environment",
  "src/EnergyPlus/ExternalInterface.cc|WarnIfExternalInterfaceObjectsAreUsed|WarnIfExternalInterfaceObjectsAreUsed|Environment",
  "src/EnergyPlus/SimulationManager.cc|ManageSimulation|ManageSimulation|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ExternalInterfaceFmuImportSchedule",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_external_interface_fmu_import_schedules",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_fmu_import_cached_schedule_series",
  "crates/ep_runtime/src/schedules/external_interface.rs::initial_value_cached_schedule_series",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_cached_schedule_series_iter",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_fmu_import_initial_value_exact_001"
first_evidence = "calendar_schedule_fmu_import_initial_value_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_fmu_import_initial_value_exact_001: one ExternalInterface:FunctionalMockupUnitImport:To:Schedule named FMU IMPORT INITIAL VALUE, one unlimited Any Number type reference, inert binding strings missing.fmu, ProbeInstance, and ProbeOutput, Initial Value 0.625, no ExternalInterface activation object or FunctionalMockupUnitImport provider object, one non-actual 2032-01-01 Thursday RunPeriod, and Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all 0.625. It also locks converted epJSON retention of schedule_type_limits_names, all three binding strings, and initial_value with activation/provider absence, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact EnergyPlus missing-activation warning once, and 1 Warning/0 Severe completion in ERR and END. Rust compiles only this inactive branch into one shared immutable initial-value scalar and precomputes the same value at every time-axis sample. The EnergyPlus warning lock is oracle evidence only; Rust warning text/count parity is unclaimed. FMU provider resolution, object-list validation or diagnostics, FMU file opening/unpacking and modelDescription parsing, FMI 1 import binding, From:Variable instances, value-reference or causality checks, live exchange and warmup updates, the activated-without-provider permissive EnergyPlus no-op branch, bounded ScheduleTypeLimits validation, downstream consumers, multiple objects, broad diagnostic parity, actual/design-day/multienvironment execution, and Rust raw ESO serialization remain unclaimed. The mapped routines and live-binding source lines remain ownership context and are not promoted wholesale by this bounded entry."

[[algorithm]]
id = "external_interface_fmu_export_schedule_initial_value"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
  "src/EnergyPlus/ExternalInterface.cc",
  "src/EnergyPlus/SimulationManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ExternalInterfaceSetSchedule|ExternalInterfaceSetSchedule|Environment",
  "src/EnergyPlus/ExternalInterface.cc|GetExternalInterfaceInput|GetExternalInterfaceInput|Environment",
  "src/EnergyPlus/ExternalInterface.cc|WarnIfExternalInterfaceObjectsAreUsed|WarnIfExternalInterfaceObjectsAreUsed|Environment",
  "src/EnergyPlus/SimulationManager.cc|ManageSimulation|ManageSimulation|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ExternalInterfaceFmuExportSchedule",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_external_interface_fmu_export_schedules",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_fmu_export_cached_schedule_series",
  "crates/ep_runtime/src/schedules/external_interface.rs::initial_value_cached_schedule_series",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_cached_schedule_series_iter",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_fmu_export_initial_value_exact_001"
first_evidence = "calendar_schedule_fmu_export_initial_value_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_fmu_export_initial_value_exact_001: one ExternalInterface:FunctionalMockupUnitExport:To:Schedule whose actual schedule identity is the inner schedule_name FMU Export Initial Value beneath the converted epJSON synthetic outer key ExternalInterface:FunctionalMockupUnitExport:To:Schedule 1, one unlimited Any Number type reference, inert ProbeInput FMU-variable metadata, Initial Value 0.875, no ExternalInterface activation or socket/provider configuration, one non-actual 2032-01-01 Thursday RunPeriod, and Timestep,4. The blocking gate proves exactly 96 ordered-exact-unique zero-tolerance Timestep Schedule Value samples and timestamps, all 0.875. It also locks the exact epJSON inner four-field shape and synthetic outer-key distinction, all raw EnergyPlus ESO values/timestamps, exact Environment and disabled-daylight-saving EIO rows, the exact EnergyPlus missing-activation warning once, and 1 Warning/0 Severe completion in ERR and END. Rust compiles only this inactive branch into one shared immutable initial-value scalar and precomputes the same value at every time-axis sample. The EnergyPlus warning lock is oracle evidence only; Rust warning text/count parity is unclaimed. socket.cfg and variables.cfg handling, provider resolution, FMU variable binding, live exchange, warmup handoff, mutable updates/currentVal, termination/final exchange, Ptolemy lifecycle, global ExternalInterface behavior, other ExternalInterface variable/actuator families, exact duplicate diagnostic text, unknown type-limit warning parity, downstream consumers, multiple objects, actual/design-day/multienvironment execution, and Rust raw ESO serialization remain unclaimed for this unlimited CP53 fixture. Its bounded behavior is not promoted by this entry: the separate calendar_schedule_fmu_export_bounded_initial_value_exact_001 entry promotes only the Continuous scalar inclusive endpoint [0.0,0.875], paired with calendar_schedule_fmu_export_bounded_initial_value_failure_001 smoke/nonclaim rejection above the upper bound; every other bounded ScheduleTypeLimits branch remains unclaimed. The mapped routines and live-binding source lines remain ownership context and are not promoted wholesale by this bounded entry."

[[algorithm]]
id = "schedule_type_limits_scalar_bounds"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
  "src/EnergyPlus/ExternalInterface.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ExternalInterfaceSetSchedule|ExternalInterfaceSetSchedule|Environment",
  "src/EnergyPlus/ExternalInterface.cc|WarnIfExternalInterfaceObjectsAreUsed|WarnIfExternalInterfaceObjectsAreUsed|Environment",
]
rust_target = [
  "crates/ep_compiler/src/compiler.rs::Compiler::validate_scalar_schedule_type_limits",
  "crates/ep_compiler/src/compiler.rs::Compiler::validate_scalar_schedule_value",
  "crates/ep_runtime/src/schedules/external_interface.rs::external_interface_fmu_export_cached_schedule_series",
  "scripts/compare/compare-calendar-schedule-fmu-export-bounded-initial-value-exact.ps1",
]
status = "conformance"
first_case = "calendar_schedule_fmu_export_bounded_initial_value_exact_001"
first_evidence = "calendar_schedule_fmu_export_bounded_initial_value_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for the paired FMU Export To Schedule Continuous scalar fixtures. calendar_schedule_fmu_export_bounded_initial_value_exact_001 declares ScheduleTypeLimits Continuous bounds [0.0,0.875] and inactive Initial Value 0.875 exactly at the inclusive upper endpoint; its blocking gate proves one 96-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series, all 0.875, exact raw ESO values/timestamps, exact Environment and disabled-DST EIO rows, the missing-activation warning once, and 1 Warning/0 Severe completion. calendar_schedule_fmu_export_bounded_initial_value_failure_001 changes only the upper bound to 0.5 and is blocking smoke/nonclaim rejection evidence: EnergyPlus terminates in ProcessScheduleInput with zero Schedule Value/timestamp data rows and 0 Warning/1 Severe, while Rust emits ScheduleValueOutsideTypeLimits during typed compilation, returns no TypedModel, and produces no graph, execution plan, result store, or runtime samples. Diagnostic text/count and numeric process-exit parity are not claimed. Rust applies the same two-sided inclusive scalar validator to Schedule:Constant, ExternalInterface:Schedule, and FMU Import To Schedule, but those families remain source/unit evidence without separate external numerical promotion. Resolved Schedule:Compact/File/Year minmax, Schedule:Day warning-only and Discrete-integer behavior, one-sided/reversed/blank numeric bounds, unit_type, unknown type-limit reference parity, multiple-violation diagnostic order/text/count/exit behavior, EMS/currentVal or live updates, and cross-family duplicate source-order parity remain unclaimed."

[[algorithm]]
id = "schedule_day_week_year_hourly_leap"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleDayHourly",
  "crates/ep_model/src/objects/schedules.rs::ScheduleWeekDaily",
  "crates/ep_model/src/objects/schedules.rs::ScheduleYear",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_day_hourly_schedules",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_week_daily_schedules",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_year_schedules",
  "crates/ep_runtime/src/schedules.rs::year_schedule_hourly_value",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_day_hourly_week_daily_year_leap_exact_001"
first_evidence = "calendar_schedule_day_hourly_week_daily_year_leap_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_day_hourly_week_daily_year_leap_exact_001: three explicit 24-value Schedule:Day:Hourly profiles, two Schedule:Week:Daily objects with all 12 typed day references, and one Schedule:Year whose source-ordered Week A 1/1-2/28 and Week B 3/1-12/31 ranges intentionally leave leap-shaped day 60 unassigned. EnergyPlus ProcessScheduleInput copies day 59's Week A pointer to day 60, after which Monday selects 201 through 224. The exact non-actual 2016-02-28 through 2016-03-01 gate proves 72 ordered-exact-unique zero-tolerance hourly Schedule Value samples and timestamps: Sunday Week A is 101 through 124, Monday on the copied Week A pointer is 201 through 224, and Tuesday Week B is 301 through 324. It also locks raw EnergyPlus ESO values/timestamps, the exact Environment and disabled daylight-saving EIO rows, and 0 Warning/0 Severe completion. Rust stores immutable fixed day, week, and 366-day direct lookup arrays. The claim excludes Day:Hourly blank/default and ScheduleTypeLimits warning parity, consumption of the other nine Week:Daily day types, Schedule:Day:Interval, Schedule:Day:List, Schedule:Week:Compact, Year range wrap plus missing/overlap/reference diagnostic parity, DST/holiday/tomorrow combinations, actual/design-day/warmup/multiyear execution, EMS/currentVal, downstream gains/HVAC consumption, Rust raw ESO, and broad ScheduleManager parity. Typed Rust rejection and wider unit branches are source evidence only. The calendar_time_state algorithm and ProcessScheduleInput/getHrTsVal routine records remain scaffold/source_mapped and are not promoted by this bounded entry."

[[algorithm]]
id = "schedule_day_interval_modes"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessIntervalFields|ProcessIntervalFields|Environment",
  "src/EnergyPlus/ScheduleManager.cc|populateFromMinuteVals|populateFromMinuteVals|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleDayInterval",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_day_interval_schedules",
  "crates/ep_runtime/src/schedules/day_table.rs::CompiledDayScheduleTable",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_day_interval_modes_exact_001"
first_evidence = "calendar_schedule_day_interval_modes_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_day_interval_modes_exact_001: three Schedule:Day:Interval profiles selected on Thursday, Friday, and Saturday through one Schedule:Week:Daily and one full-year Schedule:Year at Timestep,4. Thursday omits interpolation and proves the default-No aligned profile as 10 once then 175 for 95 samples; Friday proves Average across a non-aligned 00:20 boundary as 10, 120, then 175 for 94 samples; Saturday proves the flat first Linear interval and later ramp as 10, 40, 85, 130, then 175 for 92 samples. The non-actual 2032-01-01 through 2032-01-03 gate proves one 288-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series and timestamps, raw EnergyPlus values/timestep timestamp fields, exact Environment and disabled-daylight-saving EIO rows, and 0 Warning/0 Severe completion. Rust retains source segments and interpolation, shares DayScheduleId after all Day:Hourly objects, and compiles immutable O(1) zone-timestep day values before annual lookup. The claim excludes Schedule:Day:List, Schedule:Week:Compact, an unaligned No warning, other shapes/timestep counts, Hourly report aggregation, type-limit and malformed/overlap/incomplete diagnostic parity, holidays/design/custom days, DST/tomorrow, EMS/currentVal, downstream gains/HVAC, warmup/actual-weather/multi-environment execution, Rust raw ESO, and broad ScheduleManager parity. Wider rejection and lookup branches are source/unit evidence only; parent routines remain incomplete."

[[algorithm]]
id = "schedule_day_list_modes"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|populateFromMinuteVals|populateFromMinuteVals|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleDayList",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_day_list_schedules",
  "crates/ep_runtime/src/schedules/day_table.rs::CompiledDayScheduleTable",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_day_list_modes_exact_001"
first_evidence = "calendar_schedule_day_list_modes_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_day_list_modes_exact_001: three Schedule:Day:List profiles selected on Thursday, Friday, and Saturday through one Schedule:Week:Daily and one full-year Schedule:Year at Timestep,4. Every profile has Minutes per Item 20 and source values 10, 70, 160, then 69 values of 175. Thursday blank/default No proves 10,70,160,160 then 175 for 92 samples; Friday Average proves 10,50,100,160 then 175 for 92 samples; Saturday Linear proves the EnergyPlus 26.1 source-actual behavior Linear equals No because Day:List repeats each item without constructing an interval ramp. The non-actual 2032-01-01 through 2032-01-03 gate proves one 288-sample ordered-exact-unique zero-tolerance Timestep Schedule Value series and timestamps, all 216 source values, raw EnergyPlus values/timestep timestamp fields, exact Environment and disabled-daylight-saving EIO rows, and 0 Warning/0 Severe completion. Rust preserves source order, shares DayScheduleId after Hourly and Interval, and compiles immutable O(1) zone-timestep day values before annual lookup. The claim excludes Schedule:Week:Compact, other Minutes per Item or timestep counts, blank-value and malformed-count diagnostic parity, type-limit warning parity, a documentation-intended Day:List Linear ramp, Hourly report aggregation, holidays/design/custom days, DST/tomorrow, EMS/currentVal, downstream gains/HVAC, warmup/actual-weather/multi-environment execution, Rust raw ESO, and broad ScheduleManager parity. Wider rejection and lookup branches are source/unit evidence only; parent routines remain incomplete."

[[algorithm]]
id = "schedule_week_compact_day_types"
domain = "time"
source_map = "docs/src/porting-map/time-weather-schedule.md"
energyplus_source = [
  "src/EnergyPlus/ScheduleManager.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/ScheduleManager.cc|ProcessScheduleInput|ProcessScheduleInput|Environment",
  "src/EnergyPlus/ScheduleManager.cc|ProcessForDayTypes|ProcessForDayTypes|Environment",
  "src/EnergyPlus/ScheduleManager.cc|getHrTsVal|getHrTsVal|Environment",
]
rust_target = [
  "crates/ep_model/src/objects/schedules.rs::ScheduleWeekCompact",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_week_compact_schedules",
  "crates/ep_runtime/src/schedules/day_table.rs::week_schedule_day_schedules",
  "crates/ep_runtime/src/schedules.rs::precompute_schedule_value_series_for_environment_time_axis",
]
status = "conformance"
first_case = "calendar_schedule_week_compact_day_types_exact_001"
first_evidence = "calendar_schedule_week_compact_day_types_exact_001"
proof_variables = [
  "Schedule Value",
]
claim_level = "limited-conformance"
support_boundary = "Limited EnergyPlus 26.1 conformance only for calendar_schedule_week_compact_day_types_exact_001: one source-ordered Schedule:Week:Compact maps Weekdays, Weekends, Holiday, SummerDesignDay, WinterDesignDay, CustomDay1, and CustomDay2 to seven resolved Schedule:Day profiles across the shared Hourly, Interval, and List namespace, then one full-year Schedule:Year selects that week. The non-actual 2032-01-01 through 2032-01-07 run at Timestep,4 produces one 672-sample ordered-exact-unique zero-tolerance Schedule Value series with daily values Thursday 11, Holiday 33, Saturday 22, SummerDesignDay 44, WinterDesignDay 55, CustomDay1 66, and CustomDay2 77, each repeated 96 times. The gate also locks all raw EnergyPlus values/timestep timestamp fields, the exact Environment, disabled-daylight-saving, five special-day, and resolved 12-slot WeekSchedule EIO rows, converted epJSON pair order, and 0 Warning/0 Severe completion. Rust assigns shared WeekScheduleId values after all Week:Daily objects and compiles Compact pairs into an immutable 12-slot day-pointer table used by Year lookup. The claim excludes AllDays, AllOtherDays, omitted For prefixes, arbitrary combined day-type lists, duplicate/missing/invalid selector and missing-reference diagnostic text/count/order parity, other pair orders or overwrite behavior, other timestep or day-profile shapes, Day interpolation revalidation, EPW holiday precedence, weekend shifting, overlapping special days, DST/tomorrow rollover, Year wrap/overlap/missing ranges, actual design-day environments, Hourly aggregation, actual weather, warmup/multi-environment execution, EMS/currentVal, downstream gains/HVAC, Rust raw ESO, and broad ScheduleManager parity. Wider selector and fail-closed branches are source/unit evidence only; parent routines remain incomplete."

[[algorithm]]
id = "air_side_node_state"
domain = "hvac"
source_map = "docs/src/porting-map/node-state-source-map.md"
energyplus_source = [
  "src/EnergyPlus/DataLoopNode.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
port_ticket_mappings = [
  "src/EnergyPlus/OutputProcessor.cc|SetupOutputVariable|SetupOutputVariable|Output",
]
rust_target = [
  "crates/ep_runtime/src/node/state.rs::NodeStateStore",
  "crates/ep_runtime/src/node/projection.rs::simulate_ideal_loads_node_state_projection",
]
status = "diagnostic_only"
first_case = "air_side_node_diagnostic_001"
first_evidence = "air_side_node_diagnostic_001"
proof_variables = [
  "System Node Temperature",
  "System Node Mass Flow Rate",
]
claim_level = "none"
support_boundary = "Diagnostic node-state projection only; no HVAC loop, node, or flow numerical conformance."

[[algorithm]]
id = "ideal_loads_air_system"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/PurchasedAirManager.hh",
  "src/EnergyPlus/ZoneEquipmentManager.cc",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
  "src/EnergyPlus/DataZoneEnergyDemands.hh",
  "src/EnergyPlus/DataLoopNode.hh",
  "src/EnergyPlus/ScheduleManager.hh",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
  "src/EnergyPlus/HVACSizingSimulationManager.cc",
]
rust_target = [
  "crates/ep_runtime/src/ideal_loads/input.rs::classify_no_oa_no_limit_sensible_subset",
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_compat",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/design_flow.rs::calc_scheduled_outdoor_air_mass_flow_rate_kg_per_s",
  "crates/ep_runtime/src/ideal_loads/update.rs::supply_node_update_from_result",
  "crates/ep_runtime/src/ideal_loads/report.rs::IdealLoadsReportSnapshot",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_HEATING_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_COOLING_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_HEATING_FUEL_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_COOLING_FUEL_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_HEATING_FUEL_ENERGY_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TOTAL_COOLING_FUEL_ENERGY_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_TOTAL_HEATING_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_TOTAL_COOLING_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_HEATING_FUEL_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_COOLING_FUEL_ENERGY",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_HEATING_FUEL_ENERGY_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_COOLING_FUEL_ENERGY_RATE",
  "crates/ep_runtime/src/zone_equipment/demand.rs::ZoneSysEnergyDemand",
  "crates/ep_runtime/src/node/mod.rs::IdealLoadsSupplyNodeUpdate",
  "crates/ep_runtime/src/output.rs::RuntimeMeterRegistry",
  "crates/ep_runtime/src/output/meter_registry.rs::meter_rate_to_energy_j",
  "crates/ep_compare/src/mtr.rs::load_mtr_time_series",
  "crates/ep_compare/src/mtr.rs::load_mtr_time_series_for_frequency",
  "crates/ep_cli/src/ideal_loads.rs::monthly_meter_samples_from_detailed_energy",
  "crates/ep_cli/src/ideal_loads.rs::meter_samples_from_detailed_energy",
  "crates/ep_cli/src/ideal_loads.rs::run_period_samples",
  "crates/ep_cli/src/ideal_loads/commands/mod.rs::generate_ideal_loads_no_oa_sensible_report",
  "scripts/compare/compare-ideal-loads-fuel-efficiency-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-blank-fuel-efficiency-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-constant-fuel-efficiency-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-non-constant-fuel-efficiency-conformance-candidate.ps1",
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_sensible_with_limits_compat",
  "scripts/compare/compare-ideal-loads-capacity-limit-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-capacity-limit-conformance.ps1",
  "scripts/compare/compare-ideal-loads-flow-limit-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-flow-limit-conformance.ps1",
  "scripts/compare/compare-ideal-loads-flow-capacity-limit-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-flow-capacity-limit-conformance.ps1",
  "crates/ep_runtime/src/output.rs::RuntimeMeterRegistry",
  "crates/ep_runtime/src/ideal_loads/meters.rs::ideal_loads_facility_meter_binding",
  "scripts/compare/compare-ideal-loads-no-oa-facility-meter-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-no-oa-facility-meter-monthly-run-period-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-no-oa-report-energy-conformance-candidate.ps1",
  "crates/ep_cli/src/ideal_loads/commands/mod.rs::generate_ideal_loads_outdoor_air_design_flow_report",
]
status = "conformance"
first_case = "ideal_loads_no_oa_sensible_conformance_001"
first_evidence = "ideal_loads_no_oa_sensible_conformance_001"
proof_variables = [
  "Zone Thermostat Heating Setpoint Temperature",
  "Zone Thermostat Cooling Setpoint Temperature",
  "Zone Ideal Loads Zone Total Heating Rate",
  "Zone Ideal Loads Zone Total Cooling Rate",
  "Zone Ideal Loads Zone Sensible Heating Rate",
  "Zone Ideal Loads Zone Sensible Cooling Rate",
  "Zone Ideal Loads Supply Air Total Heating Rate",
  "Zone Ideal Loads Supply Air Total Cooling Rate",
  "Zone Ideal Loads Supply Air Total Heating Energy",
  "Zone Ideal Loads Supply Air Total Cooling Energy",
  "Zone Ideal Loads Zone Total Heating Energy",
  "Zone Ideal Loads Zone Total Cooling Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy",
  "Zone Ideal Loads Zone Heating Fuel Energy",
  "Zone Ideal Loads Zone Cooling Fuel Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy Rate",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy Rate",
  "Zone Ideal Loads Zone Heating Fuel Energy Rate",
  "Zone Ideal Loads Zone Cooling Fuel Energy Rate",
  "System Node Temperature",
  "System Node Humidity Ratio",
  "System Node Mass Flow Rate",
]
claim_level = "limited-ideal-loads-no-oa-sensible-finite-limit-report-energy-blank-constant-non-constant-fuel-meter-conformance"
support_boundary = "Tolerance-gated no-OA/no-limit IdealLoads sensible conformance for declared conformance-level outputs in ideal_loads_no_oa_sensible_conformance_001, plus no-OA numeric capacity-limit, flow-limit, and flow-and-capacity-limit conformance for declared thermostat, IdealLoads rate, and supply-node temperature/mass-flow outputs in ideal_loads_capacity_limit_conformance_001, ideal_loads_flow_limit_conformance_001, and ideal_loads_flow_capacity_limit_conformance_001, plus no-OA ReportPurchasedAir rate-to-TimeStepSysSec and OutputProcessor Sum conformance for declared non-fuel energy rows in ideal_loads_no_oa_report_energy_conformance_candidate_001, plus no-OA blank fuel-efficiency conformance for declared fuel-energy rows in ideal_loads_blank_fuel_efficiency_conformance_candidate_001, plus no-OA constant Schedule:Constant fuel-efficiency conformance for declared fuel-energy rows in ideal_loads_constant_fuel_efficiency_conformance_candidate_001, plus no-OA all-days Schedule:Compact fuel-efficiency conformance for declared fuel-energy rows in ideal_loads_non_constant_fuel_efficiency_conformance_candidate_001, plus no-OA hourly DistrictHeatingWater:Facility and DistrictCooling:Facility meter conformance in ideal_loads_no_oa_facility_meter_conformance_candidate_001, no-OA hourly/monthly/run-period DistrictHeatingWater:Facility and DistrictCooling:Facility meter conformance in the four no-OA humidity-control conformance candidates, full-year annual DistrictHeatingWater:Facility and DistrictCooling:Facility meter conformance in ideal_loads_constant_supply_humidity_cooling_annual_meter_conformance_candidate_001, ideal_loads_constant_supply_humidity_heating_annual_meter_conformance_candidate_001, ideal_loads_humidistat_dehumidification_annual_meter_conformance_candidate_001, and ideal_loads_humidistat_humidification_annual_meter_conformance_candidate_001, plus no-OA monthly/annual/run-period DistrictHeatingWater:Facility and DistrictCooling:Facility meter conformance in ideal_loads_no_oa_facility_meter_monthly_run_period_conformance_candidate_001. Dedicated ledger entries below bound selected no-OA humidity-control, outdoor-air design-flow, economizer, and heat-recovery conformance; sizing, adaptive system timestep, fuel-efficiency schedules beyond the declared blank/constant/all-days Schedule:Compact candidates, broad meter conformance beyond the declared no-OA hourly facility meter rows, humidity-control hourly/monthly/run-period facility meter rows, the four full-year humidity-control annual facility meter candidates, and monthly/annual/run-period facility meter candidate, multi-year annual grouping, and broad HVAC compatibility remain outside the claim."

[[algorithm]]
id = "ideal_loads_zone_equipment_purchased_air_source_order"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/ZoneEquipmentManager.cc",
  "src/EnergyPlus/PurchasedAirManager.cc",
]
rust_target = [
  "crates/ep_runtime/src/zone_equipment/dispatch.rs::ideal_loads_zone_equipment_stages",
  "crates/ep_runtime/src/zone_equipment/dispatch.rs::validate_ideal_loads_zone_equipment_dispatch",
  "crates/ep_runtime/src/ideal_loads/dispatch.rs::purchased_air_source_order_stages",
  "crates/ep_runtime/src/ideal_loads/dispatch.rs::sim_purchased_air_compat",
]
status = "scaffold"
first_case = "ideal_loads_no_oa_sensible_conformance_001"
proof_variables = [
  "Zone Ideal Loads Zone Total Heating Rate",
  "System Node Temperature",
]
claim_level = "none"
support_boundary = "Source-order scaffold for ZoneEquipmentManager dispatch into PurchasedAirManager. It records ManageZoneEquipment, SimZoneEquipment, SimPurchasedAir, GetPurchasedAir, InitPurchasedAir, CalcPurchAirLoads, UpdatePurchasedAir, and ReportPurchasedAir barriers and does not add IdealLoads or HVAC conformance."
routine.manage_zone_equipment.source_file = "src/EnergyPlus/ZoneEquipmentManager.cc"
routine.manage_zone_equipment.source_routine = "ManageZoneEquipment"
routine.manage_zone_equipment.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.manage_zone_equipment.completion_status = "source_mapped"
routine.manage_zone_equipment.required_for_full_domain = true
routine.sim_zone_equipment.source_file = "src/EnergyPlus/ZoneEquipmentManager.cc"
routine.sim_zone_equipment.source_routine = "SimZoneEquipment"
routine.sim_zone_equipment.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.sim_zone_equipment.completion_status = "source_mapped"
routine.sim_zone_equipment.required_for_full_domain = true
routine.sim_purchased_air.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.sim_purchased_air.source_routine = "SimPurchasedAir"
routine.sim_purchased_air.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.sim_purchased_air.completion_status = "source_mapped"
routine.sim_purchased_air.required_for_full_domain = true
routine.get_purchased_air.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.get_purchased_air.source_routine = "GetPurchasedAir"
routine.get_purchased_air.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.get_purchased_air.completion_status = "source_mapped"
routine.get_purchased_air.required_for_full_domain = true
routine.init_purchased_air.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.init_purchased_air.source_routine = "InitPurchasedAir"
routine.init_purchased_air.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.init_purchased_air.completion_status = "source_mapped"
routine.init_purchased_air.required_for_full_domain = true
routine.calc_purch_air_loads.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.calc_purch_air_loads.source_routine = "CalcPurchAirLoads"
routine.calc_purch_air_loads.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.calc_purch_air_loads.completion_status = "source_mapped"
routine.calc_purch_air_loads.required_for_full_domain = true
routine.update_purchased_air.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.update_purchased_air.source_routine = "UpdatePurchasedAir"
routine.update_purchased_air.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.update_purchased_air.completion_status = "source_mapped"
routine.update_purchased_air.required_for_full_domain = true
routine.report_purchased_air.source_file = "src/EnergyPlus/PurchasedAirManager.cc"
routine.report_purchased_air.source_routine = "ReportPurchasedAir"
routine.report_purchased_air.source_map = "docs/src/porting-map/ideal-loads-source-map.md"
routine.report_purchased_air.completion_status = "source_mapped"
routine.report_purchased_air.required_for_full_domain = true

[[algorithm]]
id = "ideal_loads_constant_shr_humidity_control"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_with_recirculation_context_compat",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_SENSIBLE_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_SENSIBLE_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_COOLING_RATE",
  "crates/ep_runtime/src/runtime.rs::energyplus_psychrometric_humidity_ratio_from_rh",
  "crates/ep_cli/src/ideal_loads.rs::ideal_loads_barometric_pressure_trace",
  "scripts/compare/compare-ideal-loads-constant-shr-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-constant-shr-conformance.ps1",
]
status = "conformance"
first_case = "ideal_loads_constant_shr_conformance_001"
first_evidence = "ideal_loads_constant_shr_conformance_001"
proof_variables = [
  "Zone Ideal Loads Zone Total Cooling Rate",
  "Zone Ideal Loads Zone Sensible Cooling Rate",
  "Zone Ideal Loads Zone Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Sensible Cooling Rate",
  "Zone Ideal Loads Supply Air Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Total Cooling Rate",
  "System Node Temperature",
  "System Node Mass Flow Rate",
  "System Node Humidity Ratio",
]
claim_level = "limited-ideal-loads-no-oa-constant-shr-cooling-conformance"
support_boundary = "Tolerance-gated no-OA ConstantSensibleHeatRatio cooling conformance for declared cooling total/sensible/latent rate rows and supply-node temperature/mass-flow/humidity rows in ideal_loads_constant_shr_conformance_001. Heating rate rows, return-node and zone-air-node proof rows, ReportPurchasedAir energy/fuel rows, ConstantSupplyHumidityRatio, Humidistat, moisture-demand branches, outdoor-air humidity control, and broad humidity-control conformance remain outside the claim."

[[algorithm]]
id = "ideal_loads_constant_supply_humidity_control"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_with_recirculation_context_compat",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_SENSIBLE_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_SENSIBLE_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_COOLING_RATE",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_constant_supply_humidity_diagnostic",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_constant_supply_humidity_cooling_conformance",
  "scripts/compare/compare-ideal-loads-constant-supply-humidity-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-constant-supply-humidity-cooling-conformance-candidate.ps1",
]
status = "conformance"
first_case = "ideal_loads_constant_supply_humidity_cooling_conformance_candidate_001"
first_evidence = "ideal_loads_constant_supply_humidity_cooling_conformance_candidate_001"
proof_variables = [
  "Zone Ideal Loads Zone Total Heating Rate",
  "Zone Ideal Loads Zone Total Cooling Rate",
  "Zone Ideal Loads Zone Sensible Heating Rate",
  "Zone Ideal Loads Zone Sensible Cooling Rate",
  "Zone Ideal Loads Zone Latent Heating Rate",
  "Zone Ideal Loads Zone Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Sensible Heating Rate",
  "Zone Ideal Loads Supply Air Sensible Cooling Rate",
  "Zone Ideal Loads Supply Air Latent Heating Rate",
  "Zone Ideal Loads Supply Air Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Total Heating Rate",
  "Zone Ideal Loads Supply Air Total Cooling Rate",
  "System Node Temperature",
  "System Node Mass Flow Rate",
  "System Node Humidity Ratio",
  "Zone Ideal Loads Supply Air Total Heating Energy",
  "Zone Ideal Loads Supply Air Total Cooling Energy",
  "Zone Ideal Loads Zone Total Heating Energy",
  "Zone Ideal Loads Zone Total Cooling Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy",
  "Zone Ideal Loads Zone Heating Fuel Energy",
  "Zone Ideal Loads Zone Cooling Fuel Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy Rate",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy Rate",
  "Zone Ideal Loads Zone Heating Fuel Energy Rate",
  "Zone Ideal Loads Zone Cooling Fuel Energy Rate",
]
claim_level = "limited-ideal-loads-no-oa-constant-supply-humidity-cooling-conformance"
support_boundary = "Tolerance-gated no-OA ConstantSupplyHumidityRatio cooling conformance for declared thermostat, heating/cooling total/sensible/latent rate, supply-air heating/cooling report-rate, supply-node temperature/mass-flow/humidity, ReportPurchasedAir energy/fuel rows, and hourly/monthly/run-period facility meter rows in ideal_loads_constant_supply_humidity_cooling_conformance_candidate_001, plus full-year annual DistrictHeatingWater:Facility and DistrictCooling:Facility meter rows only in ideal_loads_constant_supply_humidity_cooling_annual_meter_conformance_candidate_001. Return-node and zone-air-node proof rows, annual facility meter rows in the short-run candidate, other broader meter frequencies, humidistat, moisture-demand branches, outdoor-air humidity control, and broad humidity-control conformance remain outside the claim."

[[algorithm]]
id = "ideal_loads_constant_supply_humidity_heating_control"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_with_recirculation_context_compat",
  "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::heating_supply_humidity_ratio",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_HEATING_RATE",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_constant_supply_humidity_humidification_diagnostic",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_constant_supply_humidity_heating_conformance",
  "scripts/compare/compare-ideal-loads-constant-supply-humidity-heating-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-constant-supply-humidity-heating-conformance-candidate.ps1",
]
status = "conformance"
first_case = "ideal_loads_constant_supply_humidity_heating_conformance_candidate_001"
first_evidence = "ideal_loads_constant_supply_humidity_heating_conformance_candidate_001"
proof_variables = [
  "Zone Ideal Loads Zone Total Heating Rate",
  "Zone Ideal Loads Zone Total Cooling Rate",
  "Zone Ideal Loads Zone Sensible Heating Rate",
  "Zone Ideal Loads Zone Sensible Cooling Rate",
  "Zone Ideal Loads Zone Latent Heating Rate",
  "Zone Ideal Loads Zone Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Sensible Heating Rate",
  "Zone Ideal Loads Supply Air Sensible Cooling Rate",
  "Zone Ideal Loads Supply Air Latent Heating Rate",
  "Zone Ideal Loads Supply Air Latent Cooling Rate",
  "Zone Ideal Loads Supply Air Total Heating Rate",
  "Zone Ideal Loads Supply Air Total Cooling Rate",
  "System Node Temperature",
  "System Node Humidity Ratio",
  "System Node Mass Flow Rate",
  "Zone Ideal Loads Supply Air Total Heating Energy",
  "Zone Ideal Loads Supply Air Total Cooling Energy",
  "Zone Ideal Loads Zone Total Heating Energy",
  "Zone Ideal Loads Zone Total Cooling Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy",
  "Zone Ideal Loads Zone Heating Fuel Energy",
  "Zone Ideal Loads Zone Cooling Fuel Energy",
  "Zone Ideal Loads Supply Air Total Heating Fuel Energy Rate",
  "Zone Ideal Loads Supply Air Total Cooling Fuel Energy Rate",
  "Zone Ideal Loads Zone Heating Fuel Energy Rate",
  "Zone Ideal Loads Zone Cooling Fuel Energy Rate",
]
claim_level = "limited-ideal-loads-no-oa-constant-supply-humidity-heating-conformance"
support_boundary = "Tolerance-gated no-OA ConstantSupplyHumidityRatio heating conformance for declared thermostat, heating/cooling total/sensible/latent rate, supply-air heating/cooling report-rate, supply-node temperature/mass-flow/humidity, ReportPurchasedAir energy/fuel rows, and hourly/monthly/run-period facility meter rows in ideal_loads_constant_supply_humidity_heating_conformance_candidate_001, plus full-year annual DistrictHeatingWater:Facility and DistrictCooling:Facility meter rows only in ideal_loads_constant_supply_humidity_heating_annual_meter_conformance_candidate_001. Return-node and zone-air-node proof rows, annual facility meter rows in the short-run candidate, other broader meter frequencies, humidistat, moisture-demand branches, outdoor-air humidity control, active economizer/heat recovery, finite-limit humidity behavior, and broad humidity-control conformance remain outside the claim."

[[algorithm]]
id = "ideal_loads_humidistat_dehumidification_control"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/DataZoneEnergyDemands.cc",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
    "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_with_recirculation_context_compat",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::calc_no_oa_third_order_moisture_demand_compat",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::third_order_humidity_history_term",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::correct_no_oa_third_order_humidity_ratio_compat",
    "crates/ep_runtime/src/ideal_loads/humidistat.rs::NoOaHumidistatClosedLoopState",
    "crates/ep_runtime/src/ideal_loads/humidistat.rs::advance_no_oa_humidistat_zone_timestep_compat",
    "crates/ep_runtime/src/ideal_loads/calc/humidity.rs::humidistat_dehumidification_mass_flow_rate_kg_per_s",
    "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::heating_section_dehumidification_mass_flow_rate_kg_per_s",
    "crates/ep_runtime/src/ideal_loads/calc/humidity.rs::heating_supply_humidity_ratio",
    "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::coil_loads_from_states",
    "crates/ep_runtime/src/zone_equipment/demand.rs::ZoneSysEnergyDemand",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_SYSTEM_PREDICTED_HUMIDIFYING_MOISTURE_LOAD",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_SYSTEM_PREDICTED_DEHUMIDIFYING_MOISTURE_LOAD",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_COOLING_RATE",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_COOLING_RATE",
    "crates/ep_compiler/src/compiler.rs::parse_zone_humidistats",
    "crates/ep_cli/src/ideal_loads.rs::manifest_allows_humidistat_dehumidification_diagnostic",
    "crates/ep_cli/src/ideal_loads.rs::manifest_allows_humidistat_dehumidification_conformance",
    "crates/ep_cli/src/ideal_loads.rs::manifest_allows_humidistat_dehumidification_annual_meter_conformance",
    "scripts/compare/compare-ideal-loads-humidistat-dehumidification-diagnostic.ps1",
    "scripts/compare/compare-ideal-loads-humidistat-dehumidification-conformance-candidate.ps1",
    "scripts/compare/compare-ideal-loads-humidity-annual-meter-conformance-candidate.ps1",
  ]
status = "conformance"
first_case = "ideal_loads_humidistat_dehumidification_conformance_candidate_001"
first_evidence = "ideal_loads_humidistat_dehumidification_conformance_candidate_001"
proof_variables = [
    "Zone System Predicted Moisture Load to Humidifying Setpoint Moisture Transfer Rate",
    "Zone System Predicted Moisture Load to Dehumidifying Setpoint Moisture Transfer Rate",
    "Zone Ideal Loads Zone Total Heating Rate",
    "Zone Ideal Loads Zone Total Cooling Rate",
    "Zone Ideal Loads Zone Sensible Heating Rate",
    "Zone Ideal Loads Zone Sensible Cooling Rate",
    "Zone Ideal Loads Zone Latent Heating Rate",
    "Zone Ideal Loads Zone Latent Cooling Rate",
    "Zone Ideal Loads Supply Air Sensible Heating Rate",
    "Zone Ideal Loads Supply Air Sensible Cooling Rate",
    "Zone Ideal Loads Supply Air Latent Heating Rate",
    "Zone Ideal Loads Supply Air Latent Cooling Rate",
    "Zone Ideal Loads Supply Air Total Heating Rate",
    "Zone Ideal Loads Supply Air Total Cooling Rate",
    "System Node Humidity Ratio",
    "System Node Mass Flow Rate",
    "Zone Ideal Loads Supply Air Total Heating Energy",
    "Zone Ideal Loads Supply Air Total Cooling Energy",
    "Zone Ideal Loads Zone Total Heating Energy",
    "Zone Ideal Loads Zone Total Cooling Energy",
    "Zone Ideal Loads Supply Air Total Heating Fuel Energy",
    "Zone Ideal Loads Supply Air Total Cooling Fuel Energy",
    "Zone Ideal Loads Zone Heating Fuel Energy",
    "Zone Ideal Loads Zone Cooling Fuel Energy",
    "Zone Ideal Loads Supply Air Total Heating Fuel Energy Rate",
    "Zone Ideal Loads Supply Air Total Cooling Fuel Energy Rate",
    "Zone Ideal Loads Zone Heating Fuel Energy Rate",
    "Zone Ideal Loads Zone Cooling Fuel Energy Rate",
]
claim_level = "limited-ideal-loads-no-oa-humidistat-dehumidification-conformance"
support_boundary = "Tolerance-gated no-OA Humidistat dehumidification conformance for declared thermostat, heating/cooling total/sensible/latent rate, supply-air heating/cooling report-rate, supply-node temperature/mass-flow/humidity, paired trace-driven moisture-demand rows, ReportPurchasedAir energy/fuel rows, and hourly/monthly/run-period facility meter rows in ideal_loads_humidistat_dehumidification_conformance_candidate_001, plus full-year annual DistrictHeatingWater:Facility and DistrictCooling:Facility meter rows only in ideal_loads_humidistat_dehumidification_annual_meter_conformance_candidate_001. The runtime owns each seeded fixed zone-timestep predictor-to-corrector history transition; adaptive or multiple system substeps remain outside this boundary, and EnergyPlus still supplies the warmup seed histories, sensible load, temperatures, latent-gain and relative-humidity schedule inputs, and pressure. The no-OA ThirdOrder schedule-to-setpoint predictor rows are conformance rows for the declared short candidate, but fully owned EnergyPlus WPrevZoneTSTemp/warmup/system-history closure, humidistat humidification, outdoor-air humidity control, active economizer/heat recovery, finite-limit humidistat behavior, annual facility meter rows in the short-run candidate, other broader meter frequencies, and broad humidity-control conformance remain outside the claim."

[[algorithm]]
id = "ideal_loads_humidistat_humidification_control"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/DataZoneEnergyDemands.cc",
  "src/EnergyPlus/ZoneTempPredictorCorrector.cc",
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
    "crates/ep_runtime/src/ideal_loads/calc/no_oa.rs::calc_no_oa_no_limit_sensible_with_recirculation_context_compat",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::calc_no_oa_third_order_moisture_demand_compat",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::third_order_humidity_history_term",
    "crates/ep_runtime/src/ideal_loads/calc/moisture_demand.rs::correct_no_oa_third_order_humidity_ratio_compat",
    "crates/ep_runtime/src/ideal_loads/humidistat.rs::NoOaHumidistatClosedLoopState",
    "crates/ep_runtime/src/ideal_loads/humidistat.rs::advance_no_oa_humidistat_zone_timestep_compat",
    "crates/ep_runtime/src/ideal_loads/calc/humidity.rs::humidistat_humidification_mass_flow_rate_kg_per_s",
    "crates/ep_runtime/src/ideal_loads/calc/humidity.rs::heating_supply_humidity_ratio",
    "crates/ep_runtime/src/zone_equipment/demand.rs::ZoneSysEnergyDemand",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_SYSTEM_PREDICTED_HUMIDIFYING_MOISTURE_LOAD",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ZONE_LATENT_HEATING_RATE",
    "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_LATENT_HEATING_RATE",
    "crates/ep_compiler/src/compiler.rs::parse_zone_humidistats",
    "crates/ep_cli/src/ideal_loads.rs::manifest_allows_humidistat_humidification_diagnostic",
    "crates/ep_cli/src/ideal_loads.rs::manifest_allows_humidistat_humidification_conformance",
    "scripts/compare/compare-ideal-loads-humidistat-humidification-diagnostic.ps1",
    "scripts/compare/compare-ideal-loads-humidistat-humidification-conformance-candidate.ps1",
]
status = "conformance"
first_case = "ideal_loads_humidistat_humidification_conformance_candidate_001"
first_evidence = "ideal_loads_humidistat_humidification_conformance_candidate_001"
proof_variables = [
    "Zone System Predicted Moisture Load to Humidifying Setpoint Moisture Transfer Rate",
    "Zone System Predicted Moisture Load to Dehumidifying Setpoint Moisture Transfer Rate",
    "Zone Ideal Loads Zone Total Heating Rate",
    "Zone Ideal Loads Zone Total Cooling Rate",
    "Zone Ideal Loads Zone Sensible Heating Rate",
    "Zone Ideal Loads Zone Sensible Cooling Rate",
    "Zone Ideal Loads Zone Latent Heating Rate",
    "Zone Ideal Loads Zone Latent Cooling Rate",
    "Zone Ideal Loads Supply Air Sensible Heating Rate",
    "Zone Ideal Loads Supply Air Sensible Cooling Rate",
    "Zone Ideal Loads Supply Air Latent Heating Rate",
    "Zone Ideal Loads Supply Air Latent Cooling Rate",
    "Zone Ideal Loads Supply Air Total Heating Rate",
    "Zone Ideal Loads Supply Air Total Cooling Rate",
    "System Node Humidity Ratio",
    "System Node Mass Flow Rate",
    "Zone Ideal Loads Supply Air Total Heating Energy",
    "Zone Ideal Loads Supply Air Total Cooling Energy",
    "Zone Ideal Loads Zone Total Heating Energy",
    "Zone Ideal Loads Zone Total Cooling Energy",
    "Zone Ideal Loads Supply Air Total Heating Fuel Energy",
    "Zone Ideal Loads Supply Air Total Cooling Fuel Energy",
    "Zone Ideal Loads Zone Heating Fuel Energy",
    "Zone Ideal Loads Zone Cooling Fuel Energy",
    "Zone Ideal Loads Supply Air Total Heating Fuel Energy Rate",
    "Zone Ideal Loads Supply Air Total Cooling Fuel Energy Rate",
    "Zone Ideal Loads Zone Heating Fuel Energy Rate",
    "Zone Ideal Loads Zone Cooling Fuel Energy Rate",
]
claim_level = "limited-ideal-loads-no-oa-humidistat-humidification-conformance"
support_boundary = "Tolerance-gated no-OA Humidistat humidification conformance for declared thermostat, heating/cooling total/sensible/latent rate, supply-air heating/cooling report-rate, supply-node temperature/mass-flow/humidity, paired trace-driven moisture-demand rows, ReportPurchasedAir energy/fuel rows, and hourly/monthly/run-period facility meter rows in ideal_loads_humidistat_humidification_conformance_candidate_001, plus full-year annual DistrictHeatingWater:Facility and DistrictCooling:Facility meter rows only in ideal_loads_humidistat_humidification_annual_meter_conformance_candidate_001. The runtime owns each seeded fixed zone-timestep predictor-to-corrector history transition; adaptive or multiple system substeps remain outside this boundary, and EnergyPlus still supplies the warmup seed histories, sensible load, temperatures, latent-gain and relative-humidity schedule inputs, and pressure. The no-OA ThirdOrder schedule-to-setpoint predictor rows are conformance rows for the declared short candidate, but fully owned EnergyPlus WPrevZoneTSTemp/warmup/system-history closure, outdoor-air humidity control, active economizer/heat recovery, finite-limit humidistat behavior, annual facility meter rows in the short-run candidate, other broader meter frequencies, and broad humidity-control conformance remain outside the claim."

[[algorithm]]
id = "ideal_loads_outdoor_air_design_flow"
domain = "hvac"
source_map = "docs/src/porting-map/ideal-loads-source-map.md"
energyplus_source = [
  "src/EnergyPlus/PurchasedAirManager.cc",
  "src/EnergyPlus/DataSizing.cc",
  "src/EnergyPlus/DataEnvironment.hh",
  "src/EnergyPlus/OutputProcessor.cc",
]
rust_target = [
  "crates/ep_runtime/src/ideal_loads/outdoor_air.rs::sim_purchased_air_outdoor_air_compat",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/minimum_flow.rs::resolve_minimum_outdoor_air_compat",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/design_flow.rs::design_outdoor_air_volume_flow_components_m3_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/dcv.rs::occupancy_schedule_dcv_outdoor_air_volume_flow_components_m3_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/design_flow.rs::calc_design_outdoor_air_volume_flow_m3_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/design_flow.rs::calc_scheduled_outdoor_air_mass_flow_rate_kg_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/dcv.rs::calc_occupancy_schedule_dcv_outdoor_air_mass_flow_rate_kg_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/dcv.rs::calc_co2_setpoint_dcv_outdoor_air_mass_flow_rate_kg_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/economizer.rs::calc_economizer_adjusted_outdoor_air_mass_flow_rate_kg_per_s",
  "crates/ep_runtime/src/ideal_loads/outdoor_air/psychrometrics.rs::heat_recovery_saturation_adjusted_state",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_MASS_FLOW_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_STANDARD_DENSITY_VOLUME_FLOW_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_LATENT_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_TOTAL_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_OUTDOOR_AIR_TOTAL_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_MASS_FLOW_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_STANDARD_DENSITY_VOLUME_FLOW_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_TEMPERATURE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_SUPPLY_AIR_HUMIDITY_RATIO",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_MIXED_AIR_TEMPERATURE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_MIXED_AIR_HUMIDITY_RATIO",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_SENSIBLE_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_LATENT_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_TOTAL_HEATING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_SENSIBLE_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_LATENT_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_TOTAL_COOLING_RATE",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_ECONOMIZER_ACTIVE_TIME",
  "crates/ep_runtime/src/ideal_loads/report.rs::ZONE_IDEAL_LOADS_HEAT_RECOVERY_ACTIVE_TIME",
  "crates/ep_cli/src/ideal_loads/commands/mod.rs::generate_ideal_loads_outdoor_air_design_flow_report",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_flow_zone_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_flow_person_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_occupancy_dcv_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_co2_dcv_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_flow_area_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_air_changes_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_sum_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_maximum_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_differential_dry_bulb_economizer_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_differential_enthalpy_economizer_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_sensible_heat_recovery_conformance_manifest",
  "crates/ep_cli/src/ideal_loads.rs::manifest_allows_outdoor_air_enthalpy_heat_recovery_conformance_manifest",
  "scripts/compare/compare-ideal-loads-outdoor-air-flow-person-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-flow-person-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-occupancy-dcv-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-co2-dcv-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-design-flow-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-flow-zone-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-flow-area-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-air-changes-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-sum-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-maximum-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-differential-dry-bulb-economizer-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-differential-enthalpy-economizer-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-sensible-heat-recovery-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-enthalpy-heat-recovery-conformance-candidate.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-differential-dry-bulb-economizer-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-differential-enthalpy-economizer-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-flow-area-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-air-changes-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-sum-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-maximum-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-sensible-heat-recovery-diagnostic.ps1",
  "scripts/compare/compare-ideal-loads-outdoor-air-enthalpy-heat-recovery-diagnostic.ps1",
]
status = "conformance"
first_case = "ideal_loads_outdoor_air_flow_zone_conformance_candidate_001"
first_evidence = "ideal_loads_outdoor_air_flow_zone_conformance_candidate_001"
proof_variables = [
  "Zone Ideal Loads Outdoor Air Mass Flow Rate",
  "Zone Ideal Loads Outdoor Air Standard Density Volume Flow Rate",
  "Zone Ideal Loads Outdoor Air Sensible Heating Rate",
  "Zone Ideal Loads Outdoor Air Sensible Cooling Rate",
  "Zone Ideal Loads Outdoor Air Latent Heating Rate",
  "Zone Ideal Loads Outdoor Air Latent Cooling Rate",
  "Zone Ideal Loads Outdoor Air Total Heating Rate",
  "Zone Ideal Loads Outdoor Air Total Cooling Rate",
  "Zone Ideal Loads Supply Air Mass Flow Rate",
  "Zone Ideal Loads Supply Air Standard Density Volume Flow Rate",
  "Zone Ideal Loads Supply Air Temperature",
  "Zone Ideal Loads Supply Air Humidity Ratio",
  "Zone Ideal Loads Mixed Air Temperature",
  "Zone Ideal Loads Mixed Air Humidity Ratio",
  "Zone Ideal Loads Heat Recovery Sensible Heating Rate",
  "Zone Ideal Loads Heat Recovery Latent Heating Rate",
  "Zone Ideal Loads Heat Recovery Total Heating Rate",
  "Zone Ideal Loads Heat Recovery Sensible Cooling Rate",
  "Zone Ideal Loads Heat Recovery Latent Cooling Rate",
  "Zone Ideal Loads Heat Recovery Total Cooling Rate",
  "Zone Ideal Loads Economizer Active Time",
  "Zone Ideal Loads Heat Recovery Active Time",
]
claim_level = "limited-ideal-loads-outdoor-air-flow-zone-flow-person-occupancy-dcv-co2-dcv-flow-area-air-changes-sum-maximum-differential-dry-bulb-differential-enthalpy-economizer-sensible-enthalpy-heat-recovery-conformance"
support_boundary = "Tolerance-gated IdealLoads outdoor-air Flow/Zone, Flow/Person, Flow/Person OccupancySchedule DCV, Flow/Person CO2Setpoint DCV, Flow/Area, AirChanges/Hour, Sum, Maximum, Flow/Zone DifferentialDryBulb/DifferentialEnthalpy economizer, and Flow/Zone Sensible/Enthalpy heat-recovery conformance for declared mass flow, standard-density volume flow, no-humidity outdoor-air sensible/latent/total report rates, supply-air state, mixed-air state, economizer active-time, heat-recovery active-time, and heat-recovery rate rows in ideal_loads_outdoor_air_flow_zone_conformance_candidate_001, ideal_loads_outdoor_air_flow_person_conformance_candidate_001, ideal_loads_outdoor_air_occupancy_dcv_conformance_candidate_001, ideal_loads_outdoor_air_co2_dcv_conformance_candidate_001, ideal_loads_outdoor_air_flow_area_conformance_candidate_001, ideal_loads_outdoor_air_air_changes_conformance_candidate_001, ideal_loads_outdoor_air_sum_conformance_candidate_001, ideal_loads_outdoor_air_maximum_conformance_candidate_001, ideal_loads_outdoor_air_differential_dry_bulb_economizer_conformance_candidate_001, ideal_loads_outdoor_air_differential_enthalpy_economizer_conformance_candidate_001, ideal_loads_outdoor_air_sensible_heat_recovery_conformance_candidate_001, and ideal_loads_outdoor_air_enthalpy_heat_recovery_conformance_candidate_001. The OccupancySchedule DCV candidate covers current People schedule occupancy only for the declared Flow/Person outdoor-air minimum-flow adjustment. The CO2Setpoint DCV candidate covers the declared Flow/Person max(minimum OA, ZoneSysContDemand OutputRequiredToCO2SP) adjustment using the EnergyPlus Zone Air CO2 Predicted Load to Setpoint Mass Flow Rate proof input. General heat-recovery saturation-limit branch parity, broader DCV combinations, active humidity controls, People heat gains, CO2 contaminant-balance conformance, CO2 concentration conformance, and broad OA conformance remain outside the claim."

[[algorithm]]
id = "plant_loop_state_projection"
domain = "plant"
source_map = "docs/src/porting-map/plant-source-map.md"
energyplus_source = [
  "src/EnergyPlus/Plant/PlantManager.cc",
  "src/EnergyPlus/Plant/Loop.cc",
  "src/EnergyPlus/PlantUtilities.cc",
]
rust_target = [
  "crates/ep_runtime/src/plant/state.rs::PlantStateStore",
  "crates/ep_runtime/src/plant/state.rs::simulate_plant_state_projection",
]
status = "diagnostic_only"
first_case = "plant_loop_diagnostic_001"
first_evidence = "plant_loop_diagnostic_001"
proof_variables = [
  "Plant Supply Side Inlet Temperature",
  "Plant Supply Side Outlet Temperature",
]
claim_level = "none"
support_boundary = "Diagnostic plant-state projection only; no plant loop, equipment, flow, or load numerical conformance."
routine.manage_plant_loops.source_file = "src/EnergyPlus/Plant/PlantManager.cc"
routine.manage_plant_loops.source_routine = "ManagePlantLoops"
routine.manage_plant_loops.source_map = "docs/src/porting-map/plant-source-map.md"
routine.manage_plant_loops.completion_status = "source_mapped"
routine.manage_plant_loops.required_for_full_domain = true

[[algorithm]]
id = "psychrometric_routine_inventory"
domain = "psychrometrics"
source_map = "docs/src/porting-map/psychrometrics-source-map.md"
energyplus_source = [
  "src/EnergyPlus/Psychrometrics.hh",
  "src/EnergyPlus/Psychrometrics.cc",
]
rust_target = [
  "crates/ep_runtime/src/psychrometrics.rs",
]
status = "scaffold"
first_case = "official_1zone_uncontrolled_dynamic_conformance_candidate_001"
proof_variables = [
  "Site Outdoor Air Wetbulb Temperature",
  "Zone Mean Air Humidity Ratio",
]
claim_level = "none"
support_boundary = "Source-order scaffold for the 53 logical namespace-level EnergyPlus 26.1 Psychrometrics free-function identifiers. This inventory does not add implementation completion, numerical equivalence, conformance evidence, a psychrometrics domain claim, or any project-contract required routine."

routine.initialize_psych_routines.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.initialize_psych_routines.source_routine = "InitializePsychRoutines"
routine.initialize_psych_routines.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.initialize_psych_routines.completion_status = "source_mapped"
routine.initialize_psych_routines.required_for_full_domain = false

routine.show_psychrometric_summary.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.show_psychrometric_summary.source_routine = "ShowPsychrometricSummary"
routine.show_psychrometric_summary.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.show_psychrometric_summary.completion_status = "source_mapped"
routine.show_psychrometric_summary.required_for_full_domain = false

routine.psy_rho_air_fn_pb_tdb_w_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_rho_air_fn_pb_tdb_w_error.source_routine = "PsyRhoAirFnPbTdbW_error"
routine.psy_rho_air_fn_pb_tdb_w_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rho_air_fn_pb_tdb_w_error.completion_status = "source_mapped"
routine.psy_rho_air_fn_pb_tdb_w_error.required_for_full_domain = false

routine.psy_rho_air_fn_pb_tdb_w.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rho_air_fn_pb_tdb_w.source_routine = "PsyRhoAirFnPbTdbW"
routine.psy_rho_air_fn_pb_tdb_w.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rho_air_fn_pb_tdb_w.completion_status = "state_mapped"
routine.psy_rho_air_fn_pb_tdb_w.required_for_full_domain = false
routine.psy_rho_air_fn_pb_tdb_w.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rho_air_fn_pb_tdb_w.read_state = [
  "arguments `pb`, `tdb`, and `dw`; `CalledFrom` and `EnergyPlusData &state` are read only by the enabled negative-density diagnostics path",
]
routine.psy_rho_air_fn_pb_tdb_w.write_state = [
  "ordinary formula and stateless overload write no state; enabled negative-density diagnostics mutate the EnergyPlus error stream and terminate through the fatal-error path",
]
routine.psy_rho_air_fn_pb_tdb_w.history_state_ownership = "no cross-call history or cache; density output is a pure function of `pb`, `tdb`, `max(dw, 1.0e-5)`, and `Constant::Kelvin` before optional diagnostics"
routine.psy_rho_air_fn_pb_tdb_w.unsupported_state = [
  "`EP_psych_errors` severe/continue/timestamp/fatal diagnostic state delegated to `PsyRhoAirFnPbTdbW_error`",
]
routine.psy_rho_air_fn_pb_tdb_w.inactive_branches = [
  "when `EP_psych_errors` is disabled, the state argument and `CalledFrom` are unused and the negative-density diagnostic branch is compiled out",
]
routine.psy_rho_air_fn_pb_tdb_w.unsupported_active_branches = [
  "stateful overload negative-density branch with severe, input-context continuation, caller-or-Unknown timestamp, and fatal termination when `EP_psych_errors` is enabled",
]
routine.psy_rho_air_fn_pb_tdb_w.not_claimed_branches = [
  "numerical density parity, humidity-floor and nonphysical-input edges, overload parity, and all diagnostic side effects",
]

routine.psy_rho_air_fn_pb_tdb_w_fast.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rho_air_fn_pb_tdb_w_fast.source_routine = "PsyRhoAirFnPbTdbW_fast"
routine.psy_rho_air_fn_pb_tdb_w_fast.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rho_air_fn_pb_tdb_w_fast.completion_status = "state_mapped"
routine.psy_rho_air_fn_pb_tdb_w_fast.required_for_full_domain = false
routine.psy_rho_air_fn_pb_tdb_w_fast.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rho_air_fn_pb_tdb_w_fast.read_state = [
  "arguments `pb`, `tdb`, and `dw`; the debug assertion reads `dw`, and enabled `EP_psych_errors` diagnostics inspect a negative `rhoair` result",
]
routine.psy_rho_air_fn_pb_tdb_w_fast.write_state = [
  "the numerical formula writes no state; enabled negative-density diagnostics mutate the EnergyPlus error stream and terminate through `PsyRhoAirFnPbTdbW_error`",
]
routine.psy_rho_air_fn_pb_tdb_w_fast.history_state_ownership = "no cross-call numerical history or cache; the source result is a pure function of `pb`, `tdb`, and already-adjusted `dw` before optional diagnostics"
routine.psy_rho_air_fn_pb_tdb_w_fast.unsupported_state = [
  "`EP_psych_errors` severe/continue/Unknown-timestamp/fatal diagnostic state and exact C++ assertion-abort diagnostics",
]
routine.psy_rho_air_fn_pb_tdb_w_fast.inactive_branches = [
  "`NDEBUG` removes the `dw >= 1.0e-5` assertion; disabling `EP_psych_errors` removes the negative-density diagnostic branch",
]
routine.psy_rho_air_fn_pb_tdb_w_fast.unsupported_active_branches = [
  "assertion-enabled invalid-`dw` termination parity and enabled negative-density severe/continue/Unknown-timestamp/fatal behavior",
]
routine.psy_rho_air_fn_pb_tdb_w_fast.not_claimed_branches = [
  "external EnergyPlus numerical parity, invalid-precondition behavior outside the fast domain, diagnostic side effects, and C++ abort versus Rust panic equivalence",
]

routine.psy_hfg_air_fn_w_tdb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_hfg_air_fn_w_tdb.source_routine = "PsyHfgAirFnWTdb"
routine.psy_hfg_air_fn_w_tdb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_hfg_air_fn_w_tdb.completion_status = "state_mapped"
routine.psy_hfg_air_fn_w_tdb.required_for_full_domain = false
routine.psy_hfg_air_fn_w_tdb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_hfg_air_fn_w_tdb.read_state = [
  "arguments `w` and `T`; `w` is intentionally ignored and `T` is read through source `max(T, 0.0)` semantics",
]
routine.psy_hfg_air_fn_w_tdb.write_state = [
  "no state; the routine computes the gas enthalpy term minus the fluid enthalpy term without mutation",
]
routine.psy_hfg_air_fn_w_tdb.history_state_ownership = "no cross-call history or cache; output depends only on `max(T, 0.0)` and the source evaluation order"
routine.psy_hfg_air_fn_w_tdb.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_hfg_air_fn_w_tdb.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_hfg_air_fn_w_tdb.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_hfg_air_fn_w_tdb.not_claimed_branches = [
  "external EnergyPlus numerical parity, full IEEE-edge parity, and downstream latent-energy integration",
]

routine.psy_hg_air_fn_w_tdb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_hg_air_fn_w_tdb.source_routine = "PsyHgAirFnWTdb"
routine.psy_hg_air_fn_w_tdb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_hg_air_fn_w_tdb.completion_status = "state_mapped"
routine.psy_hg_air_fn_w_tdb.required_for_full_domain = false
routine.psy_hg_air_fn_w_tdb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_hg_air_fn_w_tdb.read_state = [
  "arguments `w` and `T`; `w` is intentionally ignored and `T` supplies the linear water-vapor gas enthalpy term",
]
routine.psy_hg_air_fn_w_tdb.write_state = [
  "no state; the routine evaluates `2500940.0 + 1858.95 * T` without mutation",
]
routine.psy_hg_air_fn_w_tdb.history_state_ownership = "no cross-call history or cache; output is a pure function of `T`"
routine.psy_hg_air_fn_w_tdb.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_hg_air_fn_w_tdb.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_hg_air_fn_w_tdb.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_hg_air_fn_w_tdb.not_claimed_branches = [
  "external EnergyPlus numerical parity, ignored-`w` parity across all call sites, and full IEEE-edge parity",
]

routine.psy_h_fn_tdb_w.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_h_fn_tdb_w.source_routine = "PsyHFnTdbW"
routine.psy_h_fn_tdb_w.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_w.completion_status = "state_mapped"
routine.psy_h_fn_tdb_w.required_for_full_domain = false
routine.psy_h_fn_tdb_w.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_w.read_state = [
  "arguments `TDB` and `dW`; source `max(dW, 1.0e-5)` preserves a NaN first argument and supplies the humidity-ratio floor",
]
routine.psy_h_fn_tdb_w.write_state = [
  "no state; the dry-air and humidity-weighted enthalpy terms are evaluated in source order without mutation",
]
routine.psy_h_fn_tdb_w.history_state_ownership = "no cross-call history or cache; output is a pure function of `TDB` and floored `dW`"
routine.psy_h_fn_tdb_w.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_h_fn_tdb_w.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_h_fn_tdb_w.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_h_fn_tdb_w.not_claimed_branches = [
  "external EnergyPlus numerical parity, full humidity/temperature edge parity, inverse round trips, and downstream IdealLoads replacement",
]

routine.psy_h_fn_tdb_w_fast.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_h_fn_tdb_w_fast.source_routine = "PsyHFnTdbW_fast"
routine.psy_h_fn_tdb_w_fast.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_w_fast.completion_status = "state_mapped"
routine.psy_h_fn_tdb_w_fast.required_for_full_domain = false
routine.psy_h_fn_tdb_w_fast.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_w_fast.read_state = [
  "arguments `TDB` and already-adjusted `dW`; an assertion reads `dW >= 1.0e-5` before the unclamped formula",
]
routine.psy_h_fn_tdb_w_fast.write_state = [
  "no state; the unclamped enthalpy formula evaluates without mutation after the assertion",
]
routine.psy_h_fn_tdb_w_fast.history_state_ownership = "no cross-call history or cache; output is a pure function of `TDB` and caller-adjusted `dW`"
routine.psy_h_fn_tdb_w_fast.unsupported_state = [
  "exact C++ assertion-abort diagnostics; the numerical source routine has no mutable state",
]
routine.psy_h_fn_tdb_w_fast.inactive_branches = [
  "`NDEBUG` removes the `dW >= 1.0e-5` assertion and leaves the raw unclamped expression active",
]
routine.psy_h_fn_tdb_w_fast.unsupported_active_branches = [
  "assertion-enabled invalid-`dW` termination parity and exact C++ abort versus Rust panic behavior",
]
routine.psy_h_fn_tdb_w_fast.not_claimed_branches = [
  "external EnergyPlus numerical parity and invalid-precondition behavior outside the documented fast domain",
]

routine.psy_cp_air_fn_w.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_cp_air_fn_w.source_routine = "PsyCpAirFnW"
routine.psy_cp_air_fn_w.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_cp_air_fn_w.completion_status = "state_mapped"
routine.psy_cp_air_fn_w.required_for_full_domain = false
routine.psy_cp_air_fn_w.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_cp_air_fn_w.read_state = [
  "argument `dw` and function-local static `dwSave`/`cpaSave`, both initialized to `-100.0`, are read before the cache-hit comparison",
]
routine.psy_cp_air_fn_w.write_state = [
  "cache miss writes `dwSave = dw` and `cpaSave = 1.00484e3 + max(dw, 1.0e-5) * 1.85895e3`; cache hit writes no state",
]
routine.psy_cp_air_fn_w.history_state_ownership = "EnergyPlus owns one function-local static last-call cache shared across calls and simulation states; Rust currently owns no mutable cache for this routine"
routine.psy_cp_air_fn_w.unsupported_state = [
  "the function-local `dwSave`/`cpaSave` cache, including first-call `dw == -100.0` sentinel collision that returns `-100.0`",
]
routine.psy_cp_air_fn_w.inactive_branches = [
  "none; the last-call cache is unconditional in the pinned EnergyPlus 26.1.0 source",
]
routine.psy_cp_air_fn_w.unsupported_active_branches = [
  "cache hit/miss history behavior, cross-simulation/process sharing, and sentinel-collision behavior; the cache is output-neutral for physical humidity-ratio inputs",
]
routine.psy_cp_air_fn_w.not_claimed_branches = [
  "external EnergyPlus numerical parity, C++ last-call-cache work/history parity under repeated or alternating calls, sentinel collision, and state/thread-isolation behavior",
]

routine.psy_cp_air_fn_w_fast.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_cp_air_fn_w_fast.source_routine = "PsyCpAirFnW_fast"
routine.psy_cp_air_fn_w_fast.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_cp_air_fn_w_fast.completion_status = "state_mapped"
routine.psy_cp_air_fn_w_fast.required_for_full_domain = false
routine.psy_cp_air_fn_w_fast.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_cp_air_fn_w_fast.read_state = [
  "argument `dw` is checked by `assert(dw >= 1.0e-5)` before function-local static `dwSave`/`cpaSave`, both initialized to `-100.0`, are read for an exact cache hit",
]
routine.psy_cp_air_fn_w_fast.write_state = [
  "cache miss writes `dwSave = dw` and `cpaSave = 1.00484e3 + dw * 1.85895e3`; cache hit writes no state",
]
routine.psy_cp_air_fn_w_fast.history_state_ownership = "EnergyPlus owns one function-local static last-call cache shared across calls and simulation states; Rust keeps the valid-domain numerical helper pure"
routine.psy_cp_air_fn_w_fast.unsupported_state = [
  "the function-local `dwSave`/`cpaSave` cache, cross-simulation sharing, concurrency policy, and the `NDEBUG` first-call `dw == -100.0` sentinel collision",
]
routine.psy_cp_air_fn_w_fast.inactive_branches = [
  "`NDEBUG` removes the pre-cache `dw >= 1.0e-5` assertion; the last-call cache itself is unconditional",
]
routine.psy_cp_air_fn_w_fast.unsupported_active_branches = [
  "cache hit/miss history, process-wide sharing, sentinel-collision behavior, and exact assertion-enabled C++ abort versus Rust panic parity",
]
routine.psy_cp_air_fn_w_fast.not_claimed_branches = [
  "external EnergyPlus numerical parity, C++ cache work/history parity, sentinel collision, and state/thread-isolation behavior",
]

routine.psy_tdb_fn_h_w.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_tdb_fn_h_w.source_routine = "PsyTdbFnHW"
routine.psy_tdb_fn_h_w.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdb_fn_h_w.completion_status = "state_mapped"
routine.psy_tdb_fn_h_w.required_for_full_domain = false
routine.psy_tdb_fn_h_w.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdb_fn_h_w.read_state = [
  "arguments `H` and `dW`; source `max(dW, 1.0e-5)` preserves a NaN first argument and supplies the humidity-ratio floor",
]
routine.psy_tdb_fn_h_w.write_state = [
  "no state; the dry-bulb inverse is evaluated from `H` and floored `dW` without mutation",
]
routine.psy_tdb_fn_h_w.history_state_ownership = "no cross-call history or cache; output is a pure function of `H` and floored `dW`"
routine.psy_tdb_fn_h_w.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_tdb_fn_h_w.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_tdb_fn_h_w.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_tdb_fn_h_w.not_claimed_branches = [
  "external EnergyPlus numerical parity, full IEEE-edge parity, and downstream IdealLoads inverse replacement",
]

routine.psy_rhov_fn_tdb_rh_lbnd0c.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rhov_fn_tdb_rh_lbnd0c.source_routine = "PsyRhovFnTdbRhLBnd0C"
routine.psy_rhov_fn_tdb_rh_lbnd0c.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_rh_lbnd0c.completion_status = "state_mapped"
routine.psy_rhov_fn_tdb_rh_lbnd0c.required_for_full_domain = false
routine.psy_rhov_fn_tdb_rh_lbnd0c.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_rh_lbnd0c.read_state = [
  "arguments `Tdb` and `RH` exactly as supplied; despite the historical `LBnd0C` name, the routine applies neither a 0 C temperature lower bound nor RH validation/clamping",
]
routine.psy_rhov_fn_tdb_rh_lbnd0c.write_state = [
  "no state; the pressure-independent exponential vapor-density formula evaluates without mutation",
]
routine.psy_rhov_fn_tdb_rh_lbnd0c.history_state_ownership = "no cross-call history or cache; output is a pure function of raw `Tdb`, raw `RH`, `Constant::Kelvin`, and the source exponential expression"
routine.psy_rhov_fn_tdb_rh_lbnd0c.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_rhov_fn_tdb_rh_lbnd0c.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_rhov_fn_tdb_rh_lbnd0c.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_rhov_fn_tdb_rh_lbnd0c.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform `std::exp` last-bit and floating-point exception parity, and inverse-relative-humidity integration",
]

routine.psy_rhov_fn_tdb_w_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rhov_fn_tdb_w_pb.source_routine = "PsyRhovFnTdbWPb"
routine.psy_rhov_fn_tdb_w_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_w_pb.completion_status = "state_mapped"
routine.psy_rhov_fn_tdb_w_pb.required_for_full_domain = false
routine.psy_rhov_fn_tdb_w_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_w_pb.read_state = [
  "arguments `Tdb`, `dW`, and `PB`; source `max(dW, 1.0e-5)` preserves a NaN first argument and supplies the humidity-ratio floor",
]
routine.psy_rhov_fn_tdb_w_pb.write_state = [
  "no state; the vapor-density rational expression evaluates without mutation",
]
routine.psy_rhov_fn_tdb_w_pb.history_state_ownership = "no cross-call history or cache; output is a pure function of `Tdb`, `PB`, and floored `dW`"
routine.psy_rhov_fn_tdb_w_pb.unsupported_state = [
  "none; the source routine has no mutable state or cache",
]
routine.psy_rhov_fn_tdb_w_pb.inactive_branches = [
  "none; the routine is always present and has no compile-time variant",
]
routine.psy_rhov_fn_tdb_w_pb.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_rhov_fn_tdb_w_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, full nonphysical-input/IEEE-edge parity, inverse-relative-humidity integration, and downstream surface or EMPD migration",
]

routine.psy_rhov_fn_tdb_w_pb_fast.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rhov_fn_tdb_w_pb_fast.source_routine = "PsyRhovFnTdbWPb_fast"
routine.psy_rhov_fn_tdb_w_pb_fast.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_w_pb_fast.completion_status = "state_mapped"
routine.psy_rhov_fn_tdb_w_pb_fast.required_for_full_domain = false
routine.psy_rhov_fn_tdb_w_pb_fast.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_w_pb_fast.read_state = [
  "arguments `Tdb`, caller-adjusted `dW`, and `PB`; an assertion reads `dW >= 1.0e-5` before the unclamped formula",
]
routine.psy_rhov_fn_tdb_w_pb_fast.write_state = [
  "no state; the unclamped vapor-density rational expression evaluates without mutation after the assertion",
]
routine.psy_rhov_fn_tdb_w_pb_fast.history_state_ownership = "no cross-call history or cache; output is a pure function of `Tdb`, `PB`, and caller-adjusted `dW`"
routine.psy_rhov_fn_tdb_w_pb_fast.unsupported_state = [
  "exact C++ assertion-abort diagnostics; the numerical source routine has no mutable state",
]
routine.psy_rhov_fn_tdb_w_pb_fast.inactive_branches = [
  "`NDEBUG` removes the `dW >= 1.0e-5` assertion and leaves the raw unclamped expression active",
]
routine.psy_rhov_fn_tdb_w_pb_fast.unsupported_active_branches = [
  "assertion-enabled invalid-`dW` termination parity and exact C++ abort versus Rust panic behavior",
]
routine.psy_rhov_fn_tdb_w_pb_fast.not_claimed_branches = [
  "external EnergyPlus numerical parity, C++ `NDEBUG` versus Rust debug-assertion build-policy equivalence, invalid-precondition behavior outside the documented fast domain, and downstream fast-call-site migration",
]

routine.psy_rh_fn_tdb_rhov_lbnd0c_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_rh_fn_tdb_rhov_lbnd0c_error.source_routine = "PsyRhFnTdbRhovLBnd0C_error"
routine.psy_rh_fn_tdb_rhov_lbnd0c_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov_lbnd0c_error.completion_status = "source_mapped"
routine.psy_rh_fn_tdb_rhov_lbnd0c_error.required_for_full_domain = false

routine.psy_rh_fn_tdb_rhov_lbnd0c.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rh_fn_tdb_rhov_lbnd0c.source_routine = "PsyRhFnTdbRhovLBnd0C"
routine.psy_rh_fn_tdb_rhov_lbnd0c.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov_lbnd0c.completion_status = "state_mapped"
routine.psy_rh_fn_tdb_rhov_lbnd0c.required_for_full_domain = false
routine.psy_rh_fn_tdb_rhov_lbnd0c.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov_lbnd0c.read_state = [
  "arguments `Tdb` and `Rhovapor`; `EP_psych_stats` reads the per-state call counter, while `EP_psych_errors` reads `WarmupFlag` and the shared error index and reads `CalledFrom` only for the first non-warmup extreme-range warning",
]
routine.psy_rh_fn_tdb_rhov_lbnd0c.write_state = [
  "the numerical formula and return correction write no state; `EP_psych_stats` increments the per-state call count, while the enabled extreme-range helper mutates warning, scratch-string, error-index, and recurring-warning state only outside warmup",
]
routine.psy_rh_fn_tdb_rhov_lbnd0c.history_state_ownership = "numerical output is a pure function of raw `Tdb` and `Rhovapor`; optional call statistics and one shared high/low recurring-warning history belong to each `EnergyPlusData` instance"
routine.psy_rh_fn_tdb_rhov_lbnd0c.unsupported_state = [
  "`EP_psych_stats` call counting plus `EP_psych_errors` warmup suppression, scratch string, warning stream and totals, caller timestamp, shared recurrence index/count/min/max, SQLite, and callback state",
]
routine.psy_rh_fn_tdb_rhov_lbnd0c.inactive_branches = [
  "disabling `EP_psych_stats` compiles out the call-count increment; disabling `EP_psych_errors` compiles out the extreme-range helper call and routine 16 itself without changing the numerical correction",
]
routine.psy_rh_fn_tdb_rhov_lbnd0c.unsupported_active_branches = [
  "default errors-enabled strict `< -0.05` or `> 1.01` warning flow, first-versus-recurring behavior, shared high/low recurrence, and the optional statistics-enabled every-call counter",
]
routine.psy_rh_fn_tdb_rhov_lbnd0c.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform `std::exp` last-bit and floating-point exception parity, exact diagnostic formatting/side effects, statistics history, and downstream surface, EMPD, room-air, or EMS migration",
]

routine.psy_twb_fn_tdb_w_pb.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_twb_fn_tdb_w_pb.source_routine = "PsyTwbFnTdbWPb"
routine.psy_twb_fn_tdb_w_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_twb_fn_tdb_w_pb.completion_status = "source_mapped"
routine.psy_twb_fn_tdb_w_pb.required_for_full_domain = false

routine.psy_twb_fn_tdb_w_pb_raw.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_twb_fn_tdb_w_pb_raw.source_routine = "PsyTwbFnTdbWPb_raw"
routine.psy_twb_fn_tdb_w_pb_raw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_twb_fn_tdb_w_pb_raw.completion_status = "source_mapped"
routine.psy_twb_fn_tdb_w_pb_raw.required_for_full_domain = false

routine.psy_v_fn_tdb_w_pb_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_v_fn_tdb_w_pb_error.source_routine = "PsyVFnTdbWPb_error"
routine.psy_v_fn_tdb_w_pb_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_v_fn_tdb_w_pb_error.completion_status = "source_mapped"
routine.psy_v_fn_tdb_w_pb_error.required_for_full_domain = false

routine.psy_v_fn_tdb_w_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_v_fn_tdb_w_pb.source_routine = "PsyVFnTdbWPb"
routine.psy_v_fn_tdb_w_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_v_fn_tdb_w_pb.completion_status = "state_mapped"
routine.psy_v_fn_tdb_w_pb.required_for_full_domain = false
routine.psy_v_fn_tdb_w_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_v_fn_tdb_w_pb.read_state = [
  "arguments `TDB`, `dW`, and `PB`; `EP_psych_stats` reads the per-state call counter, while `EP_psych_errors` reads `WarmupFlag` and the dedicated specific-volume error index and reads `CalledFrom` only for the first non-warmup `V <= -0.01` warning",
]
routine.psy_v_fn_tdb_w_pb.write_state = [
  "the numerical formula and `V < 0.0` fallback write no state; `EP_psych_stats` increments the per-state call count, while `PsyVFnTdbWPb_error` mutates warning, scratch-string, error-index, and recurring-warning state only outside warmup",
]
routine.psy_v_fn_tdb_w_pb.history_state_ownership = "numerical output is a pure function of raw `TDB`, source-floored `dW`, and raw `PB`; optional call statistics and one specific-volume recurring-warning history belong to each `EnergyPlusData` instance"
routine.psy_v_fn_tdb_w_pb.unsupported_state = [
  "`EP_psych_stats` call counting plus `EP_psych_errors` warmup suppression, scratch string, warning stream and totals, caller timestamp, dedicated recurrence index/count/min/max, SQLite, and callback state",
]
routine.psy_v_fn_tdb_w_pb.inactive_branches = [
  "disabling `EP_psych_stats` compiles out the call-count increment; disabling `EP_psych_errors` compiles out the `V <= -0.01` helper call and routine 20 without changing the unconditional `V < 0.0` fallback to `0.83`",
]
routine.psy_v_fn_tdb_w_pb.unsupported_active_branches = [
  "default errors-enabled `V <= -0.01` warning flow, first-versus-recurring behavior, caller context, per-state recurrence, and the optional statistics-enabled every-call counter",
]
routine.psy_v_fn_tdb_w_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform floating-point last-bit and exception parity, exact diagnostic formatting/side effects, statistics history, and downstream EMS, C API, or HVAC call-site migration",
]

routine.psy_w_fn_tdb_h_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_w_fn_tdb_h_error.source_routine = "PsyWFnTdbH_error"
routine.psy_w_fn_tdb_h_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_h_error.completion_status = "source_mapped"
routine.psy_w_fn_tdb_h_error.required_for_full_domain = false

routine.psy_w_fn_tdb_h.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_w_fn_tdb_h.source_routine = "PsyWFnTdbH"
routine.psy_w_fn_tdb_h.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_h.completion_status = "state_mapped"
routine.psy_w_fn_tdb_h.required_for_full_domain = false
routine.psy_w_fn_tdb_h.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_h.read_state = [
  "arguments `TDB` and `H`; `EP_psych_stats` reads the per-state call counter; with `EP_psych_errors` enabled, `SuppressWarnings` is read only after a calculated `W <= -0.0001`, and routine 22 reads `WarmupFlag` and the dedicated humidity-ratio error index only for `W < -0.0001`, then reads `CalledFrom` only for the first such non-warmup warning",
]
routine.psy_w_fn_tdb_h.write_state = [
  "the numerical formula and ordered `W < 0.0` fallback write no state; `EP_psych_stats` increments the per-state call count, while routine 22 mutates warning, scratch-string, error-index, and recurring-warning state only for non-warmup `W < -0.0001`",
]
routine.psy_w_fn_tdb_h.history_state_ownership = "numerical output is a pure function of raw `TDB` and `H`; `CalledFrom` and `SuppressWarnings` never change the result, while optional call statistics and one humidity-ratio recurring-warning history belong to each `EnergyPlusData` instance"
routine.psy_w_fn_tdb_h.unsupported_state = [
  "`EP_psych_stats` call counting plus `EP_psych_errors` warmup suppression, scratch string, warning stream and totals, caller timestamp, dedicated recurrence index/count/min/max with `[]` units, SQLite, and callback state",
]
routine.psy_w_fn_tdb_h.inactive_branches = [
  "disabling `EP_psych_stats` compiles out the call-count increment; disabling `EP_psych_errors` compiles out the diagnostic dispatch and routine 22 without changing the unconditional floor of every strictly negative `W` to `1.0e-5`",
  "with errors enabled, `SuppressWarnings = true` skips diagnostic dispatch for significant negative `W`, while warmup suppresses routine-22 messages and mutations; neither branch changes the numerical return",
]
routine.psy_w_fn_tdb_h.unsupported_active_branches = [
  "default errors-enabled dispatch at `W <= -0.0001 && !SuppressWarnings`, including the routine-22 strict `W < -0.0001` recheck that makes exact `-0.0001` a diagnostic no-op, plus first-versus-recurring behavior, caller context, per-state recurrence, and the optional statistics-enabled every-call counter",
]
routine.psy_w_fn_tdb_h.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform floating-point last-bit and exception parity, exact diagnostic formatting/side effects, statistics history, equivalence with existing ideal-loads approximations, and downstream C API, EMS, sizing, coil, or HVAC call-site migration",
]

routine.psy_psat_fn_temp_raw.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_psat_fn_temp_raw.source_routine = "PsyPsatFnTemp_raw"
routine.psy_psat_fn_temp_raw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_psat_fn_temp_raw.completion_status = "state_mapped"
routine.psy_psat_fn_temp_raw.required_for_full_domain = false
routine.psy_psat_fn_temp_raw.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_psat_fn_temp_raw.read_state = [
  "arguments `T` and `CalledFrom`; `EP_psych_stats` reads the per-state raw saturation-pressure call counter, while `EP_psych_errors` reads `WarmupFlag` on every raw evaluation and, for non-warmup `T <= -100.0` or `T >= 200.0`, reads the dedicated saturation-pressure error index and reads `CalledFrom` only for the first such warning",
]
routine.psy_psat_fn_temp_raw.write_state = [
  "the piecewise saturation-pressure calculation writes no state; `EP_psych_stats` increments the per-state raw call counter before the range check, while `EP_psych_errors` mutates warning, error-index, and recurring-warning state only for out-of-range calls outside warmup",
]
routine.psy_psat_fn_temp_raw.history_state_ownership = "the numerical result has no cross-call history and is a pure function of raw `T` plus the selected non-IF97 or `EP_IF97` compile branch; `CalledFrom` never changes the result, while optional raw-call statistics and one saturation-pressure recurring-warning history belong to each `EnergyPlusData` instance"
routine.psy_psat_fn_temp_raw.unsupported_state = [
  "`EP_psych_stats` raw-call counting plus `EP_psych_errors` warmup suppression, warning stream and totals, caller timestamp, dedicated recurrence index/count/min/max with `C` units, SQLite, and callback state",
]
routine.psy_psat_fn_temp_raw.inactive_branches = [
  "with `EP_nocache_Psychrometrics`, the separately named routine 24 is absent and the same body is compiled as public `PsyPsatFnTemp` on the original unquantized input",
  "disabling `EP_psych_stats` removes the raw-call increment, while disabling `EP_psych_errors` removes the inclusive endpoint/out-of-range warning flow without changing the pressure result",
  "defining `EP_IF97` replaces the liquid-water polynomial and the above-200 C constant while leaving the ice branch unchanged; the standard EnergyPlus build does not select this branch",
]
routine.psy_psat_fn_temp_raw.unsupported_active_branches = [
  "default errors-enabled warnings for `T <= -100.0` and `T >= 200.0`, including warmup suppression, first-versus-recurring behavior, caller context, and per-state recurrence, plus the optional statistics-enabled every-raw-call counter",
]
routine.psy_psat_fn_temp_raw.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform `exp`/`log` last-bit and floating-point exception parity, the `EP_IF97` variant, exact diagnostic/statistics side effects, cached-wrapper or no-cache-public-interface parity, and downstream C API, EMS, moisture, weather, or HVAC call-site migration",
]

routine.psy_psat_fn_temp.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_psat_fn_temp.source_routine = "PsyPsatFnTemp"
routine.psy_psat_fn_temp.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_psat_fn_temp.completion_status = "source_mapped"
routine.psy_psat_fn_temp.required_for_full_domain = false

routine.psy_tsat_fn_h_pb_raw.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_tsat_fn_h_pb_raw.source_routine = "PsyTsatFnHPb_raw"
routine.psy_tsat_fn_h_pb_raw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tsat_fn_h_pb_raw.completion_status = "source_mapped"
routine.psy_tsat_fn_h_pb_raw.required_for_full_domain = false

routine.psy_tsat_fn_h_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_tsat_fn_h_pb.source_routine = "PsyTsatFnHPb"
routine.psy_tsat_fn_h_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tsat_fn_h_pb.completion_status = "source_mapped"
routine.psy_tsat_fn_h_pb.required_for_full_domain = false

routine.psy_rhov_fn_tdb_rh.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rhov_fn_tdb_rh.source_routine = "PsyRhovFnTdbRh"
routine.psy_rhov_fn_tdb_rh.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_rh.completion_status = "state_mapped"
routine.psy_rhov_fn_tdb_rh.required_for_full_domain = false
routine.psy_rhov_fn_tdb_rh.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rhov_fn_tdb_rh.read_state = [
  "arguments `Tdb`, `RH`, and `CalledFrom`; the default build reads the routine-25 signed temperature tag, direct-mapped cache entry, and optional public lookup counter, while a miss reads raw saturation-pressure statistics, warmup, range-warning history, and caller context",
]
routine.psy_rhov_fn_tdb_rh.write_state = [
  "the outer ideal-gas arithmetic writes no state; every cached saturation-pressure lookup may increment its optional wrapper counter, while a miss overwrites one cache entry and may update raw-call statistics and non-warmup range-warning state",
]
routine.psy_rhov_fn_tdb_rh.history_state_ownership = "for ordinary finite inputs, the default non-IF97 numerical projection is a deterministic function of original `Tdb` and `RH` plus the tag-derived saturation-pressure representative; cache hit, miss, collision, initialization, and warning/statistics history belong to each `EnergyPlusData` instance"
routine.psy_rhov_fn_tdb_rh.unsupported_state = [
  "the routine-25 cache array and lifecycle, signed tag/hash/sentinel/collision behavior, public and raw counters, warmup suppression, warning stream and totals, caller timestamp, recurrence state, SQLite, and callback state",
]
routine.psy_rhov_fn_tdb_rh.inactive_branches = [
  "`EP_nocache_Psychrometrics` removes the nested cache and named raw interface, evaluates saturation pressure at original unquantized `Tdb`, and can change ordinary non-grid results",
  "disabling `EP_psych_stats` removes nested counters; disabling `EP_psych_errors` removes nested range diagnostics; defining `EP_IF97` replaces the liquid-water saturation-pressure branch",
]
routine.psy_rhov_fn_tdb_rh.unsupported_active_branches = [
  "the default cache lookup, representative-temperature raw miss, hit suppression of `CalledFrom` and raw side effects, sentinel/collision/lifecycle behavior, errors-enabled out-of-range diagnostics, and optional statistics-enabled counters",
]
routine.psy_rhov_fn_tdb_rh.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, exact cache or diagnostic/statistics side effects, and C API, EMS, HAMT, EMPD, surface, or other downstream migration",
]

routine.psy_rh_fn_tdb_rhov_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_rh_fn_tdb_rhov_error.source_routine = "PsyRhFnTdbRhov_error"
routine.psy_rh_fn_tdb_rhov_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov_error.completion_status = "source_mapped"
routine.psy_rh_fn_tdb_rhov_error.required_for_full_domain = false

routine.psy_rh_fn_tdb_rhov.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rh_fn_tdb_rhov.source_routine = "PsyRhFnTdbRhov"
routine.psy_rh_fn_tdb_rhov.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov.completion_status = "state_mapped"
routine.psy_rh_fn_tdb_rhov.required_for_full_domain = false
routine.psy_rh_fn_tdb_rhov.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_rhov.read_state = [
  "arguments `Tdb`, `Rhovapor`, and `CalledFrom`; optional statistics read the routine-30 counter on every call, positive vapor reads routine-25 cache/raw state with a fixed nested caller name, and extreme raw RH reads warmup plus the shared routine-29 warning index and external caller context",
]
routine.psy_rh_fn_tdb_rhov.write_state = [
  "the numerical calculation and ordered correction write no state; optional statistics increment the routine-30 counter before the positivity gate, positive vapor may mutate nested saturation-pressure cache/statistics/diagnostics state, and non-warmup extreme raw RH may mutate the shared high/low warning history",
]
routine.psy_rh_fn_tdb_rhov.history_state_ownership = "for a fixed compile variant, the numerical projection is an input function, while the routine-25 hit/miss history and routine-29 shared high/low recurrence history affect nested side effects and belong to each `EnergyPlusData` instance"
routine.psy_rh_fn_tdb_rhov.unsupported_state = [
  "the routine-30 call counter; routine-25 cache, lifecycle, counters, and range diagnostics; routine-29 warmup suppression, warning stream and totals, scratch string, caller timestamp, shared recurrence index/count/min/max, SQLite, and callback state",
]
routine.psy_rh_fn_tdb_rhov.inactive_branches = [
  "nonpositive or NaN `Rhovapor` skips saturation pressure and routine-29 dispatch but does not skip the optional routine-30 call counter",
  "`EP_nocache_Psychrometrics` evaluates exact-temperature saturation pressure on every positive-vapor call; disabling statistics or errors removes the corresponding state mutations; `EP_IF97` changes the liquid-water dependency",
]
routine.psy_rh_fn_tdb_rhov.unsupported_active_branches = [
  "default cached saturation-pressure lookup and hit/miss side effects, optional every-call statistics, and default errors-enabled dispatch only for strict raw `RH < -0.05` or `RH > 1.01` with warmup and shared high/low recurrence",
]
routine.psy_rh_fn_tdb_rhov.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, exact diagnostic/statistics/cache behavior, threshold nextafter messaging, and C API, EMS, HAMT, EMPD, room-air, surface, or other downstream migration",
]

routine.psy_rh_fn_tdb_w_pb_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_rh_fn_tdb_w_pb_error.source_routine = "PsyRhFnTdbWPb_error"
routine.psy_rh_fn_tdb_w_pb_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_w_pb_error.completion_status = "source_mapped"
routine.psy_rh_fn_tdb_w_pb_error.required_for_full_domain = false

routine.psy_rh_fn_tdb_w_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_rh_fn_tdb_w_pb.source_routine = "PsyRhFnTdbWPb"
routine.psy_rh_fn_tdb_w_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_w_pb.completion_status = "state_mapped"
routine.psy_rh_fn_tdb_w_pb.required_for_full_domain = false
routine.psy_rh_fn_tdb_w_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_rh_fn_tdb_w_pb.read_state = [
  "arguments `TDB`, `dW`, `PB`, and `CalledFrom`; optional statistics read the routine-32 counter before all work, every call reads routine-25 signed tag/hash/cache-entry and wrapper-counter state, a miss reads raw saturation-pressure statistics/range-warning state plus nested caller context, and an extreme raw RH reads routine-31 warmup/shared-error-index and external-caller state",
]
routine.psy_rh_fn_tdb_w_pb.write_state = [
  "the humidity floor, unsimplified formula, and ordered correction write no state; optional own statistics increment on every call before saturation pressure, the nested lookup may increment its wrapper counter and on a miss overwrite a cache entry and mutate raw statistics/range diagnostics, and a non-warmup extreme raw RH may mutate routine-31 warning, scratch-string, and shared recurrence state",
]
routine.psy_rh_fn_tdb_w_pb.history_state_ownership = "ordinary finite default non-IF97 output is a deterministic function of the original inputs and tag-derived saturation-pressure representative; cache hit/miss/collision history changes nested side effects, the reachable negative-NaN tag `-1000` can make full-source numerical output history-dependent through the fresh-entry sentinel, and cache plus high/low recurrence state belong to each `EnergyPlusData` instance"
routine.psy_rh_fn_tdb_w_pb.unsupported_state = [
  "the routine-32 call counter; routine-25 cache array, lifecycle, signed tag/hash/sentinel/collision behavior, public/raw counters, range diagnostics, warmup, and nested caller; routine-31 warning stream/totals, scratch string, caller timestamp, shared recurrence index/count/min/max, SQLite, and callback state",
]
routine.psy_rh_fn_tdb_w_pb.inactive_branches = [
  "an in-range raw RH skips routine-31 dispatch but never skips the optional own counter or unconditional saturation-pressure lookup; disabling statistics or errors removes the corresponding mutations",
  "`EP_nocache_Psychrometrics` evaluates saturation pressure at original unquantized `TDB` on every call and can change ordinary non-grid results; `EP_IF97` changes the liquid-water dependency",
]
routine.psy_rh_fn_tdb_w_pb.unsupported_active_branches = [
  "the default cached saturation-pressure lookup, representative miss, hit suppression of nested raw side effects/caller context, negative-NaN sentinel and lifecycle behavior, optional every-call statistics, and default errors-enabled strict `RH < -0.05` or `RH > 1.01` flow with warmup and shared high/low recurrence",
]
routine.psy_rh_fn_tdb_w_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, full IEEE and negative-NaN sentinel parity, exact diagnostic/statistics/cache behavior, threshold nextafter messaging, API or EMS dispatch, and coil, heat-recovery, thermal-comfort, weather, zone, or other downstream migration",
]

routine.psy_w_fn_tdp_pb_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_w_fn_tdp_pb_error.source_routine = "PsyWFnTdpPb_error"
routine.psy_w_fn_tdp_pb_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdp_pb_error.completion_status = "source_mapped"
routine.psy_w_fn_tdp_pb_error.required_for_full_domain = false

routine.psy_w_fn_tdp_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_w_fn_tdp_pb.source_routine = "PsyWFnTdpPb"
routine.psy_w_fn_tdp_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdp_pb.completion_status = "source_mapped"
routine.psy_w_fn_tdp_pb.required_for_full_domain = false

routine.psy_w_fn_tdb_rh_pb_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_w_fn_tdb_rh_pb_error.source_routine = "PsyWFnTdbRhPb_error"
routine.psy_w_fn_tdb_rh_pb_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_rh_pb_error.completion_status = "source_mapped"
routine.psy_w_fn_tdb_rh_pb_error.required_for_full_domain = false

routine.psy_w_fn_tdb_rh_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_w_fn_tdb_rh_pb.source_routine = "PsyWFnTdbRhPb"
routine.psy_w_fn_tdb_rh_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_rh_pb.completion_status = "state_mapped"
routine.psy_w_fn_tdb_rh_pb.required_for_full_domain = false
routine.psy_w_fn_tdb_rh_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_rh_pb.read_state = [
  "arguments `TDB`, `RH`, `PB`, and `CalledFrom`; optional statistics read the routine-36 counter before all work, every call reads routine-25 signed tag/hash/cache-entry and wrapper-counter state, a miss reads raw saturation-pressure statistics/range-warning state plus nested caller context, and raw `W <= -0.0001` reads routine-35 warmup/error-index and external-caller state",
]
routine.psy_w_fn_tdb_rh_pb.write_state = [
  "the ordered denominator and humidity-ratio floors write no state; optional own statistics increment on every call before saturation pressure, the nested lookup may increment its wrapper counter and on a miss overwrite a cache entry and mutate raw statistics/range diagnostics, and non-warmup raw `W <= -0.0001` may mutate routine-35 warning/scratch state plus a recurring record text-deduplicated with routine 38",
]
routine.psy_w_fn_tdb_rh_pb.history_state_ownership = "ordinary finite default non-IF97 output is a deterministic function of the original inputs and tag-derived saturation-pressure representative; cache hit/miss/collision history changes nested side effects, the reachable negative-NaN tag `-1000` can make full-source numerical output history-dependent through the fresh-entry sentinel, and the routine-35 index plus its title-deduplicated shared routine-38 recurrence history belong to each `EnergyPlusData` instance"
routine.psy_w_fn_tdb_rh_pb.unsupported_state = [
  "the routine-36 call counter; routine-25 cache array, lifecycle, signed tag/hash/sentinel/collision behavior, public/raw counters, range diagnostics, warmup, and nested caller; routine-35 warning stream/totals, scratch string, caller timestamp, error index, first-detail gate, title-deduplicated routine-38 recurrence count/min/max, SQLite, and callback state",
]
routine.psy_w_fn_tdb_rh_pb.inactive_branches = [
  "raw `W > -0.0001` or NaN skips routine-35 dispatch but never skips the optional own counter or unconditional saturation-pressure lookup; raw `W < 1.0e-5` still takes the numerical return floor",
  "disabling statistics or errors removes the corresponding mutations; `EP_nocache_Psychrometrics` evaluates saturation pressure at original unquantized `TDB` on every call, and `EP_IF97` changes the liquid-water dependency",
]
routine.psy_w_fn_tdb_rh_pb.unsupported_active_branches = [
  "the default cached saturation-pressure lookup, representative miss, hit suppression of nested raw side effects/caller context, negative-NaN sentinel and lifecycle behavior, optional every-call statistics, and default errors-enabled inclusive raw `W <= -0.0001` flow with warmup, first/recurring diagnostics, and cross-routine-38 recurring-title aliasing",
]
routine.psy_w_fn_tdb_rh_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, full IEEE and negative-NaN sentinel parity, exact diagnostic/statistics/cache behavior, threshold nextafter messaging, the EMS historical fourth-Null operand, API or EMS dispatch, and weather, heat-balance, IdealLoads, HVAC, coil, thermal-comfort, or other downstream migration",
]

routine.psy_w_fn_tdb_twb_pb_temperature_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_w_fn_tdb_twb_pb_temperature_error.source_routine = "PsyWFnTdbTwbPb_temperature_error"
routine.psy_w_fn_tdb_twb_pb_temperature_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_twb_pb_temperature_error.completion_status = "source_mapped"
routine.psy_w_fn_tdb_twb_pb_temperature_error.required_for_full_domain = false

routine.psy_w_fn_tdb_twb_pb_humidity_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_w_fn_tdb_twb_pb_humidity_error.source_routine = "PsyWFnTdbTwbPb_humidity_error"
routine.psy_w_fn_tdb_twb_pb_humidity_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_twb_pb_humidity_error.completion_status = "source_mapped"
routine.psy_w_fn_tdb_twb_pb_humidity_error.required_for_full_domain = false

routine.psy_w_fn_tdb_twb_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_w_fn_tdb_twb_pb.source_routine = "PsyWFnTdbTwbPb"
routine.psy_w_fn_tdb_twb_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_twb_pb.completion_status = "state_mapped"
routine.psy_w_fn_tdb_twb_pb.required_for_full_domain = false
routine.psy_w_fn_tdb_twb_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_w_fn_tdb_twb_pb.read_state = [
  "arguments `TDB`, `TWBin`, `PB`, and `CalledFrom`; optional statistics read the routine-39 counter before the clamp; strict `TWBin > TDB + 0.01` reads routine-37 `ReportErrors`, warmup, `WFnTdbTwbPb` error index, global recurrence, scratch, and external-caller state; every call reads routine-25 signed tag/hash/cache-entry and wrapper-counter state at the corrected wet bulb, while a miss reads raw saturation-pressure statistics/range-warning state and nested caller context; raw `W < 0.0` reads routine-38 `ReportErrors`, warmup, `WFnTdbTwbPb2` index, title-deduplicated global recurrence, scratch, and external-caller state before reading the routine-36 counter and a second routine-25 lookup at `TDB`",
]
routine.psy_w_fn_tdb_twb_pb.write_state = [
  "the ordered clamp, formula, and fallback selection write no state; optional own statistics increment before the clamp, an active routine-37 dispatch may mutate its first-warning and unique recurrence state before correction, the first nested lookup may overwrite a cache entry and mutate raw statistics/range diagnostics, an active routine-38 dispatch may mutate its first-warning state and the recurring record shared with routine 35 before fallback, and the negative branch may increment the routine-36 counter and mutate a second saturation-pressure cache entry and raw side effects",
]
routine.psy_w_fn_tdb_twb_pb.history_state_ownership = "ordinary finite default non-IF97 output is a deterministic function of the original inputs and corrected-wet-bulb and dry-bulb tag representatives; the first lookup can change fallback hit/overwrite order, cache collision and reachable negative-NaN sentinel history can affect full-source nonfinite numerical results, and routine-37 recurrence plus routine-38's title-deduplicated shared routine-35 recurrence belong to each `EnergyPlusData` instance"
routine.psy_w_fn_tdb_twb_pb.unsupported_state = [
  "routine-39 and fallback routine-36 counters; both routine-25 cache lookups, lifecycle, signed tags/hashes/sentinel/collisions, public/raw counters, range diagnostics, warmup, and nested callers; `ReportErrors`, shared scratch string, warning stream/totals, both error indices and first-detail gates, routine-37 recurrence, routine-38's merged routine-35 recurrence count/min/max, SQLite, and callback state",
]
routine.psy_w_fn_tdb_twb_pb.inactive_branches = [
  "`TWBin <= TDB` or an unordered comparison skips clamp and routine 37; `TDB < TWBin <= TDB + 0.01` clamps silently; a larger ordered excess still clamps even when diagnostics are gated off",
  "raw `W >= 0.0`, negative zero, or NaN skips routine 38 and the fallback; raw `W < 0.0` always takes routine 36 regardless of diagnostic compile/runtime gates",
  "disabling statistics or errors removes the corresponding mutations; `EP_nocache_Psychrometrics` evaluates original unquantized temperatures, and `EP_IF97` changes liquid-water saturation pressure",
]
routine.psy_w_fn_tdb_twb_pb.unsupported_active_branches = [
  "both default saturation-pressure cache lookups with representative misses, hit suppression, overwrite order, raw side effects, counters, negative-NaN sentinel, and lifecycle behavior",
  "default errors-enabled routine-37 and routine-38 flows with `ReportErrors`, warmup, first/recurring diagnostics, exact caller formatting, and routine-38 cross-routine-35 recurring-title aliasing",
]
routine.psy_w_fn_tdb_twb_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, full IEEE and negative-NaN sentinel parity, exact diagnostic/statistics/cache behavior, raw-W zero and final-formula denominator nextafter boundaries, the private routine-18 guess and its 100-iteration consumer, API or EMS dispatch, and weather, surface, coil, chiller, cooling-tower, evaporative, refrigeration, or other downstream migration",
]

routine.psy_h_fn_tdb_rh_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_h_fn_tdb_rh_pb.source_routine = "PsyHFnTdbRhPb"
routine.psy_h_fn_tdb_rh_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_rh_pb.completion_status = "state_mapped"
routine.psy_h_fn_tdb_rh_pb.required_for_full_domain = false
routine.psy_h_fn_tdb_rh_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_h_fn_tdb_rh_pb.read_state = [
  "arguments `TDB`, `RH`, `PB`, and `CalledFrom`; routine 40 itself reads no mutable state and owns no counter; it forwards `CalledFrom` unchanged to routine 36, which reads its optional counter, routine-25 signed tag/hash/cache entry and optional wrapper counter on every call, miss-side routine-24 statistics, warmup, range-warning, and nested caller state, and for raw `W <= -0.0001` routine-35 warmup, error index, title-deduplicated global recurrence, scratch, and external-caller state; the outer ordered floor and routine 8 read no mutable state",
]
routine.psy_h_fn_tdb_rh_pb.write_state = [
  "the three ordered humidity-ratio floors and routine-8 enthalpy arithmetic write no state; only nested routine 36 may increment its counter, overwrite a routine-25 cache entry, mutate raw saturation-pressure statistics and range diagnostics on a miss, and mutate routine-35 first-warning, scratch, index, and the recurring record shared with routine 38",
]
routine.psy_h_fn_tdb_rh_pb.history_state_ownership = "ordinary finite default non-IF97 output is deterministic from the original arguments and the dry-bulb tag representative used only for saturation pressure; cache hit, miss, and collision history changes nested side effects, while reachable negative-NaN sentinel history can change the nested humidity-ratio value and side effects but the original NaN `TDB` keeps the final enthalpy classification NaN; routine-35 first-warning/index state and its title-deduplicated routine-38 recurrence belong to each `EnergyPlusData` instance"
routine.psy_h_fn_tdb_rh_pb.unsupported_state = [
  "nested routine-36 counter; routine-25 cache lifecycle, signed tag/hash/sentinel/collisions, wrapper and raw counters, warmup range diagnostics, and nested caller context; routine-35 shared scratch string, warning stream/totals, first-detail gate, error index, routine-38-shared recurrence count/min/max, SQLite, and callback state",
]
routine.psy_h_fn_tdb_rh_pb.inactive_branches = [
  "raw routine-36 `W > -0.0001` or NaN skips routine 35; every `W < 1.0e-5` is still numerically floored, and the outer routine-40 and inner routine-8 ordered floors preserve rather than replace NaN",
  "disabling statistics removes counters, disabling errors removes routine 35, and warmup suppresses routine 35 without suppressing the numerical floors or saturation-pressure cache lookup",
  "`EP_nocache_Psychrometrics` evaluates saturation pressure at the original unquantized `TDB`, while `EP_IF97` changes the liquid-water saturation-pressure branch",
]
routine.psy_h_fn_tdb_rh_pb.unsupported_active_branches = [
  "the default cached routine-25 lookup with representative misses, hit suppression of nested raw side effects and caller context, cache lifecycle/collisions, negative-NaN sentinel, and optional routine-36 and saturation-pressure statistics",
  "the default errors-enabled inclusive raw `W <= -0.0001` routine-35 flow with warmup, first/recurring diagnostics, exact caller formatting, and cross-routine-38 recurring-title aliasing",
]
routine.psy_h_fn_tdb_rh_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, cached/no-cache/IF97 equivalence, full IEEE and negative-NaN sentinel payload parity, exact nested diagnostic/statistics/cache behavior, API or EMS dispatch, the downstream WaterCoils root solve, and cooling-tower, evaporative-cooler, hybrid-cooler, refrigeration, water-coil, or other consumer migration",
]

routine.psy_tsat_fn_pb_raw.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_tsat_fn_pb_raw.source_routine = "PsyTsatFnPb_raw"
routine.psy_tsat_fn_pb_raw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tsat_fn_pb_raw.completion_status = "state_mapped"
routine.psy_tsat_fn_pb_raw.required_for_full_domain = false
routine.psy_tsat_fn_pb_raw.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tsat_fn_pb_raw.read_state = [
  "arguments `Press` and `CalledFrom`; optional statistics read the `TsatFnPb` call and accumulated-iteration counters; with errors enabled the pressure-range preflight reads `WarmupFlag` before the saved-value shortcut and, for non-warmup `Press <= 0.0017` or `Press >= 1555000.0`, reads the range-warning index/recurrence and first-detail caller context; every raw call then reads `Press_Save` and an exact-equality hit reads `tSat_Save`; a miss reads `useInterpolationPsychTsatFnPb`, while the modeled default non-interpolation path calls routine 25 for each bounded estimate and reads its signed tag/hash/cache entry, optional lookup/raw counters, miss-side warmup/range-warning/caller state, and selected default or `EP_IF97` formula; an exhausted solve additionally reads `WarmupFlag`, the `TsatFnPb2` warning index/recurrence, and first-detail caller context",
]
routine.psy_tsat_fn_pb_raw.write_state = [
  "the pure numerical scaffold writes no state; optional statistics increment the `TsatFnPb` call counter before diagnostics and the saved-value test, then only a non-saved path adds local `iter` to the iteration total (`0` for interpolation, pressure clamps, or the strict 611.0-to-611.25 shortcut, `1..50` on convergence, and `51` after 50 exhausted evaluations); range and nonconvergence flows may mutate two error indices, recurring count/min/max records, warning/timestamp/continuation totals, SQLite, and callbacks; on a miss the source overwrites `Press_Save` before choosing interpolation or iteration and writes `tSat_Save` after the result, while nested routine 25 may update lookup/raw counters, overwrite a cache entry, and mutate miss-side diagnostics",
]
routine.psy_tsat_fn_pb_raw.history_state_ownership = "the ordinary-finite default cached-build, non-IF97, interpolation-disabled result is deterministic only for the modeled fresh/non-saved raw numerical projection; the source owns an exact `Press_Save`/`tSat_Save` pair initialized and cleared to `-99999.0`, so initial `Press == -99999.0` is a finite false hit, exact repeated pressure can return a result from an earlier interpolation mode, and direct raw calls and routine-42 misses share that pair; nested routine-25 cache and diagnostic/statistics history plus both routine-41 warning histories belong to each `EnergyPlusData` instance"
routine.psy_tsat_fn_pb_raw.unsupported_state = [
  "the saved-pair initialization, exact-equality hit/miss ordering, clear lifecycle, and sharing with routine 42; `useInterpolationPsychTsatFnPb`, the routine-53 spline tables/path, and PerformancePrecisionTradeoffs mode changes; optional call/iteration statistics; warmup, two first/recurring warning indices, caller formatting, resultant-temperature continuation, warning totals/stream, SQLite, and callbacks; nested routine-25 cache lifecycle, signed tag/hash/sentinel/collisions, lookup/raw statistics, range diagnostics, caller context, and nonfinite payload behavior",
]
routine.psy_tsat_fn_pb_raw.inactive_branches = [
  "`EP_nocache_Psychrometrics` removes the separately named routine 41 and compiles this body as public routine 42, with nested saturation pressure evaluated at each original unquantized temperature estimate",
  "defining `EP_IF97` changes the nested liquid-water saturation-pressure branch and therefore the iterative trajectory; disabling `EP_psych_stats` removes both own counter mutations, while disabling `EP_psych_errors` removes range/nonconvergence diagnostics without changing the numerical branches",
]
routine.psy_tsat_fn_pb_raw.unsupported_active_branches = [
  "exact saved-value hits, including the fresh `-99999.0` sentinel and mode-stale returns; the runtime spline branch selected by `useInterpolationPsychTsatFnPb`; default errors-enabled inclusive range diagnostics before the saved shortcut and 50-evaluation nonconvergence diagnostics afterward; nested default routine-25 hit/miss suppression, cache lifecycle, statistics, range warnings, caller relabeling, and negative-NaN sentinel behavior",
]
routine.psy_tsat_fn_pb_raw.not_claimed_branches = [
  "external EnergyPlus numerical parity, cross-platform secant/`exp`/`log` last-bit and floating-point-exception parity, full IEEE/NaN-payload parity, saved-state or interpolation parity, routine-42 cached/no-cache parity, `EP_IF97` parity, exact diagnostic/statistics/cache side effects, PerformancePrecisionTradeoffs behavior, or downstream API, EMS, dew-point, HVAC, sizing, and other consumer migration",
]

routine.psy_tsat_fn_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_tsat_fn_pb.source_routine = "PsyTsatFnPb"
routine.psy_tsat_fn_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tsat_fn_pb.completion_status = "source_mapped"
routine.psy_tsat_fn_pb.required_for_full_domain = false

routine.psy_tdp_fn_w_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_tdp_fn_w_pb.source_routine = "PsyTdpFnWPb"
routine.psy_tdp_fn_w_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdp_fn_w_pb.completion_status = "state_mapped"
routine.psy_tdp_fn_w_pb.required_for_full_domain = false
routine.psy_tdp_fn_w_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdp_fn_w_pb.read_state = [
  "arguments `W`, `PB`, and `CalledFrom`; routine 43 itself reads no mutable state and owns no counter, applies the ordered `1.0e-5` humidity-ratio floor, computes `PDEW = PB * W0 / (0.62198 + W0)`, and unconditionally forwards `PDEW` plus `CalledFrom` to routine 42; the default cached wrapper then reads mutable `tsatprecision_bits`, the signed pressure tag/hash/cache entry, and a hit's cached temperature, while a miss reads routine-41 saved-pair, interpolation, optional call/iteration statistics, warmup, two warning histories, caller context, and iterative-path routine-25 cache/statistics/diagnostic state",
]
routine.psy_tdp_fn_w_pb.write_state = [
  "the ordered humidity floor and dew-pressure arithmetic write no state, and routine 43 has no statistics or diagnostics of its own; a default routine-42 hit writes no state, while a miss writes the direct-map pressure tag before calling raw and stores the returned saturation temperature afterward; the nested raw path may overwrite `Press_Save` and `tSat_Save`, increment call/iteration statistics, mutate range/nonconvergence diagnostics, and update routine-25 cache/statistics/diagnostic state; the public wrapper owns no separate lookup counter",
]
routine.psy_tdp_fn_w_pb.history_state_ownership = "for finite physical arguments whose derived `PDEW` takes a nonzero-tag default-cache miss followed by a non-saved, interpolation-disabled, non-IF97 raw path, the modeled numerical projection is deterministic from `W` and `PB` and `CalledFrom` changes only deferred diagnostics; the full source return is nevertheless history-dependent because routine 42 stores the first original pressure for each tag, shares entries with every other routine-42 consumer, has a fresh tag-zero false hit, can derive the raw pair's fresh `-99999.0` false hit, and shares routine-41 saved/interpolation and nested state, all owned by each `EnergyPlusData` instance"
routine.psy_tdp_fn_w_pb.unsupported_state = [
  "routine-42 mutable precision, signed tag/hash/direct-map entries, first-writer aliases, tag-zero sentinel, collisions, initialization versus clear lifecycle, and hit suppression; routine-41 saved pair, interpolation flag/spline path, call/iteration statistics, warmup, range/nonconvergence warning indices and recurrences, exact caller formatting, warning stream/totals, SQLite, and callbacks; nested routine-25 cache lifecycle, sentinel/collisions, statistics, diagnostics, caller relabeling, and nonfinite payload behavior",
]
routine.psy_tdp_fn_w_pb.inactive_branches = [
  "`W < 1.0e-5`, including negative infinity and either signed zero, selects literal `1.0e-5`; `W >= 1.0e-5` retains `W`, and unordered NaN retains `W` rather than substituting the floor because source max returns its first argument; none of these local branches emits diagnostics",
  "`EP_nocache_Psychrometrics` removes routine 42's outer cache and named routine 41, evaluates the same stateful body on every derived `PDEW`, and makes nested saturation pressure use each original unquantized temperature estimate",
  "defining `EP_IF97` changes the nested liquid-water saturation-pressure formula and iterative trajectory; disabling `EP_psych_stats` removes nested counters and disabling `EP_psych_errors` removes nested range/nonconvergence diagnostics, while routine 43's floor and `PDEW` arithmetic remain unchanged",
]
routine.psy_tdp_fn_w_pb.unsupported_active_branches = [
  "the default routine-42 direct-map lookup, including ordinary-finite same-tag first-writer reuse, collisions, mutable precision, fresh tag-zero false hits, and complete raw-side-effect suppression on a hit; miss-side exact saved-value reuse, default errors-enabled diagnostics, optional statistics, nested routine-25 cache behavior, and the runtime spline branch selected by PerformancePrecisionTradeoffs",
]
routine.psy_tdp_fn_w_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, full public routine-42 sequence or cache-history parity, cached/no-cache/interpolation/`EP_IF97` equivalence, full IEEE/overflow/NaN-payload and cross-platform iterative last-bit parity, exact nested diagnostics/statistics/cache effects, C/Python API or EMS dispatch, inverse round trips through source-mapped routine 34, full stateful routine-45 cache/diagnostic sequence, or sizing, coil, radiant, refrigeration, reporting, and other downstream consumer migration",
]

routine.psy_tdp_fn_tdb_twb_pb_error.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.psy_tdp_fn_tdb_twb_pb_error.source_routine = "PsyTdpFnTdbTwbPb_error"
routine.psy_tdp_fn_tdb_twb_pb_error.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdp_fn_tdb_twb_pb_error.completion_status = "source_mapped"
routine.psy_tdp_fn_tdb_twb_pb_error.required_for_full_domain = false

routine.psy_tdp_fn_tdb_twb_pb.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_tdp_fn_tdb_twb_pb.source_routine = "PsyTdpFnTdbTwbPb"
routine.psy_tdp_fn_tdb_twb_pb.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdp_fn_tdb_twb_pb.completion_status = "state_mapped"
routine.psy_tdp_fn_tdb_twb_pb.required_for_full_domain = false
routine.psy_tdp_fn_tdb_twb_pb.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_tdp_fn_tdb_twb_pb.read_state = [
  "arguments `TDB`, original `TWB`, `PB`, and `CalledFrom`; with statistics enabled routine 45 first reads its `TdpFnTdbTwbPb` call counter, then unconditionally invokes routine 39, whose reads include its own counter, routine-25 saturation-pressure cache/tag/statistics/range-diagnostic/caller state, routines 37/38 diagnostic gates, and negative-result fallback through routines 36/35; routine 45 applies an ordered `1.0e-5` floor to `W`, unconditionally invokes routine 43, and thereby reads routine-42 direct-map precision/tag/cache plus miss-side routine-41 saved/interpolation/statistics/range/nonconvergence/caller state and nested routine-25 state; when `TDP` exceeds the original `TWB`, an errors build reads `TWB + 0.1`, and a strict excess dispatches routine 44, which rechecks the threshold and reads `WarmupFlag`, the dedicated error index/recurrence, first-detail `CalledFrom`, and simulation timestamp context",
]
routine.psy_tdp_fn_tdb_twb_pb.write_state = [
  "the pure numerical scaffold writes no state; optional statistics increment routine 45's own call counter before every nested call, while routines 39 and 43 may update their nested cache, statistics, saved/interpolation, and diagnostic state; for a strict non-warmup diagnostic excess, routine 44 may overwrite the shared scratch string twice, emit first warning/timestamp/continuations only when its index is zero, then update the recurring index/count and raw pre-cap `TDP` minimum/maximum in C plus warning totals, SQLite, and callbacks; the final numeric cap itself writes no source state",
]
routine.psy_tdp_fn_tdb_twb_pb.history_state_ownership = "for ordinary-finite arguments under the default non-IF97 build, the modeled result is deterministic only for collision-free routine-25 representative lookups followed by a routine-42 nonzero-tag miss and a routine-41 non-saved, interpolation-disabled numerical path; the full source is history-dependent through shared routine-25 entries, routine-42 first-writer/tag-zero/collision/precision state, and routine-41 saved/interpolation state, and a final wet-bulb cap may mask a changed return without suppressing nested cache, statistics, or diagnostics, all owned by each `EnergyPlusData` instance"
routine.psy_tdp_fn_tdb_twb_pb.unsupported_state = [
  "routine-45 optional statistics; routine-39 counter, corrected-wet-bulb and negative-humidity diagnostics, `ReportErrors`/warmup/index/recurrence/caller state, nested routine-25 direct-map lifecycle/sentinel/collisions/statistics/range diagnostics, and routine-36/35 fallback effects; routine-42 mutable precision, signed tag/hash/direct-map entries, first-writer aliases, tag-zero sentinel, collisions, lifecycle, and hit suppression; routine-41 saved pair, interpolation flag/spline, counters, range/nonconvergence diagnostics, caller formatting, and nested routine-25 effects; routine-44 warmup/index/recurrence/shared-string/warning/timestamp/SQLite/callback state",
]
routine.psy_tdp_fn_tdb_twb_pb.inactive_branches = [
  "routine 39 results strictly below `1.0e-5` select literal `1.0e-5`; values at or above the floor are retained, and unordered NaN is retained rather than replaced because the source max returns its first argument",
  "`TDP <= original TWB` and unordered comparisons return `TDP`; every ordered excess returns the original `TWB`, but only strict `TDP > TWB + 0.1` dispatches routine 44, so exact or smaller 0.1 C excesses cap silently",
  "disabling `EP_psych_stats` removes routine 45 and nested counters, while disabling `EP_psych_errors` removes routine 44 and all nested warnings without changing the local floor or final cap; `EP_nocache_Psychrometrics`, `EP_IF97`, and runtime interpolation select different nested numerical/state paths",
]
routine.psy_tdp_fn_tdb_twb_pb.unsupported_active_branches = [
  "complete routine-39 and routine-43 cache/history sequences, including routine-25 and routine-42 first-writer aliases, collisions, sentinels, mutable precision, routine-41 saved-value reuse and spline mode, optional statistics, and errors-enabled routine-37/38/35/44 warning, warmup, caller, scratch-string, recurring-index/count/min/max, SQLite, and callback effects",
]
routine.psy_tdp_fn_tdb_twb_pb.not_claimed_branches = [
  "external EnergyPlus numerical parity, full cache-hit/miss or diagnostic sequence parity, cached/no-cache/interpolation/`EP_IF97` equivalence, full IEEE/NaN-payload/floating-point-exception and cross-platform iterative last-bit parity, exact statistics/warnings/caller/recurrence effects, C/Python API or EMS dispatch, seven direct production consumers, downstream HVAC/collector behavior, or broad psychrometric migration",
]

routine.f6.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.f6.source_routine = "F6"
routine.f6.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.f6.completion_status = "state_mapped"
routine.f6.required_for_full_domain = false
routine.f6.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.f6.read_state = [
  "arguments `X` and `A0` through `A5` exactly as supplied; the routine has no `EnergyPlusData`, `CalledFrom`, static/global field, cache, statistic, diagnostic, or flag, and its nested source expression semantically evaluates every coefficient",
]
routine.f6.write_state = [
  "no state; the routine performs exactly five dependency-ordered multiply/add levels without mutation, explicit fused multiply-add, expansion, or coefficient scaling",
]
routine.f6.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment the result is a pure function of `X` and `A0` through `A5` in the stated Horner order"
routine.f6.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.f6.inactive_branches = [
  "none; the routine is always present and has no compile-time branch, guard, short-circuit, or loop",
]
routine.f6.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.f6.not_claimed_branches = [
  "a direct upstream F6 helper oracle, cross-compiler/platform fused contraction, excess precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity, routine-26 piecewise selection/pressure correction/cache/statistics/diagnostics, or downstream saturation-temperature migration",
]

routine.f7.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.f7.source_routine = "F7"
routine.f7.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.f7.completion_status = "state_mapped"
routine.f7.required_for_full_domain = false
routine.f7.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.f7.read_state = [
  "arguments `X` and `A0` through `A6` exactly as supplied; the routine has no `EnergyPlusData`, `CalledFrom`, static/global field, cache, statistic, diagnostic, or flag, evaluates every coefficient through six nested Horner levels, and reads literal `1.0E10` only after forming the complete numerator",
]
routine.f7.write_state = [
  "no state; the routine performs six dependency-ordered multiply/add levels and one final division without mutation, explicit fused multiply-add, expansion, early coefficient scaling, or substitution of multiplication by `1e-10`",
]
routine.f7.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment the result is a pure function of `X` and `A0` through `A6` in the stated Horner-then-division order"
routine.f7.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.f7.inactive_branches = [
  "none; the routine is always present and has no compile-time branch, guard, short-circuit, or loop",
]
routine.f7.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.f7.not_claimed_branches = [
  "a direct upstream F7 helper oracle, cross-compiler/platform fused contraction, excess precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity, routine-26 case selection/clamp/pressure correction/cache/statistics/diagnostics, or downstream saturation-temperature migration",
]

routine.cpcw.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.cpcw.source_routine = "CPCW"
routine.cpcw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.cpcw.completion_status = "state_mapped"
routine.cpcw.required_for_full_domain = false
routine.cpcw.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.cpcw.read_state = [
  "formal argument `Temperature` is passed and caller-evaluated but its value is intentionally never read by the body; the routine has no `EnergyPlusData`, static/global field, cache, statistic, diagnostic, flag, or compile setting",
]
routine.cpcw.write_state = [
  "no state; the routine returns exact literal `4180.0` without mutation or input validation",
]
routine.cpcw.history_state_ownership = "no cross-call history or cache; the return is the same constant for every finite, nonfinite, NaN, and signed-zero temperature value"
routine.cpcw.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.cpcw.inactive_branches = [
  "none; the routine is always present and has no compile-time branch, guard, validation, or loop",
]
routine.cpcw.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.cpcw.not_claimed_branches = [
  "EMS parser/dispatch parity, caller expression side effects beyond ordinary Rust argument evaluation, six direct production consumers, plant/node/ice-storage integration, or downstream migration despite the exact helper result",
]

routine.cphw.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.cphw.source_routine = "CPHW"
routine.cphw.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.cphw.completion_status = "state_mapped"
routine.cphw.required_for_full_domain = false
routine.cphw.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.cphw.read_state = [
  "formal argument `Temperature` is passed and caller-evaluated but its value is intentionally never read by the body; the routine has no `EnergyPlusData`, static/global field, cache, statistic, diagnostic, flag, or compile setting",
]
routine.cphw.write_state = [
  "no state; the routine returns exact literal `4180.0` without mutation or input validation",
]
routine.cphw.history_state_ownership = "no cross-call history or cache; the return is the same constant for every finite, nonfinite, NaN, and signed-zero temperature value"
routine.cphw.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.cphw.inactive_branches = [
  "none; the routine is always present and has no compile-time branch, guard, validation, or loop",
]
routine.cphw.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.cphw.not_claimed_branches = [
  "EMS parser/dispatch parity, caller expression side effects beyond ordinary Rust argument evaluation, 21 direct production consumers, coil/collector/tank/water-use integration, or downstream migration despite the exact helper result",
]

routine.rho_h2o.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.rho_h2o.source_routine = "RhoH2O"
routine.rho_h2o.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.rho_h2o.completion_status = "state_mapped"
routine.rho_h2o.required_for_full_domain = false
routine.rho_h2o.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.rho_h2o.read_state = [
  "argument `TB` and the four literal polynomial coefficients exactly as supplied; the routine has no `EnergyPlusData`, `CalledFrom`, static/global field, cache, statistic, diagnostic, flag, or runtime range setting, and separately evaluates `TB * TB` and `TB * TB * TB`",
]
routine.rho_h2o.write_state = [
  "no state; the routine combines the constant, linear, independently squared, and independently cubed terms in source left-associated order without mutation, validation, clamping, Horner conversion, `powi`, or explicit fused multiply-add",
]
routine.rho_h2o.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment the result is a pure function of `TB` and the stated source expression"
routine.rho_h2o.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.rho_h2o.inactive_branches = [
  "none; the routine is always present and has no compile-time branch, guard, clamp, validation, short-circuit, or loop",
]
routine.rho_h2o.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.rho_h2o.not_claimed_branches = [
  "enforcement or physical validity of the documented 0 C through 150 C range, cross-compiler/platform contraction, excess-precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity, EMS parser/dispatch parity, 51 direct production consumers, or downstream chiller, coil, evaporative, humidifier, tank, and reporting migration",
]

routine.psy_delta_h_sen_fn_tdb2_tdb1_w.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.source_routine = "PsyDeltaHSenFnTdb2Tdb1W"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.completion_status = "state_mapped"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.required_for_full_domain = false
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.read_state = [
  "arguments `TDB2`, `TDB1`, and `W` plus literal `1.0e-5`, `1.00484e3`, and `1.85895e3`; the routine has no `EnergyPlusData`, `CalledFrom`, static/global field, cache, statistic, diagnostic, or flag, and its ordered `max(1.0e-5, W)` selects the first literal when `1.0e-5 < W` is false, including equality, lower values, and unordered NaN",
]
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.write_state = [
  "no state; the routine forms the humidity-dependent coefficient, then `TDB2 - TDB1`, and multiplies them without mutation, nested enthalpy calls, validation, or explicit fused multiply-add",
]
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment the result is a pure function of `TDB2`, `TDB1`, and the literal-first ordered humidity floor"
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.inactive_branches = [
  "`1.0e-5 < W` retains `W`; equality, every lower ordered value, and unordered NaN select literal `1.0e-5`, while the routine is otherwise always present with no compile-time branch, validation, short-circuit, or loop",
]
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_delta_h_sen_fn_tdb2_tdb1_w.not_claimed_branches = [
  "a direct upstream helper oracle, bitwise identity with the explanatory two-call enthalpy subtraction for every IEEE input, cross-compiler/platform contraction, excess-precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity, the two external production call sites plus routine-52 delegation, downstream mass-flow multiplication, or zone-equipment integration",
]

routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.source_file = "src/EnergyPlus/Psychrometrics.hh"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.source_routine = "PsyDeltaHSenFnTdb2W2Tdb1W1"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.completion_status = "state_mapped"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.required_for_full_domain = false
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.read_state = [
  "arguments `TDB2`, `W2`, `TDB1`, and `W1`; the routine has no `EnergyPlusData`, `CalledFrom`, static/global field, cache, statistic, diagnostic, or flag, evaluates the ordered Objexx minimum as `W1 < W2 ? W1 : W2`, and then unconditionally reads the pure routine-51 coefficient and floor inputs",
]
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.write_state = [
  "no state; the routine stores only a local minimum and delegates `TDB2`, `TDB1`, and that value to routine 51 without mutation, validation, or duplicated enthalpy arithmetic",
]
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment the result is a pure function of the four arguments, the second-argument-on-equality-or-unordered minimum, and routine 51"
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.unsupported_state = [
  "none; the source routine and its routine-51 dependency have no mutable state, cache, counter, diagnostic, or lifecycle",
]
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.inactive_branches = [
  "ordered `W1 < W2` selects `W1`; equality, `W1 > W2`, and unordered comparisons select `W2`, after which routine 51 independently applies its literal-first `1.0e-5` floor; the routine has no compile-time branch, validation, short-circuit, or loop",
]
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.unsupported_active_branches = [
  "none; there is no stateful or compile-conditional active branch",
]
routine.psy_delta_h_sen_fn_tdb2_w2_tdb1_w1.not_claimed_branches = [
  "a direct upstream helper oracle, Rust `f64::min` semantics, cross-compiler/platform contraction, excess-precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity, 17 direct production consumers, mass-flow multiplication, or downstream furnace, duct, heat-pump, VRF, unitary, and zone-air-loop migration",
]

routine.c_splineint.source_file = "src/EnergyPlus/Psychrometrics.cc"
routine.c_splineint.source_routine = "CSplineint"
routine.c_splineint.source_map = "docs/src/porting-map/psychrometrics-source-map.md"
routine.c_splineint.completion_status = "state_mapped"
routine.c_splineint.required_for_full_domain = false
routine.c_splineint.state_mapping_ref = "docs/src/porting-map/psychrometrics-source-map.md"
routine.c_splineint.read_state = [
  "arguments `n` and `x` plus the immutable 1,651-element `tsat_fn_pb_y` and `tsat_fn_pb_d2y` compile-time tables; for the supported source-defined domain the routine truncates `x` toward zero to a C++-`int`-equivalent value, derives and clamps `j`, then reads exactly `y[j]`, `y[j + 1]`, `d2y[j]`, and `d2y[j + 1]`; it has no `EnergyPlusData`, `CalledFrom`, mutable static/global field, cache, statistic, diagnostic, flag, or runtime table owner",
]
routine.c_splineint.write_state = [
  "no state; the routine stores only local `x_int`, `j`, `A`, `B`, and `y`-expression intermediates and evaluates the fixed cubic formula without table mutation, input clamping, search, iteration, allocation, or explicit fused multiply-add",
]
routine.c_splineint.history_state_ownership = "no cross-call history or cache; under a fixed floating-point environment and within the supported source-defined input domain, the result is a pure function of `n`, `x`, and the immutable source tables"
routine.c_splineint.unsupported_state = [
  "none; the source routine has no mutable state, cache, counter, diagnostic, or lifecycle, and its parent routine's interpolation flag and saved/cache/diagnostic/statistics state are not owned here",
]
routine.c_splineint.inactive_branches = [
  "after truncation toward zero, `j = (x_int >> 6) - 1` is first raised to zero when negative and then lowered to `n - 2` when above that bound; the original `x` remains unbounded for endpoint extrapolation, and the routine is always present with no compile-time branch, short-circuit, or loop",
]
routine.c_splineint.unsupported_active_branches = [
  "none; every source-defined numeric bin and endpoint-extrapolation branch is implemented, and there is no stateful or compile-conditional active branch",
]
routine.c_splineint.not_claimed_branches = [
  "behavior for `n < 2`, `n > 1651`, NaN or infinite `x`, or finite `x` whose truncation does not fit a C++ `int` because the upstream conversion or indexing is undefined; a direct upstream helper oracle; cross-compiler/platform signed-shift, contraction, excess-precision, rounding-mode, floating-point-exception or NaN-payload last-bit parity; routine-41/42 interpolation selection, saved pair, cache, statistics, diagnostics, lifecycle, or downstream saturation-temperature migration",
]

[[algorithm]]
id = "geometry_coordinate_transformation"
domain = "geometry"
source_map = "docs/src/porting-map/geometry-source-map.md"
energyplus_source = [
  "src/EnergyPlus/SurfaceGeometry.cc",
  "src/EnergyPlus/Vectors.cc",
]
rust_target = [
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_global_geometry_rules",
  "crates/ep_compiler/src/compiler.rs::canonical_world_surface_vertices",
  "crates/ep_compiler/src/compiler/surface_space.rs::Compiler::create_missing_spaces",
  "crates/ep_compare/src/eio.rs::parse_eio_surface_geometry_rules",
  "crates/ep_compare/src/eio.rs::parse_eio_heat_transfer_surfaces",
  "crates/ep_cli/src/main.rs::run_compare_surface_geometry",
  "crates/ep_runtime/src/geometry.rs::surface_azimuth_deg",
  "crates/ep_runtime/src/geometry.rs::surface_geometry_summaries",
  "crates/ep_runtime/src/geometry.rs::zone_geometry_summaries",
]
status = "scaffold"
first_case = "surface_geometry_001"
family_cases = ["surface_geometry_transform_001"]
proof_variables = [
  "HeatTransfer Surface Class",
  "HeatTransfer Surface Area (Net)",
  "HeatTransfer Surface Area (Gross)",
  "HeatTransfer Surface Azimuth",
  "HeatTransfer Surface Tilt",
]
claim_level = "none"
support_boundary = "Geometry source-order scaffold with CP57 bounded detailed opaque-surface coordinate state: typed GlobalGeometryRules, canonical vertex order, Relative-to-world projection, horizontal azimuth semantics, and one nonclaim transformed-coordinate oracle smoke. CP97 adds a bounded composite for typed BuildingSurface:Detailed only: the GetHTSurfaceData-owned cross-section resolves optional Space references through the full pre-remainder arena and validates same-Zone membership while the full routine remains source-mapped, then state-mapped CreateMissingSpaces detects mixed assignments, appends one remainder per affected Zone, applies final fallback SpaceIds, and redirects implicit surfaces. SetupZoneGeometry, GetSurfaceData, and full GetHTSurfaceData remain source-mapped; other surface families, opposite-surface generation, reordering, Space surface lists, derived space/zone geometry, and runtime space heat balance remain deferred. This entry does not add numerical geometry conformance."

routine.setup_zone_geometry.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.setup_zone_geometry.source_routine = "SetupZoneGeometry"
routine.setup_zone_geometry.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.setup_zone_geometry.completion_status = "source_mapped"
routine.setup_zone_geometry.required_for_full_domain = false

routine.get_surface_data.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.get_surface_data.source_routine = "GetSurfaceData"
routine.get_surface_data.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.get_surface_data.completion_status = "source_mapped"
routine.get_surface_data.required_for_full_domain = false

routine.get_geometry_parameters.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.get_geometry_parameters.source_routine = "GetGeometryParameters"
routine.get_geometry_parameters.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.get_geometry_parameters.completion_status = "state_mapped"
routine.get_geometry_parameters.required_for_full_domain = false
routine.get_geometry_parameters.state_mapping_ref = "docs/src/porting-map/geometry-source-map.md"
routine.get_geometry_parameters.read_state = [
  "the unique `GlobalGeometryRules` object; its starting-vertex-position, vertex-entry-direction, surface coordinate-system, daylight-reference-point coordinate-system, and rectangular-surface coordinate-system alpha fields; zone origins used by coordinate-mismatch checks; and the input object count",
]
routine.get_geometry_parameters.write_state = [
  "`DataSurfaces::Corner`, `DataSurfaces::CCW`, `DataSurfaces::WorldCoordSystem`, `DataSurfaces::DaylRefWorldCoordSystem`, `SurfaceGeometryData::RectSurfRefWorldCoordSystem`, the caller-owned error flag, warning and severe diagnostic streams, and the `Surface Geometry` EIO row",
]
routine.get_geometry_parameters.history_state_ownership = "all geometry-rule fields are `EnergyPlusData`-owned initialization state set during surface input; the routine has no cross-call numerical cache, while diagnostics and EIO output accumulate in simulation-owned reporting state"
routine.get_geometry_parameters.unsupported_state = [
  "daylight-reference-point and rectangular-simple-surface coordinate-system consumers, exact diagnostic counts and text, and exact EIO formatting",
  "lifecycle interactions with repeated input processing, shared input scratch arrays, and caller error aggregation",
]
routine.get_geometry_parameters.inactive_branches = [
  "exactly one object parses four starting corners, counterclockwise or clockwise entry, and World or Absolute versus Relative surface coordinates; zero objects and multiple objects set severe errors",
  "invalid surface-coordinate text warns and defaults to World, invalid optional coordinate text warns and defaults to Relative, and mixed coordinate modes or nonzero zone origins can emit mismatch warnings",
]
routine.get_geometry_parameters.unsupported_active_branches = [
  "exact diagnostic counts and text, cross-coordinate mismatch warnings, EIO emission, and repeated-input lifecycle behavior",
  "downstream use of the parsed flags by simple rectangular, daylighting, fenestration, shading, solar, and heat-balance paths",
]
routine.get_geometry_parameters.not_claimed_branches = [
  "source-required-object absence parity, cross-coordinate mismatch warning parity, exact EnergyPlus diagnostic or general EIO-emission parity, alternate surface families, daylighting, `GeometryTransform`, Appendix G rotation, fenestration, shading, solar, or broad geometry conformance",
]

routine.get_ht_surface_data.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.get_ht_surface_data.source_routine = "GetHTSurfaceData"
routine.get_ht_surface_data.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.get_ht_surface_data.completion_status = "source_mapped"
routine.get_ht_surface_data.required_for_full_domain = false

routine.get_vertices.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.get_vertices.source_routine = "GetVertices"
routine.get_vertices.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.get_vertices.completion_status = "state_mapped"
routine.get_vertices.required_for_full_domain = false
routine.get_vertices.state_mapping_ref = "docs/src/porting-map/geometry-source-map.md"
routine.get_vertices.read_state = [
  "the target `SurfaceTmp` record, `SurfNum`, `NSides`, and packed input coordinates; `Corner`, `CCW`, and `WorldCoordSystem`; building and Appendix G rotation trigonometry; per-zone relative-north trigonometry and origins; maximum-vertex, warning, error-counter, surface-class, multiplier, view-factor, and `GeometryTransform` state",
]
routine.get_vertices.write_state = [
  "reordered and world-projected surface vertices; maximum vertices per surface; perimeter, side count, coincident and degenerate counters, area vectors, gross and net area, normal and local-coordinate vectors, azimuth, tilt, orientation, trigonometric fields, view factors, diagnostics, possible vertex reversal, and optional aspect-transformed coordinates",
]
routine.get_vertices.history_state_ownership = "input flags and rotation arrays are initialized earlier in the same `EnergyPlusData` geometry lifecycle and each surface result is owned by that simulation state; the routine has no independent cross-call numerical cache, but diagnostics and aggregate error counters persist across surfaces"
routine.get_vertices.unsupported_state = [
  "coincident-vertex removal, degenerate-surface accounting, exact source-order normal and area derivation, roof or floor correction, orientation diagnostics, multipliers, view-factor autocalculation, local coordinate systems, and `GeometryTransform` aspect changes",
  "detached-building, daylighting, simple rectangular, fenestration, shading, interzone, and downstream solar or heat-balance state",
]
routine.get_vertices.inactive_branches = [
  "clockwise input reverses vertices two through the final vertex, non-upper-left starting corners rotate vertex order, and counterclockwise upper-left input preserves order",
  "Relative coordinates apply zone relative-north rotation, zone-origin translation, and building rotation for zoned surfaces; World coordinates ignore those values and apply only Appendix G rotation, while detached-building surfaces follow their source-specific building branch",
]
routine.get_vertices.unsupported_active_branches = [
  "all `GetVertices` behavior outside the implemented detailed opaque-surface order and relative/world projection, including diagnostics, vertex deletion, source-order derived geometry, correction, and aspect transformation",
  "Appendix G rotation, detached surfaces, non-opaque surface families, and downstream consumers beyond the existing typed opaque-surface runtime",
]
routine.get_vertices.not_claimed_branches = [
  "full-routine `GetVertices` parity, exact trigonometric last-bit parity, diagnostic and counter parity, EIO equality outside the declared transformed fixture, zone volume closure, fenestration, shading, daylighting, solar, heat balance, or broad geometry conformance",
]

routine.create_missing_spaces.source_file = "src/EnergyPlus/SurfaceGeometry.cc"
routine.create_missing_spaces.source_routine = "CreateMissingSpaces"
routine.create_missing_spaces.source_map = "docs/src/porting-map/geometry-source-map.md"
routine.create_missing_spaces.completion_status = "state_mapped"
routine.create_missing_spaces.required_for_full_domain = false
routine.create_missing_spaces.state_mapping_ref = "docs/src/porting-map/geometry-source-map.md"
routine.create_missing_spaces.read_state = [
  "EnergyPlus calls `CreateMissingSpaces` inside `GetSurfaceData` after all surface-family input, the fatal input-error gate, adjacent Zone/Space surface generation, and base-surface reconciliation, but before surface reordering; bounded Rust calls its partition pass after validated typed `BuildingSurface:Detailed` materialization and covers only that base detailed opaque family",
  "the preceding bounded GetHTSurfaceData cross-section owns raw optional `space_name` handling: `resolve_surface_space` treats missing or blank input as implicit, rejects malformed values, searches the full pre-remainder Space arena in dense order with case-insensitive normalized matching including authored and `AutoZoneDefault` entries, and requires every explicit target to belong to the surface Zone; full GetHTSurfaceData remains source-mapped",
  "as a prerequisite to `create_missing_spaces`, every retained typed surface therefore supplies a validated ZoneId, a provisional final-candidate SpaceId equal to either its explicit target or the Zone's existing last Space, and an ephemeral explicit/implicit classification; the pass reads those preclassified assignments to detect whether each Zone has both classes, while a Zone with no retained detailed surface has neither",
]
routine.create_missing_spaces.write_state = [
  "every retained typed `BuildingSurface:Detailed` exits with a final `Surface.space: SpaceId`: an explicit valid target remains unchanged, an all-implicit Zone uses its existing last Space, and an implicit surface in a mixed explicit/implicit Zone is redirected to the newly appended remainder",
  "each mixed Zone receives exactly one Zone-order appended Space named `<ZONE>-REMAINDER`, linked last in `Zone::spaces`, with `SpaceOrigin::AutoZoneRemainder`, AutoCalculate ceiling height/volume/floor area, no tags, and the General SpaceTypeId reused or appended through `GetGeneralSpaceTypeNum`; the remainder is deliberately absent from the authored Space name map and generated Spaces do not increase typed input object count",
  "unknown, malformed, or cross-Zone explicit references fail compilation in the preceding bounded GetHTSurfaceData cross-section before this partition pass; every authored Space, SpaceList, and generated remainder is reported as `UnsupportedSpacePartitioning` and `RunBlocked`, while a sole `AutoZoneDefault` remains inactive when no remainder is needed, including when every valid typed surface explicitly references it",
]
routine.create_missing_spaces.history_state_ownership = "TypedModel owns immutable final detailed-surface SpaceIds, generated remainder descriptors, per-Zone ordered SpaceIds, and the shared SpaceType registry; this bounded pass allocates no mutable surface, geometry, space-air, zone-air, load, HVAC, sizing, or reporting history"
routine.create_missing_spaces.unsupported_state = [
  "all non-`BuildingSurface:Detailed` source families, non-heat-transfer and shading filters, InternalMass exclusion from remainder detection, subsurface inheritance, auto-generated interzone surfaces and warnings, and Outside Boundary Condition Zone/Space opposite-surface creation",
  "the remainder of `GetSurfaceData` and `SetupZoneGeometry`: surface reordering, `Space.surfaces`, first/last ranges, calculated floor area/ceiling height/volume and fractions, enclosures, vertex realization/correction beyond the existing CP57 subset, reports, and runtime consumers",
]
routine.create_missing_spaces.inactive_branches = [
  "a Zone with no retained typed detailed surface creates no remainder; a Zone whose retained surfaces are all implicit creates no remainder and assigns all of them to its existing last Space",
  "a Zone whose retained surfaces are all explicit creates no remainder; prevalidated assignments to its sole generated whole-zone default add no `UnsupportedSpacePartitioning` boundary",
  "General is appended only when a mixed Zone needs a remainder and the ordered type registry does not already contain it; otherwise the first existing General identity is reused",
]
routine.create_missing_spaces.unsupported_active_branches = [
  "every generated `AutoZoneRemainder` blocks arbitrary runtime execution until space-level geometry, heat balance, loads, HVAC, and reporting consume the partition; authored Spaces and SpaceLists remain blocked by their existing parent contracts",
  "final SurfaceId-to-SpaceId ownership is typed but does not claim surface sorting, per-Space surface lists, space heat-balance execution, or any numerical effect",
]
routine.create_missing_spaces.not_claimed_branches = [
  "complete `SetupZoneGeometry`, `GetSurfaceData`, `GetHTSurfaceData`, or `CreateMissingSpaces` parity; source allocation/resize capacity, one-based counters, partial invalid-record side effects, whitespace-preserving or ambiguous case-colliding first-match behavior, exact diagnostics/order/multiplicity, legacy detailed surfaces, InternalMass, fenestration/subsurface inheritance, auto-generated adjacent surfaces and warnings, surface reordering, Space surface lists/ranges, geometry, loads, HVAC, reporting, numerical parity, and conformance",
]

[[algorithm]]
id = "material_object_family_variants"
domain = "construction"
source_map = "docs/src/porting-map/material-source-map.md"
energyplus_source = [
  "src/EnergyPlus/HeatBalanceManager.cc",
  "src/EnergyPlus/Material.cc",
  "src/EnergyPlus/Material.hh",
  "src/EnergyPlus/WindowManager.cc",
  "src/EnergyPlus/PhaseChangeModeling/HysteresisModel.cc",
  "src/EnergyPlus/HeatBalFiniteDiffManager.cc",
  "src/EnergyPlus/MoistureBalanceEMPDManager.cc",
  "src/EnergyPlus/HeatBalanceHAMTManager.cc",
  "src/EnergyPlus/SurfaceGeometry.cc",
  "src/EnergyPlus/DataSurfaces.cc",
  "src/EnergyPlus/HeatBalanceSurfaceManager.cc",
]
rust_target = [
  "crates/ep_model/src/objects/materials.rs::Material",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_materials",
  "crates/ep_runtime/src/heat_balance/surface_manager.rs::ConstructionThermalDataCache",
  "crates/ep_runtime/src/execution_plan.rs::compile_stage_contracts",
  "crates/ep_cli/src/main.rs::construction_material_rows",
  "crates/ep_compare/src/eio.rs::parse_eio_window_material_glazing",
  "crates/ep_cli/src/main.rs::run_compare_window_glazing_spectral_average",
  "crates/ep_model/src/objects/materials.rs::WindowGlazingRefractionExtinctionMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_glazing_refraction_extinction_materials",
  "crates/ep_cli/src/main.rs::run_compare_window_glazing_refraction_extinction",
  "crates/ep_model/src/objects/materials.rs::WindowGlazingEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_glazing_equivalent_layer_materials",
  "crates/ep_compare/src/eio.rs::parse_eio_window_material_glazing_equivalent_layer",
  "crates/ep_cli/src/main.rs::run_compare_window_glazing_equivalent_layer",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasType",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasProperties",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_gas_materials",
  "crates/ep_compare/src/eio.rs::parse_eio_window_material_gas",
  "crates/ep_cli/src/main.rs::run_compare_window_material_gas",
  "crates/ep_cli/src/main.rs::energyplus_window_gas_eio_thickness",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGapVentType",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGapEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_gap_equivalent_layer_materials",
  "crates/ep_compare/src/eio.rs::parse_eio_window_material_gap_equivalent_layer",
  "crates/ep_cli/src/main.rs::run_compare_window_material_gap_equivalent_layer",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowStandardGasType",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasMixtureComponent",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasMixture",
  "crates/ep_model/src/objects/materials/window_gas.rs::WindowGasMixtureMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_gas_mixture_materials",
  "crates/ep_model/src/objects/materials/window_shade.rs::WindowShadeMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_shade_materials",
  "crates/ep_model/src/objects/materials/window_shade_equivalent_layer.rs::WindowShadeEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_shade_equivalent_layer_materials",
  "crates/ep_compare/src/eio/window_material_shade_equivalent_layer.rs::parse_eio_window_material_shade_equivalent_layer",
  "crates/ep_cli/src/window_material_shade_equivalent_layer.rs::run_compare_window_material_shade_equivalent_layer",
  "crates/ep_model/src/objects/materials/window_drape_equivalent_layer.rs::WindowDrapeEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_drape_equivalent_layer_materials",
  "crates/ep_compare/src/eio/window_material_drape_equivalent_layer.rs::parse_eio_window_material_drape_equivalent_layer",
  "crates/ep_cli/src/window_material_drape_equivalent_layer.rs::run_compare_window_material_drape_equivalent_layer",
  "crates/ep_model/src/objects/materials/window_screen.rs::WindowScreenBeamReflectanceModel",
  "crates/ep_model/src/objects/materials/window_screen.rs::WindowScreenTransmittanceMapResolution",
  "crates/ep_model/src/objects/materials/window_screen.rs::WindowScreenMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_screen_materials",
  "crates/ep_model/src/objects/materials/window_screen_equivalent_layer.rs::WindowScreenEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_screen_equivalent_layer_materials",
  "crates/ep_compare/src/eio/window_material_screen_equivalent_layer.rs::parse_eio_window_material_screen_equivalent_layer",
  "crates/ep_cli/src/window_material_screen_equivalent_layer.rs::run_compare_window_material_screen_equivalent_layer",
  "crates/ep_model/src/objects/materials/window_blind.rs::WindowBlindSlatOrientation",
  "crates/ep_model/src/objects/materials/window_blind.rs::WindowBlindMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_blind_materials",
  "crates/ep_compare/src/eio/window_material_blind.rs::parse_eio_window_material_blind",
  "crates/ep_cli/src/window_material_blind.rs::run_compare_window_material_blind",
  "crates/ep_model/src/objects/materials/window_blind_equivalent_layer.rs::WindowBlindEquivalentLayerSlatAngleControl",
  "crates/ep_model/src/objects/materials/window_blind_equivalent_layer.rs::WindowBlindEquivalentLayerMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_blind_equivalent_layer_materials",
  "crates/ep_compare/src/eio/window_material_blind_equivalent_layer.rs::parse_eio_window_material_blind_equivalent_layer",
  "crates/ep_cli/src/window_material_blind_equivalent_layer.rs::run_compare_window_material_blind_equivalent_layer",
  "crates/ep_model/src/objects/materials/roof_vegetation.rs::RoofVegetationMoistureDiffusionMethod",
  "crates/ep_model/src/objects/materials/roof_vegetation.rs::RoofVegetationMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_roof_vegetation_materials",
  "crates/ep_cli/src/material_roof_vegetation.rs::run_compare_material_roof_vegetation",
  "crates/ep_model/src/objects/materials/window_glazing.rs::WindowGlazingThermochromicState",
  "crates/ep_model/src/objects/materials/window_glazing.rs::WindowGlazingThermochromicGroupMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_glazing_thermochromic_group_materials",
  "crates/ep_model/src/objects/materials/window_simple_glazing.rs::WindowSimpleGlazingMaterial",
  "crates/ep_model/src/objects/materials/window_simple_glazing.rs::WindowSimpleGlazingMaterial::from_performance_indices",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_simple_glazing_system_materials",
  "crates/ep_cli/src/window_material_simple_glazing_system.rs::run_compare_window_material_simple_glazing_system",
  "crates/ep_model/src/objects/materials/window_complex_gap.rs::WindowComplexGapGasComposition",
  "crates/ep_model/src/objects/materials/window_complex_gap.rs::WindowComplexGapSupportPillar",
  "crates/ep_model/src/objects/materials/window_complex_gap.rs::WindowComplexGapMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_complex_gap_materials",
  "crates/ep_cli/src/window_material_gap.rs::run_compare_window_material_gap",
  "crates/ep_model/src/objects/materials/window_complex_shade.rs::WindowComplexShadeLayerType",
  "crates/ep_model/src/objects/materials/window_complex_shade.rs::WindowComplexShadeMaterial",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_window_complex_shade_materials",
  "crates/ep_cli/src/window_material_complex_shade.rs::run_compare_window_material_complex_shade",
  "crates/ep_model/src/objects/glazing_spectral_data.rs::GlazingSpectralPoint",
  "crates/ep_model/src/objects/glazing_spectral_data.rs::GlazingSpectralData",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_glazing_spectral_data",
  "crates/ep_model/src/objects/material_variable_absorptance.rs::MaterialVariableAbsorptance",
  "crates/ep_model/src/objects/material_variable_absorptance.rs::VariableAbsorptanceControl",
  "crates/ep_model/src/objects/material_variable_absorptance.rs::VariableAbsorptanceSurfaceBinding",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_variable_absorptances",
  "crates/ep_compiler/src/compiler/variable_absorptance_surface.rs::Compiler::build_variable_absorptance_surface_list",
  "crates/ep_model/src/objects/material_phase_change_hysteresis.rs::MaterialPhaseChangeHysteresis",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_phase_change_hystereses",
  "crates/ep_model/src/objects/material_phase_change.rs::MaterialPhaseChange",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_phase_changes",
  "crates/ep_model/src/objects/material_variable_thermal_conductivity.rs::MaterialVariableThermalConductivity",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_variable_thermal_conductivities",
  "crates/ep_model/src/objects/material_moisture_penetration_depth_settings.rs::MaterialMoisturePenetrationDepthSettings",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_moisture_penetration_depth_settings",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_settings.rs::MaterialHeatAndMoistureTransferSettings",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_settings",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_sorption_isotherm.rs::MaterialHeatAndMoistureTransferSorptionIsotherm",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_sorption_isotherms",
  "crates/ep_compiler/src/compiler.rs::source_effective_hamt_sorption_points",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_suction.rs::MaterialHeatAndMoistureTransferSuction",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_suctions",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_redistribution.rs::MaterialHeatAndMoistureTransferRedistribution",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_redistributions",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_diffusion.rs::MaterialHeatAndMoistureTransferDiffusion",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_diffusions",
  "crates/ep_model/src/objects/material_heat_and_moisture_transfer_thermal_conductivity.rs::MaterialHeatAndMoistureTransferThermalConductivity",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_material_heat_and_moisture_transfer_thermal_conductivities",
  "crates/ep_model/src/objects/surface_vapor_coefficients.rs::SurfaceVaporCoefficients",
  "crates/ep_compiler/src/compiler.rs::Compiler::parse_surface_vapor_coefficients",
  "crates/ep_compiler/src/compiler.rs::Compiler::validate_construction_material_layers",
  "crates/ep_compiler/src/compiler.rs::Compiler::validate_shaded_window_material_layers",
  "crates/ep_compare/src/eio/window_material_screen.rs::parse_eio_window_material_screen",
  "crates/ep_cli/src/window_material_screen.rs::run_compare_window_material_screen",
  "crates/ep_cli/src/window_material_screen_optics.rs::calculate_screen_source_optics",
  "crates/ep_compare/src/eio/material_details.rs::parse_eio_material_details",
  "crates/ep_compare/src/eio/window_material_shade.rs::parse_eio_window_material_shade",
  "crates/ep_cli/src/main.rs::run_compare_window_material_gas_mixture",
  "crates/ep_cli/src/window_material_shade.rs::run_compare_window_material_shade",
  "crates/ep_run/src/support/runtime_boundaries.rs::assess_typed_runtime_boundaries",
]
status = "scaffold"
first_case = "construction_materials_001"
family_cases = [
  "material_opaque_variants_001",
  "window_glazing_spectral_average_001",
  "window_glazing_refraction_extinction_001",
  "window_glazing_equivalent_layer_001",
  "window_material_gas_001",
  "window_material_gap_equivalent_layer_001",
  "window_material_gas_mixture_001",
  "window_material_shade_001",
  "window_material_shade_equivalent_layer_001",
  "window_material_drape_equivalent_layer_001",
  "window_material_screen_001",
  "window_material_screen_equivalent_layer_001",
  "window_material_blind_001",
  "window_material_blind_equivalent_layer_001",
  "material_roof_vegetation_001",
  "window_material_simple_glazing_system_001",
  "window_material_gap_001",
  "window_material_complex_shade_001",
]
proof_variables = [
  "Construction CTF Layer Count",
  "Material CTF Summary Thermal Resistance",
  "WindowMaterial:Gap:EquivalentLayer Layer Occurrences",
  "WindowMaterial:GasMixture Generic Definition Details",
  "WindowMaterial:Shade Generic Definition Details",
  "WindowMaterial:Shade Layer Occurrences",
  "WindowMaterial:Shade:EquivalentLayer Generic Definition Details",
  "WindowMaterial:Shade:EquivalentLayer Layer Occurrences",
  "WindowMaterial:Drape:EquivalentLayer Generic Definition Details",
  "WindowMaterial:Drape:EquivalentLayer Layer Occurrences",
  "WindowMaterial:Screen Generic Definition Details",
  "WindowMaterial:Screen Layer Occurrences",
  "WindowMaterial:Screen:EquivalentLayer Generic Definition Details",
  "WindowMaterial:Screen:EquivalentLayer Layer Occurrences",
  "WindowMaterial:Blind Generic Definition Details",
  "WindowMaterial:Blind Layer Occurrences",
  "WindowMaterial:Blind:EquivalentLayer Generic Definition Details",
  "WindowMaterial:Blind:EquivalentLayer Layer Occurrences",
  "Material:RoofVegetation Generic Definition Details",
  "WindowMaterial:SimpleGlazingSystem Generic Definition Details",
  "WindowMaterial:Gap Generic Definition Details",
  "WindowMaterial:ComplexShade Generic Definition Details",
]
claim_level = "none"
support_boundary = "CP58 material-family scaffold: all 34 EnergyPlus 26.1 public material base/overlay objects and their source-owned order are inventoried. The bounded typed implementation covers Material, Material:NoMass, Material:AirGap, Material:InfraredTransparent, WindowMaterial:Glazing:RefractionExtinctionMethod, WindowMaterial:Glazing:EquivalentLayer, WindowMaterial:Gas, WindowMaterial:Gap:EquivalentLayer, WindowMaterial:GasMixture, WindowMaterial:Shade, WindowMaterial:Shade:EquivalentLayer, and WindowMaterial:Drape:EquivalentLayer plus only the SpectralAverage branch of WindowMaterial:Glazing; unsupported optical modes explicitly fail closed. RefractionExtinction preserves its raw n/k inputs and EnergyPlus 26.1 formula/visible-back quirk. Equivalent-layer glazing preserves all 26 numeric inputs, six bounded Autocalculate states, source defaults, and the SpectralAverage-only contract without adding optical-sum validation absent from its source block; its ignored spectral-dataset string is type-checked but not retained. Ordinary Gas resolves Air, Argon, Krypton, and Xenon to exact built-in constants after type/schema checking supplied custom fields; Custom preserves the three A+B*T+C*T^2 coefficient sets, validates positive 300 K conductivity and source/schema bounds, derives nominal resistance, and intentionally retains numeric zero for a missing specific-heat ratio. Equivalent-layer Gap reuses those gas records with its schema-distinct uppercase gas enum and required Sealed, VentedIndoor, or VentedOutdoor mode, without inventing the schema note's unenforced Air-only relationship. GasMixture encodes a one-to-four active prefix of standard-only gases, validates the schema-required Gas 1/Gas 2 pairs even for count 1, preserves missing active optional Gas 3/4 fractions as zero, and allows duplicate species and non-unit sums without normalization; nominal resistance uses only the first gas at 300 K. MaterialFamily separates opaque, ordinary fenestration, and equivalent-layer consumers. Ordinary Construction rejects equivalent-layer glazing, gaps, shades, and drapes because only the still-deferred Construction:WindowEquivalentLayer may consume them, while the bounded ordinary-window subset accepts exactly Glass ((Gas|GasMixture) Glass){0..3}. Arbitrary-run support assessment explicitly counts and run-blocks the complete typed RefractionExtinction, all four EquivalentLayer objects, Gas, GasMixture, and ordinary Shade before execution. Existing material_opaque_variants_001, window_glazing_spectral_average_001, and window_glazing_refraction_extinction_001 provide only their declared nonblocking static EIO evidence. window_glazing_equivalent_layer_001 adds bounded external diagnostic evidence for the dedicated 18-token EIO row, and window_material_gas_001 adds duplicate-aware ordinary-window gas-layer occurrence evidence with exact EnergyPlus 26.1 {:.3R} thickness serialization. window_material_gap_equivalent_layer_001 adds a clean nonblocking exact seven-occurrence fixture sequence across all five gas types and all three vent modes, with one reused Argon material and one unused Air definition excluded; material name, canonical gas/vent type, and source-formatted thickness are exact. Construction names/declarations are aligned with converted-epJSON canonical order, so arbitrary IDF declaration-order parity remains unclaimed. EIO omits Custom coefficients/properties and nominal resistance. EnergyPlus emits no dedicated GasMixture construction-layer data row. window_material_gas_mixture_001 instead adds a nonblocking by-name gate for all six generic Material Details definition rows: identity, exactly-one multiplicity including the unused definition, MediumRough, source-{:.4R}-serialized thickness, and the fixed zero resistance, conductivity, density, specific-heat, and absorptance fields are exact. Its shared WindowMaterial:Gas header is exact and has zero data rows. The generic rows expose no mixture component count/species/fractions/order, nominal resistance, or occurrence/use/reuse status; two exact seven-layer WindowConstruction rows and host/window rows are oracle-only fixture locks. Construction:WindowEquivalentLayer typing/ratings, the source ASHWAT coefficient-copy/evaluation quirks, its construction-local sub-0.0001 m thickness replacement, window surfaces, optics/thermal behavior, runtime execution, and conformance remain unclaimed. At CP58, GlazingSpectralData, the then-remaining 9 base definitions, and all 12 overlays/datasets were deferred; that scaffold/source-mapped entry added no conformance claim. Current cumulative widening is recorded in the addendum below. CP98 additionally state-maps only the immutable post-GetBuildingData variable-absorptance selection for retained typed detailed surfaces and construction-layer warnings; all overlay runtime behavior remains blocked."
support_boundary_addendum = "The CP58 Shade, Drape, Screen, Screen EquivalentLayer, Blind, Blind EquivalentLayer, and RoofVegetation checkpoints, the CP64 Thermochromic typed checkpoint, the CP65 SimpleGlazing typed checkpoint, the CP67 WindowMaterial:Gap typed checkpoint, and the CP69 WindowMaterial:ComplexShade typed checkpoint extend the bounded typed list through source-order object 22. CP70 adds only a bounded ComplexShade generic-definition diagnostic and does not widen that typed boundary. CP71 leaves the inventory at 22 typed Rust material variants: 21 complete bounded base-definition slices plus 1 partial WindowMaterial:Glazing slice, and adds the separate complete bounded MaterialProperty:GlazingSpectralData dataset. CP72 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:VariableAbsorptance overlay, raising complete bounded public-object slices to 23/34, leaving 1/34 partial, and reducing wholly deferred public objects to 10/34. CP73 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:PhaseChangeHysteresis attachment, raising complete bounded public-object slices to 24/34, leaving 1/34 partial, and reducing wholly deferred public objects to 9/34. CP74 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:PhaseChange temperature-enthalpy attachment, raising complete bounded public-object slices to 25/34, leaving 1/34 partial, and reducing wholly deferred public objects to 8/34. CP75 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:VariableThermalConductivity temperature-conductivity attachment, raising complete bounded public-object slices to 26/34, leaving 1/34 partial, and reducing wholly deferred public objects to 7/34. CP76 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:MoisturePenetrationDepth:Settings attachment, raising complete bounded public-object slices to 27/34, leaving 1/34 partial, and reducing wholly deferred public objects to the six HAMT families. CP77 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:Settings attachment, raising complete bounded public-object slices to 28/34, leaving 1/34 partial, and reducing wholly deferred public objects to the five dependent HAMT datasets. CP78 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:SorptionIsotherm attachment, raising complete bounded public-object slices to 29/34, leaving 1/34 partial, and reducing wholly deferred public objects to the four later HAMT datasets. CP79 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:Suction attachment, raising typed material overlays to 8/12 and complete bounded public-object slices to 30/34, leaving 1/34 partial, and reducing wholly deferred public objects to the three later HAMT datasets. CP80 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:Redistribution attachment, raising typed material overlays to 9/12 and complete bounded public-object slices to 31/34, leaving 1/34 partial, and reducing wholly deferred public objects to the two later HAMT datasets. CP81 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:Diffusion attachment, raising typed material overlays to 10/12 and complete bounded public-object slices to 32/34, leaving 1/34 partial, and reducing wholly deferred public objects to the final MaterialProperty:HeatAndMoistureTransfer:ThermalConductivity dataset. CP82 keeps the 22 material variants unchanged and adds the complete bounded MaterialProperty:HeatAndMoistureTransfer:ThermalConductivity attachment, raising typed material overlays to 11/12 and complete bounded public-object slices to 33/34, leaving 1/34 partial, and reducing wholly deferred public objects to 0/34. The dataset, overlay, hysteresis attachment, phase-change attachment, variable-thermal-conductivity attachment, EMPD settings attachment, HAMT Settings attachment, HAMT Sorption attachment, HAMT Suction attachment, HAMT Redistribution attachment, HAMT Diffusion attachment, and HAMT ThermalConductivity attachment are held outside MaterialDefinition in separate deterministic arenas; the dataset's fixed-then-extension point vector, the PhaseChange temperature/enthalpy vector, the VariableThermalConductivity temperature/conductivity vector, and the Sorption, Suction, Redistribution, Diffusion, and ThermalConductivity input/effective point vectors preserve or materialize their respective source state, while repeated variable-absorptance, hysteresis, PhaseChange, VariableThermalConductivity, EMPD settings, HAMT Settings, HAMT Sorption, HAMT Suction, HAMT Redistribution, HAMT Diffusion, or HAMT ThermalConductivity targets fail closed until the applicable source multiplicity/order is recovered.\n\nMaterialProperty:GlazingSpectralData now has a complete bounded standalone typed-input checkpoint in the GetWindowGlassSpectralData common-startup stage before all 22 GetMaterialData base definitions. The compiler reconstructs the positional numeric stream from fixed quartets 1 through 4 followed by extension quartets: a name-only object has zero points; without extensions the last present fixed field determines the span and omitted positions inside it read as zero; a non-empty extension establishes all four fixed quartets before the extension points. Zero through 800 complete quartets are accepted, while an incomplete final quartet or excess point count fails closed. Each dataset is validated even when unused. Transmittance below 0.001 is silently replaced by 0.001; wavelength must remain in [0.1,4.0] microns and strictly increase; transmittance is at most 1.01; directional reflectances are in [0,1.02]; and transmittance plus either reflectance is at most 1.03. Normalized duplicates and invalid datasets fail closed. Valid unused datasets are deliberately runtime-inert and add no arbitrary-run blocker. Active WindowMaterial:Glazing Spectral reference resolution and consumption remain compiler-blocked, while SpectralAndAngle and BSDF remain blocked on their separate paths. Broad dataset declaration-order parity, interpolation, angular or hemispherical optics, WindowManager consumers, constructions, surfaces, EIO serialization, runtime numerical behavior, exact diagnostic text/order/multiplicity, and conformance remain nonclaims. GetWindowGlassSpectralData emits no standalone EIO row, so CP71 adds no case manifest or proof variable.\n\nMaterialProperty:VariableAbsorptance now has a complete bounded typed-input checkpoint after all base materials and after the compiler's shared schedule namespace is available. A separate overlay arena accepts only Material and Material:NoMass targets, applies the missing/blank SurfaceTemperature default, and preserves all four control modes. User schedules resolve to ScheduleId, Constant-0.0 and Constant-1.0 use explicit built-in sentinels, and the twenty EnergyPlus 26.1 Curve:* families plus Table:Lookup retain deferred raw object type and normalized name identities; dependency payload validation and evaluation remain deferred. Nonblank names that do not resolve remain source-null, while selected-family absence or any resolved opposite-family dependency fails closed. Names use a separate normalized namespace, invalid definitions reserve neither name nor target, ambiguous dependency identities fail, and a second overlay for one material fails closed because source-order last-wins IDF recovery is not yet present. Every overlay, including one on an unused material, is explicitly run-blocked. Exterior surface-list construction, triggers, clamping, the scheduled-solar pointer defect, EIO, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:PhaseChangeHysteresis now has a complete bounded typed-input checkpoint immediately after the variable-absorptance overlay. Its object key is the existing material reference and has no independent attachment namespace. A separate deterministic arena accepts only public Material and Material:NoMass targets, preserves all thirteen required finite strictly-positive numeric fields in grouped liquid/solid thermal states and melting/freezing curves, and stores the source-initialized transition specific-heat mean plus solid initial prior specific heat without replacing the base MaterialDefinition. The source defines no defaults, enums, upper bounds, or cross-field relationships. Blank or missing targets, wrong families, malformed or nonpositive values, and repeated targets fail before attachment identity or target reservation; a valid attachment may coexist with VariableAbsorptance on the same target. Every attachment, including one on an unused material, is explicitly run-blocked. Internal F/C-factor targets, material-pointer replacement and hasPCM state, mutable CondFD histories and hysteretic property evaluation, ThermalStorage:PCM, EMS pointer behavior, EIO, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:PhaseChange now has a complete bounded typed-input checkpoint in the first material-data pass of GetCondFDInput, after the common-startup PhaseChangeHysteresis attachment. Its object key is the existing material reference and has no independent attachment namespace. A separate deterministic arena accepts only public Material and Material:NoMass targets, preserves a missing or blank conductivity-temperature coefficient as finite zero, and owns the optional source-ordered vector of zero or more complete finite temperature/enthalpy pairs. The executable epJSON schema has no pair-count minimum or maximum, so zero, one, two, three, more than one hundred, and every other complete count remain valid despite the textual IDD's stale three-through-one-hundred description. Temperatures must strictly increase and enthalpies must be nondecreasing; equal or negative enthalpies and negative temperatures or coefficients remain valid. Blank or missing targets, wrong families, malformed/incomplete/nonfinite values, ordering violations, and repeated targets fail before attachment identity or target reservation; the base MaterialDefinition remains unchanged, and a valid attachment may coexist with VariableAbsorptance, PhaseChangeHysteresis, and VariableThermalConductivity. Eager validation and repeated-target failure deliberately fail closed relative to the source's no-CondFD lazy validation and last-wins overwrite. Rust applies the executable-schema default to an entirely empty object instead of reproducing EnergyPlus 26.1's native-epJSON negative-presence-count defect. Every attachment, including one on an unused or NoMass material, is explicitly run-blocked. Source-accepted one- and two-point tables stay typed but never execute because downstream CondFD assumes three points. Internal F/C-factor targets, the CondFD settings pass, MaterialFD/tk1/TempEnth and sentinel state, construction/surface/node allocation, interpolation and specific-heat/conductivity equations, Hysteresis/VTC precedence, EMS, generic CondFD reporting, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:VariableThermalConductivity now has a complete bounded typed-input checkpoint in the second material-data pass of GetCondFDInput, immediately after PhaseChange. Its object key is the existing material reference and has no independent attachment namespace, coefficient, or fixed numeric field. A separate deterministic arena accepts only public Material and Material:NoMass targets and owns the optional source-ordered vector of zero or more complete temperature/conductivity pairs. The executable epJSON schema has no item-count cap, so absent/empty and zero, one, two, three, 100, 101, and larger complete counts remain valid despite the textual IDD's inconsistent first-three-required and one-hundred-pair wording plus its declared 101st pair. Rust requires finite scalars and strictly increasing temperatures, while conductivity may be negative, zero, equal, or decreasing. EnergyPlus IDF execution can retain nonfinite numeric scalars and the VTC reader does not reject nonfinite conductivity, so finite-only Rust validation is a deliberate bounded fail-close. Blank or missing targets, wrong families, malformed/incomplete/nonfinite values, nonincreasing temperatures, and repeated targets fail before attachment identity or reservation; the base MaterialDefinition remains unchanged, and a valid attachment may coexist with VariableAbsorptance, PhaseChangeHysteresis, and PhaseChange. Eager validation and repeated-target failure deliberately fail closed relative to no-CondFD lazy validation and source last-wins assignment. Native empty VTC epJSON succeeds. Every attachment, including one on an unused or NoMass material, is explicitly run-blocked. Source-accepted one- and two-point tables stay typed but never execute because downstream consumers assume three points. Internal F/C-factor targets, the CondFD settings pass, MaterialFD/TempCond and sentinel state, interpolation, first-three activation and fallback, PhaseChange/Hysteresis/EMS precedence, EMPD/HAMT coexistence and Hysteresis subclass-lifetime hazards, CondFD state, generic reporting, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:MoisturePenetrationDepth:Settings now has a complete bounded typed-input checkpoint in a separate EMPD attachment arena after the two deterministic CondFD attachment passes; this compiler order does not claim a global source order among CondFD, EMPD, and HAMT managers. The object key is the existing material reference and has no independent namespace. Only public Material satisfies the source Regular-and-not-ROnly target gate. Each record preserves seven required finite fixed numeric inputs plus surface and deep penetration depths as AutoOrNumber, retaining missing, blank, and case-insensitive Autocalculate sentinels. Resistance and coating values are nonnegative, surface depth is strictly positive when explicit, deep depth is nonnegative, and a nonzero deep depth no greater than surface remains warning-only. Rust finite-only fixed-scalar validation deliberately fails closed relative to some EnergyPlus IDF NaN/Inf acceptance. Blank or missing targets, wrong families, malformed/missing/nonfinite/out-of-range fields, and repeated targets fail before attachment identity or reservation; the base MaterialDefinition remains unchanged, and existing VariableAbsorptance, Hysteresis, PhaseChange, and VariableThermalConductivity descriptors may coexist. Rust eagerly validates all definitions and rejects repetition relative to the source lazy semantic reader and case-only last-processed replacement. Every settings attachment, including one on an unused material, is explicitly run-blocked. Internal F/C-factor targets, automatic one-day/three-week depth materialization, MaterialEMPD replacement, construction/zone placement, moisture and latent state/history, nine output variables, Construction EMPD EIO, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:Settings now has a complete bounded typed-input checkpoint as the first pass of GetHeatBalHAMTInput, after the deterministic EMPD attachment in compiler order without claiming a global CondFD/EMPD/HAMT source order. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid for distinct targets. A separate deterministic arena accepts only public Material targets, preserves required finite porosity in [0,1], and applies the missing/blank 0.2 default or explicit finite nonnegative value for initial water-content ratio without an upper or cross-field rule. Rust finite-only validation deliberately fails closed relative to EnergyPlus IDF execution: initial water content can retain NaN or positive infinity, porosity infinity is rejected, and porosity NaN reaches an EnergyPlus 26.1 crash in the bounded oracle. Material:NoMass is warning-ignored by the source R-only branch, so Rust deliberately fails closed instead of silently discarding it. Invalid target/field records and repeated case-insensitive material targets fail before ID or ownership reservation; outer-key collisions do not. The base MaterialDefinition remains unchanged, and earlier VariableAbsorptance, Hysteresis, PhaseChange, VariableThermalConductivity, and EMPD descriptors may coexist while every combination remains run-blocked. Rust validates eagerly relative to the source first-HAMT-surface lazy reader and rejects repetition relative to later pointer replacement. Internal F/C-factor targets, MaterialHAMT/hasHAMT replacement and cross-subclass lifetime hazards, the typed SorptionIsotherm, Suction, Redistribution, Diffusion, and ThermalConductivity runtime consumers, the now-typed final SurfaceProperties:VaporCoefficients pass's boundary-vapor-transfer and runtime consumers, all-layer construction eligibility, constant-conductivity fallback, cell allocation/interpolation and nonlinear state, initial-water conversion, six fixed surface outputs, three per-cell output families, HAMT cells/origins and generic nominal-resistance EIO, SQLite porosity reporting, runtime numerics, broad diagnostics/order, and conformance remain nonclaims; there is no Settings-specific EIO row and no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:SorptionIsotherm now has a complete bounded typed-input checkpoint as the second pass of GetHeatBalHAMTInput, immediately after Settings. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid. A separate deterministic arena requires the same public MaterialId to own a typed Settings attachment and stores the Settings ID, an exact integer count from 1 through 25, the count-selected zero-filled input prefix, source-effective points, and a correction flag. The schema/IDD tail is 25 fixed pairs rather than an extension: pair 1 is required, optional active-prefix fields become zero, all supplied fields are validated even beyond count, and valid beyond-count fields are then ignored. RH is finite in [0,1]; moisture is finite and nonnegative without an upper bound. Rust appends (1.01, porosity*1000) and (0,0), reproduces the source nested-swap RH sort and up to 100 left-to-right adjacent-moisture averaging passes, and records a warning state on correction. Derived nonfinite state from finite-input addition overflow fails closed. Exact-integer, finite-only, derived-finite, eager semantic, and duplicate-target rules deliberately fail closed relative to EnergyPlus IDF fractional-count rounding, retained NaN/positive infinity, observed active-HAMT severe/access-violation paths, lazy first-surface reading, and later-effective replacement. Every definition, including one on an unused material, is run-blocked. Typed Suction and Redistribution endpoint materializations independently consume the indexed last source-effective Sorption moisture, typed Diffusion consumes the indexed last source-effective Sorption relative humidity, and typed ThermalConductivity consumes the indexed last source-effective Sorption moisture. Their runtime interpolation and other Sorption consumers, construction/cell state, outputs/EIO/SQLite, runtime numerics, exact diagnostic order/text/count, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:Suction now has a complete bounded typed-input checkpoint as the third pass of GetHeatBalHAMTInput, immediately after SorptionIsotherm. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid. A separate deterministic arena requires the same public MaterialId to own a typed SorptionIsotherm attachment and stores the SorptionIsotherm ID, an exact integer count from 1 through 25, the count-selected component-wise zero-filled authored-order input prefix, and source-effective points. The schema/IDD tail is 25 fixed moisture/coefficient pairs rather than an extension: pair 1 is required, optional active-prefix fields become zero, all supplied fields are validated even beyond count, and valid beyond-count fields are then ignored. Both columns are finite and nonnegative without an upper bound. Unordered, duplicate, or descending points and arbitrary cross-column relationships remain valid because the source performs no Suction sort, correction, monotonicity or uniqueness check, or warning. Rust appends exactly one point whose moisture is the indexed last source-effective Sorption moisture and whose coefficient is the last active Suction coefficient; it does not select a maximum or recompute porosity times 1000. Exact-integer and finite-only rules are deliberate bounded fail-closes; exact EnergyPlus IDF fractional-count, nonfinite, and missing-Sorption failure behavior remain nonclaims. Eager semantic validation, explicit Sorption dependency, and duplicate-target rejection also fail closed relative to source lazy first-surface reading, unsafe missing-Sorption indexing, and later-effective replacement. The typed Redistribution, Diffusion, and ThermalConductivity source-order siblings may coexist on the same target and independently require SorptionIsotherm, not Suction or another sibling. Every definition, including one on an unused material, is run-blocked. Typed Diffusion and ThermalConductivity interpolation, rain-selected Suction and ordinary Redistribution interpolation, construction/cell state, outputs/EIO/SQLite, runtime numerics, exact diagnostic order/text/count, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:Redistribution now has a complete bounded typed-input checkpoint as the fourth pass of GetHeatBalHAMTInput, immediately after Suction. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid. A separate deterministic arena requires the same public MaterialId to own a typed SorptionIsotherm attachment and stores the SorptionIsotherm ID, an exact integer count from 1 through 25, the count-selected component-wise zero-filled authored-order input prefix, and source-effective points. Suction is a source-order predecessor but not a dependency, so Material plus Settings plus SorptionIsotherm plus Redistribution succeeds without Suction. The schema/IDD tail is 25 fixed moisture/coefficient pairs rather than an extension: pair 1 is required, optional active-prefix fields independently become zero, all supplied fields are validated even beyond count, and valid beyond-count fields are then ignored. Both columns are finite and nonnegative without an upper bound. Unordered, duplicate, or descending points and arbitrary cross-column relationships remain valid because the source performs no Redistribution-table sorting, correction, monotonicity or uniqueness check, clamp, or table-validation warning. Rust appends exactly one point whose moisture is the indexed last source-effective Sorption moisture and whose coefficient is the last active Redistribution coefficient; it does not select a maximum or recompute porosity times 1000. Exact-integer and finite-only rules are deliberate bounded fail-closes; exact EnergyPlus IDF fractional-count, nonfinite, and missing-Sorption failure behavior remain nonclaims. Eager semantic validation, explicit Sorption dependency, and duplicate-target rejection also fail closed relative to source lazy first-surface reading, unsafe missing-Sorption indexing, and later-effective replacement. The typed Diffusion and ThermalConductivity source-order successors may coexist on the same target and independently require SorptionIsotherm, not Redistribution or each other. Every definition, including one on an unused material, is run-blocked. Diffusion and ThermalConductivity interpolation, rain-selected Suction and ordinary Redistribution interpolation, construction/cell state, outputs/EIO/SQLite, runtime numerics, exact diagnostic order/text/count including the aggregate unused-HAMT warning, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:Diffusion now has a complete bounded typed-input checkpoint as the fifth pass of GetHeatBalHAMTInput, immediately after Redistribution. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid. A separate deterministic arena requires the same public MaterialId to own a typed SorptionIsotherm attachment and stores the SorptionIsotherm ID, an exact integer count from 1 through 25, the count-selected component-wise zero-filled authored-order input prefix, and source-effective points. Suction and Redistribution are source-order predecessors but neither is a dependency, so Material plus Settings plus SorptionIsotherm plus Diffusion succeeds without either transport attachment. The schema/IDD tail is 25 fixed relative-humidity/resistance-factor pairs rather than an extension: pair 1 is required, optional active-prefix fields independently become zero, all supplied fields are validated even beyond count, and valid beyond-count fields are then ignored. Relative humidity is finite in inclusive [0,1], and the water-vapor diffusion resistance factor is finite and nonnegative without an upper bound. Unordered, duplicate, or descending relative humidities and arbitrary resistance-factor relationships remain valid because the source performs no Diffusion-table sorting, correction, monotonicity or uniqueness check, clamp, cross-column rule, or table-validation warning. Rust appends exactly one point whose relative humidity is the indexed last source-effective Sorption relative humidity and whose resistance factor is the last active Diffusion factor; it does not select a maximum, use the Sorption moisture column, or hard-code 1.01. Exact-integer and finite-only rules are deliberate bounded fail-closes; exact EnergyPlus IDF fractional-count, nonfinite, and missing-Sorption failure behavior remain nonclaims. Eager semantic validation, explicit Sorption dependency, and duplicate-target rejection also fail closed relative to source lazy first-surface reading, unsafe missing-Sorption indexing, and later-effective replacement. The typed ThermalConductivity source-order successor may coexist on the same target and independently requires SorptionIsotherm, not Suction, Redistribution, or Diffusion. Every definition, including one on an unused material, is run-blocked. Diffusion and ThermalConductivity interpolation into cell vapor resistance and conductivity, construction/cell state, outputs/EIO/SQLite, runtime numerics, exact diagnostic order/text/count including the aggregate unused-HAMT warning, and conformance remain nonclaims; no case manifest or proof variable is added.\n\nMaterialProperty:HeatAndMoistureTransfer:ThermalConductivity now has a complete bounded typed-input checkpoint as the sixth material pass of GetHeatBalHAMTInput, immediately after Diffusion. The required inner material_name is the semantic target; the outer epJSON key is a nonsemantic normalized diagnostic snapshot with no name map, so blank and case-colliding outer keys remain valid. A separate deterministic arena requires the same public MaterialId to own a typed SorptionIsotherm attachment and stores the SorptionIsotherm ID, an exact integer count from 1 through 25, the count-selected component-wise zero-filled authored-order input prefix, and source-effective points. Suction, Redistribution, and Diffusion are source-order predecessors but not dependencies, so Material plus Settings plus SorptionIsotherm plus ThermalConductivity succeeds without any of them. The schema/IDD tail is 25 fixed moisture/conductivity pairs rather than an extension: pair 1 is required, optional active-prefix fields independently become zero, all supplied fields are validated even beyond count, and valid beyond-count fields are then ignored. Supplied moisture is finite and nonnegative; supplied conductivity is finite and strictly positive; neither has an upper bound. Missing optional active conductivity nevertheless retains source numeric-blank zero. Unordered, duplicate, or descending points and arbitrary cross-column relationships remain valid because the source performs no ThermalConductivity-table sorting, correction, monotonicity or uniqueness check, clamp, cross-column rule, or table-validation warning. Rust appends exactly one point whose moisture is the indexed last source-effective Sorption moisture and whose conductivity is the last active ThermalConductivity value; it does not select a maximum or recompute porosity times 1000. Exact-integer and finite-only rules are deliberate bounded fail-closes; exact EnergyPlus IDF fractional-count, nonfinite, and missing-Sorption failure behavior remain nonclaims. Eager semantic validation, explicit Sorption dependency, and duplicate-target rejection also fail closed relative to source lazy first-surface reading, unsafe missing-Sorption indexing, and later-effective replacement. Rust does not synthesize an absent-object table: EnergyPlus later creates constant points from positive base conductivity only for an active HAMT construction, with a warning. That fallback, its error branches, thermal-conductivity interpolation, construction/cell state, outputs/EIO/SQLite, runtime numerics, exact diagnostic order/text/count including the aggregate unused-HAMT warning, and conformance remain nonclaims. Every definition, including one on an unused material, is run-blocked; no case manifest or proof variable is added.\n\nWindowMaterial:GlazingGroup:Thermochromic now has a bounded typed-input checkpoint: each parent holds a range into an ordered `(optical_data_temperature_c, MaterialId)` arena; case-insensitive child references accept only the currently typed ordinary SpectralAverage and RefractionExtinction glazing branches, while duplicate child references, duplicate temperatures, and unsorted input order are preserved. Missing or empty state arrays restore the IDD minimum-one contract and fail closed against the EnergyPlus 26.1 empty-parent crash. The dedicated ThermochromicGroup family remains distinct from executable glazing; bounded ordinary Construction consumption substitutes each parent's first state and retains the final parent's master metadata, while every arbitrary run remains blocked, including unused definitions. CreateTCConstructions, temperature-driven state switching, optics, thermal behavior, daylighting, surfaces, outputs, EIO, runtime, and conformance remain deferred; generic Material Details EIO evidence is explicitly excluded because the parent Invalid(-1) roughness would enter undefined source serialization behavior.\n\nWindowMaterial:SimpleGlazingSystem now has a complete bounded typed-input and Material-owned SetupSimpleWindowGlazingSystem block-model checkpoint. The payload preserves positive U-factor, strict (0,1) SHGC, and optional strict (0,1) visible transmittance as Option state, then reproduces winter-film removal, effective resistance/thickness/conductivity, the U-factor and SHGC branch thresholds, solar and visible normal-incidence properties, fixed VerySmooth/infrared/emissivity/dirt/solar-diffusing values, and the EnergyPlus 26.1 intermediate-U summer-film `(low - high) * fraction + low` reversed interpolation quirk. A non-positive derived layer resistance emits the dedicated warning, clamps to 0.001 m2-K/W, and still materializes. A finite schema-valid tiny positive U-factor can instead overflow reciprocal resistance and derive zero conductivity; EnergyPlus emits Severe then Fatal without a material row, while Rust preserves that fail-closed outcome with InvalidSimpleGlazingDerivedConductivity before identity reservation or materialization. The shared source-ordered namespace loads it after Thermochromic, so it cannot be a Thermochromic child. The dedicated SimpleGlazing family accepts only a sole-layer ordinary Construction, preserves the original material identity, and classifies that construction as Fenestration; multi-layer or shaded references fail closed, and arbitrary-run assessment counts and blocks every definition including consumed and unused ones. window_material_simple_glazing_system_001 adds bounded nonblocking generic Material Details evidence through crates/ep_cli/src/window_material_simple_glazing_system.rs::run_compare_window_material_simple_glazing_system and the WindowMaterial:SimpleGlazingSystem Generic Definition Details proof variable. Its warning-free no-zone fixture keeps all Z,M,A definitions unused in fixture-local source IDF order: Z uses U-factor 2.7 with missing visible transmittance, M keeps U-factor 2.7 while changing SHGC and supplying visible transmittance and therefore has an identical generic numeric payload, and A uses U-factor 5 for a distinct thermal payload. The both-selector and Materials-only lanes each emit exactly one header plus three exact 11-token rows; Constructions-only and blank/default emit neither header nor rows. Every row is VerySmooth with zero density and specific heat, 0.84 thermal absorptance, and zero solar and visible absorptance; resistance, thickness, and absorptances use source-{:.4R}, while conductivity, density, and specific heat use source-{:.3R}. The fixture emits no specialized WindowMaterial:Glazing or WindowConstruction header/data. SHGC, visible transmittance, derived normal-incidence optics, specialized ReportGlass/Construction behavior, use/reuse occurrence, angular/hemispherical optics, ratings, Rust EIO serialization, runtime, broad diagnostic/declaration-order parity, and conformance remain nonclaims, and neither GetMaterialData nor ReportGlass is promoted wholesale.\n\nWindowMaterial:Gap now has a complete bounded typed-input checkpoint as source-order object 21. Its own thickness is positive, pressure defaults to 101325 Pa and must be positive, and the schema-exact trailing-underscore `gas_or_gas_mixture_` reference resolves case-insensitively only to an already compiled WindowMaterial:Gas or WindowMaterial:GasMixture. The payload copies one gas species/property record or the ordered mixture state and retains the source MaterialId only as graph provenance; referenced name, thickness, nominal resistance, and consumer identity are not copied, and the gap invents no nominal resistance. WindowGap:DeflectionState and WindowGap:SupportPillar stay raw-only helper families outside the 34-object inventory and are resolved lazily only when referenced. Deflection thickness, initial temperature, and initial pressure are validated as nonnegative, but only deflected thickness is copied; the two initial fields are discarded. Pillar spacing and radius are positive and are the only copied pillar fields. No nominal/deflected-thickness or radius/spacing relationship absent from the source is added. Missing, ambiguous, malformed, and wrong-family references fail closed; this replaces EnergyPlus 26.1's unchecked wrong-family cast, and all fallible validation/resolution precedes material identity reservation instead of reproducing early source allocation. The dedicated ComplexFenestration family makes ordinary Construction reject the gap in every position, while CP89's bounded Construction:ComplexFenestrationState consumer accepts it only at alternating gap positions between supported solid layers; complex-window execution remains deferred. Arbitrary-run support counts and blocks every definition including unused gaps. window_material_gap_001 adds bounded nonblocking generic Material Details evidence through crates/ep_cli/src/window_material_gap.rs::run_compare_window_material_gap and the WindowMaterial:Gap Generic Definition Details proof variable. Its warning-free no-zone source-IDF fixture keeps three unused definitions in fixture-local order Z DEFAULT PRESSURE, M SAME THICKNESS DIFFERENT STATE, A DIFFERENT THICKNESS. The both-selector and Materials-only lanes each emit exactly one exact 11-token generic header and one exact target row per Gap in Z,M,A order; Constructions-only and blank/default emit neither generic Material Details header nor data. Every target row is Rough and locks source-{:.4R} own thickness plus zero resistance, conductivity, density, specific heat, and thermal, solar, and visible absorptances. Z and M both report 0.0127 m despite different referenced gas, gap pressure, deflection, and pillar state; A reports 0.006 m. The required Gas and GasMixture definitions may emit two unrelated generic rows, and Materials-enabled lanes may emit the empty Material:Air header; neither is target Gap evidence. Constructions-enabled lanes may emit empty generic Construction CTF, Material CTF Summary, Material:Air CTF Summary, and CTF headers, which do not contradict the window-specific absence claim. No dedicated WindowMaterial:Gap, WindowMaterial:Glazing, or WindowConstruction header/data appears. ConvertInputFormat reorders the Gap definitions A,M,Z and the converted epJSON EIO follows that order, so only source-IDF fixture-local Z,M,A ordering is claimed. Gas species, mixture fractions and order, referenced gas identity and thickness, gap pressure, helper state, use or occurrence, complex-fenestration packing, deflection and pillar algorithms, optics and thermal behavior, surfaces, ratings, daylighting, Rust EIO serialization, runtime, broad diagnostics, and conformance remain unclaimed. This comparator does not promote GetMaterialData or another parent source routine wholesale.\n\nWindowMaterial:ComplexShade now has a complete bounded typed-input checkpoint as source-order object 22 immediately after WindowMaterial:Gap. The six case-insensitive layer types preserve VenetianHorizontal, VenetianVertical, Woven, Perforated, BSDF, and OtherShadingType, with OtherShadingType as the default. Thickness and conductivity remain positive; IR transmittance is in [0,1]; directional emissivities are in (0,1]; the five opening multipliers are in [0,1]; and all six slat fields retain their EnergyPlus 26.1 defaults and scalar schema bounds. The payload fixes Rough and resistance-only state, stores every thermal/opening/slat input, and deliberately maps BackEmissivity to scalar AbsorpThermal while retaining separate front/back emissivity. It invents no emissivity-plus-transmittance sum, no slat geometry outside the source branch, and no thickness/conductivity resistance or nominal resistance. Only VenetianHorizontal and VenetianVertical activate the Material.cc slat relationship checks. Curve zero is flat; a positive curve below SlatWidth/2 fails, while exact equality with SlatWidth/2 is accepted by Material.cc and therefore by this typed boundary even though downstream TARCOG later rejects it. Validation completes before shared identity reservation. The shared ComplexFenestration family makes ordinary Construction reject both WindowMaterial:Gap and WindowMaterial:ComplexShade in every position. CP89's bounded Construction:ComplexFenestrationState consumer accepts ComplexShade as a solid layer alongside SpectralAverage glazing and retains its directional absorptance matrix references; BSDF optics, TARCOG/WCE thermal behavior, opening-area conversion, slat execution, shading flags, surfaces, ratings, daylighting, EIO/reporting beyond the bounded generic diagnostic below, Rust serialization, runtime, broad diagnostics, and conformance remain deferred. Arbitrary-run assessment counts and blocks every ComplexShade definition, including unused definitions. The representative compiler evidence is compiler::tests::window_material_complex_shade::window_complex_shade_materializes_source_defaults_and_source_order; focused tests additionally cover all layer types/defaults, scalar and Venetian-only validation, shared-name/source-order behavior, fixed projections, ordinary-Construction rejection, typed coverage, and all-definition runtime blocking. window_material_complex_shade_001 adds bounded nonblocking diagnostic-only generic Material Details evidence through crates/ep_cli/src/window_material_complex_shade.rs::run_compare_window_material_complex_shade and the WindowMaterial:ComplexShade Generic Definition Details proof variable. Its warning-free no-zone source-IDF fixture keeps seven unused definitions in fixture-local Z,Y,X,W,V,U,T order. The both-selector and Materials-only lanes each emit one exact 11-token generic header plus seven exact target rows; Constructions-only and blank/default emit neither. Every target row is Rough, reports its own thickness and conductivity, maps BackEmissivity to thermal absorptance, and has zero resistance, density, specific heat, solar absorptance, and visible absorptance. Empty Material:Air and generic CTF headers remain outside target evidence, and no dedicated WindowMaterial:ComplexShade, WindowMaterial:Glazing, or WindowConstruction data row appears. ConvertInputFormat and converted epJSON use T,U,V,W,X,Y,Z, so only source-IDF fixture-local Z,Y,X,W,V,U,T order is claimed. Layer type, infrared transmittance, front emissivity, opening multipliers, slat geometry and properties, curve state, construction use or occurrence, Construction:ComplexFenestrationState packing, TARCOG/WCE/BSDF behavior, thermal behavior, Rust EIO serialization, runtime, broad diagnostics, and conformance remain nonclaims. This comparator does not promote GetMaterialData or another parent source routine wholesale.\n\nMaterial:RoofVegetation applies the EnergyPlus 26.1 defaults and exact inclusive/exclusive schema bounds to all fifteen numeric fields, preserves the six canonical roughness modes and Simple/Advanced moisture method, type-checks but deliberately discards the source-inactive Soil Layer Name, and projects the dry-soil mass properties plus thickness/conductivity resistance and thickness-density-specific-heat capacity through the Opaque family. Its only material-level cross-field behavior is the source warning and clamp when initial moisture exceeds saturation; residual-above-initial and other moisture relationships remain accepted. Ordinary Construction accepts RoofVegetation only as the outside layer; Rust deliberately fails closed on EnergyPlus 26.1's unchecked interior-only placement hole. Arbitrary-run assessment counts and blocks every typed definition, including unused definitions, as UnsupportedSurfaceBoundary/RunBlocked. material_roof_vegetation_001 adds a bounded nonblocking static EIO diagnostic through crates/ep_cli/src/material_roof_vegetation.rs::run_compare_material_roof_vegetation and the compare material-roof-vegetation CLI command. Its exact 11-token generic Material Details rows lock source Z,M,A definition order and exactly-one emission for used Z plus unused M and A; resistance, thickness, and absorptances use source-{:.4R}, while conductivity, density, and specific heat use source-{:.3R}. The primary Constructions,Materials and Materials-only lanes emit generic Z,M,A; Constructions-only does not. These values are a dry input snapshot, RoofVegetation has no dedicated row, and the shared CTF header plus used-Z row are oracle-only fixture locks. Plant/moisture/method/soil-label fields, CTF and construction parity, EcoRoof runtime and water balance, the one-used-material-across-surfaces rule, broad ordering or diagnostics, and conformance remain deferred. This bounded comparator does not promote GetMaterialData or any Material.cc routine wholesale. Ordinary WindowMaterial:Shade and WindowMaterial:Shade:EquivalentLayer retain their declared typed, runtime-blocking, Construction-family, and nonblocking generic/specialized EIO boundaries. WindowMaterial:Drape:EquivalentLayer preserves all thirteen source-effective numeric inputs: shared solar beam-beam transmittance defaults to zero in [0,0.2], four directional solar beam-diffuse fields are required in [0,1), three optional visible inputs preserve source-buffer blank zero in [0,1), infrared transmittance defaults to 0.05 in [0,1), directional emissivities default to 0.87 in (0,1), and pleat width/length default to zero with minimum zero. The compiler enforces only the source's front-solar and shared-visible strict-below-one sums plus its front-infrared greater-than-one rejection; exact front-infrared equality is accepted, and the source-omitted back-solar/back-infrared checks remain omitted. Solar beam-beam is copied front/back, visible values are stored front-only with zero back-visible state, MediumRough/resistance-only and thermal projections are fixed, and both effective pleat dimensions are retained only when both inputs are present and nonzero. The shared material namespace, EquivalentLayer classification, ordinary-Construction rejection, and explicit arbitrary-run block include every drape definition. window_material_drape_equivalent_layer_001 adds nonblocking EnergyPlus 26.1 static EIO evidence. Every fixture definition, including the definition unused by every construction, has exactly one generic MediumRough/all-zero Material Details row. The specialized fixture-local ordered occurrence sequence is A,Z,Z,P,Q and excludes that unused definition. It deliberately preserves the malformed source shape: the exact header has 14 tokens with separate front/back beam-beam labels and an empty column, while each data row has 12 tokens and emits the shared beam-beam value only once. N1, N2-N5, and N9-N11 use source-{:.4R} serialization; source-effective pleat dimensions use {:.5R}; visible N6-N8 are omitted. The specialized header requires Output:Constructions selecting Constructions, at least one drape definition, and at least one window construction, while Materials-only emits no header. Surfaces do not multiply construction occurrences, and surface-unused constructions still emit independently of surface use. Fixture-only EMS construction-index references merely suppress oracle unused-construction warnings and are not compared. Construction:WindowEquivalentLayer typing and packing, arbitrary declaration-order parity, ASHWAT drape coefficients, optics/thermal/pleat behavior, visible optical use, ratings, daylighting, surfaces, EMS behavior, runtime, broad diagnostic/order parity, and conformance remain unclaimed. WindowMaterial:Screen is a complete bounded typed object with exact reflected-beam enum/default handling, required reflectances and wire geometry, schema bounds/defaults, the strict diameter-below-spacing relationship, and source-derived open-area/solid-fraction optical, airflow, resistance-only, and nominal-resistance projections. Its three source strict-sum checks use adjusted values tau+raw*(1-tau), never the unadjusted tau+raw reflectance; valid bounded inputs make the adjusted sums algebraically less than one, while explicit evaluation retains finite-precision parity. It shares the normalized material namespace, is classified as ordinary Fenestration, and is accepted by ordinary Construction only as one exterior Screen directly before Glass ((Gas|GasMixture) Glass){0..3}; interior, between-glass, multiple-device, solar-diffusing, and Screen-Gap-Glass source-hole stacks fail closed. Arbitrary-run assessment counts and blocks every screen definition including unused definitions. window_material_screen_001 adds nonblocking EnergyPlus 26.1 static EIO evidence and two diagnostic proof variables. Its generic gate locks exactly one Z,M,A definition row including unused M, MediumRough, zero resistance/density/specific heat, source-{:.4R} wire diameter and solid-fraction absorptances, and source-{:.3R} conductivity. Its exact 12-token specialized header retains the Screen To GlassDistance source typo, and its A,Z,Z construction-layer occurrence sequence excludes M. The comparator reproduces the CalcScreenTransmittance normal-incidence path and reverse-order 18 by 18 CalcWindowScreenProperties quarter-hemisphere integration for the fixture's ExteriorScreen-activated A and Z materials, then locks source-{:.5R} thickness and source-{:.3R} conductivity, thermal absorptance, normal beam transmittance and solar/visible reflectance, diffuse solar/visible reflectance, diameter/spacing ratio, and glass distance. A constant-zero schedule still initializes both materials; two surfaces sharing C do not multiply its Z row, and surface-unused D emits a second Z row because C initialized the shared material. The primary, Materials-only, and Constructions-only lanes independently lock generic/specialized report activation and complete with zero warnings and zero severe errors. This bounded static replay does not promote the two parent routines wholesale. DoNotModel specialized optics, arbitrary activation/declaration order, general angle-dependent TAR and map output, opening multipliers, WindowShadingControl or surface behavior, window optics/thermal runtime, ratings, daylighting, broad diagnostics/order, and conformance remain deferred. WindowMaterial:Screen:EquivalentLayer is a complete source-order typed object. It preserves the default Autocalculate solar beam-beam sentinel, the five required solar/visible values and all bounds, infrared defaults, and the source-effective 0/0 m geometry left by blank wire fields despite schema numeric defaults. Solar values are shared front/back; visible N4/N5 populate only the front beam record, N6 occupies the front diffuse-diffuse reflectance slot, and back-visible stays zero. The compiler enforces only numeric N1+N3 and N4+N6 below one; beam-diffuse transmittances are omitted, and the source scalar-AbsorpThermal bug means no infrared transmittance-plus-emissivity gate is invented. The one-sided branch where numeric N1 exceeds geometry-derived openness by more than one percent emits a source Severe and rewrites diameter without setting local ErrorsFound; Rust fail-closes it instead of preserving post-Severe recovery. MediumRough/resistance-only and directional thermal projections are fixed. The shared namespace, EquivalentLayer family, ordinary-Construction rejection, and explicit all-definition runtime block include the object. window_material_screen_equivalent_layer_001 adds two nonblocking diagnostic proof variables through the bounded Rust EIO parser and three-lane ep_cli comparator. Its generic gate locks one MediumRough/all-zero row for every Z,M,A definition, including construction-unused M. Its exact specialized header has nine tokens while each row has twelve, and the A,Z,Z construction-occurrence sequence excludes M. A retains raw Autocalculate as -99999.0000 and blank geometry as 0.00000/0.00000; specialized rows emit shared N1 and N7 once, duplicate N2/N3 and N8 into front/back fields, omit visible N4-N6, and use source-{:.4R} solar/infrared plus {:.5R} geometry serialization. Two surfaces sharing B do not multiply its Z occurrence; surface-unused A and C, retained only by fixture EMS construction-index references, emit A and the second Z. Primary emits generic plus specialized evidence, Materials-only emits generic Z,M,A and maps the expected MissingWindowMaterialScreenEquivalentLayerHeader to an empty occurrence set, and Constructions-only emits specialized A,Z,Z without generic rows; every oracle lane is warning- and severe-free. Exact equivalent-layer construction rows and surface/EMS topology are fixture-integrity locks only. Rust EIO serialization, Construction:WindowEquivalentLayer typing/packing, CheckAndFixCFSLayer/IS_OPENNESS/ASHWAT behavior, optics/thermal behavior, ratings, surfaces, EMS behavior, runtime, broad diagnostics/order, and conformance remain deferred. WindowMaterial:Blind is a complete bounded typed source-order object with Horizontal/Vertical orientation and the Horizontal default; seven required numeric fields; all 27 numeric inputs, schema/source-effective defaults, and bounds; ten strict-below-one optical sums; and six beam/diffuse equality rules that reject only absolute differences greater than 1e-5. Width below separation remains warning-only. Blind-to-glass distance must be at least half the slat width, and the derived inverse-sine slat-angle interval applies only under the source separation-plus-thickness-below-width gate; the fixed-angle input branch adds no N26/N27 relationship. N6-N20 project into their exact solar, visible, and infrared slat-property slots while unassigned slots and base material projections remain zero; Rough and resistance-only behavior are fixed. The shared namespace and Fenestration family extend ordinary Construction to one safe exterior, interior, double-between, or triple-between Blind while retaining the common maximum-one Shade/Screen/Blind and solar-diffusing exclusions. Between-glass gaps must match the source five-slot gas signature, differ in width by no more than 0.0005 m, and have a combined width at least the slat width; unsafe end-layer holes fail closed. Arbitrary-run assessment counts and blocks every definition including unused ones. window_material_blind_001 adds two nonblocking diagnostic proof variables through the bounded Rust EIO parser and three-lane ep_cli comparator. Its generic gate requires exactly one normalized-name Material Details row for every typed Z,M,A fixture definition, including construction-unused M, and locks Rough plus all eight numeric base fields at zero; the smoke separately locks source declaration order Z,M,A. Its exact specialized header and each row have nine tokens, and the construction occurrence sequence is A,Z,Z with M excluded and duplicate reuse preserved. Specialized rows emit raw N1 slat width, N2 separation, and N3 thickness with source-{:.4R}, then N4 angle, N6 beam solar transmittance, N7 front beam solar reflectance, and N21 glass distance with source-{:.3R}. ReportGlass reads the raw slatTAR input record while CalcBlindProperties writes separate TARs, so the comparator does not reproduce computed blind optics. In the bounded scope, Output:Constructions selecting Constructions, at least one Blind definition, and at least one typed ordinary fenestration Construction require the specialized header; a header with zero rows is valid. Primary emits generic plus specialized evidence, Materials-only emits generic Z,M,A without the specialized header, and Constructions-only emits specialized A,Z,Z without generic rows; every oracle lane is warning- and severe-free. Two surfaces sharing construction C do not multiply its Z occurrence; surface-unused D still emits the second Z, and its fixture-only EMS construction-index reference merely suppresses a warning. Exterior/interior occurrences are predicted only with an exact bare companion stack; between-glass and missing-bare report behavior fail closed. Exact WindowConstruction, surface, control, and EMS rows are fixture-integrity locks only. Rust EIO serialization, CalcBlindProperties, beam-beam and all other blind optics, ratings, daylighting, control/surface/EMS behavior, variable angle, thermal/window runtime, between-glass or missing-bare reporting, broad declaration/diagnostic ordering, Construction:WindowEquivalentLayer, and conformance remain unclaimed. WindowMaterial:Blind:EquivalentLayer is a complete bounded typed source-order object with Horizontal/Vertical orientation, FixedSlatAngle/MaximizeSolar/BlockBeamSolar control, six required numeric fields, and the exact schema bounds. Only raw N5+N7, N6+N8, N9+N11, and N10+N12 are required below one. N9-N12 and N13-N15 use their source all-present assignment gates; omitted default-zero N13 therefore leaves the solar diffuse-diffuse TAR state zero even when required N14/N15 are supplied. Complete N16-N18 input triggers the EnergyPlus 26.1 index quirk that copies N13-N15 rather than retaining the visible diffuse-diffuse values themselves. Individually blank N19-N21 leave source-effective infrared transmittance and emissivity at zero despite the schema emissivity defaults. The compiler preserves the warning-only source recovery order for sub-0.001 m separation, sub-0.001 m or at-least-double width, and crown at least half the corrected width, while the initial width-below-separation warning does not itself alter the value. Rough/resistance-only state, zero beam-beam slots, shared infrared transmittance, and directional thermal projections are retained. The shared namespace, EquivalentLayer family, ordinary-Construction rejection, and all-definition arbitrary-run block include this object. Construction:WindowEquivalentLayer typing/packing, CheckAndFixCFSLayer/ASHWAT calculations, EIO serialization, optics/thermal/control execution, surfaces, ratings, daylighting, broad diagnostics, runtime, and conformance remain unclaimed. window_material_blind_equivalent_layer_001 adds two nonblocking diagnostic proof variables through the bounded Rust EIO parser and three-lane ep_cli comparator. Its generic gate locks one Rough/all-zero Material Details row for every source-ordered Z,M,A definition, including construction-unused M. The exact specialized header has 18 tokens, including four Slate typos and a final Slat Angle Control label, while each payload has 17 tokens because angle control is omitted. Identity and orientation precede 14 source-{:.5R} numeric fields; visible inputs are omitted. A locks orientation/crown/angle defaults, zero defaulted beam transmittances, solar diffuse state, and infrared state while retaining required reflectances. Z locks a negative high-precision angle, geometry/optics, and duplicate reuse. EnergyPlus emits no trailing newline: A is directly concatenated with Construction B, the first Z with Construction C, and the final Z with the next EIO header. The logical occurrence sequence is A,Z,Z with M excluded. Two surfaces sharing B do not multiply its Z row; surface-unused A and C are retained only by fixture EMS references. Primary, Materials-only, and Constructions-only independently lock report activation and complete warning- and severe-free. Exact construction rows and surface/EMS topology are fixture-integrity locks only.\n\nCP83 keeps the 22 typed material variants, 11/12 typed material overlays, 33/34 complete bounded public-object slices, 1/34 partial slice, and 0/34 wholly deferred objects unchanged. It adds the complete bounded final SurfaceProperties:VaporCoefficients pass outside MaterialDefinition and the 34-object material inventory, completing the bounded seven-pass GetHeatBalHAMTInput reader-input sequence. A separate deterministic SurfaceVaporCoefficients arena owns typed IDs, nonsemantic normalized outer-key snapshots, resolved BuildingSurface-only SurfaceIds, two independent default-No booleans, and two finite nonnegative default-zero raw coefficient values without upper, flag/value, cross-side, HAMT-algorithm, material, Settings, or Sorption dependencies. Rust validates eagerly, rejects a second target resolving to the same SurfaceId before reservation, and thereby fails closed relative to source lazy first-HAMT-surface reading and ordered per-side last-Yes accumulation; native-epJSON canonical Yes/No casing parity versus Rust's project-wide case-insensitive parser remains unclaimed. Every definition, including both-No, zero-valued, non-HAMT, and otherwise runtime-inert records, is explicitly run-blocked. Boundary-vtc derivation or fixed replacement, the internal HMassConvInFD rewrite, zero-vtc resistance behavior, HAMT cells, reporting, runtime numerics, exact diagnostics/order/multiplicity, and conformance remain unsupported; no manifest, comparator, or proof variable is added.\n\nCP98 keeps all material inventory, typed-variant, overlay, and bounded public-object counts unchanged and state-maps only the later DataSurfaces::GetVariableAbsorptanceSurfaceList immutable selection after GetBuildingData. TypedModel::variable_absorptance_surface_bindings retains one VariableAbsorptanceSurfaceBinding per dense-order retained Outdoors BuildingSurface:Detailed whose construction outside-layer MaterialId owns an existing typed overlay. Non-Outdoors outside-layer uses produce bounded surface-order warnings without bindings, then every typed construction and each source-effective layer from layer 2 onward are scanned in construction/layer order so every overlay occurrence produces an independent bounded warning. Compiler::build_variable_absorptance_surface_list publishes the binding vector only after the scans, takes the empty-overlay early return, and publishes nothing after a typed-surface parse error. Every overlay remains UnsupportedSurfaceBoundary/RunBlocked even when selected. UpdateVariableAbsorptances, schedule/Curve/Table evaluation, all three runtime triggers, mutable thermal/solar absorptance state, clamping, the scheduled-solar pointer defect, full AllHTSurfaceList membership and source reorder parity, other surface families, exact warning text/order/multiplicity, EIO/output state, runtime numerics, and conformance remain unsupported; no capability, manifest, comparator, proof variable, or blocker count is added."

routine.get_window_glass_spectral_data.source_file = "src/EnergyPlus/Material.cc"
routine.get_window_glass_spectral_data.source_routine = "GetWindowGlassSpectralData"
routine.get_window_glass_spectral_data.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_window_glass_spectral_data.completion_status = "state_mapped"
routine.get_window_glass_spectral_data.required_for_full_domain = false
routine.get_window_glass_spectral_data.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_window_glass_spectral_data.read_state = [
  "`MaterialProperty:GlazingSpectralData` definitions at the common-startup stage in deterministic compiler order: normalized name plus the positional numeric stream formed from fixed quartets 1 through 4 followed by extensible quartets; a name-only object has zero points, while omitted values inside the active numeric span read as zero",
  "for each complete quartet, wavelength, transmittance, front reflectance, and back reflectance; point count is bounded from zero through 800, and every dataset is validated even when no glazing references it",
]
routine.get_window_glass_spectral_data.write_state = [
  "a separate normalized glazing-spectral-data name map and deterministic standalone `GlazingSpectralData` arena whose records own source-field-ordered `GlazingSpectralPoint` vectors; neither arena is a `MaterialDefinition` variant",
  "source-effective validated point values: transmittance below 0.001 is silently replaced by 0.001; wavelength remains in [0.1,4.0] microns and strictly increasing, transmittance is at most 1.01, each reflectance is in [0,1.02], and transmittance plus either reflectance is at most 1.03",
  "compile failure before typed-model publication for an incomplete final quartet, more than 800 points, duplicate normalized dataset names, invalid wavelength/order, invalid reflectance/transmittance, or either optical-sum violation; source warning/severe/fatal wording is not claimed",
]
routine.get_window_glass_spectral_data.history_state_ownership = "no cross-call or runtime history; the compiled model owns immutable dataset descriptors and point vectors for the lifetime of that typed model"
routine.get_window_glass_spectral_data.unsupported_state = [
  "glazing-material dataset reference resolution and every active `WindowMaterial:Glazing` `Spectral`, `SpectralAndAngle`, or `BSDF` consumer; WindowManager optical tables, constructions, surfaces, reporting, and runtime state",
]
routine.get_window_glass_spectral_data.inactive_branches = [
  "valid unused datasets, including zero-point name-only definitions, remain compile-time data only and add no arbitrary-run blocker or runtime execution state",
  "the no-extension branch derives its numeric span from the last present fixed field; any non-empty extension establishes all four fixed quartets before the extension points, with omitted positions zero-filled as by the source InputProcessor",
]
routine.get_window_glass_spectral_data.unsupported_active_branches = [
  "`WindowMaterial:Glazing` `Spectral` remains compiler-blocked despite the dataset arena; `SpectralAndAngle` and `BSDF` remain blocked on their separate data and complex-fenestration paths",
]
routine.get_window_glass_spectral_data.not_claimed_branches = [
  "active spectral reference linkage, interpolation or angular/hemispherical optics, WindowManager consumers, constructions, surfaces, EIO serialization, runtime numerical behavior, broad dataset declaration-order parity, exact diagnostic text/order/multiplicity, and conformance",
]

routine.setup_simple_window_glazing_system.source_file = "src/EnergyPlus/Material.cc"
routine.setup_simple_window_glazing_system.source_routine = "SetupSimpleWindowGlazingSystem"
routine.setup_simple_window_glazing_system.source_map = "docs/src/porting-map/material-source-map.md"
routine.setup_simple_window_glazing_system.completion_status = "state_mapped"
routine.setup_simple_window_glazing_system.required_for_full_domain = false
routine.setup_simple_window_glazing_system.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.setup_simple_window_glazing_system.read_state = [
  "one validated WindowMaterial:SimpleGlazingSystem definition: name, positive U-factor with film coefficients, solar heat-gain coefficient in (0,1), the explicit-visible-input flag, and the optional visible transmittance in (0,1); the calculation reads no other material, construction, surface, weather, timestep, or history state",
]
routine.setup_simple_window_glazing_system.write_state = [
  "the source-effective single-layer VerySmooth material state: winter-film-removed resistance, thickness, conductivity, NominalR-equivalent resistance, normal-incidence solar and visible transmittance/reflectances, zero infrared transmittance, 0.84 front/back emissivity and thermal absorptance, unit dirt factor, and false solar-diffusing flag",
  "a materializing high-U warning when derived layer resistance is non-positive, after replacing it with 0.001 m2-K/W; Rust records this as film_resistance_clamped and SimpleGlazingFilmResistanceClamped without claiming source warning text",
]
routine.setup_simple_window_glazing_system.history_state_ownership = "no cross-call history or cache; each typed material owns its immutable raw performance indices, optional visible-input state, derived block-model fields, and clamp flag"
routine.setup_simple_window_glazing_system.unsupported_state = [
  "multi-layer or shaded Construction packing and all construction-derived thermal state, specialized WindowMaterial:Glazing and WindowConstruction report state, angular or hemispherical optical tables, surface/window thermal state, daylighting, ratings, output variables, and runtime state; the separate GetConstructData mapping owns only sole-layer typed identity and graph linkage",
]
routine.setup_simple_window_glazing_system.inactive_branches = [
  "winter interior-film resistance branches at U-factor 5.85; thickness branches at inverse layer resistance 7; solar correlations below 3.4, from 3.4 through 4.5, and above 4.5 W/m2-K with SHGC thresholds 0.15 and 0.7206; the intermediate-U summer-film path deliberately preserves the EnergyPlus 26.1 `(low - high) * fraction + low` reversed interpolation direction",
  "missing visible transmittance copies the derived solar transmittance and reflectance, while explicit visible input uses separate front/back cubic reflectance correlations and the source 0.999-minus-transmittance clamps",
]
routine.setup_simple_window_glazing_system.unsupported_active_branches = [
  "exact EnergyPlus diagnostic wording, order, and multiplicity for the materializing high-U warning; non-finite or schema-invalid inputs are rejected before this mapped routine boundary",
  "a finite schema-valid tiny positive U-factor can overflow reciprocal resistance and derive zero conductivity (EnergyPlus 26.1 emits Severe then Fatal, with no material row); Rust preserves the fail-closed outcome with InvalidSimpleGlazingDerivedConductivity before material identity reservation, without claiming the source diagnostic wording, order, multiplicity, or post-materialization flow",
]
routine.setup_simple_window_glazing_system.not_claimed_branches = [
  "TransAndReflAtPhi incident-angle dependence, hemispherical averaging, normalized specialized glazing serialization, construction conductance/U-factor/SHGC reporting, window ratings, surfaces, daylighting, window heat transfer, runtime execution, Rust EIO serialization, broad diagnostics, and conformance",
]

routine.get_material_data.source_file = "src/EnergyPlus/Material.cc"
routine.get_material_data.source_routine = "GetMaterialData"
routine.get_material_data.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_material_data.completion_status = "source_mapped"
routine.get_material_data.required_for_full_domain = false

routine.calc_screen_transmittance.source_file = "src/EnergyPlus/Material.cc"
routine.calc_screen_transmittance.source_routine = "CalcScreenTransmittance"
routine.calc_screen_transmittance.source_map = "docs/src/porting-map/material-source-map.md"
routine.calc_screen_transmittance.completion_status = "source_mapped"
routine.calc_screen_transmittance.required_for_full_domain = false

routine.calc_window_screen_properties.source_file = "src/EnergyPlus/WindowManager.cc"
routine.calc_window_screen_properties.source_routine = "CalcWindowScreenProperties"
routine.calc_window_screen_properties.source_map = "docs/src/porting-map/material-source-map.md"
routine.calc_window_screen_properties.completion_status = "source_mapped"
routine.calc_window_screen_properties.required_for_full_domain = false

routine.report_glass.source_file = "src/EnergyPlus/WindowManager.cc"
routine.report_glass.source_routine = "ReportGlass"
routine.report_glass.source_map = "docs/src/porting-map/material-source-map.md"
routine.report_glass.completion_status = "source_mapped"
routine.report_glass.required_for_full_domain = false

routine.calc_nominal_window_cond.source_file = "src/EnergyPlus/WindowManager.cc"
routine.calc_nominal_window_cond.source_routine = "CalcNominalWindowCond"
routine.calc_nominal_window_cond.source_map = "docs/src/porting-map/material-source-map.md"
routine.calc_nominal_window_cond.completion_status = "source_mapped"
routine.calc_nominal_window_cond.required_for_full_domain = false

routine.get_variable_absorptance_input.source_file = "src/EnergyPlus/Material.cc"
routine.get_variable_absorptance_input.source_routine = "GetVariableAbsorptanceInput"
routine.get_variable_absorptance_input.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_variable_absorptance_input.completion_status = "state_mapped"
routine.get_variable_absorptance_input.required_for_full_domain = false
routine.get_variable_absorptance_input.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_variable_absorptance_input.read_state = [
  "deterministic compiler-ordered `MaterialProperty:VariableAbsorptance` definitions after all 22 base material families; Rust delays publication until the shared schedule namespace is also typed",
  "normalized overlay name, required material name, defaulted four-way control signal, and optional thermal/solar function and schedule names",
  "existing base-material registry, typed user-schedule registry plus `Constant-0.0`/`Constant-1.0`, and the raw EnergyPlus 26.1 Curve/Table name namespace",
]
routine.get_variable_absorptance_input.write_state = [
  "a separate normalized overlay-name map and `MaterialVariableAbsorptance` arena; each record resolves exactly one `Material` or `Material:NoMass` target and stores either scheduled dependencies or one of the three function signals with deferred Curve/Table identities",
  "source-effective null state for each nonblank dependency name that does not resolve; selected control still requires at least one resolved dependency, and a resolved opposite-family dependency fails compilation",
  "compile failure before identity or target reservation for malformed fields, invalid control, missing or non-Regular target, ambiguous dependency identity, selected-family absence, resolved opposite-family state, normalized overlay-name duplicate, or a second overlay for one target",
]
routine.get_variable_absorptance_input.history_state_ownership = "no runtime history in this checkpoint; the compiled model owns immutable overlay descriptors, the separate CP98 contract owns immutable surface selection, and timestep updates remain unsupported"
routine.get_variable_absorptance_input.unsupported_state = [
  "Curve/Table payload typing, dimensional validation, interpolation and evaluation; the separately state-mapped exterior variable-absorptance surface selection does not execute thermal/solar surface-array mutation, trigger evaluation, clamping, construction behavior, reporting, or conformance",
]
routine.get_variable_absorptance_input.inactive_branches = [
  "within the declared public Curve/Table and shared schedule namespaces, unresolved optional names become null as `Curve::GetCurve` or `Sched::GetSchedule` would; they are harmless when another selected-family dependency resolves",
  "overlay names share no namespace with material names, while at most one valid overlay may target each material in the bounded checkpoint",
]
routine.get_variable_absorptance_input.unsupported_active_branches = [
  "every valid overlay is typed but run-blocking, including overlays attached only to unused materials",
  "source last-wins multiplicity and IDF declaration-order recovery are deferred; a repeated target fails closed",
]
routine.get_variable_absorptance_input.not_claimed_branches = [
  "exact source diagnostic severity/text/order/early-return behavior, Curve/Table dependency validation, the separately bounded `GetVariableAbsorptanceSurfaceList` selection, `UpdateVariableAbsorptances`, the scheduled-solar pointer defect, EIO serialization, runtime numerical behavior, and conformance",
]

routine.get_variable_absorptance_surface_list.source_file = "src/EnergyPlus/DataSurfaces.cc"
routine.get_variable_absorptance_surface_list.source_routine = "GetVariableAbsorptanceSurfaceList"
routine.get_variable_absorptance_surface_list.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_variable_absorptance_surface_list.completion_status = "state_mapped"
routine.get_variable_absorptance_surface_list.required_for_full_domain = false
routine.get_variable_absorptance_surface_list.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_variable_absorptance_surface_list.read_state = [
  "EnergyPlus calls `DataSurfaces::GetVariableAbsorptanceSurfaceList` immediately after `GetBuildingData`; bounded Rust calls `Compiler::build_variable_absorptance_surface_list` after the typed `BuildingSurface:Detailed` pass and publishes no selection when that surface pass added an error",
  "the deterministic typed `MaterialVariableAbsorptance` arena, retained detailed opaque surfaces in dense SurfaceId order, each surface's resolved ConstructionId and `OutsideBoundaryCondition`, each construction's source-effective material-layer order, and all typed constructions in dense ConstructionId order",
  "for surface selection, the source scans `AllHTSurfaceList`, skips zero-layer or null-outside-layer constructions and non-Regular outside materials, then tests whether the outside material has variable-absorptance control; bounded Rust scans only its already validated typed `BuildingSurface:Detailed` subset and matches the existing overlay by outside-layer MaterialId",
]
routine.get_variable_absorptance_surface_list.write_state = [
  "`TypedModel::variable_absorptance_surface_bindings`, an immutable deterministic vector of `VariableAbsorptanceSurfaceBinding { surface, variable_absorptance }` records for each retained `Outdoors` detailed surface whose construction outside layer owns a typed overlay; binding order follows dense SurfaceId order and records no absorptance value or mutable surface state",
  "a non-Outdoors retained detailed surface whose outside layer owns an overlay produces one bounded warning and no binding; those surface warnings are accumulated in dense SurfaceId order before construction-layer warnings are published",
  "every typed construction is then scanned in dense ConstructionId order and every source-effective inside layer from one-based layer 2 onward is scanned in layer order; each occurrence whose MaterialId owns an overlay produces a warning independently, including repeated material occurrences, and never produces a surface binding",
  "the binding vector is published once after both scans; an empty overlay arena publishes an empty vector without scanning, and a typed-surface parse error leaves the model's initially empty vector unpublished rather than exposing a partial selection",
]
routine.get_variable_absorptance_surface_list.history_state_ownership = "the compiled TypedModel owns immutable surface-to-overlay bindings only; this checkpoint creates no timestep history, mutable absorptance arrays, schedule/function evaluation state, trigger state, or output state"
routine.get_variable_absorptance_surface_list.unsupported_state = [
  "`UpdateVariableAbsorptances`: schedule or Curve/Table evaluation, SurfaceTemperature/SurfaceReceivedSolarRadiation/SpaceHeatingCoolingMode triggers, exterior thermal/solar absorptance mutation, `[0.0001, 0.9999]` clamping, and the EnergyPlus 26.1 scheduled-solar thermal-pointer defect",
  "the full source `AllHTSurfaceList`, source surface-family ingestion and reordering, legacy detailed surfaces, InternalMass, fenestration/subsurfaces, shading, generated counterpart surfaces, and any runtime or reporting consumer of the selected list",
]
routine.get_variable_absorptance_surface_list.inactive_branches = [
  "with no valid typed variable-absorptance overlay, the binding vector is empty and the source-equivalent `AnyVariableAbsorptance` early-return branch performs no surface or construction scan",
  "an overlay on an unused material or on a construction outside layer used only by a non-Outdoors retained surface creates no binding; the overlay remains covered by the existing all-definition `UnsupportedSurfaceBoundary` runtime block",
  "an overlay used only below construction layer 1 creates no binding and only the bounded occurrence-local warning state; repeated inside-layer occurrences are not deduplicated",
]
routine.get_variable_absorptance_surface_list.unsupported_active_branches = [
  "every typed `MaterialProperty:VariableAbsorptance` definition remains `UnsupportedSurfaceBoundary` and `RunBlocked`, including a definition with one or more compiled exterior bindings; selection does not add an executable capability, proof variable, manifest, comparator, or numerical claim",
  "surface and inside-layer warnings are compile diagnostics only; Rust does not claim the source's exact warning severity text, punctuation, object naming, order across deferred surface families, or multiplicity outside the typed arenas",
]
routine.get_variable_absorptance_surface_list.not_claimed_branches = [
  "complete `AllHTSurfaceList` membership or source reorder parity, other surface families, malformed/partial source recovery, exact warning text/order/multiplicity, `UpdateVariableAbsorptances`, construction or surface heat-transfer behavior, EIO/output serialization, runtime numerics, and conformance",
]

routine.update_variable_absorptances.source_file = "src/EnergyPlus/HeatBalanceSurfaceManager.cc"
routine.update_variable_absorptances.source_routine = "UpdateVariableAbsorptances"
routine.update_variable_absorptances.source_map = "docs/src/porting-map/material-source-map.md"
routine.update_variable_absorptances.completion_status = "source_mapped"
routine.update_variable_absorptances.required_for_full_domain = false

routine.get_hysteresis_data.source_file = "src/EnergyPlus/PhaseChangeModeling/HysteresisModel.cc"
routine.get_hysteresis_data.source_routine = "GetHysteresisData"
routine.get_hysteresis_data.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_hysteresis_data.completion_status = "state_mapped"
routine.get_hysteresis_data.required_for_full_domain = false
routine.get_hysteresis_data.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_hysteresis_data.read_state = [
  "deterministic compiler-ordered `MaterialProperty:PhaseChangeHysteresis` definitions after the earlier variable-absorptance overlay; the object key is the existing material reference and has no independent overlay namespace",
  "thirteen required finite numeric fields in source order: total latent heat; liquid conductivity/density/specific heat; high/peak/low melting-curve temperatures; solid conductivity/density/specific heat; and high/peak/low freezing-curve temperatures; every value is strictly greater than zero, with no defaults, enums, upper bounds, or cross-field rules",
  "the existing public base-material registry, where actual EnergyPlus `Group::Regular` admits `Material` and `Material:NoMass`; Rust synthesizes private `~FC_Concrete` and `~FC_Insulation_n` material slots but deliberately omits them from public `material_names` and attachment-target resolution",
]
routine.get_hysteresis_data.write_state = [
  "a separate deterministic `MaterialPhaseChangeHysteresis` attachment arena whose records own a typed ID, normalized target snapshot, resolved `MaterialId`, total latent heat, grouped liquid/solid thermal states, and grouped melting/freezing curves; the object key is not published as a separate name map",
  "source-initialized transition specific heat equal to the solid/liquid mean and initial prior specific heat equal to the solid value; the referenced base `MaterialDefinition` remains unchanged",
  "compile failure before attachment ID or target reservation for a blank key, missing or non-Regular-group public target, any missing/malformed/nonpositive numeric field, or a second case-insensitive attachment for one material",
]
routine.get_hysteresis_data.history_state_ownership = "no mutable phase, reversal, enthalpy, or prior-specific-heat history in this checkpoint; the compiled model owns immutable attachment descriptors while every downstream consumer remains unsupported"
routine.get_hysteresis_data.unsupported_state = [
  "source material-pointer replacement and `hasPCM`; CondFD material/node allocation, phase and reversal histories, hysteretic enthalpy/Cp/density/conductivity evaluation, construction and surface behavior, `ThermalStorage:PCM`, EMS pointer rebinding, reporting, and conformance",
]
routine.get_hysteresis_data.inactive_branches = [
  "all-positive inputs remain valid regardless of melting/freezing peak order or liquid/solid property relationships because the source defines no cross-field constraint",
  "`Material:NoMass` passes the source `Group::Regular` gate even though downstream CondFD R-only branches can bypass PCM evaluation",
]
routine.get_hysteresis_data.unsupported_active_branches = [
  "every valid attachment is typed but run-blocking, including one attached only to an unused material",
  "internally generated F/C-factor material targets remain outside the executable boundary; typed PhaseChange, VariableThermalConductivity, EMPD settings, and all six HAMT attachments may coexist on the same public target while every combination remains run-blocking",
]
routine.get_hysteresis_data.not_claimed_branches = [
  "exact source diagnostic severity/text/order/multiplicity, material-pointer replacement and EMS-address behavior, mutable hysteresis state-machine and known source defects, CondFD or PCM-storage numerical behavior, generic CondFD EIO/output variables, runtime numerical behavior, and conformance",
]

routine.get_cond_fd_input.source_file = "src/EnergyPlus/HeatBalFiniteDiffManager.cc"
routine.get_cond_fd_input.source_routine = "GetCondFDInput"
routine.get_cond_fd_input.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_cond_fd_input.completion_status = "state_mapped"
routine.get_cond_fd_input.required_for_full_domain = false
routine.get_cond_fd_input.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_cond_fd_input.read_state = [
  "deterministic compiler-ordered `MaterialProperty:PhaseChange` definitions after the common-startup hysteresis attachment; the object key is the existing material reference, the optional or blank conductivity-temperature coefficient defaults to zero, and the optional source-ordered `values` array contains zero or more complete temperature/enthalpy pairs with no executable-schema item-count cap",
  "deterministic compiler-ordered `MaterialProperty:VariableThermalConductivity` definitions after PhaseChange; the object key is the existing material reference, and the optional source-ordered `values` array contains zero or more complete temperature/thermal-conductivity pairs with no executable-schema item-count cap",
  "the existing public base-material registry, where actual EnergyPlus `Group::Regular` admits `Material` and `Material:NoMass`; Rust synthesizes private `~FC_Concrete` and `~FC_Insulation_n` material slots but deliberately omits them from public `material_names` and attachment-target resolution",
  "finite coefficient, temperature, and enthalpy scalars with no scalar bounds; source-ordered temperatures must strictly increase and enthalpies must be nondecreasing, so equal or negative enthalpies and negative temperatures or coefficients remain valid",
  "finite Rust temperature and thermal-conductivity scalars with no scalar bounds; source-ordered temperatures must strictly increase, while conductivity has no ordering or sign rule, so negative, zero, equal, or decreasing finite conductivity remains valid",
]
routine.get_cond_fd_input.write_state = [
  "a separate deterministic `MaterialPhaseChange` attachment arena whose records own a typed ID, normalized target snapshot, resolved `MaterialId`, conductivity-temperature coefficient, and source-ordered vector of complete temperature/enthalpy points; the object key is not published as a separate name map",
  "a separate deterministic `MaterialVariableThermalConductivity` attachment arena whose records own a typed ID, normalized target snapshot, resolved `MaterialId`, and source-ordered vector of complete temperature/conductivity points; the object key is not published as a separate name map",
  "the referenced base `MaterialDefinition` remains unchanged and no mutable `hasPCM` state is invented",
  "compile failure before attachment ID or target reservation for a blank or missing target, a non-Regular-group public target, malformed/incomplete/nonfinite values, temperature or enthalpy ordering violations, or a second case-insensitive PhaseChange attachment for one material",
  "compile failure before attachment ID or target reservation for a blank or missing target, a non-Regular-group public target, malformed/incomplete/nonfinite values, nonincreasing temperatures, or a second case-insensitive VariableThermalConductivity attachment for one material",
]
routine.get_cond_fd_input.history_state_ownership = "no mutable `MaterialFD`, `TempEnth`, `TempCond`, sentinel, CondFD-node property, or timestep history in this checkpoint; the compiled model owns immutable PhaseChange and VariableThermalConductivity attachment descriptors while every downstream consumer remains unsupported"
routine.get_cond_fd_input.unsupported_state = [
  "the preceding `HeatBalanceSettings:ConductionFiniteDifference` pass, `MaterialFD` allocation and `tk1`/`TempEnth`/`TempCond` plus both three-point `-100` sentinel states, construction/surface/node allocation, interpolation, activation, specific-heat/conductivity and PhaseChange/VTC/Hysteresis precedence execution, EMS, generic CondFD reporting, and conformance",
]
routine.get_cond_fd_input.inactive_branches = [
  "`Material:NoMass` passes the source `Group::Regular` gate even though downstream R-only CondFD execution bypasses both temperature-enthalpy and temperature-conductivity tables",
  "zero-point PhaseChange and VariableThermalConductivity tables are valid typed empty state, and Rust does not materialize either source three-point `-100` sentinel; Rust applies the executable-schema coefficient default to an entirely empty PhaseChange object instead of reproducing its native-epJSON negative-presence-count defect, while a native empty VTC object succeeds",
  "EnergyPlus skips target and table-order validation for both attachment families when CondFD initialization does not invoke `GetCondFDInput`; selecting `HeatBalanceAlgorithm = ConductionFiniteDifference` invokes the reader even with no surfaces, while Rust deliberately validates every definition eagerly and fails closed",
]
routine.get_cond_fd_input.unsupported_active_branches = [
  "every valid PhaseChange or VariableThermalConductivity attachment is typed but run-blocking, including one attached only to an unused material",
  "source-accepted one- and two-point tables in either family remain typed but are never executed because downstream CondFD directly assumes the first three points; first-three enthalpy/conductivity activation quirks and fallback behavior are also not executed",
  "internally generated F/C-factor targets and source last-wins repeated-target overwrite behavior remain outside the executable boundary",
]
routine.get_cond_fd_input.not_claimed_branches = [
  "the PhaseChange textual IDD three-through-one-hundred description, the VTC IDD's inconsistent first-three-required/one-hundred-pair text and declared 101st pair, native-epJSON PhaseChange zero-pair presence-count behavior, EnergyPlus IDF acceptance of nonfinite VTC scalars, exact diagnostic severity/text/order/multiplicity, source duplicate overwrite order, `MaterialFD` sentinel/interpolation/activation and specific-heat/conductivity behavior including the known boundary-conductivity defect, PhaseChange/VTC/Hysteresis/EMS precedence, EMPD/HAMT coexistence and Hysteresis subclass-lifetime hazards, generic EIO/output variables, runtime numerical behavior, and conformance",
]

routine.get_moisture_balance_empd_input.source_file = "src/EnergyPlus/MoistureBalanceEMPDManager.cc"
routine.get_moisture_balance_empd_input.source_routine = "GetMoistureBalanceEMPDInput"
routine.get_moisture_balance_empd_input.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_moisture_balance_empd_input.completion_status = "state_mapped"
routine.get_moisture_balance_empd_input.required_for_full_domain = false
routine.get_moisture_balance_empd_input.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_moisture_balance_empd_input.read_state = [
  "deterministic compiler-ordered `MaterialProperty:MoisturePenetrationDepth:Settings` definitions whose object keys are existing material references with no independent attachment namespace",
  "seven required finite numeric fields: water-vapor diffusion resistance factor, four moisture-equation coefficients, coating-layer thickness, and coating-layer water-vapor diffusion resistance factor; the resistance factor, coating thickness, and coating resistance factor are nonnegative while the four coefficients have no scalar or cross-field bounds",
  "surface-layer depth as missing, blank, case-insensitive Autocalculate, or an explicit finite value greater than zero; deep-layer depth uses the same automatic states or an explicit finite value greater than or equal to zero",
  "the existing public base-material registry, where only `Material` satisfies the source Regular-and-not-ROnly target gate; Rust synthesizes private `~FC_Concrete` state but deliberately omits it from public `material_names` and attachment-target resolution",
]
routine.get_moisture_balance_empd_input.write_state = [
  "a separate deterministic `MaterialMoisturePenetrationDepthSettings` attachment arena whose records own a typed ID, normalized target snapshot, resolved `MaterialId`, all seven fixed numeric inputs, and both penetration depths as `AutoOrNumber`; the object key is not published as a separate name map",
  "the referenced base `MaterialDefinition` remains unchanged and no mutable `MaterialEMPD` subclass or `hasEMPD` state is invented",
  "compile failure before attachment ID or target reservation for a blank or missing target, any non-`Material` public target, any missing/malformed/nonfinite/out-of-range field, or a second case-insensitive attachment for one material",
]
routine.get_moisture_balance_empd_input.history_state_ownership = "no mutable EMPD surface/deep-layer vapor density, moisture content, flux, latent heat, report, or timestep history in this checkpoint; the compiled model owns immutable settings descriptors while every downstream consumer remains unsupported"
routine.get_moisture_balance_empd_input.unsupported_state = [
  "the one-day and three-week penetration-depth autocalculation using fixed 24 C, 0.45 RH, 101325 Pa, Psychrometrics saturation pressure, base-material density, diffusion resistance, and sorption-curve slope",
  "source material-pointer replacement and `hasEMPD`; active EMPD surface discovery, inside/outside/middle construction-layer and every-zone placement validation, positive-resistance per-zone eligibility and nonpositive-resistance calculation bypass, initialization, timestep calculation/update, latent coupling, nine EMPD output variables, Construction EMPD EIO reporting, runtime numerics, and conformance",
]
routine.get_moisture_balance_empd_input.inactive_branches = [
  "EnergyPlus performs semantic target resolution, default materialization, pointer replacement, and placement checks only when an actual EMPD surface first enters moisture calculation; a CTF model and a global EMPD selection with no zones or surfaces do not invoke this reader, while Rust deliberately performs its bounded semantic validation for every definition eagerly and fails closed",
  "missing, blank, and explicit Autocalculate depths remain typed sentinels rather than prematurely materializing source-derived numbers",
  "an explicit deep depth of zero disables the deep layer; a nonzero deep depth no greater than the surface depth remains accepted because the source emits only a warning, whose exact parity is deferred",
]
routine.get_moisture_balance_empd_input.unsupported_active_branches = [
  "every valid settings attachment is typed but run-blocking, including one attached only to an unused material",
  "a schema-valid zero water-vapor diffusion resistance factor is retained as typed input but never reaches source per-zone eligibility or calculation-bypass behavior",
  "internally generated F/C-factor concrete targets and source effective later-replacement behavior for repeated case-insensitive targets remain outside the executable boundary",
]
routine.get_moisture_balance_empd_input.not_claimed_branches = [
  "exact EnergyPlus diagnostic severity/text/order/multiplicity, lazy algorithm/use gating, source duplicate replacement order, EnergyPlus IDF acceptance of some nonfinite fixed scalars, automatic-depth floating-point results and nonfinite hazards, material-subclass and cross-attachment pointer lifetime behavior, zone and construction placement validation, moisture/latent runtime behavior, nine output variables, Construction EMPD EIO serialization, runtime numerical behavior, and conformance",
]

routine.get_heat_bal_hamt_input.source_file = "src/EnergyPlus/HeatBalanceHAMTManager.cc"
routine.get_heat_bal_hamt_input.source_routine = "GetHeatBalHAMTInput"
routine.get_heat_bal_hamt_input.source_map = "docs/src/porting-map/material-source-map.md"
routine.get_heat_bal_hamt_input.completion_status = "state_mapped"
routine.get_heat_bal_hamt_input.required_for_full_domain = false
routine.get_heat_bal_hamt_input.state_mapping_ref = "docs/src/porting-map/material-source-map.md"
routine.get_heat_bal_hamt_input.read_state = [
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:Settings` definitions whose outer epJSON keys are nonsemantic diagnostic snapshots that may be blank or collide case-insensitively, while the required inner `material_name` field is the semantic target",
  "required finite porosity in the inclusive range [0,1], plus initial water-content ratio as missing or blank default 0.2 or an explicit finite value greater than or equal to zero, with no upper or cross-field bound",
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:SorptionIsotherm` definitions after Settings; each has a nonsemantic outer-key snapshot, a semantic inner material target, and a required exact-integer coordinate count from 1 through 25",
  "the fixed 25-pair sorption tail: pair 1 is required, pairs 2 through 25 are optional, every supplied scalar is validated, missing optional active-prefix scalars become zero, and positions beyond the declared count are ignored after validation; relative humidity is finite in inclusive [0,1] and moisture content is finite and nonnegative without an upper bound",
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:Suction` definitions after SorptionIsotherm; each has a nonsemantic outer-key snapshot, a semantic inner material target, and a required exact-integer suction-point count from 1 through 25",
  "the fixed 25-pair suction tail: pair 1 is required, pairs 2 through 25 are optional, every supplied scalar is validated, missing optional active-prefix scalars become zero, and positions beyond the declared count are ignored after validation; moisture content and liquid transport coefficient are finite and nonnegative without upper or cross-field bounds, and authored order is retained without sorting or correction",
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:Redistribution` definitions after Suction; each has a nonsemantic outer-key snapshot, a semantic inner material target, and a required exact-integer redistribution-point count from 1 through 25",
  "the fixed 25-pair redistribution tail: pair 1 is required, pairs 2 through 25 are optional, every supplied scalar is validated, missing optional active-prefix scalars become zero, and positions beyond the declared count are ignored after validation; moisture content and liquid transport coefficient are finite and nonnegative without upper or cross-field bounds, and authored order is retained without sorting or correction",
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:Diffusion` definitions after Redistribution; each has a nonsemantic outer-key snapshot, a semantic inner material target, and a required exact-integer data-pair count from 1 through 25",
  "the fixed 25-pair diffusion tail: pair 1 is required, pairs 2 through 25 are optional, every supplied scalar is validated, missing optional active-prefix scalars become zero, and positions beyond the declared count are ignored after validation; relative humidity is finite in inclusive [0,1] and water-vapor diffusion resistance factor is finite and nonnegative without an upper or cross-field bound, and authored order is retained without sorting or correction",
  "deterministic compiler-ordered `MaterialProperty:HeatAndMoistureTransfer:ThermalConductivity` definitions after Diffusion; each has a nonsemantic outer-key snapshot, a semantic inner material target, and a required exact-integer thermal-coordinate count from 1 through 25",
  "the fixed 25-pair thermal-conductivity tail: pair 1 is required, pairs 2 through 25 are optional, every supplied scalar is validated, missing optional active-prefix scalars become zero, and positions beyond the declared count are ignored after validation; supplied moisture content is finite and nonnegative, supplied thermal conductivity is finite and strictly positive, neither has an upper bound, and authored order is retained without sorting or correction",
  "deterministic compiler-ordered `SurfaceProperties:VaporCoefficients` definitions as the final seventh pass after ThermalConductivity and after `BuildingSurface:Detailed` compilation; each has a nonsemantic outer-key snapshot and a required semantic inner `surface_name` target resolved only through the typed BuildingSurface namespace",
  "the two independent constant flags as missing or blank default No or case-insensitive Yes/No, plus their two raw vapor-coefficient values as missing or blank finite zero or supplied finite nonnegative values without upper, flag/value, or cross-side bounds; Yes with zero and No with a positive value remain valid typed input",
  "the existing public base-material and attachment registries, where only `Material` with an existing typed Settings attachment is a Sorption target and only the same material with a typed Sorption attachment is independently eligible for Suction, Redistribution, Diffusion, and ThermalConductivity; none of those four attachments depends on another, Rust synthesizes private `~FC_Concrete` state but deliberately omits it from public attachment resolution, and it deliberately fails closed on source warning-ignored R-only `Material:NoMass`",
  "the existing typed `BuildingSurface:Detailed` registry, where any resolved `SurfaceId` is eligible without a HAMT-algorithm, construction, material, Settings, or Sorption dependency, while other EnergyPlus `SurfaceNames` families deliberately fail closed",
]
routine.get_heat_bal_hamt_input.write_state = [
  "a separate deterministic `MaterialHeatAndMoistureTransferSettings` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, porosity, and initial water-content ratio; the outer key is not published as a name map",
  "a separate deterministic `MaterialHeatAndMoistureTransferSorptionIsotherm` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, Settings ID, declared count, count-selected zero-filled input points, source-effective points, and a moisture-adjustment flag; the outer key is not published as a name map",
  "source-effective Sorption points formed by appending (1.01, porosity*1000) and (0,0), the source nested-swap ascending-relative-humidity sort, and up to 100 left-to-right adjacent-moisture averaging passes; any correction records one warning state and any derived nonfinite point fails closed",
  "a separate deterministic `MaterialHeatAndMoistureTransferSuction` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, SorptionIsotherm ID, declared count, count-selected zero-filled input points, and source-effective points; the outer key is not published as a name map",
  "source-effective Suction points formed by preserving the active input prefix exactly, then appending one point whose moisture content is the indexed last source-effective Sorption moisture and whose liquid transport coefficient is the last active Suction coefficient; no Suction sort, correction, warning, monotonicity, uniqueness, or cross-column rule is invented",
  "a separate deterministic `MaterialHeatAndMoistureTransferRedistribution` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, SorptionIsotherm ID, declared count, count-selected zero-filled input points, and source-effective points; the outer key is not published as a name map",
  "source-effective Redistribution points formed by preserving the active input prefix exactly, then appending one point whose moisture content is the indexed last source-effective Sorption moisture and whose liquid transport coefficient is the last active Redistribution coefficient; no Redistribution-table sorting, correction, monotonicity or uniqueness check, clamp, cross-column rule, or table-validation warning is invented",
  "a separate deterministic `MaterialHeatAndMoistureTransferDiffusion` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, SorptionIsotherm ID, declared count, count-selected zero-filled input points, and source-effective points; the outer key is not published as a name map",
  "source-effective Diffusion points formed by preserving the active input prefix exactly, then appending one point whose relative humidity is the indexed last source-effective Sorption relative humidity and whose water-vapor diffusion resistance factor is the last active Diffusion factor; no Diffusion-table sorting, correction, monotonicity or uniqueness check, clamp, cross-field rule, or table-validation warning is invented",
  "a separate deterministic `MaterialHeatAndMoistureTransferThermalConductivity` attachment arena whose records own a typed ID, normalized outer-key snapshot, resolved `MaterialId`, SorptionIsotherm ID, declared count, count-selected zero-filled input points, and source-effective points; the outer key is not published as a name map",
  "source-effective ThermalConductivity points formed by preserving the active input prefix exactly, then appending one point whose moisture content is the indexed last source-effective Sorption moisture and whose thermal conductivity is the last active ThermalConductivity value; no thermal-conductivity-table sorting, correction, monotonicity or uniqueness check, clamp, cross-field rule, or table-validation warning is invented",
  "a separate deterministic `SurfaceVaporCoefficients` arena whose records own a typed ID, normalized outer-key snapshot, resolved `SurfaceId`, and separate external/internal constant booleans plus raw values; the outer key is not published as a name map and no mutable surface or HAMT cell state is written",
  "the referenced base `MaterialDefinition` remains unchanged and no mutable `MaterialHAMT` subclass or `hasHAMT` state is invented",
  "compile failure before attachment ID or target reservation for a blank or missing inner target, any non-`Material` public target, missing Settings or Sorption dependency, missing required field, malformed/nonfinite/out-of-range supplied field, invalid Sorption, Suction, Redistribution, Diffusion, or ThermalConductivity count, derived nonfinite Sorption state, or a repeated case-insensitive Settings, Sorption, Suction, Redistribution, Diffusion, or ThermalConductivity attachment for one material; blank or case-colliding outer keys remain valid for distinct targets",
  "compile failure before `SurfaceVaporCoefficientsId` or surface ownership reservation for a blank or missing surface target, a target outside the typed `BuildingSurface:Detailed` namespace, malformed flags, malformed/nonfinite/negative values, or a repeated target resolving to the same `SurfaceId`; blank or case-colliding outer keys remain valid for distinct targets",
]
routine.get_heat_bal_hamt_input.history_state_ownership = "no mutable HAMT cell temperature, relative humidity, vapor pressure, water content, flux, iteration, report, boundary-vapor-transfer, or timestep history in this checkpoint; the compiled model owns immutable Settings plus source-effective Sorption, Suction, Redistribution, Diffusion, and ThermalConductivity descriptors and immutable surface vapor-coefficient input snapshots while every runtime consumer remains unsupported"
routine.get_heat_bal_hamt_input.unsupported_state = [
  "source `MaterialHAMT` pointer replacement and `hasHAMT`, plus the later active-construction absent-table base-conductivity fallback and its diagnostics",
  "active HAMT surface discovery, all-layer construction eligibility, boundary vapor-transfer derivation or fixed replacement, the internal `HMassConvInFD` rewrite, zero-vtc resistance behavior, cell-grid allocation, initial-water-to-relative-humidity conversion, interpolation and nonlinear heat/moisture state, six fixed surface outputs, three per-cell output families, HAMT cells/origins and generic nominal-resistance EIO, SQLite porosity reporting, runtime numerics, and conformance",
]
routine.get_heat_bal_hamt_input.inactive_branches = [
  "EnergyPlus performs semantic target resolution for all six ordered HAMT material passes plus the final surface-vapor-coefficient pass only when the first actual HAMT surface enters `ManageHeatBalHAMT`; a global HAMT selection with no such surface does not invoke this reader, while Rust deliberately performs bounded validation for every definition eagerly and fails closed",
  "EnergyPlus warning-ignores an R-only `Material:NoMass` Settings target; Rust rejects it so no valid-looking typed attachment silently disappears",
  "EnergyPlus checks shared HAMT state before Suction, Redistribution, Diffusion, and ThermalConductivity but each pass then indexes the Sorption endpoint without an explicit missing-Sorption guard; Rust requires the typed Sorption dependency independently for all four attachments and fails closed on those unsafe paths without inventing dependencies among them",
  "a missing or blank initial water-content ratio becomes 0.2; missing optional Sorption, Suction, Redistribution, Diffusion, or ThermalConductivity scalars inside the declared prefix become zero and supplied fields beyond the count are ignored after validation; an omitted optional active thermal conductivity may therefore become zero although a supplied zero is invalid, and no nonsemantic outer instance key owns target identity or duplicate detection",
  "missing or blank external/internal VaporCoefficients flags become No and missing or blank values become zero; source No leaves an earlier same-side Yes write unchanged, while Rust rejects every repeated surface target until declaration-order and per-side last-Yes accumulation are recovered",
]
routine.get_heat_bal_hamt_input.unsupported_active_branches = [
  "every valid Settings, Sorption, Suction, Redistribution, Diffusion, or ThermalConductivity attachment is typed but run-blocking, including one attached only to an unused material",
  "every valid SurfaceProperties:VaporCoefficients definition is typed but run-blocking, including both-No, zero-valued, non-HAMT, and otherwise runtime-inert records",
  "inclusive porosity and relative-humidity endpoints, zero or above-porosity Sorption moisture, unordered or duplicate relative humidity, source-adjusted descending Sorption moisture, unordered, duplicate, or descending Suction and Redistribution moisture/coefficient points, unordered or duplicate Diffusion relative humidity with arbitrary nonnegative resistance-factor ordering, and unordered, duplicate, or descending ThermalConductivity moisture/value points including source-zero-filled optional values remain typed state but never reach HAMT initialization, interpolation, or numerical state",
  "internally generated F/C-factor concrete targets and source effective later-replacement behavior for repeated case-insensitive Settings, Sorption, Suction, Redistribution, Diffusion, or ThermalConductivity material targets remain outside the executable boundary",
]
routine.get_heat_bal_hamt_input.not_claimed_branches = [
  "exact EnergyPlus diagnostic severity/text/order/multiplicity including Sorption correction-warning parity, lazy algorithm/use gating, the aggregate unused-HAMT warning counted in `SurfaceGeometry`, R-only warning-and-ignore parity, source duplicate replacement order, VaporCoefficients per-side last-Yes accumulation and declaration order, native-epJSON canonical Yes/No casing parity versus Rust's project-wide case-insensitive parser, known Sorption IDF fractional-count rounding and exact Suction, Redistribution, Diffusion, or ThermalConductivity IDF fractional-count behavior, source retention of nonfinite Settings or Sorption scalars and their observed severe/crash paths, exact source handling of nonfinite Suction, Redistribution, Diffusion, ThermalConductivity, or VaporCoefficients scalars or missing Sorption state, material-subclass and cross-attachment pointer lifetime behavior, legacy non-BuildingSurface `SurfaceNames` targets, active-construction absent-table base-conductivity fallback and its warning/error branches, construction and cell initialization, boundary vtc derivation/replacement and `HMassConvInFD` mutation, sorption plus rain-selected suction, ordinary redistribution, diffusion, thermal-conductivity interpolation and heat/moisture runtime behavior, zero-vtc resistance behavior, surface and cell output variables, EIO or SQLite serialization, runtime numerical behavior, and conformance",
]
